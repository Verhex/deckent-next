{
 "generatedAt": "2026-09-15T21:36:56.563Z",
 "sourceHead": "509fffa64ed788218fba49878e93f5d0dbd88a18",
 "files": 3224,
 "titles": 40828,
 "areas": {
  "agent": {
   "agent/anthropic-adapter.test.ts": [
    "streams text deltas + usage + done",
    "accumulates a tool_use block from input_json_delta fragments",
    "throws on non-ok status",
    "synthesizes an index-scoped id when the tool_use block omits an id",
    "throws when the stream carries a protocol error event"
   ],
   "agent/anthropic-err-body.test.ts": [
    "includes the response body text in the thrown error on a non-ok status",
    "still throws a status-only error when the error response has no body",
    "does not affect the success path — text deltas + usage + done still stream through"
   ],
   "agent/anthropic-parallel-toolresult.test.ts": [
    "merges 2 consecutive sibling tool results into ONE user message",
    "merges 3+ sibling tool results into one message, preserving order",
    "single-tool path is unchanged: one tool result -> one user message, one-element content array",
    "does not merge across a non-tool message: two separate rounds stay two separate user messages"
   ],
   "agent/approval-qol.test.ts": [
    "a live ApprovalAllowScopeStore grant short-circuits guard() to allow, without waiting on the broker",
    "without a live grant, the same short timeout falls through to the deny fallback (no false-positive allow)",
    "exposes the constructed allowStore on the handle for callers to grant into",
    "the 2nd identical (scopeId, scope, cmd) guard() call after a real deny never reaches the broker again",
    "a different cmd under the same scopeId is NOT cached — it still reaches the broker",
    "dispose() clears the deny-cache — a repeat command reaches the broker again afterward",
    "end-to-end: guardRiskyWorkerAction (unmodified, src/agents/worker.ts) surfaces the deny-cache guidance inside the [approval-denied] tool-result text",
    "never caches an action with no discernible cmd (conservative — avoids empty-key collisions)",
    "refreshes the task .hb file on HEARTBEAT_WRITE_INTERVAL_MS cadence while guard() is pending, and stops once it settles",
    "is a no-op (no crash) when the .hb file does not exist yet — fail-soft",
    "createWorkerApprovalGate still returns a genuine WorkerApprovalGate-shaped handle backed by a real broker",
    "setupWorkerApprovalGateFromEnv stays disabled (byte-identical) when the gate env flag is off"
   ],
   "agent/bash-perm-resource.test.ts": [
    "resolves the permission resource from the real `cmd` schema key, not `command`",
    "does not fall back to an empty resource when only the stale `command` key is present",
    "still resolves `path`/`file_path` args first, ahead of `cmd` (fallback order preserved)"
   ],
   "agent/checkpoint-continuity.test.ts": [
    "writes a v2 payload with the trail, digest refs (never a path) and a host-stamped createdAt when the model summary is missing",
    "opens the next epoch on the rendered trail with injected labels and the content-ref tool name",
    "records a spilled result with its full-content digest so the whole file stays reachable by digest",
    "overwrites the model createdAt, keeps the summary fields and installs the host trail",
    "a malformed model summary (non-string array) falls back to the deterministic checkpoint, never a half-valid write",
    "serves a post-checkpoint byte-identical read-only call from the trail and never re-executes it",
    "is armed only by a checkpoint: identical calls BEFORE any checkpoint still execute",
    "a side-effecting call after the checkpoint invalidates replay — a later identical read runs for real",
    "a different call after the checkpoint executes normally (the guard matches bytes, not tools)",
    "B2: a byte-identical deckent_call_tool(write) after the checkpoint is EXECUTED, never served — silent tier is not purity",
    "B2: a write routed through deckent_call_tool invalidates earlier read replays (no stale read-after-write)",
    "B2: an ok:false record (denied / error) is never replayed — the identical call runs again after the checkpoint",
    "the trail is per turn: a new user turn starts empty and disarmed",
    "a 64-entry cadence checkpoint on a 32k window renders a bounded opening (newest entries + omission line) and never trips the high-water",
    "a pressure request with nothing new since the epoch is suppressed (typed notice) instead of re-checkpointing the same opening"
   ],
   "agent/checkpoint-trail.test.ts": [
    "sorts keys at every nesting level and ignores undefined members",
    "digests are order-independent yet distinguish nested differences (a top-level-only replacer would not)",
    "B1: the digest separator is a source-level escape — the module is a text file with no raw NUL byte",
    "records completed calls with digest refs, bounds retention to the newest N and persists the rendered result",
    "downgrades ok from protocol markers, parses the full-content digest, and survives a failing store honestly",
    "invalidateReplay keeps history but drops served outputs; discard drops a proposal; reset clears all",
    "bound resolution clamps nonsense into a sane non-zero range and the config default is 64",
    "is empty for an empty trail and deterministic otherwise, with labels injected and the tool name substituted",
    "B3: keeps the NEWEST entries inside the token budget and names the omitted count + full-trail digest",
    "B3: the opening budget is window-derived, capped at half the transcript reserve, with a fixed fallback",
    "B3: the compaction text is the summary projection — no toolTrail / lastAssistantText JSON, counts + ref instead",
    "bounds the last assistant text with an honest digest marker",
    "retains host-verified reference evidence even when the model omits it",
    "host-stamps createdAt, installs the host trail over any model-authored one and merges counters (host wins)",
    "falls back to the host objective on a blank one and rejects any malformed or missing summary field (fail-closed)",
    "accepts v1, requires a well-formed trail for v2 and rejects half-migrated shapes",
    "round-trips a v2 payload through the real scratch store and still reads a v1 lineage"
   ],
   "agent/content-ref-tool.test.ts": [
    "reads bytes it wrote by digest with offset/limit, refuses unknown digests, malformed refs and bad ranges",
    "detects a tampered file (digest mismatch) and refuses everything after close",
    "is silent/core/builtin with a low-risk file-read classification that echoes the loop resource",
    "validates args: digest only, byte caps at the broker preview ceiling, default slice = preview budget",
    "reads a known digest with a meta line and returns typed [mcp-error] refusals otherwise",
    "registers the tool over a real session store and reads a spilled deckent_read_file result by digest",
    "B2: replayability is an explicit allow-list — pure reads only; call_tool, CLI-bridge, run-flow and MCP tools never",
    "stays absent over a write-only content writer or no store at all (honest absence, never a failing handler)",
    "%s exists in EN and TR and differs between them",
    "the replay notice code localizes through the bridge signal path",
    "content-ref refusal codes localize through the bridge signal path (they ride the tool-result `code`)",
    "the read hint carries the {tool} placeholder in both languages",
    "limit %i: following nextOffset reproduces the source bytes exactly and never splits a character",
    "offset at every byte: a start inside a sequence snaps forward to the next boundary and says so; boundaries are byte-exact",
    "default cap boundary: a multibyte character straddling the 16384-byte limit is deferred whole to the next slice",
    "the reviewer case: tool reads \"A😀BçC\" with limit 3 following nextOffset → \"A😀BçC\", never \"A��BçC\"",
    "binary / malformed bytes are never repaired or dropped: a random buffer round-trips exactly at any limit",
    "alignUtf8Slice unit contract: back-off, over-return for a too-small limit, EOF tail, malformed lead"
   ],
   "agent/context-admission.test.ts": [
    "denies the doomed request typed instead of shipping an oversized body",
    "requests ONE budget checkpoint BEFORE denying",
    "proceeds when the checkpoint consumer compacts the epoch",
    "never gates when no effective context is configured (pre-NT-02 behavior)",
    "admits a request that fits the effective context",
    "the loop sets outputCeilingTokens from the resolved native budget",
    "omits outputCeilingTokens when no native budget is resolved",
    "the OpenAI-compatible adapter sends it as max_tokens (and omits it when unset)",
    "rejects a present-but-invalid ceiling at the request contract",
    "resolves min(configured, server-reported) with typed provenance",
    "takes the NARROWEST configured knob (native_context_tokens may narrow)",
    "router-aware probe: bare /props silent (n_ctx=0), model-scoped /props?model= reports — server ceiling wins with NO config knob",
    "a /v1 endpoint probes the SERVER ROOT — /props, never /v1/props (owner incident 2026-08-18)",
    "local_llm.contextSize alone is configured authority for the per-turn resolver (owner incident 2026-08-18)",
    "stays honestly config-only when the server does not report",
    "lets explicit config narrow but NEVER widen above a known server ceiling",
    "uses the model-advertised FULL window; config only narrows (owner directive 2026-08-18)"
   ],
   "agent/context-budget-pairing.test.ts": [
    "never leaves an orphan tool_result when the budget cut lands exactly on the tool-result boundary",
    "preserves an in-flight multi-round tool chain (no interleaving user message) even over budget",
    "holds the no-orphan invariant across every possible budget cut point (two resolved turns)",
    "does not regress normal (non-pairing) compaction when the window already fits",
    "never leaves an orphan tool_result when the input has NO leading user message at all"
   ],
   "agent/context-budget.test.ts": [
    "estimates ~chars/4, rounded up",
    "counts tool-call names + serialized args plus the message envelope",
    "returns the input unchanged when it already fits",
    "returns the input unchanged for a non-positive budget (fitting disabled)",
    "drops the oldest messages first and keeps the newest window",
    "never opens the window on an orphan tool result — advances to a user turn",
    "always keeps the final message even when it alone exceeds the budget",
    "handles an empty transcript",
    "keeps the digest and conservative upper bound identical with absent, live and aborted signals",
    "still measures changes to actual message, tool schema and output-ceiling fields",
    "reuses the measured wire request across cancellation handles without altering the capability input"
   ],
   "agent/context-continuity-session.test.ts": [
    "takes an epoch before the single overflow retry; repeated refusal=%s",
    "persists old tool bodies before deterministic micro-compaction and then summarizes",
    "persists byte-exact overlapping JSON fragments only after terminal stop and accounts for both segments",
    "continues hidden-only length output with a complete visible JSON response",
    "does not retry malformed JSON from a normal terminal stop",
    "exhausts at three length-terminated segments without committing an epoch",
    "cancels during continuation before a third request and retains observed usage",
    "stops before the next segment when checkpoint usage exhausts the native cumulative budget",
    "persists the complete transcript before fallback and records the summary failure explicitly",
    "keeps the old epoch when fallback persistence has %s",
    "shrinks retained large tool bodies to durable paired references even when the model summary succeeds",
    "persists the full transcript after one hidden-only length response and retains its reported usage",
    "retains all 24 paired small references and does not execute the batch a second time"
   ],
   "agent/context-epoch-lineage.test.ts": [
    "proof 1 — an overflowing transcript is checkpointed from bounded deltas, never the whole transcript",
    "proof 2 — a 26-char intent with a 99,327-char expansion compacts to raw intent + lineage",
    "proof 3 — /renew keeps every cumulative counter and refreshes the context epoch safely"
   ],
   "agent/context-lifecycle-battery.test.ts": [
    "uses the exact counter for the incident-shaped 222K request and never dispatches it",
    "preserves wrapper overflow raw code, emits its decision first, and never dispatches or retries",
    "does not restore stale request attribution when measurement resolves after an identity clear",
    "derives a safe ceiling far greater than the 4,096 protected minimum",
    "reaches BOTH transports as that ceiling, not as 4,096",
    "regression: the Anthropic transport no longer caps at 4,096 when the request carries a ceiling",
    "does not relabel unrelated preparation faults as preamble budget exhaustion",
    "recovers reasoning-only length into one visible answer without exposing reasoning",
    "stitches overlapping visible segments exactly once",
    "never executes a tool call emitted by a length-cut segment",
    "proof — bounded delta planner + live proactive high-water checkpoint",
    "relays checkpoint measurement live while partial-failure usage remains one aggregate",
    "keeps a typed checkpoint denial causal but non-terminal and closes an abandoned relay",
    "accrues observed usage once when a multi-chunk checkpoint relay is closed early",
    "proof — three carriers separated, live context compacts, attachment never re-enters",
    "proof — renewal does not rewrite history, next send checkpoints normally",
    "labels a missing exact capability only as a proven conservative upper bound",
    "bounds a stalled exact counter, caches the conservative result, and never relabels it exact",
    "prefers the per-request computed ceiling over the operator-pinned one",
    "falls back to the operator-pinned ceiling when the request carries none",
    "is unresolved — never a constant — when no authority exists",
    "fails closed on a present-but-invalid authority instead of degrading a tier",
    "wires an identical ceiling on both transports — ${c.name}",
    "neither transport invents a ceiling when none was resolved",
    "${file} carries no hardcoded generation ceiling",
    "classifyContextLifecycleEvent resolves every real wire event to its class, pure",
    "renders pairwise-distinct real ${lang} messages (no class shows another class's message)",
    "en and tr texts differ for every class, and only INPUT_CONTEXT_OVERFLOW claims the context window is full",
    "OUTPUT_CEILING_REACHED end-to-end: a length-cut segment with visible text renders the ceiling-reached notice and recovers the full answer",
    "EMPTY_VISIBLE_CONTENT_WITH_REASONING end-to-end: hidden reasoning with no visible text renders the reasoning-recovery notice, distinct from a plain empty response",
    "CONTINUATION_EXHAUSTED end-to-end: repeated length-cuts past the continuation cap render the exhaustion notice, not an ongoing \"ceiling reached\" message",
    "an impossible five-token preamble produces a distinct localized hold before transcript recovery",
    "INPUT_CONTEXT_OVERFLOW + MEASURED_CONTEXT_PRESSURE end-to-end: overflowed context renders measured-pressure checkpoint and terminal overflow notice"
   ],
   "agent/context-snapshot-compact.test.ts": [
    "reports window, measured input tokens, epoch, transcript shape and checkpoint state",
    "is honest without a context authority: window and measurement are undefined, nothing is guessed",
    "planContextRefresh() marks the next turn without renewing the working budget",
    "accepts one complete outer Markdown JSON fence and preserves payload validation",
    "classifies malformed provider JSON as operational degradation while preserving observed usage",
    "without a scratch store it yields the typed unavailable notice and keeps the epoch",
    "with a scratch store it takes a checkpoint epoch immediately: usage + saved notice, epoch advances, preamble set",
    "cancel() during an explicit compaction aborts the checkpoint call; the epoch is kept and reported degraded"
   ],
   "agent/e2e-multi-tool-turn.test.ts": [
    "runs two tool calls from one turn in order, then concludes"
   ],
   "agent/events.test.ts": [
    "isTerminalEvent is true for turn-end and error",
    "isTerminalEvent is false for streaming/intermediate events"
   ],
   "agent/guard-cost.test.ts": [
    "accrues tokens across turns and reports spentUsd",
    "does not trip when no ceiling is set (advisory-only default)",
    "trips with the COST_GATE_EXCEEDED reason once a hard ceiling is crossed"
   ],
   "agent/guard-recursion.test.ts": [
    "is false at and below the cap, true above it",
    "defaults to DEFAULT_MAX_ITERATIONS when no max is given"
   ],
   "agent/guard-self-modifying.test.ts": [
    "elevates when a write targets deckent source inside the deckent repo",
    "does not elevate for non-source writes inside the deckent repo",
    "does not elevate in a non-deckent project (user editing their own src is normal)",
    "does not elevate when there are no write targets"
   ],
   "agent/identity-narration.test.ts": [
    "cites the config-resolved thresholds (5 / 12 / 90 s) in EN and TR",
    "stays tight: the added preamble is a few hundred bytes per language (measured, reported in proof)",
    "without a narration policy the prompt is byte-identical to the pre-7114 composition",
    "places the contract right after the immutable core and before the scratchpad mechanism (EN + TR)",
    "a soul file cannot remove or transform the contract (non-overridable core block)"
   ],
   "agent/identity.test.ts": [
    "always includes the immutable safety core",
    "uses the default soul when no .deckent/soul.md exists",
    "uses a custom .deckent/soul.md when present",
    "appends DECKENT.md project knowledge when present",
    "wraps workspace identity as digest-bound context-only data",
    "omits the scratchpad mechanism section when no scratchDir is supplied",
    "injects the scratchpad path and its volatility contract when scratchDir is supplied",
    "never lets a soul file remove the immutable core"
   ],
   "agent/interim-deliverable.test.ts": [
    "defaults are the owner-stated bounds: 12 tool calls / 90 s / 200 chars",
    "fires at exactly 12 tool calls, not at 11",
    "fires at 90 s of wall clock with few calls, not at 89.999 s",
    "7114-b REVISE: a long reply that still calls a tool is narration EVEN while a request is pending",
    "7114-b: only a real ANSWER is a deliverable — narration alongside tool calls never is",
    "while a request is pending, the host re-asks only after a full window since the request",
    "rejects a non-positive or non-integer policy loudly",
    "keeps a trailing host instruction after the epoch rebuild, never a trailing USER-origin message",
    "after 12 silent tool calls (3 rounds × 4) the loop injects the interim host turn, the model answers live, counters reset",
    "does NOT fire at 11 tool calls",
    "fires on the 90 s wall-clock bound with only two tool calls (fake clock), not at 89 s",
    "a second text-only answer after the continue nudge ends the turn (bounded, no loop)",
    "7114-b: narration between batches no longer counts as delivery, so the bound is actually reached",
    "legacy callers without a tracker are byte-identical: no host turns, no events",
    "300-char narration across many tool rounds never resets the counters",
    "a host-requested answer resets the counters; the wall clock restarts from it",
    "the wall-clock bound is reported ONCE while a round is still in flight",
    "nudging is finite: after the per-turn ceiling the host stops asking",
    "the per-turn tool ceiling demands ONE honest final answer and closes the tool budget",
    "repeated failures against ONE exact target close that line; other work is never implicated",
    "a success on the failing target clears its streak",
    "the per-turn tool ceiling stops execution and forces the final answer",
    "three failures on one target close that line and the host asks for an honest answer",
    "a slow in-flight round reports the answer as overdue while it is still streaming",
    "ends the turn on the wall budget, keeps the partial text, and claims nothing"
   ],
   "agent/loop-budget.test.ts": [
    "returns the frozen defaults when nothing is authored",
    "merges every authored dimension independently over the defaults",
    "fails loudly on unknown keys and non-positive values",
    "terminates each dimension with its own typed code",
    "no-progress first requests ONE checkpoint, then terminates if still stuck",
    "checkpoint cadence fires once per threshold crossing (rounds and toolCalls)",
    "counters are session-cumulative — nothing about the state resets on an epoch change",
    "legacy unwired guard is byte-identical (25 rounds default)",
    "token-pressure at 80% requests ONE checkpoint before the hard stop",
    "fresh-token base field starts at zero (delta accounting seam)",
    "resolves window and tool shares from owner config",
    "rejects invalid native ratio %s",
    "rejects contradictory single, retained and highwater allocations"
   ],
   "agent/loop-cancel.test.ts": [
    "interrupts an in-flight stream: a tool-call event arriving after cancel() is never proposed or committed",
    "7114: an AbortError thrown mid-stream retains the streamed narration, text-only",
    "7114: an interrupt with nothing streamed commits no empty assistant message",
    "the next turn after a mid-stream cancel is clean (no leftover state breaks the following provider call)",
    "mid-batch cancel: an unexecuted trailing tool call gets a synthetic cancelled result, preserving tool_use/tool_result pairing",
    "a clean (non-cancelled) multi-tool-call turn is unaffected — regression guard for existing behavior"
   ],
   "agent/loop-exposure-wire.test.ts": [
    "progressive ON: round 1 carries only core+meta, round 2 carries the revealed tool",
    "progressive OFF (getter present, flag false): all 5 tools + meta on BOTH rounds",
    "regression pin: no getter → byte-identical requests and events vs. the pre-NT-06 loop",
    "the loop holds NO exposure state: a view nothing reveals into stays flat"
   ],
   "agent/loop-honest-signals.test.ts": [
    "surfaces an empty provider response as an empty-response error, not a silent turn",
    "does NOT flag a normal text turn as empty",
    "recovers a 'length' stop via bounded continuation (typed OUTPUT_LIMIT, no passive truncated notice)",
    "compacts an over-budget transcript and reports it via a 'context-compacted' notice",
    "does not compact (or notice) when no budget is configured",
    "reads the adapter and model through the getters on every provider call",
    "falls back to the fixed adapter/model when no getters are supplied"
   ],
   "agent/loop.test.ts": [
    "streams text then ends the turn when the model returns no tool calls",
    "runs a silent tool call (tool-proposed→executing→result), feeds it back, then ends",
    "asks for permission on a confirm-tier tool and aborts the call on deny",
    "emits an error + turn-end when the adapter throws",
    "stops with an error when the recursion cap is exceeded (tool never satisfies the model)",
    "never persists a grant for a self-modifying-elevated call (re-asks every time)",
    "passes active deny rules to decide() so a matching deny blocks an otherwise-silent tool",
    "aborts the turn with a COST_GATE_EXCEEDED error when the hard cost ceiling is crossed mid-turn",
    "does not abort on usage when the cost guard is advisory-only (no ceiling configured)",
    "does not append an empty assistant turn when the stream yields no text and no tool calls",
    "uses the selected adapter measurement before dropping old context",
    "requests a highwater checkpoint immediately after a measured tool round",
    "bounds aggregate tool bytes and renews allocation after a context epoch",
    "shares the remaining allocation across all results and preserves every content reference",
    "shrinks old retained results before owning new calls, then emits every paired result within budget",
    "does not re-invoke the handler when interceptToolCall serves replay (no duplicate effect)",
    "withholds wire body under zero allocation while preserving executedOk and spill"
   ],
   "agent/message-roundtrip.test.ts": [
    "OpenAI: assistant message carries tool_calls; tool message carries tool_call_id",
    "Anthropic: assistant message is text+tool_use blocks; tool result is a user tool_result block",
    "Anthropic: an assistant turn with tool_use but no text omits the text block"
   ],
   "agent/native-permission-binding.test.ts": [
    "constructs a frozen strict binding without retaining raw arguments",
    "hashes nested JSON canonically and domain-separates changed values",
    "rejects cycles, accessors and sparse arrays without invoking accessors",
    "permits longer lifetimes only for ordinary confirm-tier calls",
    "uses the existing tool-wide grant pattern for session and always",
    "strictly rejects unknown, malformed and forged bindings",
    "compares every invocation and intent field exactly"
   ],
   "agent/native-permission-session.test.ts": [
    "registers before yield, accepts one exact early response, and rejects duplicates",
    "rejects unknown and modified invocations without caching an unissued answer",
    "returns typed boundary outcomes for malformed JSON-shaped request and response values",
    "retires accepted and pending authority on turn replacement despite reused provider call ids",
    "rehashes session-owned args before response and performs no grant or handler on mutation",
    "rechecks live deny policy before grant and keeps the transcript paired",
    "rechecks args after tool-executing yield and consumes no effect on mutation",
    "cancels after tool-executing without running the handler and preserves pairing",
    "maps an unconfirmed terminal restoration hold to a structured zero-effect result",
    "keeps cancellation authoritative when terminal restoration HOLD arrives late",
    "derives nested identity from the active parent and consumes one exact nested effect",
    "uses the logical scratch session id but a fresh process identity per session",
    "preserves exact whitespace and empty producer resources without normalizing identity",
    "holds an ask-tier tool with unavailable producer classification before any prompt or effect"
   ],
   "agent/native-read-approval-registry.test.ts": [
    "matches list_dir default path and grep/glob pattern fallbacks",
    "auto-allows silent deckent_read_file without a permission prompt",
    "auto-allows deckent_list_dir with empty args (cwd default resource \".\")",
    "prompts on tierMap confirm for deckent_read_file and runs after grant",
    "prompts on tierMap confirm for deckent_list_dir {} with resource \".\"",
    "asks on alwaysFloor even when the tool tier is silent, and deny skips execution",
    "fail-closes invalid deckent_read_file args on confirm tier (no handler, no prompt)",
    "prompts on confirm tier for numeric list_dir path with resource \"42\" (not cwd \".\")",
    "fail-closes list_dir file_path alias on confirm tier (dispatcher uses path only)",
    "prompts on confirm tier for whitespace list_dir path without trimming resource",
    "prompts on confirm tier for pattern-only deckent_grep and runs after grant",
    "alwaysFloor on deckent_glob denies without executing",
    "denies deckent_grep when explicit path \".\" matches a deny rule (no handler run)",
    "runs deckent_grep and deckent_glob silently when args classify",
    "nested parity denies deckent_grep when explicit path \".\" matches deny glob",
    "fail-closes file_path alias on nested list_dir (same as direct loop)",
    "nested confirm deckent_grep pattern-only uses pattern as permission resource"
   ],
   "agent/native-tool-approval.test.ts": [
    "classifies actual builtin arguments",
    "holds missing, external, and malformed classifications",
    "classifies silent read surfaces with file-read scope aligned to primaryResource",
    "fail-closes read classifiers on invalid args or empty resource (no broad scope)",
    "coerces list_dir path like chat-tool-exec String(args.path ?? \".\")",
    "grep explicit path \".\" stays resource \".\" so deny(.) holds (not pattern fallback)",
    "preserves whitespace in list_dir path resource identity (no trim)",
    "classifies a proven read-only deckent_bash command as file-read when the project root is supplied",
    "keeps the context-free classifier on the shell-exec floor",
    "follows the live cwd per call and the host dialect"
   ],
   "agent/ollama-adapter.test.ts": [
    "has name \"ollama\" and streams via the OpenAI-compatible path at the ollama host"
   ],
   "agent/openai-adapter.test.ts": [
    "streams text deltas then usage then done",
    "accumulates a streamed tool-call across delta fragments",
    "throws on a non-ok HTTP status",
    "flushes accumulated tool-calls when the stream finishes with finish_reason:stop (R6 silent-drop)",
    "flushes accumulated tool-calls when the stream ends with no finish_reason at all",
    "does not double-emit when finish_reason:tool_calls already flushed the accumulator",
    "synthesizes unique ids for same-named parallel tool calls with omitted ids",
    "surfaces the SAFE upstream error body instead of an opaque status line",
    "wraps connection failures as an honest connect error (cold-start class)"
   ],
   "agent/openai-reasoning-wire.test.ts": [
    "enable_thinking descriptor: off → chat_template_kwargs.enable_thinking=false, on → true (existing kwargs preserved)",
    "reasoning_effort descriptor: values come from the descriptor data, never from code",
    "none / unknown descriptor or no directive: nothing is invented on the wire",
    "structured request (reasoning off) wires enable_thinking:false for a template-toggle descriptor",
    "thinking on: enable_thinking:true and max_tokens is the caller\\'s INCLUSIVE ceiling (visible + reasoning budget)",
    "reasoning_effort descriptor maps off/on to the descriptor\\'s own enum values",
    "no descriptor / unknown descriptor: the directive is an honest no-op on the wire",
    "exposes the descriptor capability on the adapter so the loop reads the SAME authority",
    "a throwing descriptor resolver degrades to no-op mapping, never a failed request"
   ],
   "agent/openai-toolcall-id.test.ts": [
    "disambiguates when the backend echoes the same explicit id for two parallel calls",
    "disambiguates three-way id collisions with incrementing suffixes",
    "a disambiguated id correlates the correct tool_result on the round-trip (toOpenAIMessage)",
    "regression: distinct explicit ids and omitted-id synthesis are unaffected by dedup"
   ],
   "agent/output-ceiling-parity.test.ts": [
    "prefers the per-request computed ceiling over the operator-pinned one",
    "falls back to the operator-pinned ceiling when the request carries none",
    "is unresolved — never a constant — when no authority exists",
    "fails closed on a present-but-invalid authority instead of degrading a tier",
    "derives a safe ceiling far greater than the 4,096 protected minimum",
    "reaches BOTH transports as that ceiling, not as 4,096",
    "regression: the Anthropic transport no longer caps at 4,096 when the request carries a ceiling",
    "wires an identical ceiling on both transports — ${c.name}",
    "neither transport invents a ceiling when none was resolved",
    "${file} carries no hardcoded generation ceiling"
   ],
   "agent/permission-decide.test.ts": [
    "silent tier auto-allows with no rule",
    "confirm tier asks with no rule in suggest mode",
    "an allow-rule auto-allows a confirm-tier tool",
    "a deny-rule overrides an allow-rule",
    "always-floor tool asks even with an allow-rule",
    "full-auto auto-allows confirm tier BUT never overrides the floor",
    "auto-edit auto-allows edits, still asks the shell tool (bash AND deckent_bash)",
    "always-floor tool asks even when tier is silent"
   ],
   "agent/permission-grants.test.ts": [
    "uses tool-wide coverage for a %s grant",
    "pins deny → floor → silent → grant → mode precedence",
    "never lets a tool-wide grant cover an always-tier call",
    "is an intermediate AgentEvent with the complete audit shape"
   ],
   "agent/permission-match.test.ts": [
    "matches exact tool + ** pattern (any resource)",
    "respects tool name mismatch",
    "matches a directory glob src/**",
    "matches single-segment * (no slash)",
    "matches a bash command prefix pattern"
   ],
   "agent/permission-policy.test.ts": [
    "returns the safe default when no policy file exists",
    "merges an enterprise-locked override (mode + extra floor)",
    "merges a solo-YOLO override (full-auto) but keeps the safe floor",
    "falls back to safe default on malformed JSON (fail-safe)",
    "drops tierMap entries whose tier value is not silent/confirm/always (rejects bad tiers)",
    "cannot shrink the floor below baseline when alwaysFloor:[] is supplied"
   ],
   "agent/permission-resolve-tier.test.ts": [
    "falls back to the tool default tier when policy has no override",
    "a name override wins over the tool default",
    "a category override applies when no name override exists",
    "a name override beats a category override"
   ],
   "agent/permission-store-merge.test.ts": [
    "preserves an unrelated top-level custom key across a grant",
    "preserves an unrelated permissions.* sub-key across a grant",
    "preserves unrelated keys across a revoke too",
    "backs up a malformed settings.local.json instead of silently discarding it, and warns",
    "malformed file grant still succeeds (best-effort) even if a backup somehow cannot be written"
   ],
   "agent/permission-store.test.ts": [
    "grant \"once\" does not persist and is not remembered",
    "grant \"session\" remembers in memory but does not persist",
    "grant \"always\" persists to settings.local.json AND is active",
    "does not activate a new \"always\" grant when persistence fails",
    "migrates legacy permissions.allow[toolName] → rule tool(**) on load",
    "revoke removes a session rule",
    "revoke of an \"always\" rule updates the persisted file",
    "activeDenies returns persisted permissions.deny rules (kept separate from allow rules)",
    "activeDenies is empty when no deny rules are configured",
    "persist preserves unrelated top-level keys AND the legacy allow key (native-flag coexistence)"
   ],
   "agent/preamble-budget-registry-matrix.test.ts": [
    "prices the real registry and repository across the supported context matrix"
   ],
   "agent/preamble-budget.test.ts": [
    "reduces references then schemas using measured share, retaining immutable safety",
    "preserves full original composition on a sufficiently large window",
    "reveals discovered schemas idempotently and retains access through meta dispatch",
    "does not discard references on failed persistence or forged digest",
    "uses the adapter measurement authority instead of enforcing byte heuristics",
    "validates configured preamble shares",
    "wires session discovery to next request and reports preamble alongside completion",
    "reduces identity only after references and schemas cannot fit",
    "keeps capabilities when discovery is unavailable instead of silently dropping schemas",
    "shares successful search reveals with the bridge NT-06 exposure object",
    "localizes durable reference stubs without losing their authority or digest",
    "admits an irreducible floor over the target only inside all three reserves"
   ],
   "agent/provider-detect-i18n.test.ts": [
    "names the winning signal with a typed code and a technical detail",
    "the mechanism reason is technical English — never a Turkish sentence",
    "every reason code has a catalog sentence in en and tr"
   ],
   "agent/provider-detect.test.ts": [
    "detects anthropic-api from ANTHROPIC_API_KEY",
    "detects openai-compatible from OPENAI_API_KEY",
    "detects openai-compatible from a config base_url even without env key",
    "detects ollama from config ollama_host",
    "returns none with an honest reason when nothing is configured",
    "prefers anthropic-api over ollama when both are present",
    "prefers anthropic-api over openai-compatible when both keys present",
    "prefers openai-compatible over ollama when both are configured"
   ],
   "agent/provider-types.test.ts": [
    "returns null for a well-formed request",
    "rejects empty model",
    "rejects a message with an unknown role",
    "rejects non-array tools",
    "a mock adapter yields normalized ProviderEvents"
   ],
   "agent/qwen-incident-regression.test.ts": [
    "the incident-shaped turn completes with every provider request bounded",
    "the raw pre-fix variant terminates typed instead of shipping the oversized request"
   ],
   "agent/readonly-bash-silent-tier.test.ts": [
    "runs %s without a permission-request and records an auto-decision",
    "a destructive command keeps the always floor even though reads are silent",
    "a mixed round asks exactly once — only for the non-read call"
   ],
   "agent/reasoning-boundaries.test.ts": [
    "never retries a nested AbortError even without a signal on the request",
    "does not treat a permanent TLS identity failure as transient",
    "attaches a bounded cancellation signal when a props caller omits one",
    "rechecks the preamble hard reserve before raising the reasoning retry ceiling"
   ],
   "agent/reasoning-continuation.test.ts": [
    "recovers reasoning-only length into one visible answer without exposing reasoning",
    "stitches overlapping visible segments exactly once",
    "never executes a tool call emitted by a length-cut segment",
    "holds after the bounded continuation limit"
   ],
   "agent/reasoning-control-plan.test.ts": [
    "no policy (legacy caller): no directive, ceiling = visible reserve, byte-identical arithmetic",
    "auto + descriptor proves budget sharing: thinking on, reasoning budget added ON TOP of the visible reserve",
    "auto + unknown descriptor: thinking directive still on, but the ceiling is NEVER inflated without evidence",
    "structured request: thinking OFF regardless of mode, ceiling = visible reserve only",
    "mode off: directive off for ordinary turns too",
    "no policy → none (legacy continuation path stays untouched)",
    "auto + toggleable (enable_thinking or reasoning_effort) → retry with reasoning off",
    "auto + not toggleable → retry with the ceiling raised to visible + exhaustedRetryBudgetTokens",
    "forced on → never switches thinking off; raises the ceiling instead",
    "backend ignored an explicit off (mode off, reasoning still observed) → raise the ceiling",
    "a raise that would not exceed the exhausted ceiling is refused as none (no pointless retry)",
    "reads the adapter capability (sync or async) and treats any failure as no evidence",
    "projects chars to a conservative token count for display only"
   ],
   "agent/reasoning-exhaustion.test.ts": [
    "descriptor proves budget sharing → outputCeilingTokens = visible reserve + reasoning budget, directive on",
    "unknown descriptor → ceiling stays the visible reserve (never inflated without evidence)",
    "mode off → directive off on ordinary turns",
    "legacy caller (no native budget): no directive, no ceiling, no transportRetry — byte-identical request",
    "surfaces counts (never text) with a running cumulative total",
    "auto + toggleable: notice + retry with reasoning off, same messages, same ceiling; the answer is recovered",
    "not toggleable: retry with a raised, config-bounded ceiling (visible + exhaustedRetryBudgetTokens)",
    "forced on: thinking is never switched off — the ceiling is raised instead",
    "the retry also exhausts → typed error native.reasoning_exhausted, hold, turn-end — exactly two requests, never a third",
    "a raised ceiling that no longer fits the context is refused typed instead of shipped",
    "legacy caller (no native budget) keeps the classic continuation path",
    "visible text that merely hit the ceiling is still the classic continuation, not a reasoning retry",
    "descriptor resolution failure is no evidence: the turn still runs and recovers via the raised ceiling"
   ],
   "agent/reasoning-fix-package-b.test.ts": [
    "passes a composed AbortSignal to EVERY fetch even when the caller omits one, and still yields evidence",
    "deadline fires → typed probe-timeout descriptor (unknown toggle/budget), fetch aborted",
    "caller abort (turn cancelled) → typed probe-aborted descriptor; the default deadline is the measurement bound",
    "timeout/abort outcomes are returned typed but NEVER cached — the next resolve probes again",
    "planning treats probe-timeout / probe-aborted exactly like unknown (no ceiling inflation, not toggleable)",
    "config: reasoningProbeTimeoutMs is a positive safe integer, merged over the default",
    "native-transport threads the turn signal and the config deadline into the local-llm probe",
    "Astra's scenario: window 32768, preamble 8000, ceiling 12288 → raised 20480 (hard limit 6963 < 8000) ⇒ typed hold, ONE request",
    "a preamble that still fits the raised hard limit is re-prepared and the retry ships with the raised ceiling",
    "a PreambleBudgetError from the raised-ceiling prepare is the typed hold; unrelated prepare faults still propagate",
    "the raise fallback: only auto + toggleable + thinking requested may switch reasoning off; forced on / off-ignored hold",
    "isAbortError: nested Error/DOMException AbortError and abort codes at any depth",
    "adapter: nested abort → one fetch, rethrown as the caller\\'s own abort (Astra boundary)",
    "%s is transient (connect phase only)",
    "%s is permanent: one fetch, typed error with noRetryReason=permanent",
    "a code-less `fetch failed` is unknown → permanent (no speculative retry)",
    "transportRetry=%i → fetch count and error vars/code follow the configuration",
    "error message names the configured count, never \"once\""
   ],
   "agent/reasoning-send-signal.test.ts": [
    "cold unknown descriptor retains turn abort through loop -> send -> second probe"
   ],
   "agent/reference-digest-foundation.test.ts": [
    "carries the table header as context across a 20KB row without double-counting coverage",
    "fails closed on mismatch, quota, cancelled input and gaps/overlaps",
    "preserves small-ref prompt bytes; descriptors and truncated inline both require digest",
    "binds journal identity to source/policy/descriptor AND tenant/session, independent of property order",
    "uses the real session store, preserves immutable bytes after source changes and rechecks read policy",
    "rejects out-of-root alias, canonical protected target, binary, stale digest and cancellation"
   ],
   "agent/reference-digest-progress.test.ts": [
    "publishes every real phase in order and never reports a section total it does not have",
    "counts only settled usage and verified requests, never a reserved ceiling",
    "observed bytes grow while the source is verified, and coverage grows only with verified nodes",
    "records the typed failure on the terminal projection",
    "a throwing view never fails the program",
    "publishes nothing when no view is listening"
   ],
   "agent/reference-digest-runner.test.ts": [
    "maps all UTF8/CRLF bytes, reduces with tools=[]/off, reserves answer, reopens journal without repeating calls or usage",
    "retries invalid structured output exactly once and accounts BOTH calls",
    "fails %s after one retry without fake coverage",
    "a confirmed length truncation subdivides instead of repeating the same request, and stays finite",
    "holds uncertain usage with a durable reservation and never repeats that request on resume",
    "settles identical duplicate usage only once and refuses conflicting usage",
    "reserves final answer before admitting calls and honors maxRequests/depth/fan-in",
    "uses conservative admission without a counter and declines a pair that cannot fit",
    "measures oversized sections before splitting on UTF8 boundaries and preserves partitions on resume",
    "aborts a noncooperative stream at the deadline and never silently retries",
    "honors pre-abort, descriptor absence, source mismatch and ledger refusal before sending",
    "does not claim stored coverage when content persistence fails",
    "detects journal tampering and refuses a foreign plan",
    "bounds concurrent sends by all three ceilings and preserves deterministic coverage with out-of-order completion",
    "an external sink crash after settlement can be replayed without double accounting or another provider call",
    "a second journal writer cannot dispatch a duplicate request",
    "refuses changed snapshot bytes before a provider call and reports explicit source failure",
    "retains completed coverage when a later call loses usage and resume never replays either node",
    "propagates corrupted node readback and maxDepth exhaustion honestly",
    "includes continuation context but never double-counts it",
    "rejects model-owned %s",
    "rejects ranges outside child union, blank values, unknown nested keys and excessive arrays"
   ],
   "agent/reference-first-answer.test.ts": [
    "records each phase separately, with an epoch stamp and a run identity",
    "keeps what an INTERRUPTED request really measured, and invents no total",
    "replays a legacy journal as UNKNOWN instead of filling zeros",
    "reports the consumer hand-off as its own phase, not as request time",
    "is due only while the turn has delivered nothing, and only within the finite allowance",
    "a failing first attempt neither silences the turn nor escapes the ceiling",
    "keeps coverage gapless, non-overlapping and complete",
    "produces the first validated node from fewer source bytes",
    "is declared, validated and defaulted through the existing policy chain",
    "refuses an unusable first-part size instead of silently ignoring it"
   ],
   "agent/reference-interim-delivery.test.ts": [
    "does not count an interim answer the consumer never received",
    "carries an unreconciled interim cost to the session as an OPEN hold"
   ],
   "agent/reference-interim.test.ts": [
    "answers from the FIRST verified node, before mapping is finished",
    "carries only validated evidence, no tools and no reasoning",
    "bills through the same ledger exactly once and keeps the request counted",
    "produces NOTHING when there is no verified node yet",
    "refuses honestly when the remaining budget cannot admit another call",
    "never counts an empty answer as a delivery, and still settles its usage",
    "leaves an unconfirmed reservation unknown: not settled, not zeroed, and never re-billed",
    "does not deliver after the turn was cancelled",
    "is inert when the host offers no interim capability",
    "counts the interim request on its OWN line, never as coverage work",
    "gives every typed skip reason a bilingual sentence",
    "records its OWN exhaustion as a deadline, not as the user cancelling",
    "still records a REAL user abort as cancelled",
    "refuses to deliver on $name",
    "rejects an oversize answer instead of quietly cutting it",
    "closes the seam after a breach instead of retrying it silently",
    "keeps a real confirmed spend even when the answer is refused",
    "carries an unresolved reservation to the RESULT, whatever happens later",
    "answers at the boundary after a failed map, using the node already in hand",
    "never runs beside another provider call and never interrupts one"
   ],
   "agent/reference-invalid-diagnostics.test.ts": [
    "names the exact contract each malformed response broke",
    "every declared reason is a distinct member of the typed set",
    "a schema-invalid response is recorded as payload-json-parse with sizes and digests, never text",
    "a stream that stops for another reason is recorded as stream-non-stop, not as a payload fault",
    "a stream that never completes is recorded as stream-missing-done",
    "an oversize stream keeps the FULL-stream digest apart from the retained slice",
    "a valid response keeps its record clean: no diagnostics on success",
    "a record written before this version reads back as unknown, never as a guess",
    "usage accounting and the bounded retry are untouched by the new record"
   ],
   "agent/reference-length-recovery.test.ts": [
    "splits the truncated part and succeeds on the smaller children (fail before, split success after)",
    "keeps accounting exactly once: the truncated attempt is reserved, settled and never lost",
    "an indivisible range ends honestly instead of splitting forever",
    "the request budget still terminates the program: no growth to fit the splits",
    "a restart replays the recorded split, not the original oversized part",
    "only a confirmed length truncation splits: a schema fault keeps the bounded retry",
    "progress counts every issued child request, not only the verified ones",
    "persists the split depth so a resume cannot halve past maxDepth",
    "derives an honest depth for a journal written before the field existed",
    "a crash between the invalid record and the partition update replays the split, not the same request"
   ],
   "agent/reference-pin-cancel.test.ts": [
    "an aborted pin publishes nothing and leaves no temporary behind",
    "an attempt aborted mid-flight still publishes nothing, and a later clean attempt does",
    "a pin that never returns no longer holds the turn: the orchestrator wait is bounded",
    "the turn abort also releases a hung pin wait",
    "source root, tenant and digest custody survive the fix: a foreign root still refuses"
   ],
   "agent/reference-progress-session.test.ts": [
    "delivers progress events while the digest program is still running",
    "publishes the retained digest cost and the source path on /context",
    "an abandoned turn stops the digest program instead of leaving it running",
    "emits no progress at all when large references are disabled"
   ],
   "agent/reference-recovery.test.ts": [
    "pins source and nodes, recreates store custody and makes source digest readable through the existing reader",
    "tampered and changed members fail without publishing partially restored refs",
    "native ledger restores the SAME counters and cost, settles IDs once across reopen, and keeps ordinary usage",
    "owner budget renewal rebinds the working counters while cumulative usage remains",
    "rejects expired pins and accounts restored captures against the live byte allowance"
   ],
   "agent/reference-session.test.ts": [
    "largeReference is defaultOFF, strictly validates keys/types/bounds, and is config-resolved",
    "keeps the small-reference payload byte-identical and never invokes the digest runner",
    "wires large references to measured child calls and returns only digests to the parent with exact total usage",
    "denies a reference under the normal read policy before any provider call",
    "reopens a fresh session/store over retained scratch and resumes completed digest nodes without repeating child calls",
    "asks once for a scoped source and honors revocation before capture",
    "config factory and hydrated bridge isolate child contexts while retaining parent history",
    "cancels a reference during measured admission without dispatching a child request",
    "persists ordinary round counters AFTER their usage event alongside digest counters",
    "a real session checkpoint retains host source and digest refs when summarization cannot produce JSON"
   ],
   "agent/reference-structured-output.test.ts": [
    "accepts only the dialects it can express and refuses anything else loudly",
    "treats absent evidence as unknown, and unknown/none/absent as \"cannot enforce\"",
    "prefers configured evidence over the catalog, and answers unknown without either",
    "writes response_format.json_schema with strict enforcement only under an enforcing descriptor",
    "sends the field on the real wire body and leaves an ordinary request untouched",
    "never drops a directive silently — a transport that cannot enforce throws",
    "keeps the measurement body a PROMPT projection: response_format never enters it",
    "holds BEFORE any provider call when nothing proves the server enforces the schema",
    "attaches the host-authored schema to every dispatched request without touching the prompt",
    "describes shape only — size ceilings stay with the host validator",
    "still rejects a payload the server \"enforced\" but the host cannot validate",
    "refuses a journal written under the prompt-only contract instead of replaying it as verified",
    "records the response contract IN the durable plan digest",
    "is cancellable while resolving the descriptor, without dispatching",
    "gives the new failure code a bilingual sentence that names the exact config key",
    "carries the owner-declared capability from config through the admission wrapper to the adapter",
    "declares no enforcement without owner evidence, and refuses an invalid declaration",
    "$label projects every key the native transport reads"
   ],
   "agent/request-measurement.test.ts": [
    "uses the exact counter for the incident-shaped 222K request and never dispatches it",
    "emits one immutable admitted decision before dispatch without recounting",
    "labels a missing exact capability only as a proven conservative upper bound",
    "bounds a stalled exact counter, caches the conservative result, and never relabels it exact"
   ],
   "agent/scratch-checkpoint.test.ts": [
    "rejects an immediate duplicate in one turnId and records origin",
    "compacts to checkpoint lineage without orphaning tool results",
    "writes durable checkpoints to the canonical session path without coupling them to scratch deletion",
    "reads a legacy scratch checkpoint when the canonical lineage is empty, without migrating it",
    "atomically writes mode-protected data and verifies its checksum",
    "returns typed corruption instead of throwing",
    "classifies malformed stored JSON without returning parser text or stored bytes",
    "classifies valid non-envelope JSON %s as fixed schema corruption",
    "retains only the latest five checkpoints",
    "rejects unsafe identity paths and symlink checkpoint targets",
    "rejects a pre-existing symlink inside the canonical checkpoint lineage",
    "rejects a symlink in the canonical ancestry before creating sessions",
    "returns typed degradation when a canonical parent is replaced by a symlink",
    "resolves the approved layout and reuses an existing root for recovery",
    "deletes the whole session root — scratchpad and any sibling content dir",
    "keeps a fresh sibling and sweeps one idle beyond the 10-minute window",
    "measures INACTIVITY — a checkpoint write re-stamps the live session",
    "sweeps strict-prefix legacy mkdtemp roots and nothing else",
    "fail-open: a broken clock degrades hygiene without downing the session",
    "fail-open per entry: a dangling symlink is skipped, its stale neighbour still reaped",
    "keeps the 10-minute recovery window as the resolved default"
   ],
   "agent/self-modify-path-normalize.test.ts": [
    "elevates when the write target is the absolute-path variant of a blocked source path",
    "elevates when the write target has a ./ prefix",
    "elevates when the write target uses .. segments that still resolve inside a source dir",
    "does not elevate for the absolute-path variant of a legitimate non-source write",
    "does not elevate when an absolute path escapes the repo root entirely"
   ],
   "agent/session-budget-renewal.test.ts": [
    "blocks exhausted sends cheaply, then renews fresh working counters without touching cost"
   ],
   "agent/session-lifecycle-snapshot.test.ts": [
    "separates user idle from work budget and marks in-flight turn",
    "reports budget-blocked when exhausted and idle"
   ],
   "agent/session-tool-content.test.ts": [
    "never follows a replaced capture pathname when appending the next chunk",
    "captures a prefix separately from all observed bytes and marks quota partial",
    "releases an aborted capture reservation and permanently expires opaque references on close",
    "rejects tampering and symlink replacement without accepting a filesystem path",
    "validates finite limits before allocating and bounds range requests",
    "keeps a bounded trailing protocol marker authoritative beyond the preview",
    "keeps malformed UTF-8 replacement expansion within the preview byte limit",
    "preserves trailing exit precedence alongside an earlier retained denied marker",
    "does not promote a mid-line marker at the carry buffer boundary",
    "retains split line markers and timeout context separated by long chunks",
    "does not create a capture hole after quota is later freed, and sees mid-stream markers",
    "preserves the standalone custom temporary directory prefix",
    "invalidates a range read closed while its asynchronous file open is pending",
    "never recreates a closed store from a pending capture",
    "does not read or delete a replacement content root",
    "returns a typed failure when publication collides with an existing directory",
    "preserves an empty complete stream digest without a fabricated detail reference",
    "returns unsupported rather than silently omitting no-follow protection"
   ],
   "agent/session.test.ts": [
    "fails a send after close before provider or auto-allowed tool execution",
    "persists the transcript across turns (turn 2 request includes turn 1)",
    "bridges respondPermission to the loop suspension",
    "setApprovalMode(full-auto) auto-allows a confirm-tier tool without a prompt",
    "cancel() retires a pending permission and ends the turn as cancelled",
    "cancel() also stops a later auto-tier tool in the same in-flight batch",
    "bridges respondPermission when requestPermission parks FIRST (ordering B)",
    "exposes the cross-turn transcript as a copy for trace recording"
   ],
   "agent/shell-risk.test.ts": [
    "classifies safe read: %s",
    "classifies destructive floor: %s",
    "classifies conservative modify: %s",
    "uses the worst risk across compound commands and substitutions",
    "promotes output redirection and tee to modify",
    "does not demote compound writes via the read-only parser",
    "runs safe reads promptlessly even when the registry tier is confirm",
    "keeps destructive deckent_bash on the always floor in full-auto"
   ],
   "agent/sse.test.ts": [
    "parses data-only events (OpenAI style), split arbitrarily across chunks",
    "parses event+data records (Anthropic style)",
    "handles CRLF line endings and ignores comment/other lines",
    "yields a trailing event with no final blank line"
   ],
   "agent/tool-budget-units.test.ts": [
    "converts bytes with a measured ratio instead of treating token caps as bytes",
    "probe K: degraded non-tool half falls back to byte upper bound instead of fail-open zero",
    "probe L: degraded full half falls back to byte upper bound without false zero diff",
    "regression pin: 18 KB retained tripped the old byte pre-batch math but not token caps",
    "131072 window + ~18 KB tool output does NOT request token-pressure checkpoint",
    "131072 window requests checkpoint when measured request exceeds 75% high-water",
    "32768 window scales retained share proportionally"
   ],
   "agent/tool-exposure.test.ts": [
    "keeps legacy schemas byte-identical when progressive mode is off",
    "exposes core definitions and monotonically reveals only registered names",
    "resolves progressive mode fail-closed",
    "starts with direct exec plus meta tools and reveals on describe and call",
    "returns deterministic bounded search cursors"
   ],
   "agent/tool-mutation-targets.test.ts": [
    "returns no targets for read_file paths",
    "returns paths for write tools and custom path-bearing tools",
    "deckent_read_file under deckent repo stays non-elevated"
   ],
   "agent/tool-registry.test.ts": [
    "registers and gets a tool by name",
    "throws on invalid definition",
    "last-write-wins on duplicate name",
    "toNativeSchemas maps to provider tool_use shape"
   ],
   "agent/tool-result-broker.test.ts": [
    "passes an under-cap result through inline, byte-identical, with no contentRef",
    "spills a generated 470k-char output to the store and hands the loop a ≤64KB envelope",
    "clamps a caller preview budget to the hard cap and still renders ≤64KB",
    "never splits a multi-byte UTF-8 character at the preview boundary",
    "reports a store failure honestly instead of inventing a contentRef",
    "rejects a content reference whose digest does not match the summarised bytes",
    "marks a bash result ending in [exit 2] as not ok, even when the caller said ok",
    "leaves an [exit 0] marker as ok",
    "types a timeout as ok:false / timeout from the caller reason and from the marker",
    "types a real non-zero exit code and a terminating signal",
    "classifies the denied and generic tool-error protocol markers",
    "never upgrades a caller-reported failure into a success",
    "keeps stderr in its own field and surfaces it as a labelled block, never merged",
    "writes outside the source tree with a sha256 name and restrictive modes",
    "anchors its content directory inside the session scratch root when given one",
    "close() is idempotent and a no-op when nothing was ever written",
    "chat-tool-exec: a generated 470k-char file read reaches the loop as a ≤64KB envelope",
    "chat-tool-exec: read_file honours a server-side {offset, limit} range",
    "chat-tool-exec: a bash command exiting non-zero stays visibly not-ok",
    "chat-tool-bridge: a real non-zero exit code becomes ok:false with stderr kept separate",
    "chat-tool-bridge: a clean exit 0 renders exactly the stdout it always did",
    "chat-tool-bridge: an unbounded CLI stdout is contained before it reaches the loop",
    "brokerToolResult is the one-call shape both dispatchers use",
    "bounds preview plus durable receipt and preserves the complete UTF8 content",
    "fails closed when even the durable receipt cannot fit"
   ],
   "agent/tool-result-delivery-withhold.test.ts": [
    "preserves executedOk and spills full bytes when store is available",
    "marks ref unavailable when store write fails without flipping executedOk",
    "rejects store receipt when sha256 does not match payload",
    "does not conflate handler failure with delivery withholding",
    "spills to session store and resolves byte-identical bytes via readContentRef"
   ],
   "agent/tool-result-retention.test.ts": [
    "mutates backing store and updates contentHash",
    "rollback restores prior bytes and hash",
    "refuses preview-only when store write fails",
    "shrinks with durable contentRef when store succeeds",
    "leaves transcript byte-identical when store write fails",
    "shrinks oldest large inline results first",
    "5×6KB reads at low window fill shrink instead of token-pressure checkpoint",
    "does not emit window-pressure checkpoint when exact measurement is unavailable"
   ],
   "agent/tool-surface-scale.test.ts": [
    "(a) round-1 progressive schema bytes stay under BASELINE_MAX_EAGER_SCHEMA_BYTES, an order of magnitude below the full catalog",
    "(b) a scripted search→describe→call chain reveals a deep tool schema on the following round and completes",
    "(c) legacy (non-progressive) mode ships the full 1000-tool catalog every round — honest contrast against (a)"
   ],
   "agent/tool-types.test.ts": [
    "returns null for a well-formed definition",
    "rejects empty name",
    "rejects unknown tier",
    "rejects non-object inputSchema",
    "rejects missing handler",
    "rejects whitespace-only name",
    "rejects array inputSchema"
   ],
   "agent/trace-recorder.test.ts": [
    "maps a ProviderMessage[] + system into OpenAI-messages shape (tool_calls arguments are a JSON string)",
    "omits tool_calls on a plain assistant message",
    "appends one JSON line per call (valid JSONL)"
   ],
   "agent/trace-retention.test.ts": [
    "an EMPTY policy deletes nothing (deletion only above an explicit threshold)",
    "maxSegments keeps the most-recent N, deletes older ones (oldest-first)",
    "maxAgeMs deletes segments older than now - maxAgeMs (injected clock)",
    "an unparseable lastTs is never age-deleted (unknown age is conservative)",
    "collapses exact re-appends to one line and surfaces the merged recordId",
    "KEEPS a distinct-content record that shares a recordId (no silent deletion)",
    "is idempotent — a second compaction is a pure no-op",
    "keeps the manifest recordCount truthful after collapse",
    "every compacted record still round-trips through the legacy parseTraceLine reader",
    "the live path holds the OLD complete content until the rename commits",
    "an EMPTY policy deletes nothing and writes no journal",
    "maxSegments deletes the oldest segments, journals them, and drops them from the manifest",
    "maxAgeMs deletes only the aged-out segment (kept ones untouched)"
   ],
   "agent/trace-segment.test.ts": [
    "is deterministic and position-independent (same logical record ⇒ same id)",
    "defaults attempt→1 and purpose→original when omitted",
    "distinguishes a FIX re-run from its original (attempt + purpose)",
    "produces a readable sprint-<id>.jsonl and never double-prefixes",
    "sanitizes unsafe characters and falls back to a stable name when empty",
    "writes to a per-sprint segment, stamps meta.recordId, and does NOT mutate the caller example",
    "is append-only — repeated appends add lines, never overwrite",
    "partitions distinct sprints into distinct segment files",
    "folds deltas into per-sprint stats (recordCount + first/last ts)",
    "readManifest is fail-soft on a missing file (empty aggregate)",
    "parseManifestDelta rejects malformed/torn lines; readManifest skips them",
    "foldManifest preserves first-seen sprint order (pure)",
    "a v2 record keeps top-level schemaVersion + telemetry byte-untouched (raw JSON)",
    "both v1 and v2 segment records still round-trip through the legacy parseTraceLine reader"
   ],
   "agent/transcript-eviction.test.ts": [
    "never evicts a small transcript under default caps",
    "never evicts a small transcript under small-but-unhit explicit caps",
    "evicts the oldest resolved turns once the token budget is crossed, pairing-safe",
    "preserves an in-flight multi-round tool chain intact even over the token budget",
    "evicts the oldest resolved turns once the message-count cap is crossed, pairing-safe",
    "keeps the in-flight turn intact even when it alone exceeds maxMessages",
    "holds the no-orphan invariant across many turns with both caps active",
    "droppedMessageCount accumulates across repeated evictions rather than resetting"
   ],
   "agent/transcript.test.ts": [
    "builds a user → assistant(+toolCalls) → tool-result sequence (§13)",
    "omits toolCalls on a plain assistant turn",
    "toProviderMessages returns copies (callers cannot mutate internal state)"
   ],
   "agent/transport-errors.test.ts": [
    "reads the DEEPEST coded cause (cause.cause) — ECONNRESET under UND_ERR_SOCKET is a reset with errno/syscall",
    "classifies connect / timeout / abort / unknown honestly",
    "strips control characters from the detail and bounds it",
    "transient = connect/reset/timeout in the connect phase only",
    "ProviderTransportError message carries the real code and the retry count",
    "sleepWithSignal rejects with an AbortError when the turn is cancelled",
    "retries ONCE on a transient connect-phase reset when authorized, then succeeds",
    "retry-once-then-typed-error: a second failure surfaces ProviderTransportError with the real code and attempts=2",
    "no authorization (transportRetry absent) → exactly one attempt, typed error with retries=0",
    "honors a config-resolved budget above one (3 attempts = 1 + 2 retries) and never exceeds it",
    "NEVER retries after a partial stream: a mid-body failure is a typed stream-phase error, one fetch only",
    "an abort is the caller\\'s own decision: rethrown as-is, never retried, never wrapped",
    "keeps the legacy cold-start message shape for callers matching on it",
    "the loop authorizes the config-resolved retry on every request and localizes the failure by code",
    "a stream-phase failure is reported under the no-retry code"
   ],
   "agent/turn-abort-seam.test.ts": [
    "validateProviderRequest accepts a request that carries an AbortSignal",
    "anthropic adapter passes req.signal to fetch as init.signal and ends when it aborts",
    "openai-compatible adapter passes req.signal to fetch as init.signal",
    "cancel() aborts req.signal; the turn ends with turn-end and no error event; the next turn is clean",
    "every send() hands the adapter a fresh, un-aborted signal even after a cancelled turn on the same session"
   ],
   "agent/work-budget-snapshot.test.ts": [
    "projects elapsed work time from startedAtMs"
   ]
  },
  "agents": {
   "agents/adaptive-agent-runtime.test.ts": [
    "triggers adaptation and suggests skills when agent has high NO_GO rate",
    "no-op when agent is performing well — no skill or prompt changes",
    "integrates outcome data — maps coverage weakness to testing-expert suggestion",
    "does not suggest a skill that is already in currentSkills",
    "is idempotent — calling twice with same inputs produces identical output",
    "returns no skill suggestions when results are empty",
    "skill suggestions are deduplicated when multiple weaknesses map to same skill"
   ],
   "agents/adaptive-agent.test.ts": [
    "returns zero successRate for empty results",
    "returns 100% successRate for all DONE results",
    "counts GO_WITH_TECH_DEBT as success",
    "marks needsImprovement when successRate < 70%",
    "does not mark needsImprovement when successRate >= 70%",
    "considers only the last 3 sprints",
    "detects high-nogo-rate weakness",
    "detects low-coverage weakness",
    "detects declining performance weakness",
    "detects tech-debt-heavy weakness",
    "detects inconsistent coverage",
    "reports multiple weaknesses at once",
    "handles single result without marking needsImprovement",
    "returns empty weaknesses when all results are good",
    "returns unchanged prompt when no weaknesses",
    "adds Error Handling section for NO_GO weakness",
    "adds Test Coverage section for coverage weakness",
    "adds Quality Focus for declining performance",
    "adds Completion Standards for tech debt weakness",
    "adds Consistency for inconsistent coverage",
    "includes reasoning in the diff",
    "preserves original prompt content",
    "handles multiple weaknesses with unique sections"
   ],
   "agents/agent-genealogy.test.ts": [
    "registers a root agent",
    "registers a child agent",
    "removes an agent from genealogy",
    "returns false when removing non-existent agent",
    "builds family tree from genealogy data",
    "returns empty tree when no data",
    "finds common ancestor of two agents",
    "returns self if same agent",
    "returns null when no common ancestor",
    "gets all descendants of an agent",
    "returns empty for leaf agent",
    "gets direct children only",
    "returns parent id",
    "returns null for root agent",
    "returns true for existing agent",
    "returns false for non-existent agent",
    "handles invalid JSON gracefully",
    "handles array JSON gracefully"
   ],
   "agents/agent-retirement.test.ts": [
    "recommends retirement when all criteria met",
    "does not retire built-in agents",
    "does not retire with too few uses",
    "does not retire with too few sprints",
    "does not retire with adequate success rate",
    "respects custom config thresholds",
    "handles learned source same as user",
    "moves agent to retired directory",
    "returns false when agent file does not exist",
    "prevents retiring built-in agents",
    "returns false on invalid JSON",
    "restores agent from retired to active",
    "returns false when retired agent not found",
    "returns false on invalid JSON",
    "returns empty array when retired dir does not exist",
    "returns retired records",
    "skips non-directory entries",
    "skips invalid retired.json",
    "returns empty on readdir error"
   ],
   "agents/agentic-prompt-parity.test.ts": [
    "carries scope / goNogo / verify-precedence byte-for-byte from the shared source builders (no leak)",
    "injects the operative-ADR block verbatim (mandatory-constraints protected element)",
    "the agentic prompt diff-equals the CLI prompt on the WHOLE protected set (4/4 elements present)",
    "is present even when NO operative-ADR is supplied (always-on, like the CLI T0 note)",
    "omitting operative-ADR omits it honestly — the section is absent, never faked",
    "a prompt missing the verify-precedence note is flagged (the NO_GO condition)",
    "a reworded scope block is flagged even when goNogo + verify-precedence survive",
    "still describes the five-tool surface and the task_done mandate",
    "still carries the scope-violation self-correct loop and informational read paths",
    "still carries the tech-debt note and the small-steps closing instruction",
    "is byte-identical across repeated builds for the same inputs"
   ],
   "agents/agentic-worker-entry.test.ts": [
    "propagates runner tokenUsage (eval_count/prompt_eval_count) into .result.tokenUsage with cacheReadTokens=0 and model from argv",
    "passes the receipt-bound host-compiled prompt to the agentic runner instead of task description",
    "fails closed before the runner when the compiled prompt bytes are tampered",
    "computes linesAdded/Removed from `git diff --numstat HEAD` against a baseline commit",
    "non-git tmpdir → linesAdded/Removed default to 0 with an honest [diff] note in result.notes",
    ".result has the exact 10 worker-written fields in api-surface.md shape with correct primitive types",
    "new untracked file (not in HEAD) falls back to disk line-count for linesAdded",
    "emits testsPassed:null + coverage:null when the runner ran no tests (honest absence, not fabricated 0/false)",
    "preserves a measured testsPassed:true from the runner while coverage stays null",
    "no env + no config file → liveTrace filled { enabled: false } (headless default pinned)",
    "DECKENT_LIVE_TRACE=1 (inherited from an interactive coordinator) → { enabled: true }",
    "no env + .deckent/config.json { live_trace: { enabled: true } } → { enabled: true } (disk opt-in respected)"
   ],
   "agents/agentic-worker-runner.test.ts": [
    "write_file dispatches to chat-tool-exec → file appears on disk; filesChanged populated",
    "edit_file replaces text in an existing in-scope file; filesChanged tracks path",
    "task_done with missing/invalid selfAssessment but files changed → GO_WITH_TECH_DEBT (work not punished as NO_GO)",
    "task_done with missing selfAssessment and no files changed → NO_GO",
    "out-of-scope write_file is HARD-REJECTED and the scope error is fed into the NEXT request",
    "returns the documented .result-shape fields (taskId / filesChanged / selfAssessment / notes / iterations / terminationReason)",
    "maxIterations exhausted WITH file changes → GO_WITH_TECH_DEBT",
    "maxIterations exhausted WITHOUT any file changes → NO_GO",
    "task_done propagates selfAssessment and notes verbatim",
    "Ollama unreachable (fetch throws) → NO_GO and notes contain the reason",
    "Ollama returns 500 → NO_GO and status surfaces in notes",
    "exports DEFAULT_MAX_ITERATIONS = 25 (spec §6 config-surfaced cap)"
   ],
   "agents/api-usage-accumulate.test.ts": [
    "accumulates prompt_eval_count/eval_count across a multi-turn /api/chat loop into .result.tokenUsage",
    "clamps a malformed (negative/NaN) runner usage to honest 0 instead of propagating garbage",
    "captures usage from a single /api/chat response (task_done on the first turn)"
   ],
   "agents/auditor.test.ts": [
    "runHeartbeat returns a HeartbeatRunResult synchronously",
    "runHeartbeat creates default HEARTBEAT.md when missing",
    "runHeartbeat handles empty HEARTBEAT.md",
    "runHeartbeat skips completed (checked) tasks",
    "runHeartbeat executes pending whitelisted tasks",
    "runHeartbeat handles failing commands gracefully",
    "runHeartbeat blocks non-whitelisted commands",
    "runHeartbeat writes log to .brain/heartbeat-log.md",
    "executes multiple pending tasks in parallel",
    "start() returns a HeartbeatRunResult synchronously",
    "stop() returns void and sets running to false",
    "writes and removes PID file asynchronously",
    "readDaemonPid returns null when no PID file",
    "readDaemonPid returns PID for current process",
    "readDaemonPid cleans up stale PID file",
    "stopDaemonByPid returns false when no daemon running",
    "handles multiple concurrent runHeartbeat calls",
    "allows whitelisted commands",
    "rejects shell metacharacters",
    "rejects non-whitelisted commands",
    "completes scan of 3 tasks under 5 seconds"
   ],
   "agents/builtin-agents.test.ts": [
    "should have all 7 built-in agent directories",
    "should have agent.json in every agent directory",
    "should have PROMPT.md in every agent directory",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel and restricted tools",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel and read-only tools",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel and high effort",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have valid JSON in agent.json",
    "should have correct id and name",
    "should have non-empty triggerKeywords",
    "should have valid preferredModel",
    "should have effortMultiplier in valid range",
    "should have PROMPT.md with substantial content",
    "should have unique ids across all agents",
    "should have unique names across all agents",
    "should all have source set to builtin",
    "should all have enabled set to true",
    "should all have valid stats object with expected shape",
    "should all have non-empty description",
    "should all have non-empty expertise array",
    "should all have allowedTools as array"
   ],
   "agents/cross-sprint-analyzer.test.ts": [
    "returns empty report when learning dir does not exist",
    "returns empty report when directory read fails",
    "returns empty report when no entries match agent",
    "filters entries within sprint range",
    "computes success trend per sprint",
    "counts GO_WITH_TECH_DEBT as success in trend",
    "computes coverage trend per sprint",
    "computes task type distribution",
    "identifies best and worst task types",
    "handles single task type for best and worst",
    "suggests improvement when success rate is low",
    "suggests coverage improvement when average is low",
    "detects declining success trend",
    "detects over-concentration on single task type",
    "returns no suggestions for healthy agent",
    "produces full report from learning files",
    "skips invalid JSON files gracefully",
    "skips non-array JSON files",
    "only reads .json files from learning dir"
   ],
   "agents/http-agentic-scope-event.test.ts": [
    "out-of-scope write_file → emits exactly one SCOPE_INSUFFICIENT event AND still feeds the model",
    "in-scope write_file → no SCOPE_INSUFFICIENT event emitted",
    "two out-of-scope attempts → exactly two events (one per violation)",
    "default emitter + active sprint → SCOPE_INSUFFICIENT event written to the project event stream",
    "SCOPE_INSUFFICIENT_CHANNEL is the canonical protocol value"
   ],
   "agents/http-agentic-worker-local.test.ts": [
    "runs the exact Qwen identity without a key and writes guard, heartbeat, usage, result, and settlement",
    "preserves the config-resolved %s endpoint on the canonical subprocess path"
   ],
   "agents/http-agentic-worker.test.ts": [
    "executes read→write tool calls through chat-tool-exec; file lands on disk + filesChanged tracked",
    "HARD-REJECTS an out-of-scope write: no disk write, scope-error fed into the next send",
    "rejects a cwd-escape write (../escape.ts) — never written outside the project root",
    "content-only final turn after a write → DONE (no_tool_calls termination)",
    "send() throwing (provider unreachable) → NO_GO + api_error with reason",
    "reads task json, drives the REAL loop via injected send, writes a valid .result + .hb (DONE)",
    "writes a NO_GO .result and exits 1 when argv is incomplete",
    "writes a NO_GO .result when the loop throws",
    "spawn() no longer throws — launches `node <entry> <taskId> <model> <baseURL> <apiKeyEnv> <name>`",
    "kill(taskId) sends SIGTERM and clears the workers map",
    "child exit event clears the workers map",
    "spawn() still rejects an unsupported model honestly"
   ],
   "agents/log-event-to-activity.test.ts": [
    "SDK envelope → 🔧 {name}({primary-arg}) with tool+args detail",
    "picks cmd as the primary arg for Bash",
    "a tool with no path/cmd/url arg → bare 🔧 {name}",
    "raw streaming content_block_start form is also handled",
    "success → ↳ ✓ ok with ok:true + toolUseId",
    "error → ↳ ✗ error with ok:false",
    "usage / assistant-text → null",
    "plain stdout text → null",
    "workerId is omitted when not supplied",
    "flag ON: a tool_use event lands as a \"🔧 …\" line on the ACTIVITY channel",
    "flag ON: a non-tool event (usage) emits NOTHING",
    "flag OFF: zero-cost no-op — nothing written",
    "a pathological event shape never escapes the closure (fail-soft invariant)"
   ],
   "agents/manifest-v2-validation.test.ts": [
    "all 8 agent.json files exist",
    "all agents have manifestVersion: 2",
    "all agents have valid activation object",
    "each activation rule has when and score",
    "all 11 skill manifest.json files exist",
    "all skills have manifestVersion: 2",
    "all skills have valid activation object",
    "ci-testing skill has exclude for implementation intent",
    "security-auditor activates on security intent",
    "security-auditor excludes documentation intent",
    "ci-guardian activates on devops intent",
    "ci-guardian excludes implementation intent",
    "api-builder agent activates on domains containing api",
    "doc-writer excludes implementation intent",
    "security-auditor preserves triggerKeywords",
    "ci-testing preserves priority: 12",
    "all agents still have enabled: true",
    "all skills still have enabled: true"
   ],
   "agents/permission-guard.test.ts": [
    "blocks brain from modifying brain.ts",
    "blocks auditor from modifying auditor.ts",
    "blocks worker from modifying worker.ts",
    "blocks worker from modifying .claude/settings.json",
    "blocks auditor from modifying .mcp/ configs",
    "allows brain to modify tool configs (brain is exempt)",
    "blocks worker from modifying .claude/rules/",
    "blocks auditor from modifying .deckent/workspace/",
    "allows brain to modify agent configs",
    "blocks auditor from writing to src/",
    "blocks auditor from writing to tests/",
    "allows worker to write within assigned scope",
    "allows brain to write to .brain/",
    "allows auditor to write to .dashboard",
    "normalizes absolute paths to relative",
    "logs blocked attempts",
    "does not log allowed attempts",
    "creates log directory if needed",
    "returns default log path",
    "respects custom logDir"
   ],
   "agents/prompt-ab-test.test.ts": [
    "creates an experiment with correct fields",
    "persists experiment to disk",
    "throws if agent already has active experiment",
    "allows experiment for different agents",
    "returns null when no experiments exist",
    "returns active experiment",
    "returns null after experiment is completed",
    "returns A or B",
    "distributes approximately 50/50 over many calls",
    "records a result for variant A",
    "records multiple results",
    "throws for non-existent experiment",
    "throws for completed experiment",
    "returns inconclusive with no results",
    "returns inconclusive with fewer than 4 samples",
    "declares winner A when A is significantly better",
    "declares winner B when B is significantly better",
    "computes aStats correctly",
    "throws for non-existent experiment",
    "returns inconclusive when only one variant has data",
    "computes confidencePercent",
    "marks experiment as completed",
    "throws for non-existent experiment",
    "returns null for non-existent experiment",
    "returns experiment by id"
   ],
   "agents/prompt-analytics.test.ts": [
    "creates an instance with projectRoot",
    "creates a new experiment",
    "throws when agent already has active experiment",
    "allows different agents to have experiments simultaneously",
    "persists experiment to disk",
    "returns null when no experiments exist",
    "returns active experiment",
    "returns null after experiment is completed",
    "returns null for non-existent experiment",
    "returns experiment by id",
    "returns A or B",
    "deterministically returns B when experiment has 3 A-results and 0 B-results",
    "falls back to A or B without throwing when experiment is not found",
    "returns A when B has more results (A is under-represented)",
    "returns A or B (random) when counts are equal",
    "records a result and persists it",
    "throws for non-existent experiment",
    "throws for completed experiment",
    "returns inconclusive with no results",
    "declares winner A when A is significantly better",
    "throws for non-existent experiment",
    "marks experiment as completed",
    "throws for non-existent experiment",
    "handles empty versions array",
    "identifies best and worst versions",
    "detects improving trend",
    "detects declining trend",
    "reports experiment status from passed experiment",
    "formats a complete report with all fields",
    "collects metrics with no active experiment",
    "collects metrics and automatically fetches active experiment",
    "reports none after experiment is completed",
    "integrates AB test results into metrics report"
   ],
   "agents/prompt-evolution.test.ts": [
    "records an evolution event",
    "appends to existing events",
    "returns empty timeline when no file exists",
    "returns timeline with events",
    "handles invalid JSON gracefully",
    "handles non-array JSON gracefully",
    "formats empty timeline",
    "formats timeline with events",
    "includes stats in formatted output",
    "returns 0 for non-existent agent",
    "returns correct count",
    "clears all events for an agent"
   ],
   "agents/prompt-metrics.test.ts": [
    "handles empty versions array",
    "reports current version as highest version number",
    "reports total versions",
    "reports current success rate from highest version",
    "identifies best version",
    "identifies worst version",
    "reports experiment status as none when no experiment",
    "reports experiment status as active",
    "reports experiment status as completed",
    "detects improving trend",
    "detects declining trend",
    "detects stable trend",
    "returns stable for single version",
    "formats a complete report",
    "formats report with zero values",
    "includes all required fields"
   ],
   "agents/prompt-rollback.test.ts": [
    "returns false when uses < 3",
    "returns false when uses is 0",
    "returns true when successRate < 50% and uses >= 3",
    "returns false when successRate >= 50%",
    "returns true when successRate is 0 and uses is 3",
    "returns false when successRate is exactly 50%",
    "returns false when no versions exist",
    "returns false with only 1 version",
    "returns true with 2+ versions",
    "returns null when canRollback is false",
    "rolls back to best historical version",
    "activates the rolled-back version",
    "logs the rollback event",
    "writes rollback log to disk",
    "appends to existing log",
    "includes timestamp in log entry",
    "returns empty array when no log exists",
    "returns all log entries"
   ],
   "agents/prompt-version.test.ts": [
    "creates version 1 as first version",
    "increments version number",
    "persists version file to disk",
    "writes PROMPT.md file",
    "sets current version",
    "prunes oldest when exceeding max 10 versions",
    "returns null for non-existent version",
    "returns version by number",
    "returns correct version among multiple",
    "returns null when no versions exist",
    "returns the most recent version after create",
    "returns activated version after activateVersion",
    "returns empty array when no versions exist",
    "returns all versions sorted by number",
    "returns versions for correct agent only",
    "returns false for non-existent version",
    "activates version and writes PROMPT.md",
    "returns true on success",
    "increments uses count",
    "calculates successRate correctly for DONE",
    "calculates successRate correctly for mixed evaluations",
    "counts GO_WITH_TECH_DEBT as success",
    "is no-op for non-existent version",
    "records a use against the agent CURRENT version",
    "reflects the evaluation in successRate (NO_GO → 0)",
    "is a no-op when the agent has no versioned prompt"
   ],
   "agents/scope-w1-escalation.test.ts": [
    "out-of-scope write_file → SCOPE_INSUFFICIENT event emitted with correct payload",
    "in-scope write_file → no SCOPE_INSUFFICIENT event emitted",
    "SCOPE_INSUFFICIENT_CHANNEL has the correct protocol value",
    "multiple out-of-scope write attempts → one SCOPE_INSUFFICIENT event per violation"
   ],
   "agents/scoped-typecheck-authority.test.ts": [
    "stays green when an unrelated concurrent file holds the only type error",
    "still fails when the error is inside the task own scope",
    "fails on the in-scope error while ignoring a concurrent foreign one",
    "keeps the compile whole-program so in-scope errors cannot be hidden",
    "attributes an error in a sibling file of the same scope directory as in-scope",
    "judges every diagnostic when the scope declares no write authority",
    "treats a diagnostic with no file location as in-scope",
    "does not consume FIX retries on a concurrent foreign error",
    "still exhausts retries when the error is inside the task scope",
    "reads the piped tsc diagnostic format",
    "reads the pretty tsc diagnostic format",
    "normalises Windows separators",
    "returns null for a line carrying no file location",
    "unions filesWrite and directories, normalised and deduplicated",
    "returns an empty authority set for an undefined scope",
    "strips the project root from an absolute diagnostic path before matching",
    "does not treat a path prefix collision as in-scope"
   ],
   "agents/shared-context.test.ts": [
    "writes and reads a key-value pair",
    "returns undefined for non-existent key",
    "overwrites existing key",
    "readAll returns all entries",
    "clear removes all entries",
    "remove deletes a specific key",
    "remove returns false for non-existent key",
    "size returns the number of entries",
    "has returns true for existing key",
    "throws on empty key",
    "throws on empty agentId",
    "handles corrupted file gracefully",
    "handles non-object file content gracefully",
    "stores timestamp as ISO string",
    "supports complex value types"
   ],
   "agents/specialization-drift.test.ts": [
    "returns zero drift for empty results",
    "returns zero drift for empty keywords and empty results",
    "returns low drift when keywords match task content",
    "detects high drift when tasks differ from keywords",
    "recommends respecialize for moderate drift",
    "recommends create_new_agent for very high drift",
    "extracts unique keywords from results",
    "filters short tokens from extraction",
    "returns 0 drift when both sets empty",
    "returns 1 drift when original is empty but actual has keywords",
    "returns 0 drift when actual is empty but original has keywords",
    "keeps agent below 0.6 drift",
    "respecializes between 0.6 and 0.8",
    "creates new agent at 0.8 and above",
    "preserves original specialization in report"
   ],
   "agents/verify-ran-atomic.test.ts": [
    "atomic write: .tmp intermediary is invisible as final path until rename",
    "atomic write prevents 0-byte marker: tmp is overwritten before rename"
   ],
   "agents/wlt-emit.test.ts": [
    "flag-off (default, no liveTrace opt) never creates .progress.jsonl",
    "flag-off explicit (liveTrace: { enabled: false }) also writes nothing",
    "flag-on writes ordered start → edit-file → verify-running → result-writing events with strictly increasing seq",
    "flag-on still emits start + result-writing even when the model never calls a tool",
    "flag-on emits result-writing on an api_error termination too",
    "fail-soft: a write failure (progress path blocked by a same-named file) never throws or fails the run",
    "does not change filesChanged/selfAssessment/terminationReason vs. flag-off for the same script",
    "does not touch the .hb heartbeat file (that stays agentic-worker-entry.ts responsibility)"
   ],
   "agents/worker-activity.test.ts": [
    "clipActivityLine: ≤80 chars, whitespace-flattened, ellipsis on overflow",
    "disabled flag → zero events (cheap no-op)",
    "enabled → ACTIVITY event with clipped line + detail on the sprint stream",
    "heartbeat path emits a live status row when live_trace is enabled (the #582 core ask)",
    "heartbeat path stays silent when live_trace is absent/false (flag-gated)"
   ],
   "agents/worker-agent.test.ts": [
    "includes agentId when provided",
    "agentId is undefined when not provided",
    "agentId is included in JSON serialization",
    "other heartbeat fields remain correct with agentId",
    "progress is calculated correctly with agentId present",
    "handles empty string agentId",
    "writes heartbeat with agentId to file",
    "writes heartbeat without agentId correctly",
    "persists agentId in result file",
    "handles result without agentId"
   ],
   "agents/worker-approval-gate-real-wire.test.ts": [
    "classifies a plain shell command as shell-exec/medium (baseline risky class)",
    "classifies git push --force as git-mutation/critical",
    "classifies npm install as network/medium",
    "prioritizes git-mutation over network when a command matches both (git push)",
    "RISKY_APPROVAL_SCOPES is exactly the 3 named registry-risk-consistent classes",
    "returns a genuine WorkerApprovalGate instance backed by a real, disk-backed ApprovalBroker",
    "submits a REAL request to disk and fails closed (deny) when nobody decides before the timeout",
    "allows and returns no deniedOutput once the real broker decides allow",
    "re-exports the exact same functions as src/agents/worker.ts (no duplicate definition)",
    "disabled → undefined (zero footprint on the worker env)",
    "enabled → gate flag + sprint-scoped scopeId (grant leak guard, advisor S5)",
    "resolveApprovalConfig carries question_bridge (typed, default-off)",
    "env off → no gate, dispose is a safe no-op",
    "env on → real gate with env-provided scopeId",
    "cross-process ALLOW: second broker decides, poll driver discovers, dispatch proceeds",
    "cross-process DENY: structured [approval-denied], base never called",
    "non-risky tool bypasses the gate entirely (no request file)",
    "gate with tiny timeout + NO poll driver: external allow written pre-timeout wins",
    "gate env set → runner receives approvalGate with env scopeId",
    "gate env absent → runner opts carry NO approvalGate (byte-identical default)"
   ],
   "agents/worker-auth-check.test.ts": [
    "(a) skips when CLAUDE_AUTH_REQUIRED is not set → ok=true, no .result, no event",
    "(b) auth OK → valid loggedIn=true status succeeds without leaking account metadata",
    "(c) auth fail (non-zero exit) → writes AUTH_FAILED .result + emits audit event",
    "(d) exact Vitest-only bypass skips when auth is required",
    "(e) ignores a lone production DECKENT_AUTH_SKIP and fails closed",
    "(f) empty/invalid JSON despite exit 0 is AUTH_FAILED",
    "(g) exit 0 with loggedIn=false is AUTH_FAILED"
   ],
   "agents/worker-authority-enforce.test.ts": [
    "enforceRbac:true + write OUTSIDE filesWrite → false (hard deny)",
    "enforceRbac:true + in-scope write (filesWrite) → true (allow)",
    "enforceRbac:true + in-scope write (within scope.directories) → true (allow)",
    "enforceRbac:false + out-of-scope write → true (V1.0 soft default preserved)",
    "opts omitted + out-of-scope write → true (byte-for-byte today’s soft default)"
   ],
   "agents/worker-authority.test.ts": [
    "flag-on + scope violation → returns false (hard deny)",
    "flag-off + scope violation → returns true (soft allow, byte-identical)",
    "no opts (undefined) + scope violation → returns true (byte-identical default)",
    "flag-on + within-scope write → returns true (no violation)",
    "flag-on + within directories (new file) → returns true (no violation)",
    "flag-on + self-modifying sprint + out-of-scope → returns true (self-mod bypass)"
   ],
   "agents/worker-doc-skip.test.ts": [
    "returns false for undefined scope",
    "returns false for empty directories array",
    "returns false for src/ directory",
    "returns false for tests/ directory",
    "returns false for lib/ directory",
    "returns false for exact \"src\" directory",
    "returns false for mixed doc and source directories",
    "returns true for docs/ directory only",
    "returns true for multiple non-source directories",
    "returns true for root directory scope",
    "skips compilation for doc-only scope",
    "runs compilation when scope includes source dirs",
    "runs compilation when no scope provided",
    "skips tests for doc-only scope",
    "runs tests when scope includes source dirs",
    "skips compilation loop for doc-only scope and returns 0 attempts",
    "skips test verify loop for doc-only scope and returns 0 attempts"
   ],
   "agents/worker-edge.test.ts": [
    "reads and parses a valid task file",
    "throws with \"Task file not found\" when file is missing",
    "throws with \"Invalid JSON\" when file contains malformed JSON",
    "returns all fields from a complete task file",
    "transitions task status from PENDING to CLAIMED",
    "persists the CLAIMED status to disk",
    "throws TaskClaimError when task is already CLAIMED",
    "throws TaskClaimError when task status is DONE",
    "throws TaskClaimError when task is already assigned to another worker",
    "sets updatedAt timestamp",
    "creates a lock file and returns LockInfo",
    "is idempotent — same worker re-acquiring returns existing lock",
    "throws LockError when another worker holds the lock",
    "LockError message includes the file path",
    "handles EEXIST race condition — re-reads lock owner",
    "throws LockError for a corrupted lock file (cannot determine owner)",
    "releases a lock held by the calling worker",
    "is a no-op when no lock file exists",
    "throws LockError when another worker tries to release the lock",
    "LockError message identifies actual owner vs attempted releaser",
    "allows corrupted lock file to be deleted without error",
    "returns true for a file within an allowed directory",
    "returns true when directory has trailing separator",
    "protects against prefix overlap — src/core-extra is NOT within src/core",
    "returns true for exact directory match",
    "returns true for a file in filesWrite",
    "returns false for a file outside scope",
    "returns false for a file that is only in filesRead (not write scope)",
    "handles nested directories correctly",
    "handles empty scope gracefully",
    "writes result file to disk",
    "DONE selfAssessment sets task status to DONE",
    "GO_WITH_TECH_DEBT selfAssessment sets task status to DONE",
    "NO_GO selfAssessment sets task status to NO_GO",
    "persists result fields to disk",
    "returns a Heartbeat object with all required fields",
    "timestamp is a valid UTC ISO 8601 string",
    "includes optional file and sequence when provided",
    "currentFile is undefined when not provided",
    "heartbeat serializes to valid JSON"
   ],
   "agents/worker-feedback.test.ts": [
    "should return empty arrays for passing test output",
    "should extract FAIL file-level markers",
    "should extract FAIL lines with test names",
    "should extract × (cross mark) failure lines",
    "should extract ✕ failure lines",
    "should deduplicate test names",
    "should extract summary line",
    "should return empty summary when no summary line present",
    "should handle empty output",
    "should handle output with only FAIL file markers and no test-level failures",
    "should handle mixed FAIL markers and × failures",
    "should return success when vitest passes",
    "should call execSync with correct command",
    "should include scope directories in command",
    "should return failure when vitest throws (test failures)",
    "should extract failed test names from error output",
    "should handle error with only stderr",
    "should handle error with no stdout/stderr",
    "should pass empty scope as no scope args",
    "should set timeout to 120 seconds",
    "should handle non-Error thrown values",
    "should succeed on first attempt when tests pass",
    "should retry on failure and succeed on second attempt",
    "should retry up to MAX_TEST_RETRIES times",
    "should call fix callback with failed test names between retries",
    "should not call fix callback after last failed attempt",
    "should work without a fix callback",
    "should pass scope to verifyTests",
    "should succeed on third attempt",
    "should return correct failuresFixed count",
    "should accumulate failuresFixed across retries",
    "should return last result output when all retries fail",
    "should be 3",
    "is set to 3",
    "is a positive integer",
    "extracts TypeScript error lines from stdout",
    "extracts errors from stderr if stdout is empty",
    "falls back to message if stdout and stderr are empty",
    "returns \"Unknown compilation error\" for null/undefined input",
    "returns \"Unknown compilation error\" for empty string",
    "returns \"Unknown compilation error\" for object with all empty strings",
    "handles plain string error input",
    "returns up to 20 non-TS-error lines when no TS pattern found",
    "filters out empty lines",
    "handles mixed TS errors and non-error lines",
    "handles error TS pattern without file location",
    "returns success when tsc exits cleanly",
    "calls tsc with correct arguments",
    "returns failure with errors when tsc fails",
    "returns failure with parsed errors for multiple compilation errors",
    "handles timeout errors",
    "returns CompilationResult shape",
    "returns success on first attempt when tsc passes",
    "writes heartbeat with VERIFYING status on each attempt",
    "retries on failure and succeeds on second attempt",
    "retries on failure and succeeds on third attempt",
    "returns failure after max retries exhausted",
    "enforces max retries = MAX_COMPILATION_RETRIES",
    "calls onAttempt callback on each failed attempt",
    "does not call onAttempt on successful attempt",
    "heartbeat shows correct attempt number",
    "respects custom maxRetries parameter",
    "returns errors from the last failed attempt",
    "returns CompilationLoopResult shape on success",
    "returns CompilationLoopResult shape on failure",
    "works with maxRetries = 1 (no retries)",
    "heartbeat workerId and taskId match arguments",
    "creates .tasks/ directory if needed",
    "returns 65 for VERIFYING status",
    "returns 65 for VERIFYING string",
    "VERIFYING is between CODING and TESTING progress values",
    "exists in AgentStatus enum",
    "is a string value",
    "returns a fresh tracker with all zeros",
    "returns independent instances",
    "increments tscAttempts on first call",
    "accumulates totalRetryTimeMs",
    "does NOT count tscErrorsFixed on first attempt success",
    "counts tscErrorsFixed when success on retry",
    "does not count tscErrorsFixed on retry failure",
    "increments testAttempts",
    "does NOT count testFailuresFixed on first attempt success",
    "counts testFailuresFixed when success on retry",
    "accumulates totalRetryTimeMs alongside tsc attempts",
    "returns 0 for empty results array",
    "returns 0 when no results have feedbackLoop",
    "returns 0 when all tasks pass on first try",
    "returns 100 when all retried tasks self-healed",
    "returns correct rate for mixed outcomes",
    "treats GO_WITH_TECH_DEBT as self-healed",
    "returns 0 when single NO_GO result with retries",
    "rounds to nearest integer",
    "returns zeros for empty results",
    "skips results without feedbackLoop",
    "correctly aggregates across multiple results",
    "counts task as first-pass when both tsc and test are 1 attempt",
    "counts task as retry when tsc > 1 even if test = 1",
    "feedbackLoop is optional — undefined is valid",
    "feedbackLoop can be attached via createFeedbackLoop + record*",
    "feedbackLoop serializes to JSON correctly",
    "recordTscAttempt works with 0ms duration",
    "recordTestAttempt works with large duration",
    "aggregateFeedbackLoops with mix of feedback and no-feedback results"
   ],
   "agents/worker-heartbeat-authority-wire.test.ts": [
    "auto-assigns the first host sequence without the caller supplying one",
    "advances the sequence itself across repeated in-process observations for the same attempt",
    "has no sequence field on its bounded observation input — the store, not the worker, owns sequencing",
    "returns a typed HOLD for a foreign identity instead of overwriting the fenced attempt",
    "round-trips the exact attempt identity unchanged through the authority store",
    "does not touch the legacy IPC heartbeat file path",
    "still writes the .hb file directly with a worker-supplied sequence",
    "triggers adaptation when agent has high NO_GO rate (needsImprovement=true)",
    "no-op when agent is performing well — no prompt changes suggested",
    "integrates ResultEntry outcome format from sprint results",
    "is idempotent — calling twice with same inputs produces the same result",
    "returns empty results gracefully when no outcome data is available",
    "diff preserves original prompt in output regardless of weaknesses"
   ],
   "agents/worker-heartbeat-single-writer.test.ts": [
    "never lets two competing writers regress the persisted heartbeat",
    "advances every approval-wait refresh monotonically",
    "routes a provider heartbeat through the monotonic primitive in a tmpdir"
   ],
   "agents/worker-ipc.test.ts": [
    "returns true for a valid IPCMessage",
    "returns false for null",
    "returns false for non-object",
    "returns false if type is missing",
    "returns false if taskId is missing",
    "returns false if timestamp is missing",
    "returns true with optional payload",
    "creates a channel with supportsIPC = true when proc.send exists",
    "creates a channel with supportsIPC = false when proc has no send",
    "send() calls proc.send with correct IPCMessage shape",
    "send() returns false after channel is closed",
    "send() returns false when proc has no send function",
    "onMessage() registers handler and dispatches incoming messages",
    "does not dispatch messages with wrong taskId",
    "does not dispatch non-IPC shaped messages",
    "multiple handlers for the same type are all called",
    "handlers for different types do not cross-fire",
    "close() removes listeners and marks channel closed",
    "close() is idempotent (calling twice is safe)",
    "sendHeartbeat() sends HEARTBEAT message",
    "requestStatus() sends STATUS_REQUEST message",
    "pause() sends PAUSE message",
    "resume() sends RESUME message",
    "kill() sends KILL message",
    "after close(), no handlers are dispatched",
    "handler errors are swallowed (channel stays stable)",
    "send() returns false when proc.send throws",
    "close() uses removeListener fallback when off is unavailable",
    "register() and get() work correctly",
    "has() returns true for registered taskId",
    "has() returns false for unknown taskId",
    "get() returns undefined for unknown taskId",
    "remove() closes and removes the channel",
    "remove() returns false for unknown taskId",
    "closeAll() closes all channels and clears registry",
    "listTaskIds() returns all registered taskIds",
    "size() returns correct count",
    "supportsIPC() returns false when process.send is not a function",
    "send() returns false when process.send is not available",
    "send() returns false when channel is closed",
    "isClosed() returns false before close",
    "isClosed() returns true after close",
    "close() is idempotent",
    "onMessage() registers handlers that fire on emitter message events",
    "does not dispatch messages with wrong taskId",
    "send() with mocked process.send sends correct message shape",
    "send() returns false when process.send throws",
    "send() returns true on successful process.send",
    "close() uses removeListener fallback when off is unavailable",
    "handler errors are swallowed in WorkerSideChannel dispatch",
    "does not dispatch non-IPC shaped messages",
    "multiple handlers for same type all fire",
    "getQuestionPath returns correct path",
    "getAnswerPath returns correct path",
    "round-trips a question through write and read",
    "readQuestionFile returns undefined for missing file",
    "round-trips an answer through write and read",
    "readAnswerFile returns undefined for missing file",
    "removes both question and answer files",
    "does not throw when files do not exist",
    "returns Brain answer when answer file is written promptly",
    "returns default continue on timeout",
    "passes context and suggestedAction to question file"
   ],
   "agents/worker-lifecycle.test.ts": [
    "writes to tmp, fsyncs, then renames",
    "returns false when result file does not exist",
    "returns true when result file exists and fsync succeeds",
    "returns false when no result file exists",
    "returns true and writes DONE heartbeat for DONE result",
    "returns false for NO_GO result",
    "returns true for GO_WITH_TECH_DEBT result",
    "creates a zeroed feedback loop",
    "increments tscAttempts and adds duration",
    "increments tscErrorsFixed on second successful attempt",
    "increments testAttempts",
    "returns 0 for empty results",
    "returns 100% when all retried tasks succeeded",
    "returns 0 when all retried tasks failed",
    "aggregates across multiple results",
    "writes baseline JSON file",
    "readVerifyDeltaBaseline returns null when file missing",
    "readVerifyDeltaBaseline parses valid JSON",
    "returns null when no baseline exists",
    "returns DONE for high completion ratio",
    "returns NO_GO for low completion ratio",
    "starts in SPAWNING state by default",
    "transitions through valid states",
    "throws InvalidStateTransitionError on invalid transition",
    "canTransition returns true for valid, false for invalid",
    "tracks history",
    "isStoppable is true during active states",
    "isTerminal is true for terminal states",
    "forceState bypasses validation",
    "toJSON serializes state and history",
    "createWorkerStateMachine creates and stores state machine",
    "getWorkerStateMachine creates on first access",
    "removeWorkerStateMachine deletes entry",
    "isWorkerStoppable returns false for unknown worker",
    "isWorkerStoppable returns true for active worker",
    "clearWorkerStateRegistry empties registry",
    "VERIFY_DELTA_DONE_THRESHOLD is 0.8",
    "VERIFY_DELTA_NO_GO_THRESHOLD is 0.5",
    "has an entry for every state",
    "EXITED has no valid transitions",
    "ORPHAN has no valid transitions",
    "STOPPABLE_STATES contains active states",
    "TERMINAL_STATES contains terminal states"
   ],
   "agents/worker-log.test.ts": [
    "formats a basic log line with emoji indicator",
    "formats with plain indicator when noColor is true",
    "formats all action types with correct emoji indicators",
    "formats all action types with correct plain indicators",
    "preserves task ID format in brackets",
    "handles empty detail string",
    "formats single directory with file count",
    "formats multiple directories",
    "uses singular \"file\" for count 1",
    "uses plural \"files\" for count > 1",
    "works with noColor option",
    "formats a passing test result",
    "formats a failing test result with detail",
    "includes retry info for attempts > 1",
    "does not include retry info for first attempt",
    "works with noColor option",
    "formats a passing verification",
    "formats a failing verification with error count",
    "defaults error count to 0 when not provided",
    "works with noColor option",
    "formats a DONE result with no retries",
    "formats a DONE result with retries",
    "formats a GO_WITH_TECH_DEBT result",
    "formats a NO_GO result with Error action",
    "works with noColor option",
    "creates log file and appends formatted line",
    "appends multiple lines to the same file",
    "prepends ISO timestamp to each line",
    "all log lines follow [taskId] indicator Action: detail pattern"
   ],
   "agents/worker-no-selfcount.test.ts": [
    "no longer exports the defaultTokenUsageStub self-count helper",
    "worker.ts code carries no self-authored `inputTokens: 0` placeholder"
   ],
   "agents/worker-progress.test.ts": [
    "returns 10 for EXECUTING status",
    "returns 70 for TESTING status",
    "returns 85 for DOCUMENTING status",
    "returns 100 for DONE status",
    "returns 0 for IDLE status",
    "returns 0 for PLANNING status",
    "returns 0 for ERROR status",
    "returns 0 for PAUSED status",
    "returns 30 when filesChangedCount is 0",
    "returns 36 when filesChangedCount is 1",
    "returns 42 when filesChangedCount is 2",
    "returns 48 when filesChangedCount is 3",
    "returns 54 when filesChangedCount is 4",
    "returns 60 when filesChangedCount is 5 (max)",
    "caps at 60 when filesChangedCount exceeds 5",
    "sets progress=10 when status is EXECUTING",
    "sets progress=70 when status is TESTING",
    "sets progress=85 when status is DOCUMENTING",
    "sets progress=100 when status is DONE",
    "sets CODING progress based on filesChangedCount parameter",
    "sets CODING progress=30 when filesChangedCount is 0 (default)",
    "always sets the progress field (never undefined)"
   ],
   "agents/worker-rbac.test.ts": [
    "allows write to file within scope.filesWrite",
    "soft-allows write to file outside scope but emits authority violation event",
    "soft-allows write to path outside scope.directories but emits violation",
    "emits AUTHORITY_VIOLATION event on scope violation",
    "allows write to new file within scope.directories",
    "allows src/** writes in self-modifying sprint mode"
   ],
   "agents/worker-result-write-authority.test.ts": [
    "publishes canonical result bytes without dropping provider usage or billing evidence",
    "rejects control characters before publishing or advancing terminal worker state",
    "fails with a typed worker-visible error when canonical attempt identity is absent"
   ],
   "agents/worker-shutdown.test.ts": [
    "should finalize HB as DONE when result exists with DONE selfAssessment (Sprint 134 exact case)",
    "should leave HB untouched when no .result file exists (honest FAILED)",
    "should leave HB untouched when .result has NO_GO selfAssessment",
    "should leave HB untouched when .result has malformed JSON (fail-safe)",
    "should finalize HB as DONE when result has GO_WITH_TECH_DEBT selfAssessment",
    "should leave HB untouched when .result has no selfAssessment field"
   ],
   "agents/worker-verify-coverage.test.ts": [
    "Test #1 — parses real coverage-summary.json (total.lines.pct → number)",
    "Test #2 — rejects (returns null) when coverage-summary.json is missing or malformed",
    "Test #3 — doc/audit task with coverage=null gets unmeasured partial credit + 90 ceiling",
    "Test #4 — Sprint 179 9-task TECH_DEBT pattern does NOT reproduce + code tasks still penalised"
   ],
   "agents/worker-verify-isolation-wire.test.ts": [
    "accepts a passing adapter result exactly once",
    "returns HOLD for a foreign concurrent error without consuming a retry",
    "reports an attributed compiler failure as failed, not a false pass",
    "fails closed when the legacy worker gate receives no admission",
    "returns the exact same dispatcher reference (byte-identical, zero wrapping)",
    "never calls gate.guard when disabled, even for a shell-exec command",
    "dispatches through to the base dispatcher once the broker decides allow",
    "bypasses the gate entirely for a non-risky tool (read_file) even when enabled",
    "returns a structured [approval-denied] string and never calls the base dispatcher",
    "never leaks raw secret-bearing args unmasked onto the submitted request",
    "a never-decided request settles via the gate default fallback (deny) — no second timeout path here",
    "returns null for non-run_bash tools",
    "classifies a plain shell command as shell-exec/medium (the baseline risky class)",
    "classifies git push --force as git-mutation/critical",
    "classifies git reset --hard as git-mutation/critical",
    "classifies plain git commit as git-mutation/high",
    "classifies npm install as network/medium",
    "classifies curl as network/medium",
    "prioritizes git-mutation over network when a command matches both (git push)",
    "supports the `command` arg alias, same as the runner test-sniffer convention",
    "RISKY_APPROVAL_SCOPES is exactly the 3 named registry-risk-consistent classes"
   ],
   "agents/worker-verify-lang.test.ts": [
    "returns tsc + vitest for TypeScript projects",
    "returns py_compile + pytest for Python projects",
    "returns mvn compile + mvn test for Java Maven projects",
    "returns gradle build + gradle test for Java Gradle projects",
    "returns go build + go test for Go projects",
    "returns cargo build + cargo test for Rust projects",
    "returns cmake commands for C/CMake projects",
    "falls back to empty strings for unknown stack",
    "returns make commands for C/Make projects",
    "runs tsc --noEmit for TypeScript projects",
    "runs go build for Go projects",
    "runs mvn compile for Java Maven projects",
    "skips verification when build command is empty",
    "returns errors on compilation failure for non-TS projects",
    "runs vitest with --reporter=verbose for TypeScript projects",
    "runs pytest for Python projects (no --reporter flag)",
    "runs go test for Go projects",
    "runs cargo test for Rust projects",
    "appends scope args to test command",
    "returns failure info on test failure for non-TS projects"
   ],
   "agents/worker-verify.test.ts": [
    "returns TypeScript build and test commands",
    "returns false for empty directories",
    "returns true for docs-only scope",
    "returns false when scope includes src/",
    "returns false when scope includes tests/",
    "returns false for exact \"src\"",
    "returns undefined/false for no scope",
    "parses FAIL lines",
    "extracts summary line",
    "returns empty for clean output",
    "deduplicates failed test names",
    "extracts TS error lines",
    "falls back to first 20 lines if no TS pattern",
    "handles unknown error",
    "handles string error",
    "MAX_TEST_RETRIES is 3",
    "MAX_COMPILATION_RETRIES is 3"
   ],
   "agents/worker.test.ts": [
    "parses valid JSON",
    "throws when file does not exist",
    "throws for invalid JSON",
    "claims PENDING task → CLAIMED + assignedWorker",
    "throws TaskClaimError when status is not PENDING",
    "throws TaskClaimError when already assigned",
    "returns updated Task with status + workerId",
    "writes .plan file",
    "creates directory if it does not exist",
    "creates lock file and returns LockInfo",
    "throws LockError when locked by another worker",
    "is idempotent for same worker (no throw, no write)",
    "creates .locks/ directory if missing",
    "deletes lock file",
    "throws LockError when owned by different worker",
    "is no-op when lock does not exist",
    "returns LockInfo when locked",
    "returns null when not locked",
    "returns proper Heartbeat with all fields",
    "uses explicit sequence parameter",
    "defaults sequence to 0 when omitted",
    "writes .hb file",
    "writes .result file",
    "keeps control characters JSON-safe at the atomic result boundary",
    "sets task status to DONE for selfAssessment DONE",
    "sets task status to NO_GO for selfAssessment NO_GO",
    "sets task status to DONE for GO_WITH_TECH_DEBT",
    "deletes .hb file when it exists (immediate cleanup)",
    "is a no-op when .hb file does not exist",
    "calls cleanup immediately when cleanupDelayMs is 0",
    "schedules delayed cleanup when cleanupDelayMs > 0",
    "updates status (CLAIMED → EXECUTING)",
    "updates updatedAt automatically",
    "returns updated Task",
    "releases all locks owned by worker",
    "does not touch locks owned by other workers",
    "returns 0 when .locks/ does not exist",
    "returns true for file in scope directory",
    "returns false for file outside scope",
    "returns true for file in filesWrite",
    "returns true for nested path within scope",
    "returns false for prefix overlap (src/core-extra/)",
    "allows symlink whose target is within scope",
    "denies symlink whose target is outside scope (sibling project)",
    "denies recursive symlink (ELOOP)",
    "falls through to normal check on ENOENT (new file creation)",
    "works without projectRoot (backward compatible)",
    "returns file content when log exists",
    "returns null when log file does not exist",
    "writes .verify-delta.json with correct fields",
    "defaults testFailCount to 0 when omitted",
    "creates .tasks/ directory if missing",
    "returns VerifyDeltaBaseline when file exists",
    "returns null when file does not exist",
    "returns null on JSON parse error",
    "returns null when baseline file does not exist",
    "recommends DONE when completion >= 80% threshold",
    "recommends GO_WITH_TECH_DEBT for 50-79% completion",
    "recommends NO_GO when completion < 50% (Sprint 137 regression scenario)",
    "penalizes newly introduced test failures",
    "populates endState with actual values and timestamp",
    "uses filesChangedActual as denominator when expectedFilesChangedCount is omitted",
    "initializes in SPAWNING state by default",
    "accepts custom initial state",
    "follows valid transition SPAWNING → STARTING → EXECUTING → TESTING → WRITING_RESULT → DONE → EXITED",
    "throws InvalidStateTransitionError for invalid transition",
    "prevents transition from EXITED (terminal state)",
    "prevents transition from ORPHAN (terminal state)",
    "allows ERROR transition from any active state",
    "allows ORPHAN transition from any active state",
    "canTransition returns correct boolean",
    "isStoppable returns true for active states",
    "isTerminal returns true for terminal states",
    "forceState bypasses validation (for orphan recovery)",
    "toJSON serializes correctly",
    "supports VERIFYING → TESTING → EXECUTING retry loop",
    "createWorkerStateMachine creates and registers a new SM",
    "createWorkerStateMachine replaces existing SM",
    "getWorkerStateMachine creates new SM if not found",
    "removeWorkerStateMachine removes from registry",
    "isWorkerStoppable checks registry correctly",
    "clearWorkerStateRegistry removes all entries",
    "has an entry for every state",
    "EXITED has no valid transitions (true terminal)",
    "ORPHAN has no valid transitions (true terminal)",
    "every active state can transition to ERROR",
    "every active state can transition to ORPHAN",
    "STOPPABLE_STATES contains all active states",
    "STOPPABLE_STATES does not contain terminal states",
    "TERMINAL_STATES contains all terminal states",
    "STOPPABLE_STATES and TERMINAL_STATES are disjoint",
    "worker in DONE state is not stoppable — prevents \"No such container\"",
    "worker in EXITED state is not stoppable",
    "worker in EXECUTING state IS stoppable",
    "cleaned-up worker (no SM in registry) is not stoppable",
    "warns when .plan file is missing",
    "does not warn when .plan file exists",
    "full chain: DONE worker with 100% completion stays DONE through both layers",
    "full chain: Sprint 137 regression — worker claims DONE but 39% completion → NO_GO through both layers",
    "full chain: partial completion 60% — DONE worker downgraded to GO_WITH_TECH_DEBT",
    "full chain: GO_WITH_TECH_DEBT worker with ratio < 50% escalated to NO_GO",
    "verify-delta baseline file is written to correct path with correct JSON schema",
    "computeVerifyDelta returns null when baseline file was never written",
    "exact boundary: completionRatio === 0.8 is treated as DONE (not TECH_DEBT)",
    "newly introduced test failures cause NO_GO even with file changes",
    "emits HEARTBEAT event when sprintId is explicitly provided",
    "emits HEARTBEAT event with auto-detected sprintId when not provided",
    "does NOT emit HEARTBEAT event when sprintId cannot be determined",
    "still writes .hb file even if event stream is unavailable",
    "emits RESULT event with correct payload on DONE result",
    "emits CODE_VERIFY_REQUEST event after result write",
    "emits both RESULT and CODE_VERIFY_REQUEST events",
    "does NOT emit result events when sprintId cannot be determined",
    "emits QUESTION event with question and context",
    "emits QUESTION with empty context when not provided",
    "auto-detects sprintId from sprint-state.json when not provided",
    "does NOT emit when sprintId cannot be determined"
   ]
  },
  "analytics": {
   "analytics/agent-comparison-data.test.ts": [
    "returns empty array for no agents",
    "calculates success rate correctly",
    "calculates totalTasks correctly",
    "preserves agent fields",
    "handles zero total tasks",
    "sorts by successRate descending",
    "sorts by successRate ascending",
    "sorts by agentId alphabetically",
    "sorts by avgDurationMs",
    "does not mutate original array",
    "returns null for empty array",
    "returns agent with highest success rate",
    "breaks tie by totalTasks",
    "returns null for empty array",
    "returns agent with lowest success rate",
    "breaks tie by totalTasks (more tasks = more notable)",
    "formats milliseconds",
    "formats seconds",
    "formats minutes",
    "formats sub-second correctly"
   ],
   "analytics/analytics-data.test.ts": [
    "parses total tasks correctly",
    "parses completed tasks",
    "parses tech debt count",
    "parses no-go count",
    "parses coverage percentage",
    "parses duration in ms",
    "sets sprint id from parameter",
    "returns zero for missing metrics",
    "returns empty array when no sprints directory",
    "loads all sprint files from .brain/sprints",
    "ignores non-sprint files",
    "returns sorted sprint data",
    "builds overview with correct totalSprints",
    "builds overview with correct totalTasks",
    "builds overview with correct overallSuccessRate",
    "builds overview with coverageTrend array",
    "builds overview with sprintIds",
    "returns zero overview for empty data",
    "filters by sprint number range",
    "returns all data for non-numeric date range",
    "formats overview as string",
    "formats overview without coverage when empty"
   ],
   "analytics/skill-heatmap-data.test.ts": [
    "returns empty matrix for no entries",
    "builds symmetric matrix",
    "counts co-usage correctly",
    "includes self-usage (diagonal)",
    "handles single-skill entries",
    "deduplicates skills within an entry",
    "returns null for no entries",
    "finds most common pair",
    "returns null when all entries have single skill",
    "returns pairs above success threshold",
    "excludes pairs below threshold",
    "returns empty for empty entries",
    "returns 0 for zero maxCount",
    "returns 0 for zero count",
    "returns 1 for max count",
    "returns correct intensity fraction",
    "rounds to 2 decimal places",
    "returns empty for empty matrix",
    "returns cells with correct structure",
    "intensity values are between 0 and 1",
    "returns empty for no entries",
    "returns sorted unique skills",
    "deduplicates across entries"
   ],
   "analytics/success-chart-data.test.ts": [
    "returns empty array for no sprints",
    "calculates success rate correctly",
    "handles zero total tasks",
    "preserves coverage rate",
    "preserves task count",
    "handles multiple sprints",
    "returns stable for single data point",
    "returns improving for upward trend",
    "returns declining for downward trend",
    "returns stable for flat data",
    "returns stable for empty data",
    "returns null for empty data",
    "finds sprint with highest success rate",
    "returns first sprint when single entry",
    "returns null for empty data",
    "finds sprint with lowest success rate",
    "returns empty for empty data",
    "returns empty for zero window size",
    "calculates moving average with window 3"
   ]
  },
  "api": {
   "api/agent-surface-readmodel.test.ts": [
    "CLI and MCP surfaces re-export the SAME function object as core (no duplicate builder can drift)",
    "identical ids and counts across the projection on one tree",
    "the API payload shape preserves every pre-S5 field and adds the read-model truth",
    "D4 truth flows through: a capability-less agent is non-routable on every surface"
   ],
   "api/ai-session-tool-allowlist.test.ts": [
    "kind:\"ai\" + disallowed tool → 400, and the disallowed binary is never spawned",
    "kind:\"ai\" + an arbitrary non-allowlisted string tool → 400 (not just known-bad binaries)",
    "kind:\"ai\" + allowlisted tool %s → 201",
    "kind:\"ai\" with no tool field → 201 (default-to-claude path unaffected)",
    "kind:\"shell\" gate is unaffected by the ai-tool allowlist (regression check)"
   ],
   "api/api-approvals.test.ts": [
    "returns empty buckets when no approvals exist",
    "lists pending/approved buckets, maskedArgs-only — no rawArgsRef leak",
    "returns the entry detail with maskedArgs, never raw",
    "uses the core lookup schema for dotted canonical ids",
    "404s for an unknown id",
    "400s for a path-traversal-shaped id",
    "403s with an explanation when approval.api_decide is off (default)",
    "HOLDs a static bearer when no verified approval authority is composed",
    "resolves a path-safe legacy v1 id but never grants it static-bearer authority",
    "400s on an invalid decision body",
    "409s when the approval is already decided",
    "404s when the approval id is unknown"
   ],
   "api/approval-expiry-wire.test.ts": [
    "server.ts source constructs, starts, and stops the driver (RED->GREEN grep-proof)",
    "driver.running is true immediately after createHttpServer()",
    "close() stops the driver — no lingering sweep after shutdown",
    "the setInterval call driven by approvalExpirySweepMs returns an unref\\'d timer",
    "config-absent: falls back to the hardcoded default (byte-identical, no regression)",
    "approval.expiry_sweep_ms from config.json drives the real interval",
    "an explicit opts.approvalExpirySweepMs always wins over config.json",
    "a non-numeric/invalid approval.expiry_sweep_ms is ignored, falling back to default",
    "an already-expired request submitted through api.approvalBroker gets swept to a ttl-expire decision on disk",
    "a request submitted by a DIFFERENT broker instance (other process) IS swept — the born-679 cross-process expiry capability",
    "route is registered: returns 200 + empty page when no history exists",
    "serves settled entries, most-recent-first, maskedArgs-only",
    "honors status/limit/offset query params",
    "400s on an invalid status filter",
    "fails closed: 401 without a valid auth token (auth middleware chain preserved)",
    "routing order: /api/approvals/history is not swallowed by the /api/approvals/:id block"
   ],
   "api/approval-history-endpoint.test.ts": [
    "defaults to status=all, limit=20, offset=0 when nothing is provided",
    "accepts each valid status filter",
    "rejects an unknown status filter",
    "accepts limit at the boundaries (1 and max)",
    "rejects limit outside [1, max] and non-integer limit",
    "rejects a negative or non-integer offset",
    "accepts offset 0 and positive offsets",
    "returns an empty page when the store has no entries",
    "excludes pending entries and includes approved/denied, most-recent-first",
    "filters by a single status category",
    "serializes maskedArgs-only — rawArgsRef is never a field on the entry",
    "paginates: total/hasMore/offset slicing over 3 settled entries with limit=2",
    "orders an overdue-unswept expired entry by expiresAt (no decision yet)"
   ],
   "api/approval-lifecycle-runtime-wire.test.ts": [
    "wires config, startup/scheduled SLA relay, durable ACK and timeout receipt end to end",
    "keeps lifecycle gate-off fail-closed for new governed requests",
    "sweeps unattended confirmation, autonomous and pairing stores on startup",
    "runs broker timeout settle-back and durable federation ACK in the scheduled composition",
    "drains durable settlement with exact authority while creation policy is disabled",
    "close waits for an in-flight reconciliation and restart safely replays it",
    "coalesces overlapping ticks and emits a correlated JSON failure audit"
   ],
   "api/approvals-endpoint.test.ts": [
    "400s a path-traversal-shaped id with the catalog en text, same status/field shape",
    "400s a path-traversal-shaped id with the catalog tr text when DECKENT_LANGUAGE=tr",
    "404s an unknown id with the catalog en text",
    "404s an unknown id with the catalog tr text when DECKENT_LANGUAGE=tr",
    "400s a path-traversal-shaped id with the catalog en text",
    "403s with the catalog en explanation when approval.api_decide is off (default)",
    "403s with the catalog tr explanation when approval.api_decide is off and DECKENT_LANGUAGE=tr",
    "404s an unknown id with the catalog en text",
    "409s an already-decided approval with the catalog en text, interpolating the category",
    "409s an already-decided approval with the catalog tr text when DECKENT_LANGUAGE=tr"
   ],
   "api/attended-approval-oidc.test.ts": [
    "decides only through the shared verified runtime and survives exact restart use"
   ],
   "api/audit-tenant.test.ts": [
    "a real auth-context tenantId is persisted verbatim and marked resolved",
    "the honest local fallback (no auth-context available) is marked fallback",
    "existing content shape is untouched — tenant_source lives outside content",
    "round-trips tenant provenance through a real MemoryStore (legacy insert path)",
    "round-trips tenant provenance through the HMAC chain path and preserves chain integrity"
   ],
   "api/auth-bootstrap.test.ts": [
    "returns null when the URL has no query string",
    "returns null when the query string has no token key",
    "returns the token value verbatim",
    "decodes percent-encoded token values",
    "returns null when the token value is empty",
    "still finds the token when it is not the first parameter",
    "injects the bootstrap script immediately before </head>",
    "JSON-encodes the token to defeat HTML/JS injection via the token value",
    "returns the input unchanged when token is empty",
    "returns the input unchanged when HTML has no </head>",
    "accepts 127.0.0.1 / ::1 / IPv4-mapped IPv6",
    "rejects non-loopback and undefined",
    "prefers explicit config token over env var",
    "falls back to env var when no explicit token",
    "returns null when neither source has a token",
    "accepts a matching ?token= on /api/events when no Authorization header is sent",
    "401s on /api/events when no token at all is provided",
    "403s on /api/events when ?token= is wrong",
    "does NOT honour ?token= on non-SSE routes (Bearer-only)",
    "Bearer header still wins on /api/events when both header and query are present",
    "injects window.__DECKENT_API_TOKEN__ into / for localhost callers",
    "does NOT inject when no token is configured (auth-disabled mode)"
   ],
   "api/auth-me-endpoint.test.ts": [
    "extracts sub, email, name from a valid JWT",
    "extracts preferred_username claim",
    "extracts role from \"role\" claim (admin)",
    "extracts role from namespaced claim (https://deckent.io/role)",
    "omits role when not present in claims",
    "does NOT include the token itself in the response body",
    "returns mode:static for a non-JWT opaque token",
    "returns mode:static for a malformed partial JWT (2 segments)",
    "returns mode:static when no Authorization header (graceful)",
    "does not handle a different path (returns false)",
    "does not handle POST /api/auth/me (returns false)",
    "omits claimsVerified by default — runtime shape unchanged (fail-closed)",
    "stamps claimsVerified:true ONLY when the caller asserts authGateVerified",
    "authGateVerified:false leaves the flag unset (explicit untrusted)",
    "static / no-bearer principal carries the flag only when asserted",
    "SECURITY: a forged {alg:none} token decodes role/tenant but is NEVER auto-trusted"
   ],
   "api/auth-no-disable-flag.test.ts": [
    "localhost caller without Authorization passes when allowLocalhostAutoInject=true",
    "remote caller still requires a token even with localhost auto-inject enabled",
    "invalid Bearer token from localhost is still rejected with 403",
    "DECKENT_API_AUTH_DISABLED=1 remains optional — auto-inject does not require it",
    "DECKENT_API_LOCALHOST_AUTO=1 env var activates the same path as the config flag",
    "auto-inject works when NO token is configured (dashboard dev mode)",
    "isLocalhostRequest recognizes loopback and rejects everything else",
    "localhost auto-inject is OFF by default — no env, no config flag → 401"
   ],
   "api/auth-oidc.test.ts": [
    "valid HS256 JWT passes after the static compare misses (200-path)",
    "static token still authenticates when oidc is configured",
    "expired JWT → 403",
    "wrong issuer → 403",
    "wrong signature (signed with a different secret) → 403",
    "alg:none token → 403 (bypass attempt rejected)",
    "audience mismatch → 403; matching audience passes",
    "missing Authorization header still → 401 (unchanged)",
    "no Authorization header → 401 (JWT is mandatory)",
    "valid HS256 JWT → passes",
    "garbage Bearer value → 403",
    "exempt path passes without any credentials",
    "localhost auto-inject (opt-in) still passes a header-less loopback request",
    "valid RS256 JWT passes (oidc-only, RS256 pinned)",
    "HS256 token is rejected when RS256 is pinned (no algorithm confusion)",
    "no token + no oidc → legacy 401 with the configure-hint message",
    "static token + wrong bearer → 403 with no JWT second chance",
    "static token correct bearer → passes (unchanged)",
    "?token= on an opted-in path still authenticates via the static token",
    "wrong ?token= on an opted-in path → 403 (unchanged)"
   ],
   "api/auth.test.ts": [
    "401 when neither apiToken nor AUTH_DISABLED is set",
    "401 when Authorization header is completely missing",
    "401 when scheme is not Bearer (Basic auth rejected)",
    "401 when Bearer value is empty (header present but no token)",
    "403 when Bearer token is present but does not match",
    "200 when Bearer token matches the configured apiToken",
    "/health is exempt from auth even when no token configured",
    "/api/health is also exempt (the exempt list covers both shapes)",
    "DECKENT_API_AUTH_DISABLED=1 bypasses auth even without apiToken"
   ],
   "api/autonomous-approval-lifecycle.test.ts": [
    "returns typed 409 and cannot allow an expired autonomous request",
    "threads strict tenant authority and refuses a tenant-less API caller"
   ],
   "api/autonomous-endpoint.test.ts": [
    "GET /api/autonomous/status returns pendingCount + backlogSummary + recentAudit",
    "GET /api/autonomous/pending returns an array (empty on a fresh project)",
    "GET /api/autonomous/backlog returns an array (empty on a fresh project)",
    "POST /api/autonomous/approve/<id> on a PARKED trigger returns 200 with the approved id",
    "POST /api/autonomous/reject/<id> on a PARKED trigger returns 200 with the rejected id",
    "POST /api/autonomous/approve/<forged-id> is refused with a typed 403",
    "POST /api/autonomous/reject/<forged-id> is refused with a typed 403",
    "unknown /api/autonomous/* path falls through to 404"
   ],
   "api/chat-a-adapter.test.ts": [
    "calls adapter.send with the user message and returns its reply",
    "does not fall back to the classifier for unrecognized natural-language text",
    "adapter.send is NOT called for keyword commands (backward-compat)",
    "/status returns sprint status via dispatch",
    "/durum is an alias for /status",
    "/recall returns guidance (not null)",
    "/plan returns guidance (not null)",
    "unknown slash command returns an error string (not null)",
    "non-slash input returns null (caller handles via classifier or adapter)",
    "/status handled by dispatch (adapter.send not called)",
    "keyword \"status\" (no slash) routes to classifier, not dispatch, not adapter",
    "/recall reply does not hit adapter.send",
    "natural-lang message returns real adapter reply (not classifier)",
    "/status message returns dispatch result (adapter not consulted)"
   ],
   "api/chat-a-roundtrip.test.ts": [
    "routes a natural-language message to adapter.send(), not classifier",
    "does not call adapter for explicit commands like \"status\"",
    "returns honest error (not Anlamadım) when no adapter is configured",
    "returns honest error when adapter throws",
    "classifies NL messages as non-command",
    "classifies commands as explicit",
    "returns the adapter reply for a natural-language message (not classifier)",
    "keeps \"status\" command on the classifier (adapter bypassed)",
    "returns honest error (not Anlamadım) when adapter throws mid-reply",
    "returns honest error when no adapter is configured"
   ],
   "api/chat-endpoint-adapter.test.ts": [
    "routes a natural-language message to the adapter (not the classifier)",
    "keeps explicit commands on the classifier and never calls the adapter",
    "returns an honest i18n error (en) when no adapter is configured",
    "returns the Turkish honest error when lang=tr",
    "surfaces the adapter failure reason in the honest error",
    "treats an empty adapter reply as an honest error (not silence)",
    "matches status/help commands and slash-prefixed input",
    "does not match natural-language messages",
    "returns the adapter reply for a natural-language message",
    "keeps explicit \"status\" on the classifier (adapter not consulted)",
    "returns an honest error (not \"Anlamadım\") when the adapter throws"
   ],
   "api/chat-handler.test.ts": [
    "answers a status query with the provided sprint status",
    "answers Turkish \"durum\" the same way",
    "returns command guidance for help",
    "returns helpful guidance for an unrecognized message (not a stub error)",
    "handles empty message gracefully with guidance"
   ],
   "api/chat-stream-auth.test.ts": [
    "FIX: /api/chat/stream?token=<valid> authenticates (the 282-001 root is closed)",
    "control: /api/events?token=<valid> still authenticates (no regression)",
    "control: /api/chat/stream WITH Authorization header still authenticates (header path unaffected)",
    "wrong ?token= → 403 forbidden (not a silent pass)",
    "missing token (no header, no ?token=) → 401",
    "the configured token never appears in a rejection body (no leak)",
    "flows data: chunk + done frames over a header-less ?token= request",
    "surfaces an HONEST SSE error event (never silent-empty) when the adapter throws",
    "rejects a wrong query-token with 403 at the real server (no silent stream)",
    "does not leak the token into the streamed body"
   ],
   "api/chat-stream-live-repro.test.ts": [
    "live serve config whitelists /api/chat/stream for query-token auth (server.ts:1208)",
    "control: /api/events query-token authenticates (the SSE query-token mechanism works)",
    "control: /api/chat/stream WITH Authorization header authenticates (header transport unaffected)",
    "REPRO: /api/chat/stream?token=<valid> authenticates — the EventSource transport now works (was 401, DASH-UX-1)",
    "security: wrong /api/chat/stream?token= is rejected 403 (fix does not weaken auth)"
   ],
   "api/chat-stream.test.ts": [
    "forwards each provider stream chunk as a ChatStreamEvent chunk",
    "accumulates chunks and emits a terminal `done` event with the full reply",
    "falls back to `send()` when `stream()` is not implemented",
    "emits an error event (not a throw) when the provider rejects",
    "rejects empty / whitespace-only message with an error event and no provider call",
    "streamToSseLines formats each event as a `data: ${JSON}\\\\n\\\\n` SSE frame"
   ],
   "api/config-editor.test.ts": [
    "GET /api/config/defaults returns default config",
    "GET /api/config returns current project config",
    "GET /api/config returns 404 when config missing",
    "POST /api/config saves valid config and returns merged result",
    "POST /api/config returns 422 on validation error",
    "POST /api/config returns 400 for non-object body",
    "POST /api/config merges with existing config preserving untouched fields",
    "GET /api/config/defaults includes all expected categories",
    "GET /api/config/defaults includes memory_budget field",
    "GET /api/config/defaults includes scan_interval field",
    "GET /api/config/defaults includes rollback_policy field",
    "GET /api/config/defaults includes fix_phase_enabled field",
    "GET /api/config/defaults includes expanded memory and auditor fields",
    "GET /api/config/defaults includes advanced fields"
   ],
   "api/control-mutation-ratchet.test.ts": [
    "governs every former dashboard control endpoint, including the GET-that-writes chat stream",
    "NEVER governs monitoring reads, run-flow, enterprise, auth or the VS Code rpc",
    "POST /api/start|kill/all|cleanup and GET /api/chat/stream answer the honest 403; monitoring GET stays 200",
    "the refusal names the equivalent surfaces and the emergency key (deep-link v1)",
    "with api.control_mutations: true in config the request reaches its normal handler (no 403)"
   ],
   "api/cors-loopback-reflect.test.ts": [
    "reflects loopback http origins on ANY port + the packaged file:// null-origin",
    "never reflects non-loopback / https-spoof / missing origins",
    "a JSON api response reflects the loopback dev-renderer origin (was: hardcoded default port)",
    "a closure-served route carries the header too (was: NO ACAO at all on /api/run-flow/*)",
    "pürüz-5: an UNAUTHENTICATED preflight to closure-served /api/terminal/* answers 204 (never the token-gate 401)",
    "the packaged renderer (`Origin: null`) reflects; a non-loopback origin gets NOTHING"
   ],
   "api/coverage-endpoint.test.ts": [
    "returns 200 with history and budget when no sprint logs exist (empty)",
    "returns coverage data for each sprint log",
    "returns budget.perSprint from config when api mode budget_per_sprint is set",
    "is wired in server — does not 404"
   ],
   "api/dashboard-data-parity.test.ts": [
    "GET /api/status returns sprint shape with phase and progress",
    "GET /api/workers returns empty array when no heartbeat files exist",
    "GET /api/workers returns worker entries from seeded hb files",
    "GET /api/memory returns seeded memory export content",
    "GET /api/agents returns an array of agent objects with required fields",
    "GET /api/debt returns seeded debt export content"
   ],
   "api/directives-endpoint.test.ts": [
    "GET-content: returns existing DIRECTIVES.md verbatim with 200",
    "GET-empty-file-200: returns {content:\"\"} with 200 when DIRECTIVES.md is missing (never 404)",
    "POST-saves: writes DIRECTIVES.md and returns {success, taskCount}",
    "Bearer-less request to /api/terminal/sessions earns 401 (guard must fire to prevent this)"
   ],
   "api/docs-health-endpoint.test.ts": [
    "buckets rows by rank-range × state with counts",
    "responds 200 with rows + heatmap for GET /api/docs/health",
    "returns false for an unrelated URL"
   ],
   "api/endpoints.test.ts": [
    "GET /api/health returns 200 + status payload (no auth required)",
    "GET /health (no /api prefix) is also exempt from auth",
    "GET /api/status returns idle snapshot when nothing is seeded",
    "GET /api/status reflects seeded .dashboard JSON",
    "GET /api/history returns array of parsed sprint logs",
    "GET /api/tasks returns seeded task JSON files",
    "GET /api/memory returns the exports/memory.md view",
    "GET /api/memory returns 404 when export is missing",
    "GET /api/debt returns the exports/debt.md view",
    "GET /api/config/defaults returns a config snapshot",
    "GET /api/config returns 404 when config file is missing",
    "GET /api/config returns the seeded config payload",
    "POST /api/chat replies to the status command",
    "POST /api/chat 400 when body is invalid",
    "returns text/event-stream and a retry directive",
    "401 when no token configured and auth not disabled",
    "401 when Bearer header is missing",
    "403 when Bearer token is wrong",
    "health endpoint is exempt even without token",
    "returns 429 once max requests are exceeded within window",
    "health endpoint shares the same /api/* bucket so /health stays clean",
    "OPTIONS preflight from localhost gets 200 + Allow headers",
    "OPTIONS preflight from disallowed origin returns 403",
    "405 for unsupported HTTP method",
    "GET unknown /api/* path → 404",
    "POST unknown /api/* path → 404",
    "POST with malformed JSON → 400",
    "GET /api/v1/health behaves like /api/health",
    "GET /api/v1/status mirrors /api/status",
    "boots once and reuses the connection for multiple sequential calls",
    "exposes a bound port via baseUrl"
   ],
   "api/ent-rbac-roundtrip.test.ts": [
    "POST role → immediately visible in GET with the same value",
    "PUT update → GET reflects the new permissions immediately",
    "DELETE → GET no longer returns the role, built-ins untouched",
    "custom role sharing a built-in name overrides its persisted permissions on read",
    "POST rule → immediately visible in GET with the same limit",
    "PUT update → GET reflects the new limit immediately",
    "DELETE → GET no longer returns the rule",
    "persisted rule coexists with the live RateLimiter snapshot"
   ],
   "api/ent3-sec-lineage.test.ts": [
    "static token + 'local' event → visible (200, len1)",
    "static token + 'acme' event → NOT visible (fail-closed, 200, len0)",
    "OIDC tenant=acme + acme+globex → only acme visible (200, len1)",
    "OIDC cross-tenant (globex) + acme-only events → empty, no leak (200, len0)",
    "OIDC admin → all events across tenants (200, len2)",
    "no bearer + 'local' event → 'local'-scope (200, len1)",
    "no bearer + 'acme' event → NOT visible (fail-closed, 200, len0)"
   ],
   "api/enterprise-audit-actor.test.ts": [
    "uses JWT sub as audit actor when OIDC bearer is present",
    "uses preferred_username as actor when sub is absent",
    "falls back to \"local\" for a static/opaque bearer (non-JWT)",
    "falls back to \"local\" when no Authorization header is present",
    "falls back to \"local\" when req is undefined (backward compat)",
    "skips audit write when no sprint events file exists (no sprintId)",
    "records correct action for /api/enterprise/rbac route"
   ],
   "api/enterprise-crud.test.ts": [
    "POST create → 201, persists tenant, writes create audit",
    "POST with explicit status/users is honoured",
    "POST duplicate id → 409, no second entry",
    "POST invalid payload → 400 (bad id, missing name, unknown field)",
    "PUT update → 200, merges fields, keeps untouched ones",
    "PUT non-existent tenant → 404",
    "DELETE → 200, removes only the target tenant",
    "DELETE non-existent tenant → 404",
    "RBAC: OIDC viewer → 403, no write, denial audited",
    "RBAC: OIDC admin → 201 allowed",
    "RBAC: no bearer → 403 (fail-secure)",
    "falls through (returns false) for non-tenant routes and GET"
   ],
   "api/enterprise-endpoint.test.ts": [
    "tenants: returns 200 with [] when no tenants exist",
    "tenants: maps config `tenants` entries to the TenantInfo shape",
    "tenants: lists .deckent/tenants/* directories",
    "rbac: returns the three-role matrix with inherited permissions",
    "audit: returns 200 with [] when no events exist",
    "audit: maps audit-channel events to the AuditEntry shape",
    "audit: filters by ?channel= (exact match)",
    "audit: honours ?limit= and keeps the newest entries",
    "rate: returns live limiter state in the RateLimitInfo shape",
    "rate: returns 200 with [] when rate limiting is disabled",
    "rejects unauthenticated requests with 401",
    "returns 404 for an unknown /api/enterprise/ sub-path"
   ],
   "api/enterprise-missions-audit.test.ts": [
    "returns 200 with [] when no mission audit events exist",
    "maps mission audit events to AuditEntry[] with correct fields",
    "route /api/enterprise/missions-audit is handled (not 404)",
    "returns 401 when no authorization header is provided",
    "299-002: static/opaque token → 200 (v1-default localhost gate passes)",
    "300-001: no-bearer (auth-disabled) → 200, full view",
    "300-001: OIDC non-admin tenant=acme + acme+globex seed → 200, only acme entries",
    "299-002: OIDC admin Bearer → 200",
    "300-001: /api/enterprise/audit OIDC non-admin → 200 (279-008 open-contract restored)"
   ],
   "api/enterprise-roles-rate.test.ts": [
    "POST create → 201, persists role in config, writes create audit",
    "PUT /:role update → 200, persists updated permissions",
    "DELETE /:role → 200, removes only the target role",
    "RBAC gate: OIDC viewer → 403, nothing persisted",
    "POST invalid body → 400 (missing permissions, unknown field)",
    "POST create → 201, persists rule in config, writes create audit",
    "PUT /:id update → 200, merges fields",
    "DELETE /:id → 200, removes only the target rule",
    "RBAC gate: OIDC viewer → 403, nothing persisted",
    "POST invalid body → 400 (missing limit, unknown field)"
   ],
   "api/enterprise-routes-complete.test.ts": [
    "all 4 endpoints return 200 with an array when no data is present",
    "tenants: empty project → 200 []",
    "tenants: config entries map to TenantInfo shape",
    "rbac: returns admin/operator/viewer roles with permissions arrays",
    "audit: empty project → 200 []",
    "audit: events map to AuditEntry shape; access:denied → result=denied",
    "rate: rate limiting disabled → 200 []",
    "rate: active limiter returns RateLimitInfo shape entries",
    "all 4 endpoints reject unauthenticated requests with 401",
    "unknown /api/enterprise/ sub-path → 404"
   ],
   "api/evaluate-health-endpoint.test.ts": [
    "counts all 4 tracked channels and ignores unrelated ones",
    "lastEventAt is the max timestamp among tracked-channel events only",
    "honest clean state when no tracked events are present",
    "defaults to 20 when absent, invalid, or non-positive",
    "clamps to the 200 max",
    "honors a valid in-range value",
    "returns [] when recently-works dir is absent",
    "sorts by numeric sprint id descending and slices to N",
    "ignores the long-lived autonomous-events.jsonl stream",
    "returns false for an unrelated URL",
    "returns an honest clean 200 when no recently-works dir exists",
    "aggregates counts across multiple sprint event streams on disk",
    "respects the ?n= window — only scans the N most recent sprint ids"
   ],
   "api/evolution-endpoint.test.ts": [
    "returns empty family tree when no genealogy data exists",
    "returns populated family tree when genealogy.json exists",
    "returns empty array when no agents have been retired",
    "returns retired agent records when .retired directory exists",
    "returns empty array when no agents are registered",
    "returns prompt metrics report shape with required fields"
   ],
   "api/git-routes.test.ts": [
    "GET status → branch + entries; GET diff → the pending hunk (monitoring reads)",
    "GET proposal → clean-note on a clean tree; files + suggested message with a change (+flowId intent)",
    "POST commit {message} → REAL commit lands (stage-all + seal, runs--commit semantics)",
    "empty message → 400; clean-tree commit → honest 500 with git\\'s own error",
    "RATCHET: with the flag off, POST commit answers the honest 403 — the GETs stay open (SURF-7 rule)"
   ],
   "api/gov-minors.test.ts": [
    "wrong opaque bearer denied (403) when a static token IS configured",
    "correct opaque bearer matching the configured token is authorized (201)",
    "no static token configured → existing \"opaque bearer = local owner\" convention is preserved",
    "127.0.0.1 origin with a wrong opaque bearer is still denied (403)",
    "::1 (IPv6 loopback) origin with a wrong opaque bearer is still denied (403)",
    "loopback origin WITH the correct token still succeeds (no unintended lockout)",
    "POST with an empty-string permission → 400, nothing persisted",
    "POST with a whitespace-only permission → 400",
    "POST with an uppercase / malformed permission token → 400",
    "PUT with a malformed permission → 400, existing role untouched",
    "well-formed permission grants (bare word, resource:action, wildcard) are accepted — 201"
   ],
   "api/health-endpoint-identity.test.ts": [
    "keeps the legacy minimal contract (status + timestamp)",
    "adds identity fields for loopback callers (adopt-vs-spawn handshake)",
    "serves the same enrichment on the /api/health alias"
   ],
   "api/health.test.ts": [
    "returns 200 with ok status",
    "returns application/json content-type",
    "returns 404 since /ready endpoint is not yet implemented",
    "returns application/json content-type even for 404"
   ],
   "api/kill-all-endpoint.test.ts": [
    "kills all workers and returns count when workers exist",
    "returns killed=0 when no active workers",
    "returns 500 when killAllWorkers throws",
    "returns 401 when auth is required and no token provided",
    "POST /api/kill/:workerId still works (regression)"
   ],
   "api/kpi-endpoint.test.ts": [
    "seeded DB → 200 with a valid kpis[] carrying a numeric cost_per_sprint value",
    "cross-tenant isolation: a different tenant does NOT see acme KPI values (anti-IDOR)",
    "?tenantId= cannot widen scope for a non-admin caller (IDOR via query param)",
    "admin may scope to a specific tenant via ?tenantId= (cross-tenant admin view)",
    "empty DB (no memory.db) → 200 { kpis: [] }, never 500"
   ],
   "api/kpi-trend-endpoint.test.ts": [
    "seeded multi-sprint DB → 200 with a numeric series ordered old→new",
    "?n= caps the window to the most-recent points (still old→new)",
    "cross-tenant isolation: a different tenant sees no acme points (anti-IDOR)",
    "?tenantId= cannot widen scope for a non-admin caller (IDOR via query param)",
    "admin may scope to a specific tenant via ?tenantId= (cross-tenant admin view)",
    "unknown kpiId → 200 { series: [] }, never 500",
    "empty/absent kpiId → 200 { series: [] }, never 500",
    "empty DB (no memory.db) → 200 { series: [] }, never 500"
   ],
   "api/limits-endpoint.test.ts": [
    "returns false (unhandled) for a non-matching URL",
    "GET /api/limits returns 200 with 3 windows for a full probe",
    "omits the week_fable window when the probe has no Fable line",
    "returns { unavailable: true, windows: [] } with HTTP 200 (never 500) when the probe spawn fails",
    "returns { unavailable: true, windows: [] } when the CLI output is unparseable",
    "verdict is \"ok\" below the warn threshold",
    "verdict is \"warn\" at the warn threshold boundary",
    "verdict is \"block\" at the block threshold boundary",
    "week_fable reuses week_all resetAt (probe has no separate Fable reset field)",
    "unavailable probe formats to { unavailable: true, reason, windows: [] }"
   ],
   "api/live-events.test.ts": [
    "pushes a worker_heartbeat when a .hb file changes",
    "pushes a worker_done when the canonical event tail carries a RESULT line",
    "does not push a worker_done when only a .result file appears",
    "tails the active sprint event-stream JSONL into deckent_event",
    "starts without throwing when .tasks/ and .deckent/ do not exist",
    "survives a throwing onEvent sink — later events still flow",
    "handles a non-task-prefixed hb fixture (smoke path)",
    "emits a named SSE event field so typed pushes do not collide with the snapshot",
    "formats a deckent_event frame with the embedded event"
   ],
   "api/memory-search-endpoint.test.ts": [
    "returns matching results for a valid query",
    "returns empty array for empty query",
    "returns empty array when query has no matching results",
    "finds entries via FTS5 normalized Turkish search",
    "returns empty array when memory DB does not exist",
    "returns the versioned scoped view and a typed HOLD instead of a fake empty result when the DB is absent",
    "upgrades the /api/memory compatibility route to a bounded v1 scoped projection",
    "strict ON: a tenant-less caller is refused with 403 (no all-tenant read)",
    "strict ON: a tenant-claimed caller only sees its own tenant rows",
    "strict OFF: a tenant-less caller keeps the v1 unfiltered read (operator parity)"
   ],
   "api/missions-route.test.ts": [
    "returns { missions: [] } when the store is empty",
    "returns { missions: [] } when autonomous.db does not exist (fail-safe)",
    "returns 2 MissionViews with correct render_as + shape when 2 missions exist",
    "returns 404 for an unknown mission id",
    "returns the MissionView for a known mission id",
    "returns 404 when autonomous.db does not exist (fail-safe)",
    "(a) acme-principal sees only acme missions, not globex",
    "(b) acme-principal GET /api/missions/:id for a globex mission → 404 (no existence leak)",
    "(c-a) claim-siz (no-tenant) principal + acme/globex missions → list empty (fail-closed regression guard)",
    "(c-b) claim-siz + local (untagged) mission → visible (v1-default)",
    "(d) admin-role principal sees all missions regardless of tenant",
    "GET /api/missions resolves 200 through createHttpServer dispatch (not 404)",
    "strict ON: a tenant-less caller is refused with 403",
    "strict ON: a caller WITH a tenant claim is served normally",
    "strict OFF (default): the tenant-less caller is served as before (v1)"
   ],
   "api/nervous-endpoint.test.ts": [
    "GET /api/nervous/status returns panicGuard + detectors + pendingCount",
    "GET /api/nervous/pending returns an array (empty on a fresh project)",
    "POST /api/nervous/accept/<id> returns 200 with the accepted id",
    "POST /api/nervous/reject/<id> returns 200 with the rejected id",
    "unknown /api/nervous/* path falls through to 404",
    "GET /api/nervous/pending surfaces nervous-pending.json approvals (unified hub)",
    "GET /api/nervous/status counts nervous-pending approvals in pendingCount",
    "POST /api/nervous/accept/<id> writes a nervous-ipc approval (executor round-trip)",
    "GET /api/nervous/recommendations returns [] on a fresh project",
    "GET returns only open recommendations by default; ?all=1 includes dismissed",
    "POST /recommendations/dismiss/<id> flips open → dismissed (then excluded)",
    "POST /recommendations/dismiss/<unknown> returns 404"
   ],
   "api/oidc-callback-endpoint.test.ts": [
    "discovers, exchanges, verifies the id_token and returns { token, claims }",
    "sends grant_type, code, code_verifier, client_id/secret and redirect_uri to the token endpoint",
    "rejects an id_token signed by a key the JWKS does not serve (bad signature)",
    "rejects an id_token from a different issuer",
    "rejects an id_token whose aud is not our client_id",
    "rejects an alg:none id_token (the classic bypass)",
    "invalid_request when code is missing (no network touched)",
    "invalid_request when code_verifier is missing",
    "discovery_failed on a non-2xx discovery response",
    "discovery_failed when the discovery doc lacks token_endpoint",
    "token_exchange_failed on a non-2xx token response",
    "id_token_missing when the token response omits id_token",
    "fetch_unavailable when no fetch implementation exists",
    "returns null when the block is absent (default-off)",
    "returns null when enabled is false",
    "returns null when a required field is missing",
    "resolves a complete enabled block",
    "does not handle a different path (returns false)",
    "does not handle GET on the exchange path (returns false)",
    "responds 404 oidc_disabled when dashboard_oidc is off",
    "exchanges and returns { token, claims } on success — without leaking the client_secret",
    "maps an id_token failure to 401 with an honest code",
    "maps a discovery failure to 502"
   ],
   "api/output-stream-live.test.ts": [
    "backfills the full JSONL history on connect",
    "pushes newly appended events live after backfill (backfill + push)",
    "keeps pushing across multiple appends without duplication",
    "returns an empty backfill (no throw) when the log file is absent",
    "tolerates malformed / non-LogEvent lines (never drops the valid ones)",
    "returns 400 when taskId query param is missing",
    "emits done and closes on max-connection-timeout",
    "cleanup stops polling (no further writes after cleanup)"
   ],
   "api/output-stream.test.ts": [
    "writes correctly formatted SSE event",
    "returns false when response is already ended",
    "returns false when response is destroyed",
    "serializes complex payload as JSON",
    "sets Content-Type to text/event-stream",
    "sets Cache-Control to no-cache",
    "sets Connection to keep-alive",
    "parses taskId from query string",
    "parses taskId and sprintId together",
    "returns null when taskId is missing",
    "returns null when there is no query string",
    "returns null when taskId is empty string",
    "trims whitespace from taskId",
    "leaves sprintId undefined when not provided",
    "handles req.url being undefined",
    "returns true for GET /api/output-stream",
    "returns true without query string",
    "returns false for other paths",
    "returns false for POST method",
    "returns false for partial path match",
    "returns 400 when taskId is missing",
    "sends snapshot event on connect when collector has data",
    "sends empty snapshot when worker not registered",
    "sends output event when new lines arrive during polling",
    "sends done event when worker leaves active workers list",
    "closes connection when client disconnects (req close event)",
    "closes connection after maxConnectionMs",
    "returned cleanup function stops polling"
   ],
   "api/planner-evidence-routes.test.ts": [
    "${route.path} returns HTTP 409 for %s without raw evidence",
    "preserves the existing proposal response for an unknown error"
   ],
   "api/process-endpoint.test.ts": [
    "POST /api/process/submit (erp.write → parks) returns an executionId + pending-approval",
    "GET /api/process/status/<id> reflects the submitted (parked) entry",
    "POST /api/process/submit without a description → 400",
    "returns a structured provider-authority HOLD for an auto task without HTTP 500",
    "GET /api/process/status/<unknown> → 404",
    "SECURITY: client-supplied actor/tenant in the body is IGNORED (server-derived)",
    "SECURITY (anti-IDOR): a cross-tenant OIDC bearer gets 404 on another tenant\\'s entry (no existence leak)",
    "SECURITY (fail-closed): claim-siz (no-tenant) principal cannot access another-tenant entry → 404",
    "SECURITY (fail-closed): claim-siz principal sees local (untagged) entry → 200 (v1-default)",
    "SECURITY (tenant-stamp): submit stamps the durable entry tenant from the OIDC claim, not the client body"
   ],
   "api/provider-authority-front-door.test.ts": [
    "retires direct start (410) and HOLDs plan before context read, planner, or detached spawn",
    "HOLDs RunFlow propose before planner or flow persistence",
    "HOLDs an approved RunFlow before START_REQUESTED, run handle, or detached spawn",
    "writes one exact unattended Brain HOLD event per rejected ingress"
   ],
   "api/rate-limit.test.ts": [
    "allows requests up to the configured maxRequests",
    "returns 429 with JSON error body on the bucket-overflow request",
    "keeps blocking subsequent requests after the first 429 (sticky in same window)",
    "rate-limiter only scopes /api/* routes — /health stays open",
    "rateLimit: 0 disables the limiter entirely",
    "429 response carries strict security headers (no leaks)",
    "rateLimitExemptLoopback: true never 429s loopback callers (Sprint 269 dashboard fix)"
   ],
   "api/reactive-endpoint.test.ts": [
    "appends a normalized webhook event to the durable inbox",
    "rejects a body with no event name (400)",
    "appends multiple events as JSONL (append-only)",
    "returns 401 when no bearer token is provided (auth-gate proof)"
   ],
   "api/request-logging.test.ts": [
    "does not produce structured request logs (feature not implemented)",
    "server responds to /api/history without request logging",
    "does NOT log static file routes (non-/api/ paths)",
    "createHttpServer accepts legacy positional args"
   ],
   "api/routing-endpoint.test.ts": [
    "returns distribution data with correct shape when learnings.json exists",
    "returns empty state when learnings.json does not exist",
    "returns 401 when no auth token is configured and auth is not disabled",
    "detects imbalance and populates warnings when a single agent exceeds 80%"
   ],
   "api/rpc-endpoint.test.ts": [
    "401s without a bearer token — auth chain is not weakened for this route",
    "answers an unknown method honestly (UNKNOWN_METHOD), not a 500",
    "answers a write method left unwired this slice with METHOD_NOT_IMPLEMENTED (honest unsupported)",
    "rejects a malformed RPC envelope with 400 (never reaches the dispatcher)",
    "limits.get round-trips a schema-valid result via a fake spawn (no real claude binary)",
    "approval.list round-trips against a real ApprovalBroker fixture, filtered by scopeId",
    "session.list + run.status round-trip against a fake terminal-backend session lifecycle",
    "run.status reports \"pending\" honestly for an unknown runId (no fabricated verdict)"
   ],
   "api/rpc-write-handlers.test.ts": [
    "throws when requester is blank",
    "throws when requester is whitespace-only",
    "succeeds and registers both write methods when requester is present",
    "names session.resume as the one write method still left unwired",
    "keeps approval.decide METHOD_NOT_IMPLEMENTED when approval.api_decide is off",
    "rejects an invalid bearer when approval.api_decide is on",
    "spawns node with the resolved entry.js + tokenized argv, detached + windowsHide, stdio to the log fd",
    "quote-aware tokenization keeps a quoted multi-word argument as one argv element",
    "honors params.cwd for the child cwd, but keeps the log dir under deps.projectRoot",
    "merges caller-supplied env but the audit identity (DECKENT_RPC_REQUESTER) always wins",
    "writes a log file under <projectRoot>/.deckent/recently-works/ that exists on disk",
    "falls back to a generated runId when the spawn handle reports no pid",
    "rejects a blank command without ever calling spawn",
    "never invokes the real node:child_process spawn (no spawnFn override) unless explicitly told to",
    "routes decisions through the OIDC authority ingress, never ApprovalBroker.decide directly",
    "rejects a request with a blank decidedBy without entering authority",
    "pins flag-open/invalid-token behavior as unauthorized at the ingress",
    "fails closed when no approval authority/token context was supplied"
   ],
   "api/run-flow-api-composition.test.ts": [
    "drives the full chain through the REAL createHttpServer dispatch (auth-gate + route table), not a local mini-server",
    "reject path composes end-to-end too: propose -> reject -> CANCELLED state",
    "tenant isolation still holds when routed through the real (mock-auth) dispatch",
    "a live subscriber on the REAL flowId (from an actual propose() response) observes the chain events in publish order, shape-consistent with the REST responses",
    "global-broadcast guard still holds inside the composed server: a different flowId does not see this flow\\'s events"
   ],
   "api/run-flow-durability-1c.test.ts": [
    "restart loses NOTHING: GET re-resolves from the durable event fold",
    "approve AFTER restart builds the StoredApprovedSnapshot from the durable plan record",
    "every durable event is live-published to the SSE layer (coordinator onEvent wire)",
    "duplicate decision is command-idempotent across a restart (no double snapshot line)",
    "GET run-flow SSE with correct ?token= authenticates (EventSource lane)",
    "GET with WRONG ?token= is 403 (constant-time mismatch shape)",
    "POST can NEVER authenticate via query-token — even on an allowlisted prefix",
    "non-allowlisted path gets no query-token lane at all",
    "GET /api/run-flow/list enumerates durable flows with state + intent",
    "POST /:id/start on an APPROVED flow spawns detached and records RUN_STARTED (fake spawn via env-less dry assertion)",
    "POST /:id/start on a non-APPROVED flow is a typed 409",
    "POST /:id/cancel → durable CANCELLED; terminal flow rejects further decisions (409)",
    "cancel on a RUNNING flow closes the record and says the process is lifecycle-owned",
    "SSE frames carry id: <sequence> and ?after=N backfills the durable gap",
    "a foreign tenant's GET/decision/cancel/start all see 404 — indistinguishable from unknown flowId",
    "list is tenant-scoped: the foreign flow is simply ABSENT (no leak, no error)",
    "the owner tenant still sees and drives its own flow (control)"
   ],
   "api/run-flow-event-stream.test.ts": [
    "matchRunFlowEventStream extracts the flowId segment",
    "matchRunFlowEventStream returns null for unrelated or sibling paths",
    "isValidRunFlowId allows the safe charset and rejects traversal",
    "resolveReplayCursor: Last-Event-ID header WINS over the frozen ?after= URL cursor (SURF-6 reconnect-dedupe)",
    "formatRunFlowEventFrame emits a named SSE event field and preserves the versioned shape",
    "a published event reaches ONLY subscribers of that exact flowId",
    "unsubscribe stops further delivery to that listener only",
    "publishing to a flowId with no subscribers is a silent no-op (never throws)",
    "a faulting subscriber does not break fan-out to other subscribers of the same flow",
    "flag-off (terminal.run_flow_v2 unset) → 404, honest disabled message",
    "flag-on: streams a published event to its own flowId and preserves the versioned shape",
    "global-broadcast guard: a DIFFERENT flowId does not receive an event published to another flow",
    "rejects an invalid flowId (dot) with 400"
   ],
   "api/run-flow-jobs-join.test.ts": [
    "maps COMPLETE/FAILED records by flowId; skips running/corrupt/flowId-less",
    "missing jobs dir → empty map (never throws)",
    "GET /:flowId and the list upgrade a jobs-closed do-origin flow; the durable store stays untouched",
    "a FAILED jobs record surfaces state + failureReason",
    "a LIVE durable flow (real event log, no jobs record) is untouched by the join",
    "answers the honest not-a-git-repo shape on a bare test root (tenant-guarded 200)",
    "approve on a jobs-COMPLETED do-origin flow refuses (no live preview / not awaiting)"
   ],
   "api/run-flow-routes.test.ts": [
    "POST /propose, GET /:flowId, GET /:flowId/preview, POST /:flowId/decision all 404",
    "rejects a body with no intentSummary (400)",
    "drives COLLECTING -> AWAITING_APPROVAL with a REAL plan preview (compiler+preview-service+reducer)",
    "derives tenant/actor from the verified bearer, never from the request body",
    "a planner failure surfaces as an honest 502, nothing persisted",
    "404 for an unknown flowId",
    "400 for a syntactically invalid flowId",
    "returns the full context / the live preview for a known flow",
    "tenant isolation: a caller from a different tenant gets 404 (no existence leak)",
    "400 for an invalid decision body",
    "404 for an unknown flowId",
    "approve -> APPROVED with a CAS-matched approvedSnapshot, persisted to run-flow-store",
    "reject -> CANCELLED",
    "a second decision on an already-CANCELLED flow -> 409 (typed transition error, not a silent no-op)",
    "tenant isolation: a different-tenant caller cannot decide someone else\\'s flow",
    "gate ON: an unverified principal is refused BEFORE the flow is created",
    "gate OFF (default): the same unverified principal still creates the flow (v1 behaviour)",
    "strict ON: a tenant-less caller is refused with 403, no flow created",
    "strict ON: a caller WITH a tenant claim proceeds normally",
    "strict OFF (default): the tenant-less caller still lands in local (v1 behaviour)",
    "strict ON refuses a tenant-less caller, strict OFF keeps local (resolver level)",
    "a malformed config fails SOFT to the permissive default (never a silent hard-deny)",
    "honours the GLOBAL layer when the project says nothing",
    "the PROJECT layer wins over the global one, in both directions",
    "a malformed layer says NOTHING — the next layer decides, no silent flip"
   ],
   "api/security-headers.test.ts": [
    "API responses include Content-Type: application/json",
    "API responses include Access-Control-Allow-Origin",
    "CORS preflight (OPTIONS) returns 200 with CORS headers for localhost",
    "CORS preflight rejects non-localhost origins",
    "unknown API routes return 404 with JSON error",
    "unsupported methods return 405",
    "security headers (X-Content-Type-Options, X-Frame-Options, CSP, HSTS) are present"
   ],
   "api/serve-daemon-meta.test.ts": [
    "writes the handshake file with self-stamped identity fields",
    "writes with mode 0600 regardless of umask (token-carrying file)",
    "leaves no .tmp residue (atomic temp+rename)",
    "round-trips through readServeDaemonMeta",
    "read returns null when the file is absent",
    "read returns null on garbage and on half-formed JSON (never a partial meta)",
    "clear removes the file and never throws (absent file included)",
    "optional tokens stay absent when the daemon runs auth-less/terminal-less"
   ],
   "api/serve-dashboard-missing.test.ts": [
    "renderDashboardNotBuiltPage is valid HTML naming the build command and the live API",
    "serves the honest page (200, not a bare 404) on the root path when the bundle is missing",
    "serves the honest page on a deep-link route too (SPA path, bundle missing)",
    "keeps the JSON API working while the dashboard bundle is missing"
   ],
   "api/serve-localhost-token-inject.test.ts": [
    "mints a token and serves /api/status=200 when none is configured",
    "emits a 64-char hex token (randomBytes(32).toString(\"hex\"))",
    "does not override an explicitly configured token",
    "does not mint when DECKENT_API_AUTH_DISABLED=1"
   ],
   "api/serve-spa-token-inject.test.ts": [
    "injects the token on the root path /",
    "injects the token on a deep-link SPA route (/enterprise)",
    "injects the token on a /status deep-link refresh",
    "deep-link injected token authenticates /api/status (200)",
    "serves deep-link HTML without a token when auth is disabled",
    "does not inject on a remote bind with no configured token",
    "explicit apiToken wins over DECKENT_API_TOKEN env",
    "DECKENT_API_TOKEN env is used when no explicit token is passed",
    "config api_auth_token is used when no explicit/env token exists",
    "streams chunk + done SSE events when a chat adapter is bound",
    "emits an honest SSE error event when the adapter throws",
    "serves /api/output-stream on the first request before workers attach"
   ],
   "api/serve-token-inject.test.ts": [
    "injects window.__DECKENT_API_TOKEN__ into / for a localhost caller",
    "injects the exact finalToken passed to createHttpServer (param path)",
    "injects DECKENT_API_TOKEN when no explicit token is configured (env path)",
    "does not inject when no token is configured (auth-disabled no-op)",
    "non-localhost remoteAddress fails the isLoopbackRemote gate"
   ],
   "api/serve-token-matrix.test.ts": [
    "terminal-on + no-token: mints both terminalToken and API finalToken",
    "terminal-off + no-token: mints only API finalToken (no terminalToken)",
    "explicit-token: serves the given token verbatim, no auto-mint",
    "auth-disabled: skips auto-mint — HTML carries no API token"
   ],
   "api/server-auth.test.ts": [
    "returns config token when provided",
    "falls back to DECKENT_API_TOKEN env var",
    "returns null when neither config nor env set",
    "config token takes precedence over env var",
    "returns missing when no Authorization header",
    "returns missing when scheme is not Bearer",
    "returns ok when token matches",
    "returns invalid when token does not match",
    "returns 401 when no token configured (secure by default)",
    "passes through when DECKENT_API_AUTH_DISABLED=1",
    "passes through for exempt paths",
    "returns 401 when no token provided",
    "returns 403 when wrong token provided",
    "returns 200 when correct token provided",
    "health endpoint bypasses auth",
    "uses DECKENT_API_TOKEN env var when no explicit token",
    "POST endpoints also require auth when token configured",
    "returns 401 when no token configured (secure by default)",
    "bypasses auth when DECKENT_API_AUTH_DISABLED=1",
    "rejects CORS preflight from non-localhost origin",
    "includes security headers in API responses"
   ],
   "api/server-body-schemas.test.ts": [
    "returns 400 for invalid body (autoApprove as string)",
    "valid empty body passes the schema and reaches the retired-route contract (410)",
    "returns 400 for invalid mode value",
    "returns 200 for valid mode:ai",
    "returns 400 for missing content field",
    "returns 400 for empty content",
    "returns 400 for array body",
    "returns 200 for valid workerId with empty body",
    "returns 400 for invalid workerId format"
   ],
   "api/server-edge.test.ts": [
    "produces exactly 64 hex characters",
    "only contains lowercase hex characters",
    "generates statistically unique tokens across many calls",
    "token is suitable as Bearer value in Authorization header",
    "consecutive calls return different values",
    "returns 401 when no token configured (secure by default)",
    "returns 401 when Authorization header is absent and token is configured",
    "returns 403 when wrong token value is supplied",
    "returns 401 when auth scheme is Basic instead of Bearer",
    "returns 401 when Bearer header has no value",
    "returns 401 when Authorization header is empty string",
    "allows POST with correct token",
    "GET routes require auth when token is set",
    "GET responses include a localhost CORS header",
    "OPTIONS preflight reflects localhost origin in CORS header",
    "non-localhost origin falls back to default localhost CORS header",
    "missing origin header gets no CORS header (same-origin/CLI callers need none)",
    "OPTIONS preflight with non-localhost origin returns 403 CORS reject",
    "parses body sent in multiple chunks",
    "returns empty object when body is empty string",
    "rejects with error on stream error",
    "rejects on malformed JSON with trailing garbage",
    "parses deeply nested JSON body",
    "responds with correct SSE headers",
    "SSE response includes CORS header",
    "multiple SSE clients can connect simultaneously",
    "SSE client disconnect removes client from set",
    "returns 400 with correct JSON error body for invalid JSON POST",
    "returns 400 for /api/start with non-boolean autoApprove",
    "returns 401 with correct error message format",
    "returns 404 for completely unknown route",
    "returns idle state for /api/status when dashboard does not exist",
    "returns idle state for /api/status when dashboard contains invalid JSON",
    "returns 405 for unsupported HTTP methods",
    "returns 500 when plan throws an error",
    "returns 500 when killWorker throws",
    "returns 400 for /api/kill with empty workerId",
    "returns 400 for /api/kill with path-traversal workerId",
    "returns 500 when set-directives writeFileSync throws",
    "returns error JSON with \"error\" field on all error responses",
    "accepts port number as second argument",
    "accepts options object as second argument",
    "close() resolves and stops accepting connections"
   ],
   "api/server-endpoint-wire.test.ts": [
    "GET /api/limits dispatches to registerLimitsRoute",
    "GET /api/evaluate-health[?n=] dispatches to registerEvaluateHealthRoute with projectRoot",
    "fails closed: 401 without a valid auth token for both new routes (auth middleware chain preserved)"
   ],
   "api/server-security.test.ts": [
    "allows requests within limit",
    "blocks requests exceeding limit",
    "tracks IPs independently",
    "resets after window expires",
    "returns 429 when rate limit exceeded",
    "disables rate limiting when rateLimit=0",
    "returns 413 for oversized body",
    "accepts normal-sized body",
    "uses deepMerge instead of shallow merge",
    "auto-generates token when autoGenerateToken=true",
    "POST requires auth when autoGenerateToken is set",
    "/api/v1/status routes to /api/status",
    "/api/v1/history routes correctly",
    "reflects localhost origin",
    "rejects non-localhost origin with 403",
    "sends retry field on SSE connect",
    "legacy start never spawns or tracks jobs — 410 on every call, job lookups 404"
   ],
   "api/server-tenant-scope-wire.test.ts": [
    "A1 lineage: OIDC non-admin (globex) + acme-only events → scoped empty",
    "A1 lineage: OIDC admin → full chain visible",
    "A2 missions-audit: OIDC non-admin (globex) + acme-only events → scoped empty",
    "remote bind (0.0.0.0): a denied command is blocked (guard enforces)",
    "loopback bind (127.0.0.1): the same command passes through (owner-trusted)"
   ],
   "api/server.test.ts": [
    "starts and listens on given port",
    "returns idle state when dashboard file missing",
    "returns dashboard JSON when file exists",
    "returns 404 when no sprints dir",
    "returns 404 when sprints dir empty",
    "returns latest sprint log",
    "returns empty array when no sprints",
    "returns all sprint logs",
    "returns SSE headers",
    "returns 404 when config missing",
    "returns config JSON when file exists",
    "returns doctor checks",
    "returns 404 when memory file missing",
    "returns memory content when the export exists",
    "returns 404 when debt file missing",
    "returns debt content when file exists",
    "returns empty array when .tasks/ does not exist",
    "returns task list when task files exist",
    "returns 200 with CORS headers",
    "returns 200 with a reply for a help message (no longer a 404 stub)",
    "returns 400 when message is missing",
    "returns 410 LEGACY_START_RETIRED with the canonical run-flow path",
    "repeated starts stay 410 — no sprint or job state accumulates",
    "returns plan JSON with the runFlow authority surface",
    "passes mode param to planRunFlow previewOptions",
    "response includes reasoning when present",
    "kills worker and returns success",
    "returns 500 when kill fails",
    "writes directives and returns task count",
    "returns 400 when content is missing",
    "returns success with file counts when no active tasks",
    "returns 409 when sprint is active (EXECUTING task exists)",
    "merges and writes config",
    "creates config from scratch when none exists",
    "mode field: POST economic → GET returns economic",
    "language field: POST tr → GET returns tr",
    "nested key: POST { git: { auto_commit: true } } → GET returns git.auto_commit true",
    "memory_budget 900: writeFileSync called with memory_budget: 900",
    "invalid config → returns 422 with VALIDATION_ERROR code",
    "deepMerge preserves existing fields — only sent keys change",
    "round-trip with 5 fields: POST → GET values match",
    "nested key round-trip: skill_routing sub-keys merged correctly",
    "nested key round-trip: modes.performance.max_workers preserved",
    "returns 404 when no active job",
    "legacy start mints no jobId — response carries only the retirement contract",
    "job lookups after a retired start remain 404 (no phantom job records)",
    "returns task and log when both exist",
    "returns null log when no log file exists",
    "returns 404 for nonexistent task",
    "returns 405 for unsupported methods",
    "returns 404 for unknown GET routes",
    "returns 404 for unknown POST routes",
    "returns 400 for invalid JSON POST body",
    "returns idle state when dashboard file is invalid JSON",
    "serves static files with correct MIME type",
    "serves index.html for root path",
    "falls back to index.html for SPA routes",
    "returns 404 when no static dir and unknown route",
    "API routes take priority over static files",
    "serves the honest dashboard-not-built page (200) when static dir set but the bundle is missing (DASH-OPS-1)",
    "returns 403 for path traversal attempts",
    "returns 400 for /api/start with invalid body",
    "returns 400 for /api/plan with invalid body",
    "returns 400 for /api/set-directives with empty content",
    "returns 400 for /api/kill with invalid workerId chars",
    "returns 400 for /api/config with non-object body",
    "response includes sprint, agents, progress, alerts top-level fields",
    "progress sub-field has done, active, blocked, total fields",
    "sprint sub-field has id and phase fields",
    "returns flat config object without extra wrapping",
    "returns defaults config object",
    "each sprint entry has id field",
    "content field is a string, not an object",
    "response has boolean ok field and checks array",
    "each check item has name, passed, message fields",
    "logs an info message to stderr when no token is configured",
    "does not log the info message when a token is configured",
    "info message is logged exactly once at server creation, not per request",
    "info message includes how to configure auth token",
    "parses valid JSON body",
    "returns empty object for empty body",
    "rejects on invalid JSON",
    "calls onChange after debounce on file change",
    "debounces rapid changes",
    "accepts valid token (exact match)",
    "rejects wrong token",
    "rejects missing Authorization header",
    "handles shorter token without crashing (no timingSafeEqual length error)",
    "handles longer-than-expected token without crashing",
    "rejects wrong Bearer scheme (Basic instead of Bearer)",
    "returns 401 when no token configured (secure by default)"
   ],
   "api/sprint-inspector-endpoints.test.ts": [
    "lists the current authority run and archived run projections",
    "serves the legacy live keys plus canonical lifecycle authority",
    "streams an initial full live snapshot with the legacy active key",
    "uses sinceRevision as the observer cursor and skips the duplicate snapshot",
    "disposes the observer and ping interval when the client closes",
    "rejects invalid sinceRevision=%s with a typed 400 and no stream",
    "caps task plan text and rejects an invalid task id without reading outside the fixture",
    "keeps a stale raw task projection distinct from its exactly associated canonical run",
    "passes a bounded tailLines override through to the task log tail",
    "rejects invalid tailLines=%s with a typed 400",
    "returns an honest 404 for an unknown valid task id"
   ],
   "api/sprint-job-runner.test.ts": [
    "returns a jobId with job- prefix",
    "calls spawn with detached:true and stdio:ignore",
    "calls unref() on the child process so serve event loop is not blocked",
    "handles spawn error gracefully — returns jobId even when spawn throws"
   ],
   "api/sse.test.ts": [
    "returns 200 + Content-Type: text/event-stream",
    "first chunk contains the SSE retry: directive",
    "exposes cache-control: no-cache + connection: keep-alive headers",
    "fans out the seeded dashboard snapshot to a newly connected client",
    "SSE endpoint is behind the auth gate — 401 without token",
    "client disconnect (AbortController) releases the SSE slot cleanly"
   ],
   "api/status-reconcile.test.ts": [
    "returns idle when sprint-state is COMPLETE, even if dashboard shows EXECUTE",
    "overrides dashboard lifecycle/progress with the persisted read-model revision",
    "returns dashboard data unchanged when sprint-state is ACTIVE",
    "preserves canonical lifecycle while holding an unavailable read model",
    "returns idle when sprint-state file is missing (no active sprint)",
    "does not let a display-only terminal dashboard override persisted run authority",
    "returns idle when sprint-state is ABORTED"
   ],
   "api/task-output-stream.test.ts": [
    "writes canonical public SSE bodies with no capability/content or task-DONE inference",
    "passes only derived project/principal plus validated exact selectors to the shared read service",
    "projects a rich authenticated principal to the custody caller's exact own-key contract",
    "rejects malformed, mismatched, and policy-over-limit requests before service construction",
    "surfaces foreign, ambiguous, and not-dispatched custody states without observation",
    "returns a machine unavailable code for unreadable or invalid view policy without service construction",
    "awaits SSE drain and closes only the observer view on client disconnect",
    "keeps an observer closure distinct from task completion",
    "selects the registered shared projection route for both legacy URL shapes"
   ],
   "api/tenant-ws-strict-e2e.test.ts": [
    "strict ON: the terminal WS upgrade is refused with the tenant-scope close code",
    "strict OFF: the same valid token opens the shell — v1 behaviour intact"
   ],
   "api/terminal/audit-integrity.test.ts": [
    "(a) computeAuditHmac is deterministic",
    "(b) chain link integrity: changing prevHmac changes digest",
    "(c) verifyAuditChain returns ok on clean chain",
    "(d) verifyAuditChain detects UPDATE tamper",
    "(e) loadOrCreateAuditKey generates 32-byte hex key on first call"
   ],
   "api/terminal/audit.test.ts": [
    "records a structured event and never stores raw output",
    "serializes structured fields only — drops anything not in AuditEvent shape",
    "handles events without optional fields",
    "persists tenant_id round-trip through real MemoryStore",
    "additive tenant_id migration is idempotent across reopens"
   ],
   "api/terminal/auth-provider-jwks.test.ts": [
    "verifyAsync accepts a valid RS256 token resolved via JWKS (mock fetch, one call)",
    "verifyAsync rejects a token from the wrong issuer",
    "verifyAsync rejects a token with the wrong audience",
    "verifyAsync rejects an unknown kid (fail-closed, rotation re-fetch exhausted)",
    "verifyAsync rejects HS256 tokens outright (RS256 algorithm pinning)",
    "verifyAsync rejects alg:none tokens (classic bypass attempt)",
    "verifyAsync rejects an expired token (injected clock)",
    "verifyAsync rejects undefined and empty credentials",
    "sync verify ALWAYS returns false — even for a token verifyAsync accepts",
    "DELIBERATELY ignores DECKENT_API_AUTH_DISABLED — terminal is never bypassed",
    "constructor rejects empty issuer/jwksUrl without echoing key material",
    "verifyAsync fails closed (false, no throw) when the JWKS fetch errors",
    "awaits verifyAsync when defined — async accept wins over an always-false sync verify",
    "denies with 4401 when verifyAsync resolves false — no session, no auth.ok",
    "falls back to sync verify when verifyAsync is absent — LocalTokenAuthProvider regression",
    "treats a REJECTING verifyAsync as deny (fail closed, no crash)",
    "denies a JwksAuthProvider-backed gateway upgrade presenting the local token, accepts a real JWT"
   ],
   "api/terminal/auth-provider-mtls.test.ts": [
    "interface contract: verifyClientCert is optional on AuthProvider",
    "LocalTokenAuthProvider: verifyClientCert is undefined (no-op)",
    "custom MtlsAuthProvider: verifyClientCert returns TenantId or null"
   ],
   "api/terminal/auth-provider-oidc.test.ts": [
    "accepts a valid HS256 token (real HMAC signature)",
    "rejects a token from the wrong issuer",
    "rejects a token with the wrong audience",
    "rejects a token signed with the wrong secret",
    "rejects a tampered payload (signature no longer matches)",
    "rejects undefined, empty, and malformed credentials",
    "rejects an expired token (exp in the past, injected clock)",
    "honors the injected clock — same token flips to expired when time advances",
    "rejects an alg:none token (signature bypass attempt)",
    "DELIBERATELY ignores DECKENT_API_AUTH_DISABLED — terminal is never bypassed",
    "throws on construction with empty issuer or key (fail-fast)",
    "works without an audience constraint when audience is omitted"
   ],
   "api/terminal/auth-provider.test.ts": [
    "accepts the correct token",
    "rejects a wrong token",
    "rejects empty/undefined",
    "is independent of DECKENT_API_AUTH_DISABLED"
   ],
   "api/terminal/e2e-reattach.test.ts": [
    "ws1 attach→input→disconnect → mgr.write MARKER_ONE+TWO while detached → ws2 reattach replays both"
   ],
   "api/terminal/server-routes.test.ts": [
    "POST /api/terminal/sessions → 201; GET lists; DELETE → 200",
    "terminal token is generated even when API auth is disabled (spec §1c.2)",
    "disables terminal routes when cfg.terminal.enabled === false",
    "injects window.__DECKENT_TERMINAL_TOKEN__ into index.html for 127.0.0.1",
    "does NOT inject when terminal is disabled"
   ],
   "api/terminal/session-backend.test.ts": [
    "spawns a process, streams output, and reports exit"
   ],
   "api/terminal/session-manager.test.ts": [
    "creates a session and buffers output (bounded ring)",
    "detach does NOT kill; kill is explicit",
    "enforces maxSessions",
    "idle reaper kills idle shell but exempts deckent kind"
   ],
   "api/terminal/ws-gateway-mtls.test.ts": [
    "denies upgrade when verifyClientCert returns null (presented cert rejected)",
    "accepts + bridges when verifyClientCert returns a TenantId",
    "ignores a presented cert when no verifier is wired (token auth stands)",
    "treats a throwing verifyClientCert as deny (fail-closed)"
   ],
   "api/terminal/ws-gateway.test.ts": [
    "rejects upgrade with invalid subprotocol token — no session spawned",
    "accepts valid token, attaches a session, replays buffer + streams output"
   ],
   "api/terminal-audit-wire.test.ts": [
    "Smoke: POST /api/terminal/sessions on the real served path persists a chain-linked audit row",
    "persists events but skips the HMAC chain when terminal_audit_integrity.enabled=false",
    "MemoryStoreAuditSink.insert() generates its own id — safe even without an integrity config",
    "MemoryStoreAuditSink chain-links rows when TerminalAudit is given an integrity config",
    "config absent: maxSessions default of 10 is preserved (byte-identical)",
    "terminal.maxSessions=2: the 3rd POST /api/terminal/sessions is rejected (409)",
    "terminal.idleTimeoutMs is read from config: reapIdle() kills a session past the configured timeout",
    "terminal.allowShellKind=false: shell sessions are rejected (403), other kinds unaffected",
    "config absent: shell sessions are still allowed by default (byte-identical)",
    "terminal.scrollbackBytes is read from config: the ring buffer truncates to the configured size",
    "terminal.bind fallback flows into the actual server bind (no separate declared-vs-actual bind)",
    "an explicit host always wins over terminal.bind config (no API-contract change)",
    "config absent: server still binds to 127.0.0.1 by default (byte-identical)",
    "terminal.outboundDailyQuotaBytes is read from config: exceeding it closes the WS with outbound_kill"
   ],
   "api/terminal-shell-fallback.test.ts": [
    "POSIX: honors $SHELL when set (existing behavior preserved)",
    "POSIX: falls back to sh (not bash) when SHELL is unset",
    "POSIX: falls back to sh when SHELL is an empty string",
    "win32: uses ComSpec when set",
    "win32: honors a ComSpec override to powershell.exe",
    "win32: falls back to cmd.exe when ComSpec is unset",
    "win32: never falls back to bash/sh even if SHELL happens to be set",
    "defaults to the real process.platform/process.env when called with no args",
    "kind=shell spawns the platform-resolved default shell"
   ],
   "api/terminal-token-endpoint.test.ts": [
    "valid API bearer → 200 { token } matching the minted terminal token, Cache-Control: no-store",
    "no bearer → 401; wrong bearer → 401 (the API token gate, constant-time path)",
    "the TERMINAL token itself does not unlock the endpoint (API bearer only — no cross-token confusion)",
    "DECKENT_API_AUTH_DISABLED=1 → STILL 401 without a valid bearer (inv#1: bypass-independent, fail-CLOSED)",
    "terminal disabled (no backend) → 404 with an honest error body",
    "does not disturb the terminal-token-gated /api/terminal/sessions block (same prefix, different bearer)"
   ],
   "api/token-redaction.test.ts": [
    "autoGenerateToken: stderr carries no raw token, but a fingerprint + 0600 file with the active token",
    "localhost auto-mint: stderr carries no raw token, fingerprint + same api-token file, real auth works",
    "terminal token: stderr carries no raw token, separate terminal-token file, WS/HTML consumer unaffected",
    "i18n: serve.token.* keys resolve to real EN/TR text (not the raw key) via getMessage"
   ],
   "api/watcher.test.ts": [
    "calls fs.watch with the given file path",
    "returns an object with a close() method",
    "creates watcher immediately (not lazily)",
    "supports watching different file paths",
    "calls onChange after debounce timeout when file changes",
    "debounces rapid successive change events (only one call)",
    "fires onChange again on second change after debounce settles",
    "does not call onChange before debounce window elapses",
    "close() calls the underlying FSWatcher.close()",
    "close() cancels a pending debounce timer so onChange is not called",
    "close() is safe to call when no pending timer",
    "propagates error when fs.watch throws (file not found)",
    "propagates error when fs.watch throws (permission denied)"
   ],
   "api/worker-logs-render.test.ts": [
    "the .log line is opaque JSON, not human text",
    "renders tool_use as tool-name + short args snippet (direct shape)",
    "renders tool_use from the Claude SDK envelope shape",
    "renders text as its content",
    "renders usage as a compact in/out token summary",
    "renders lifecycle compactly using its subtype",
    "renders tool_result, stderr and turn",
    "passes through a non-JSON line unchanged (legacy codex/gemini stdout)",
    "passes through valid JSON that is not LogEvent-shaped",
    "passes through JSON with an unrecognized type field",
    "passes through a truncated/partial JSON line unchanged (no data loss)",
    "passes through an empty string unchanged",
    "FIX: ?render=human rewrites a LogEvent line to a human-readable summary",
    "geriye-uyum: no render param keeps raw-JSONL passthrough (pin)",
    "render=human still passes a non-JSON legacy line through unchanged",
    "an irrelevant ?render= value (not \"human\") also keeps raw passthrough"
   ],
   "api/worker-logs.test.ts": [
    "matchWorkerLogStream extracts the taskId segment",
    "matchWorkerLogStream returns null for unrelated paths",
    "isValidTaskId allows the safe charset and rejects traversal",
    "formatWorkerLogFrame emits a named SSE event field",
    "FIX: a valid ?token= authenticates the dynamic worker-log path",
    "control: /api/events?token= still authenticates (exact whitelist no regression)",
    "behavior-preserving: /api/workers LIST (no trailing slash) is NOT query-token eligible → 401",
    "no prefix bleed: an exact whitelist entry is not widened (/api/eventsX → 401)",
    "wrong ?token= on the worker-log path → 403 and never leaks the token",
    "worker-log path WITH a valid Authorization header authenticates (header path intact)",
    "backfills existing log lines on connect",
    "streams a line appended AFTER connect (live tail ≤2s)",
    "emits an HONEST log_unavailable event when the log file is missing",
    "rejects a path-traversal taskId with 403 (before any fs access)",
    "rejects an invalid taskId (dot) with 403",
    "rejects a wrong query-token with 403 (no silent stream, no token echo)"
   ],
   "api/ws-tenant-propagation.test.ts": [
    "propagates the mTLS-resolved tenant to auth.ok (not the local hardcode)",
    "falls back honestly to local when no auth-context tenant is resolvable",
    "session-scoped events carry the session real tenant, not the local hardcode",
    "strict ON: a token-only caller (no resolvable tenant) is refused, no bridge",
    "strict ON: an mTLS-resolved tenant still connects (the claim IS resolvable)",
    "strict OFF (default): the same token-only caller still connects — v1 unchanged",
    "strict ON: an INVALID token still fails as auth, not as tenant scope"
   ]
  },
  "approvals": {
   "approvals/expired-decide-response.test.ts": [
    "an approve attempt on an overdue request returns typed expired and persists the TTL denial",
    "a reject attempt on the same overdue request also returns typed expired and persists the TTL denial",
    "a late decide attempt AFTER the TTL sweep already closed it also returns typed expired (never throws, never double-decides)",
    "resolves the English expired message with expiresAt interpolated",
    "resolves the Turkish expired message with expiresAt interpolated",
    "returns null for a normal (non-expired) ApprovalDecision",
    "a still-valid request decides identically via decideChecked, and resolves awaitDecision",
    "decideChecked on an already-decided-by-a-real-channel request still throws APR_ALREADY_DECIDED (not swallowed as expired)",
    "approvalCallbackData -> parseApprovalCallback -> decideChecked -> renderExpiredDecideReply round trip for an expired card",
    "a still-open card round-trips to a normal ack (renderExpiredDecideReply returns null)"
   ],
   "approvals/expiry-sweep-cross-process.test.ts": [
    "closes an overdue fixture with an honest ttl-expire decision and never reports it pending",
    "a second sweepExpired() call on the same instance closes nothing new and leaves the decision byte-identical",
    "two independent ApprovalStore instances sharing storeDir converge to exactly one decision file, no throw",
    "leaves a still-valid pending fixture untouched",
    "never re-decides an already-decided (non-ttl) fixture, even one whose request has since elapsed"
   ],
   "approvals/notify-dedup-restart.test.ts": [
    "markNotified persists across a simulated restart — a brand-new instance already knows the id",
    "clear() persists — a restart-simulated instance no longer sees the id",
    "markNotified is a no-op re-persist when the id is already marked",
    "recovers to an empty set from a torn (invalid JSON) state file without throwing",
    "recovers to an empty set from a structurally-wrong-shape (valid JSON, wrong version) state file",
    "a subsequent markNotified overwrites a corrupt file with a valid version-1 file and leaves no leftover .tmp",
    "a restart-simulated dedup+relay does NOT re-dispatch a pending request the previous process already notified",
    "broker.expire() (TTL path) clears the dedup record, and the clear survives a simulated restart"
   ]
  },
  "audit": {
   "audit/worker-brain-audit-parity.test.ts": [
    "parity (a) — parser reads Tests line (2 failed), NOT Test Files line (1 failed)",
    "parity (a-edge) — file fail > test fail (impossible in vitest but regex must still target Tests)",
    "parity (a-clean) — all-pass run reports 0 failures",
    "parity (b) — parser produces identical output across 5 successive calls",
    "parity (b-no-side-effect) — calling parser does not mutate input strings",
    "parity (c) — delta.fail = current - baseline (positive when regression)",
    "parity (c-zero) — delta.fail = 0 when current matches baseline",
    "parity (c-regression) — delta.fail negative when fixes occurred",
    "parity (c-null-baseline) — when baseline is null, delta == current (no comparison)",
    "parity (d) — concurrent parser invocations do not interfere",
    "parity (d-stateless) — runVitestAuditGate accepts injected gather fn (no shared state)",
    "worker-brain parity: same fail count emerges from same suite output",
    "regression detected: baseline fail=0, current fail=2 → GATE_FAILURE",
    "worker fixed regressions: baseline fail=17, current fail=0 → PASS (negative delta)",
    "gather failure (SPAWN_FAIL) → gate INCONCLUSIVE, not GATE_FAILURE"
   ]
  },
  "audits": {
   "audits/dead-code-decisions.test.ts": [
    "decision matrix document exists",
    "contains all required sections",
    "covers all 11 modules from the audit report",
    "has decision rationale for each detailed entry",
    "assigns correct decisions: 3 Remove, 3 Defer, 4 Deprecate, 1 False Positive",
    "Remove decisions include only low-value modules",
    "Defer decisions reference future sprint reassessment",
    "successor adr-d-006 exists and references ADR-038",
    "adr-d-006 has accepted status",
    "adr-d-006 has Decision and Context fields",
    "adr-d-006 references dead code disposition",
    "adr-d-006 defines disposition categories",
    "adr-d-006 mentions alternatives/consequences",
    "every Remove decision has LoC count",
    "every Defer decision has future connection",
    "false positive section explains root cause",
    "execute checklist lists all Remove targets",
    "decision matrix references ADR-028 for dormant modules"
   ]
  },
  "backends": {
   "backends/docker-corrupt-result-recovery.test.ts": [
    "preserves corrupt bytes and deterministically replays a typed NO_GO receipt",
    "keeps a valid worker result authoritative instead of fabricating recovery",
    "rejects %s-invalid exit-zero bytes without replacing them"
   ],
   "backends/docker-non-root.test.ts": [
    "should have USER directive switching to non-root user",
    "should create a dedicated deckent system user",
    "should set correct file ownership with chown before USER switch",
    "should create workspace directory that deckent user can write to"
   ]
  },
  "blueprint": {
   "blueprint/files.test.ts": [
    "AGENTS.md exists and is non-empty",
    "docs/en/reference/api-surface.md exists and is non-empty",
    ".deckent/workspace/IDENTITY.md exists and is non-empty (skip in CI)",
    "AGENTS.md contains Architecture section",
    "docs/en/reference/api-surface.md contains task format"
   ],
   "blueprint/pull-request-template.test.ts": [
    "pull_request_template.md exists",
    "template is non-empty",
    "contains Summary section",
    "contains Changes section",
    "contains Test Plan section with vitest",
    "contains Related Issues section",
    "contains Checklist section",
    "contains Deckent-specific checklist items",
    "contains tsc --noEmit check"
   ],
   "blueprint/security-md.test.ts": [
    "SECURITY.md exists at repository root",
    "SECURITY.md at root is non-empty",
    "docs/en/reference/platform-security.md exists",
    "root SECURITY.md and docs/en/reference/platform-security.md both exist and are non-empty",
    "SECURITY.md contains security model overview",
    "SECURITY.md contains permission hierarchy content"
   ],
   "blueprint/sprint-history.test.ts": [
    "MCP server section shows 27 tools",
    "MCP server section shows 8 resources",
    "Resources section header shows (8)",
    "Sprint history table contains sprint 046 with 10K+ tests",
    "Sprint history table contains sprint 047",
    "Sprint history table contains sprints 039 through 047",
    "CLI commands files count reflects current state",
    "10K tests milestone entry exists"
   ]
  },
  "brain": {
   "brain/decisions.test.ts": [
    "decisions.md export exists",
    "contains ADR headers in the new adr-g-NNN / adr-d-NNN taxonomy",
    "ADR ids are class-prefixed, zero-padded 3-digit numbers (adr-g-NNN / adr-d-NNN)",
    "each ADR has Decision content and an explicit Status field",
    ".deck secret file system topic (old ADR-014) covers adr-g-005",
    "TaskRouter topic (old ADR-015) covers adr-g-006",
    "Connector module topic (old ADR-016) covers adr-g-007",
    "MCP-native provider adapters topic (old ADR-017) covers adr-g-008",
    "multi-environment config topic (old ADR-018) covers adr-g-004",
    "language-agnostic worker verify topic (old ADR-019) covers adr-g-009",
    "rich sprint output topic (old ADR-020) covers adr-g-010",
    "Kraken ASCII brand identity topic (old ADR-021) covers adr-g-010"
   ]
  },
  "build": {
   "build/npm-pack-dockerfile.test.ts": [
    "includes Dockerfile.worker in the packed tarball file list"
   ],
   "build/readme-package-links.test.ts": [
    "every docs/ link in README is either absolute or packed in the tarball"
   ]
  },
  "catalog": {
   "catalog/cache-archetype.test.ts": [
    "%s → IMPLICIT_AUTO",
    "anthropic-api → EXPLICIT_MARKER",
    "bedrock → EXPLICIT_MARKER",
    "vertex → EXPLICIT_MARKER",
    "qwen-explicit → EXPLICIT_MARKER",
    "gemini-cachedcontent → EXPLICIT_RESOURCE",
    "moonshotai → EXPLICIT_RESOURCE",
    "%s → LOCAL_KV",
    "cohere → NONE",
    "returns null for unknown provider — never silently defaults",
    "returns null for empty string",
    "returns null for custom/private LLM identifier",
    "does not treat aliases without explicit entry as known",
    "deepseek → prompt_cache_hit_tokens",
    "anthropic-api → cache_read_input_tokens",
    "bedrock → cache_read_input_tokens",
    "vertex → cache_read_input_tokens",
    "gemini-impl → cachedContentTokenCount",
    "gemini-cachedcontent → cachedContentTokenCount",
    "%s → \"\"",
    "cohere → \"\"",
    "openai → prompt_tokens_details.cached_tokens",
    "groq → prompt_tokens_details.cached_tokens",
    "mistral → prompt_tokens_details.cached_tokens",
    "xai → prompt_tokens_details.cached_tokens",
    "together → prompt_tokens_details.cached_tokens",
    "claude-cli → prompt_tokens_details.cached_tokens",
    "qwen-explicit → prompt_tokens_details.cached_tokens (EXPLICIT_MARKER, no override)",
    "moonshotai → prompt_tokens_details.cached_tokens (EXPLICIT_RESOURCE, no override)",
    "unknown provider → null (not empty string, not a default)",
    "empty string → null",
    "raw anthropic (no -api suffix) → null"
   ],
   "catalog/catalog-registry.test.ts": [
    "exposes the canonical source id",
    "normalizes cost-config into CatalogEntry[] (price, limits, archetype)",
    "maps null cache costs to 0 and missing output limit to 0 (ollama / LOCAL_KV)",
    "canonicalizes provider aliases (kimi → moonshotai) with the default cache profile",
    "returns [] (never throws) when the config reader fails — graceful offline",
    "reads .deckent/cost-config.json from disk and normalizes it",
    "does not crash when cost-config.json is absent (baseline fallback)",
    "merges sources with custom overwriting builtin-default for the same model",
    "applies precedence by tier rank, independent of registration order",
    "layers the full precedence stack (builtin < enrichment < local-override < custom)",
    "returns undefined for an unknown provider/model (no crash)",
    "get() performs no network — fetch only happens at sync-time",
    "skips a failing source without aborting the merge (graceful)",
    "exposes getAll() and size over the merged set; sync is idempotent",
    "works end-to-end with LocalStaticSource alone, offline (registry.get)"
   ],
   "catalog/catalog-types.test.ts": [
    "exports exactly 5 values",
    "contains all required archetype keys",
    "key and value are identical strings (constant semantics)",
    "CacheArchetype type assignment compiles for all members",
    "accepts all three valid regime values",
    "accepts a fully populated confirmed entry",
    "accepts an unconfirmed entry without minCacheablePrefix",
    "contains exactly 5 entries",
    "kimi → moonshotai",
    "qwen → alibaba",
    "grok → xai",
    "together → togetherai",
    "fireworks → fireworks-ai",
    "resolves known aliases",
    "returns the raw value unchanged for unknown providers",
    "can be implemented with the required interface shape"
   ],
   "catalog/enrichment-sources.test.ts": [
    "exposes the canonical source id",
    "normalizes models.dev JSON to CatalogEntry[] with correct price mapping",
    "maps cost.cache_read → price.cacheRead and cost.cache_write → price.cacheWrite",
    "defaults missing cache cost fields to 0 (no cache_read/cache_write in response)",
    "canonicalizes provider aliases (kimi → moonshotai)",
    "returns [] (never throws) on network failure",
    "returns [] (never throws) on HTTP error status",
    "returns [] (never throws) on unexpected response shape (not an object)",
    "skips provider entries with non-object values without crashing",
    "fetch is only called during the fetch() call (no eager network)",
    "exposes the canonical source id",
    "normalizes OpenRouter JSON to CatalogEntry[] with correct price mapping",
    "maps pricing.cache_read → price.cacheRead and pricing.cache_write → price.cacheWrite",
    "defaults missing cache pricing to 0",
    "parses price strings and number values correctly",
    "canonicalizes provider aliases (kimi → moonshotai)",
    "skips model entries with invalid id format (no slash or slash at end)",
    "returns [] (never throws) on network failure",
    "returns [] (never throws) on HTTP error status",
    "returns [] (never throws) on unexpected response shape (no data array)",
    "applies minCacheablePrefix for openai provider",
    "fetch is only called during the fetch() call (no eager network)",
    "enrichment-tier source overwrites builtin-default for the same model",
    "local-override overwrites enrichment for the same model",
    "OpenRouterSource at enrichment tier: registry.get() performs no network"
   ]
  },
  "cli": {
   "cli/acceptance-confirmation-messages.test.ts": [
    "%s is a genuine EN/TR catalog entry",
    "%s has identical EN/TR placeholders",
    "catalogs every public service state explicitly",
    "covers list, route, HOLD, APPLIED, expiry, corrupt, foreign, provider separation and runtime audit copy",
    "keeps stable reasonCode values separate from localized user copy",
    "keeps state copy provider/model-neutral and routes through the supplied authenticated surface"
   ],
   "cli/agent-delete-confirm.test.ts": [
    "proceeds without prompting when --force is set",
    "aborts (returns false) when no flag is set and the user declines",
    "proceeds (returns true) when no flag is set and the user confirms"
   ],
   "cli/agent-surface-readmodel.test.ts": [
    "reports identical ids and counts on both surfaces and the resolver snapshot",
    "never lists an archived record, at any nesting depth",
    "renders one provenance vocabulary on both surfaces — the learned/temp split is gone",
    "exposes enabled and routability with typed reasons on both payloads",
    "surfaces a schema-invalid record instead of silently dropping it",
    "orders entries by id with a fixed collation, not a locale-dependent one",
    "renders the human table from the same read model",
    "leaves no raw directory scan on either migrated surface"
   ],
   "cli/agentic-confirm-harden.test.ts": [
    "asks the question on the caller-owned rl, not a second interface",
    "never closes a reused rl — caller keeps ownership of its lifecycle",
    "honors the reused rl answer even if opts.input/output are also set (rl wins)",
    "requireConfirmIfRisky forwards opts.rl through to confirmAction for a risky action",
    "end-to-end with a REAL node:readline/promises Interface: no stdin collision, rl left open",
    "without opts.rl, falls back to opening (and closing) its own interface as before",
    "classifies a name containing both \"list\" (safe) and \"run\" (risky) as risky",
    "classifies a name containing both \"status\" (safe) and \"reset\" (risky) as risky",
    "classifies a name containing both \"read\" (safe) and \"delete\" (risky) as risky",
    "still classifies a purely safe action as safe (no regression)",
    "requireConfirmIfRisky actually gates a SAFE-substring risky action instead of auto-approving"
   ],
   "cli/agentic-confirm.test.ts": [
    "classifies kill as risky",
    "classifies start as risky",
    "classifies cleanup as risky",
    "classifies status as safe",
    "classifies recall as safe",
    "classifies history as safe",
    "defaults unknown action to risky (fail-safe)",
    "returns true when user types \"y\"",
    "returns false when user types \"n\"",
    "returns false when user presses Enter (default N)",
    "returns false when user types \"Y\" (uppercase y should be truthy)",
    "auto-approves safe action without prompting",
    "auto-approves recall (safe) without prompting",
    "requires confirm for risky action and returns true on approval",
    "requires confirm for risky action and returns false on rejection",
    "cancels risky kill action when user declines",
    "runs risky start action when user confirms",
    "returns the chosen index (1-based input → 0-based index)",
    "empty input → defaultIndex",
    "out-of-range input → defaultIndex (0 default)",
    "non-numeric input → defaultIndex",
    "empty choices → fallback without prompting"
   ],
   "cli/agentic-dispatch-overmatch.test.ts": [
    "\"beni sonra ara\" (call me later) — no longer misroutes to deckent_memory_query",
    "\"işten çıkınca beni ara lütfen\" — no longer misroutes to deckent_memory_query",
    "\"I have a great memory for names\" — bare \"memory\" alone no longer dispatches",
    "\"this laptop needs more memory\" — bare \"memory\" alone no longer dispatches",
    "\"did you find the bug?\" — bare \"find\" alone no longer dispatches",
    "\"can you find my keys\" — bare \"find\" alone no longer dispatches",
    "\"let\\'s search for a new apartment\" — bare \"search\" alone no longer dispatches",
    "\"hafızam çok zayıf bu aralar\" — memory-noun without a search-verb no longer dispatches",
    "\"bu duruma göre karar verelim\" — bare durum stem (dative) no longer dispatches",
    "\"olağanüstü durumda ne yapmalı\" — bare durum stem (locative) no longer dispatches",
    "\"just checking on the status of my order\" — bare \"status\" alone no longer dispatches",
    "\"how is sprint going for you these days?\" — small talk no longer dispatches",
    "\"how are we doing today, feeling ok?\" — small talk no longer dispatches",
    "\"geçmiş olsun, tekrar dene\" (get well soon) — idiom no longer dispatches",
    "\"what\\'s the plan for tonight?\" — bare \"plan\" alone no longer dispatches",
    "\"I need a diet plan\" — bare \"plan\" alone no longer dispatches",
    "\"sprint durumu ne\" → deckent_status",
    "\"sprint durumu nasıl\" → deckent_status",
    "\"what is the status\" → deckent_status",
    "\"son sprinti göster\" → deckent_history",
    "\"son sprintleri göster\" → deckent_history",
    "\"show me sprint history\" → deckent_history",
    "\"hafızada rbac ara\" → deckent_memory_query with extracted query",
    "\"hafızada adr-037 ara\" → deckent_memory_query",
    "\"hafızada docker ara\" → deckent_memory_query",
    "\"search memory for docker\" → deckent_memory_query",
    "\"sprint planla\" → deckent_plan",
    "\"hello there friend\" → no_match (unaffected true negative)",
    "\"let\\'s grab lunch tomorrow\" → no_match (unaffected true negative)"
   ],
   "cli/agentic-session.test.ts": [
    "calls appendChatTurn with correct sessionId, role, and content",
    "is a no-op when DB does not exist",
    "always closes the store even if appendChatTurn throws",
    "persists assistant role turns correctly",
    "calls getChatHistory with sessionId and default limit",
    "passes custom limit through to getChatHistory",
    "returns empty array when DB does not exist",
    "returns the history from getChatHistory",
    "returns the provided sessionId unchanged",
    "generates a unique id when no sessionId is provided",
    "generates a unique id when empty string is provided",
    "generates a unique id when whitespace-only string is provided",
    "appendChatTurn wires through to persistTurn (calls MemoryStore.appendChatTurn)",
    "getChatHistory wires through to resumeSession (calls MemoryStore.getChatHistory)",
    "appendChatTurn is a no-op when DB does not exist",
    "getChatHistory returns [] when DB does not exist"
   ],
   "cli/analyze-coverage.test.ts": [
    "returns a formatted table string",
    "outputs formatted table by default",
    "outputs raw JSON with --json flag"
   ],
   "cli/app-approval-wire.test.tsx": [
    "returns footerLines unchanged when no approval is pending",
    "returns [] unchanged when footerLines is empty and nothing pending",
    "compresses a multi-line footer to exactly its first line while pending",
    "never fully disappears — a single-line footer stays that one line (\"footer kaybolmaz\")",
    "an empty footer stays empty even while pending (nothing to reserve room for)",
    "never leaks the internal dual-stream placeholder into rendered output",
    "uses actual terminal width and the caller-owned overflow marker",
    "forwards every source event to the downstream consumer unchanged and in order",
    "feeds the tracker so its head reflects the tapped events after consumption",
    "a pending event makes the card \"visible\" (queue head non-null) and compresses the footer",
    "y (approve) sends the exact decision shape ApprovalCard.sendDecision produces to the fake relay, then resolving retires the head and restores the footer",
    "n (deny) does not send an allow decision, and a cross-decided broadcast (e.g. dashboard) also retires the head + restores the footer",
    "retires only the exact expired request and keeps stdin/layout pending while another request remains",
    "releases stdin/layout only after the final exact request is accepted",
    "enabled=false -> watchFactory is never called, returns undefined",
    "a request submitted by a DIFFERENT broker instance appears as a pending event, without a duplicate write",
    "the obsolete raw terminal call cannot persist a decision",
    "a decision written by a THIRD party after pending was observed surfaces as cross-decided",
    "a decision already on disk BEFORE this process attached (no cached request) never crashes or emits"
   ],
   "cli/approval-card.test.tsx": [
    "returns null head / size 0 when empty",
    "ingest(pending) appends in arrival order; head() is the oldest",
    "ingest(pending) with a repeated id upserts — does not duplicate in order",
    "ingest(dropped) is a no-op — queue state and onChange both unaffected",
    "ingest(cross-decided) retires the request even when it is NOT the current head",
    "resolve(id) retires the head and advances the burst index/total",
    "resolve() on an unknown id is a defensive no-op",
    "burst counter resets to 0 once the queue fully drains, then restarts at 1",
    "mid-burst arrival: a new pending item grows total while the head stays put",
    "similarTo(id) returns only OTHER pending requests sharing scopeId",
    "similarTo(id) on an unknown id returns empty",
    "resolveSimilar(id) resolves the target AND every same-scopeId match, leaving the rest",
    "resolveSimilar(id) on an unknown id resolves nothing",
    "resolveSimilar with no same-scope siblings resolves only the target",
    "maps %s -> %s",
    "unmapped key %j is a no-op (null)",
    "keeps Esc pending rather than deciding",
    "approve → the approved template with {summary} interpolated",
    "deny → the rejected template with {summary} interpolated",
    "throws the typed guard error when labels are absent — no English fallback (TERMINAL-TOOLS-002)",
    "resolves the approval.terminal.* keys in en + tr with {summary} interpolation"
   ],
   "cli/approval-channel-dispose.test.ts": [
    "decide() after dispose() is a no-op: the broker never observes the decision",
    "decide() after dispose() never cross-broadcasts to other attached channels",
    "repeated dispose() calls stay idempotent no-ops (decisionHandler already null)",
    "a fresh bridge on a new channelName after the first is disposed still decides correctly (handler swap is clean)"
   ],
   "cli/approval-inputbar-mutex.test.tsx": [
    "idle (no confirm, no approval pending): only InputBar is active",
    "approval pending, no confirm modal open: InputBar is INACTIVE — the born-508 fix",
    "legacy confirm modal open, no approval pending: only the confirm modal is active",
    "confirm modal open AND an approval also pending: confirm modal wins — neither InputBar nor ApprovalCard is active",
    "InputBar and the confirm modal are never simultaneously active, for every input combination",
    "InputBar is never active while an approval is pending, for every confirm-modal state",
    "reproduces the pre-fix hazard: mapApprovalKey(\\'y\\') alone WOULD approve — the mutex is what prevents delivery",
    "while an approval is pending, InputBar\\'s resolved active flag is false — Ink would never route the keystroke to it",
    "with nothing pending, InputBar owns stdin and ordinary confirm-style keys (y/n) are NOT consumed by the (inactive) confirm modal",
    "only suppresses fallback Ctrl-C for the active picker, its actual consumer"
   ],
   "cli/approval-lifecycle-messages.test.ts": [
    "provides every lifecycle state and timeout disposition in EN/TR",
    "renders expiry and quarantine evidence without unresolved placeholders"
   ],
   "cli/approval-terminal-channel.test.ts": [
    "keeps the event-stream read path and tenant filter intact",
    "routes a card intent only through the injected adapter and never the raw relay writer",
    "returns typed HOLD for an absent adapter and for the obsolete raw call shape",
    "does not blindly retire a cross-channel decision that fails durable authorization",
    "forwards a cross-channel retirement only after exact verification",
    "reconciles an independently verified durable winner while the local intent returns HOLD",
    "keeps an in-flight conflict untrusted when independent verification rejects it",
    "suppresses only the matching accepted race and forwards an accepted mismatch for verification",
    "deduplicates a concurrent intent and fails closed after dispose"
   ],
   "cli/approvals-confirmation-lineage.test.ts": [
    "rejects a foreign-tenant target with zero lifecycle, mirror, or decision mutation",
    "rejects a foreign-tenant target resolved by short code with zero mutation",
    "revalidates an existing broker mirror against the exact source lineage before live auth",
    "uses one live human decision surface and withholds success and rule grants without reconciliation",
    "requires the canonical receipt to reach APPLIED rather than accepting PREPARED",
    "retries reconciliation after mutate-then-HOLD and promotes only after immutable winner APPLIED",
    "returns the exact canonical APPLIED receipt again on authenticated replay",
    "reports success only after the exact federation reconciliation receipt is observed"
   ],
   "cli/approvals-confirmation-run.test.ts": [
    "uses --author only as the missing request author and carries provider authority unchanged",
    "fails closed when effective lifecycle policy is absent",
    "passes the effective lifecycle unchanged into the LLM composition"
   ],
   "cli/approvals-context-truth.test.ts": [
    "renders exact %s request authority without claiming execution or reachability",
    "renders a declared execution subject only when present and keeps unknown values honest",
    "uses action-specific neutral confirmation and consequence copy in both languages",
    "distinguishes readline cancellation from authentication failure and localizes its truthful boundary"
   ],
   "cli/approvals-federation-list.test.ts": [
    "enumerates every origin class returned by the federation module",
    "restricts federated rows to the requested class",
    "rejects an unknown class through the localized typed-error path"
   ],
   "cli/approvals-lifecycle-view.test.ts": [
    "renders the effective durable lifecycle, audit lineage, receipts and quarantine without writes",
    "uses the authenticated ingress and renders its typed expired result without a late grant",
    "rejects a policy-tightened effective expiry before live auth can grant it",
    "keeps a quarantined durable row visible but never sends it to the decision ingress"
   ],
   "cli/archive-terminal-seal.test.ts": [
    "captures canonical strict-prefix parity without mutating either journal",
    "keeps inspect and terminal-complete verify semantically distinct",
    "repairs through the real Commander surface, persists authority, and verifies idempotent replay",
    "reports a staged partial core failure truthfully and replays the same original preimages",
    "rejects non-canonical receipt and hot paths before mutation",
    "rejects empty reason and incorrect digests without producing a seal receipt",
    "normalizes malformed canonical receipts to a typed localized error without mutation",
    "ships every new operator status through both message catalogs",
    "renders typed terminal failures through localized catalog text",
    "labels success digests separately for manifest, Brain index, and guarded summary"
   ],
   "cli/at-ref-budget.test.ts": [
    "keeps a small single reference inline with full typed lineage",
    "uses a lossless descriptor when the full reference exceeds the budget",
    "admits references in order and descriptors the first one that no longer fits",
    "preserves the exact legacy prompt when no budget is supplied",
    "proves the incident shape: 3 x ~50K refs with ~120K budget descriptors the remainder",
    "subtracts output, safety, and transcript reservations before taking a conservative share",
    "preserves legacy inline behavior when authority is absent or throws"
   ],
   "cli/at-ref.test.ts": [
    "extracts a token at start of text and after whitespace",
    "stops a token at the next whitespace",
    "ignores emails (non-space before the @)",
    "ignores a literal @@ escape and a bare @",
    "de-duplicates repeated refs in first-seen order",
    "no tokens → empty array",
    "no refs → byte-identical passthrough, empty refs",
    "injects one fenced block per resolved ref, after the user text",
    "escalates the fence when the content itself contains ```",
    "caps content at AT_REF_MAX_CHARS with an explicit truncation marker",
    "expands at most AT_REF_MAX_REFS refs and NOTES the extras by name",
    "notes an unreadable ref honestly instead of dropping it",
    "maps master-plan.md to docs/MASTER-PLAN.md",
    "resolves by basename when unique",
    "matches path-prefix and basename queries; rejects unrelated strings",
    "empty query → root-level dirs and root files from the index (not a fixed slice)",
    "narrows live as a path prefix is typed (`src/cli/re`)",
    "ranks basename-prefix > basename-substring > path-prefix > path-substring (subsequence DROPPED)",
    "Alperen live-fix (@cost): the file NAMED cost-config.json wins; subsequence noise is gone",
    "is case-insensitive and excludes non-matches",
    "breaks ties by shorter path, then lexicographic (deterministic)",
    "caps at the limit (default 8)",
    "matches a directory candidate by its basename despite the trailing slash",
    "opens on a bare @ at start of buffer (empty query)",
    "opens after whitespace and carries the typed query up to the cursor",
    "never opens for an email-style @ (non-space before it)",
    "never opens for a literal @@ escape",
    "closed when the cursor left the @-word or there is no @",
    "replaces the token with @path + trailing space and places the cursor after it",
    "preserves text right of the cursor",
    "closed without a provider (feature inert when unwired)",
    "open with the active token + fuzzy matches",
    "closed for the Esc-dismissed token, open again for a different @ index",
    "closed when nothing matches the query",
    "accepts project-relative paths (incl. balanced ..)",
    "rejects escapes, absolutes (POSIX + Windows forms), and empties",
    "lists rel files + derived ancestor dirs (trailing /), sorted",
    "caches within the TTL, re-walks after it, invalidates on root change",
    "stops the walk at the file cap and never returns more than `cap` entries",
    "reads an in-scope file; null for missing / escape / absolute / binary",
    "refuses a symlink inside cwd whose real target escapes it (born-536 mirror)"
   ],
   "cli/atref-tool-mediated-battery.test.ts": [
    "expandAtRefs produces no inline body and 3 descriptor lines under a narrow budget",
    "measured request under the descriptor path admits where the equivalent inline path would not",
    "deckent_read_file (registry dispatch) returns a real numbered slice of the descriptored file",
    "parseAtRefLineage still recovers rawIntent/expandedPayload/references for an inline ref",
    "no descriptor info line renders for an all-inline turn",
    "renders exactly once with the right count in %s, framed as information not a rejection"
   ],
   "cli/attended-execution-spawn-authority.test.ts": [
    "verifies and consumes the exact live-session receipt immediately before backend.spawn",
    "uses the approved attempt identity for Docker settlement instead of minting a second attempt"
   ],
   "cli/audit-command.test.ts": [
    "query: returns results and shows matched count",
    "query: tenant filter passes tenantId to queryAudit",
    "query: action filter passes channel to queryAudit",
    "query: since filter passes from timestamp to queryAudit",
    "query: RBAC gate — role passed to queryAudit; invalid role returns 0 matched",
    "query: --json outputs raw JSON result",
    "query: queryAudit error → printError called + exit 1"
   ],
   "cli/audit-readside.test.ts": [
    "runComplianceReport builds a report over the live audit chain",
    "runComplianceReport on an empty sprint stream → zero events, intact chain",
    "runSiemExport writes normalized NDJSON records to the out file and reports the count",
    "runSiemExport on an empty sprint stream → count 0, no file forced",
    "runSiemHttpForward POSTs the normalized records via the injected fetch and reports count + url",
    "runSiemHttpForward on an empty sprint stream → count 0 and fetch is never called",
    "runSiemHttpForward throws synchronously-validated errors (malformed URL) before forwarding",
    "runSiemHttpForward does NOT reject on a failing endpoint — forwarder retries then drops (fail-safe)",
    "runSiemSyslogForward ships one RFC 5424 message per record via the injected sendImpl",
    "runSiemSyslogForward on an empty sprint stream → count 0 and sendImpl is never called",
    "runSiemSyslogForward rejects on invalid transport options (empty host) before any send",
    "parseSyslogTarget parses host[:port] with the syslog default 514",
    "dry-run reports the plan and writes NOTHING (stream byte-identical, no archive file)",
    "compliance over a retained stream is ARCHIVE-AWARE: chain verifies across archive + live (hmac'd head archived)",
    "apply with maxCount: atomic rewrite — readAuditEvents returns the keep-set, dropped event is archived, non-audit events survive",
    "apply pruning a legacy (hmac-less) head record keeps verifyAuditChain intact on the kept chain",
    "apply with nothing to drop leaves the stream untouched (byte-identical)",
    "empty stream → zero plan and apply creates no files"
   ],
   "cli/auto-approve-consistency.test.ts": [
    "defaults autoApprove to false when --auto-approve is not passed",
    "honors --auto-approve: sets autoApprove to true",
    "defaults autoApprove to false when --auto-approve is not passed",
    "honors --auto-approve: sets autoApprove to true"
   ],
   "cli/auto-setup.test.ts": [
    "max subscription + 16GB + small project → performance, ~4 workers",
    "pro subscription + 8GB + large project → economic, ~3 workers",
    "unknown subscription → economic safe default",
    "reasons array is non-empty and contains strings",
    "SetupRecommendation interface has all required fields",
    "small project halves the workers (ceil)",
    "medium project uses 0.75 multiplier",
    "large project uses full workers",
    "maxWorkers is always at least 1",
    "max subscription gets ai planning, pro gets structured",
    "reasons mention subscription, system, and project size"
   ],
   "cli/autonomous-approval-lifecycle.test.ts": [
    "late approve returns failure, persists timeout authority and never creates a human replay",
    "pending is an expiry-aware read and omits stale legacy rows"
   ],
   "cli/autonomous-approve.test.ts": [
    "approve <id> → a parked trigger resolves approved for the running loop",
    "reject <id> → a parked trigger resolves rejected for the running loop",
    "approve on an unknown id → exitCode 1 and records no decision",
    "pending → lists every parked trigger by id"
   ],
   "cli/autonomous-backlog.test.ts": [
    "add writes an entry, list returns it",
    "added entry has expected fields",
    "duplicate id throws a getMessage-based error",
    "remove deletes the entry",
    "removing a missing id throws a getMessage-based error",
    "list returns empty array when no backlog file exists",
    "multiple adds accumulate entries",
    "remove leaves other entries intact",
    "turkish error messages work (lang=tr)",
    "add with cron creates a recurring entry",
    "add without cron keeps the one-off trigger (backward-safe)",
    "add with a malformed cron throws a getMessage-based error (en + tr)",
    "add with kind=capability creates an entry with a capabilityTarget",
    "add with kind=capability records the connector when given",
    "add with kind=capability but no --capability throws an i18n error",
    "add with invalid --args JSON throws an i18n error and persists nothing"
   ],
   "cli/autonomous-command.test.ts": [
    "stop → writes stop marker file under .deckent/autonomous/",
    "cleanup → sweeps stray task-run-* artifacts, keeps real tasks, reports count",
    "status → summary with pending count + last audit events",
    "start → loop kurar, maxIterations=1 ile temiz biter (idle, no flows)",
    "plan re-queues a terminal (failed) entry with the same id (replace, not silent drop)",
    "plan does NOT disturb an active (pending) entry with the same id",
    "start refuses when autonomous.enabled is false (flag-gate)",
    "default-deny korunur — bilinmeyen tenant flow → park/pending (approval required), oto-exec yok",
    "CLI wiring — `autonomous status` parses + emits status header",
    "start with reactive disabled runs the engine loop unchanged",
    "start with reactive enabled + empty reactive-map attaches + tears down cleanly",
    "start with nervous_system enabled drives + disposes the live nervous system (N1)",
    "start with reactive.repo_watch + reactive.webhook enabled runs + tears down cleanly (N2)",
    "start with nervous_system disabled creates no live nervous system (N1 opt-in)"
   ],
   "cli/autonomous-enable.test.ts": [
    "flips autonomous.enabled=true in the project config (creates the file if absent)",
    "preserves unrelated existing config keys (deep-merges the flag)"
   ],
   "cli/autonomous-flow-wire.test.ts": [
    "routes a step to both the print sink and the audit sink",
    "registerFlow is imported in index.ts",
    "registerFlow is called with program in index.ts",
    "buildProgram registers a flow command",
    "flow command has subcommands (list and add)"
   ],
   "cli/autonomous-i18n.test.ts": [
    "status (en, default) → English strings sourced from the central catalog",
    "status (tr) → Turkish strings sourced from the central catalog",
    "pending-count interpolation honors {count} placeholder",
    "recent-audit header uses i18n with {count}",
    "stop → i18n message in both en and tr, marker file written",
    "autonomous source has zero hardcoded user-facing console output"
   ],
   "cli/autonomous-mission.test.ts": [
    "creates a list-mission + work-items in the real store",
    "passes tenant and deliverTo through to the mission",
    "uses getMessage key for output (i18n-first scaffold)",
    "rejects $name before mission persistence with a localized reason",
    "creates a goal-mission in the real store",
    "uses goal as title when --title is omitted",
    "uses --title when provided",
    "prints empty message when no db exists",
    "lists existing missions as a table",
    "outputs valid JSON with --json flag",
    "parses bare kind",
    "parses kind:json-spec",
    "parses kind:plain-string as {description}",
    "registers autonomous-mission command group on program",
    "DISABLED (no config): create-list warns loudly the mission will not drain",
    "DISABLED (autonomous.enabled=false): create-goal warns",
    "ENABLED (autonomous.enabled=true): NO warning — mission will drain",
    "ENABLED: create-goal also stays quiet"
   ],
   "cli/autonomous-notify-lifecycle.test.ts": [
    "constructs connectors once for the whole loop and awaits canonical close on normal stop",
    "aborts on SIGTERM and closes the same dispatcher without leaving signal listeners",
    "still closes notifications when sibling teardown throws"
   ],
   "cli/autonomous-plan.test.ts": [
    "writes the planned items to the backlog as pending+planned",
    "dry-run prints but does NOT write",
    "writes a v2 plan atomically to MissionStore without creating a backlog",
    "replays an exact v2 plan without duplicate missions, items, or create audit events",
    "v2 dry-run creates neither MissionStore nor backlog state",
    "persists a changed v2 planner batch as a distinct mission",
    "rejects an unwired v2 kind before creating MissionStore state and localizes the reason"
   ],
   "cli/autonomous-planner-transport.test.ts": [
    "delivers adapter-owned stdin larger than 128 KiB without putting it in argv",
    "preserves argv-fed adapters without synthesizing stdin",
    "surfaces spawn errors through the canonical planner error",
    "classifies the deadline as timeout and forwards its configured bound",
    "rejects non-zero status even when the provider wrote partial stdout",
    "unwraps the provider response envelope only after successful transport",
    "retains the existing bounded response limit",
    "does not block the event loop while the planner transport is pending",
    "registers every planner transport failure in both supported languages",
    "renders the typed transport failure in Turkish at the CLI boundary",
    "localizes the canonical typed output-limit failure without leaking its core message"
   ],
   "cli/autonomous-tick-reporter.test.ts": [
    "notifies ONCE per parked trigger across repeated pending cycles",
    "suppresses idle no_trigger ticks (no print, no notify)",
    "prints a feedback line when a trigger parks",
    "re-notifies if the same trigger parks again after resolving"
   ],
   "cli/autonomous-v1-provider-authority.test.ts": [
    "opens once, skips provider bootstrap, injects HOLD admission, and closes once",
    "preserves prior v1 bootstrap behavior when no owner-authored authority is configured"
   ],
   "cli/autonomous-v2-goaldeps.test.ts": [
    "uses the configured Brain model rather than the worker default",
    "preserves an explicit canonical API model identity",
    "fails loud when an explicit provider does not own the model",
    "retains parsed provider output for durable mission commit instead of accepting it in the parser",
    "runs the injected role admission before either provider completion",
    "preserves id, kind, description, and canonical trigger through the Goal projection",
    "returns [] when the LLM emits an empty item list (goal-reached / dry signal)",
    "tolerates a code-fenced provider envelope and still parses items",
    "feeds the goal + prior-work into the planner prompt (so the model can go dry)",
    "returns true when the LLM verdict is { reached: true }",
    "returns false when the LLM verdict is { reached: false }",
    "defaults to false on an unparseable / ambiguous verdict (conservative)",
    "injects the exact contract and returns host-provenanced criterion evidence",
    "does not apply the legacy positive-token regex to an explicit contract",
    "passes goalDeps (with author + accept fns) into runV2Engine",
    "holds production Goal author/accepter before the planner provider when authority is unavailable",
    "parks v2 task work before Task JSON or provider spawn when host authority is unavailable"
   ],
   "cli/bg-turns-producer.test.ts": [
    "parses the sprint-finalizer.ts COMPLETE shape (evaluations{})",
    "parses the job-runner.ts FAILED shape (tasks[], no metrics)",
    "a RUNNING job returns null — nothing to report yet",
    "corrupt JSON returns null, never throws",
    "an unknown status string returns null",
    "falls back to the filename-derived id when jobId/sprintId are both absent",
    "a job ALREADY COMPLETE on disk at construction time does not fire onComplete",
    "a RUNNING job at construction time DOES fire once it later transitions to COMPLETE",
    "a brand-new job file appearing AFTER attach, already terminal, fires (live-session scenario)",
    "onComplete fires exactly once across multiple scans for the same unchanged terminal record",
    "a transition is detected via the poll timer alone when fs.watch never fires",
    "the fs.watch path reacts independently of the poll timer",
    "the poll interval is created unref\\'d (never pins the host process alive)",
    "dispose() clears the poll interval and closes the fs.watch handle",
    "dispose() is idempotent — calling it twice does not throw or double-close",
    "no handler fires for an event that arrives after dispose() (MOAT-2 linger)",
    "fs.watch construction failure degrades gracefully — poll fallback still works",
    "detects an externally-written COMPLETE job without any injected watch/scan",
    "a missing jobsDir degrades to no-op (never throws at construction)",
    "formats a COMPLETE job as a compact, language-neutral summary",
    "formats a FAILED job with an error message",
    "formats a FAILED job with no error message",
    "prefers sprintId over jobId as the event source when both are present",
    "missing metrics counts default to 0 rather than \"undefined\" in the summary",
    "uses the resolved Turkish catalog while preserving ids, counts, verdict codes, and raw error",
    "maps only owned punctuation in ASCII mode and preserves raw source and error bytes",
    "enabled=false -> watchFactory is never called, returns undefined",
    "constructs the watch against the supplied jobsDir and feeds a completed job into enqueueBg as a ChatTurnBgEvent",
    "a FAILED job feeds a FAILED-shaped ChatTurnBgEvent through the same enqueueBg site",
    "threads the session language through the watch callback",
    "threads the resolved glyph palette through the actual watch callback",
    "uses the real createRunCompletionWatch as its default watchFactory when none is injected"
   ],
   "cli/bin-entry-validation.test.ts": [
    "bin.deckent resolves to ./dist/cli/entry.js",
    "bin entry path starts with ./dist (compiled output)",
    "package has type:module required for ESM shebang execution",
    "engines.node is >= 24 (Active LTS — Node 18/20/22 EOL by May 2026)",
    "files array contains dist so bin target is published",
    "prepublishOnly script runs build to ensure dist/ is fresh",
    "first line is shebang #!/usr/bin/env node",
    "calls buildProgram().parseAsync(process.argv)",
    "has unhandledRejection handler to catch async errors",
    "handles SIGINT for graceful ctrl-c shutdown",
    "handles SIGTERM for container/systemd shutdown",
    "has Node version guard requiring >= 24 (Active LTS)",
    "imports handleCliError from helpers/process",
    "buildProgram is exported from src/cli/index.ts",
    "buildProgram registers doctor command (deckent doctor)",
    "buildProgram sets program name to deckent",
    "buildProgram has --version option for deckent --version",
    "buildProgram() returns a Command instance without throwing",
    "program.name() returns deckent",
    "program has doctor command registered",
    "program has init command registered",
    "program has start command registered",
    "no postinstall script in package.json (npm handles chmod +x via bin field)",
    "entry.ts shebang is compatible with npx (no shell wrapper needed)"
   ],
   "cli/bot-approval-risk-tier.test.ts": [
    "rejects a high+critical request before channel authentication"
   ],
   "cli/bot-listen.test.ts": [
    "no connectors configured → prints nothing-to-listen + disposes + returns (bounded)",
    "active connectors → prints active banner listing them, then waits, then stops",
    "stops safely when the process ownership record cannot be claimed",
    "status reports ambiguous ownership as an error",
    "start and stop preserve fail-closed ownership results"
   ],
   "cli/bot-relay.test.ts": [
    "handleBotListen keeps the disabled path byte-compatible with the two-argument bootstrap",
    "relay_enabled=false performs zero authority or relay construction",
    "attaches the existing Telegram transport and pushes pending requests",
    "resolves a short code and returns the localized decided outcome",
    "rejects an ambiguous short code before invoking authority",
    "critical requests fail authentication and return the CLI-only hint"
   ],
   "cli/buildProgram-smoke.test.ts": [
    "does not throw on commander register",
    "registers all commands without duplicates",
    "every command has a non-empty description",
    "every command has a unique name",
    "known critical commands are present",
    "watch command description advertises docker-worker following",
    "top-level help points users to the localized quick-reference"
   ],
   "cli/builtins/agsk-expand-skills.test.ts": [
    "%s has manifest.json and SKILL.md",
    "%s manifest passes validateSkillDefinition",
    "%s manifest id matches directory name",
    "%s manifest has entrypoint SKILL.md and is enabled",
    "loads all 3 new skill ids from the real .deckent/skills/ tree",
    "each loaded skill keeps its declared category",
    "%s SKILL.md is <= 4KB",
    "%s SKILL.md starts with a markdown heading",
    "%s SKILL.md has an Anti-Patterns section",
    "%s SKILL.md has a Karpathy Notes section",
    "%s SKILL.md references a real sprint/born lesson",
    "composableWith references only real, existing skill ids",
    "no new skill is composable with itself",
    "all new skill ids are unique across the pool"
   ],
   "cli/calltool-exec-wire.test.ts": [
    "absent block or enabled!==true → undefined (fail-closed on config-load failure)",
    "enabled:true → options; valid riskThreshold kept",
    "INVALID riskThreshold is DROPPED (pre-fix it fell through and disabled the confirm gate — fail-open)",
    "run.tsx threads resolveToolSurfaceOptions into BOTH buildNativeToolRegistry and createNativeEngine",
    "unknown target → throws (catalog⊂registry invariant fail-closed)",
    "DENY-GLOB PARITY (advisor P0): a user deny-rule on the target holds through call_tool with ZERO prompt",
    "silent-tier target executes with no prompt; handler result returned",
    "confirm-tier target asks EXACTLY ONCE (double-prompt regression) and executes on y",
    "confirm-tier target: user n → rejected, handler never runs",
    "an allow-grant on the target auto-allows the nested call too (grant parity)",
    "armed surface: call_tool on a safe registered tool executes for real",
    "default (bridge absent): call_tool still fails closed with NOT_WIRED message",
    "REAL e2e: fake mcpBridge tool → call_tool handler → dispatch → parity → target handler, EXACTLY 1 prompt",
    "tierMap override parity: policy's silent→confirm escalation asks on the nested path too",
    "alwaysFloor parity: a floored tool asks even in full-auto mode (decide rule-2 precedes mode)",
    "mode parity: full-auto lets a confirm-tier target through with no prompt",
    "self-mod elevation parity: in a deckent repo, a src/ write asks DESPITE a '**' grant",
    "'a' degrade pin: nested 'a' runs the call but the SECOND identical call asks again (no persist)",
    "explicit riskThreshold honor (P1): threshold=safe escalates even a silent-tier allow to ask",
    "a partial tool_surface block (riskThreshold only) keeps enabled=true",
    "explicit enabled:false still opts out; absent block stays default-ON",
    "omitting toolSurface registers nothing new (no meta-tools)",
    "toolSurface.enabled=false produces a byte-identical tool list to omitting it entirely",
    "registers deckent_search_tools / deckent_describe_tool / deckent_call_tool",
    "does not remove or alter any pre-existing tool (superset of the flag-off list)",
    "finds a bridged tool by keyword and embeds the TOOL-CORE deferred-index pointer in its description",
    "returns an empty array for a query with no matches",
    "describes a known bridged tool with category/risk/params",
    "marks an unknown tool name as ok:false with an [mcp-error] tag",
    "unknown tool name short-circuits to unknown_tool, never reaching confirm/exec",
    "a safe-risk tool (silent tier) executes via the injected fake execImpl with no confirm required",
    "a moderate+-risk tool (confirm/always tier) is denied when no confirm seam is injected — risk threshold gates it (fail-closed)",
    "a moderate+-risk tool runs the injected fake execImpl once the injected confirm seam allows it",
    "never performs real execution by default: an absent execImpl fails closed with a descriptive error, not a silent no-op success"
   ],
   "cli/canonical-cutover-specs.test.ts": [
    "is named deckent_propose_run and states it is the canonical entry point",
    "is NOT part of the base CLI_BRIDGE_TOOLS catalog — only the registry adds it, flag-on",
    "the note literally labels the tools an expert-level escape hatch pointing at the canonical path",
    "the escape-hatch trio is exactly set_directives/plan/start — no drift",
    "nogo guard: none of the escape-hatch trio has been deleted from CLI_BRIDGE_TOOLS",
    "flag-off bit-eş guard: the trio\\'s base descriptions carry no escape-hatch text",
    "base catalog stays at or above the born-596 parity floor",
    "flag-on total (as derivable from this module alone) is exactly base + 1"
   ],
   "cli/catalog-lazy-bootstrap.test.ts": [
    "read-only / non-model commands are NOT catalog-dependent",
    "the model-dependent class (start/plan/run/models/chat) IS catalog-dependent",
    "unknown command names default to non-dependent (safe default)",
    "every catalogDependent entry is a real registered command",
    "extracts the first non-flag token as the top-level command",
    "returns undefined for flag-only / empty argv",
    "RED→GREEN: status-class commands do not trigger catalog bootstrap (network-free)",
    "GREEN: model-dependent commands still trigger catalog bootstrap",
    "does not false-positive on nested subcommands sharing a leaf name with a top-level catalog-dependent command",
    "honors the parsed command-local --offline option before catalog bootstrap",
    "preserves DECKENT_OFFLINE=1 without treating other values as offline",
    "skips redundant global bootstrap only for models list/active-set JSON reads",
    "keeps human, mutating, unknown, and sibling commands on normal bootstrap policy",
    "fires exactly once immediately before a genuine network fetch (cold cache, online)",
    "never fires when the cache is warm — warm-cache path stays byte-behavior-identical",
    "warm-cache loadCatalog result is unaffected by onFetchAttempt presence (byte-identical)",
    "never fires in offline mode (DECKENT_OFFLINE=1 path)",
    "has both en and tr translations, distinct from the raw key and from each other"
   ],
   "cli/catalog-render.test.ts": [
    "groups entries by category in first-appearance order, preserving entry order within a group",
    "is deterministic across repeated calls with the same fixture",
    "suppresses the risk marker entirely for low/medium risk (empty label string)",
    "returns labels.emptyState verbatim when entries is empty",
    "renders entirely different visible text for a structurally identical TR labels fixture",
    "preserves identical line/category structure across label fixtures (same line count)",
    "emits no ANSI escape codes when options.noColor=true",
    "emits no ANSI escape codes when the NO_COLOR env var is set (auto-detect path)",
    "emits ANSI color codes by default when NO_COLOR is unset (proves the color path is reachable)",
    "emits no ANSI by default when stdout is not a TTY (pipe / redirect determinism)",
    "FORCE_COLOR=1 re-enables color off-TTY by default (same precedence as theme.ts)",
    "colored and non-colored renders carry the same visible text once ANSI is stripped"
   ],
   "cli/chat-agentic-dispatch.test.ts": [
    "maps Turkish \"sprint durumu ne\" → deckent_status",
    "maps \"son sprinti göster\" → deckent_history",
    "maps \"hafızada rbac ara\" → deckent_memory_query with query",
    "maps \"plan\" / \"sprint planla\" → deckent_plan with mode auto",
    "returns no_match for unrelated input",
    "returns no_match for empty/whitespace input",
    "status intent → dispatches deckent_status and returns its output",
    "recall intent → dispatches deckent_memory_query with extracted query",
    "unknown intent → graceful no_match, dispatcher NOT called",
    "handles multiple distinct intents — each call routes to its own tool",
    "English intents — \"status\" / \"history\" / \"search\" also map correctly"
   ],
   "cli/chat-banner-i18n.test.ts": [
    "renders the injected hint verbatim and never the old hardcoded Turkish literal",
    "throws a typed InjectedLabelMissingError when the hint is not injected (no silent English fallback)",
    "carries no user-visible text of its own: the rendered banner contains only ctx fields and the injected hint",
    "tui.banner.hint resolves in en AND tr; en is English, tr is the former Turkish text",
    "stays silent off-TTY (pipe contract unchanged) — with the hint still required by contract"
   ],
   "cli/chat-banner.test.ts": [
    "renders banner with deckent name and provider when tty=true",
    "includes /help hint in banner output",
    "reflects the provided provider name in output",
    "returns empty string when tty=false (pipe/non-TTY context)",
    "includes dir in banner output",
    "contains ANSI escape codes when tty=true and the color gate allows it (FORCE_COLOR=1)",
    "contains no ANSI escape codes when tty=false"
   ],
   "cli/chat-enterprise-bridge.test.ts": [
    "/audit → dispatches to audit handler",
    "/rbac → dispatches to rbac roles handler",
    "/cost → dispatches to cost show handler",
    "/flow → dispatches to flow list handler",
    "unknown slash → handled: false, spawnFn not called",
    "case-insensitive — /AUDIT dispatches correctly",
    "extra args are appended after default subcommand args",
    "enterpriseSlashNames returns all 4 enterprise commands"
   ],
   "cli/chat-intent-dispatch.test.ts": [
    "resolves '${reply}' to dispatch descriptor ${JSON.stringify(descriptor)}",
    "every resolved command name exists in the command-registry SSOT",
    "requiresConfirm is derived from the registry risk tier, not hardcoded true for every bridge",
    "does not touch lastMetaResponse\\'s shape (368-004 exact-match tests stay intact)",
    "lastMetaDispatch is set alongside lastMetaResponse and preserves interrupt-resume (`pending` untouched)",
    "lastMetaDispatch clears alongside lastMetaResponse on a subsequent normal reply",
    "lastMetaDispatch clears alongside lastMetaResponse on a subsequent unrecognized reply",
    "never invokes the resolved command — descriptor data only, no side effects",
    "state remains plain JSON data after a meta-intent reply, including the new field (pause/resume safe)"
   ],
   "cli/chat-intent-executor.test.ts": [
    "confirm-approve: requiresConfirm=true + confirm resolves true -> runner runs -> status \"ran\"",
    "confirm-reject: requiresConfirm=true + confirm resolves false -> runner never called -> status \"cancelled\"",
    "success: requiresConfirm=false -> confirm never invoked, runner runs directly -> status \"ran\"",
    "runner-error: runner throws -> status \"refused\" with error captured",
    "honest-cancel: requiresConfirm=true with confirm omitted -> \"cancelled\", runner never called",
    "supports a synchronous (non-Promise) runner and confirm",
    "passes args through unchanged to runner",
    "runner receives a command name that exists in the command-registry SSOT (read-only assert)",
    "result stays plain JSON-serializable data (no function/promise leaks)",
    "never invokes the real command-registry or a TTY — runner/confirm are the only side-effect seams"
   ],
   "cli/chat-layout.test.ts": [
    "plain `› ` prefix on non-TTY",
    "colour-wrapped prefix on TTY, line still present",
    "plain `● deckent` on non-TTY",
    "colour-wrapped + bold on TTY",
    "returns empty string on non-TTY (dropped by caller)",
    "returns a decorated rule on TTY"
   ],
   "cli/chat-mode.test.ts": [
    "defaults to user mode when no config and no env var",
    "returns user mode when config.chat.mode = \"user\"",
    "returns enterprise mode when config.chat.mode = \"enterprise\"",
    "DECKENT_CHAT_MODE=enterprise overrides config (enterprise wins)",
    "DECKENT_CHAT_MODE=user overrides config (user wins)",
    "invalid config value falls back to user mode",
    "undefined chat config falls back to user mode",
    "null-ish chat.mode falls back to user mode",
    "user mode: hides all enterprise commands from /help list",
    "user mode: retains user-facing commands (/status, /recall, /help)",
    "enterprise mode: shows all commands including enterprise ones",
    "enterprise mode: returns full registry (no filtering)",
    "enterprise-slash hidden in user /help but full registry still has it (capability present)",
    "empty registry returns empty array for both modes",
    "/audit is enterprise",
    "/rbac is enterprise",
    "/flow is enterprise",
    "/cost is enterprise",
    "/status is NOT enterprise",
    "/help is NOT enterprise",
    "/recall is NOT enterprise"
   ],
   "cli/chat-naive-mode.test.ts": [
    "pins the Trinity AI-Asistan persona phrase",
    "documents the casual / naïve rule with no-MCP guidance",
    "documents the task rule with concrete tool mappings",
    "documents the ambiguous rule with clarification step",
    "forbids fabricated tools / tool results",
    "returns \"casual\" for greetings in TR and EN",
    "returns \"task\" for actionable verbs",
    "returns \"task\" for autonomous + nervous automation intents (TR + EN)",
    "returns \"ambiguous\" when no rule matches",
    "prefers \"task\" over \"casual\" when both markers are present",
    "omits the system prompt when naïve mode is off (default — preserves legacy spawn shape)",
    "injects DECKENT_CHAT_SYSTEM_PROMPT env var for every host when naïve mode is on",
    "passes --append-system-prompt to claude with the prompt body",
    "does NOT pass --append-system-prompt to codex or gemini (env var only)"
   ],
   "cli/chat-native-clear-context.test.ts": [
    "/clear still empties the transcript when the provider has no exit()",
    "does not call .exit and does not throw for a non-persistent provider",
    "/clear calls provider.exit() when the provider exposes it (warm-child reset)",
    "/clear resets context: the resumed turn cannot see pre-clear history in what is sent",
    "/clear awaits a slow exit() before dispatching the next turn",
    "a real ENOENT spawn does not crash the process and rejects chunks iteration",
    "wait resolves with a null exitCode even though close is not guaranteed after error"
   ],
   "cli/chat-native-flag.test.ts": [
    "ChatOptions interface accepts native boolean and command exposes --native option (flag parse)",
    "default-B preserved: without --native, spawn is used and runChatNativeLoop is not called",
    "native-route: with --native, runChatNativeLoop is called and spawn is not"
   ],
   "cli/chat-native-flags.test.ts": [
    "ChatOptions interface accepts once and message fields",
    "--once and --message options are registered on the chat command",
    "--message <text> uses provided text as single-turn input and calls runChatNativeLoop",
    "--native --once calls runChatNativeLoop with maxTurns=1 and gracefulErrors=true",
    "without --once/--message, --native routes to interactive REPL (no maxTurns limit)",
    "when createSubscriptionChatAdapter throws, falls back to stub provider and still calls runChatNativeLoop"
   ],
   "cli/chat-native-indicator.test.ts": [
    "emits user message, assistant header and separator when layoutEnabled",
    "does NOT echo the user line when layoutEnabled is absent (default off)",
    "starts after header and stops once output is produced",
    "stops the indicator even on an empty response (finally safety net)",
    "stops the indicator exactly once across multiple output chunks"
   ],
   "cli/chat-native-memory.test.ts": [
    "appends user and assistant turns to memory after each exchange",
    "does not append assistant turn when provider returns empty text",
    "pre-populates transcript with history from memory when resumeLimit > 0",
    "works normally when getChatHistory returns empty array (fresh session)",
    "works normally without memory option at all (backward compat)",
    "each turn is persisted exactly once — no double-appends per user message"
   ],
   "cli/chat-native-multiturn.test.ts": [
    "returns full transcript when n is undefined (no truncation)",
    "returns full transcript when transcript.length <= n",
    "returns last n turns when transcript longer than n",
    "order preserved: last-n turns appear in original order",
    "last N inject: only contextWindowSize turns sent to provider on each call",
    "window overflow truncate: large history with small window sends correct slice",
    "first turn empty context: fresh session first message → only 1 user turn sent",
    "order preserved: context turns arrive in correct chronological order",
    "no contextWindowSize: full transcript sent (backward-compat)",
    "echoProvider regression: streaming path unaffected by context window"
   ],
   "cli/chat-native-persist.test.ts": [
    "appends user and assistant turns to memory with correct sessionId",
    "auto-generates sessionId when not provided and persists with it",
    "pre-populates transcript with prior history when resumeLimit > 0",
    "does not call getChatHistory when resumeLimit is 0 (default)",
    "/resume (no arg) lists recent sessions",
    "/resume <n> loads that session into the transcript and switches it",
    "reports only a successful memory-backed resume through the optional App callback",
    "does not change the App identity callback for missing memory or a failed resume",
    "restores a strict ledger before legacy memory, preserves tool linkage, and keeps numeric picker precedence",
    "restores an exact literal ledger beyond the five recent memory rows",
    "treats an empty or malformed present ledger as unavailable instead of falling back",
    "appends caller-owned local chat context to /status without interpreting run output",
    "/resume <bad> reports not-found",
    "/resume with no memory adapter reports memory unavailable",
    "handles empty getChatHistory gracefully — fresh session starts with no prior turns",
    "works without memory adapter at all — no-op, backward compatible",
    "only sends last N turns to provider when contextWindowSize is set",
    "getRecentTurns returns full transcript when contextWindowSize exceeds length",
    "getRecentTurns slices to last N when transcript is longer than window"
   ],
   "cli/chat-native-provider-switch.test.ts": [
    "rebuilds the live adapter and reports the real switch",
    "surfaces an honest error and never falls back to claude",
    "reports switching unavailable instead of a fake \"switched\" confirmation",
    "honors an injected switchUnavailable formatter (caller-localized / i18n)",
    "normal conversation turns are unaffected (no /provider)",
    "bare /provider still shows the usage hint",
    "switchProvider rebuilds the adapter; the proxy delegates to the rebuilt one",
    "switchProvider throws ProviderNotFoundError for an unknown name (no fallback)"
   ],
   "cli/chat-native-provider.test.ts": [
    "resolves the named provider from the registry",
    "falls back to the registry default when no name is given",
    "throws ProviderNotFoundError when the named provider is missing",
    "subscription env strips ANTHROPIC_API_KEY before spawning the CLI",
    "honors a custom binary + extraArgs override",
    "send() concatenates stdout chunks into the response text",
    "stream() yields each non-empty chunk then a terminal done marker",
    "tags each message by role; tool turns use tool-result",
    "exposes a chunks AsyncIterable and a wait promise"
   ],
   "cli/chat-native-roundtrip.test.ts": [
    "resolves adapter from named provider and exposes ChatProviderAdapter shape",
    "hata: throws ProviderNotFoundError when registry lookup misses",
    "runs user → adapter → stdout → transcript round-trip with no tools",
    "subscription path: spawns the claude binary with --print and stripped API keys",
    "honors custom binary + extraArgs override (e.g. for codex CLI)",
    "round-trip survives multiple sequential user turns sharing one adapter",
    "boş mesaj: skips empty/whitespace input without invoking the adapter",
    "hata graceful: continues session when adapter.send throws",
    "uses registry default when no providerName is given"
   ],
   "cli/chat-native-stream.test.ts": [
    "stream chunk flow: each text chunk writes to output incrementally",
    "full response assembles: concatenated chunks match transcript assistant turn",
    "empty stream: only done chunk → no text output but assistant turn recorded",
    "mid-stream error: stream throws → error propagates from loop",
    "fallback: provider without stream() still uses send() (regression guard)"
   ],
   "cli/chat-native-tooldispatch.test.ts": [
    "dispatches a known tool through the registry and returns its string result",
    "returns [mcp-error] for unknown tool names instead of throwing",
    "serializes non-string registry output via JSON.stringify",
    "captures invoke errors as tagged error strings (no throw)",
    "enforces allowList — disallowed tool returns [mcp-error] without invoking",
    "feeds a real registry-backed tool result back into the chat loop transcript",
    "handles multiple tool calls in one turn sequentially",
    "round-trips an unknown tool name without breaking the loop"
   ],
   "cli/chat-nervous-bridge.test.ts": [
    "returns empty array when no pending file exists",
    "reads notifications from .deckent/nervous/nervous-pending.json",
    "returns empty array on corrupted JSON",
    "returns empty array when file contains non-array JSON",
    "returns empty string when no pending items",
    "returns plain summary on non-TTY",
    "returns ANSI-styled banner on TTY",
    "includes accept/reject hint on TTY",
    "lists pending notifications (no-arg defaults to list)",
    "reports \"bekleyen yok\" when pending is empty on list",
    "accept removes notification from pending file",
    "reject removes notification from pending file",
    "accept by partial id prefix works",
    "appends to history on accept",
    "appends to history on reject",
    "returns error when id missing for accept",
    "returns error when notification not found",
    "returns ANSI-colored output on accept in TTY mode",
    "pending-yok → silent empty on list (no TTY)"
   ],
   "cli/chat-no-cli-fallback.test.ts": [
    "shows a clear error listing all searched providers when none are found",
    "includes --native flag as an alternative in the error message",
    "includes deckent serve / dashboard chat suggestion in the error message",
    "includes install instructions in the error message",
    "spawns normally when a CLI is available (no no-provider error)"
   ],
   "cli/chat-perm-concurrency.test.ts": [
    "two concurrent grants from separate store instances both persist (no last-writer-wins loss)",
    "a store instance created after two concurrent grants observes both",
    "B loads its stale snapshot after A has already granted once, then A grants again concurrently — all three survive",
    "concurrent grants preserve unrelated top-level fields (merge, not overwrite)",
    "granting the same tool from two stale instances does not duplicate or lose entries"
   ],
   "cli/chat-permissions.test.ts": [
    "isAllowed is false before any approval",
    "allow() persists to .deckent/settings.local.json under permissions.allow",
    "a fresh store loads previously persisted approvals",
    "preserves other fields in settings.local.json (merge, not overwrite)",
    "malformed file → empty allow-set (fail-safe)"
   ],
   "cli/chat-render-markdown.test.ts": [
    "bold survives an inline-code span nested inside it",
    "heading style survives an inline-code span nested inside it",
    "bold style survives a markdown link span nested inside it",
    "plain bold with no nested span is unaffected (no spurious extra RESET/BOLD)",
    "does not truncate a Wikipedia-style URL with a balanced paren",
    "still handles a plain URL with no parens (no regression)",
    "still handles a link with a title attribute (no regression)",
    "uses ASCII frames/lists/rules while preserving raw Unicode content",
    "degrades wide tables to one bullet line per cell when maxTerminalWidth is set",
    "wraps an overlong compact cell value across display-width lines",
    "aligns authored code/table frames by display cells for CJK and emoji"
   ],
   "cli/chat-render.test.ts": [
    "renders fenced code blocks framed by the accent role with the code-role label (never dim)",
    "renders ATX headings with a visual hierarchy (# bold+info, ## bold)",
    "renders **bold** text with BOLD ANSI code",
    "returns plain text unchanged when tty=false (pipe/non-TTY context)",
    "renders inline code with the code role (bright blue), never dim",
    "renders unordered list items as accent bullet points (indent-preserving)",
    "handles empty string without throwing",
    "handles plain prose (no markdown) unchanged in TTY mode",
    "never emits SGR dim on any element (kitchen sink)",
    "tty=true still obeys the color gate: NO_COLOR → markers stripped, no ANSI at all",
    "markdown link → OSC-8 hyperlink with the underlined link role (hyperlinks on)",
    "markdown link (hyperlinks off / default) → label in the link role + the URL visible, no OSC byte",
    "bare URL → clickable OSC-8 (on); untouched plain URL (off)",
    "does NOT double-wrap a URL already inside a markdown link",
    "a file reference is never wrapped in OSC 8 — the host detects path:line:col itself",
    "project file path → code role",
    "file path with line number → colored incl. :NN",
    "relative link → link role text + the path in parentheses (no dim)",
    "non-TTY → passthrough (no ANSI, no OSC-8)",
    "does not colorize a path segment inside a URL",
    "table → boxed + aligned (├ ┼ │ borders, headers bold)",
    "fenced code block → framed box with language label",
    "syntax highlighting uses roles and attributes only (keyword bold, comment italic, no dim, no raw chalk color)",
    "admonition [!WARNING] → warning role header word + left bar",
    "<kbd> → inverse badge",
    "strikethrough ~~x~~ → STRIKE ansi",
    "horizontal rule --- → accent line",
    "ordered list 1. → accent number",
    "blockquote > → left bar, quoted text in the default foreground",
    "code block content is NOT corrupted by inline path/bold passes",
    "non-TTY → all block elements pass through unchanged"
   ],
   "cli/chat-repl-ux.test.ts": [
    "starts empty with size 0",
    "push stores entry and navigateUp returns it",
    "navigateUp then navigateDown returns undefined (back at current line)",
    "respects capacity (ring buffer evicts oldest)",
    "does not push empty or whitespace-only entries",
    "reset() brings navIndex back so navigateUp starts from latest",
    "/exit → action: exit",
    "/quit → action: exit (alias)",
    "/clear → action: clear",
    "regular text → action: none",
    "/EXIT (uppercase) → action: exit (case-insensitive)",
    "unknown slash command → action: none",
    "first handle() returns cancel (message cancel, not exit)",
    "second handle() returns exit (user pressed Ctrl-C twice)",
    "reset() after first handle allows cancel again",
    "multiple resets do not affect handle sequence",
    "single line without trailing backslash is complete",
    "line ending with backslash is not complete yet",
    "second line without backslash completes multi-line input",
    "reset() discards accumulated parts",
    "three-line continuation joins all parts with newline"
   ],
   "cli/chat-resume-flag.test.ts": [
    "is 10",
    "returns empty array when DB does not exist (clean start)",
    "uses DEFAULT_RESUME_LIMIT when limit is omitted",
    "shows \"Starting fresh\" when turns array is empty",
    "formats turns with role prefix and turn_index",
    "includes turn count in the header"
   ],
   "cli/chat-resume.test.ts": [
    "renders a numbered list with preview + turn count",
    "renders the empty-state message when there are no sessions",
    "honors Turkish",
    "maps a 1-based number to the Nth session id",
    "returns null for an out-of-range number",
    "treats a non-numeric arg as a literal session id",
    "returns null for a blank arg",
    "renders a header + user/assistant prefixed turns",
    "returns the not-found message for an empty history",
    "truncates a very long turn"
   ],
   "cli/chat-session-multi-result.test.ts": [
    "3 consecutive tool messages → all 3 results present in combined block",
    "order preserved: [1/N], [2/N], [3/N] labels appear in sequence",
    "toolUseId included as label for each result entry",
    "single tool message → exact existing format preserved (bit-for-bit)",
    "single tool message does not include [1/1] order label",
    "pure user message → falls back to last user text content",
    "user+assistant transcript → extracts last user text"
   ],
   "cli/chat-session-persistent.test.ts": [
    "extracts incremental text from content_block_delta events",
    "marks done on result events and surfaces resultText as fallback",
    "returns empty text for unknown / malformed events without throwing",
    "unwraps stream_event envelope to extract incremental token deltas",
    "ignores non-delta stream_event envelopes (message_start, etc.)",
    "spawns exactly once for multiple sends (reuse the same child)",
    "writes valid stream-json user lines for each turn to the SAME stdin",
    "exit() kills the persistent child and marks the session not alive (idempotent)",
    "exit() before any send is a no-op (lazy spawn)",
    "yields incremental delta chunks then a final done chunk",
    "falls back to resultText when no deltas arrived (single end_turn line)",
    "buildSubscriptionEnv strips API-key vars but keeps the rest",
    "DEFAULT_PERSISTENT_ARGS opts the CLI into persistent stream-json mode",
    "defaultPersistentSpawn returns the expected handle shape (structural smoke)",
    "parses a single <deckent_tool> directive into a ToolCall",
    "parses multiple directives with positional ids",
    "skips malformed JSON tags and returns [] for plain prose",
    "surfaces <deckent_tool> reply as stopReason:tool_use + toolCalls",
    "feeds a tool result back as a user turn (so the model replies, not re-loops)",
    "passes the agentic system prompt via --append-system-prompt when set"
   ],
   "cli/chat-session-reconcile.test.ts": [
    "resultText longer than streamed deltas → missing suffix is streamed, screen matches final",
    "assistantText longer than streamed deltas → missing suffix is streamed, screen matches final",
    "longer candidate that does NOT extend the streamed text is discarded, not silently swapped",
    "resultText and assistantText both extend the stream but diverge from each other → longer candidate wins, screen stays consistent",
    "normal turn: delta sum already equals final text → no extra chunk, behavior unchanged",
    "no deltas at all (result-only) → fallback yields exactly once, no double-yield"
   ],
   "cli/chat-session-stream-collect.test.ts": [
    "extracts text from assistant complete-message event into assistantText",
    "joins multiple text content blocks from assistant event",
    "skips non-text content blocks (tool_use, image) in assistant event",
    "returns empty when assistant event has no text content blocks",
    "returns empty for assistant event with malformed message structure",
    "(1) bare tag in single delta → tool_use dispatched",
    "(2) bare tag split across multiple deltas → tool_use dispatched",
    "(3) tag only in result event, no deltas → tool_use dispatched",
    "(4) prose in deltas + tag only in result → tool_use dispatched (H2-A)",
    "(5) tag in middle of prose, single delta → tool_use dispatched",
    "(6) tag in middle of prose, surrounding prose split across deltas → tool_use",
    "(7) two tags in assistant event, result empty → both tags dispatched (H2-B)",
    "(8) tag-1 in delta, tag-2+3 in assistant event → all 3 dispatched (H2-C)",
    "(9) tag inside code fence, single delta → tool_use dispatched",
    "(10) code fence prose in deltas, tag only in result → tool_use dispatched",
    "backward-compat: no-delta result-only still yields text and correct done",
    "three-tag reply with result as authority (result longer than delta-sum)",
    "assistant event with multiple text parts accumulates all into assistantText",
    "end_turn reply (no tags) stays end_turn after reconciliation",
    "does not contain \"AÇIKLAMA YAPMA\" (kırılganlık-semptomu kaldırıldı)",
    "still instructs model to produce deckent_tool tags",
    "still lists valid tools"
   ],
   "cli/chat-setup-intents.test.ts": [
    "recognizes '${action}' across its tr+en phrasings",
    "is case-insensitive for ascii phrasings",
    "still falls through to unknown for text matching no meta pattern",
    "an exact pending-choice match wins over the meta matcher",
    "yes/no/skip word matching is unaffected by the new meta patterns",
    "a meta reply during mcp_suggestion leaves `pending` untouched and records a suggestion",
    "each of the 4 meta actions maps to its own suggestion key",
    "lastMetaResponse clears once a normal (non-meta) reply is given",
    "lastMetaResponse also clears on a subsequent unrecognized reply",
    "the fallback seam is never invoked for a deterministically-matched meta intent",
    "a meta reply during a workspace_mode choice question also preserves the pending choice question",
    "every state remains plain JSON data after a meta-intent reply (pause/resume safe)"
   ],
   "cli/chat-slash-interrogate.test.ts": [
    "registry includes /interrogate",
    "/interrogate has no agenticTool (meta-command)",
    "/interrogate has a non-empty desc",
    "shows intro header when DIRECTIVES.md exists",
    "renders 5 numbered questions from DIRECTIVES",
    "does NOT call provider for /interrogate (pure meta-command)",
    "shows honest \"not found\" message when DIRECTIVES.md is missing",
    "adds /interrogate exchange to transcript",
    "/interrogate with extra args still works (bare command match)"
   ],
   "cli/chat-slash-menu.test.ts": [
    "non-slash → no matches (menu closed)",
    "bare / → all commands, /quit hidden",
    "prefix filters",
    "empty matches → empty string",
    "non-TTY → plain lines, selected marked ❯",
    "TTY → ANSI styling on the selected row",
    "char \"/\" opens the menu",
    "typing after / builds the filter query",
    "backspace on bare / closes the menu",
    "down/up wrap the selection",
    "select → returns the chosen command name + closes",
    "escape closes the menu",
    "lone \"/\" → show once, marks shownFor",
    "already shown for \"/\" → does not re-show (no scrollback spam)",
    "refined query \"/st\" → no re-show (Tab completer handles it)",
    "non-slash line → reset shownFor so menu can reopen later",
    "empty line → reset"
   ],
   "cli/chat-slash-new-commands.test.ts": [
    "includes /autonomous, /audit, /directives, /mcp",
    "/autonomous and /audit map to their MCP tools in the catalog",
    "/autonomous %s → action:%s",
    "bare /autonomous defaults to the read-only status action",
    "/autonomous backlog list → backlog_list",
    "/autonomous backlog add <title> --cron <expr> → backlog_add with id/title/cron",
    "/autonomous backlog add <title> without --cron omits cron (one-off)",
    "/autonomous backlog add without a title → i18n usage message",
    "/autonomous %s <id> → triggerId mapped",
    "/autonomous approve without id → i18n usage message",
    "unknown subaction → i18n error message (no fabricated dispatch)",
    "/audit gate sprint-269 → action gate + sprintId",
    "/audit gate without sprint omits sprintId",
    "/audit query api → action query + channel",
    "/audit compliance → action compliance",
    "CLI-only actions not in MCP (forward/retention) → honest i18n message",
    "bare /audit and flag args resolve to none (legacy enterprise CLI bridge keeps them)",
    "bare /directives → show-directives action",
    "/directives set <content> → deckent_set_directives with content",
    "/directives set without content → i18n usage message",
    "unknown /directives subaction → i18n error",
    "/mcp → honest i18n not-wired message (never round-trips to provider)",
    "/autonomous approve t-1 dispatches deckent_autonomous end-to-end",
    "/audit gate sprint-269 dispatches deckent_audit (enterprise bridge NOT used)",
    "bare /audit still goes through the legacy enterprise CLI bridge (behaviour preserved)",
    "confirm gate: declined /directives set is cancelled and never dispatched",
    "approved /directives set dispatches deckent_set_directives with content",
    "unknown subaction renders the localized i18n error (en)",
    "/mcp outputs the honest not-wired notice without a provider round-trip",
    "outputs the DIRECTIVES.md content from projectRoot",
    "missing DIRECTIVES.md → localized not-found message (no throw)",
    "moved en templates stay byte-identical to the prior hardcodes",
    "default (two-arg) keeps the exact English output (backward compat)",
    "lang=tr renders the Turkish notice"
   ],
   "cli/chat-slash-registry-i18n.test.ts": [
    "every catalog entry carries a tui.slash.desc.* key that resolves in en AND tr",
    "desc is the catalog message resolved for the requested language",
    "English descriptions carry no Turkish-only glyphs and differ from the Turkish text",
    "Turkish descriptions are preserved byte-for-byte for existing users",
    "danger rows keep their textual carrier but drop the ⚠️ emoji icon (SINGLE-SURFACE §7, PLATFORM-MATRIX §6)",
    "no description in either language carries emoji, variation selectors or ZWJ (ambiguous-width glyphs)",
    "English copy carries no internal capability ids or vendor repo paths and points /model at /models",
    "command names and dispatch metadata are language-invariant",
    "native-only busy controls are advertised only when their real handlers are mounted",
    "buildSlashRegistry() without a language resolves through getLanguage()",
    "renderHelp localizes the \"Commands:\" header",
    "renderHelp lists the resolved-language description",
    "deprecated commands and compatibility aliases remain routable but are absent from discovery",
    "getVisibleCommands(mode, simpleMode, lang) builds the catalog for that language",
    "buildHelpOutput(mode, \"en\") renders the English catalog end-to-end",
    "help catalog tier headers localize (Danger → Tehlike in tr, unchanged in en)",
    "an unknown tier fails closed with a typed UnknownHelpTierError — the raw token is never rendered as user text"
   ],
   "cli/chat-slash-resources.test.ts": [
    "/resources is present in the registry and maps to deckent_resources",
    "bare /resources → agentic deckent_resources with empty args",
    "/resources --log → agentic deckent_resources with log:true (default path)",
    "/resources --log /path/to/log → agentic deckent_resources with log path",
    "/resources unknown → message action with slash_unknown_subaction key",
    "bare args → [resources]",
    "log:true → [resources, --log]",
    "log path → [resources, --log, path]",
    "bare deckent_resources → spawns [resources]",
    "deckent_resources with log:true → spawns [resources, --log]",
    "deckent_resources → read (no confirmation required)"
   ],
   "cli/chat-slash-usage.test.ts": [
    "/usage is present in the registry and maps to deckent_usage",
    "bare /usage → agentic deckent_usage with empty args",
    "/usage --sprint 275 → agentic deckent_usage with sprint arg",
    "/usage since 2026-06-01 → agentic deckent_usage with since arg",
    "/usage --since 2026-06-01 → agentic deckent_usage with since arg (flag form)",
    "/usage --sprint (no value) → message action i18n key",
    "/usage since (no value) → message action i18n key",
    "/usage unknown → message action with slash_unknown_subaction key",
    "bare args → [usage]",
    "sprint arg → [usage, --sprint, N]",
    "since arg → [usage, --since, ISO]",
    "until arg → [usage, --until, ISO]",
    "sprint + since combined → both flags in argv",
    "bare deckent_usage → spawns [usage]",
    "deckent_usage with sprint → spawns [usage, --sprint, N]",
    "deckent_usage → read (no confirmation required)"
   ],
   "cli/chat-spinner.test.ts": [
    "start() immediately writes first frame to stderr",
    "advances frames on interval ticks",
    "stop() clears line and stops further writes",
    "TTY=false (pipe) → no-op: nothing written on start/stop",
    "stop on first chunk: stop() called immediately after start() works",
    "calling start() twice is idempotent (single interval)"
   ],
   "cli/chat-status-line.test.ts": [
    "renders provider + sprint + dir when statusLine is undefined (default)",
    "renders provider + sprint + dir when statusLine=true",
    "starts with \"deckent\" prefix",
    "reflects the actual provider name",
    "returns empty string when statusLine=false",
    "returns empty string even with active sprint when disabled",
    "omits sprint token when activeSprint is absent",
    "omits sprint token when activeSprint is null",
    "shows only provider when {provider:true, sprint:false, dir:false}",
    "shows only dir when {dir:true}",
    "shows sprint only when {sprint:true} and sprint is active",
    "returns empty string when all fields are false",
    "does NOT show cost with statusLine=true (cost is opt-in)",
    "shows cost when fields.cost=true and ctx.cost is set",
    "does NOT show cost when fields.cost=true but ctx.cost is undefined"
   ],
   "cli/chat-tool-bridge.test.ts": [
    "propagates the resolved session language through the canonical child env",
    "deckent_status → spawns the `status` subcommand and returns its stdout",
    "deckent_history → spawns the `history` subcommand",
    "deckent_memory_query → appends query as `recall <query>` positional",
    "deckent_memory_query without query or detail_ref → mcp-error, no spawn",
    "deckent_memory_query forwards opaque continuation and detail references without inventing a query",
    "deckent_plan → spawns `plan` (confirm-gated one layer up in run.tsx)",
    "unknown tool → tool not allowed, no spawn",
    "spawn rejection → tagged mcp-error, never throws",
    "appends _rest positional args to the subcommand (e.g. /explain sprint-224)",
    "deckent_config (show) → spawns `config`",
    "deckent_config set → spawns `config set <k> <v>` via _rest",
    "%s → spawns %j (Faz E write/destructive)",
    "deckent_recover _rest sprint id uses dry-run gate (no --force bypass)",
    "deckent_kill passes through user flags via _rest",
    "deckent_kill maps MCP-shaped args to CLI argv",
    "deckent_cleanup maps decay/dryRun booleans to CLI flags",
    "deckent_recover defaults to MCP dryRun=true (--dry-run) on CLI bridge",
    "deckent_recover dryRun=false is unavailable on CLI bridge (no --force without approval binding)",
    "deckent_recover with sprintId ignores _rest (structured contract wins)",
    "deckent_recover rejects _rest + dryRun:false counterexample (Astra 187)",
    "deckent_recover without sprint id is unavailable",
    "deckent_audit gate/query/compliance → bridged to audit CLI argv",
    "deckent_audit forward/retention stay excluded (network/destructive)",
    "deckent_autonomous read/write actions bridge; start stays excluded (long-running)",
    "deckent_set_directives bridges with --content; empty content rejected",
    "ignores a non-array _rest (defensive)",
    "maps an allow-listed tool to its subcommand",
    "appends _rest positional args",
    "returns null for a tool not in the allow-list",
    "revalidates the request, spawns one canonical offline command, and preserves useful nonzero JSON",
    "rejects a forged request before invoking the capture spawn",
    "returns a sanitized typed envelope when session capture admission is unavailable",
    "revalidates %j and invokes the shared capture exactly once",
    "rejects a forged action before capture and preserves legacy dispatch behavior independently"
   ],
   "cli/chat-tool-exec-dialect.test.ts": [
    "%s"
   ],
   "cli/chat-tool-exec-errors.test.ts": [
    "is registered, distinct from DECKENT_E005",
    "has full human-context fields (whatHappened/why/howToFix)",
    "does not collide with any other registered code",
    "ELOOP symlink-cycle throws DECKENT_E075, NOT DECKENT_E005",
    "a path segment that does not exist yet resolves leniently (no throw)",
    "a plain existing file (no symlinks) resolves to itself, no throw",
    "a textual out-of-scope path (../escape) → [mcp-error] carries DECKENT_E005",
    "a symlink that resolves outside cwd (not a cycle) → [mcp-error] carries DECKENT_E005",
    "a symlink-cycle path → [mcp-error] carries DECKENT_E075, NOT DECKENT_E005",
    "deckent_write_file with a symlink-cycle path → DECKENT_E075, file never written"
   ],
   "cli/chat-tool-exec-git.test.ts": [
    "git_status reads branch + entries without a confirm call",
    "git_log lists commits; git_diff shows the pending hunk — both silent",
    "a clean tree answers `git diff: empty` honestly",
    "add asks with the paths summary; commit asks with the subject; approval → real commit lands",
    "a DENIED add/commit returns the denial marker and leaves the repo untouched",
    "git_add with an out-of-scope path is refused before git ever runs (E005 scope marker)",
    "an empty commit message is refused with an honest marker",
    "every tool answers `not a git repository` instead of throwing"
   ],
   "cli/chat-tool-exec-read-surface.test.ts": [
    "list_dir lists entries with dir markers and refuses out-of-scope paths",
    "grep returns path:line:text hits, skips node_modules, honest no-match",
    "grep scans a multi-MB file with long lines via the dispatcher (7111-c)",
    "grep reports skipped too-large files instead of silent no-match",
    "grep reports binary/read-error skip reasons when every file is skipped (7111-c REVISE)",
    "glob matches ** and * against project-relative paths",
    "baseline always covers node_modules/.git/dist/build/out/coverage",
    "unions the baseline with unambiguous root .gitignore directory entries",
    "a missing/garbled .gitignore degrades to the baseline (never throws)",
    "the walker skips ignored dirs so dist/ duplicates never surface (the @cost class)"
   ],
   "cli/chat-tool-exec.test.ts": [
    "deckent_write_file → creates the file with content",
    "deckent_read_file → returns content (read is unconfirmed)",
    "deckent_edit_file → replaces matched text",
    "deckent_bash → returns injected command output",
    "scope-guard → path outside cwd is rejected with [mcp-error]",
    "confirm reject → side-effecting tool is cancelled, no file written",
    "unknown tool → [mcp-error], never throws",
    "default (no labels) → English confirm summary, never hardcoded Turkish",
    "injected labels → confirm summary uses the caller-localized text (mechanism string-free)",
    "partial label override → missing labels fall back to the English default"
   ],
   "cli/chat-turn-queue-runflow.test.ts": [
    "idle + enabled=true: produces the turn immediately and clears the buffer",
    "mid-turn + enabled=true: buffers only, returns [] — Hermes no-inject rule preserved",
    "mid-turn buffered event surfaces via the EXISTING drainAsTurns() once the turn ends",
    "enabled=false while idle: zero production — nothing buffered, nothing returned",
    "enabled=false mid-turn: zero production — byte-identical to never calling the method",
    "coalesces with an already-buffered same-source bucket (reuses enqueueBg verbatim)",
    "idle + enabled=true also drains any OTHER already-buffered bucket alongside the new one",
    "baseline sanity: enqueueBg/drainAsTurns/size behavior is unaffected by this addition"
   ],
   "cli/chat-turn-queue.test.ts": [
    "starts empty with userTurnActive false",
    "mid-turn: bg-events are queued, NOT injected — drainAsTurns is a no-op while userTurnActive",
    "turn-end: drain returns sequential turn payloads in arrival order, then clears",
    "coalesce: consecutive same-source events merge into one bucket/payload",
    "coalesce: a different-source event in between keeps buckets separate, order preserved",
    "resuming after a drain starts a fresh bucket even for a repeated source"
   ],
   "cli/checkpoint-i18n.test.ts": [
    "binds bilingual descriptions and positional help on the real child commands",
    "keeps --lang caller-local while routing to the authenticated ingress",
    "localizes an invalid adapter identity without claiming a checkpoint write"
   ],
   "cli/checkpoint-list.test.ts": [
    "projects all canonical statuses and only pending when asked",
    "fails closed before filtering a legacy checkpoint under strict isolation",
    "deep-merges readonly global then project policy, including tenant and language",
    "holds invalid config without changing its bytes and preserves the disabled gate",
    "keeps unknown records during pending filtering and rejects scalar-shaped status values",
    "rejects createdAt with %s before terminal rendering",
    "holds unsafe sources and bounds oversize records without displaying terminal controls"
   ],
   "cli/cleanup-authority.test.ts": [
    "allows IDLE and terminal quiescent runs",
    "holds terminal cleanup without a matching fenced receipt",
    "holds a live coordinator without touching its projections",
    "holds resumable PAUSED and ORPHANED states",
    "holds unknown coordinator ownership"
   ],
   "cli/cleanup-log-archive.test.ts": [
    "creates archive dir and returns true for byte-exact copy",
    "archive file content is byte-identical to the original",
    "returns false when archive dir path is under a file (mkdirSync fails)",
    "returns false when claimed content length exceeds what was written (byte mismatch)",
    "faithful-regression: .log file is archived to the canonical sprint tasks namespace after cleanup",
    "kopya+byte-eşit→sil: live .log is deleted after successful archive",
    "archive-kısa→live-kalır: .log is restored when archive fails (blocked archive dir)",
    "multiple .log files: all archived when present",
    "no .log files → cleanup runs without error"
   ],
   "cli/cli-bin-invocation.test.ts": [
    "argümansız (no args) routes to the default REPL branch",
    "`help` arg routes to commander (NOT default REPL)",
    "`serve` arg routes to commander (NOT default REPL)",
    "unknown subcommand routes to commander so it can show its error",
    "`--help` / `-h` short-circuit the REPL (help UX preserved)",
    "`--version` short-circuits the REPL (version UX preserved)",
    "`node dist/cli/entry.js help` prints commander Usage line (NOT silent)",
    "`node dist/cli/entry.js --version` prints a version string (NOT silent)",
    "`node dist/cli/entry.js unknowncommandfoo` prints commander unknown-command error"
   ],
   "cli/cli-bridge-tool-parity.test.ts": [
    "registers every catalog spec with its description",
    "destructive/execute tools are always-confirm — never silently runnable",
    "arg-aware traps are pinned at their most-privileged action (over-ask, never under-ask)",
    "read-only tools stay silent (no new friction on the read path)",
    "deckent_review is confirm-gated — it REWRITES task JSON files (advisor catch, was silent since the original six)",
    "deliberately excluded names stay unadvertised (watch would block a REPL turn forever)"
   ],
   "cli/cli-description-catalog.test.ts": [
    "registers at least the known command surface",
    "gives every registered command a non-empty description",
    "serves every command description from a catalog key that carries both en and tr",
    "keeps every cli.*.desc catalog entry bilingual and non-empty",
    "resolves descriptions in the language DECKENT_LANGUAGE selects"
   ],
   "cli/cli-inventory.test.ts": [
    "does not throw on commander register",
    "returns a Command instance",
    "registers at least 45 top-level commands",
    "has no duplicate top-level command names",
    "every command has a non-empty description",
    "\"${name}\" is registered",
    "\"${name}\" description is at least 10 chars",
    "has at least ${subs.length} subcommands",
    "has \"${sub}\" subcommand",
    "has \"${flag}\" option",
    "\"help-info\" has \"info\" alias"
   ],
   "cli/cli-memory-catalog-family-contract.test.ts": [
    "owns a non-trivial, exclusively namespaced key set",
    "carries a real bilingual pair on every row",
    "is registered once in the shared message catalog",
    "resolves per language and degrades diagnosably",
    "interpolates {placeholders}",
    "keeps DB-first entry/projection terminology in the memory-family rows",
    "registers every scoped root",
    "describes every nested path",
    "serves every option help string from a bilingual catalog row",
    "describes every positional argument from a bilingual catalog row",
    "renders option and argument help in the language DECKENT_LANGUAGE selects",
    "keeps sprint/task/iteration codes out of every family row",
    "keeps sprint/task/iteration codes out of the scoped option and argument help",
    "states the read/mutation split on \\`${testCase.path}\\`",
    "drops the parity framing from the command description",
    "states the cross-host scope of the standard in the path help",
    "names the transcript source it actually parses",
    "promises no provider-neutral aggregation it does not implement"
   ],
   "cli/cli-run-family-contract.test.ts": [
    "reports ZERO violations across every declared path and axis (${lang})",
    "verification is not vacuous — the family really covers the core surface",
    "declares no duplicate paths",
    "every row declares the full platform matrix drawn from the shared list",
    "detects option-shape drift",
    "detects argument-shape drift",
    "detects an option whose help key is not bilingual",
    "detects an option whose catalog text is not the live help text",
    "detects an argument whose catalog text is not the live help text",
    "detects effect / output / default-execution drift against the path SSOT",
    "detects a contract path that does not exist in the live tree",
    "detects an option that declares no help key at all",
    "every row is bilingual, non-empty, and really translated",
    "owns exactly the `cliContract.` namespace",
    "is registered once in the shared message catalog",
    "resolves an unknown key to the key itself and falls back to English",
    "interpolates named parameters instead of leaking the placeholder",
    "every key referenced by a contract row exists in some catalog",
    "a flag-opt-in confirmation always has an opt-in flag to point at",
    "operator-attestation authority is only claimed where the attestation flags exist",
    "a dry-run default is never claimed without an apply-style opt-in",
    "every declared axis value renders bilingually in the contract block",
    "renderContractHelp emits every axis, in both languages, for a help-block row",
    "the family's user-facing surface is code-free (${lang})",
    "the detector is not vacuous — it catches the codes this family removed",
    "documents the run one-shot vs run start|status|retro|history namespace overlap",
    "keeps the four compatibility sub-commands declared as delegating aliases",
    "says `test` is a Deckent test sprint, not the project unit-test runner",
    "says `output` reads persisted worker stdout/stderr/result evidence",
    "says `finalize` publishes a DB-first terminal projection",
    "names the platform/backend prerequisites the handlers really depend on",
    "binds by name, ignores unknown names, and returns the same object"
   ],
   "cli/cli-surface-consolidation-battery.test.ts": [
    "zero numeric-ADR hits across every MESSAGES key, excluding the one pinned mechanism-text key",
    "the grandfathered key is the sole survivor — pinned, not silently growing",
    "does not register a `web` command",
    "typed `web` gets no \"Did you mean\" suggestion (too far from any surviving command)",
    "buildProgram() yields a positive, scan-derived top-level command count with no duplicates",
    "scripts/lint-cli-mcp-parity.mjs exits 0 against the real repo"
   ],
   "cli/cli-surface-truth-battery.test.ts": [
    "reports ZERO violations across every path and every declared axis",
    "assertCommandContract() does not throw on the real program",
    "verification is not vacuous — coverage counters match the real tree",
    "walks nested sub-paths, not just top-level commands",
    "detects option drift",
    "detects argument drift",
    "detects alias drift",
    "detects hidden drift",
    "detects actionless-group effect drift in both directions",
    "detects defaultExecution drift",
    "detects output drift",
    "detects a contract path that no longer exists in the tree",
    "does not whitelist an unknown compatibility child as mapped help",
    "checks mapped help option and argument shape against the real replacement child",
    "parses exact replacement prefixes and rejects unresolved child help",
    "detects a live command with no contract row at all",
    "detects summary-binding drift when summaryKey stops matching the description",
    "detects option-description binding drift",
    "detects argument-description binding drift",
    "assertCommandContract() throws with every violation named",
    "defaultExecution equals the derivation from effect + live flags for every cli row",
    "output equals the derivation from the live flags for every cli row",
    "a --json flag always derives a json-carrying output mode",
    "an execute opt-in flag turns a mutating command into a dry-run default",
    "has no duplicate path keys",
    "every row declares at least one surface and a non-empty path",
    "mixed paths explicitly separate a read default from opt-in mutation capability",
    "every row declares a real platform set drawn from ALL_PLATFORMS",
    "every effect maps to a plain-risk-language value",
    "summaryKey is an i18n key, never display text",
    "every registry-projected row is a single-segment top-level path",
    "contractPathKey normalizes both accepted input shapes",
    "every summaryBinding: exact row really resolves through the catalog (en)",
    "has no command-summary binding debt",
    "every option and argument descriptionKey is a real bilingual catalog row",
    "option and argument contract coverage is complete",
    "every option and argument description binding matches the live Turkish tree",
    "every summaryKey used by an exact-bound row is a real bilingual catalog row",
    "resolving an exact-bound summaryKey never returns the raw key back",
    "no live help text exposes internal project tracking labels",
    "keeps alias narrative separate from exact replacement metadata in both languages",
    "documents reachable virtual children without relabeling real legacy children",
    "renders each language from its own strings and emits bilingual manifest fields",
    "checks real output/watch and checkpoint decision paths as ordinary nodes",
    "option flags are read verbatim from the live command",
    "argument shapes carry name / required / variadic",
    "hidden commands are detected through the public visibility API",
    "every actionless namespace parent is explicitly contracted as a group"
   ],
   "cli/cli-tool-capture.test.ts": [
    "resolves %s as an exact offline read",
    "rejects unsafe or unsupported selection %s without normalizing it",
    "rejects forged typed requests instead of trusting caller-provided argv",
    "resolves %s to one closed action and canonical JSON argv",
    "projects the validated action back to exact tool args for classification and approval binding",
    "rejects unsupported or ambiguous structured action %s",
    "rejects forged action requests instead of trusting embedded argv or normalized values",
    "returns sanitized incomplete receipts and performs zero spawn when capture admission fails",
    "detaches local handles after bounded reap observation without claiming the child reaped",
    "keeps a bounded standalone preview while hashing and counting the entire stream",
    "drains exact multibyte stdout/stderr bytes and preserves nonzero exit truth",
    "terminates a timed-out private child tree and settles on close"
   ],
   "cli/color-gate.test.ts": [
    "varsayılan: env yok + TTY değil → renk kapalı",
    "FORCE_COLOR=1 TTY olmasa da rengi açar",
    "FORCE_COLOR=0 bastırır",
    "NO_COLOR spec: BOŞ STRING dahil varlığı bastırır",
    "FORCE_COLOR>0, NO_COLOR override edilir",
    "--no-color flag, FORCE_COLOR dahil her seyi ezer",
    "argv --no-color da bastırır",
    "bastırılmışsa none",
    "FORCE_COLOR=3 açık istek: truecolor (zemin sezgisiz)",
    "FORCE_COLOR=2 → ansi256",
    "truecolor YETENEĞİ tek başına yetmez — zemin bilinmiyor → ansi16",
    "koyu zemin biliniyor (COLORFGBG) + truecolor yeteneği → truecolor",
    "AÇIK zemin biliniyor → ansi16 (terminal şeması okunabilirliği çözer)",
    "256-yetenek + koyu zemin → ansi256",
    "ansi16 kademesinde SGR kodları eski Theme ile aynı",
    "truecolor kademesinde NOVA token hex'i akar (success = novaGo #43E39A)",
    "renk kapalıyken düz metin (bilgi kaybı yok)",
    "strip bileşik SGR'yi de söker",
    "NO_COLOR boş-string: düz metin (eski lokal kontrol bug'ının kapanışı)",
    "truecolor kademesinde marka truecolor kodları",
    "ansi16 kademesinde truecolor SIZMAZ, 16-renk degrade basılır",
    "NO_COLOR ile düz metin",
    "renk açıkken SGR birebir eski davranış"
   ],
   "cli/command-registry.test.ts": [
    "is non-empty and has no duplicate names",
    "every entry uses a spec-valid category",
    "every entry uses a spec-valid TERM-5 plain-risk-language value",
    "every entry declares at least one valid surface",
    "summaryKey is an i18n key, never inline display text",
    "mcpNames is present iff surfaces includes \"mcp\", and never empty when present",
    "no mcpNames value is reused across two different entries",
    "scanned at least 45 top-level CLI commands (sanity floor)",
    "every registered top-level CLI command has a registry entry with the \"cli\" surface",
    "scanned at least 30 MCP tools (sanity floor)",
    "every registered MCP tool appears in some entry\\'s mcpNames",
    "no registry mcpNames value is fabricated (all resolve to a real registered tool)",
    "every slash command (excluding documented aliases) resolves to a repl-surfaced entry",
    "byCategory returns only entries of that category",
    "byRisk returns only entries of that risk tier",
    "bySurface returns only entries exposing that surface",
    "getCommand finds an exact entry by name, undefined otherwise",
    "search matches by name substring, case-insensitively",
    "search matches by category and scope keywords",
    "search returns empty array for blank query",
    "has exactly one entry per contract row carrying a registry block",
    "every registry entry resolves back to its contract row",
    "risk is derived from path effect unless the top-level family declares an aggregate override",
    "aggregate risk overrides preserve mutating family discovery without mislabeling bare group paths",
    "category / scope / mcpNames / surfaces / catalogDependent come from the contract row verbatim",
    "summaryKey is derived as cmdCatalog.<name>.summary for every projected row",
    "projectRegistryEntry refuses a contract row without a registry block",
    "catalog-dependent commands are exactly the contract rows flagged catalogDependent"
   ],
   "cli/commands/agent-crud.test.ts": [
    "should delete an existing agent",
    "should not have directory after delete",
    "should show error for non-existent agent",
    "should update model via --model",
    "should update description via --description",
    "should show error for non-existent agent",
    "should show info when no options given",
    "should show agent details",
    "should include PROMPT.md content",
    "should show error for non-existent agent"
   ],
   "cli/commands/agent-display-fix.test.ts": [
    "reads from stats.totalUses (builtin agent format)",
    "reads from direct uses field (custom agent format)",
    "returns 0 when both uses and stats.totalUses are missing",
    "returns 0 when uses is NaN",
    "prefers stats.totalUses over uses when both present",
    "reads from stats.successRate (builtin agent format)",
    "reads from direct successRate field (custom agent format)",
    "returns 0 when both are missing",
    "returns 0 when successRate is NaN",
    "rounds to integer",
    "displays \"0\" uses and \"0%\" success for builtin agent with stats.totalUses=0",
    "displays correct uses from stats.totalUses for builtin agent",
    "displays correct uses from direct uses field (custom agent)",
    "serializes uses as number (not undefined) for builtin agent",
    "serializes uses as number from direct fields (custom agent)",
    "serializes 0 for missing stats (not undefined)",
    "parses agent from 3-col | Task | Agent | Status | table",
    "parses agent from 4-col | Task | Agent | Skills | Status | table",
    "extracts skills from 4-col table",
    "ignores header and separator rows in table",
    "does not include generic agent in agents list",
    "merges agents from Agents: header and table format",
    "deduplicates agents appearing in both formats",
    "extracts agents from 4-col task table in sprint log",
    "extracts skills from 4-col task table in sprint log",
    "falls back to Agents: header when table has no agents"
   ],
   "cli/commands/agent-improvements.test.ts": [
    "shows stats for existing agent",
    "outputs JSON with --json flag",
    "reports error for missing agent",
    "shows overall success rate",
    "accepts valid trigger keywords",
    "rejects empty string",
    "rejects whitespace-only string",
    "rejects triggers with spaces",
    "accepts mixed valid triggers and returns no errors",
    "enforces validation during agent create --triggers",
    "saves valid triggers during agent create --triggers",
    "enforces validation during agent edit --triggers",
    "saves valid triggers during agent edit --triggers",
    "creates agent.json with systemPrompt from PROMPT.md",
    "systemPrompt matches PROMPT.md content",
    "defaults to sonnet when --model not specified",
    "uses opus when --model opus specified",
    "uses haiku when --model haiku specified",
    "rejects invalid model name",
    "shows model in create output"
   ],
   "cli/commands/agent.test.ts": [
    "registers agent command on program",
    "has list subcommand",
    "has create subcommand",
    "has enable subcommand",
    "has disable subcommand",
    "prints no agents message when directory does not exist",
    "prints no agents message when directory is empty",
    "prints formatted table when agents exist",
    "outputs JSON when --json flag is used",
    "shows disabled status for disabled agent",
    "handles multiple agents",
    "sets exitCode=1 on error",
    "skips non-directory entries",
    "skips malformed agent configs",
    "rounds success rate to integer",
    "creates agent directory and files",
    "writes valid agent.json with defaults",
    "writes PROMPT.md template with agent name",
    "prints success message with directory",
    "rejects invalid name with special characters",
    "rejects name with underscores",
    "rejects empty-like name (spaces)",
    "rejects duplicate agent",
    "accepts alphanumeric name with hyphens",
    "prints file list after creation",
    "sets exitCode=1 on filesystem error",
    "sets enabled=true in agent.json",
    "prints success message",
    "updates updatedAt timestamp",
    "sets exitCode=1 when agent not found",
    "sets enabled=false in agent.json",
    "prints success message",
    "sets exitCode=1 when agent not found",
    "returns successRate ~33% when 3 rows: 1 DONE + 2 NO_GO (not 100% via mentions-conflation)",
    "shows - for rate when agent is mentioned but has no task rows",
    "does not count tasks assigned to other agents",
    "returns empty stats when sprints directory does not exist"
   ],
   "cli/commands/analyze.test.ts": [
    "registers the analyze command",
    "registers with correct description",
    "registers --json flag",
    "calls analyzeProject with resolved root",
    "calls resolveProjectRoot to determine root",
    "prints formatted table output by default",
    "prints JSON output with --json flag",
    "JSON output is pretty-printed with 2-space indent",
    "registers the --bootstrap-vocabulary flag",
    "does NOT bootstrap when the flag is absent",
    "flag → bootstrap derives candidates and writes the vocabulary layer",
    "a bootstrap failure surfaces via printError and sets a non-zero exit code",
    "includes language in output",
    "includes test framework in output",
    "includes build tool in output",
    "includes CI in output",
    "includes file count in output",
    "falls back to process.cwd() when resolveProjectRoot throws",
    "still calls analyzeProject even when resolveProjectRoot fails",
    "passes correct headers to formatTable",
    "includes all expected rows",
    "converts numeric fields to strings"
   ],
   "cli/commands/approvals.test.ts": [
    "uses the shared fail-closed OS actor projection required by terminal reauth",
    "advertises the catalog-backed class filter on list only",
    "keeps the foreign federated source guard when a same-id native mirror is local",
    "does not reject a same-tenant native target at the containment gate",
    "fails a checkpoint-constrained caller closed for %s before mutation",
    "reuses terminal live-auth and refuses a checkpoint decision without a TTY"
   ],
   "cli/commands/archive-debt.test.ts": [
    "%s emits one catalog warning and forwards every argument"
   ],
   "cli/commands/attach-overhaul.test.ts": [
    "registers attach command",
    "F) --list shows windows when session active",
    "F) --list shows \"No active session\" when session inactive",
    "F) --list does not call attach()",
    "G) warns about nested tmux when TMUX env is set",
    "G) does not warn about nested tmux when TMUX env is not set",
    "still attaches after nested tmux warning",
    "sets exitCode=1 when no session and not --list"
   ],
   "cli/commands/audit.test.ts": [
    "registers the canonical verify subcommand with its JSON output option"
   ],
   "cli/commands/autonomous.test.ts": [
    "registers mission as a canonical autonomous subcommand"
   ],
   "cli/commands/chat-native.test.ts": [
    "send() on proxy uses the initial adapter",
    "stream() is always defined on the proxy",
    "send() delegates to new adapter after switchProvider()",
    "stream() delegates to new adapter after switchProvider()",
    "throws ProviderNotFoundError for unknown initialProviderName",
    "switchProvider() throws ProviderNotFoundError for unknown name",
    "uses registry default when initialProviderName is omitted",
    "subsequent turns use new provider after /provider <name>",
    "emits the tui.switched message after /provider <name>",
    "emits switch usage hint when /provider is called with no argument",
    "emits error message via getMessage when switchProvider throws",
    "loop continues after a failed /provider switch (no crash)",
    "exits cleanly with empty input iterator (no provider call)",
    "end_turn flow: provider response goes straight to output, no dispatch",
    "dispatches a tool call then sends result back and ends the turn",
    "aborts the inner tool chain when maxToolHops is hit",
    ":exit terminates the outer loop before any provider call",
    "returns a typed ToolCall when given a tagged JSON block",
    "returns null for plain text or malformed JSON"
   ],
   "cli/commands/chat-slash-registry.test.ts": [
    "appears in the catalog with a description and NO agenticTool (meta-command)",
    "resolveSlash returns { action: none } so the app.tsx branch owns /do",
    "does NOT shadow /doctor (a /do-prefixed dispatch command still resolves)",
    "does NOT shadow /directives (its own structured handler still runs)",
    "slashCompleter surfaces /do for the /do and /d prefixes",
    "renderHelp lists the /do command line with the Turkish description (tr)",
    "renderHelp lists the /do command line with the English header and description (en)",
    "returns a non-empty registry with at least 5 commands",
    "includes /help, /status, /recall, /plan, /sprint, /exit, /clear",
    "/status maps to deckent_status MCP tool",
    "/recall maps to deckent_memory_query MCP tool",
    "/plan maps to deckent_plan MCP tool",
    "/sprint maps to deckent_history MCP tool",
    "meta commands /help /exit /clear have no agenticTool",
    "each call returns a new array (immutable copy)",
    "starts with \"Komutlar:\" header for lang=tr (explicit — never the process locale)",
    "starts with \"Commands:\" header for lang=en (explicit — never the process locale)",
    "lists each command on its own line with name and description",
    "does not duplicate /quit as a separate entry (alias shown in /exit desc)",
    "works with a custom minimal registry",
    "/status → action:agentic, tool:deckent_status, root:.",
    "/STATUS (uppercase) → also resolves (case-insensitive)",
    "/sprint → action:agentic, tool:deckent_history",
    "/plan → action:agentic, tool:deckent_plan, mode:auto",
    "/recall → typed query prompt; never dispatches an empty query",
    "/agent and /skill are compatibility aliases for the canonical plural commands",
    "/recall docker heartbeat → extracts query",
    "%s → agentic %s",
    "/explain sprint-224 → positional flows through args._rest",
    "read-only commands without args set no _rest",
    "/nervous is a meta-command (in catalog, no agenticTool)",
    "%s → agentic %s (Faz E write/destructive)",
    "/recover sprint-224 → deckent_recover with _rest",
    "/config set max_workers 4 → deckent_config with _rest",
    "/help → action:help with registry",
    "/exit → action:exit",
    "/quit → action:exit (alias)",
    "/clear → action:clear",
    "/unknown → action:none",
    "plain text (no slash prefix) → action:none",
    "empty string → action:none",
    "whitespace-only → action:none"
   ],
   "cli/commands/chat.test.ts": [
    "maps a reachable+model server to ready=true",
    "maps an unreachable server to ready=false with the honest reason",
    "treats a reachable-but-model-less server (partial) as NOT ready",
    "passes the real createCliToolDispatcher() to runChatNativeLoop (no placeholder stub)",
    "interactive --native REPL also receives the real dispatcher",
    "honest-fails (exit 1, no silent fallback) when no local runtime is reachable",
    "wires the Ollama adapter + real dispatcher into the native loop when ready",
    "returns detect results in claude → codex → cursor → gemini order",
    "swallows adapter detect() errors and reports as not-ready",
    "prefers ready: true over partial",
    "falls back to partial when nothing is fully ready",
    "returns null when no provider has any binary",
    "spawns the chosen tool with DECKENT_MCP_AUTO_ATTACH=1 and stdio inherit",
    "registers a shutdown hook (not a raw SIGINT listener); the hook kills the child via default SIGTERM and detach() unregisters it",
    "registers a chat command with --tool, --local, --check-mcp options",
    "auto-detects the first ready provider and spawns it",
    "honors --tool override even when other providers are ready",
    "errors with install hint when no provider is available",
    "errors when --tool names a provider whose binary is missing"
   ],
   "cli/commands/checkpoint.test.ts": [
    "routes %s with its complete sprint/phase identity",
    "rejects malformed identity %j/%j before the decision ingress",
    "rejects missing and extra identity arguments through Commander",
    "derives only safe producer filename stems"
   ],
   "cli/commands/ci-dashboard.test.ts": [
    "returns null when no CI data provided",
    "shows baseline info when only baseline provided",
    "shows tsc FAIL when baseline tscPassed is false",
    "shows report delta when report is provided",
    "prefers report over baseline when both provided",
    "shows no-data message when empty",
    "shows baseline info when only baseline provided",
    "shows full report details when report is present",
    "shows trend section when 2+ reports provided",
    "shows regression count in trend entries when non-zero",
    "returns null when file does not exist",
    "returns parsed baseline when file exists",
    "returns null when file is malformed JSON",
    "returns empty array when brain dir does not exist",
    "reads and parses ci-report files",
    "returns empty array when no ci-report files exist",
    "skips malformed report files",
    "returns null when no reports exist",
    "reads by sprintId when provided and file exists",
    "includes CI status line when baseline is provided",
    "shows CI report data when report is provided",
    "omits CI section when no CI data provided",
    "includes CI Health section when reports are provided",
    "includes CI Health section when baseline is provided",
    "omits CI Health section when no CI data provided",
    "shows trend when multiple reports provided"
   ],
   "cli/commands/cleanup-dryrun.test.ts": [
    "should list task files without deleting them",
    "should not call cleanup() or destroy()",
    "should list lock files correctly",
    "does not advertise canonical execution-authority artifacts as cleanup targets",
    "should list prompt files correctly",
    "should handle empty directories without errors",
    "should handle missing tasks directory",
    "should show tmux session info",
    "should show individual file names",
    "should count only matching extensions for task files"
   ],
   "cli/commands/cleanup.test.ts": [
    "registers cleanup command on program",
    "has --decay option",
    "has an exact --sprint cleanup selector",
    "rejects malformed sprint selectors before reading task artifacts",
    "reads task files from .tasks/ directory",
    "kills the tmux session via spawnSync",
    "--decay calls runDecay with force and prints result",
    "--decay with zero removals does not print removal line",
    "filters only task-*.json files from .tasks/",
    "does not treat a landing proposal as a task record",
    "sets exitCode=1 when cleanup throws",
    "silently handles spawnSync failure (session may not exist)",
    "shows budget warning when .brain/ exceeds budget after cleanup",
    "does not show budget warning when .brain/ is within budget",
    "--decay also runs normal cleanup (no early return)",
    "warns when EXECUTING tasks are present",
    "warns when CLAIMED tasks are present",
    "A) dry-run calls readdirSync exactly once for tasksDir (single pass)",
    "A) dry-run correctly separates task files and prompt files in single pass",
    "A) --sprint dry-run excludes foreign task and prompt artifacts",
    "B) sprint ID derived from sprint-state.json when it exists",
    "B) exact --sprint selector supplies the cleanup number used by artifact ownership",
    "B) sprint ID falls back to task sprintId when sprint-state.json is missing",
    "B) sprint falls back to cleanup-timestamp when no state or tasks",
    "C) uses tmux_session from config when available",
    "C) falls back to default TMUX_SESSION_NAME when no config",
    "C) falls back to default when config has no tmux_session field",
    "D) adds !.brain/archive/ exception to .gitignore when archive is ignored",
    "D) inserts !.brain/archive/ immediately after .brain/archive/ line",
    "D) skips .gitignore update when !.brain/archive/ already present",
    "D) appends !.brain/archive/ when .brain/archive/ line not found in .gitignore",
    "uses en language by default when config missing",
    "reads language from config and uses tr output when language=tr",
    "decay complete uses tr language when config language=tr",
    "falls back to en when config has malformed JSON",
    "archived_sprints message uses tr language when language=tr",
    "E) calls archivePromptFiles with sprint ID from sprint-state.json",
    "E) prints archived count when prompt files were archived",
    "E) ignores the retired cleaned counter from the archive adapter",
    "E) reads prompt_archive_retention from config (default 5)",
    "E) dry-run shows prompts will be archived not deleted",
    "F) dry-run includes .timeout files in task file count",
    "F) dry-run does not invoke either archive retention mutator",
    "F) calls cleanTasksArchive with retention count after normal cleanup",
    "F) prints removed count when old archive dirs were cleaned"
   ],
   "cli/commands/cli-polish.test.ts": [
    "returns empty string for undefined",
    "returns empty string for zero",
    "formats seconds only (< 60s)",
    "formats whole minutes (no remainder)",
    "formats minutes with seconds",
    "formats whole hours (no remainder)",
    "formats hours with remaining minutes",
    "uses Math.round for sub-second rounding (500ms rounds up)",
    "sorts agents with equal task counts alphabetically",
    "still sorts by task count primarily (higher count first)",
    "alphabetical tiebreak is deterministic with 3 agents of equal task count",
    "serializes tasks as number in JSON output",
    "serializes completed as number in JSON output",
    "serializes noGo as number in JSON output",
    "keeps dash as string when field is missing",
    "includes src/api/ in triggerScopes",
    "retains src/auth/ and src/middleware/ in triggerScopes"
   ],
   "cli/commands/config-export.test.ts": [
    "throws when config file does not exist",
    "prints JSON to stdout when no outputFile given",
    "writes JSON to file when outputFile given",
    "strips block comments before export",
    "strips line comments before export",
    "throws when config content is invalid JSON after stripping",
    "handles empty JSON object {}",
    "strips multiline block comments spanning multiple lines",
    "handles config with nested objects",
    "throws when import file does not exist",
    "throws on invalid JSON in import file",
    "validates imported config before writing",
    "merges import over existing config",
    "creates new config when no existing config file",
    "handles malformed existing config gracefully",
    "throws ConfigValidationError when import fails validation",
    "imports an empty JSON object without error",
    "preserves extra fields in existing config during merge",
    "import data with numeric value is preserved",
    "import data overrides existing config field of same name",
    "registers export subcommand",
    "exports config to stdout when no file arg",
    "exports config to file and prints confirmation",
    "sets exitCode 1 when config file missing",
    "registers import subcommand",
    "imports config and prints confirmation",
    "sets exitCode 1 when import file not found",
    "sets exitCode 1 on validation error and prints invalid config message",
    "sets exitCode 1 on generic error during import",
    "writes merged config with import data overriding existing"
   ],
   "cli/commands/config-native-migration.test.ts": [
    "passes the explicit provider/model pair to the canonical migration service",
    "renders the %s state truthfully",
    "localizes a typed refusal without exposing a raw migration error",
    "emits one bounded JSON result without raw error or backup paths",
    "keeps thrown failures machine-readable without inventing mutation certainty"
   ],
   "cli/commands/config-nested.test.ts": [
    "should deep merge nested objects on import",
    "should override top-level fields on import",
    "should expand nested import into existing nested object",
    "setNestedValue should create nested path",
    "setNestedValue should preserve sibling keys",
    "getNestedValue should return nested value",
    "getNestedValue should return undefined for missing key",
    "getNestedValue should return object for intermediate path",
    "getNestedValue should return string for top-level key",
    "top-level key should work with simple assignment",
    "nested key should be detected by dot presence",
    "should deep merge objects"
   ],
   "cli/commands/config-overhaul.test.ts": [
    "outputs category headers",
    "outputs key names under each category",
    "shows default values in list output",
    "outputs all key names",
    "outputs keys in sorted order",
    "shows raw file content without loading defaults",
    "shows empty object when config file does not exist",
    "calls needsMigration when config file exists",
    "calls migrateConfig when needsMigration returns true",
    "imports JSON with line comments",
    "imports JSON with block comments",
    "registers list subcommand",
    "registers keys subcommand"
   ],
   "cli/commands/config.test.ts": [
    "registers config command with set subcommand",
    "shows current config as JSON",
    "handles loadConfig rejection with error",
    "config set writes value to file",
    "config set parses JSON values",
    "config set keeps string for non-JSON values",
    "config set prints confirmation",
    "config set reports validation errors",
    "config set handles generic errors",
    "config set creates new object when file does not exist"
   ],
   "cli/commands/dashboard-overhaul.test.ts": [
    "renders sprint info",
    "renders progress bar",
    "renders \"No alerts.\" when no alerts",
    "renders alert messages",
    "renders agent/skill columns (D)",
    "adapts width to terminal columns (C)",
    "uses ASCII when noColor=true (NO_COLOR support)",
    "respects NO_COLOR environment variable via isNoColor()",
    "isNoColor returns false without NO_COLOR env",
    "isNoColor returns true when flag is explicitly true",
    "returns null when file does not exist",
    "returns null when file has invalid JSON",
    "returns parsed state when file is valid"
   ],
   "cli/commands/do-planning-heartbeat.test.ts": [
    "prints the initial notice naming the governing timeout in minutes + the config knob",
    "non-TTY: emits a full elapsed line every 30s and never ticks after stop()",
    "TTY: refreshes one line in place every 5s and erases it on stop()"
   ],
   "cli/commands/do.test.ts": [
    "lang=${lang}: scopeGateResult='fail' renders the verdict label and every scopeGateMessage line",
    "lang=${lang}: scopeGateOverridden=true renders the override notice",
    "lang=${lang}: clean scope (pass, not overridden) renders neither",
    "lang=${lang}: scopeGateResult absent entirely (undefined) — same as clean, no crash",
    "en and tr renderings genuinely differ for a scope-gate fail preview",
    "the digest line still renders after the scope-gate block (ordering did not get lost in the refactor)",
    "renders the shared structural topology before the digest"
   ],
   "cli/commands/docs-add-interactive.test.ts": [
    "creates docs.json with default template content (pure-adapter: no host files)",
    "does not overwrite existing config",
    "is idempotent — calling twice produces same result",
    "adds doc with all parameters specified",
    "adds doc with minimal parameters (path only)",
    "validates empty-ish paths",
    "validates deeply nested valid paths",
    "rejects sneaky traversal attempts"
   ],
   "cli/commands/docs-json-template.test.ts": [
    "is valid JSON with version 1 and a docs array",
    "includes an identity-md managed-doc so user IDENTITY.md auto-updates"
   ],
   "cli/commands/doctor-json.test.ts": [
    "should output valid JSON",
    "should include ok boolean field",
    "should include checks array",
    "should include providers array",
    "projects bounded auth truth without serializing raw probe detail",
    "should include profile when --profile is set",
    "should not include profile without --profile flag"
   ],
   "cli/commands/doctor-watch-provider.test.ts": [
    "getTaskProvider returns \"claude\" when task file does not exist",
    "getTaskProvider returns provider from task JSON",
    "getTaskProvider returns \"claude\" as fallback when provider missing in task JSON",
    "getTaskProvider returns \"gemini\" when task uses gemini",
    "watchSubprocessLog shows \"not found\" message when log file missing",
    "watchSubprocessLog spawns tail -f on log file when it exists",
    "watchSubprocessLog prints tailing message before spawning",
    "--follow uses subprocess log viewer for codex provider task",
    "--follow uses tmux attachToWorkerPane for claude provider task",
    "--follow uses subprocess log viewer for gemini provider task",
    "watchSubprocessLog handles spawn error gracefully",
    "checkTmux is exported and respects provider list",
    "tmux required when no providers given (default)"
   ],
   "cli/commands/doctor.test.ts": [
    "registers the doctor command",
    "registers --profile flag on doctor command",
    "has correct description for doctor command",
    "calls formatDoctorResult and print on --legacy run",
    "uses human-friendly format by default",
    "injects the required provider-authority keyring check at the production boundary",
    "does NOT show profile info without --profile flag",
    "shows profile info when --profile flag is used",
    "uses resolveProjectRoot to determine project root",
    "falls back to process.cwd() if resolveProjectRoot throws",
    "returns DoctorResult with ok and checks array",
    "includes node check in results",
    "includes git check in results",
    "includes tmux check in results",
    "includes claude check in results",
    "marks ok=true when all required checks pass",
    "marks ok=false when a required check fails (node not found)",
    "marks ok=false when tmux is missing",
    "marks ok=false when git is missing",
    "marks node check failed for Node <18",
    "non-required checks do not affect ok",
    "doctor result checks have name, passed, message, required fields",
    "failed check has passed=false and descriptive message",
    "passed check has passed=true and version info in message",
    "returns a string containing System Profile header",
    "includes cpu core count in output",
    "includes RAM info in output",
    "includes subscription info when provided",
    "omits subscription line when no subscription provided",
    "formats as box with border characters",
    "includes recommended workers count",
    "prints system profile when --profile is used",
    "detects subscription when --profile is used",
    "passes unknown subscription label as \"unknown\"",
    "passes detected plan label when subscription is known",
    "node not found — check returns passed=false with message",
    "claude not found — check returns passed=false",
    "workspace missing — check returns passed=false with hint message",
    "stale lock detected — locks check returns passed=false",
    "brain budget over the config-resolved budget — check passes=false with decay hint",
    "brain budget between the retired 900 literal and the config default stays green",
    "sets process.exitCode=1 when a required check fails",
    "does NOT set process.exitCode=1 when all required checks pass",
    "prints doctor.checks_passed message in legacy mode",
    "checks_passed message includes total check count in legacy mode",
    "uses tr language when config has language=tr in legacy mode",
    "falls back to en when config missing in legacy mode",
    "falls back to en when config has invalid JSON in legacy mode",
    "returns passed=false for win32 with unsupported message",
    "win32 check is not required (warning only)",
    "returns passed=true for linux",
    "returns passed=true for darwin (macOS)",
    "returns passed=true with untested note for unknown platform",
    "detects WSL2 via WSL_DISTRO_NAME env var",
    "detects WSL2 via WSL_INTEROP env var",
    "native Windows platform check does not affect ok (non-required)",
    "returns true when WSL_DISTRO_NAME is set",
    "returns true when WSL_INTEROP is set",
    "returns true when /proc/version contains \"microsoft\"",
    "returns true when /proc/version contains \"Microsoft\" (case insensitive)",
    "returns false when no WSL env vars and /proc/version throws",
    "returns false when /proc/version exists but no microsoft mention",
    "starts with \"Deckent Health Check\" header",
    "shows \"Your System:\" section",
    "shows \"Your Project:\" section",
    "shows \"Recommendation:\" section",
    "shows system checks with version numbers",
    "shows provider status with auth method",
    "shows failed provider with hint",
    "shows memory percentage and health",
    "shows memory OVER BUDGET when exceeds limit",
    "shows last sprint ID",
    "omits last sprint when null",
    "shows debt items with warning",
    "shows critical debt items count",
    "shows positive recommendation when all ok",
    "shows fix recommendation when required check fails",
    "uses the explicit required keyring check without reading host authority state",
    "keeps compatibility fixtures deterministic without an ambient keyring fallback",
    "shows provider tips for unavailable providers",
    "shows brain decay tip when over budget",
    "shows stale lock warning in project section",
    "shows codex provider hint when unavailable",
    "shows all providers as OK when all available",
    "does not include tips when all providers available",
    "returns sprint ID from config",
    "returns null when config file does not exist",
    "returns null when config has no last_sprint_id",
    "returns null when config is invalid JSON",
    "returns zero when no DB file",
    "counts debt entries from MemoryStore",
    "returns zero for empty DB",
    "handles MemoryStore error gracefully",
    "returns Gemini tip when Gemini unavailable",
    "returns Codex tip when Codex unavailable",
    "returns no tips when all providers available",
    "returns multiple tips when multiple providers unavailable",
    "returns Claude tip when Claude unavailable",
    "returns \"healthy\" for usage below 50%",
    "returns \"moderate\" for usage 50-79%",
    "returns \"high\" for usage 80-100%",
    "returns \"OVER BUDGET\" for usage above 100%",
    "returns \"2/3 providers ready\" when 2 of 3 available",
    "returns \"0/3 providers ready\" when none available",
    "returns \"3/3 providers ready\" when all available",
    "returns \"0/0 providers ready\" for empty array",
    "returns \"READY\" when all checks pass",
    "returns \"NOT READY\" when a required check fails",
    "returns \"READY (with warnings)\" when brain over budget",
    "returns \"READY (with warnings)\" when optional check fails",
    "shows provider summary line",
    "shows Status: READY when all ok",
    "shows Status: NOT READY when required check fails",
    "shows Status: READY (with warnings) when optional check fails",
    "shows tiered memory label \"healthy\" for low usage",
    "shows tiered memory label \"high\" for 80%+ usage",
    "shows claude provider hint when claude is missing",
    "shows claude provider tip when claude is unavailable",
    "shows \"Provider Health:\" header",
    "shows available provider with session auth",
    "shows available provider with API key auth",
    "shows unavailable provider with hint",
    "shows .deck file status when secrets exist",
    "shows .deck file not found status",
    "shows detected environment",
    "shows tmux environment when detected",
    "returns \"not found\" when no secrets loaded",
    "returns configured key count when secrets present",
    "does not count empty string values as configured",
    "shows error flag when validation fails",
    "does not show error flag when validation passes",
    "includes Provider Health section when projectRoot is provided",
    "does not include Provider Health section when projectRoot is missing",
    "shows environment detection in provider health section",
    "returns npm install command for claude",
    "returns npm install command for codex",
    "returns npm install command for gemini",
    "returns empty string for unknown provider",
    "all install hints start with \"install:\"",
    "maps available provider with session auth to ok status",
    "maps unavailable provider to missing auth status",
    "converts full provider list to health results",
    "returns empty array for empty provider list",
    "maps api_key auth to ok status",
    "maps available local Ollama runtime to auth-not-applicable ok status",
    "sets cliVersion to null when version is undefined",
    "starts with \"Provider Health:\" header",
    "shows [PASS] for available provider with ok auth",
    "shows [PASS] with API key label for non-Claude provider",
    "shows [WARN] with install hint for unavailable provider",
    "shows [WARN] with auth missing for available provider with missing auth",
    "shows local Ollama as available without inventing cloud auth",
    "shows .deck file status line",
    "shows [WARN] for missing .deck file",
    "shows environment detection line",
    "shows install hint for codex when unavailable",
    "shows install hint for claude when unavailable",
    "uses connector format ([PASS]) when connectorHealthResults provided",
    "shows [WARN] for missing provider in connector format",
    "falls back to OK/FAIL format when connectorHealthResults absent",
    "shows environment in connector health section",
    "tmux not found is required=true when only claude provider",
    "tmux not found is required=false when codex provider used",
    "tmux not found is required=false when gemini provider used",
    "tmux found is required=true when only claude (default)",
    "tmux not found is required=true when no provider names given (default)",
    "tmux not found is required=true when mixed but claude included",
    "returns passed=false when auth check fails",
    "returns passed=true when auth check passes",
    "does not run auth check when checkAuth=false (default)",
    "collapses a docker-info timeout into the same message as a genuinely missing binary",
    "reports Docker unavailable via `docker info` even while direct `docker version` (client+server) succeeds",
    "stale lock message includes cleanup hint",
    "returns passed=true when directories are writable",
    "returns passed=false when .tasks/ is not writable",
    "returns passed=true when directories do not exist",
    "is a required check",
    "returns passed=true when .deck file does not exist",
    "returns passed=false when .deck is tracked by git",
    "returns passed=true when .deck exists but not tracked",
    "is not a required check",
    "System Health section does not repeat memory line from Your Project",
    "System Health uses debtItems.total without re-reading file",
    "includes .deck Security check",
    "includes Write Permissions check",
    "passes providerNames to checkTmux — tmux not required for non-claude",
    "passes when all files are gitignored and not tracked",
    "fails when a file is tracked by git",
    "fails when .gitignore is missing",
    "fails when .gitignore entry is missing",
    "is not a required check",
    "returns the ollama-specific actionable hint",
    "still returns sensible hints for the cloud CLI providers",
    "falls back to a generic message for unknown providers",
    "formats an Ollama partial-state line with the \"server reachable\" reason",
    "formats an Ollama unreachable line as \"server not reachable\"",
    "labels Ollama ready state with \"server\" instead of \"CLI\" in the version suffix",
    "exists as an exported function from doctor.ts",
    "returns an entry for every provider including ollama",
    "ollama entry always carries the ollama-specific hints array"
   ],
   "cli/commands/explain-enhanced.test.ts": [
    "should show specific sprint by ID",
    "should error on non-existent sprint ID",
    "should default to latest sprint without --sprint",
    "should extract goal from DIRECTIVES.md",
    "should fallback when no goal available",
    "should output valid JSON",
    "should show Turkish labels when language is tr",
    "should handle empty content gracefully",
    "should extract goal line",
    "should return null when no DIRECTIVES.md",
    "should extract first line after sprint heading",
    "should return null for empty content",
    "should use Turkish labels"
   ],
   "cli/commands/finalize.test.ts": [
    "does not finalize or print terminal completion when shared recovery holds",
    "routes --force to ABORTED settlement and never invokes normal finalize"
   ],
   "cli/commands/flow.test.ts": [
    "calls runtime.tick exactly once",
    "tick callback receives dispatches and routes through handleFlowDispatchTick",
    "tick callback with due flows emits self-dispatch queue report",
    "calls runtime.start (not tick) for daemon setup",
    "prints daemon-started message",
    "daemon callback invokes handleFlowDispatchTick when interval fires",
    "daemon callback with due flows queues self-dispatch and reports correctly"
   ],
   "cli/commands/history-agents.test.ts": [
    "registers --agent and --skill options",
    "includes Agents and Skills columns in table",
    "--agent filter returns matching sprints",
    "--skill filter returns matching sprints",
    "shows no matching history for unmatched agent",
    "shows no matching history for unmatched skill",
    "uses agent/skill info from sprint log content when filtering",
    "parses agents from content",
    "parses skills from content",
    "returns empty when no agents or skills",
    "handles single agent",
    "handles single skill",
    "includes agents and skills in record",
    "returns dash for missing agents and skills",
    "formats ms to seconds",
    "formats ms to minutes and seconds"
   ],
   "cli/commands/history-overhaul.test.ts": [
    "outputs JSON array when --json is passed",
    "JSON output includes all fields",
    "limits to last N sprints",
    "shows all sprints when --last is not specified",
    "--last 1 shows only most recent sprint",
    "sorts sprint-1000 after sprint-999",
    "reads from archive dir when it exists",
    "deduplicates between sprints dir and archive dir",
    "shows token and call counts from usage file",
    "shows dash for tokens when no usage file exists",
    "parses Files Changed field from sprint log",
    "returns dash for files changed when not present",
    "registers --json option",
    "registers --last option"
   ],
   "cli/commands/history.test.ts": [
    "registers history command",
    "shows message when sprints dir does not exist",
    "shows message when sprints dir is empty",
    "filters only sprint-*.md files",
    "sorts sprint files numerically",
    "formats ms to seconds",
    "formats ms to minutes and seconds",
    "returns raw value for non-ms format",
    "handles zero milliseconds",
    "formats large values",
    "parses standard table format",
    "returns Unknown for missing title",
    "returns dash for missing fields",
    "calculates 0% no-go rate when total=0",
    "falls back to non-table format"
   ],
   "cli/commands/i18n-integration.test.ts": [
    "returns English sandbox message for lang=en",
    "returns Turkish sandbox message for lang=tr",
    "returns English --force hint for lang=en",
    "returns Turkish --force hint for lang=tr",
    "returns English watch-ignored note for lang=en",
    "returns Turkish watch-ignored note for lang=tr",
    "interpolates {number}, {id}, {count} in start.sprint_planned",
    "interpolates {count} and {model} in start.workers_info",
    "returns dry-run complete message for en",
    "returns Turkish dry-run complete message for tr",
    "returns watch-created message for en",
    "returns watch-no-tmux message for en",
    "returns English plan.approved for lang=en",
    "returns Turkish plan.approved for lang=tr",
    "returns English plan.rejected for lang=en",
    "returns Turkish plan.rejected for lang=tr",
    "interpolates plan.sprint_planned with number, id, count",
    "returns Turkish sprint_planned message for tr",
    "interpolates plan.note_sprint_size with size and reason",
    "returns Turkish note_sprint_size for tr",
    "returns English no_active_sprint for lang=en",
    "returns Turkish no_active_sprint for lang=tr",
    "returns English dashboard_read_failed for lang=en",
    "returns Turkish dashboard_read_failed for lang=tr",
    "returns key itself when key is not registered",
    "falls back to English when lang is unknown",
    "falls back to English when lang is empty string",
    "leaves unreferenced placeholders intact",
    "returns \"en\" when config file does not exist",
    "returns \"tr\" when config has language=\"tr\"",
    "returns \"en\" when config has language=\"en\"",
    "returns \"en\" when config has unknown language",
    "returns \"en\" when config has no language field",
    "returns \"en\" when config file is invalid JSON",
    "uses English sandbox message when lang=en",
    "uses Turkish sandbox message when lang=tr",
    "uses English --force hint when lang=en",
    "uses Turkish --force hint when lang=tr",
    "dry-run prints dry-run-complete in English for lang=en",
    "dry-run prints dry-run-complete in Turkish for lang=tr",
    "prints \"Plan rejected.\" in English when lang=en",
    "prints Turkish rejection message when lang=tr",
    "prints \"Plan approved.\" in English when lang=en and confirmed",
    "shows \"No active run (sprint)\" English message when dashboard missing and config has lang=en",
    "shows Turkish \"no active sprint\" when config has lang=tr",
    "shows \"Failed to read dashboard file.\" in English when parse fails and lang=en"
   ],
   "cli/commands/init-gitignore.test.ts": [
    "gitignores memory.db so users do not commit the binary DB",
    "gitignores the ERRORS.md runtime log",
    "still ignores .tasks/ and .locks/ runtime dirs",
    "is idempotent — a second call does not duplicate entries"
   ],
   "cli/commands/init.test.ts": [
    "registers init command",
    "registers --auto flag",
    "registers --manual flag",
    "has correct description",
    "creates .deckent directory",
    "creates .brain directory",
    "creates .tasks directory",
    "creates .locks directory",
    "creates .brain/sprints sub-directory",
    "creates .deckent/workspace directory",
    "creates .deckent/plugins directory",
    "writes DECKENT.md when it does not exist",
    "does not overwrite DECKENT.md when it already exists",
    "writes TOOLS.md to workspace dir",
    "writes BOOT.md to workspace dir",
    "writes DIRECTIVES.md when it does not exist",
    "TOOLS.md reads scripts from package.json when it exists",
    "TOOLS.md falls back gracefully when package.json missing",
    "writes config.json with mode, language, projectName",
    "merges with existing config.json preserving other fields",
    "falls back to fresh config if existing config.json is malformed",
    "config.json uses auto-detected mode in --auto mode",
    "calls ensureDeckentImport for AGENTS.md",
    "calls ensureDeckentImport for CLAUDE.md",
    "calls ensureDeckentImport for AGENTS.md and CLAUDE.md",
    "writes AGENTS.md with DECKENT.md reference when it does not exist",
    "writes .gitignore with .tasks/ entry when it does not exist",
    "writes .gitignore with .locks/ entry",
    "writes .gitignore with .dashboard entry",
    "skips duplicate entries when .gitignore already has all required entries",
    "only appends missing entries to existing .gitignore",
    "calls getSystemProfile in --auto mode",
    "calls detectSubscription in --auto mode",
    "calls analyzeProject in --auto mode",
    "calls generateSetupRecommendation in --auto mode",
    "does NOT call promptSelect in --auto mode",
    "does NOT call promptText in --auto mode",
    "prints welcome banner in --auto mode",
    "prints next steps after init",
    "calls promptSelect for plan mode",
    "calls promptSelect for language",
    "calls promptText for project name",
    "does NOT call getSystemProfile in interactive mode",
    "calls printError and sets exitCode=1 when mkdirSync throws",
    "calls printError and sets exitCode=1 when writeFileSync throws",
    "prints welcome banner in --auto mode",
    "prints detected setup in --auto mode",
    "prints setup progress with checkmarks",
    "prints next steps with numbered instructions (en)",
    "prints zero-config hint with deckent start example",
    "prints Turkish next steps when language is tr (interactive mode)",
    "prints Turkish zero-config mode when language is tr",
    "calls detectAvailableProviders during init",
    "shows detected provider info in human-friendly output",
    "calls buildProviderWizardSteps",
    "writes provider config to config.json",
    "auto-configures single provider without wizard in --auto mode",
    "runs wizard when autoConfig is null (multiple providers, interactive)",
    "resolves wizard result with resolveProviderWizardResult",
    "calls formatProviderAuthGuidance",
    "prints auth guidance when providers are unavailable",
    "skips auth guidance print when all providers available",
    "writes fallback_provider to config when present",
    "merges provider config into existing config.json",
    "calls runDoctorChecks during init",
    "prints health issue message when doctor returns not-ok",
    "does NOT print health message when doctor returns ok",
    "continues if runDoctorChecks throws",
    "calls ensureDeckentImport for CLAUDE.md in shell environment",
    "calls ensureDeckentImport for CLAUDE.md in vscode environment",
    "does NOT write GEMINI.md in shell environment",
    "does NOT write cursor rules in shell environment",
    "registers --cursor flag",
    "registers --claude-code flag",
    "calls getMCPGuidance during init",
    "uses cursor IDE when --cursor flag is passed",
    "uses claude-code IDE when --claude-code flag is passed",
    "calls detectIDEEnvironment when no IDE flag is passed",
    "does NOT call detectIDEEnvironment when --cursor is passed",
    "does NOT call detectIDEEnvironment when --claude-code is passed",
    "prints MCP guidance lines",
    "includes \"Welcome to Deckent!\"",
    "shows \"I detected your setup:\" header",
    "shows Node.js version when provided",
    "shows available providers with auth method",
    "shows unavailable providers as \"Not configured\"",
    "shows project stack from analysis",
    "skips unknown stack values",
    "skips framework \"none\" in stack",
    "shows provider version when provided",
    "handles multiple providers",
    "includes \"Setting up your AI development team...\" header",
    "shows completed steps with ✓ icon",
    "shows incomplete steps with · icon",
    "shows multiple steps in order",
    "includes numbered steps 1-3 in English",
    "includes deckent set-directives command example",
    "includes deckent plan command",
    "includes deckent start command",
    "includes zero-config mode hint with deckent start + description",
    "includes \"You\\'re ready!\" message in English",
    "returns Turkish steps when language is tr",
    "includes Turkish zero-config hint",
    "defaults to English for unknown language",
    "calls showSplash during init",
    "prints splash output",
    "continues if showSplash throws",
    "calls detectEnvironment during init",
    "creates AGENTS.md template when codex environment detected",
    "AGENTS.md for codex contains Deckent Integration header",
    "creates GEMINI.md template when gemini environment detected",
    "GEMINI.md contains correct context reference",
    "creates .cursor/rules/deckent.mdc when cursor environment detected",
    "cursor rule file contains DECKENT.md reference",
    "prints environment info line in summary for any environment",
    "prints environment info in summary",
    "prints codex-specific message for codex environment",
    "registers --env flag",
    "registers --all-envs flag",
    "default init (no --env) does NOT call generateCodexConfig",
    "default init (no --env) does NOT call generateGeminiConfig",
    "--env codex calls generateCodexConfig",
    "--env codex wires AGENTS.md additively, never overwrites",
    "--env gemini calls generateGeminiConfig",
    "--env gemini wires GEMINI.md additively, never overwrites",
    "--env cursor calls generateCursorConfig",
    "--env codex,cursor creates both environment configs",
    "--all-envs calls generateCodexConfig, generateGeminiConfig, and generateCursorConfig",
    "--all-envs wires every adapter additively, never overwrites",
    "multi_ide_mode set in config when --env has multiple values",
    "multi_ide_mode set when --all-envs is used",
    "multi_ide_mode NOT set when only single env specified",
    "detectFullStack is called for stack-aware templates when --env is used",
    "gracefully handles detectFullStack failure",
    "ignores invalid env names in --env flag",
    "calls createDeckTemplate during init",
    "continues if createDeckTemplate throws",
    "returns \"tr\" when LANG=tr_TR.UTF-8",
    "returns \"en\" when LANG=en_US.UTF-8",
    "prefers DECKENT_LANG over LANG (canonical chain)",
    "falls back to \"en\" when no language env is set",
    "returns empty string for empty reasons",
    "formats reasons with arrow prefix",
    "detects language from system locale in --auto mode",
    "shows recommendation reasons after auto-detect",
    "does not show recommendations when reasons array is empty",
    "uses commands from detectFullStack in DECKENT.md",
    "falls back to tsc defaults when detectFullStack throws",
    "registers --upgrade flag",
    "overwrites DECKENT.md when --upgrade is passed",
    "does not overwrite an existing AGENTS.md",
    "wires the env even when its file already exists (no --force needed)",
    "registers --repair flag",
    "shows retry hint when init fails",
    "uses deepMerge instead of shallow Object.assign for config merge",
    "preserves nested fields during config merge",
    "calls ensureDeckGitignore after createDeckTemplate",
    "ensureDeckGitignore is called when createDeckTemplate succeeds",
    "skips ensureDeckGitignore if createDeckTemplate throws",
    "uses deepMerge for provider config merge with existing config",
    "writes fresh config when no existing config exists",
    "assigns fallback_provider when multiple providers available in --auto mode",
    "does not set fallback_provider when only one provider in --auto mode",
    "runs analyzeProject once for stack detection in --auto mode",
    "calls analyzeProject in interactive mode too (stack detection always runs)",
    "starts with @DECKENT.md reference (single-source principle)",
    "contains workflow steps",
    "mentions GO/NO_GO review",
    "is valid JSON",
    "registers deckent server with deckent-mcp command",
    "creates .cursor/rules/deckent.mdc when .cursor/ dir exists",
    "creates .vscode/mcp.json when .vscode/ dir exists",
    "calls ensureDeckentImport for AGENTS.md when codex.md is absent",
    "skips ensureDeckentImport when codex.md exists",
    "--all-envs creates cursor adapter even without .cursor/ dir",
    "--all-envs creates vscode adapter even without .vscode/ dir",
    "returns exists action when .cursor/rules/deckent.mdc already exists",
    "--force preserves an existing .cursor/rules/deckent.mdc through additive upsert",
    "prints cursor adapter message when .cursor/ dir detected during init",
    "prints vscode adapter message when .vscode/ dir detected during init",
    "--all-envs flag creates all IDE adapters"
   ],
   "cli/commands/intelligence.test.ts": [
    "routes watch run through the scheduled flow capability id and fixture loader",
    "maps --dry-run to mutation-free capability mode without registering a flow",
    "ensures the canonical flow idempotently through FlowRegistry",
    "renders injected event history and last-run state in Turkish"
   ],
   "cli/commands/kill-enhanced.test.ts": [
    "updates task status to PAUSED after kill",
    "finds task with sprint prefix pattern",
    "warns but does not error when task file not found",
    "releases locks owned by killed worker",
    "preserves locks owned by other workers",
    "does not error when locks directory does not exist",
    "kills all EXECUTING tasks with --all",
    "kills CLAIMED tasks with --all",
    "reports no active workers with --all on empty tasks dir",
    "reports no active workers when tasks dir does not exist",
    "cleans prompt files for the killed task",
    "shows TmuxError message for non-existent worker",
    "kill + lock cleanup is idempotent (2nd run safe)"
   ],
   "cli/commands/kill.test.ts": [
    "registers kill command with taskId argument",
    "requires taskId argument (or --all flag)",
    "calls killWorker with the provided taskId",
    "prints success message when worker is killed",
    "handles TmuxError with custom message and exitCode=1",
    "does not set exitCode when worker is successfully killed",
    "rethrows non-TmuxError exceptions",
    "passes correct taskId format to killWorker",
    "uses English success message when language is en",
    "uses Turkish success message when language is tr",
    "uses English not-found message when language is en",
    "uses Turkish not-found message when language is tr",
    "falls back to English when config load fails",
    "calls loadConfig with project root",
    "releases only legacy lock namespaces and preserves execution authority bytes"
   ],
   "cli/commands/limits.test.ts": [
    "registers every catalog-backed provider filter"
   ],
   "cli/commands/marketplace-improvements.test.ts": [
    "accepts 1.0.0",
    "accepts 0.0.1",
    "accepts 10.20.30",
    "accepts 1.0.0-alpha.1 (pre-release)",
    "accepts 1.0.0-beta+build (pre-release + build)",
    "accepts 2.0.0+build.123 (build metadata)",
    "rejects partial version 1.0",
    "rejects single number 1",
    "rejects v-prefixed v1.0.0",
    "rejects \"latest\"",
    "rejects empty string",
    "rejects \"1.0.0.0\" (four parts)",
    "getCached returns null for unknown key",
    "setCache + getCached returns stored value",
    "clearRegistryCache removes all entries",
    "different cache keys are independent",
    "setCache overwrites existing entry for same key",
    "cache stores complex objects"
   ],
   "cli/commands/memory.test.ts": [
    "registers recall and remember as canonical memory subcommands"
   ],
   "cli/commands/multi-provider-spawn-kill-run.test.ts": [
    "spawnWorkerMultiProvider fails closed for unmetered tmux claude execution",
    "rejects raw attended evidence without an exact dispatch binding before subprocess provider work",
    "spawnWorkerMultiProvider uses subprocess for gemini models",
    "spawnWorkerMultiProvider passes autoApprove to a measured backend",
    "spawnWorkerMultiProvider passes autoApprove to subprocess",
    "reports the subprocess route after dispatching a codex task",
    "killSingle tries subprocess for codex task before tmux",
    "exports registerKill function",
    "buildRunTask creates valid task for any model",
    "run imports spawnWorkerMultiProvider from spawn module",
    "createRunTaskId generates unique IDs",
    "cleanupRunTask removes task files"
   ],
   "cli/commands/nervous.test.ts": [
    "sets exitCode=1 and calls printError when id is not found",
    "writes to pending + history directly when no live executor",
    "routes through NervousIpcQueue when poller is alive (does NOT write pending/history)",
    "race-repro: two concurrent edits with live poller produce 2 IPC files, not a torn pending write",
    "falls back to direct-write when heartbeat is stale (>5s old)"
   ],
   "cli/commands/onboard.test.ts": [
    "registers the onboard command on the program",
    "onboard command has a description",
    "onboard command description mentions wizard or onboard",
    "prints a message when the onboard command is invoked",
    "prints a welcome or setup message",
    "does not throw or crash when invoked",
    "does not call printError on normal invocation",
    "prints at least one message to guide the user",
    "does not call any config write on current stub implementation",
    "does not set a non-zero exit code on stub invocation",
    "invocation with no extra arguments works correctly",
    "does not crash if print throws unexpectedly",
    "exit code is not set to 1 under normal conditions",
    "returns available:true when claude CLI found",
    "returns available:false when claude CLI not found",
    "returns available:false when spawnSync throws",
    "detects project name from package.json",
    "detects TypeScript when tsconfig.json exists",
    "returns unknown when no package.json",
    "returns array of wizard steps",
    "includes language selection step",
    "includes mode selection step",
    "includes init confirmation step",
    "prints welcome message",
    "prints Claude CLI status",
    "prints system info",
    "prints ready message",
    "calls runWizard",
    "registers onboard command",
    "has --non-interactive option",
    "has a description"
   ],
   "cli/commands/plan.test.ts": [
    "registers plan command with options",
    "calls readContext and planSprint",
    "prints task table with ID, Title, Model, Priority",
    "--structured passes mode=structured to planSprint",
    "--no-confirm auto-approves the exact plan and skips prompt",
    "default (with confirm) shows approval prompt",
    "rejected plan records a reject decision and publishes nothing",
    "shows reasoning and planningMode when present",
    "handles loadConfig error gracefully",
    "handles planSprint error gracefully",
    "--dry-run delegates to the shared structured plan preview service",
    "--dry-run prints dry-run message and skips confirmation",
    "--dry-run preserves pre-existing DRAFT tasks by never invoking cleanupDraftTasks",
    "--dry-run surfaces and fails an undeclared shared-writer topology",
    "delegates planning to the exact-plan flow service without legacy draft cleanup",
    "registers --dry-run option on the command",
    "calls bootstrapProviders before planning",
    "falls back to structured mode when bootstrapProviders fails",
    "does not override --structured flag when bootstrap fails",
    "--dry-run uses structured mode without calling bootstrapProviders",
    "--dry-run + --structured skips bootstrap and uses structured",
    "successful bootstrap does not force structured mode"
   ],
   "cli/commands/plugin-create.test.ts": [
    "registers the create subcommand",
    "create subcommand has the correct description",
    "calls createPlugin with plugin name and pluginsDir",
    "prints confirmation message with plugin name",
    "prints the plugin directory path",
    "prints manifest.json creation notice",
    "prints SKILL.md creation notice",
    "prints README.md creation notice",
    "does not set exitCode on success",
    "sets exitCode=1 and calls printError when createPlugin throws PluginError",
    "sets exitCode=1 and calls printError on generic error",
    "does not print success message on error",
    "uses .deckent/plugins under project root",
    "passes the exact plugin name to createPlugin"
   ],
   "cli/commands/plugin-improvements.test.ts": [
    "removes an existing plugin directory",
    "prints success message after removal",
    "reports error for non-existent plugin",
    "remove command is registered",
    "warns about missing entrypoint in plugin list",
    "shows OK for valid entrypoint in plugin info",
    "warns about missing entrypoint in plugin info",
    "does not warn about valid entrypoint in list",
    "warns when creating a plugin that already exists",
    "creates plugin successfully when no conflict",
    "update command is registered"
   ],
   "cli/commands/plugin.test.ts": [
    "registers plugin command on program",
    "has install subcommand",
    "has list subcommand",
    "has info subcommand",
    "calls installPlugin with source and prints success",
    "prints plugin location after install",
    "sets exitCode=1 and calls printError when installPlugin throws",
    "passes git URL source to installPlugin",
    "prints no plugins message when list is empty",
    "prints plugin count and details for each plugin",
    "prints plugin description in list",
    "sets exitCode=1 and calls printError when scanPlugins throws",
    "calls scanPlugins with project root",
    "prints all plugin fields on success",
    "prints directory info",
    "sets exitCode=1 and calls printError when loadPlugin throws",
    "calls loadPlugin with the given directory"
   ],
   "cli/commands/process.test.ts": [
    "submit → prints executionId in output",
    "submit parks an ambiguous task (no scope) and prints pending-approval status",
    "throws a friendly error when description is empty",
    "closes the process-scoped controller after submit",
    "status → prints entry status after submit",
    "status with missing executionId → prints not_found message",
    "status with empty executionId → throws friendly error",
    "result → prints entry result after submit",
    "result with invalid executionId → prints not_found message",
    "result with empty executionId → throws friendly error"
   ],
   "cli/commands/recover.test.ts": [
    "binds a forced mutation to the exact recovery identity",
    "keeps dry-run read-only and does not manufacture approval",
    "projects stable JSON fields from the shared operation",
    "maps a typed HOLD without printing terminal success",
    "forwards skip-audit to the one shared application operation",
    "keeps restore as an explicit mutually exclusive rollback surface",
    "does not hide non-recovery programming errors",
    "routes released-unaccepted retention without reporting generic recovery completion",
    "refuses conflicting exact retention modes before executing recovery"
   ],
   "cli/commands/resume.test.ts": [
    "does not write sprint-state.json and invokes the normal path with a preplanned sprint",
    "builds the preplanned sprint from the full checkpoint and exact resumable ids",
    "commits the same ids reported by dry-run before building or spawning",
    "HOLDs before build/spawn when the checkpoint commit fails",
    "exits early on dry-run without writing sprint-state.json or calling runSprint",
    "keeps dry-run read-only when host settlement is pending",
    "runs settlement-first recovery without reset or preplanned spawn",
    "passes exact Store ownership to disposition before the reset boundary",
    "propagates exact Store ownership HOLD before kill, reset, or provider bootstrap",
    "HOLDs an invalid settlement without invoking recovery",
    "exits early when all tasks already completed without calling runSprint",
    "terminalizes a legacy completed test checkpoint without redispatching work",
    "resolves the private-Store terminal revalidator from the selected project root",
    "passes the Store-backed exact discriminator into completed-checkpoint terminalization",
    "exits with error when no checkpoint found",
    "does not claim completion when controller returns PAUSED"
   ],
   "cli/commands/retro-json.test.ts": [
    "should output valid JSON",
    "should not include raw field",
    "should include parsed metrics",
    "should include delta with --compare",
    "should prefer --json over --raw",
    "should handle empty retro gracefully"
   ],
   "cli/commands/retro-parse-fix.test.ts": [
    "should parse \"Tasks completed | 5/8\" correctly",
    "should parse \"Tasks completed | 0/3\" correctly",
    "should parse \"NO_GO rate | 25% (2/8)\" correctly",
    "should parse \"NO_GO rate | 0% (0/5)\" correctly",
    "should parse \"Sprint time | 3m 38s\" correctly",
    "should parse \"Coverage | 85.2%\" correctly",
    "should parse full sprint-reporter output",
    "should return defaults for empty content",
    "should count GO_WITH_TECH_DEBT occurrences as techDebt fallback",
    "should still parse legacy \"| Total Tasks |\" format",
    "should use fallback non-table format",
    "should return second-to-last sprint with 3+ files",
    "should return null with only 1 file",
    "should return null with 0 files",
    "--compare should compute real delta (previous != current)"
   ],
   "cli/commands/retro-rich.test.ts": [
    "registers --raw and --compare options",
    "shows rich summary by default",
    "--raw flag shows raw content",
    "shows no retrospective message when no retro entry exists",
    "shows no-retro message when the retro entry is empty",
    "--compare shows delta when a previous sprint retro exists",
    "--compare shows message when no previous sprint",
    "parses table format correctly",
    "returns defaults for missing fields",
    "stores raw content",
    "includes sprint id",
    "calculates success rate",
    "shows 0% success for zero tasks",
    "computes positive delta",
    "computes negative delta"
   ],
   "cli/commands/retro.test.ts": [
    "registers --explain with --task passthrough on retro"
   ],
   "cli/commands/review-finalize-onboard-upgrade-plugin-archive-debt-improvements.test.ts": [
    "saveReviewState writes to .brain/reviews/ path",
    "loadReviewState checks .brain/reviews/ first",
    "registers --sprint option on finalize command",
    "--sprint filters tasks by sprint ID",
    "does not call finalizeSprint when sprint filter matches no tasks",
    "buildOnboardSteps includes api mode option",
    "detectProviders returns not available when keys missing",
    "detectProviders returns available when OPENAI_API_KEY is set",
    "detectProviders returns available when GOOGLE_API_KEY is set",
    "runOnboard shows provider status output",
    "getChangelog returns changelog string when available",
    "getChangelog falls back to description when changelog field is undefined",
    "getChangelog returns null when npm fails",
    "registerUpgrade registers --changelog option",
    "executeUpgrade --changelog shows changelog for latest",
    "executeUpgrade --check shows Update available when version is newer",
    "plugin list --json outputs JSON array",
    "plugin list --json includes entrypointOk field",
    "plugin info resolves relative paths",
    "plugin test command is registered",
    "plugin test passes when plugin is valid",
    "plugin test fails when plugin not found",
    "plugin test fails when entrypoint is missing",
    "registers --count option on archive-debt command",
    "--count shows resolved count (covered by archive-debt.test.ts)",
    "--count with --before shows filtered count (covered by archive-debt.test.ts)",
    "--count returns 0 when no resolved items (covered by archive-debt.test.ts)",
    "archive-debt reads from DB when memory.db exists (covered by archive-debt.test.ts)"
   ],
   "cli/commands/review-finalize-overhaul.test.ts": [
    "--approve-all approves all pending tasks",
    "--reject-all rejects all pending tasks",
    "--auto assigns retry for DONE + testsPassed=false",
    "detects mixed sprint IDs",
    "saves review state to both .tasks/ and .brain/reviews/",
    "loads review state from .brain/reviews/ persistent path",
    "interactive review calls promptSelect for pending tasks",
    "registers --approve-all and --reject-all options",
    "blocks finalize when tasks are in-progress without --force",
    "--force with in-progress tasks kills live workers and settles through forceAbortSprint (ABORTED), not finalizeSprint",
    "blocks duplicate finalize without --force",
    "detects mixed sprint IDs and warns",
    "integrates review rejected tasks as NO_GO",
    "registers --force option",
    "detects EXECUTING and CLAIMED tasks",
    "returns unique sprint IDs"
   ],
   "cli/commands/review.test.ts": [
    "registers review command with --auto and --json",
    "shows no tasks message when no task files",
    "shows review table with pending tasks",
    "--auto approves DONE tasks",
    "--auto rejects NO_GO tasks",
    "--auto keeps a task pending while host settlement is pending despite raw DONE output",
    "--auto uses the closed host result when raw worker output disagrees",
    "--json outputs JSON state",
    "shows summary with counts",
    "loads existing review state if available",
    "handles error gracefully",
    "detects sprint id from tasks",
    "multiple tasks review: shows all in table",
    "returns null when file does not exist",
    "returns parsed state when file exists",
    "returns null for malformed JSON",
    "writes review state to file"
   ],
   "cli/commands/run-overhaul.test.ts": [
    "buildRunTask creates valid task with PENDING status",
    "waitForRunResult returns null on timeout",
    "waitForRunResult returns result immediately when file exists",
    "cleanupRunTask removes all 5 file extensions",
    "cleanupRunTask is idempotent when files missing",
    "resolveAgentPrompt is called with project root and task",
    "buildWorkerPrompt accepts agentPrompt and skillPrompts",
    "returns immediately when log file never appears",
    "registerRun does not throw during setup",
    "run command has expected options"
   ],
   "cli/commands/run.test.ts": [
    "uses TASK_FILE_EXTENSIONS to clean up all task files",
    "cleans up .json task files",
    "cleans up .hb task files",
    "cleans up .result task files",
    "skips missing files silently",
    "ignores unlink errors gracefully",
    "creates a task object with provided id and description",
    "truncates long descriptions to 80 chars in title",
    "returns a string starting with run-",
    "returns unique IDs on successive calls",
    "TASK_FILE_EXTENSIONS contains all expected task file types",
    "cleanupRunTask covers all TASK_FILE_EXTENSIONS"
   ],
   "cli/commands/runs.test.ts": [
    "bare `runs` on an empty project prints the empty notice",
    "bare `runs` lists a phantom legacy flow honestly as running (unverified)",
    "`runs --close-stale` is a DRY-RUN: reports the candidate, writes nothing",
    "`runs --close-stale --yes` durably closes and the trailing list shows cancelled",
    "`runs --close-stale` with nothing stale says so",
    "`runs <n>` prints the rich detail — real start from the handle, humanized (F-3b)",
    "`runs <out-of-range>` prints the honest not-found",
    "binds exact start lineage to the durable approval actor, not the surface label",
    "`runs <n> --approve` approves the Desktop-proposed flow and shows the daemon-truth detail",
    "`runs <n> --reject --reason` cancels durably and echoes the reason",
    "`--approve --reject` together is an honest flag-conflict error",
    "`--reason` without `--reject` is refused",
    "decide flags without a run number are refused with the usage hint",
    "decide flags with an out-of-range number print the honest not-found",
    "`--approve` on a flow with no live preview fails honestly via the shared service",
    "`runs <flowId-prefix>` (no decide flag) shows the rich DETAIL too — the handoff handle works read-only",
    "decide accepts a unique flowId PREFIX (stable cross-surface handle; row numbers shift on re-sort)",
    "an AMBIGUOUS flowId prefix is an honest not-found, never a guess",
    "SURF-6 kuyruk-D: approving a gate-FAIL plan prints the honest gate warning first",
    "583/N1: `runs <prefix> --diff` prints the run\\'s unified footprint from the recorded base",
    "executeInboxDecision (REPL-card glue): approve/reject return the localized outcome line",
    "executeInboxDecision NEVER throws — a refusal comes back as an honest Error: line",
    "a flow whose jobs-dir record is terminal is NEVER offered for closure (execution truth wins)",
    "`--commit` on a non-terminal run is refused honestly (commit is a post-run step)",
    "`--commit` with a clean tree says so and commits nothing",
    "`--commit --yes` shows the proposal and lands a REAL commit (intent subject + deckent-run trailer)",
    "`--commit --yes --message` overrides the suggested message",
    "non-interactive without --yes prints the hint and touches NOTHING (no stage, no commit)",
    "`--commit` outside a git repository answers honestly"
   ],
   "cli/commands/serve-acceptance-composition.test.ts": [
    "opens the production reconciler from resolved ownership and injects its runtime into the API",
    "drains each restarted server and all of its owners exactly once",
    "keeps the default LLM drain on HOLD while the unavailable human branch fails closed",
    "closes every opened owner when API startup fails even if one close throws",
    "fails startup and closes provider ownership when later composition bootstrap fails",
    "never falls back to an injection-only server when default reconciliation cannot open"
   ],
   "cli/commands/serve-overhaul.test.ts": [
    "contains all original MIME types",
    "contains image MIME types",
    "contains font MIME types",
    "has more than 10 MIME type entries",
    "returns exists=false when directory does not exist",
    "returns exists=true, hasContent=true when directory has files",
    "returns exists=true, hasContent=false when directory is empty",
    "handles readdirSync errors gracefully",
    "registers serve command with expected options",
    "starts server on default port",
    "rejects invalid port",
    "warns when dist directory is missing"
   ],
   "cli/commands/skill-crud.test.ts": [
    "skill enable should set enabled=true in manifest",
    "skill enable on non-existent skill should fail",
    "skill disable should set enabled=false in manifest",
    "skill disable on non-existent skill should fail",
    "skill enable/disable should preserve other manifest fields",
    "skill delete should remove the skill directory",
    "skill delete on non-existent skill should fail",
    "skill info should show manifest details",
    "skill info should show SKILL.md snippet",
    "skill info on non-existent skill should fail"
   ],
   "cli/commands/skill-improvements.test.ts": [
    "returns a 64-char hex string (SHA-256)",
    "is deterministic for same content",
    "differs for different content",
    "parses URL with # version pin",
    "parses URL without version pin",
    "does not treat git@github.com: as version split",
    "parses @ version suffix on https URL",
    "parses branch name with # syntax",
    "errors when skill does not exist",
    "errors when skill has no source metadata",
    "errors gracefully when local source no longer exists",
    "shows usage stats when --stats flag given",
    "shows the truth-source usage statistics with --stats",
    "does not show stats section without --stats flag",
    "shows last sprint with --stats (truth store, never manifest)",
    "excludes node_modules when installing from local path",
    "saves .source.json after local install",
    "shows checksum in output after install"
   ],
   "cli/commands/skill-marketplace.test.ts": [
    "displays search results in table format",
    "shows message for no results",
    "outputs JSON with --json flag",
    "passes --category option to client",
    "passes --limit option to client",
    "shows offline fallback on network error",
    "validates manifest before publish",
    "shows validation errors for bad manifest",
    "--dry-run validates without publishing",
    "requires authentication for publish",
    "errors when manifest.json not found"
   ],
   "cli/commands/skill.test.ts": [
    "registers skill command on program",
    "has list subcommand",
    "has create subcommand",
    "has install subcommand",
    "prints no skills message when directory does not exist",
    "prints no skills message when directory is empty",
    "prints formatted table when skills exist",
    "outputs JSON when --json flag is used",
    "filters by category with --category",
    "shows disabled status for disabled skill",
    "handles multiple skills",
    "sets exitCode=1 on error",
    "skips non-directory entries",
    "skips malformed skill manifests",
    "truncates triggers longer than 3",
    "shows empty triggers correctly",
    "creates skill directory and files",
    "writes valid manifest.json with defaults",
    "writes SKILL.md template with skill name",
    "prints success message with directory",
    "rejects invalid name with special characters",
    "rejects name with underscores",
    "rejects special character only name",
    "rejects duplicate skill",
    "accepts alphanumeric name with hyphens",
    "prints file list after creation",
    "sets exitCode=1 on filesystem error",
    "installs skill from local directory",
    "rejects non-existent source path",
    "rejects non-directory source",
    "rejects source without manifest.json",
    "rejects invalid manifest.json",
    "rejects duplicate without --force",
    "overwrites existing with --force",
    "sets exitCode=1 on filesystem error",
    "clones from git URL and installs",
    "handles git clone failure",
    "handles missing manifest in cloned repo",
    "detects https:// as git URL",
    "detects git@ as git URL",
    "rejects duplicate from git without --force"
   ],
   "cli/commands/small-commands-improvements.test.ts": [
    "outputs dashboard JSON when --json flag used",
    "outputs error JSON when no active sprint and --json used",
    "dashboard command description mentions deckent status",
    "replaces existing section when heading exists",
    "appends section when heading does not exist",
    "handles case-insensitive heading match",
    "handles content with no trailing newline",
    "shows explicit warning when no sprint found",
    "returns null when heartbeat file does not exist",
    "returns parsed heartbeat data when file exists",
    "returns null when heartbeat file is malformed",
    "registers --min-coverage option",
    "rejects invalid --min-coverage values",
    "rejects --min-coverage > 100",
    "registers --prompt option on agent create",
    "registers --description option on agent create",
    "uses --prompt content as PROMPT.md content",
    "uses --description as agent description",
    "validates a valid manifest",
    "rejects manifest missing required fields",
    "rejects manifest with empty id",
    "accepts manifest with optional description and category",
    "validateSemver accepts valid semver",
    "validateSemver rejects invalid versions",
    "publish requires author field in manifest",
    "parseRetroLearnings respects maxItems parameter",
    "buildExplainOutput includes task details when verbose=true",
    "buildExplainOutput does NOT include task details when verbose=false",
    "registers --verbose option on explain command"
   ],
   "cli/commands/spawn-enhanced.test.ts": [
    "uses buildWorkerPrompt for rich prompt (not simple one-liner)",
    "injects agent context when agent is assigned",
    "injects skill context when skills are assigned",
    "blocks spawn when task status is DONE without --force",
    "allows spawn when task is DONE with --force",
    "blocks spawn when task status is EXECUTING",
    "blocks spawn when task status is NO_GO without --force",
    "passes autoApprove true when --auto-approve flag is set",
    "shows error when task file is not found",
    "shows scope directories after spawn",
    "shows write files after spawn",
    "prompt is at least 100 characters (rich prompt test)"
   ],
   "cli/commands/spawn.test.ts": [
    "registers spawn command with taskId argument",
    "calls readTask with project root and taskId",
    "uses the configured measured backend without opening a tmux session",
    "calls the measured backend with taskId, model, prompt, and root",
    "prints success message with task ID and model",
    "handles readTask error and sets exit code",
    "does not consult tmux when the configured measured backend is available",
    "handles configured backend spawn errors and sets exit code",
    "passes different canonical model IDs correctly to the measured backend",
    "uses English message when language is en",
    "uses Turkish message when language is tr",
    "fails closed in English when config loading removes the measured backend contract",
    "calls loadConfig with project root",
    "returns standard tool set when task has scope directories",
    "returns standard tool set when task has filesWrite",
    "returns undefined when scope has no directories or filesWrite",
    "returns tools when both directories and filesWrite are present",
    "blocks a budgetless remote worker before creating an execution backend or session",
    "does not elevate a worker-writable Task JSON budget into host spawn authority",
    "blocks a budgeted remote worker when its backend cannot meter live usage",
    "blocks the implicit Claude tmux fallback because it cannot meter live usage",
    "returns a measured configured backend and codex provider for OpenAI models",
    "returns a measured configured backend and gemini provider for Gemini models",
    "passes allowedTools to a measured configured backend for Claude models",
    "passes allowedTools to subprocess backend for non-Claude models",
    "prints provider name after spawning worker",
    "injects allowedTools into spawn when task has scope"
   ],
   "cli/commands/start.test.ts": [
    "registers the start command",
    "registers --dry-run flag",
    "registers --watch flag",
    "registers --auto-approve flag",
    "registers --force flag",
    "registers --sandbox-mode flag",
    "holds configured provider authority before zero-config mutation or provider work",
    "runs doctor checks before starting sprint",
    "aborts and sets exit code 1 when required doctor check fails",
    "skips doctor checks when --force is provided",
    "prints --force hint after pre-flight failure",
    "calls planSprint in dry-run mode",
    "does not call runSprint in dry-run mode",
    "prints task list in dry-run mode",
    "prints dry-run complete message",
    "ignores --watch flag in dry-run mode and prints a note",
    "passes readContext to planSprint",
    "prints sprint reasoning when present",
    "prints planning mode when present",
    "calls setupWatchWindow when tmux session is active",
    "prints watch window instructions when tmux session is active",
    "skips watch setup when no active tmux session",
    "prints note when tmux session is not active (uses subprocess log watch)",
    "passes autoApprove: true to runSprint when --auto-approve is set",
    "passes autoApprove: false to runSprint by default (K3 fail-closed — no flag, no auto-approve)",
    "handles BrainError and includes phase in message",
    "handles generic error and sets exit code 1",
    "handles missing DIRECTIVES via BrainError",
    "handles sandbox-mode: prints sandbox message and continues sprint",
    "prints formatted sprint summary on successful run",
    "reports a PAUSED run as blocked instead of completed",
    "handles loadConfig failure gracefully",
    "calls prepareZeroConfig when description argument is provided",
    "does NOT call prepareZeroConfig when no description is provided",
    "calls cleanupZeroConfig after a successful sprint when description was provided",
    "calls cleanupZeroConfig on sprint error when description was provided",
    "does NOT call cleanupZeroConfig when alreadyExisted is true",
    "prints warning when DIRECTIVES.md already exists",
    "prints zero-config created message when DIRECTIVES was created",
    "still runs sprint when description provided and DIRECTIVES already existed",
    "runs sprint normally when no description provided (backward-compatible)",
    "cleans up on pre-flight failure when description was provided",
    "cleans up on sandbox-mode when description was provided",
    "readProviderCache returns null when no cache file exists",
    "writeProviderCache creates cache file with correct content",
    "readProviderCache returns parsed cache after write",
    "isProviderCacheFresh returns false when configHash differs",
    "isProviderCacheFresh returns true for recent cache with matching hash",
    "isProviderCacheFresh returns false for expired cache",
    "applySandbox returns applied:false in non-git directory",
    "restoreSandbox is a no-op when applied is false",
    "returns a cleanup function",
    "cleanup function cancels the interval without throwing",
    "handles missing .tasks directory gracefully"
   ],
   "cli/commands/status-agents.test.ts": [
    "registers --verbose option",
    "shows agent and skill assignments when tasks exist",
    "does not show assignments when no tasks directory",
    "returns empty array when tasks dir does not exist",
    "loads task files",
    "skips malformed task files",
    "accepts only canonical task JSON records and excludes JSON sidecars",
    "shows agents grouped by name",
    "shows generic for tasks without agent",
    "shows no assignments message when empty",
    "verbose mode shows table with task ids",
    "shows skills grouped by name",
    "shows no skill assignments for tasks without skills",
    "verbose mode shows table"
   ],
   "cli/commands/status-mode.test.ts": [
    "registers --mode option on status command",
    "calls formatStatus when --mode explainatory is provided",
    "calls formatStatus when --mode verbose is provided",
    "calls formatStatus when --mode standart is provided",
    "does NOT call formatStatus when --mode is not provided (default path)",
    "outputs formatted result via print when --mode is set",
    "passes sprint phase from dashboard to formatStatus",
    "calls formatStatus and also prints agent assignments with --verbose"
   ],
   "cli/commands/status.test.ts": [
    "sweeps detached run deaths exactly once before rendering status",
    "registers status command with --watch, --json, --raw, and --no-color options",
    "shows no active sprint message when dashboard and tasks do not exist",
    "(A) shows standalone status from task files when no dashboard",
    "(A) standalone with --json outputs JSON",
    "adds read-only raw/effective receipt evidence to standalone JSON and closes the projection",
    "projects all status tasks through one bulk read instead of per-task queries",
    "surfaces open receipt reconciliation evidence in human status",
    "adds the same immutable settlement projection to follow snapshots",
    "renders human-friendly output by default",
    "(W0) isDashboardOrphaned=true routes to the no-active-sprint message, not the live view",
    "--raw renders legacy formatted dashboard",
    "--json outputs raw JSON",
    "(E) --json --verbose includes agent/skill info",
    "handles corrupt dashboard file with error",
    "(D) --watch uses fs.watch with fallback interval",
    "--watch with --json outputs JSON on each render",
    "--mode json emits one machine document without human settlement suffixes",
    "--mode json --graph preserves the single-document machine contract",
    "--watch --mode json emits ANSI-free NDJSON records",
    "--watch bounds burst output to in-flight plus latest and drains before shutdown",
    "--follow --json emits ANSI-free NDJSON instead of the human TUI",
    "--watch uses human-friendly output by default",
    "--watch --raw uses legacy dashboard",
    "human-friendly output is called with dashboard data and projectRoot",
    "human-friendly output includes tasks from loadTaskFiles",
    "dashboard with alerts renders in human-friendly mode",
    "(F) readSprintMeta tolerant regex matches various formats",
    "registers --graph option on status command",
    "prints Mermaid diagram when --graph is provided and depgraph exists",
    "prints \"no dependency graph\" message when depgraph does not exist",
    "prints \"no active run\" when getCurrentSprintId returns null",
    "Mermaid output includes sprint-specific header",
    "Mermaid output includes task nodes with wave notation",
    "returns null when mmd file does not exist",
    "returns file content when mmd file exists",
    "returns null when readFileSync throws",
    "constructs correct mmd path using sprint-NNN-depgraph.mmd pattern",
    "checks path inside .deckent directory"
   ],
   "cli/commands/sync-onboard-upgrade-overhaul.test.ts": [
    "returns all files joined when <= 50",
    "truncates at 50 with \"and N more...\" when > 50",
    "handles exactly 51 files",
    "returns single file without truncation",
    "truncates modified files list at 50",
    "syncs CLAUDE.md, AGENTS.md, GEMINI.md, .cursor/rules, .codex/AGENTS.md",
    "dry-run does NOT call ensureDeckentImport",
    "creates .cursor directory if not exists",
    "--json outputs JSON when DECKENT.md not found",
    "--dry-run does not call ensureDeckentImport",
    "uses git commit date when git log succeeds",
    "falls back to mtime when git log returns empty",
    "returns framework from stack-detector",
    "returns testFramework from stack-detector",
    "falls back gracefully when stack-detector throws",
    "includes api mode option",
    "has 4 mode choices including api",
    "skips init when already initialized and no --force",
    "runs init when --force is set even if already initialized",
    "passes --mode to init command",
    "passes --language to init command when not en",
    "does not pass --language for default en",
    "registers --force option",
    "parses basic version",
    "parses pre-release version",
    "parses canary version",
    "strips v prefix",
    "release > pre-release of same version",
    "pre-release < release of same version",
    "pre-release versions compared lexicographically",
    "equal pre-release versions",
    "newer canary > older canary",
    "global strategy uses npm install -g",
    "local strategy uses npm install without -g",
    "beta channel uses deckent@beta tag",
    "canary channel uses deckent@canary tag",
    "queries npm with beta tag when channel=beta",
    "queries npm with canary tag when channel=canary",
    "shows error when no rollback version saved",
    "registers --canary option",
    "registers --beta option",
    "registers --rollback option",
    "returns npx when npm_execpath contains npx",
    "returns global when npm list -g shows deckent",
    "returns local when npm list (no -g) shows deckent",
    "returns unknown when not found anywhere"
   ],
   "cli/commands/sync.test.ts": [
    "returns null when sprints directory does not exist",
    "returns null when sprints directory is empty",
    "returns the latest sprint file by mtime",
    "ignores non-sprint files",
    "returns true when inside a git repository",
    "returns false when not a git repository",
    "returns commit lines from git log",
    "returns empty array when git log fails",
    "returns empty array when no commits found",
    "legacy-compat: matches probeCommitsSince(...).commits ([]) on a git log failure",
    "categorizes modified, added, deleted, and renamed files",
    "returns empty arrays when commitCount is 0",
    "returns empty arrays and reports GIT_DIFF_FAILED when git diff fails",
    "returns empty arrays and reports GIT_REV_LIST_FAILED when the history-depth probe fails",
    "uses ls-tree root enumeration (not a fixed empty-tree diff id) when commitCount reaches history depth",
    "returns empty arrays and reports GIT_LS_TREE_FAILED when the root-fallback ls-tree enumeration fails",
    "throws RangeError for commitCount=%p without calling the git adapter",
    "returns commit lines and issue null on success",
    "returns commits [] and a typed GIT_LOG_FAILED issue with the adapter detail verbatim on failure",
    "forwards the adapter-supplied \"<kind>: exit <status>\" detail verbatim when stderr was empty",
    "short-circuits on a git log failure: commits 0, detection unavailable/GIT_LOG_FAILED, adapter called exactly once (never reaches getChangedFiles)",
    "collects commit count + changed files on success (log, rev-list, diff)",
    "shows \"No changes\" when commits is 0",
    "formats full sync output with all change types",
    "omits empty categories",
    "upserts an Out-of-band Changes memory entry",
    "records the latest sync (single upserted entry, last wins)",
    "does nothing when memory.db does not exist",
    "returns null when not a git repo",
    "returns null when no sprint files exist",
    "errors when DECKENT.md does not exist",
    "calls ensureDeckentImport for CLAUDE.md and AGENTS.md when DECKENT.md exists",
    "skips adapter sync with --git-only flag",
    "skips git detection with --adapters-only flag",
    "calls ensureDeckentImport on GEMINI.md",
    "skips ensureDeckentImport in dry-run mode",
    "always returns true",
    "ensures the canonical Cursor rules file when rules dir exists",
    "creates .cursor/rules and syncs when dir does not exist",
    "returns false when mkdirSync throws",
    "skips rules mutation in dry-run mode even when dir exists",
    "returns true in dry-run when dir does not exist (would create)",
    "returns false when .codex/ dir does not exist",
    "calls ensureDeckentImport on .codex/AGENTS.md when dir exists",
    "skips ensureDeckentImport in dry-run mode",
    "returns true when .codex/ exists",
    "returns map with claude and gemini always synced",
    "includes codex as not synced when .codex dir does not exist",
    "includes codex as synced when .codex dir exists",
    "reports cursor sync status",
    "calls ensureDeckentImport for each provider when not dry-run",
    "keeps the deprecated provider-named alias behavior-compatible"
   ],
   "cli/commands/test-run-overhaul.test.ts": [
    "produces valid XML with test cases",
    "escapes XML special characters in task titles",
    "produces no failures element for passing tasks",
    "produces TAP header and test lines",
    "marks all as ok when no NO_GO tasks",
    "gitStash returns false when no local changes",
    "gitStash returns true when changes stashed",
    "gitStash returns false on execSync error",
    "gitStashPop does not throw on error",
    "test command has --directives, --sandbox, --model, --reporter options",
    "test command rejects invalid reporter",
    "test command rejects invalid model"
   ],
   "cli/commands/truth.test.ts": [
    "never serializes missing cells as JavaScript nullish text"
   ],
   "cli/commands/upgrade.test.ts": [
    "returns 0 for equal versions",
    "returns -1 when current < latest",
    "returns 1 when current > latest",
    "handles v prefix",
    "compares patch versions",
    "compares major versions",
    "returns version when npm view succeeds",
    "returns null when npm view fails",
    "returns null when spawnSync throws",
    "prints current version",
    "shows \"Could not check\" when npm fails",
    "shows \"Already up to date\" when same version",
    "--check flag only shows info without installing",
    "registers upgrade command on program",
    "upgrade command has a description",
    "has --check option"
   ],
   "cli/commands/watch-overhaul.test.ts": [
    "registers watch command",
    "creates watch layout when no --follow",
    "attaches to worker pane when --follow is given",
    "sets exitCode=1 when no dashboard file",
    "sets exitCode=1 when no tmux session",
    "I) shows hint when worker window not found (TmuxError)",
    "I) shows \"Worker finished\" hint when result file exists",
    "J) warns when task is from a different sprint",
    "H) cleanupWatchWindow is exported and callable"
   ],
   "cli/commands/watch.test.ts": [
    "(1) does NOT warn when the followed task matches the ACTIVE sprint, even if config.last_sprint_id is stale (pre-fix RED)",
    "(2) when the task IS stale, the warning names the ACTIVE sprint (not config.last_sprint_id)",
    "(3) honors the sprint-active.json override (canonical active→state)"
   ],
   "cli/commands/xverify.test.ts": [
    "projects only an opaque owner decision ref for an exact admitted pair",
    "labels a non-exception equal-or-higher pair as normal admission",
    "keeps an equal-or-higher allowed pair classified as normal admission"
   ],
   "cli/commands.test.ts": [
    "reports all passing checks",
    "reports failing required check",
    "detects old Node version",
    "handles missing Node",
    "handles git version output format",
    "sets ok=false when required check fails",
    "reports workspace missing",
    "reports brain budget over limit",
    "reports critical debt",
    "reports stale locks",
    "attaches when session is active",
    "prints error when no session",
    "handles TmuxError",
    "kills a worker",
    "handles not found worker",
    "does not show Worker not found for non-TmuxError",
    "prints retro content when a retro entry exists",
    "prints message when no retro file",
    "handles an empty retro entry",
    "shows dashboard when file exists",
    "prints message when no dashboard",
    "handles corrupt dashboard file",
    "renders human-friendly status by default",
    "--json outputs parseable JSON",
    "--watch sets up interval",
    "shows sprint history table with 8 columns",
    "prints message when no history dir",
    "prints message when no sprint files",
    "shows \"-\" for missing tasks/coverage/duration fields",
    "shows \"Unknown\" for sprint without title",
    "formats duration from ms to human-readable",
    "calculates no-go rate as percentage",
    "shows 0% no-go rate when no failures",
    "falls back to non-table format for legacy logs",
    "converts ms under 60s",
    "converts ms over 60s",
    "returns 0s for 0ms",
    "passes through non-ms values",
    "parses full table format",
    "returns dashes for missing fields",
    "handles zero total tasks",
    "shows current config",
    "sets a config value",
    "handles missing config file on set",
    "handles validation error on set",
    "handles loadConfig error",
    "spawns a worker for a task",
    "handles task not found",
    "passes correct model to spawnWorker",
    "uses the configured backend without opening a tmux session",
    "spawn does not use haiku_allowed as autoApprove",
    "cleans up with tasks",
    "handles empty tasks dir",
    "handles missing tasks dir",
    "skips malformed task JSON files",
    "handles destroy() throwing silently",
    "handles cleanup() throwing",
    "--decay flag runs runDecay with force",
    "--decay with no archived sprints",
    "runs sprint and shows summary",
    "handles --auto-approve",
    "handles --sandbox-mode: runs sprint with sandbox context",
    "passes sandboxMode=undefined to runSprint when not set",
    "sandbox-mode passes sandboxMode:true to runSprint",
    "handles BrainError",
    "handles generic error",
    "pre-flight failure prevents runSprint",
    "--force skips pre-flight",
    "--dry-run plans but does not spawn",
    "--watch creates watch window before sprint when tmux session active",
    "--watch + --dry-run skips watch setup",
    "--watch without active tmux session falls back to subprocess watching",
    "plans and shows task table",
    "handles planning error",
    "shows multiple tasks",
    "--structured passes mode=structured to planRunFlow",
    "--no-confirm skips approval flow",
    "shows reasoning when present",
    "plugin list shows no plugins message",
    "upgrade shows current version",
    "onboard shows welcome message",
    "creates directory structure and config",
    "creates config with selected mode",
    "appends to existing .gitignore without duplicates",
    "creates DIRECTIVES.md",
    "creates brain directory files",
    "does not create PROJECT-IDENTITY.md (B6 — DB-first identity, Memory V2)",
    "creates claude rules",
    "handles prompt error",
    "shows honest outcome instead of unconditional next steps (412-001 INIT-01)",
    "creates plugins directory",
    "does not create a legacy per-project i18n directory",
    "creates TOOLS.md in workspace",
    "creates BOOT.md in workspace",
    "does not copy the legacy en.json catalog into the project",
    "does not copy the legacy tr.json catalog into the project",
    "creates DECKENT.md with full template",
    "calls ensureDeckentImport for CLAUDE.md (not destructive write)",
    "calls ensureDeckentImport for AGENTS.md",
    "does not overwrite CLAUDE.md with writeFileSync",
    "merges config when existing config.json present",
    "does not add .deckent/ to .gitignore",
    "brain.md template has frontmatter and 13 rules",
    "auditor.md template has frontmatter and rules",
    "worker-default.md template has heartbeat and result rules"
   ],
   "cli/compatibility-command-help.test.ts": [
    "registers deprecated top-level catch-all aliases without rewired legacy options",
    "shows deprecation banner and replacement SSOT help body",
    "routes parsed deprecated child help to the real canonical child without actions",
    "does not claim a canonical child for an unknown deprecated child token",
    "lists every deprecated forwarding command from surface-contract SSOT",
    "does not invoke either forwarding action while Commander renders help",
    "restores the canonical output writer after capturing full target help",
    "restores the canonical output writer when target help throws"
   ],
   "cli/compatibility-forwarding.test.ts": [
    "parseReplacementSurface splits inject flags and target segments",
    "buildBaselineForwardArgv preserves prefix flags and passthrough tokens",
    "forwards attach leaf flags to watch with from:node argv",
    "forwards dashboard group flags with inject prefix and negated boolean",
    "passes unknown tokens through without opts serializer"
   ],
   "cli/config-global.test.ts": [
    "shows project config by default when no flags",
    "config output is valid JSON",
    "config shows error when loadConfig fails",
    "exports config to stdout when no outputFile",
    "exports config to file when outputFile is given",
    "throws when config file does not exist",
    "strips JSON comments before exporting",
    "throws on invalid JSON after stripping comments",
    "imports and merges config over existing",
    "throws when import file does not exist",
    "throws on invalid JSON in import file",
    "creates fresh config when existing config is malformed",
    "calls validatePartialConfig on imported data",
    "loadGlobalConfig returns null when file does not exist",
    "mergeConfigs gives project config priority over global",
    "mergeConfigs works with null global config"
   ],
   "cli/config-legacy-mode-set.test.ts": [
    "config set succeeds with a legacy pro_plan config and persists canonical mode",
    "config set on a canonical-mode config leaves mode unchanged",
    "validatePartialConfig normalizes a legacy mode in place and does not throw",
    "validatePartialConfig leaves a canonical mode untouched",
    "validatePartialConfig still rejects a truly invalid mode",
    "migrateConfig surfaces the legacy mode rename in renamedFields",
    "migrateConfig reports a rename-only migration (no fields added)"
   ],
   "cli/config-nervous-i18n.test.ts": [
    "set mode confirmation is localized",
    "list matrix title, headers and preset descriptions are localized",
    "invalid preset error is localized",
    "safety-floor block message is localized",
    "English remains intact (default path) for set mode"
   ],
   "cli/config-nervous.test.ts": [
    "should set mode to strict and persist to config",
    "should add COMMIT_PUSH override to action_overrides",
    "should display 4-row authority matrix table",
    "should reset action_overrides to empty object",
    "should error on invalid preset and set exitCode to 1",
    "should error on unknown action ID",
    "should reject safety floor override with autonomous policy and show warning",
    "should reject safety floor override with suggest-5m as well",
    "should allow safety floor override with approve policy",
    "should wire up set mode via Commander subcommand",
    "should wire up override via Commander subcommand",
    "should wire up list via Commander subcommand",
    "should wire up reset via Commander subcommand",
    "should preserve existing mode when adding override",
    "should show active overrides in list output",
    "should mark the current active preset in list"
   ],
   "cli/config-set-values.test.ts": [
    "writes the native pin keys atomically and keeps unrelated keys",
    "sets dotted keys as nested values",
    "a validation failure is a typed outcome and leaves the file untouched",
    "rejects malformed terminal.startup.recent_sessions without writing the patch",
    "allows partial sprint-context limits, then requires both before enabling",
    "localizes an invalid sprint-context limit and leaves the patch unwritten",
    "validates an enabled project sprint context against global limits without copying them down",
    "keeps a project sprint-context limit authoritative over a global limit",
    "fails closed on invalid global sprint-context limits without writing the project patch",
    "fails closed with a localized diagnostic when the canonical global config is malformed (%s)",
    "a missing config file is created from the patch alone"
   ],
   "cli/confirmations-acceptance-service.test.ts": [
    "registers canonical flags and preserves process-risk metadata on the legacy route",
    "rejects the legacy unauthenticated human flag before any mutation or provider call",
    "dispatches %s run through the canonical composition",
    "passes all statements, flags, and exact provider authority into XVerify",
    "keeps UNCLEAR pending without opening settlement",
    "closes the production composition exactly once when settlement throws",
    "uses a byte-preserving read-only projection for expired input",
    "filters foreign-tenant rows before a provider call"
   ],
   "cli/confirmations-lifecycle.test.ts": [
    "routes the legacy human command and leaves the expired UNDECIDABLE tombstone authoritative",
    "does not invoke an llm after expiry has already won the read-side race"
   ],
   "cli/connect-auth-state.test.ts": [
    "renders the 3-state lines (connected/missing/unknown) via the shared doctor.auth_state_* keys",
    "renders localized Turkish state labels",
    "adds a guidance hint ONLY for the missing provider, naming its env var + .deck key + $DECK: pattern",
    "never prints a secret VALUE — the example command is always the literal placeholder <value>",
    "adapts the example command to the detected shell: %s",
    "omits the Auth State section entirely when no report is supplied (default param — backward compat)",
    "reflects real env state as connected/missing (3 providers) with zero mocking of buildAuthStateReport itself",
    "NEVER leaks a real secret value into text or JSON output — only key NAMES appear",
    "is diagnostic-only for auth state too: no login flow is triggered and no network call is made",
    "the %s guidance hint names an env var (%s) that doctor.ts actually honors"
   ],
   "cli/connect-cmd.test.ts": [
    "registers a connect command with --provider and --json options",
    "--json reports a fully-connected matrix with zero steps and no error exit",
    "--json surfaces suggested steps and exits 1 when a provider is logged out",
    "--provider scopes detection/planning to a single provider target",
    "rejects an unknown --provider value without crashing or probing",
    "prints a human-readable summary with provider/mcp/ide/shell sections",
    "renders suggested steps through the real i18n connect.step.* messages",
    "defaults to the all target when no --provider is given",
    "accepts each known provider name: %s",
    "returns an error object (never throws) for an unknown provider name",
    "accepts claude/codex/gemini and rejects everything else",
    "reports fully-connected with no steps",
    "lists each suggested step with its risk level and localized description",
    "renders in Turkish when lang=tr"
   ],
   "cli/connect-wizard.test.ts": [
    "reports a fully connected matrix when every probe is healthy",
    "preserves CONNECT_PROVIDERS order for providers[] and mcp[] regardless of probe resolution order",
    "marks a provider CLI-missing when diagnostics report binaryFound=false",
    "surfaces the GAP-4 distinction: CLI present but auth logged-out",
    "reports unknown auth state without inventing a confident answer",
    "mcp: unsupported host vs supported-but-unattached vs fully attached",
    "ide: %s passes through verbatim",
    "winShell: passes through %j verbatim",
    "WSL wins even though os.platform() reports linux inside WSL2",
    "non-Windows, non-WSL → posix",
    "native Windows + MSYSTEM set → gitbash",
    "native Windows + PSModulePath set (no MSYSTEM) → powershell",
    "native Windows + neither env var → cmd fallback",
    "MSYSTEM takes priority over PSModulePath when both are set",
    "a fully healthy provider needs zero steps",
    "missing CLI → install step sourced from planInstall (real reuse, not invented)",
    "CLI present but not logged in → login step, CLI takes priority when both are missing",
    "unknown provider name resolves to an empty step list, not a throw",
    "unsupported host → info-only advisory step (no command)",
    "supported but unattached → attach step sourced from getAttachCommand (real reuse)",
    "already attached → zero steps",
    "claude-code needs nothing",
    "cursor gets a concrete deckent init --cursor step",
    "terminal gets an advisory-only step (no command)",
    "native Windows (non-WSL) → wsl --install recommendation",
    "WSL already in use → zero steps",
    "posix host → zero steps",
    "a fully healthy matrix needs zero steps across every dimension",
    "concatenates in fixed dimension order: providers, then mcp, then ide, then winShell",
    "is deterministic — identical input yields deep-equal output on repeat calls"
   ],
   "cli/context-lifecycle-ux.test.ts": [
    "classifies the real wire event for ${cls}",
    "returns undefined for a generation-recovery event with action \"hold\"",
    "classifies cadence-toolcalls as CADENCE_REQUIRES_CHECKPOINT",
    "returns undefined for no-progress stall checkpoints",
    "never maps token-pressure to a reference-expansion message key",
    "returns undefined for TRANSPORT_EMPTY (empty-response) — a real event, but not one of the formal classes",
    "returns undefined for an unrelated event type (e.g. text-delta)",
    "the classifier resolves to a distinct, non-empty message key per class",
    "renders pairwise-distinct real ${lang} messages (no class shows another class's message)",
    "MEASURED_CONTEXT_PRESSURE uses measured-context-pressure wording (en)",
    "MEASURED_CONTEXT_PRESSURE uses ölçülmüş bağlam baskısı wording (tr)",
    "CADENCE uses execution budget checkpoint wording (en)",
    "CADENCE uses yürütme bütçesi checkpoint wording (tr)",
    "ONLY INPUT_CONTEXT_OVERFLOW claims the context window is full — the other classes never do (en)",
    "MEASURED_CONTEXT_PRESSURE + INPUT_CONTEXT_OVERFLOW: overflowed context renders measured-pressure checkpoint and terminal overflow",
    "CADENCE_REQUIRES_CHECKPOINT end-to-end: cadence budget tick renders execution budget checkpoint notice"
   ],
   "cli/crash-artifact-schema.test.ts": [
    "produces every required field with the correct type",
    "stack is null (not undefined) when no stack is available",
    "resolves a real semver-ish deckent version, never throwing",
    "digest is deterministic for the same root and differs for different roots",
    "redacts an sk-... API key embedded in a single argv element",
    "redacts a password= assignment split by the normal CLI --flag value convention",
    "preserves non-sensitive argv content",
    "buildCrashArtifact never leaks a secret through the command field",
    "produces unique filenames for the same pid under a frozen clock",
    "keeps the .log suffix for backward-compatible readers",
    "embeds the pid in the filename",
    "writes valid JSON round-tripping the artifact, with no leftover temp file",
    "uses least-privilege file mode (owner read/write only)",
    "still prints FATAL and exits(1) when the crash directory cannot be created",
    "produces exactly one .log file with a valid CrashArtifactV1 body",
    "two fatals in the same process never collide on filename"
   ],
   "cli/crash-retention-reader.test.ts": [
    "returns the documented defaults when no env is set",
    "honors valid env overrides",
    "falls back to defaults for non-numeric or non-positive env values",
    "prunes beyond maxCount, keeping the newest N",
    "keeps an artifact exactly at the age boundary, prunes one ms past it",
    "prunes oldest entries once the size cap would be exceeded",
    "returns nothing to prune when every entry fits under all three limits",
    "classifies non-JSON (pre-schema free-text) content as legacy, never v1",
    "classifies JSON without a matching schemaVersion as legacy",
    "classifies a well-formed CrashArtifactV1 body as v1",
    "lists newest-first by file mtime",
    "enforces the hard cap even when a much larger limit is requested",
    "returns an empty array for a nonexistent directory",
    "never deletes legacy artifacts regardless of age/count/size pressure",
    "prunes v1 artifacts beyond maxCount, keeping the newest",
    "never throws even when the target path cannot be read as a directory (never-mask)",
    "never throws when a pruned file vanishes before unlink races it (never-mask)",
    "prunes down to maxCount after repeated fatals while still exiting(1) every time"
   ],
   "cli/cred-resolution.test.ts": [
    "resolves deepseek from a .deck DECKENT_DEEPSEEK_API_KEY with no env var set",
    "resolves qwen from a .deck DECKENT_DASHSCOPE_API_KEY with no env var set",
    "resolves glm from a .deck DECKENT_ZHIPU_API_KEY with no env var set",
    ".deck secret takes precedence over env when both are set (ADR-G-005)",
    "still falls back to the bare canonical env var when .deck has no key (regression)",
    "still refuses when neither .deck nor env has a key (detail unchanged)",
    ".stream() strips GEMINI_API_KEY/GOOGLE_API_KEY/DECKENT_GOOGLE_API_KEY from the gemini spawn env",
    "--model-override .send() strips GEMINI_API_KEY/GOOGLE_API_KEY/DECKENT_GOOGLE_API_KEY from the gemini spawn env",
    "claude spawn env is unaffected by the gemini-key stripping (existing providers not broken)"
   ],
   "cli/cu-status.test.ts": [
    "reports disabled + honest reason when config is entirely absent (no probing needed)",
    "reports enabled + full capability matrix when flag on and prober says everything present",
    "reports each capability unavailable with a reason when the prober finds nothing",
    "marks an unknown platform honestly unavailable without probing",
    "applies reasonOverride in place of the default disabled reason (config-load-error path)",
    "grants exactly the allowlisted subset (darwin, click+type only)",
    "returns one of the 4 known platform ids or null on this host",
    "never throws and returns a boolean for a real probe (used only as a smoke — not part of goCriteria)",
    "serializes the full report shape",
    "renders the honest disabled + how-to-enable hint (en)",
    "renders the Turkish disabled hint",
    "renders all 4 capability kinds when enabled",
    "surfaces the unsupported-platform warning line",
    "--json reports an honest disabled status when computer_use is absent from config",
    "--json reports full capability matrix when computer_use is enabled",
    "renders a human table (non-JSON) by default",
    "degrades to an honest config-load-error reason (never throws, never non-zero exit)",
    "renders the Turkish header when getLangFromConfig returns tr",
    "registers a working `cu-status --json` command"
   ],
   "cli/dashboard-dir.test.ts": [
    "resolves to the package dist/dashboard from a dist/cli/helpers module url",
    "is independent of the user project cwd (installed-package safe)"
   ],
   "cli/dead-listener-migration.test.ts": [
    "registers a shutdown hook instead of process.on(SIGINT)",
    "the registered hook unwatches the history file — real cleanup, idempotent",
    "non-follow `nervous log` registers no hook (unchanged, non-daemon path)",
    "registers a shutdown hook instead of process.on(SIGINT/SIGTERM)",
    "the registered hook kills a still-alive child; detach() unregisters the hook (normal-exit path)",
    "the hook is a no-op once the child already exited (idempotent guard preserved)",
    "registers a shutdown hook instead of process.on(SIGINT)",
    "the registered hook stops the runtime — real cleanup, idempotent (FlowRuntime.stop() self-guards)",
    "`flow run --once` registers no hook (single tick, no daemon loop)",
    "registers a shutdown hook instead of process.on(SIGINT/SIGTERM)",
    "the registered hook stops the daemon — real cleanup, idempotent (HeartbeatDaemon.stop() self-guards)",
    "registers a shutdown hook instead of process.on(SIGINT/SIGTERM)",
    "the registered hook closes the watcher + clears the fallback timer — real cleanup, idempotent"
   ],
   "cli/debt-counter.test.ts": [
    "returns zero for empty DB",
    "counts all debt entries",
    "counts critical debt entries",
    "returns zero when DB file does not exist",
    "returns 0 for empty DB",
    "filters out resolved entries",
    "filters out closed entries",
    "returns 0 when DB file does not exist",
    "returns correct count with mixed statuses"
   ],
   "cli/deckent-bash-timeout-platform.test.ts": [
    "posix (linux/darwin) → bash -lc <cmd>, never a Windows shell",
    "win32 (native, no WSL/Git-Bash) → PowerShell, never the hardcoded bash",
    "defaults to process.platform when no platform arg is given",
    "a hanging command (never closes) is killed and the turn recovers instead of hanging forever",
    "kills the WHOLE posix process group (not just the bash pid) so a hung pipeline leaves no orphans",
    "win32 timeout uses a plain child kill — no POSIX group-signal attempt",
    "a command finishing within budget resolves normally and is never killed",
    "a spawn-level error resolves immediately as [mcp-error] — no hang until the timeout fires",
    "spawns with detached:true on posix (group-killable) and detached:false on win32",
    "uses the DEFAULT_BASH_TIMEOUT_MS (5min) budget when no timeoutMs is given"
   ],
   "cli/default-repl.test.ts": [
    "returns true when argv has no subcommand and no flags (bare `deckent`)",
    "returns false for every explicit top-level subcommand",
    "returns false for an unknown subcommand so Commander can show its error",
    "returns false for --help / -h / help token (help UX preserved)",
    "returns false for --version / -V / --version-json (version UX preserved)",
    "is a pure function — result depends only on argv, not on TTY/runtime state",
    "rewrites bare argv to `chat --native` while preserving argv[0]/[1]",
    "passes through argv unchanged when a subcommand is present",
    "passes through argv unchanged when --help is present",
    "returns a fresh array so callers cannot mutate the input",
    "falls back to safe defaults when argv lacks argv[0]/[1] (graceful edge case)"
   ],
   "cli/denied-tool-honesty.test.ts": [
    "pre-fix bug: the pre-gate early-return used to skip toolSink entirely — the denied call vanished from the transcript with no model-visible marker",
    "regression guard: an APPROVED confirm-tier call runs the real dispatcher and never touches toolSink (CLI-bridge tools are not in the toolInfoFor switch — pinned no-sink-on-approve behavior)",
    "pre-fix bug: the ALWAYS_CONFIRM branch shares the same early-return — a denied deckent_kill also used to vanish silently",
    "regression guard: an APPROVED always-tier call is asked every time (no memoized skip) and runs for real",
    "pre-fix bug context (regression pin, not a masked path itself): the [deckent-denied] branch already reached toolSink before this task — this pins it stays that way and uses the identical cancelled-label convention as the newly-fixed CLI-bridge path above",
    "regression guard: an approved EXEC_TOOLS write reports the real ToolInfo change block (verb/target/added), not a failed record",
    "regression guard: an mcp-error is reported failed:true with the raw error text as verb (distinct from a denial)",
    "regression guard: no askConfirm call, real dispatcher result returned, no sink record",
    "regression guard: a denial with no sink registered still returns the honest cancelled marker"
   ],
   "cli/deprecated-alias-forwarding.test.ts": [
    "defines one non-empty, single-line typed warning for every deprecated command"
   ],
   "cli/describe-tool-params.test.ts": [
    "reports non-empty params for a bridged tool with a real object inputSchema",
    "marks non-required fields optional and preserves declared field types",
    "a native meta-tool with a declared string+number schema also reports real params",
    "does not fabricate fields for a schema-less bridged tool (empty properties stays empty)"
   ],
   "cli/desktop-floor-message.test.ts": [
    "the manifest declares an engines.node floor",
    "desktop.error.node_not_found (en) interpolates the live manifest floor",
    "desktop.error.node_not_found (tr) interpolates the live manifest floor",
    "error.node_version_low (en) interpolates the live manifest floor, not a source literal",
    "error.node_version_low (tr) interpolates the live manifest floor, not a source literal",
    "a caller-supplied floor still overrides the manifest default",
    "no message key matching /node/i carries a hardcoded major-version literal"
   ],
   "cli/detached-start-terminal-result.test.ts": [
    "reports %s as a non-success outcome",
    "reports completion instead of pretending execution just started",
    "leaves an unsettled child to the admission/liveness checks"
   ],
   "cli/detached-start.test.ts": [
    "spawns node with the resolved entry.js + argv, detached + windowsHide, stdio piped to the log fd",
    "returns a log path under the owned detached runtime-log namespace",
    "sanitizes an unsafe argv[0] into the log filename",
    "returns pid: null when the child handle reports no pid",
    "defaults projectRoot to process.cwd() when omitted",
    "liveTrace: true → child env carries DECKENT_LIVE_TRACE=1 (interactive-origin marker)",
    "resolved session language is exported to the detached child",
    "liveTrace omitted → env twin ABSENT (programmatic default pinned: SDK/MCP spawns stay off)",
    "spawns the canonical exact-start argv, carries the live-trace twin, and returns the child pid",
    "is true for start / run / process submit",
    "is false for status, config, kill, and process status/result",
    "deckent_start → routes through spawnDetachedFn, never touches the synchronous spawnFn",
    "deckent_run → routes through spawnDetachedFn with the resolved run argv",
    "deckent_process submit → routes through spawnDetachedFn",
    "deckent_process status → stays on the synchronous spawnFn path (read-only poll)",
    "deckent_status → unaffected, still routes through the synchronous spawnFn (goCriteria: sync path unchanged)",
    "deckent_run without a description → tool-not-allowed, no spawn at all",
    "detachedLabels override replaces the English defaults",
    "spawnDetachedFn throw → tagged mcp-error, never throws out of dispatch"
   ],
   "cli/do-cmd.test.ts": [
    "registers the canonical planning and execution options",
    "rejects whitespace-only goals through the localized catalog before planning"
   ],
   "cli/do-real-plan.test.ts": [
    "drives the fake-planner plan through the REAL compileRunProposal/buildDirectives chain, then approves + starts via the real RunFlow services (runSprint mock)",
    "still builds the real fake-planner multi-task plan, honestly shows GATE: FAIL / POLICY: NEEDS APPROVAL, and rejects without --yes"
   ],
   "cli/do-runflow-adapter.test.ts": [
    "registers a --yes option alongside --run",
    "holds provider authority before controller/planner work",
    "confirms, approves with the real local actor, and awaits the exact snapshot in-process",
    "explicit false preserves decline without approving or executing",
    "holds configured provider authority before bootstrap/controller/planner work",
    "prints the real RunFlow preview and exact continuation command without approving, starting or replanning",
    "threads --write-allowlist into the digest-bound exact plan authority",
    "binds a force-scope acknowledged greenfield allowlist entry into the preview",
    "applies --write-allowlist on the flag-off canonical path",
    "rejects non-interactively (no prompt) and never calls approve/startApproved/spawnStart",
    "drives propose -> approve -> startApproved through the REAL services and prints a rich started result",
    "reports a controller error via runFlow.mount.error and sets exitCode=1, without throwing",
    "renders the --run banner with the real task count and task summary lines",
    "renders the dry-run banner when run=false",
    "renders the no-tasks placeholder when taskSummaries is empty",
    "genuinely localizes to Turkish (en !== tr)",
    "the runDoRunFlow function body never references swapDirectives/restoreDirectives/defaultSpawnStart"
   ],
   "cli/docs-track.test.ts": [
    "scans the repo, writes front-matter, persists to memory.db",
    "ok=true when no critical-stale docs",
    "ok=false and lists violations when a CRITICAL_STALE doc exists"
   ],
   "cli/doctor-auth-probe.test.ts": [
    "probes only available claude/codex/gemini and skips unavailable + unknown providers",
    "runs every target probe (parallel Promise.all) and aggregates all results",
    "degrades a throwing probe to state \"unknown\" (never rejects)",
    "passes a short timeout to each probe so doctor never stalls",
    "returns an empty map when no providers are probeable",
    "downgrades an adapter-inferred Claude session when the local probe says logged-out",
    "preserves confirmed Codex subscription semantics without calling it an API key",
    "keeps unknown and configured-but-unverified API-key auth fail-closed",
    "does not invent cloud auth evidence for Ollama while bounding its models to catalog-only",
    "projects only logged-out and unknown cloud evidence into optional warnings",
    "downgrades a logged-out provider to [WARN] with login command (claude)",
    "uses the codex login command for a logged-out codex",
    "uses the gemini login command for a logged-out gemini",
    "reports a confirmed session when the probe says logged-in",
    "reports [WARN] when the probe state is \"unknown\" instead of guessing ready",
    "does not mislabel a confirmed codex subscription session as an API key",
    "is a no-op when no authProbes map is supplied (backward compatible)",
    "localizes the logged-out line to Turkish when lang=tr",
    "propagates auth failure into top summary, readiness, recommendation, and honest summary",
    "preserves the [PASS] line when no authProbes are supplied",
    "renders confirmed codex subscription truth consistently in the top and health sections"
   ],
   "cli/doctor-backend-aware.test.ts": [
    "win32 + docker backend → PASS, no misdiagnosis (Win+docker fix)",
    "win32 + subprocess backend → PASS, honest line",
    "win32 + no configured backend (undefined) → unchanged legacy FAIL (regression guard)",
    "win32 + explicit tmux backend → still FAIL (tmux genuinely unsupported natively)",
    "win32 + auto backend → conservative legacy FAIL (unresolved at this layer)",
    "linux + docker backend → unaffected, still PASS \"Linux\"",
    "linux + subprocess backend → unaffected, still PASS \"Linux\"",
    "darwin + docker backend → unaffected, still PASS \"macOS\"",
    "win32 + docker backend end-to-end → Platform entry passed=true",
    "win32 + subprocess backend end-to-end → Platform entry passed=true",
    "win32 + no backend configured end-to-end → Platform entry passed=false (unchanged)",
    "linux + docker backend end-to-end → Platform entry unaffected (passed=true, Linux)"
   ],
   "cli/doctor-checks.test.ts": [
    "returns true when WSL_DISTRO_NAME is set",
    "returns true when /proc/version contains \"microsoft\"",
    "returns false when no WSL indicators",
    "returns passed=true for darwin",
    "returns passed=false for win32",
    "returns untested for unknown platform",
    "win32 + docker backend passes (backend-aware, not a blanket UNSUPPORTED)",
    "win32 + subprocess backend passes (backend-aware)",
    "win32 with no backend override still fails (genuine platform incompatibility, not a config choice)",
    "not required for docker backend",
    "not required for subprocess backend",
    "required when claude provider and tmux missing",
    "not required for non-claude providers",
    "is i18n\\'d via getMessage for the \"not required\" reason (lang param, born-651)",
    "returns passed=false when not installed",
    "returns passed=true when installed",
    "checks auth when checkAuth=true",
    "passes when docker not installed and not wanted",
    "fails when docker wanted but not available",
    "passes when docker available",
    "passes when all files gitignored and not tracked",
    "fails when .gitignore missing",
    "fails when file is tracked",
    "passes when directories writable",
    "fails when directory not writable",
    "passes when .deck not found",
    "fails when .deck tracked by git",
    "passes when .deck exists but not tracked",
    "returns 0 when db file missing",
    "returns count from MemoryStore",
    "returns sprint ID from config",
    "returns null when config missing",
    "returns null for invalid JSON",
    "returns zero when no DB file",
    "counts debt entries from MemoryStore",
    "returns 0 when no DB file",
    "filters resolved entries",
    "returns null when file missing",
    "returns parsed baseline",
    "returns empty array when brain dir missing",
    "returns null when no reports",
    "returns ok=true when all required pass",
    "returns checks array with 17 items",
    "returns ok=false when node missing",
    "reads debt from MemoryStore.getByType(\"debt\") (DB-first) — a CRITICAL entry fails the check",
    "uses DB-first \"open debt items\" wording, not the removed DEBT.md-parser\\'s \"debt items\" wording",
    "falls back to doctor checks when script fails"
   ],
   "cli/doctor-daemon-hygiene-wire.test.ts": [
    "surfaces the stale-daemon advisory section + kill hint for an injected stale snapshot",
    "shows a clean PASS line for an injected clean snapshot (no deckent daemons)",
    "never makes doctor exit non-zero even when stale daemons are found",
    "never throws / never kills when the process lister itself fails",
    "flags a long-lived deckent daemon as stale and renders advisory lines",
    "returns a clean PASS (no stale daemons) for a benign snapshot",
    "degrades to a benign PASS note on an unsupported platform (Yasa #2 — honest skip)",
    "never throws — a throwing lister degrades to a benign advisory note",
    "localizes the advisory to Turkish when lang=tr",
    "renders a single PASS line for an empty stale list",
    "renders a WARN count, a per-daemon entry, and a kill hint when stale daemons exist"
   ],
   "cli/doctor-dedup-505.test.ts": [
    "exactly one `function runPreFlightHealthCheck` definition exists across doctor.ts + doctor-checks.ts (grep disk-verify)",
    "doctor.ts re-exports rather than redefines runPreFlightHealthCheck (D-004-clean same-layer sibling import — both files live in src/cli/commands/)",
    "doctor.js re-exported runPreFlightHealthCheck(root) returns a real PreFlightResult against a real tmpdir",
    "produces the exact same result as calling doctor-checks.js directly (single implementation, not two that happen to agree)"
   ],
   "cli/doctor-fix.test.ts": [
    "is exactly mkdir/chmod/config-migrate/config-recreate/unlock — no docker/login, no delete of live data",
    "returns an empty list on a fully clean fixture",
    "proposes mkdir for a missing .deckent/ directory, without creating it",
    "proposes mkdir for a missing .tasks/ directory, without creating it",
    "proposes mkdir for BOTH missing dirs when neither exists",
    "proposes chmod for a stale-mode .deck-shadow, without touching its mode",
    "does NOT propose a chmod when .deck-shadow is already 0o600",
    "proposes config-migrate when config.json is missing default fields, without writing to it",
    "does NOT propose config-migrate when config.json does not exist",
    "proposes config-recreate (backup + rewrite) when config.json is corrupt JSON, without touching it",
    "does NOT propose config-recreate when config.json is valid JSON",
    "proposes unlock for a stale lock file, without deleting it",
    "does NOT propose unlock for a fresh (non-stale) lock file",
    "ignores .spawnlock files entirely (distinct namespace, never proposed for unlock)",
    "every planned action kind is a member of the closed whitelist",
    "creates missing .deckent/ and .tasks/ directories for real",
    "resets a stale .deck-shadow file to 0o600 for real",
    "applies migrateConfig for real, adding missing defaults and leaving a backup",
    "applies config-recreate for real: backs up the corrupt file and rewrites valid defaults",
    "applies unlock for real: removes the stale lock file",
    "captures a per-action failure without aborting the remaining actions",
    "never emits or applies a kind outside the closed whitelist",
    "reports \"nothing to repair\" for an empty action list",
    "lists each action with a \"would fix\" prefix in dry-run mode, and points at --yes",
    "lists each result with a \"fixed\" or \"FAILED\" prefix when results are supplied",
    "surfaces a FAILED line with the error message when a result did not apply",
    "renders a \"before:\" line for an action that carries a previousValue (dry-run)",
    "renders a \"before:\" line for an applied action that carries a previousValue",
    "does NOT render a \"before:\" line when previousValue is absent (backward-compat fixture shape)",
    "appends a \"Manual\" section listing non-auto-fixable checks, honestly labeled",
    "replaces \"nothing to repair\" with an honest \"manual attention needed\" line when actions is empty but manual is not",
    "still reports \"nothing to repair\" when BOTH actions and manual are empty",
    "renders the Turkish variant when lang=\"tr\" is passed"
   ],
   "cli/doctor-followups.test.ts": [
    "win32 + no override → honest platform-incompatibility label, NOT \"subprocess backend\"",
    "win32 + an override that is neither docker nor subprocess (e.g. \"tmux\") → still the platform label",
    "win32 + \"auto\" override → still the platform label (no fake config-preference reason)",
    "win32 + spawnBackend=\"subprocess\" → honest config-preference label",
    "win32 + spawnBackend=\"docker\" → honest config-preference label",
    "linux + spawnBackend=\"docker\" → unaffected, still \"docker backend\"",
    "linux + spawnBackend=\"subprocess\" → unaffected, still \"subprocess backend\"",
    "linux + no override + tmux missing → unaffected required-provider logic",
    "win32 + no override, lang=tr → localized Turkish platform label",
    "win32 + docker, lang=tr → localized Turkish docker-backend label",
    "win32 + subprocess, lang=tr → localized Turkish subprocess-backend label",
    "lang omitted → defaults to English (backward-compatible for existing callers)",
    "win32 + no override + lang=tr → tmux entry uses the honest Turkish platform label",
    "win32 + no override + lang omitted → defaults to English, still honest (not \"subprocess backend\")",
    "win32 + spawnBackend=\"subprocess\" + lang=en → tmux entry correctly labels the config choice"
   ],
   "cli/doctor-honest.test.ts": [
    "classifies every passing check as ready",
    "classifies a failing \"Workspace\" check as one-command-fix (deckent doctor --fix can repair it)",
    "classifies a failing check outside the --fix whitelist as missing",
    "counts missingCount as the combined total of missing + one-command-fix",
    "never classifies a passing check as anything other than ready, regardless of name",
    "renders an all-ready celebratory line when nothing is missing",
    "renders \"N ready · M missing (K fixed by `deckent doctor --fix`)\" when some are fixable",
    "renders \"N ready · M missing\" with no fix-hint parenthetical when none are fixable",
    "renders the Turkish variants with hazır/eksik/düzelir wording",
    "gives ready checks an empty explanation",
    "gives a known check name a non-technical, non-empty explanation",
    "appends a fixable-suffix mentioning `deckent doctor --fix` only for one-command-fix state",
    "falls back to a generic i18n template for an unknown/future check name (never breaks silently)",
    "includes the header and summary line, and one line per not-ready check only",
    "emits no per-check lines when everything is ready",
    "appends \"Honest Summary:\" as the final section of the human doctor output",
    "respects the Turkish lang option end-to-end through formatHumanDoctor"
   ],
   "cli/doctor-icon-consolidate.test.ts": [
    "returns [PASS] for pass",
    "returns [WARN] for warn",
    "returns [FAIL] for fail",
    "renders [PASS] for an available provider (not ✓)",
    "renders [WARN] for a partial provider (not ⚠)",
    "renders [FAIL] for a missing provider (not ✗)",
    "still renders [PASS] for a healthy provider",
    "still renders [WARN] for an unavailable provider",
    "has no stray Unicode status symbols anywhere in the output",
    "renders a single [PASS] line when ready",
    "renders a [WARN] line when not ready",
    "has no stray Unicode status symbols anywhere in the output"
   ],
   "cli/doctor-image-check.test.ts": [
    "renders a single [PASS] line when the image is ready (no warnings)",
    "renders a [WARN] line with the suggested build command when missing",
    "lists missing CLIs and ca-certificates for a stale image",
    "names missing exact runtime authority for a stale image",
    "names missing image-owned dependency authority for a stale image",
    "localizes the WARN lines to Turkish when lang=tr",
    "includes the Worker Image section when a report is supplied",
    "omits the Worker Image section entirely when no report is supplied (no regression)",
    "builds (spawn called) when --fix-image is on, image not ready, and user confirms",
    "does NOT spawn when the user declines the confirmation",
    "does NOT prompt or spawn when --fix-image is absent (enabled=false)",
    "does nothing when the image is already ready (no build even if enabled)",
    "reports build-failed when the build process exits non-zero",
    "declines (no spawn) under tr locale as well"
   ],
   "cli/doctor-memory-v2.test.ts": [
    "passes when .brain/memory.db + .brain/exports/decisions.md exist (no DECISIONS.md)",
    "passes when only legacy .brain/DECISIONS.md exists (no memory.db, no exports/)"
   ],
   "cli/doctor-platform-auth.test.ts": [
    "win32: label is \"Windows (native)\" and 3 adapted checks are honestly disclosed",
    "win32: adapted-check notes are localized to Turkish",
    "linux (no WSL signal): isWSL=false, no adapted checks, honest empty disclosure",
    "linux + WSL_DISTRO_NAME env var: isWSL=true, WSL label",
    "linux + /proc/version microsoft signature: isWSL=true",
    "darwin: macOS label, no adapted checks",
    "unknown platform: honest \"untested\" label naming the actual platform value",
    "renders header + platform/label line",
    "win32: renders the adapted-checks disclosure with all 3 entries",
    "non-win32: no adapted-checks disclosure section (nothing to silently hide)",
    "localizes the header to Turkish",
    "env var present (native SDK key) → connected",
    "env var present (deckent alias key) → connected",
    "gemini via GOOGLE_API_KEY → connected",
    "no env + no .deck file → missing for all 3 default providers",
    "no env + .deck file with a matching key → connected via deck source, others missing",
    "empty-string env var does NOT count as connected (falls through to .deck/missing)",
    "an unsupported provider name → unknown (honest, never guessed — same convention as probeProviderAuth)",
    "env var short-circuits before the .deck lookup even when unrelated providers are unknown",
    "renders one localized line per provider (en)",
    "renders localized Turkish states"
   ],
   "cli/doctor-preflight-honesty.test.ts": [
    "reports honestly instead of silently falling back to generic doctor checks",
    "does NOT silently substitute runDoctorChecks output (would be 15 unrelated checks)",
    "does not block the sprint on a dev-only diagnostic being unavailable",
    "never spawns a subprocess when the script is absent (no blind spawnSync attempt)",
    "also reports honestly when scripts/ exists but the specific file does not",
    "delegates to the real pre-flight script instead of the honest-fallback"
   ],
   "cli/doctor-preflight-single.test.ts": [
    "only ONE `export function runPreFlightHealthCheck` exists across doctor.ts + doctor-checks.ts",
    "doctor.ts imports (delegates to) runPreFlightHealthCheck from doctor-checks.js instead of redefining it",
    "doctor-checks.ts source also has a single PreFlightResult/PreFlightCheckResult interface pair (no duplicate types either)",
    "doctor.ts and doctor-checks.ts export the exact same function reference"
   ],
   "cli/doctor-profile.test.ts": [
    "contains CPU, RAM, and Workers info",
    "includes subscription when provided",
    "omits subscription line when not provided",
    "uses box-drawing characters",
    "shows System Profile title",
    "correctly converts MB to GB",
    "--profile flag is registered in commander",
    "without --profile: system profile not shown",
    "with --profile: system profile is shown",
    "with --profile: subscription info is shown",
    "with --profile: normal doctor checks still appear",
    "without --profile: normal doctor output is unchanged",
    "includeProfile=true → systemProfile field present",
    "includeProfile=false → no systemProfile field",
    "includeProfile omitted → no systemProfile field (defaults to false)",
    "includeProfile=true → systemProfile.subscription matches detected value",
    "existing fields (ok, checks) are preserved with includeProfile=true",
    "existing fields (ok, checks) are preserved with includeProfile=false"
   ],
   "cli/doctor-provider-authority.test.ts": [
    "absent → failed required check whose remedy names the authoring flow, not keyring init",
    "authored-empty is distinguished from absent — its own message, still failed",
    "present → passes with the policy count visible",
    "remedy strings live in the message authority with both languages distinct",
    "the keyring remedy no longer claims to fix the limit-authority hold"
   ],
   "cli/doctor-providers.test.ts": [
    "returns one ProviderAvailabilityDetail per built-in provider",
    "reflects partial state for codex when binary OK but no auth",
    "formatProviderDiagnostics produces a human-readable table with OK/PARTIAL/MISSING markers"
   ],
   "cli/doctor-ram-experiment.test.ts": [
    "parses \"2g\" as 2 GB",
    "parses \"3g\" as 3 GB",
    "parses \"512m\" as ~0.5 GB",
    "returns 2 for undefined (default)",
    "returns 2 for invalid string",
    "parses \"1gb\" case-insensitively",
    "host 24GB + 6 workers × 2g → Safe (requires 14 GB)",
    "host exactly meets requirement → Safe",
    "host 12GB + 6 workers × 2g → Risky (requires 14 GB)",
    "host 8GB + 6 workers × 2g → Risky",
    "host 0 GB (detect failure) → Cannot determine",
    "returns all required fields",
    "2-worker × 3g scenario peak = 6 + 2 = 8 GB",
    "Safe report contains ✓ and Safe",
    "Risky report contains ⚠ and Risky",
    "Cannot determine report contains ? symbol"
   ],
   "cli/doctor-resources.test.ts": [
    "renders the header line",
    "includes memory limit, swap and max workers in limits line",
    "calculates RAM ceiling correctly (6 workers × 4g = 24 GB)",
    "shows warning when RAM ceiling exceeds 60% of host",
    "does NOT show warning when RAM ceiling is under 60% of host",
    "shows resource_monitor enabled line with interval",
    "shows resource_monitor disabled line when enabled=false",
    "omits resource_monitor line when resourceMonitor is undefined",
    "renders Turkish translation (tr lang)",
    "Turkish warn line uses correct percent format",
    "handles zero hostTotalBytes gracefully (no warn, pct=0)",
    "uses default interval_ms=5000 when not specified in resource_monitor"
   ],
   "cli/doctor-routing-journal.test.ts": [
    "flags the legacy decisions-v3 rename leftover",
    "reads the newest journal and reports corrupted lines"
   ],
   "cli/doctor-subprocess-secret-warn.test.ts": [
    "non-subprocess backend (docker) → silent PASS even with a fully populated .deck",
    "non-subprocess backend (undefined / not configured) → silent PASS",
    "subprocess backend + no .deck file → silent PASS (false-positive guard)",
    "subprocess backend + .deck exists but is a template (all empty values) → silent PASS (false-positive guard)",
    "subprocess backend + .deck exists with at least one non-empty secret → WARN (passed:false, required:false)",
    "WARN message never leaks the .deck key name or secret value (no content disclosure)",
    "is i18n\\'d via getMessage — en default text",
    "is i18n\\'d via getMessage — tr text differs from en for the same WARN case",
    "treats a whitespace-only value as empty (false-positive guard — not a real secret)",
    "end-to-end: subprocess backend + populated .deck → the new check appears and WARNs",
    "end-to-end: docker backend + populated .deck → the new check appears and stays silent-pass"
   ],
   "cli/doctor-twin-dedup.test.ts": [
    "doctor.ts has no local `runDoctorChecks` check-list construction",
    "only ONE `function runDoctorChecks(` definition exists across doctor.ts + doctor-checks.ts",
    "doctor.ts imports+re-exports runDoctorChecks from doctor-checks.js instead of redefining it",
    "doctor.ts has no local `function %s(` body (imported + re-exported from doctor-checks.ts instead)",
    "only ONE `function %s(` definition exists across doctor.ts + doctor-checks.ts",
    "doctor.ts and doctor-checks.ts export the exact same runDoctorChecks function reference",
    "%s is re-exported from doctor.ts as the SAME reference as doctor-checks.ts (not a re-implemented twin)",
    "411-002 regression lock: a check that lives only in doctor-checks.ts\\'s array (.deck Subprocess Visibility) is visible through doctor.ts\\'s exported runDoctorChecks — the two check-lists can no longer silently diverge"
   ],
   "cli/doctor-ux.test.ts": [
    "uses green [PASS] for passing checks",
    "uses red [FAIL] for required failing checks",
    "uses yellow [WARN] for non-required failing checks",
    "summary shows fail count in red",
    "summary shows all passed in green",
    "tmux failure message includes install suggestion",
    "claude failure message includes npm install",
    "node version failure message includes upgrade suggestion",
    "multiple checks are all listed",
    "reset codes appear after colors",
    "handles empty checks list",
    "handles all failing checks",
    "handles mix of required and optional failures",
    "long check names are handled without crashing"
   ],
   "cli/doctor-windows-profile.test.ts": [
    "does NOT plan a chmod action for a stale-mode .deck-shadow on win32 (NTFS has no POSIX bits)",
    "still plans the chmod action on POSIX — no regression vs. the pre-existing behavior",
    "win32 suppresses chmod even for a very-loose 0o666 mode; darwin (POSIX-like) still proposes it",
    "other action kinds (mkdir) are unaffected by the platform override",
    "omitting platformOverride falls back to the real (mocked) platform() — win32 still suppresses chmod",
    "does not propose a chmod when .deck-shadow does not exist, on any platform",
    "win32 + .deck-shadow present → one honest manual item reusing doctor.platform_adapt_permissions (en)",
    "win32 + .deck-shadow present → localized Turkish message, distinct from EN",
    "win32 + .deck-shadow absent → empty (nothing to caveat)",
    "non-win32 → always empty, regardless of .deck-shadow presence",
    "omitting platformOverride falls back to the real (mocked) platform()",
    "win32: shows the Platform Profile disclosure + an honest .deck-shadow manual caveat, and plans NO chmod action",
    "linux: no Platform Profile section is shown, and the chmod action IS planned as [would fix] — POSIX behavior unchanged",
    "win32 --json: platformProfile is included, chmod is absent from actions, and the manual caveat is present",
    "linux --json: platformProfile.adaptedChecks is empty — no Windows caveats leak onto POSIX",
    "omitting authProbes reproduces the exact legacy shape (sessionState/conflict undefined)",
    "populates source=\"env\" when connected via a native SDK env var",
    "populates source=\"deck\" when connected via the .deck file only",
    "source is \"none\" when missing",
    "cross-references a supplied authProbes: connected config + logged-out real session → conflict=true",
    "cross-references a supplied authProbes: missing config + logged-in real session → conflict=true (config missed a working credential)",
    "agreeing config + session states → conflict=false (not true, not undefined)",
    "never invents a conflict verdict when the real probe itself says \"unknown\" (Law #2 — never guess)",
    "an authProbes entry for an unrelated provider does not affect other providers",
    "bare fixtures (no source/sessionState/conflict) render byte-identically to the legacy 3-state output",
    "appends a provenance suffix when source is env/deck",
    "omits the provenance suffix when source is \"none\"",
    "appends a conflict note only when conflict is true",
    "renders no conflict note when conflict is false"
   ],
   "cli/dpp-provision.test.ts": [
    "maybeProvisionDockerImage has no production call-site under src/",
    "reprovisionWorkerImageAfterUpgrade has no production call-site under src/",
    "the only doc-comment mention of either helper lives in init.ts, describing them as unwired siblings",
    "maybeProvisionDockerImage carries a disk-verified deprecation marker recording the 0-caller evidence"
   ],
   "cli/dual-stream.test.ts": [
    "returns both regions, approval first, when everything fits",
    "returns [] when height is 0",
    "returns [] when both inputs are empty, regardless of height",
    "shows full status when approval is empty and it fits",
    "shows full status up to height with no approval competing, cropping only on true overflow",
    "crops status with the overflow marker when status alone exceeds height",
    "reserves 1 status line even when approval wants all remaining room",
    "gives 1 real line to each region rather than a bare marker when both are squeezed to 1 row",
    "documented edge case: height=1 with both non-empty gives the single row to status",
    "approval gets the single row when status is empty",
    "fits within height=${height} width=${width}",
    "truncates individual lines to width with an ellipsis",
    "truncates to a single character at width=1",
    "normalizes non-finite dimensions without unbounded allocation: $width × $height",
    "returns byte-identical output for identical input across repeated calls",
    "uses the injected overflow label instead of the English default",
    "fails closed only when overflow is actually needed and the caller omitted the marker",
    "crops the approval region with a marker when it gets 2+ rows but still overflows",
    "direct clip normalizes width=%s to one finite cell",
    "%s truncation stays within cells and never splits a grapheme",
    "uses the caller-selected ASCII marker within the same cell budget",
    "renders marker-only when a %s grapheme cannot fit before the width-4 ASCII marker",
    "keeps a wide grapheme atomic at width=%i",
    "does not split ANSI sequences while truncating visible cells",
    "closes an OSC 8 link using its %s terminator before the overflow marker",
    "orders OSC 8 close before marker and SGR reset for combined styled links",
    "gives status extra rows when approval needs less than its reserved share"
   ],
   "cli/edit-file-unique.test.ts": [
    "single match → replaces normally",
    "multiple matches without replaceAll → [mcp-error], file unchanged",
    "multiple matches + replaceAll:true → all occurrences replaced",
    "multiple matches + replace_all:true (snake_case) → all occurrences replaced",
    "empty old_string → explicit [mcp-error], file unchanged",
    "zero matches → existing \"no match\" error still fires (regression guard)",
    "replaceAll on a single match behaves the same as a normal replace"
   ],
   "cli/enterprise-bridge-timeout.test.ts": [
    "gives audit (provider-backed gate, documented 30-60s+) a budget past the 30s default",
    "keeps the conservative 30s default for the short enterprise reads",
    "falls back to the default for an empty argv",
    "a hung short command (cost) is killed at the 30s budget and returns a tagged notice (never rejects)",
    "a bare /audit that legitimately exceeds 30s is NOT killed and completes normally",
    "the long-running budget is still finite — an audit that never closes is eventually killed",
    "a spawn-level error (ENOENT) settles immediately with a tagged notice — no hang until timeout",
    "a normal close well within budget resolves with trimmed output and clears the timer"
   ],
   "cli/entry-ndjson-fallback.test.ts": [
    "surfaces a failed result event (is_error: true) instead of silently dropping it",
    "falls back to subtype when the failed result has no result text",
    "never crashes/throws on a failed result event mid-stream",
    "regression: a successful result event is still silently skipped (unchanged)",
    "regression: a result event with is_error: false is treated as successful (unchanged)",
    "regression: system events are still silently skipped (unchanged)",
    "regression: malformed JSON lines still fall back to raw passthrough (unchanged)",
    "regression: assistant deltas are yielded exactly as before, unaffected by the new branch",
    "surfaces a failed result event even when split across chunks with no trailing newline",
    "extracts the result text from a failed result event",
    "falls back to subtype when result text is absent",
    "falls back to a generic message when neither result nor subtype is present",
    "returns undefined for a successful result event",
    "returns undefined for a result event with is_error: false",
    "returns undefined for a result event missing the is_error field entirely",
    "returns undefined for non-result event types (system / assistant)",
    "returns undefined for non-JSON / empty / malformed lines (never throws)",
    "does not affect extractClaudeStreamDelta — both parsers agree the line is non-assistant"
   ],
   "cli/entry-provider-ssot.test.ts": [
    "claude routes .send() through resolveChatAdapter and returns real text",
    "codex routes .send() through resolveChatAdapter with exec --full-auto args",
    "gemini routes .send() through resolveChatAdapter with -p flag",
    "claude .stream() still yields NDJSON per-token deltas after the SSOT-delegated refactor",
    "happy path delegates to resolveChatAdapter and returns its text",
    "HTTP-status error delegates to resolveChatAdapter and surfaces its message verbatim (now includes host)",
    "network failure still calls resolveChatAdapter but rewraps the error with the Turkish \"erişilemedi\" hint",
    "opts.model override bypasses resolveChatAdapter and injects --model locally",
    "the default persistent-session path (no spawnFn) never touches resolveChatAdapter",
    "deepseek/qwen/glm openai-compatible presets never touch resolveChatAdapter",
    "entry.ts imports resolveChatAdapter from the SSOT module"
   ],
   "cli/epipe-graceful.test.ts": [
    "removing the installed listener reproduces the pre-fix throw",
    "EPIPE on stdout exits silently (exit code 0), no throw → no crash-log path reached",
    "EPIPE on stderr exits silently (exit code 0), no throw → no crash-log path reached",
    "EOF on stdout (Windows equivalent, 410-002) exits silently, no throw",
    "EOF on stderr (Windows equivalent, 410-002) exits silently, no throw",
    "non-EPIPE stream error is re-thrown (existing crash-log behavior preserved)",
    "non-EPIPE stream error on stderr is re-thrown too",
    "EPIPE on stdout leaves .deckent/crashes uncreated",
    "EOF on stderr leaves .deckent/crashes uncreated"
   ],
   "cli/error-handler-redact.test.ts": [
    "redacts an sk-... API key from message and stack in stderr and crash-log",
    "redacts a Bearer token from message and stack in stderr and crash-log",
    "redacts an API_KEY=... assignment from message and stack in stderr and crash-log",
    "preserves ordinary non-sensitive error text unchanged",
    "keeps exit code and crash-log path unchanged"
   ],
   "cli/evolve-command.test.ts": [
    "report command prints no-data message when analyzedSprintCount is 0",
    "report command renders trend table with mock data",
    "report --json outputs valid JSON with analyzedSprintCount",
    "registerEvolve is wired in src/cli/index.ts"
   ],
   "cli/execution-authority.test.ts": [
    "is a machine-readable dry-run by default",
    "requires explicit operator and justification before apply",
    "forwards a bounded explicit attestation and emits the adopted DTO",
    "maps authority refusal to a localized error without leaking core text"
   ],
   "cli/f1005-ollama-image.test.ts": [
    "assets/Dockerfile.worker exists",
    "Dockerfile.worker contains INSTALL_OLLAMA build-arg (default-off)",
    "Dockerfile.worker contains conditional ollama install line",
    "ollama install is gated (not unconditional)",
    "worker image provides the extraction dependency required by Ollama",
    "existing INSTALL_CODEX and INSTALL_GEMINI patterns still present",
    "Cursor support does not replace or alter the adjacent Ollama gate",
    "declares and consumes the default-off INSTALL_CURSOR build argument",
    "installs Cursor into a non-root-readable prefix and verifies the binary"
   ],
   "cli/f11-014-codex-parity.test.ts": [
    "send(): spawns \"codex\" with \"exec\" and \"--full-auto\"",
    "stream(): spawns \"codex\" with the same \"exec --full-auto\" arg table",
    "renders a multi-role transcript via buildSubscriptionPrompt and passes it as the last spawn arg",
    "strips ANTHROPIC_API_KEY and DECKENT_CLAUDE_API_KEY from the codex spawn env",
    "send() propagates a rejected spawn `wait` instead of silently returning success",
    "stream() propagates a rejected spawn `wait` after yielding prior chunks (never swallowed)",
    "stream() never yields empty-string chunks (no fabricated content on empty stdout)",
    "passes model \"gpt-5\" through as \"--model gpt-5\" on the codex spawn args",
    "passes model \"gpt-5.5\" through as \"--model gpt-5.5\" on the codex spawn args",
    "composes the full codex arg table when a model override is set: exec --full-auto --model <id> <prompt>",
    "ignores an out-of-contract \"model\" field — the SSOT arg table never emits --model"
   ],
   "cli/f1df-pack.test.ts": [
    "package.json files[] includes \"assets\"",
    "assets/Dockerfile.worker exists",
    "assets/Dockerfile.worker contains required build-arg markers",
    "npm pack --dry-run lists assets/Dockerfile.worker"
   ],
   "cli/f9-002-discovery.test.ts": [
    "fake-server returning 3 tools → registry has 3 `server__tool` keys",
    "listSlashLines renders all 3 namespaced tool names after loadAndConnectAll",
    "listTools() returns 3 NamespacedTool entries after loadAndConnectAll",
    "skips a server that throws during connect — REPL stays usable",
    "returns an empty raw list and records the no-config observation when no servers are configured",
    "multiple servers → all tools namespaced independently",
    "registers tools under server__tool format and returns NamespacedTool[]",
    "reconnect (second connectAndRefresh) refreshes without duplicating entries",
    "skips connect when already connected",
    "registry is shared: loadAndConnectAll fills it, listSlashLines reads same state",
    "/mcp list output contains all 3 tool descriptions"
   ],
   "cli/f9-003-trust-gate.test.ts": [
    "\"%s\" → \"%s\"",
    "\"%s\" → \"confirm\"",
    "never returns \"always\" for any tool name",
    "is case-insensitive — LIST_USERS → \"read\"",
    "list_items tool → confirmFn NOT called, callTool called, audit outcome=ok",
    "get_config tool → confirmFn NOT called, result is stringified",
    "read-only tool throws → confirmFn NOT called, audit outcome=error, ok=false",
    "write_file + confirm=false → cancelled output, callTool NOT called, audit outcome=cancelled",
    "execute_command + confirm=false → callTool NOT called",
    "confirmFn receives correct McpConfirmAction shape for risky tool",
    "create_record + confirm=true → callTool called, audit outcome=ok",
    "classifyExternalTool never returns \"always\" for any external tool name",
    "dispatch: even an \"always\"-named namespaced external tool is confirmed, not always",
    "MCP_AUDIT_CHANNEL constant is defined and non-empty"
   ],
   "cli/finalize-attempt-custody.test.ts": [
    "does not convert a raw V2 result self-report into terminal authority",
    "ignores a forged public DONE when the Store discriminator reports exact custody without a current receipt"
   ],
   "cli/finalize-orphan-normal.test.ts": [
    "NORMAL finalize SIGTERMs an owned-and-alive external pid, clears the pid file, and prints the advisory",
    "does NOT self-signal when finalize runs IN the coordinator (recorded pid === process.pid)",
    "does NOT signal a recorded-but-dead pid",
    "does nothing when no pid file is recorded",
    "--force path is byte-for-byte: it still terminates the orphan exactly once (no double-fire from the new normal block)",
    "HOLDs finalize and preserves PID authority when termination is unverified",
    "HOLDs before terminal settlement when an in-progress worker cannot be contained",
    "--force settles a pre-dispatch PENDING sprint through the shared recovery operation",
    "does not print terminal success when shared force settlement fails"
   ],
   "cli/finalize-refinalize.test.ts": [
    "evaluationDecision empty + selfAssessment=DONE → counted as success (DONE), even with testsPassed=false",
    "evaluationDecision wins over selfAssessment (Brain decision is authoritative)",
    "non-terminal selfAssessment (TIMEOUT_WITH_WORK) falls back to evaluateResultSync re-grading",
    "missing result still evaluates to NO_GO",
    "missing result for a dependency-parked task remains DEFERRED",
    "legacy cascade-skip evidence is DEFERRED, not a worker NO_GO",
    "auto-detection ignores landing proposals and other task-prefixed JSON residue",
    "archived task is included in totals and its archived .result lifts the synthetic NO_GO",
    "id-dedupe: .tasks/ wins over archive on collision (tasks AND results)",
    "explicit --sprint filter resolves the archive even when .tasks/ is empty",
    "V1: second finalize does not re-count agent/skill uses (lastUsedInSprint pre-scan)",
    "V2: second finalize skips recordOutcome via the recentSprints marker",
    "jobs summary reports duration honestly: real when startedAt known, \"unknown\" when not",
    "applied exact replay is byte- and metadata-read-only with a live unrelated Brain WAL",
    "identity, logical digest, and post-seal policy drift HOLD before any authority write",
    "resumes staged and seal-only receipts without appending terminal events",
    "deferTerminalAuthority remains on the live lifecycle path",
    "stamps sprint-state COMPLETE/COMPLETE and clears the dead pid marker",
    "does NOT stamp a different sprint\\'s state file (sprintId mismatch guard)",
    "clears only the finalized sprint pause authority",
    "preserves a different sprint pause authority",
    "--force resumes an existing COMPLETE receipt with its exact flow identity instead of aborting it",
    "--force counts archived tasks in the fenced ABORTED receipt, preserves startedAt, stamps ABORTED state, clears pid",
    "a foreign sprint-state blocks the ABORTED lifecycle stamp with a typed hold (fail-closed), state preserved",
    "double CLI finalize --force is idempotent end-to-end and records no agent stats at all"
   ],
   "cli/finalize-rule-regen.test.ts": [
    "passes onRuleRegen callback to finalizeSprint",
    "invokes regenerateRules when onRuleRegen callback fires",
    "CUSTOM_TEMPLATE is an empty placeholder, not a copy of AUTO content",
    "mergeWithCustom regeneration is idempotent (Sprint 166 idempotency gate)"
   ],
   "cli/finalize-sprint-scope.test.ts": [
    "excludes results whose task record is outside the selected sprint"
   ],
   "cli/finalize-termination-truth.test.ts": [
    "returns \"killed\" (not boolean true) when the backend kill succeeds",
    "returns \"not-found\" (not boolean false) when the backend reports no such worker, and still prints the not-found message",
    "still throws (does not swallow into \"failed\") on a genuine backend error",
    "not-found settles as terminated (goal state reached), not failed",
    "a real kill failure still fails the sweep",
    "an unexpected throw from the kill callback is treated as failed",
    "mixed case: killed, not-found and failed are bucketed independently",
    "restores a not-found callback\\'s exitCode side effect but preserves it for a real failure",
    "proceeds (never HOLDs) when the only incomplete worker is already dead, and prints the truthful already-terminated line",
    "still HOLDs terminal settlement when a live worker cannot really be killed",
    "i18n: already-terminated message resolves to distinct en/tr text (no hardcoded strings, no missing translation)"
   ],
   "cli/flow-command.test.ts": [
    "prints empty message when no flows found",
    "prints table when flows exist",
    "filters by tenant when --tenant option is provided",
    "outputs JSON when --json flag is set",
    "adds a flow with valid cron and action",
    "uses custom tenant when --tenant option is provided",
    "reports error for invalid cron expression"
   ],
   "cli/flow-run-command.test.ts": [
    "calls tick once when --once flag is provided",
    "starts daemon when --once is not set",
    "prints no-flows message when registry is empty (--once)",
    "reports due flows + self-dispatch queueing when flows are due (--once)"
   ],
   "cli/flow-self-dispatch.test.ts": [
    "queues due flows for self-dispatch and persists the pending-approval queue",
    "no flows due → nothing queued, no file written, honest report",
    "accumulates across ticks (the persisted queue grows)"
   ],
   "cli/force-finalize-contract.test.ts": [
    "settles ABORTED with a truthful summary and promotes only resolved lineages",
    "never counts a DONE result that carries no host attribution as verified completion",
    "preserves settled task evidence in the canonical archive",
    "settles unresolved lineages as unresolved instead of promoting them",
    "fails closed when only unbound result evidence survives",
    "fails closed when a partially lost projection cannot bind every result",
    "reports nothing to settle when no evidence of any kind survives",
    "re-forcing an already-finalized sprint republishes the same terminal truth"
   ],
   "cli/gateway-pair-lifecycle.test.ts": [
    "approves through the canonical scoped transition and a second decision is visibly late",
    "renders expiry and never grants when approval arrives after the boundary",
    "lists only live pending records from the revisioned map",
    "scope mismatch returns a quarantined/HOLD message and preserves denial"
   ],
   "cli/gateway-pair.test.ts": [
    "approve moves a pending pairing onto the project allowlist",
    "approve by project NAME authorizes the canonical project PATH (router read key)",
    "list shows pending pairings and the empty case",
    "reject removes a pending pairing; unknown code reports unknown"
   ],
   "cli/gateway.test.ts": [
    "routes an unbound chat to /use guidance through the connector"
   ],
   "cli/gemini-parity-gated.test.ts": [
    "send(): spawns \"gemini\" with \"-p\"",
    "stream(): spawns \"gemini\" with the same \"-p\" arg table",
    "renders a multi-role transcript via buildSubscriptionPrompt and passes it as the last spawn arg",
    "strips ANTHROPIC_API_KEY and DECKENT_CLAUDE_API_KEY from the gemini spawn env (parity w/ claude/codex)",
    "strips GEMINI_API_KEY, GOOGLE_API_KEY and DECKENT_GOOGLE_API_KEY from the gemini spawn env (regression pin for the fix in this task)",
    "send() propagates a rejected spawn `wait` instead of silently returning success",
    "stream() propagates a rejected spawn `wait` after yielding prior chunks (never swallowed)",
    "stream() never yields empty-string chunks (no fabricated content on empty stdout)",
    "passes model \"gemini-2.5-pro\" through as \"--model gemini-2.5-pro\" on the gemini spawn args",
    "passes model \"gemini-2.5-flash\" through as \"--model gemini-2.5-flash\" on the gemini spawn args",
    "composes the full gemini arg table when a model override is set: -p --model <id> <prompt>",
    "ignores an out-of-contract \"model\" field — the SSOT arg table never emits --model",
    "send() returns real, non-empty text from the live gemini CLI"
   ],
   "cli/health-snapshot-live-provider.test.ts": [
    "composes health from the canonical native resolver result without another selection",
    "uses the actual legacy fallback health context after a native resolution failure",
    "consumes native boot evidence without re-selecting or re-probing auth",
    "keeps a failed explicit native selection unknown and skips auth",
    "an injected provider (entry.ts providerName, env override included) wins over config",
    "without an injected provider the config chain still resolves (other callers unchanged)",
    "claude/codex/gemini map brain_model through registry equivalence (unchanged)",
    "does not duplicate identical registry id/apiId labels",
    "a non-registry provider shows its configured native_model",
    "a non-registry provider without native_model is honestly unknown — never the claude brain_model",
    "every native error code has a native.boot.* row in en and tr, distinct from the switch row",
    "localizeNativeError(err, lang, \"boot\") uses the boot row; \"switch\" keeps the switch row",
    "run.tsx resolves and refuses native boot before resources; successful selection reaches health",
    "keeps a one-argument boot callback structurally compatible with the optional glyph argument"
   ],
   "cli/health-snapshot.test.ts": [
    "config load failure degrades provider/model/mode/auth to unknown — never throws",
    "config load timeout (never resolves) degrades to unknown within the snapshot budget",
    "resolved config with a valid provider — provider field is ok",
    "missing brain_model on the active mode degrades model to unknown while provider stays ok",
    "model label is sourced LIVE from modelRegistry — not a hardcoded literal",
    "non-registry-typed provider (ollama) shows native_model when configured, else an honest unknown",
    "logged-in maps to ok",
    "logged-out maps to warn (actionable, not a hard failure)",
    "probe throwing degrades to unknown, never propagates",
    "probe that never resolves times out to unknown within the snapshot budget",
    "unresolved provider skips the auth probe entirely",
    "zero configured servers is an honest warn, not an error",
    "configured servers resolve to ok with the real count",
    "a throwing mcp-config reader degrades to unknown",
    "no memory.db on disk degrades to unknown (real fs check, no injection)",
    "a real tmpdir memory.db is opened + counted via the default reader (real MemoryStore I/O)",
    "count above budget degrades status to warn",
    "resolves the active mode name when config is available",
    "renders localized segment labels differently for en vs tr",
    "includes the raw provider/model/cwd data regardless of language",
    "respects NO_COLOR — no ANSI escape codes when set",
    "emits ANSI color when FORCE_COLOR=1 (proves color path is reachable, contrasts NO_COLOR test)",
    "an unknown-status field renders the localized \"unknown\" word, not the raw label",
    "an unknown provider collapses the provider/model segment to just the localized \"unknown\" word",
    "uses ASCII owned separators while preserving raw provider, model, cwd, and injected warning bytes"
   ],
   "cli/help-dynamic-counts.test.ts": [
    "returns 0 agents when registry is empty (graceful)",
    "returns count matching actual agents in registry",
    "reflects additional agents added at runtime",
    "returns 0 skills when registry is empty (graceful)",
    "returns count matching actual skills in registry",
    "defaults to 0 when no override provided",
    "uses overrideToolCount when provided",
    "returns all three counts together",
    "formatHelp EN output contains no hardcoded count literals",
    "formatHelp TR output contains no hardcoded count literals",
    "HELP_CONTENT has no hardcoded count strings in static data"
   ],
   "cli/help-surface-wire.test.ts": [
    "every `en` heading row equals the exact string Commander emits",
    "the `en` built-in help labels equal Commander\\'s own description",
    "the `en` root footer is the exact literal index.ts used to inline",
    "the `en` version-flag descriptions are the exact previous literals",
    "applying `en` labels leaves a fixture program\\'s help output unchanged",
    "applying `en` labels leaves a SUBCOMMAND\\'s help output unchanged",
    "translates every known heading and passes unknown titles through",
    "renders Turkish headings in a fixture program\\'s help output",
    "renders the Turkish built-in --help description",
    "localizes SUBCOMMAND help too, including the Arguments heading",
    "never rewrites a non-help option description",
    "is idempotent — re-applying with a different language replaces cleanly",
    "collectCommandTree returns root first, then every descendant",
    "attachRootHelpFooter appends the footer to the ROOT only",
    "the real root program renders the localized footer",
    "the real root program describes its version flags from the catalog",
    "EVERY command in the real tree carries the localized help configuration",
    "the real English help output still contains Commander\\'s own headings",
    "the cli-common family is registered and non-empty",
    "every cli-common key lives in the reserved `cli.help.` namespace",
    "every key cli-help injects is a real bilingual (en+tr) catalog row",
    "the injected keys are exactly the cli-common family keys",
    "merging a family whose key already exists in the base THROWS",
    "merging two families that share a key THROWS and names the family",
    "a clean merge keeps base rows and adds family rows",
    "a real cli-common key resolves through getMessage() in both languages",
    "is on when DECKENT_NATIVE_AGENT=1",
    "is on when --native is passed",
    "is OFF by default (legacy path)"
   ],
   "cli/helpers/agent-templates.test.ts": [
    "includes project name and stack",
    "includes all commands",
    "includes AGENTS.md header",
    "references DECKENT.md",
    "includes sprint instructions",
    "includes project name and stack",
    "includes build and test commands",
    "does not include lint command",
    "includes GEMINI.md header",
    "includes YAML frontmatter",
    "includes description with project name",
    "includes project name and stack",
    "references DECKENT.md",
    "appends section to content without Deckent marker",
    "skips if Deckent section already exists",
    "uses correct separator when appending",
    "handles empty existing content"
   ],
   "cli/helpers/codex-config.test.ts": [
    "creates section from empty string",
    "appends section to existing TOML",
    "replaces existing deckent section",
    "replaces deckent section while preserving other sections",
    "does not duplicate on repeated calls",
    "handles deckent section at end of file",
    "creates project .codex/config.toml in project root",
    "creates .codex directory if missing",
    "preserves existing project config",
    "handles invalid existing file gracefully",
    "returns both global and project paths",
    "is idempotent — running twice produces same result"
   ],
   "cli/helpers/config-reader.test.ts": [
    "should return language from config",
    "should return \"en\" when config does not exist",
    "should return \"en\" on malformed JSON",
    "should return \"en\" when language field is missing",
    "should return \"en\" when readFileSync throws",
    "should fall through to the environment chain for an unsupported config language",
    "should be overridden by DECKENT_LANG env even when config is supported",
    "should read from PROJECT_CONFIG_PATH",
    "should return \"en\" for empty config object",
    "${file} should not have local readJsonSafe definition",
    "${file} should not have local readLanguage definition"
   ],
   "cli/helpers/cursor-config.test.ts": [
    "creates .cursor/mcp.json with deckent entry",
    "creates .cursor/rules/deckent.mdc",
    "returns correct paths",
    "creates .cursor directory if missing",
    "preserves existing mcpServers entries",
    "updates existing deckent entry in mcp.json",
    "is idempotent — running twice produces same mcp.json",
    "is idempotent — running twice produces same rules file",
    "preserves owner-authored Cursor rules and adds only the Deckent reference",
    "handles invalid JSON in mcp.json gracefully",
    "handles mcpServers being a non-object in mcp.json",
    "rules file contains all required sections"
   ],
   "cli/helpers/error-handler.test.ts": [
    "handles DeckentError with human context (noColor)",
    "handles DeckentError with color (default)",
    "handles DeckentError without human context (legacy format)",
    "handles DeckentError legacy format with docLink",
    "handles generic Error",
    "handles non-Error values",
    "shows stack trace when verbose",
    "does not show stack when not verbose",
    "prints error code and message for DeckentError",
    "prints suggestion when available",
    "prints docLink when available",
    "does not print suggestion when not provided",
    "prints stack trace in verbose mode",
    "does not print stack trace without verbose",
    "prints error message",
    "prints report URL",
    "prints stack in verbose mode",
    "handles string thrown as error",
    "handles number thrown as error",
    "writes FATAL line with error name and message",
    "includes stack trace when DECKENT_DEBUG=1",
    "omits stack trace when DECKENT_DEBUG unset",
    "writes a crash log under .deckent/crashes/",
    "handles non-Error thrown values",
    "skips installation in vitest environment by default",
    "installs handlers when force=true and is idempotent on repeat",
    "reset helper removes both listeners"
   ],
   "cli/helpers/exact-sprint-runtime.test.ts": [
    "projects settled completion through the real coordinator and keeps duplicate replay terminal",
    "denies a caller whose causation does not bind the approved plan lineage"
   ],
   "cli/helpers/gemini-config.test.ts": [
    "creates ~/.gemini/settings.json when missing",
    "returns correct settings path",
    "creates .gemini directory if missing",
    "writes valid JSON with mcpServers.deckent",
    "preserves existing settings when merging",
    "updates existing deckent entry",
    "is idempotent — running twice produces same result",
    "handles invalid JSON gracefully",
    "handles array as existing settings (resets to object)",
    "handles mcpServers being a non-object (resets to object)"
   ],
   "cli/helpers/human-status.test.ts": [
    "returns \"0 sec\" for negative values",
    "returns seconds for < 60s",
    "returns minutes for < 60 min",
    "returns hours and minutes",
    "returns hours only when exact",
    "returns 0 sec for 0ms",
    "returns null when no tasks done",
    "returns null when all tasks done",
    "estimates remaining time proportionally",
    "returns estimate with tilde prefix",
    "returns empty for healthy state",
    "flags agent errors",
    "flags NO_GO tasks",
    "reports agent error even without matching task",
    "shows sprint number in header",
    "shows sprint title when provided",
    "shows progress as fraction and percentage",
    "shows active worker count",
    "uses singular \"worker\" for count of 1",
    "shows elapsed time when sprintStartedAt provided",
    "shows ETA when tasks in progress",
    "does not show time when sprintStartedAt is absent",
    "includes \"What\\'s happening:\" section",
    "shows done tasks with checkmark",
    "shows active tasks with arrow",
    "shows agent current action for active tasks",
    "shows NO_GO tasks with X mark",
    "shows waiting tasks with dependencies",
    "collapses waiting tasks when > 3",
    "shows issues section for NO_GO tasks",
    "shows issues section for agent errors",
    "shows blocked count when > 0",
    "does not show blocked section when 0",
    "shows \"Next\" section for queued tasks",
    "does not show \"Next\" when no waiting tasks",
    "handles empty task list gracefully",
    "truncates long task titles",
    "full integration: mixed status tasks produce coherent output",
    "shows paused tasks correctly",
    "100% progress when all done",
    "0% progress at start"
   ],
   "cli/helpers/injected-label.test.ts": [
    "returns the injected string unchanged",
    "throws a typed InjectedLabelMissingError carrying the label in a structured field",
    "the error message is exactly the stable technical code — no natural-language prose can leak to a user surface",
    "treats an empty string as missing (an empty label is not a rendered label)"
   ],
   "cli/helpers/message-catalog/memory-export.test.ts": [
    "registers every family key with explicit English and Turkish text",
    "builds distinct canonical English and Turkish compact label bundles"
   ],
   "cli/helpers/messages.test.ts": [
    "localizes batch reconciliation, approval, receipt, and HOLD wording in English and Turkish",
    "distinguishes normal admission from an owner-pair exception in English and Turkish",
    "renders an opaque invocation receipt reference in both languages",
    "returns English message for hint.COMPLETE with lang=en",
    "returns Turkish message for hint.COMPLETE with lang=tr",
    "returns English for hint.EXECUTE in en",
    "returns Turkish for hint.IDLE in tr",
    "returns English for hint.PLAN in en",
    "returns English for status.no_sprint in en",
    "returns Turkish for status.no_sprint in tr",
    "returns English for attach.no_active_session in en",
    "returns the key itself for a missing key",
    "returns the key itself for missing key in tr lang",
    "falls back to en for unknown lang code",
    "interpolates {taskCount} in status.tasks_running",
    "localizes finalize and recovery adapter text in both supported languages",
    "interpolates {taskCount} in Turkish status.tasks_running",
    "interpolates {sprintId} in status.sprint_active",
    "interpolates {taskId} in kill.worker_killed",
    "interpolates {taskId} and {model} in spawn.worker_spawned tr",
    "interpolates {taskId} in kill.worker_not_found tr",
    "leaves placeholder when var is missing",
    "leaves placeholder when vars is undefined",
    "handles extra vars that do not match any placeholder",
    "returns \"tr\" when config says \"tr\"",
    "returns \"en\" when config says \"en\"",
    "normalizes \"TR\" to \"tr\"",
    "normalizes \"EN\" to \"en\"",
    "falls back to \"en\" for unsupported config language \"fr\"",
    "detects Turkish from LANG env \"tr_TR.UTF-8\"",
    "detects English from LANG env \"en_US.UTF-8\"",
    "prefers LC_ALL over LANG when both set",
    "falls back to \"en\" with no config and no env",
    "falls back to \"en\" when env lang is unsupported (de_DE)",
    "config language takes priority over env vars",
    "returns \"en\" for undefined configLanguage with no env",
    "every known key has an English translation (not returning key itself)",
    "every known key has a Turkish translation (not returning key itself)",
    "en and tr translations differ for hint keys",
    "status keys have translations in both languages",
    "kill command keys have translations in both languages",
    "start.sandbox_not_implemented returns English text",
    "start.sandbox_not_implemented returns Turkish text",
    "start.use_force has --force mention in both languages",
    "start.watch_ignored_dry_run mentions dry-run mode",
    "start.sprint_planned interpolates number, id, count",
    "start.reasoning interpolates reasoning",
    "start.planning_mode interpolates mode in both languages",
    "start.workers_info interpolates count and model",
    "start.dry_run_complete mentions Dry-run in both languages",
    "start.watch_window_created mentions tmux",
    "start.watch_no_tmux mentions tmux in both languages",
    "plan.sprint_planned interpolates number, id, count",
    "plan.sprint_planned Turkish version",
    "plan.reasoning interpolates reasoning",
    "plan.planning_mode interpolates mode",
    "plan.note_sprint_size interpolates size and reason",
    "plan.approved returns approval message in both langs",
    "plan.rejected returns rejection message in both langs",
    "cleanup.decay_complete interpolates before and after",
    "cleanup.decay_complete Turkish version",
    "cleanup.archived_sprints interpolates sprints list",
    "cleanup.removed_items interpolates debt and patterns",
    "cleanup.complete interpolates count",
    "doctor.checks_passed interpolates passed and total",
    "doctor.checks_passed Turkish version",
    "provides distinct English and Turkish authority for every image-command string",
    "interpolates Cursor, path, command, tag, and failure details in both languages",
    "status.no_active_sprint has proper English text",
    "status.no_active_sprint has proper Turkish text",
    "status.dashboard_read_failed English mentions Failed",
    "status.dashboard_read_failed Turkish mentions okunamadı",
    "status.desc mentions run dashboard in both languages",
    "status.graph_no_active_run mentions run (not sprint) in both languages",
    "status.graph_not_found interpolates {id} and mentions run in both languages",
    "keeps dry-run and mutation guard copy explicit in both languages",
    "renders raw/effective status and opaque receipt evidence without dropping fields",
    "localizes the typed pre-dispatch reason option, guards, and rendered value",
    "localizes every stable settlement decision and reason enum used by the human surface",
    "history.desc mentions run history in both languages",
    "history.opt_last mentions runs in both languages",
    "history.opt_trend mentions runs in both languages",
    "history.trend_header interpolates {n} in both languages",
    "provides complete and distinct English and Turkish rows",
    "uses matching placeholders in both locales",
    "desktop.shell.bridge.no_sprint mentions run (not sprint) in both languages",
    "init.auto_detecting English mentions Auto-detecting",
    "init.auto_detecting Turkish mentions algılanıyor",
    "init.recommendation English mentions Recommendation",
    "init.recommendation Turkish mentions Öneri",
    "init.initialized interpolates name, mode, language",
    "init.next_steps returns next steps in both languages",
    "init.next_step_directives mentions DIRECTIVES.md in both langs",
    "init.next_step_start mentions deckent start in both langs",
    "do.slash_flag_off names terminal.run_flow_v2 in both languages",
    "do.slash_usage mentions /do in both languages",
    "both keys resolve (not the raw key) and en differs from tr",
    "scopeGate.fail resolves and literally contains \"Scope gate: FAIL\" in en (pins tests/cli/run-flow-scope-mirror.test.ts\\'s pre-existing substring check)",
    "scopeGate.overridden mentions --force-scope in both languages",
    "both keys resolve (not the raw key) and en differs from tr",
    "empty string config falls through to LC_ALL env",
    "empty string config falls through to LANG env",
    "empty string config falls back to en with no env",
    "getMessage with empty string key returns empty string",
    "getMessage with empty lang falls back to en behavior",
    "getMessage with undefined vars arg returns raw template",
    "getMessage with null-value vars keeps placeholder",
    "getMessage for attach.no_active_session in tr",
    "variable replacement is single-pass (does not re-replace)",
    "getMessage returns key for partially-defined key namespace",
    "getLanguage accepts config with trailing chars beyond 2 (e.g. tr_TR)",
    "emits warn to stderr in dev mode (NODE_ENV unset) when key is missing",
    "includes lang in the dev-mode warn message",
    "does NOT emit warn in production mode (NODE_ENV=production)",
    "does NOT emit warn when key exists (even in dev mode)",
    "still returns the key string as fallback after emitting warn",
    "returns English hint for known key",
    "returns Turkish hint for known key",
    "returns key itself for unknown key",
    "returns key for unknown key in Turkish too",
    "interpolates variables in template",
    "interpolates sprintId variable",
    "Turkish variable interpolation works",
    "leaves placeholder if variable missing",
    "works without vars parameter",
    "hint.EXECUTE English",
    "hint.PLAN Turkish",
    "unknown lang falls back to English",
    "returns configLanguage when a supported language is provided",
    "normalizes locale-style configLanguage (tr_TR → tr)",
    "falls back to LANG env var when configLanguage not provided",
    "prefers LC_ALL over LANG env var",
    "returns en when LANG is an unsupported language",
    "returns en when no config and no env var set",
    "falls back to LANG env when configLanguage is unsupported",
    "handles en_US LANG → returns en"
   ],
   "cli/helpers/output-mode.test.ts": [
    "default mode is normal",
    "sets mode to quiet",
    "sets mode to verbose",
    "sets mode to normal",
    "resets to normal",
    "quiet mode: allows quiet messages",
    "quiet mode: blocks normal messages",
    "quiet mode: blocks verbose messages",
    "normal mode: allows quiet messages",
    "normal mode: allows normal messages",
    "normal mode: blocks verbose messages",
    "verbose mode: allows quiet messages",
    "verbose mode: allows normal messages",
    "verbose mode: allows verbose messages",
    "quiet method calls writeFn when mode is quiet",
    "normal method does not call writeFn when mode is quiet",
    "verbose method does not call writeFn when mode is normal",
    "verbose method calls writeFn when mode is verbose",
    "all methods call writeFn when mode is verbose"
   ],
   "cli/helpers/output-skills.test.ts": [
    "returns muted (plain, never dim) \"none\" for undefined skills",
    "returns muted (plain, never dim) \"none\" for empty skills array",
    "returns yellow-highlighted single skill",
    "returns comma-separated skills",
    "includes ANSI reset code",
    "handles three or more skills",
    "shows skills for agent with assignedSkills",
    "shows \"none\" for agent without assignedSkills",
    "shows \"none\" for agent with empty assignedSkills",
    "preserves backward compatibility with agents having no skills field",
    "renders both agent and skills columns",
    "renders multiple agents each with different skills"
   ],
   "cli/helpers/output-status-overhaul.test.ts": [
    "isNoColor returns false when NO_COLOR is not set",
    "isNoColor returns true when NO_COLOR env is set",
    "isNoColor returns true when --no-color is in argv",
    "stripAnsi removes ANSI escape codes",
    "color applies ANSI when NO_COLOR is not set",
    "color returns plain text when NO_COLOR is set",
    "formatAgentLabel respects NO_COLOR",
    "formatSkillsLabel respects NO_COLOR",
    "formatDoctorResult respects NO_COLOR",
    "renders standalone status from tasks",
    "handles empty tasks",
    "uses linear estimate when no completion times",
    "uses weighted average when completion times provided",
    "ignores completion times with fewer than 2 entries",
    "returns null when all tasks done",
    "returns null when no tasks done"
   ],
   "cli/helpers/process-runtime-provider-authority.test.ts": [
    "persists a typed HOLD before task/sprint/provider bootstrap side effects",
    "keeps provider-free capability execution available",
    "does not treat a ready runtime without an exact candidate as an execution permit"
   ],
   "cli/helpers/process-runtime-provider-threading.test.ts": [
    "passes the exact process-scoped authority to task and sprint runners"
   ],
   "cli/helpers/prompt-choice.test.ts": [
    "prints the numbered rows with the typed hint and asks the localized choice question",
    "a number, an id or a label chooses; Enter cancels; unknown is not-found; blocked is reported"
   ],
   "cli/helpers/prompt.test.ts": [
    "returns trimmed answer from readline",
    "returns defaultValue when answer is empty",
    "returns empty string when no answer and no default",
    "appends defaultValue hint to question",
    "closes readline interface after resolving",
    "closes readline interface even if answer is empty",
    "returns selected option value",
    "returns second option when 2 selected",
    "retries on invalid input then accepts valid",
    "closes readline interface after selection",
    "returns true for \"y\"",
    "returns true for \"yes\"",
    "returns false for \"n\"",
    "returns false for \"no\"",
    "returns defaultValue (true) for empty answer",
    "returns defaultValue (false) for empty answer",
    "default is true when not specified",
    "is case-insensitive: \"Y\" → true",
    "returns false for unrecognized input",
    "closes readline interface after answering",
    "includes Y/n hint when defaultValue is true",
    "includes y/N hint when defaultValue is false"
   ],
   "cli/helpers/redact-sensitive.test.ts": [
    "redacts OpenAI-style sk- API keys",
    "redacts Anthropic-style sk-ant- API keys",
    "redacts key- prefixed API keys",
    "does not redact short sk- strings that are not API keys",
    "redacts Bearer tokens",
    "redacts bearer tokens case-insensitively",
    "redacts passwords in URLs",
    "redacts passwords in URLs with special characters",
    "redacts OPENAI_API_KEY=value",
    "redacts ANTHROPIC_API_KEY=value",
    "redacts SECRET_KEY=value",
    "redacts ACCESS_TOKEN=value",
    "returns empty string for empty input",
    "leaves non-sensitive text unchanged",
    "handles null-ish input gracefully",
    "redacts multiple sensitive values in one string",
    "preserves surrounding text structure",
    "does not redact normal words that happen to contain \"key\""
   ],
   "cli/helpers/splash.test.ts": [
    "contains dome shape characters",
    "contains tentacle characters",
    "contains base characters",
    "uses only printable ASCII",
    "preserves kraken silhouette markers",
    "returns Unicode art by default",
    "returns printable art when ASCII is required",
    "only permits an explicit override toward safer ASCII",
    "includes DECKENT text",
    "includes version string",
    "includes tagline",
    "includes Unicode kraken by default",
    "includes printable kraken when ascii option is set",
    "includes ANSI codes when NO_COLOR is not set",
    "produces clean text when NO_COLOR is set to 1",
    "produces clean text when NO_COLOR is empty",
    "keeps Unicode glyphs under NO_COLOR while stripping ANSI",
    "uses printable art under dumb terminal even with color",
    "degrades color tier with truecolor capability",
    "uses 16-color tier when truecolor capability lacks dark background",
    "suppresses color when FORCE_COLOR=0",
    "enables color when FORCE_COLOR is positive even without TTY semantics",
    "lets positive FORCE_COLOR override NO_COLOR per theme gate",
    "suppresses color when NO_COLOR is set without FORCE_COLOR",
    "respects --no-color argv ahead of FORCE_COLOR",
    "returns null when output_splash is false",
    "returns splash when output_splash is true",
    "returns null when output_splash is undefined",
    "forwards ascii option to showSplash",
    "returns null when output_splash is false",
    "returns splash string when output_splash is true",
    "returns null when output_splash is undefined (falsy gate)"
   ],
   "cli/helpers/sprint-summary-rich.test.ts": [
    "full output contains all 7 section headers",
    "NO_COLOR produces clean text without ANSI codes",
    "--no-color in argv produces clean text (superset SSOT — was RED pre-fix)",
    "quiet mode shows only results line",
    "results counts are correct for done/debt/nogo",
    "git diff truncation at 5 files",
    "empty sprint handles gracefully",
    "next steps include NO_GO fix suggestion when applicable",
    "duration formatted correctly from timestamps",
    "verbose mode includes task detail section",
    "agent performance table shows correct data",
    "learnings section renders up to 3 items with icons",
    "next steps suggest tech debt resolution",
    "next steps suggest coverage improvement when below 80%",
    "uses sprint id when number is not provided",
    "no git diff shows placeholder",
    "formats seconds correctly",
    "formats minutes and seconds",
    "formats hours and minutes",
    "handles zero",
    "handles negative values",
    "renders header with sprint number and duration",
    "renders header with date from startedAt",
    "renders header using durationMs when no timestamps provided",
    "renders results with done/debt/no-go counts",
    "renders task table with task IDs and titles",
    "shows TECH_DEBT status label instead of GO_WITH_TECH_DEBT",
    "renders agent performance with success rates",
    "shows placeholder when no agent data provided",
    "auto-generates NO_GO fix next step",
    "auto-generates tech debt next step",
    "auto-generates coverage next step when below 80%",
    "shows \"ready for next sprint\" when all tasks done",
    "renders learnings when provided",
    "shows placeholder when no learnings",
    "strips ANSI codes when NO_COLOR is set",
    "includes ANSI codes when NO_COLOR is not set",
    "produces output containing all section headers",
    "formatDuration handles hours and minutes",
    "formatDuration handles minutes and seconds",
    "formatDuration handles seconds only",
    "renders gracefully with empty evaluations",
    "handles sprint without number field",
    "quiet mode returns only results line",
    "renders evaluation summary counts"
   ],
   "cli/helpers/sprint-summary.test.ts": [
    "includes sprint id and number",
    "lists task evaluations",
    "shows summary counts",
    "shows all DONE when no failures",
    "contains RESULTS header",
    "contains CHANGES header",
    "lists file changes with +/- counts",
    "shows (new) marker for files with only additions",
    "does not show (new) marker when there are removals",
    "limits to 10 files",
    "shows \"No file changes recorded\" when empty",
    "aggregates files from multiple results",
    "contains TESTS header",
    "shows passing test count",
    "shows average coverage",
    "shows 0.0% when no coverage data",
    "contains all three sections",
    "sections are separated by blank lines"
   ],
   "cli/helpers/status-renderer.test.ts": [
    "returns the raw phase string when noColor is true",
    "wraps known phases in ANSI color codes when noColor is false",
    "falls back to dim color for an unrecognized phase",
    "returns 🔴 when there is no heartbeat at all",
    "returns 🟢 for a heartbeat under 2 minutes old",
    "returns 🟡 for a heartbeat between 2 and 5 minutes old",
    "returns 🔴 for a heartbeat older than 5 minutes",
    "maps RESULT+DONE to ✅",
    "maps SPAWN to 🔁",
    "maps NO_GO / NOGO to ❌",
    "maps ALERT to ⚠️",
    "maps NOTIFY to 🔵",
    "falls back to • for an unrecognized event type",
    "is case-insensitive",
    "returns plain text when noColor is true, regardless of pct",
    "colors green under 50%",
    "colors yellow between 50% and 80%",
    "colors red at 80% or above",
    "renders all-empty bar when total is 0",
    "renders the done/total (pct%) label",
    "renders fully filled bar at 100%",
    "applies color when noColor is false",
    "returns [] when the events file does not exist",
    "returns the latest ACTIVITY line per task, keyed by taskId",
    "skips malformed/torn lines without throwing",
    "caps the returned rows at maxTasks",
    "renders an honest \"unknown\" snapshot with no dashboard, tasks, or config",
    "reads sprintId, phase, progress, and agents from .dashboard when present",
    "falls back to task files to detect sprintId when no dashboard exists",
    "reads sprint_started_at / sprint_hard_timeout from .deckent/config.json",
    "uses ASCII box-drawing below the 60-column unicode threshold",
    "uses unicode box-drawing at or above the 60-column threshold",
    "strips ANSI color codes entirely when noColor is true",
    "renders recent events with icons when provided",
    "delegates to costColor using the configured noColor setting",
    "redraw clears the screen and writes the full content",
    "partialRedraw does a full write on the first call",
    "partialRedraw only rewrites changed lines on subsequent calls",
    "resetRedrawState forces the next partialRedraw to be a full write"
   ],
   "cli/helpers/terminal-capabilities.test.ts": [
    "treats unset locale as UTF-8 capable",
    "accepts explicit UTF-8 locales",
    "treats empty locale strings like unset (UTF-8 capable fallback)",
    "rejects non-UTF-8 locales",
    "applies LC_ALL > LC_CTYPE > LANG precedence",
    "treats unset locale keys as absent in the chain",
    "defaults to Unicode on capable terminals",
    "does not read ambient TERM when the injected environment omits it",
    "respects locale precedence without reading the host locale",
    "selects ASCII for dumb terminals",
    "selects ASCII when DECKENT_ASCII=1",
    "ignores DECKENT_ASCII values other than exact \"1\"",
    "selects ASCII for non-UTF-8 locales",
    "does not conflate NO_COLOR with glyph tier",
    "normalizes TERM trim and case for dumb detection",
    "honors explicit term override independent of env TERM",
    "selects ASCII for POSIX locale spellings"
   ],
   "cli/helpers/terminal-links.test.ts": [
    "reads the host from the environment signals each terminal sets",
    "a multiplexer wins over an inherited host signal (the pane, not the outer app, renders)",
    "accepts auto | on | off (case-insensitive) and falls back to auto",
    "auto: enabled only on a proven host",
    "auto: an old VTE, Apple Terminal, a multiplexer and an unknown host get no OSC bytes",
    "not a TTY: never, whatever the setting says",
    "setting on / off overrides the evidence on a TTY",
    "carries the host so a status surface can name it"
   ],
   "cli/helpers/terminal-readability-gate.test.ts": [
    "every role declares a contrast class and never uses SGR dim",
    "the roles the Terminal surfaces need exist",
    "focus is a theme-agnostic carrier (inverse), muted is the default foreground in the host tier",
    "${role} (${entry.class}) reads on ${theme.id}",
    "${role} token hex reads on ${theme.id}",
    "${role} nearest xterm-256 color reads on ${theme.id}",
    "composes attributes and the tier color; a bare default-foreground role is null"
   ],
   "cli/helpers/terminal-surface.test.ts": [
    "interactive TTY with a capable TERM → the Ink surface",
    "DECKENT_INK=0 → the legacy readline surface (still interactive)",
    "TERM=dumb → the line surface: no cursor control is ever admitted, even on a TTY",
    "pipe / redirect on either side → the line surface",
    "an unset TERM (Windows native consoles) is not demoted — only an explicit dumb TERM is",
    "unknown DECKENT_INK values keep the default surface",
    "color suppressed (NO_COLOR / --no-color) and no FORCE_COLOR → FORCE_COLOR=0 for the Ink process",
    "never overrides an explicit FORCE_COLOR the user set (theme.ts precedence: FORCE_COLOR beats NO_COLOR)",
    "no suppression → no projection (Ink keeps its own TTY detection)",
    "the boot path admits the surface through resolveTerminalSurfaceFromProcess",
    "the color-gate preload is the FIRST import of entry.ts (chalk is loaded by static chains before the REPL branch)",
    "the preload module depends only on theme.ts and terminal-surface.ts (nothing that loads chalk)",
    "shouldUseColor / suppressionTier are off on a dumb terminal, and FORCE_COLOR>0 overrides it",
    "isDumbTerminal is case/whitespace tolerant and false for unset TERM"
   ],
   "cli/helpers/theme.test.ts": [
    "is a class that can be instantiated",
    "singleton theme instance exists",
    "success wraps text in green ANSI",
    "error wraps text in red ANSI",
    "warning wraps text in yellow ANSI",
    "info wraps text in bright-blue ANSI",
    "muted is plain text in the host tier (no dim)",
    "link is underlined bright-blue, code is bright-blue, focus is inverse",
    "accent wraps text in cyan ANSI",
    "bold wraps text in bold ANSI",
    "success returns plain text",
    "error returns plain text",
    "warning returns plain text",
    "success returns plain text",
    "success returns colored text",
    "removes ANSI escape codes",
    "returns plain text unchanged",
    "handles multiple ANSI codes"
   ],
   "cli/helpers/wcag-contrast.test.ts": [
    "black and white span the full 1:21 range",
    "#767676 on white sits on the AA boundary (≈4.54)",
    "accepts upper-case and short forms",
    "returns the foreground untouched when the ratio already holds",
    "lifts a dark foreground on a dark background until 4.5 holds",
    "darkens a pale foreground on a light background until 4.5 holds",
    "ratio 1 (host feature disabled) never changes anything"
   ],
   "cli/helpers/wizard-provider.test.ts": [
    "detects Claude Code via CLAUDE_CODE env var",
    "detects Claude Code via CLAUDE_SESSION_ID env var",
    "detects Claude Code via parent process name",
    "detects Cursor via CURSOR_SESSION env var",
    "detects Cursor via CURSOR_TRACE_ID env var",
    "detects Cursor via .cursor/ directory",
    "returns terminal as fallback",
    "returns terminal when parent process check fails",
    "returns auto-configured message for claude-code",
    "returns mcp.json instructions for cursor",
    "returns terminal guidance for terminal",
    "auto-configures single available provider",
    "returns steps for multiple available providers",
    "includes fallback step when 3+ providers available",
    "does not include fallback step for exactly 2 providers",
    "handles zero available providers gracefully",
    "provider choices include auth method",
    "resolves brain and worker providers",
    "resolves fallback provider when set",
    "omits fallback when set to none",
    "selectedProviders includes unique providers only",
    "selectedProviders includes all distinct providers",
    "returns null for available provider",
    "returns OPENAI_API_KEY for unavailable codex",
    "returns GOOGLE_API_KEY for unavailable gemini",
    "returns null for unavailable claude (session-based)",
    "returns empty for all available providers",
    "returns guidance for unavailable codex",
    "returns guidance for unavailable claude",
    "returns guidance for multiple unavailable providers",
    "resolves defaults in non-interactive mode",
    "resolves fallback default to none in non-interactive mode"
   ],
   "cli/helpers.test.ts": [
    "writes to stdout with newline",
    "writes Error message to stderr",
    "converts non-Error to string",
    "returns all dots at 0%",
    "returns all hashes at 100%",
    "returns half-filled at 50%",
    "clamps negative values",
    "clamps values over 100",
    "respects custom width",
    "formats headers and rows with padding",
    "handles empty rows",
    "handles rows with fewer cells than headers (uses empty string)",
    "contains box-drawing characters",
    "shows sprint number in title",
    "shows agent lines",
    "shows progress line",
    "handles empty agent list",
    "shows alert count",
    "shows check marks for passing checks",
    "shows X marks for failing checks",
    "shows correct summary with mixed results",
    "shows basic sprint info",
    "shows metrics when available",
    "has correct values",
    "prints error and sets exit code",
    "returns current working directory",
    "shows --:-- when updatedAt is undefined",
    "uses phaseLabel when agent has no currentAction",
    "shows DONE progress bar for DONE agent",
    "shows IDLE progress bar for IDLE agent",
    "slices content when agent line exceeds column width",
    "falls back to unknown status tag",
    "returns trimmed answer when provided",
    "returns default when answer is empty",
    "returns empty string when empty and no default",
    "appends default suffix to question",
    "returns selected value for valid input",
    "retries on invalid input then accepts valid",
    "prints options list",
    "returns true for \"y\"",
    "returns true for \"yes\" (case insensitive)",
    "returns false for \"n\"",
    "returns default true for empty answer",
    "returns default false for empty answer",
    "shows Y/n hint when default is true",
    "shows y/N hint when default is false"
   ],
   "cli/hints.test.ts": [
    "COMPLETE phase returns Turkish hint",
    "COMPLETE phase returns English hint",
    "EXECUTE phase returns hint",
    "EXECUTE phase returns Turkish hint",
    "EXECUTE phase with taskCount includes task count hint",
    "EXECUTE phase with sprintId includes sprint hint",
    "PLAN phase returns correct hint",
    "PLAN phase returns Turkish hint",
    "IDLE phase returns correct hint",
    "IDLE phase returns Turkish hint",
    "lowercase phase name works",
    "unknown phase returns empty array",
    "default lang is English"
   ],
   "cli/i18n-errors.test.ts": [
    "getMessage returns English message for \"${key}\"",
    "getMessage returns Turkish message for \"${key}\"",
    "English catalog has key \"${key}\"",
    "Turkish catalog has key \"${key}\"",
    "en.json and tr.json have different values for error.tmux_not_found",
    "en.json error messages contain actionable info"
   ],
   "cli/i18n-hardcode-cleanup.test.ts": [
    "${key} resolves to non-empty en string",
    "${key} resolves to non-empty tr string distinct from en",
    "doctor.daemon_header en equals prior literal",
    "doctor.daemon_clean en equals prior literal",
    "doctor.daemon_found en equals prior literal (interpolated)",
    "doctor.daemon_entry en equals prior literal (interpolated)",
    "doctor.daemon_kill_hint en equals prior literal (interpolated)",
    "${key} resolves to non-empty en string",
    "${key} resolves to non-empty tr string distinct from en",
    "evolve.no_sprint_data en matches prior literal",
    "evolve.report_header en matches prior template (interpolated)",
    "evolve.nogo_trend en matches prior template (interpolated)",
    "evolve.agent_trends en matches prior literal",
    "evolve.skill_trends en matches prior literal",
    "resolves to non-empty en string",
    "resolves to non-empty tr string distinct from en",
    "en matches prior literal exactly",
    "does not contain DAEMON_MESSAGES",
    "does not contain daemonMsg function definition",
    "does not contain hardcoded \"Daemon Hygiene:\" literal",
    "does not contain hardcoded \"No stale deckent daemons\" literal",
    "uses getMessage for daemon strings",
    "does not contain hardcoded \"No sprint data found\" literal",
    "does not contain hardcoded \"Evolution Report —\" string literal",
    "does not contain hardcoded \"Agent Trends:\" literal",
    "does not contain hardcoded \"Skill Trends:\" literal",
    "uses getMessage for evolution strings",
    "does not contain hardcoded \"DECKENT.md not found\" literal",
    "uses getMessage for DECKENT.md not found string"
   ],
   "cli/i18n.test.ts": [
    "returns \"en\" when no config and no env vars",
    "returns \"tr\" when config.language is \"tr\"",
    "returns \"en\" when config.language is \"en\"",
    "detects Turkish from LANG=tr_TR.UTF-8",
    "detects Turkish from LC_ALL=tr_TR.UTF-8 (higher priority than LANG)",
    "config language takes priority over LANG env var",
    "falls back to LANG env when no config directory exists",
    "returns \"en\" for unsupported LANG value",
    "returns English messages for lang=\"en\"",
    "returns Turkish messages for lang=\"tr\"",
    "supports variable interpolation",
    "Turkish variable interpolation works",
    "falls back to English for unsupported lang",
    "normalizes tr_TR.UTF-8 to \"tr\"",
    "returns \"en\" for en_US.UTF-8",
    "accepts explicit configLanguage override",
    "key \"${key}\" has both EN and TR translations",
    "returns English content for lang=\"en\"",
    "returns Turkish content for lang=\"tr\"",
    "EN and TR outputs are different",
    "surfaces the operations/monitoring commands (watch --follow, resources) in EN and TR",
    "contains all sections in English output",
    "contains all 3 sections in Turkish output",
    "returns true for \"en\"",
    "returns true for \"tr\"",
    "returns false for unsupported codes",
    "SUPPORTED_LANGS contains en and tr"
   ],
   "cli/identity-messages.test.ts": [
    "rbac.unauthorized interpolates permission (EN + TR)",
    "identity.verify_prompt + binding_unconfigured resolve in both langs"
   ],
   "cli/image-build.test.ts": [
    "resolves the packaged assets/Dockerfile.worker path and prints the build plan WITHOUT spawning",
    "dry-run echoes a custom --tag in both the plan and the tag line",
    "dry-run does NOT use process.cwd() — the resolved path is rooted at the injected package root",
    "preserves spaces in Dockerfile and context paths without spawning a shell",
    "invokes docker build with the resolved -f Dockerfile path and the --tag",
    "uses the package root as build context so trusted runtime authority is available",
    "honest-fails before docker spawn when the trusted runtime authority is absent",
    "honest-fails before docker spawn when root package identity metadata is absent",
    "honest-fails before docker spawn when the locked dependency authority is absent",
    "defaults the tag to deckent-worker:latest when none is given",
    "--image is a back-compat alias for --tag (upgrade/init callers)",
    "--tag wins over the deprecated --image alias",
    "a non-zero docker exit is propagated (build failure)",
    "--with-codex / --with-gemini / --with-ollama / --with-cursor add exact INSTALL_* build-args",
    "no provider flags → no INSTALL_* build-args",
    "exposes --with-cursor on deckent image build",
    "docker absent (ENOENT) → non-zero exit and an actionable stderr message (never a silent 0)",
    "a generic spawn launch error is also surfaced (non-zero, no silent success)",
    "missing Dockerfile → non-zero, actionable message, and NO docker spawn",
    "dry-run on a missing Dockerfile still prints the resolved path, flagged NOT FOUND",
    "Turkish missing-Dockerfile guidance is localized and never spawns docker"
   ],
   "cli/img2-init-fold.test.ts": [
    "returns undefined without spawning anything when spawn_backend is subprocess",
    "returns undefined without spawning when config file is absent",
    "returns undefined and does NOT build when image is already ready",
    "calls handleImageBuild (spawns docker build) when image is absent",
    "passes the custom image tag to docker build when worker_image is configured",
    "returns undefined and spawns nothing when spawn_backend is not docker",
    "returns undefined when config file is absent",
    "returns undefined without building when image is still ready after upgrade",
    "calls handleImageBuild to re-provision when image is missing after upgrade",
    "includes codex build-arg when worker_provider is codex"
   ],
   "cli/index-runflow-wiring.test.ts": [
    "registerDo is imported in index.ts",
    "registerPlanNl is imported in index.ts",
    "registerDo is called with program as its first argument in index.ts",
    "registerPlanNl is called with program in index.ts",
    "index.ts does not define its own \"do\"/\"plan-nl\" action handler (no shadow implementation)",
    "registers exactly one \"do\" command",
    "registers exactly one \"plan-nl\" command",
    "nogo guard: no separate RunFlow-specific command variant was introduced",
    "\"do\" keeps exactly its known options — --run, --yes, --force-scope (B1), --write-allowlist (closed write scope), no other flag",
    "\"plan-nl\" keeps exactly its pre-existing options — --write, no new flag",
    "help-text consistency pin: \"do\" description text is unchanged",
    "help-text consistency pin: \"plan-nl\" description text is unchanged",
    "help-text consistency pin: \"do\" --yes option text still documents the RunFlow flag (terminal.run_flow_v2)",
    "`deckent do <goal>` (dry-run) through the FULL program prints the legacy golden-flow preview — no RunFlow markers",
    "`deckent plan-nl <goal>` through the FULL program prints the legacy scaffold-only preview — no compatibility-adapter marker"
   ],
   "cli/index.test.ts": [
    "exports buildProgram as a function",
    "importing index.ts does NOT trigger parseAsync (no side-effects)",
    "buildProgram returns a Command instance",
    "program name is \"deckent\"",
    "program has --version-json option",
    "program has -V/--version flag",
    "writes exactly one plain version line for non-TTY --version without a splash",
    "keeps the splash and version line for TTY --version",
    "registers all 28 command functions",
    "each register function receives a Command instance",
    "threads provider authority into both run and manual spawn composition roots",
    "calling buildProgram multiple times creates independent programs",
    "version matches DECKENT_VERSION constant",
    "buildProgram does not call parseAsync",
    "program description is set correctly",
    "entry.ts file exists and exports from index",
    "bin.deckent points to dist/cli/entry.js"
   ],
   "cli/init-backend-transaction.test.ts": [
    "resolves true when `docker info` exits 0 (daemon reachable)",
    "resolves false when `docker info` exits non-zero (daemon down — the RC2-B scenario)",
    "resolves false when the spawn itself errors (docker missing entirely)",
    "resolves false when spawn throws synchronously",
    "resolves false (and kills the child) after the timeout when the daemon never responds",
    "CLI + daemon both alive -> docker, not downgraded",
    "CLI present, daemon dead -> subprocess, flagged as downgraded (never docker)",
    "CLI absent -> subprocess, not downgraded (nothing to downgrade from)",
    "win32 -> subprocess regardless of docker signals",
    "CLI present + daemon DOWN (the RC2-B bug scenario) -> GREEN: subprocess written, honest fallback message printed, docker never written",
    "CLI present + daemon spawn errors (docker binary vanished mid-probe) -> subprocess, honest message",
    "CLI present + daemon ALIVE -> docker selected, no fallback message",
    "CLI absent -> subprocess, daemon never probed at all (no wasted `docker info` spawn)",
    "honest fallback message localizes to Turkish (i18n en+tr requirement)",
    "decline -> config downgraded docker->subprocess + remediation command printed",
    "build failure (non-zero exit) -> config downgraded docker->subprocess + remediation command printed",
    "decline when config was already subprocess -> no-op, no fallback message (nothing to downgrade)",
    "successful build -> stays docker, no downgrade"
   ],
   "cli/init-exitcode-contract.test.ts": [
    "unconditional handleCliError on a post-settle rejection overwrites an already-decided SETUP_INCOMPLETE(2)",
    "delegates to handleCliError exactly as before, for a rejection during normal execution",
    "SETUP_INCOMPLETE(2): a later rejection no longer crushes the exit code",
    "READY(0, captured as undefined): a later rejection still resolves to exit 0, never 1",
    "FAILED(1, a genuine earlier crash): still preserved at 1, not silently changed",
    "a second lock() call does not re-capture a later exitCode mutation",
    "guardUnhandledRejection is registered as an unhandledRejection listener on process"
   ],
   "cli/init-image-integration.test.ts": [
    "--no-image → opted-out, nothing probed/built",
    "non-interactive (no TTY / CI) → opted-out, no build",
    "--yes (CI auto flag) → opted-out, never auto-builds",
    "honest-skips with an actionable message and never builds",
    "returns image-present and does not prompt or build",
    "confirm → delegates to handleImageBuild with the resolved (config) tag",
    "falls back to DEFAULT_WORKER_IMAGE tag when no worker_image configured",
    "derives codex/gemini build-args from the project config",
    "explicit image override wins over config",
    "decline → no build, returns declined",
    "build failure (non-zero exit) → build-failed",
    "works with no config file at all (defaults, claude-only build-args)"
   ],
   "cli/init-next-steps.test.ts": [
    "surfaces the autonomous + nervous opt-in capabilities (EN)",
    "surfaces them in Turkish too"
   ],
   "cli/init-noninteractive.test.ts": [
    "rejects with PromptEOFError instead of hanging when piped stdin runs out mid-sequence",
    "rejects immediately (stdin=/dev/null equivalent — no input at all)",
    "--yes opens ZERO prompts and applies lang=en/plan=balanced/name=basename(cwd) defaults, config.json still written",
    "--yes with multiple available providers skips the provider wizard prompt too (runWizard called nonInteractive)",
    "--yes never treats unattended defaults as npm-global install consent",
    "--yes --install explicitly authorizes the existing bounded installer",
    "non-TTY without --yes -> FAILED, non-zero exit, honest i18n message, zero prompts, zero config write (silent-exit-0 DIES)",
    "non-TTY without --yes never reaches --auto\\'s system-detection path (auto still bypasses the TTY gate on purpose)",
    "EOF (PromptEOFError) during a real interactive prompt -> FAILED via the EXISTING classifyInitOutcome/formatInitOutcomeBlock contract, non-zero exit"
   ],
   "cli/init-outcome-honesty.test.ts": [
    "READY when no fatal failure and no blockers",
    "SETUP_INCOMPLETE when no fatal failure but at least one blocker exists",
    "FAILED wins over blockers whenever a fatal failure occurred",
    "maps the three-state contract to exit codes 0 / 2 / 1",
    "returns no blockers when a provider is available and doctor is clean",
    "adds a no-provider blocker with an exact, runnable remediation command when zero providers are available",
    "adds one blocker per still-failing required doctor check, naming the check",
    "adds a doctor-verification-failed blocker when the doctor step itself threw",
    "renders Turkish blocker text for lang=tr",
    "READY output states the outcome token and has no blockers section",
    "SETUP_INCOMPLETE output states the token, lists blockers with a Fix: line, and never says \"You\\'re ready\"",
    "FAILED output states the token and never says \"You\\'re ready\"",
    "zero providers available -> SETUP_INCOMPLETE, exit 2, blocker+remediation shown, \"You\\'re ready\" NEVER printed",
    "required doctor check still failing after provisioning -> SETUP_INCOMPLETE, exit 2, names the check",
    "--yes with a failing required doctor check -> same SETUP_INCOMPLETE/exit-2 contract",
    "happy path (provider available, doctor ok) -> READY, exit stays 0 (undefined), \"You\\'re ready\" still printed",
    "a fatal (unwrapped) step failure -> FAILED, exit 1 (unchanged), outcome block says FAILED"
   ],
   "cli/init-published.test.ts": [
    "package.json files field includes dist and LICENSE",
    "bin entry points to dist/cli/entry.js",
    "DECKENT_VERSION resolves to the real package version (install-path, not the 0.0.0 fallback)",
    "constants.ts resolves DECKENT_VERSION via import.meta.url, never process.cwd()",
    "init.ts constructs paths with join(), never from process.cwd()"
   ],
   "cli/init-repair-failedsteps.test.ts": [
    "happy path + --repair: failedSteps stays empty, no \"Failed steps:\" section printed",
    "a locally-caught step failure fills failedSteps and is shown under --repair",
    "the same failure WITHOUT --repair stays silent (happy-path/no-flag behavior unchanged)",
    "a fatal (unwrapped) step failure records the failing step name via the currentStep breadcrumb"
   ],
   "cli/init-splash-consumer.test.ts": [
    "wires showSplash at init.ts consumer site (static)",
    "invokes showSplash once on init --yes before other output",
    "does not skip splash when showSplash returns non-empty under dumb env"
   ],
   "cli/init.test.ts": [
    "should have at least 40 ADR entries",
    "every entry has required fields",
    "each ADR has a valid status",
    "ADR IDs are unique",
    "contains known ADRs (spot check)",
    "ADR-005 is deprecated",
    "createIdentitySeed returns valid entry",
    "DECKENT.md templates reference exports/summary.md, not MEMORY.md",
    "describes memory_budget as the retained-entry decay threshold in both init locales",
    "init.ts does not create DECISIONS.md for new projects",
    "mcp/tools/init.ts does not create DECISIONS.md for new projects (CLI/MCP parity)"
   ],
   "cli/input-bar-527.test.ts": [
    "a chunk that reduces to pure spaces on an empty buffer is noop, not submit",
    "a chunk that reduces to a tab-only line on an empty buffer is noop",
    "an existing whitespace-only buffer plus a purely-newline chunk stays noop (combined line is still blank)",
    "does not regress the happy path: a real (non-whitespace) line still submits, untrimmed",
    "a purely-newline chunk on a non-blank existing buffer still submits that buffer (not swallowed)",
    "Ink-native key.home/key.end map to the readline home/end names and move the cursor",
    "resolves via os.tmpdir(), never a hardcoded POSIX /tmp literal"
   ],
   "cli/input-bar-cluster.test.ts": [
    "Ink-native key.home=true (the real Ink 7.0.5 shape) maps to {name:\"home\"}",
    "Ink-native key.end=true (the real Ink 7.0.5 shape) maps to {name:\"end\"}",
    "raw xterm escape sequences fall back to home/end when the boolean is unset",
    "Home/End cursor hareketi çalışır: feeding the mapped key through editInput moves the cursor",
    "does not misfire on unrelated keys (arrows, return, backspace still take priority)",
    "an internal-newline paste (real multi-line paste) inserts as text, buffer unchanged otherwise",
    "an internal-newline paste WITH a trailing newline still inserts (trailing strip does not remove the internal one)",
    "CRLF is normalized to LF on insert",
    "a single line + trailing newline submits the combined buffer+chunk",
    "REGRESSION (born-527): a chunk that is PURELY newline/CR bytes with an empty buffer never submits or records — noop",
    "a purely-newline chunk with a NON-empty existing buffer still submits that buffer (not swallowed)",
    "defaults to the OS temp dir, never a hardcoded POSIX /tmp literal",
    "honors an explicit DECKENT_INK_DEBUG_LOG override"
   ],
   "cli/inspect.test.ts": [
    "renders the canonical run listing",
    "renders absent archived debt hold evidence as unavailable rather than zero",
    "renders the real core task detail DTO including distinct task and current-run truth",
    "does not invent current-run truth for a historical task DTO",
    "returns typed exit 1 for an unknown task",
    "preserves listing and detail machine shapes with --json",
    "follows snapshot revisions and disposes the observer on close",
    "follows one task status and heartbeat on each revision",
    "registers a help-visible inspect command with JSON and follow options"
   ],
   "cli/intelligence-command.test.ts": [
    "host owns the build ritual and runs real-binary proof after sprint settlement"
   ],
   "cli/json-output-contract.test.ts": [
    "covers every command file that declares a --json flag",
    "finds no prose written to stdout on any --json-reachable path",
    "flags prose printed before the json branch",
    "flags prose printed inside the json branch",
    "flags a human renderer call reachable in json mode",
    "flags prose after an IMPURE json guard returns (the output.ts class)",
    "accepts prose guarded by !json, folded into stderr, or after a pure json return",
    "skill list --json emits one document (builtin catalog, no human hint)",
    "skill list --json emits [] rather than the \"create one\" hint when filtered empty",
    "checkpoint list --json emits one document on an empty project",
    "history --json emits one document and keeps the notice on stderr",
    "agent list --json emits one parseable document",
    "status --json emits one parseable document",
    "xverify routes its waiting-approval signal to stderr, never stdout",
    "human mode is NOT required to emit JSON on stdout"
   ],
   "cli/kill-all-confirm.test.ts": [
    "proceeds without prompting when --force is set",
    "proceeds without prompting when --user-explicit is set",
    "aborts (returns false) when no flag is set and the user declines",
    "proceeds (returns true) when no flag is set and the user confirms"
   ],
   "cli/kill-cascade.test.ts": [
    "full cascade: SIGTERM workers → SIGTERM controller → 5s grace → SIGKILL stragglers → metadata cleanup → tmux socket → SPRINT_KILLED event",
    "controller-only: with no active workers, still SIGTERMs and (if needed) SIGKILLs controller PID",
    "tmux-socket cleanup runs even when there is no active sprint state",
    "reconciles host-owned Docker settlements after containment before reporting success",
    "terminalizes a stale active dashboard even when the controller PID file is already gone"
   ],
   "cli/kill-sprint-targeted.test.ts": [
    "🔴 reused pid (stored token ≠ live token) → REFUSES, does NOT kill the foreign process",
    "owned pid (token matches live process) → SIGTERM/SIGKILL, process dies",
    "dead pid → already-stopped, stale pid file cleaned, nothing signalled",
    "no pid file → already-stopped (idempotent — a second kill is harmless)"
   ],
   "cli/kpi-breach-surface.test.ts": [
    "goCriteria: output includes breach section when cost_per_sprint is critical (en)",
    "goCriteria: breach section names ONLY the breached KPI, not the healthy ones (en)",
    "goCriteria: no breach section when all KPIs are healthy",
    "goCriteria: breach section uses TR language titles when lang=tr",
    "scorecard table is still rendered before the breach section",
    "JSON mode: no breach section emitted (JSON mode returns before table render)"
   ],
   "cli/kpi-no-arg-fallback.test.ts": [
    "goCriteria: resolves the real latest sprint id with a non-empty kpis[]",
    "picks the LATEST finalized sprint when several have results",
    "table mode: renders the finalized sprint scorecard (not kpi.no_data)",
    "explicit --sprint wins over the latest-finalized fallback",
    "active sprint wins over the fallback (active path unchanged)",
    "truly-empty store (DB exists, no kpi_results) → empty kpis[], no crash",
    "DB without the KPI schema → degrades to empty (no \"no such table\" crash)",
    "no DB file at all → kpi.no_data and the DB is NOT created as a side effect"
   ],
   "cli/lang-authority.test.ts": [
    "applies every precedence tier and normalizes locale values",
    "keeps the short alias above config and lets unsupported sources fall through",
    "keeps getLanguage compatible while LANG supplies the no-config fallback",
    "makes all three public paths resolve the same config input",
    "supports the short config env alias with long-name precedence"
   ],
   "cli/limits-command.test.ts": [
    "returns an empty object when no config file exists",
    "roundtrips enabled/session_max_pct/weekly_max_pct written to project config.json",
    "project config overrides global config field-by-field",
    "drops invalid field types instead of propagating garbage",
    "tolerates a corrupted config.json without throwing",
    "returns ok when all windows are below the (default) warn floor",
    "returns warn when session pct crosses the default warn floor but not its block ceiling",
    "returns block when session pct meets the configured session_max_pct",
    "evaluates the weekly window independently of the session window",
    "includes week (Fable) when present, using the weekly threshold",
    "returns unknown when the probe is unavailable, regardless of config",
    "is a byte-identical no-op (zero probe calls) when limit_gate.enabled is absent",
    "is a no-op when limit_gate.enabled is explicitly false",
    "blocks sprint start on a block verdict when enabled and no forceLimits",
    "bypasses a block verdict when forceLimits is set",
    "does not block on a warn verdict — proceeds with an advisory message",
    "surfaces an unavailable probe as unknown without silently claiming ok",
    "renders the Turkish start_gate_blocked message when lang=tr",
    "--json output includes a top-level sessionPct field (Smoke contract)",
    "--json includes gate config and verdict fields",
    "--json sets sessionPct null and unavailable true when the probe fails",
    "renders a human-readable table (non-JSON) with window/usage/resets columns",
    "renders UNKNOWN in table mode when live evidence is unavailable",
    "sets process.exitCode = 1 on a block verdict (table mode)",
    "does not set a non-zero exitCode on ok/warn verdicts",
    "renders the Turkish header when getLangFromConfig returns tr",
    "registers a working `limits --json` command"
   ],
   "cli/limits-provider-general.test.ts": [
    "deduplicates providers from every effective-config provider slot",
    "keeps only authenticated configured providers and honors all filters"
   ],
   "cli/line-edit-codepoint.test.ts": [
    "typing a surrogate-pair emoji then a single Backspace leaves no mojibake",
    "a single Delete removes a whole emoji ahead of the cursor, no dangling surrogate",
    "Left steps over a mid-buffer emoji as ONE atomic move, not into its middle",
    "Right steps over a mid-buffer emoji as ONE atomic move, not into its middle",
    "Backspace inside a larger buffer removes only the whole emoji, preserving surrounding text",
    "Delete inside a larger buffer removes only the whole emoji, preserving surrounding text",
    "Backspace at buffer start next to an emoji is still a clean no-op",
    "Left/Right stay code-point-safe across a run of consecutive emoji",
    "plain-ASCII Backspace/Delete/Left/Right are unaffected (code-point index == UTF-16 offset)"
   ],
   "cli/line-edit.test.ts": [
    "inserts printable chars at the cursor",
    "inserts Turkish chars correctly",
    "backspace removes char before cursor",
    "backspace at start is a no-op",
    "delete removes char at cursor",
    "left/right move the cursor within bounds",
    "Home/End and Ctrl-A/Ctrl-E jump to edges",
    "Enter submits a non-empty line and clears the buffer",
    "Enter on empty line does not submit",
    "Ctrl-C signals int and clears; Ctrl-D on empty signals eof",
    "Ctrl-U clears the line",
    "↑/↓ request history navigation",
    "multi-char paste sequence inserts as one block",
    "drops lone control bytes (e.g. bare ESC)",
    "strips embedded ANSI/control bytes from a paste (escape-injection)",
    "drops a printable-headed paste that normalizes to nothing (DEL + control tail)",
    "still drops control-HEADED chunks whole (unmapped CSI stays out of the buffer)",
    "keeps unicode + interior tab intact while sanitizing",
    "↑ walks older entries, ↓ returns toward the live draft",
    "preserves the live draft when starting to navigate up",
    "empty history → navigation returns the live line",
    "does not store consecutive duplicates",
    "returns matching entries most-recent-first",
    "is case-insensitive",
    "empty query → all entries most-recent-first",
    "no match → empty"
   ],
   "cli/live-footer.test.ts": [
    "collapses an entirely empty state to a single honest \"idle\" line",
    "empty strings for running/next are treated as absent (still idle)",
    "renders a terminal failure without stale running or elapsed language",
    "renders an ORPHANED/dead run without stale activity and preserves recovery",
    "renders exactly one line when only \"running\" (Q1) is supplied",
    "renders an elapsed line (Q2) from startedAt vs the injected clock",
    "elapsed formats hours+minutes once past 60 minutes",
    "elapsed formats seconds when under a minute",
    "an unparseable startedAt degrades to the unknown label rather than throwing",
    "renders provider-health (Q3) — healthy/degraded/unknown",
    "renders auth-state (Q4) — logged-in/logged-out/unknown",
    "renders \"next\" (Q5)",
    "renders all 5 questions as 5 lines, in Q1..Q5 order, when fully populated",
    "omits lines for questions the caller did not supply (partial state)",
    "respects NO_COLOR — no ANSI escape codes on colorized lines",
    "emits ANSI color when FORCE_COLOR=1 on health/auth lines",
    "plain (uncolored) lines like \"running\"/\"next\" stay ANSI-free even under FORCE_COLOR",
    "truncates a line exceeding the given width, preserving an ellipsis",
    "leaves a line untouched when it already fits the width",
    "does not corrupt ANSI escapes when truncating a colorized line (truncate-then-color)",
    "uses display cells and the caller marker for CJK, combining, and ZWJ content",
    "uses caller-supplied labels instead of the English defaults",
    "falls back to English defaults for any label not overridden"
   ],
   "cli/local-llm-command.test.ts": [
    "preserves owner-authored local_llm launch authority through real loadConfig",
    "derives endpoint, host, and port from resolved config",
    "rejects a non-loopback bind host from resolved config",
    "builds the direct llama.cpp argv and configures its shared-library directory",
    "reports health and identity from the model listing route without authorization",
    "stops the persisted server process and removes its pid file",
    "registers start, status, and stop under local-llm",
    "projects platform-specific library path variables without changing argv",
    "projects an explicit CUDA backend, full layer offload, and ordered runtime libraries",
    "uses target-platform loader variables and delimiters for explicit backends",
    "makes CPU placement explicit without changing the portable default",
    "rejects malformed or contradictory acceleration policy %#"
   ],
   "cli/local-llm-context.test.ts": [
    "takes the minimum known value and reports every provenance signal",
    "keeps the configured ceiling when optional metadata is absent and says so",
    "subtracts system, serialized tools, output reserve, and safety reserve visibly",
    "reports status mismatch and restart-required in both languages",
    "never fabricates an effective value when the live server is unavailable",
    "renders unavailable context even when model identity discovery is also unreachable"
   ],
   "cli/mcp-attach.test.ts": [
    "returns concrete command tuples for the three supported hosts",
    "returns null for unknown host names",
    "marks supported=false when the host CLI lacks an mcp subcommand",
    "returns attached=true when deckent is already in mcp list",
    "returns attached=false when deckent is missing from mcp list",
    "does not match substring servers like deckent-dev",
    "detects deckent when mcp list output is ANSI-colorized",
    "survives runner exceptions and reports supported=false",
    "short-circuits when deckent is already attached",
    "runs the host add subcommand and reports success",
    "returns ok=false with a clear message when add exits non-zero",
    "skips when already attached and prints the ready message",
    "prompts the user and attaches when they accept",
    "does NOT attach when the user declines the prompt",
    "checkOnly mode never prompts and never mutates host config",
    "autoYes bypasses the prompt and attempts attach immediately",
    "emits a warning when host CLI lacks mcp support and does not call add"
   ],
   "cli/mcp-bridge-drop-warn.test.ts": [
    "read-tier: non-serializable result → single \"ok\" audit + visible [mcp-warn], not a fake error",
    "confirm-tier (approved): non-serializable result → single \"ok\" audit + visible [mcp-warn]",
    "regression: JSON-serializable result still audits once \"ok\" with the real output",
    "regression: a genuine callTool failure still audits once \"error\""
   ],
   "cli/mcp-broker-wire.test.ts": [
    "connects and lists tools under server__tool format",
    "skips connect when already connected (isConnected:true)",
    "does not render tools retained from a server absent in the latest live set",
    "approved confirm → callTool called + audit outcome ok",
    "rejected confirm → callTool NOT called + audit outcome cancelled",
    "broker throws → result [mcp-error] + audit outcome error",
    "unknown namespaced name → no confirm, no callTool, unknown-tool audit",
    "confirm receives McpConfirmAction with correct shape and tier ≥ confirm",
    "/mcp list with TTY opts → namespaced tools output, no provider call",
    "layoutEnabled:true does not interfere with /mcp output delivery",
    "mcpConfirm returns false → [mcp] cancelled output, REPL continues",
    "mcpConfirm returns true → bridge.dispatch proceeds (call dispatched)",
    "parses a JSON object argument",
    "parses key=value tokens",
    "returns {} for empty / unparseable input",
    "list → connects then renders the namespaced tool catalogue",
    "bare /mcp (no subaction) defaults to list",
    "call <tool> [args] → dispatches through the broker with parsed args",
    "call without a tool name → localized usage notice (en), no dispatch",
    "broker error is caught without exposing raw connection details",
    "unknown subaction → localized notice differs by language (i18n tr/en)",
    "injected bridge: /mcp lists tools without a provider round-trip",
    "injected bridge: /mcp call with a rejecting confirm → cancelled, REPL stays alive",
    "mcpBridge:null injection → honest no-server notice (chat.mcp_not_wired)",
    "prior success followed by config removal does not render the stale catalogue",
    "no .mcp.json under projectRoot → honest no-server notice (real loadMcpServers)",
    "trusted .mcp.local.json → connects with NO flag, no disabled-notice",
    "git-tracked .mcp.json (project scope) but mcp_client_enabled absent → honest disabled-notice, no bridge",
    "git-tracked .mcp.json and mcp_client_enabled true → gate opens (neither notice is shown)"
   ],
   "cli/mcp-client-gate.test.ts": [
    "truth-table: only an explicit true opts in",
    "off → initReplMcpBridge returns null, no connection attempted",
    "on → initReplMcpBridge composes a bridge but still does not auto-connect",
    "is declared in the config schema and passed through the resolved-literals (born-464 pattern)",
    "is documented as a supported option somewhere under docs/",
    "run.tsx (native-agent default REPL path) consults the smart-split gate",
    "chat-native.ts (legacy REPL loop) consults the smart-split gate"
   ],
   "cli/mcp-command.test.ts": [
    "add → writes stdio entry to .mcp.json (project scope default)",
    "add http URL auto-detects transport=http and writes to .mcp.json",
    "list → lists all merged servers across scopes",
    "list (empty) → friendly i18n message, no exception",
    "remove → deletes entry from project scope file",
    "--scope local honored — add writes to .mcp.local.json (not .mcp.json)",
    "--scope user honored — add writes to $HOME/.deckent/mcp.json",
    "get → prints details of registered server",
    "get unknown name throws not-found error",
    "remove unknown name throws not-found error",
    "add --transport http requires URL target",
    "add --scope invalid throws",
    "CLI wiring — registerMcp wires `mcp` as a subcommand",
    "CLI wiring — `mcp` subcommand description is non-empty",
    "add then remove round-trip — file ends without the entry",
    "add with --env writes env map",
    "list precedence — local entry wins over project for same name (via loadMcpServers)"
   ],
   "cli/mcp-connect-plan.test.ts": [
    "no MCP config anywhere → no connect, no notice",
    "trusted scope (.mcp.local.json) → connects even with the flag OFF, no notice",
    "project scope (git-tracked .mcp.json) with flag OFF → no connect, notice shown",
    "project scope with flag ON → connects, includes project scope, no notice",
    "trusted AND project, flag OFF → trusted connects AND notice for the skipped project scope",
    "trusted AND project, flag ON → connects everything, no notice",
    "a project server shadowed by a same-named trusted server → no notice (it connects anyway)"
   ],
   "cli/mcp-tool-count.test.ts": [
    "returns a positive number from the tool registry",
    "returns the same value as the DECKENT_MCP_TOOL_COUNT re-export",
    "never throws — graceful when called multiple times",
    "DECKENT_MCP_TOOL_COUNT is a number (backward-compatible re-export)"
   ],
   "cli/memory.test.ts": [
    "prints error when memory.db does not exist",
    "shows statistics for populated DB",
    "shows zero total for empty DB",
    "prints error when memory.db does not exist",
    "exports all guarded Memory V2 projections from DB",
    "forwards resolved project memory_export limits to the guarded CLI writer",
    "creates exports directory if it does not exist",
    "holds a partial non-empty DB before it replaces richer ADR snapshots",
    "prints error when DB already exists",
    "prints error when no exports directory exists",
    "rebuilds DB from decisions.md export",
    "imports from original DECISIONS.md when exports are empty",
    "prints error when memory.db does not exist",
    "lists relations between entries",
    "returns a versioned bounded view and reaches a deferred complete entry only through its detail reference",
    "holds rather than treating an empty recall query as an unbounded corpus read"
   ],
   "cli/messages-completeness.test.ts": [
    "follows a static named relative re-export only to a frozen in-repo catalog",
    "rejects malformed, non-frozen, empty, missing, and cyclic static re-export targets",
    "parses a non-trivial number of MESSAGES entries (parser sanity)",
    "every key has both en and tr translations (key set parity)",
    "no empty string values in either language",
    "{param} placeholder sets match between en and tr for each key"
   ],
   "cli/messages-pending-keys.test.ts": [
    "no longer defines a local LOCAL_MESSAGES fallback map",
    "no longer defines a local healthMsg() helper",
    "imports getMessage from the shared i18n module",
    "every health.* label call site goes through getMessage(...)",
    "%s resolves to a non-empty, non-key-echo string in en",
    "%s resolves to a non-empty, non-key-echo string in tr",
    "logged_in / logged_out / unknown differ between en and tr (genuinely localized, not copy-pasted)",
    "names authentication distinctly from a terminal session and preserves other labels",
    "a pre-existing, unrelated key still resolves exactly as before",
    "a genuinely unknown key still echoes the key itself (fallback contract intact)",
    "an unsupported lang falls back to the en template, not a crash"
   ],
   "cli/messages-round6-keys.test.ts": [
    "%s resolves to a non-empty, non-key-echo string in en",
    "%s resolves to a non-empty, non-key-echo string in tr",
    "%s is exactly what run.tsx buildLiveFooterLabels injects for its field (the mechanism owns no default set)",
    "the key → field map covers every LiveFooterLabels field",
    "healthy / degraded / unknown / logged_in / logged_out differ between en and tr (genuinely localized)",
    "connect.step.install_cli interpolates {provider} and {instruction}",
    "connect.step.login interpolates {provider}",
    "connect.step.mcp_unsupported interpolates {host}",
    "connect.step.attach_mcp interpolates {host}",
    "connect.step.ide_cursor_setup takes no params and resolves in both langs",
    "connect.step.ide_terminal_guidance takes no params and resolves in both langs",
    "connect.step.wsl_recommended interpolates {shell}",
    "every connect.step.* key genuinely differs between en and tr",
    "a pre-existing, unrelated key still resolves exactly as before",
    "a genuinely unknown key still echoes the key itself (fallback contract intact)",
    "an unsupported lang falls back to the en template, not a crash"
   ],
   "cli/messages-round7-keys.test.ts": [
    "%s resolves to a non-empty, non-key-echo string in en",
    "%s resolves to a non-empty, non-key-echo string in tr",
    "every tui.mode_* key genuinely differs between en and tr",
    "resolveModeLabel renders the en rows from an injected set (the mechanism owns no fallback since TERMINAL-TOOLS-002)",
    "resolveModeLabel prefers an injected label over the fallback (real caller path, unaffected by these keys)",
    "plan_nl.preview_banner resolves to a non-empty, non-key-echo string in en and tr",
    "plan_nl.preview_banner en text is byte-identical to formatPlanNlPreview\\'s real banner line",
    "plan_nl.preview_banner genuinely differs between en and tr",
    "plan_nl.backup_created interpolates {path} in en and tr",
    "plan_nl.backup_created genuinely differs between en and tr",
    "a pre-existing, unrelated key still resolves exactly as before",
    "round-6\\'s own keys still resolve exactly as before (no cross-round collision)",
    "a genuinely unknown key still echoes the key itself (fallback contract intact)",
    "an unsupported lang falls back to the en template, not a crash"
   ],
   "cli/messages-round8-keys.test.ts": [
    "%s resolves to a non-empty, non-key-echo string in en",
    "%s resolves to a non-empty, non-key-echo string in tr",
    "every approval_card.* text key genuinely differs between en and tr",
    "%s resolves to a non-empty, non-key-echo string in en and tr",
    "every approval_card.risk_* key genuinely differs between en and tr",
    "approval_card.progress is the identical numeric-notation template in en and tr (same precedent as tui.confirm_progress)",
    "%s resolves to a non-empty, non-key-echo string in en and tr",
    "every do.* plain key genuinely differs between en and tr",
    "%s interpolates its variable(s) in en and tr",
    "do.preview_banner_run/dry_run genuinely differ between en and tr",
    "do.cancelled / do.finished genuinely differ between en and tr",
    "do.task_go_criteria keeps the \"goCriteria\" schema-field name identical in en/tr by convention (technical term, not translated — same category as tui.confirm_progress)",
    "en preview banner (run) is byte-identical to the canonical formatter first line",
    "en preview banner (dry-run) is byte-identical to the canonical formatter first line",
    "the canonical formatter genuinely localizes the full preview",
    "a pre-existing, unrelated key still resolves exactly as before",
    "round-8\\'s telegram dependency (355-003) reused pre-existing keys, unaffected by this task\\'s additions",
    "round-7\\'s own keys still resolve exactly as before (no cross-round collision)",
    "a genuinely unknown key still echoes the key itself (fallback contract intact)",
    "an unsupported lang falls back to the en template, not a crash"
   ],
   "cli/messages-round9-keys.test.ts": [
    "%s resolves to a non-empty, non-key-echo string in en and tr",
    "every doctor.fix_* plain key genuinely differs between en and tr",
    "%s interpolates its variable(s) in en and tr",
    "every doctor.fix_* interpolated key genuinely differs between en and tr",
    "en \"nothing to repair\" text is byte-identical to formatDoctorFixLines([])\\'s real line",
    "en dry-run output is byte-identical to formatDoctorFixLines(actions)\\'s real lines",
    "en applied-with-no-failures output is byte-identical to formatDoctorFixLines(actions, results)\\'s real lines",
    "en applied-with-a-failure output is byte-identical to formatDoctorFixLines(actions, results)\\'s real lines",
    "a pre-existing, unrelated key still resolves exactly as before",
    "round-8\\'s own keys still resolve exactly as before (no cross-round collision)",
    "round-7\\'s own keys still resolve exactly as before (no cross-round collision)",
    "a genuinely unknown key still echoes the key itself (fallback contract intact)",
    "an unsupported lang falls back to the en template, not a crash"
   ],
   "cli/messages-writer-lease.test.ts": [
    "renders English with tool + pid filled",
    "renders Turkish with tool + pid filled"
   ],
   "cli/mode-command.test.ts": [
    "prints current mode from config",
    "defaults to sprint when deckent_style is not set",
    "writes sprint mode to project config",
    "writes task mode to project config",
    "detects sprint mode when git + DIRECTIVES exist",
    "detects task mode when git is missing",
    "sets global config for valid style",
    "rejects invalid style"
   ],
   "cli/mode-help.test.ts": [
    "lists sprint, task, process and auto in the top-level help text",
    "fixes the stale \"global\" subcommand description to include process",
    "appends the RUN-RENAME pre-note to the top-level help",
    "the RUN-RENAME note is available in Turkish and differs from the English copy"
   ],
   "cli/mode-run-alias.test.ts": [
    "bridges \"sprint\" to the \"run (sprint)\" label",
    "passes through \"task\" and \"process\" unchanged",
    "writes deckent_style: \"sprint\" to project config, same as `mode sprint`",
    "still writes deckent_style: \"sprint\" with the original confirmation message",
    "keeps the pinned \"Current: sprint\" line AND adds the \"run (sprint)\" bridge line",
    "does not add a bridge line for \"task\"",
    "does not add a bridge line for \"process\"",
    "presents the modes as \"run (sprint) | task | process\"",
    "lists the new `run` subcommand alongside the existing ones"
   ],
   "cli/models-i18n.test.ts": [
    "every models output row exists in en and tr and the two differ where the text is prose",
    "carries no raw SGR color literal and no dim",
    "carries no English sentence inside a print() call (rows come from the catalog)",
    "the provider list quoted in --provider help comes from the registry, not a literal map",
    "renderModelsTable localizes the empty message and the column headers",
    "sourceBadge and colorTier paint through palette roles (94 for code/info, never dim)"
   ],
   "cli/models-interactive.test.ts": [
    "activate and deactivate take an optional model and provider and resolve them through the shared choice flow",
    "the interactive rows exist in en and tr",
    "picker.ts takes its cell helpers from cursor-model, not from the Ink status row"
   ],
   "cli/models-json.test.ts": [
    "builds a deterministic filtered catalog document with a digest",
    "uses code-point ordering and ignores runtime property insertion order in its digest",
    "separates catalog-relative permission from unknown active records",
    "renders a typed hold without requiring a catalog"
   ],
   "cli/models-policy-read.test.ts": [
    "active-set JSON reads one strict snapshot and one offline catalog without mutation",
    "active-set JSON fails closed before catalog loading when authority is unavailable",
    "reads one provider when mode is omitted and never mutates policy",
    "retains the explicit two-argument mutation path"
   ],
   "cli/models.test.ts": [
    "renders header + separator + rows for each model",
    "returns a \"No models found\" message when list is empty",
    "finds a model by id",
    "finds a model by apiId",
    "returns undefined when model is not found",
    "labels remote source as \"live\"",
    "labels cache source as \"cached\"",
    "labels bundled source as \"bundled\"",
    "returns a padded tier label",
    "handles premium_plus tier",
    "lists all models when no provider filter is given",
    "filters models by provider when --provider is specified",
    "emits one ANSI-free JSON catalog document and preserves offline loading",
    "shows a \"no models\" message when provider filter matches nothing",
    "shows warnings when catalog has non-fatal issues",
    "reports error and sets exit code on catalog failure",
    "keeps JSON failure stdout machine-readable and does not expose the raw error",
    "calls loadCatalog with forceRefresh: true",
    "prints model count and source badge after refresh",
    "reports error when refresh fails",
    "prints tier info for a known model",
    "reports error and sets exit code when model is not found",
    "looks up a model by apiId"
   ],
   "cli/native-agent-bridge.test.ts": [
    "matches canonical delivery and typed budget-exhausted code",
    "surfaces withheld delivery on a successful execution without marking failed",
    "does not emit operator signal for ordinary successful delivery",
    "localizes terminal restoration HOLD guidance in %s without executing the tool",
    "close aborts a parked top-level permission decision and runs no handler",
    "nested permission never falls back to raw confirm and rechecks a changed deny before effect",
    "persists a compact checkpoint through the real engine-session-store chain at the canonical project path",
    "streams text via output and ends the turn",
    "relays the exact request-admission event without claiming transport success",
    "clears only the cached last-request display attribution after successful hydration",
    "asks the confirm-queue on a side-effecting tool, then executes it on \"y\" (real write)",
    "reports only canonical tool execution, then clears the matching activity after its result",
    "a \"n\" answer denies the tool (no write) and feeds a rejection back",
    "accrues usage and emits a cost advisory once a configured ceiling trips",
    "does not emit a cost advisory when no ceiling is set",
    "uses the injected localizer for the permission summary",
    "resolveCostCeilingUsd: env override wins, config is the fallback, invalid → undefined",
    "records the transcript after a completed turn when a recorder is provided",
    "hydrateTranscript nextTurnIndex continues recordTurn numbering across a resume (564-004)"
   ],
   "cli/native-agent-budget-wiring.test.ts": [
    "passes the config-resolved profile to engine construction",
    "%s renders non-empty, distinct en/tr messages",
    "renders the remaining-budget hint in both languages",
    "falls back to the raw mechanism message for an unknown code"
   ],
   "cli/native-agent-scratch-wire.test.ts": [
    "threads scratch ids + a non-empty checkpointInstruction into createAgentSession",
    "omits scratch entirely from createAgentSession deps when no scratch ids are supplied",
    "threads the caller-owned contentStore into createAgentSession by identity (564-002 hand-completion)",
    "persists a permission-auto-decision event to the durable audit sink",
    "falls back to tenantId \"local\" when no scratch ids were supplied",
    "engine.close delegates to session.close with the given options",
    "config absent -> false regardless of env",
    "config-off -> false regardless of env",
    "config-on + env unset -> true",
    "config-on + DECKENT_TRACE=0 -> false (env may only force OFF)",
    "config-off + DECKENT_TRACE=1 -> still false (env can never force ON)",
    "enabled=false never touches the filesystem",
    "enabled=true writes the trace file mode 0600 (posix only)"
   ],
   "cli/native-boot-policy.test.ts": [
    "keeps stable secret-free terminal error codes",
    "does not instantiate the deferred legacy adapter on native-session teardown",
    "closes an already-instantiated deferred adapter exactly once",
    "resolves every new operator notice in English and Turkish",
    "persists native and legacy-host picker identities without authority drift",
    "orders native admission before every session resource and keeps model-only intent explicit",
    "keeps Ink legacy construction lazy and rejects non-Ink native intent before construction"
   ],
   "cli/native-budget-renewal-surface.test.ts": [
    "renders exactly ONE offer line per exhaustion, not one per send",
    "offers again for a NEW exhaustion after an explicit renewal (epoch advanced)",
    "the offer gate is localized and key-resolving in both languages",
    "calls the engine seam and confirms with the new epoch",
    "answers honestly when the engine has no renewal seam (legacy loop path)",
    "ignores anything that is not /renew",
    "the wrapped engine answers /renew locally and passes everything else through",
    "both /renew labels resolve in en and tr (no hardcoded strings)"
   ],
   "cli/native-default-flip.test.ts": [
    "default (no config, no flag) → native selected",
    "unrelated argv + empty config → still native (default)",
    "rollback path 1: config terminal.native_agent=false → legacy selected",
    "config terminal.native_agent=true (explicit) → native selected",
    "rollback path 2: --legacy-loop CLI flag → legacy selected",
    "--legacy-loop wins even when config explicitly sets native_agent=true",
    "both rollback paths set simultaneously → still legacy (no contradiction)",
    "`deckent --legacy-loop` (flag-only, no subcommand) still routes to the default REPL",
    "a real subcommand alongside --legacy-loop still defers to Commander (unchanged routing contract)"
   ],
   "cli/native-elapsed.test.ts": [
    "reports a non-negative elapsedMs and forwards tokens",
    "omits tokens when outputTokens is undefined"
   ],
   "cli/native-grep.test.ts": [
    "normalizes CRLF and legacy CR to logical lines",
    "rejects files larger than the byte limit without full allocation",
    "finds a match in a 1.2MB file with 12–20KB lines and elides the preview",
    "stops collecting inside the scanner when maxHitsRemaining is reached",
    "reports too-large instead of silent skip",
    "reports binary instead of no-match",
    "reports read-error for missing files",
    "never claims no matches when files were skipped",
    "emits typed skip reasons even when there are zero hits",
    "elides excess skipped-file notes while keeping counts honest",
    "includes cap metadata when hits exist but output was truncated"
   ],
   "cli/native-mcp-ingress.test.tsx": [
    "routes bare/list and localized unknown in %s without provider fallback",
    "parses %s and preserves the explicit confirm denial",
    "gates call before bridge connect/dispatch and delegates no-bridge honestly",
    "renders typed empty list state in %s",
    "describes configured-zero without the obsolete not-wired roadmap claim (%s)",
    "native composition distinguishes no-config and disabled at command time (%s)",
    "legacy absent-bridge copy is truthful and has no roadmap claim (%s)",
    "keeps healthy tool output and reports partial connection in %s",
    "does not expose a thrown connection error",
    "reports connected-no-tools together with a partial failure",
    "mounted native App dispatches list once and never falls through to provider",
    "keeps the project-only disabled notice available at command time (%s)",
    "mounted native App denies call in Ask before callback and no-bridge uses registry fallback",
    "does not claim unrelated or legacy slash input",
    "refuses a stale call after config removal or refresh failure",
    "keeps explicit legacy bridge compatibility without invented observation"
   ],
   "cli/native-mock-adapter.test.ts": [
    "returns a scripted adapter that replays the mock script, ignoring real transport"
   ],
   "cli/native-parity-gate.test.ts": [
    "every declared divergence names both sides and carries a real rationale (not a hand-wave)",
    "legacy and native surface the identical final reply text for a plain conversational turn",
    "confirm(allow): both engines dispatch the tool exactly once with identical args, then reach the same final reply",
    "confirm(deny): both engines skip real execution and still recover with a final reply — denial MARKER TEXT diverges (see KNOWN_DIVERGENCES \"tool-denial-marker-text\")",
    "turn-2\\'s outbound request carries turn-1\\'s user+assistant content — role/content sequence is byte-identical across both engines",
    "pre-call provider failure: native ALWAYS recovers inline; legacy recovers ONLY when gracefulErrors is opted in",
    "divergence evidence: legacy default (gracefulErrors unset) RETHROWS a pre-call provider failure — native never does",
    "divergence evidence: legacy gracefulErrors does NOT protect a MID-STREAM failure (output already emitted) — native has no such gap",
    "both surface the same token numbers via onTurnEnd, but the callback payload SHAPE diverges (see KNOWN_DIVERGENCES \"onturnend-stats-shape\")"
   ],
   "cli/native-propose-run-bootstrap.test.ts": [
    "bootstraps the provider registry BEFORE controller.proposeRun when the registry is empty",
    "a bootstrap fault falls through to the existing honest [mcp-error] path — no throw escape, no new error shape",
    "skips the bootstrap seam entirely when the registry already has providers",
    "does not double-bootstrap on a second call once the registry becomes populated (idempotent at the seam)"
   ],
   "cli/native-provider-setting.test.ts": [
    "the REPL re-exports the core array (one identity, no copy)",
    "accepts every native transport the picker can save as default",
    "refuses a typo or a non-string with a message naming the valid values",
    "native_provider has metadata with the native names as options",
    "brain/chat/worker/fallback provider options equal VALID_PROVIDERS_ALL and their type strings name every provider"
   ],
   "cli/native-read-file-modes.test.ts": [
    "derives the per-line share from the tool-result cap and clamps overrides",
    "returns null for the legacy argument shapes so they stay byte-identical",
    "infers search mode from pattern and ignores malformed numbers instead of guessing",
    "lists Markdown headings with line numbers outside fenced code, plus size/line/longest-line stats",
    "pages headings deterministically under a tiny budget and names the continuation",
    "says so honestly when a file has no headings",
    "elides the 20 KB line behind an explicit marker and keeps the whole result under the cap",
    "continues an elided line from the named lineByteOffset and reaches its end",
    "stops at the byte budget with hasMore + nextStartLine instead of truncating silently",
    "answers an out-of-range start honestly (empty range, never clamped)",
    "boundLine never splits a multi-byte character and respects the per-line budget exactly",
    "returns grep -n style hits with bounded context and honest counts",
    "supports literal + ignoreCase, caps matches and names the resume line",
    "reports an invalid regular expression instead of throwing",
    "stays under the byte budget with many matches",
    "keeps deckent_read_file silent-tier + core exposure and advertises the views in its schema",
    "honours a caller-supplied readFile budget",
    "keeps the legacy full read and ranged read byte-identical",
    "keeps the dispatcher containment as the single gate"
   ],
   "cli/native-read-ranged.test.ts": [
    "returns the raw content byte-identically when neither offset nor limit is given",
    "degrades a malformed offset/limit to the default full read instead of guessing a slice",
    "an oversized default read stays honestly truncated AND now reports totalLines",
    "returns exactly the requested window, cat -n numbered, with a meta line",
    "offset alone reads to EOF and reports hasMore=false",
    "limit alone starts at line 1",
    "an out-of-range offset is an honest empty result plus meta, never a clamped slice",
    "refuses an out-of-scope path in ranged mode exactly as in default mode",
    "reports a missing file honestly in ranged mode",
    "three slices cover the file completely and their union equals the whole file",
    "resolveReadFileRange: null means \"default full read\"",
    "splitFileLines: a trailing newline terminates the last line, empty file has zero lines",
    "renderRangedRead: reports totalLines/returned/hasMore for the requested window"
   ],
   "cli/native-reasoning-surface.test.ts": [
    "%s exists in en and tr, distinct, non-empty",
    "applySignalVars interpolates plain vars and localizes the recovery action through the catalog",
    "%s is a recognized native-agent signal in both languages",
    "the new native error code is part of the boot/switch contract and localizes in both phases",
    "emits a string-free thinking event with the injected label and approx tokens, cleared when visible text arrives",
    "without a consumer the indicator is inert (no throw, no output)",
    "%s: renders the exhaustion notice with tokens, ceiling and the localized recovery action",
    "%s: a typed transport failure names the real code and the retry count",
    "sends reasoning off + the config-resolved transport retry, ceiling = visible reserve",
    "exposes a descriptor capability that resolves from live /props evidence and rides through the admission wrapper",
    "owner config outranks the probe; invalid config is a typed selection refusal"
   ],
   "cli/native-repl-wire.test.ts": [
    "returns \"claude\" when neither chat_provider nor brain_provider is set",
    "returns \"claude\" when config is null/undefined (defensive default)",
    "honors chat_provider override — codex",
    "honors chat_provider override — ollama (local LLM)",
    "falls back to brain_provider when chat_provider is unset",
    "chat_provider wins over brain_provider when both are set",
    "returns \"claude\" for out-of-set values (defensive)",
    "builds a working claude adapter that returns real text via injected spawn",
    "codex adapter spawns the codex binary (not claude)",
    "streams chunks as they arrive and yields a final done marker",
    "throws a clear error for an unknown provider name (no silent skeleton fallback)",
    "builds an ollama adapter that talks to the HTTP server via injected fetch",
    "ollama adapter surfaces HTTP errors (no silent skeleton)"
   ],
   "cli/native-slash-bridge.test.ts": [
    "bare /recall resolves to a localized typed prompt instead of dispatch or provider passthrough",
    "passes through plain chat text (not a slash line)",
    "passes through an unknown slash command (preserves prior plain-text behavior)",
    "passes through commands app.tsx already special-cases earlier in handleSubmit",
    "/help renders the full trust-badged catalog in the context language (en), not plain text",
    "/help renders the Turkish header and descriptions for a `lang: tr` context (same production chain)",
    "/help consumes the mounted capability registry and hides routable compatibility entries",
    "slash completion menu hides compatibility entries while direct routing remains available",
    "/help does not advertise native busy controls when the mounted registry lacks them",
    "/kill resolves to a real agentic dispatch (not a no-op enqueue)",
    "/cleanup and /recover also resolve to agentic dispatch",
    "/sync (a confirm-tier, non-always tool) also resolves to agentic dispatch",
    "/directives set <content> resolves to agentic dispatch with the content arg",
    "/nervous renders an honest reply (no pending file — empty-state message)",
    "/interrogate renders questions when DIRECTIVES.md exists",
    "/interrogate degrades honestly when DIRECTIVES.md is missing (no crash, no silent drop)",
    "bare /directives shows DIRECTIVES.md content when present",
    "/mcp resolves to an honest not-wired reply instead of a silent drop",
    "/autonomous status resolves to a read-only agentic dispatch",
    "synthesizes one canonical command line for normal dispatch and gating",
    "releases every canonical control, including argument-bearing steer",
    "cancels locally and leaves non-prompt input unchanged",
    "exposes a callable setApprovalMode on the returned engine",
    "a confirm-tier tool still asks under the default (suggest) mode",
    "/approve full-auto (via setApprovalMode) skips the y/n prompt for a confirm-tier tool",
    "setApprovalMode(full-auto) does NOT bypass an always-floor tool"
   ],
   "cli/native-stabilization-proof.test.ts": [
    "isNativeAgentEnabled resolves ON for the config this suite proves against",
    "dispatches queued lines to nativeEngine in exact submit order",
    "a line submitted WHILE nativeEngine is awaiting is queued and dispatched next, not dropped",
    "an immediate resubmission of the same trimmed text swallows, never double-dispatches",
    "the dupe guard resets after a turn dispatches — a deliberate later repeat is not swallowed",
    "cancel() (the Canceller app.tsx:897 wires into applyInterrupt) empties the queue",
    "resets the dupe guard so the same text is queueable again right after a cancel",
    "a /steer note submitted mid-turn is dispatched BEFORE a real message queued in the same turn",
    "prose lines, a fenced code block, and a table all emit correctly through the native path",
    "reassembles a Turkish+emoji line fed through nativeEngine output() one byte at a time",
    "recreateSegmenter() (the clearScreen FIX-3 mechanism) starts clean; the old partial never stitches onto post-clear text",
    "an emoji built via insert/move survives the whole submit→dequeue→nativeEngine(line) round-trip unbisected",
    "a long emoji-heavy line queued through submit() truncates without bisecting a surrogate pair"
   ],
   "cli/native-tool-registry.test.ts": [
    "registers the exec tools with native tiers (read→silent, write→confirm, bash floor stays confirm)",
    "exec handler runs the real dispatcher with NO internal confirm (single gate), mapping string→ToolResult",
    "threads opts.contentStore into the SHARED exec dispatcher — an over-cap read spills its full bytes there (564-002 hand-completion)",
    "marks a dispatcher error string as ok:false",
    "registers the CLI-bridge tools too (deckent_status is silent/read)",
    "exposes the memory query continuation and detail-reference schema to the actual native registry",
    "registers MCP bridge tools as confirm-tier ToolDefinitions (single-gate dispatch)",
    "registers the 3 progressive-disclosure meta-tools when fed the config-resolved default (tool_surface default-ON)",
    "omitting toolSurface entirely still registers none of the meta-tools (local opt-out default unchanged)",
    "an explicit config-resolved tool_surface { enabled: false } (opt-out) registers none of the meta-tools"
   ],
   "cli/native-transport-limit.test.ts": [
    "keeps the provider-neutral typed error available through the legacy CLI export",
    "refuses an Ollama selection with no context authority (typed, no 24k literal — 7086/560-001)",
    "JSDoc above resolveContextBudgetTokens documents typed refusal — no literal fallback ceilings (7086/560-001)"
   ],
   "cli/native-transport-local.test.ts": [
    "resolves the canonical local_llm authority returned by loadConfig",
    "resolves the real streaming tool dispatcher under the local-llm identity and surfaces health",
    "never treats an OpenAI base URL as local-llm selection authority",
    "reports an unhealthy endpoint without changing provider identity"
   ],
   "cli/native-transport-measurement-root.test.ts": [
    "strips OpenAI-compatible /v1 from %s",
    "calls /tokenize and /apply-template at the server root when endpoint ends with /v1",
    "typed probe reports http-404 when root tokenize is missing",
    "typed probe reports exact-available when root routes succeed",
    "hosted openai selection reports unsupported-endpoint",
    "sends the system prompt as a role:system message, OpenAI function tools and model on tokenize",
    "omits the tools field when the request carries no tools",
    "measures an identical request once: the request-digest cache serves the repeat with zero fetches",
    "typed probe tokenizes with the model name so router mode accepts it",
    "apply-template carries chat_template_kwargs.enable_thinking=false when reasoning is off",
    "apply-template carries chat_template_kwargs.enable_thinking=true when reasoning is on",
    "leaves chat_template_kwargs off when no reasoningControl authority exists",
    "boot probe apply-template uses the same reasoning-off wire fields",
    "boot probe bounds cooperative reasoningControl reject-on-abort within deadline",
    "boot probe times out when fetch ignores abort until the probe deadline",
    "boot probe returns within deadline when reasoningControl never resolves and ignores abort",
    "reports the boot probe before any request",
    "a later exact measurement overrides a timed-out or failed boot probe",
    "a failing measurement after a passing probe is reported, keeping a typed boot reason when there is one"
   ],
   "cli/native-transport-selection.test.ts": [
    "refuses without an API key (errorCode missing-api-key, no silent fallback)",
    "resolves with a .deck DECKENT_CLAUDE_API_KEY (ADR-G-005: .deck over env)",
    "resolves with env ANTHROPIC_API_KEY when .deck has no key",
    "rejects every legacy alias before provider credential lookup or adapter construction",
    "never ships a non-claude model id at the anthropic transport (incident guard)",
    "refuses an unrecognized non-claude model id instead of shipping it (errorCode unknown-model)",
    "rejects a claude-shaped id until catalog registration supplies authority",
    "rejects an inactive Fable 5.1 from the project store before credential use",
    "holds an unreadable project model authority before credential use",
    "resolves ollama from config host + keeps a local tag model",
    "refuses ollama without a host (errorCode missing-ollama-host)",
    "does not leak a claude alias from config into the ollama default",
    "resolves vendor presets from their key envs (deepseek) and refuses without",
    "honestly refuses subscription-CLI providers (codex/gemini)",
    "binds claude+fable from settings when a .deck key exists",
    "fails honestly (no detection fall-through) when the pinned provider cannot resolve",
    "keeps the detection order when no pin is set (env key → claude)",
    "detects ollama from config when nothing else is bound",
    "uses the explicit config override when valid",
    "refuses every provider family with no authority (7086/560-001 — no per-family literals)",
    "an invalid override is not authority — typed refusal, never a literal fallback",
    "maps canonical Claude IDs but never infers a provider from legacy aliases",
    "maps name:tag shapes to ollama and gpt/o-series to openai",
    "returns null for ambiguous ids (no silent re-route of vendor models)",
    "refuses a missing model selection with a typed error — no hardcoded fallback identity",
    "carries the exact selected model and a modelIdentity seam",
    "discovery parses exact published router IDs and tolerates failure as typed data",
    "validation verdicts: valid / unknown-model with published IDs / unreachable cold-start"
   ],
   "cli/native-turn-accounting.test.ts": [
    "accumulates usage across ALL rounds of a multi-tool-call turn, not just the last round",
    "a three-round turn sums all three rounds",
    "a single-round turn still reports exactly that round\\'s usage (no double-count from a fresh accumulator)",
    "bg-turns wiring: a multi-round real turn and a multi-round bg turn each report their OWN accumulated total — reused onTurnEnd never leaks totals across calls"
   ],
   "cli/nervous-accept-panic.test.ts": [
    "CLI accept-panic <task-id> writes a file-based IPC marker",
    "MCP deckent_nervous_subscribe surfaces pending panic events as PANIC_GUARD_KILL_PENDING",
    "MCP deckent_nervous_accept with id=\"panic:<taskId>\" writes IPC marker"
   ],
   "cli/nervous-accept-recommendation.test.ts": [
    "accepts a SPRINT_START recommendation from its recommendation store",
    "rejects a recommendation by unique prefix and durably records its reason",
    "preserves the typed unknown-id rejection when neither store has the id"
   ],
   "cli/nervous-command.test.ts": [
    "shows \"No pending notifications\" when none exist",
    "displays 3 pending notifications in table format",
    "accept with no live executor → dismissed (removed, no execution; APPROVE-007)",
    "rejects a pending notification with reason",
    "shows limited history records",
    "filters history by since duration",
    "log command starts without error and shows existing content",
    "shows help on unknown subcommand",
    "shows error when accepting non-existent notification",
    "uses ANSI escape codes for colored output",
    "lists open recommendations with action id + payload summary",
    "--all includes dismissed recommendations",
    "--dismiss flips an open recommendation to dismissed (persisted)",
    "--dismiss of an unknown id exits 1 with not-found",
    "default dashboard surfaces the Brain inbox section when open recs exist"
   ],
   "cli/nervous-edit-bridge.test.ts": [
    "edit with kv args writes IPC file with modifiedPayload",
    "edit with JSON payload writes IPC file with modifiedPayload",
    "edit removes notification from pending after IPC write",
    "edit with unknown id returns honest error message (en)",
    "edit with unknown id returns honest error message (tr)",
    "edit with missing payload args returns error without crashing",
    "edit with invalid JSON returns error without crashing",
    "edit with invalid kv arg returns error without crashing",
    "i18n: success message differs between en and tr",
    "edit with missing id returns id-required error",
    "edit TTY mode wraps success in ANSI green"
   ],
   "cli/nervous-enable.test.ts": [
    "flips nervous_system.enabled=true with the default balanced authority",
    "accepts an explicit authority preset and preserves unrelated keys",
    "rejects an invalid authority preset (sets exit code, does not enable)",
    "states the auto-apply window when a positive timeout is configured",
    "states auto-proceed is DISABLED when the timeout is <= 0"
   ],
   "cli/nervous-i18n.test.ts": [
    "dashboard empty-state is localized by --lang",
    "accept confirmation is localized (dismiss fallback — no live executor)",
    "history empty-state is localized",
    "REPL /nervous bridge is localized"
   ],
   "cli/nervous-ipc-route.test.ts": [
    "routes accept to the IPC queue when the executor is alive AND clears the durable pending entry (W0-TRUTH #491)",
    "dismisses accept with a warning when no executor is alive (no execution, no accepted history)",
    "integration: a routed approval reaches the real poller and runs the action handler"
   ],
   "cli/nervous-slash-applies.test.ts": [
    "accept: routes to the nervous-ipc queue when the executor is alive (disk-check nervous-state)",
    "reject: routes to the nervous-ipc queue when the executor is alive (unblocks its parked decision)",
    "accept: does NOT write to nervous-ipc and does NOT claim a fabricated success when no executor is alive",
    "reject: reports honestly when no executor is alive (no audit lie, no crash)",
    "a stale (>5s old) heartbeat is treated as dead, same as no heartbeat"
   ],
   "cli/nervous-tui-live.test.ts": [
    "renders all required TUI sections: header, Pending, Config",
    "pending count in output matches number of written notifications",
    "history --limit 5 shows exactly 5 records when 10 exist",
    "output contains ANSI escape codes and stripped content is valid structure",
    "invalid subcommand causes exit error (commander exitOverride)"
   ],
   "cli/nested-dispatch-honesty.test.ts": [
    "pre-fix bug: dispatchToolCall reports status:\"executed\" (execImpl never threw) — outer ok must honestly mirror the inner ok:false, and the inner error text must survive",
    "regression guard: a nested handler that itself succeeds (ok:true) still reports outer ok:true with a clean, unprefixed JSON body",
    "pre-fix bug: createParityExecImpl\\'s policy-deny throw landed in status:\"error\"/[mcp-error] — must now surface as [approval-denied] with status:\"denied\"",
    "regression guard: the PRE-EXISTING dispatch-level denial (no confirm seam supplied, risk over threshold) keeps its own [deckent-denied] tag unchanged",
    "pre-fix bug: the nested ask reused the top-level \"Run tool: X\" summary verbatim, with no hint that \"a\" here is one-time only",
    "regression guard: \"a\" still degrades to a one-time allow — the SAME nested call asks again right after (never persisted), matching the honest label",
    "pre-fix bug: toolSink was never called for the nested target — only the OUTER call_tool invocation reached it (via the loop\\'s own tool-result event, out of this resolver\\'s reach)",
    "regression guard: omitting toolSink entirely is a fully inert no-op — the nested dispatch still runs and returns normally"
   ],
   "cli/nl-dispatch-class-gate.test.ts": [
    "path 1/3 — Oku-direct: 'sprint durumu ne' dispatches deckent_status WITHOUT ever calling confirm",
    "path 1/3 — Oku-direct: recall intent (deckent_memory_query) also skips confirm entirely",
    "path 2/3 — Değiştir-confirm: 'sprint planla' still calls confirm; decline → cancelled, no dispatch",
    "path 2/3 — Değiştir-confirm: 'sprint planla' + approved confirm → dispatcher called",
    "path 3/3 — no-match passthrough: ordinary sentence bypasses the class-gate entirely",
    "Oku-tier match: 'recall the incident postmortem' dispatches deckent_memory_query directly, no confirm",
    "Değiştir-tier match: 'generate plan for the migration' still requires confirm before anything runs",
    "agenticDispatch: false (default) → unaffected, class-gate never runs"
   ],
   "cli/nl-dispatch-evidence.test.ts": [
    "regression-guard: \"beni sonra ara\" (call me later) → no memory-noun pairing, correctly no_match",
    "regression-guard: \"işten çıkınca beni ara lütfen\" (call me when you leave work) → no memory-noun pairing, correctly no_match",
    "regression-guard: \"bu duruma göre karar verelim\" (let\\'s decide based on this) → declined form, correctly no_match",
    "regression-guard: \"olağanüstü durumda ne yapmalı\" (what to do in an emergency) → declined form, correctly no_match",
    "regression-guard: \"I have a great memory for names\" → memory-noun with no search-verb, correctly no_match",
    "regression-guard: \"this laptop needs more memory\" → memory-noun with no search-verb, correctly no_match",
    "regression-guard: \"did you find the bug?\" → search-verb with no memory-noun, correctly no_match",
    "regression-guard: \"can you find my keys\" → search-verb with no memory-noun, correctly no_match",
    "regression-guard: \"let\\'s search for a new apartment\" → search-verb with no memory-noun, correctly no_match",
    "regression-guard: \"what\\'s the plan for tonight?\" → bare noun \"plan\", correctly no_match",
    "regression-guard: \"I need a diet plan\" → bare noun \"plan\", correctly no_match",
    "regression-guard: \"just checking on the status of my order\" → not the literal \"what\\'s/is the status\" phrase, correctly no_match",
    "regression-guard: \"how is sprint going for you these days?\" (small talk, not a status query) → correctly no_match",
    "regression-guard: \"how are we doing today, feeling ok?\" → correctly no_match",
    "regression-guard: \"hafızam çok zayıf bu aralar\" (my memory is weak lately) → memory-noun with no standalone search-verb, correctly no_match",
    "regression-guard: \"geçmiş olsun, tekrar dene\" (get well soon, try again) → not \"gecmis + sprint\" shape, correctly no_match",
    "dispatch-OLMAZ: \"merhaba nasılsın bugün hava çok güzel\" (greeting/weather chat) → correctly no_match",
    "dispatch-OLMAZ: \"let\\'s grab lunch tomorrow\" → correctly no_match",
    "dispatch-OLMAZ: \"thanks for the help earlier\" → correctly no_match",
    "dispatch-OLMAZ: \"kahve ister misin\" (want some coffee?) → correctly no_match"
   ],
   "cli/npx-compat.test.ts": [
    "src/cli/entry.ts has shebang #!/usr/bin/env node",
    "package.json bin.deckent points to ./dist/cli/entry.js",
    "package.json bin.deckent-mcp points to ./dist/mcp/server.js",
    "package.json exports has . entry with import and types",
    "package.json main points to ./dist/index.js",
    "package.json types points to ./dist/index.d.ts",
    "package.json has type: module for ESM",
    "package.json engines requires node >= 24 (Active LTS)",
    "package.json files includes dist"
   ],
   "cli/off-tty-autoapprove.test.ts": [
    "no flags → does not auto-approve",
    "unrelated flags → does not auto-approve",
    "--auto-approve → auto-approves",
    "--yes → auto-approves",
    "--auto-approve combined with other flags → auto-approves",
    "bare `deckent --auto-approve` still launches the default REPL (not Commander)",
    "bare `deckent --yes` still launches the default REPL (not Commander)",
    "`deckent --native --auto-approve` still launches the default REPL",
    "an explicit subcommand is unaffected (still routes to Commander)",
    "no flag: destructive write is denied, no file written (SEC fix)",
    "no flag: destructive bash is denied, never invoked",
    "--auto-approve: write proceeds exactly like the old blanket-approve behavior",
    "--yes: write proceeds exactly like the old blanket-approve behavior",
    "no flag: read-only tool is unaffected (reads never confirm-gated)"
   ],
   "cli/ollama-repl-error-i18n.test.ts": [
    "exists in en and tr, names the host and the reason, and differs between languages",
    "entry.ts resolves the hint through the catalog and carries no Turkish literal for it"
   ],
   "cli/onboard-apply-wire.test.ts": [
    "registers --apply, --dry-run and --yes alongside the pre-existing options",
    "never writes config.json (Smoke: exit clean, preview only)",
    "prints a preview report naming the config path, with no confirmation asked",
    "writes config.json with the plan fields, skipping confirmation",
    "prints a before/after field-change report and an applied confirmation",
    "merges onto an existing config.json — preserves unrelated keys",
    "never spawns a subprocess (no deckent init call, no global-scope path)",
    "applies when the fake-stdin answer is \"y\"",
    "cancels without writing when the fake-stdin answer is \"n\"",
    "cancels without writing on an empty fake-stdin answer (confirm default is false)",
    "--dry-run short-circuits before ever asking for confirmation",
    "renders both languages without leaking a raw unresolved i18n key"
   ],
   "cli/onboard-command.test.ts": [
    "registers --plan-only and --json options alongside the pre-existing ones",
    "calls runOnboardingWizard exactly once and prints a text report",
    "never spawns a subprocess (no deckent init call)",
    "prints valid JSON of the wizard result with --json",
    "is available directly via runOnboardPlanOnly (non-CLI entry point)",
    "renders every section with resolved (non-key) i18n text, both languages",
    "marks the present/logged-in provider [OK] and the missing one [--] or dim",
    "includes the config path from the plan",
    "mounts the Ink OnboardingWizardView with a resolveLabel bound to the wizard result",
    "prints the \"not applied\" notice after onApply fires, and never writes/spawns",
    "does not print the \"not applied\" notice on cancel",
    "does not call runOnboardingWizard or mount Ink when falling back to the old flow"
   ],
   "cli/onboarding-apply.test.ts": [
    "is pure — every non-undefined plan field becomes a fieldChange with previousValue undefined when no config exists",
    "captures the real previous value from an existing config object",
    "changed=false when the plan value already matches the existing value",
    "dryRunOnboardingApply produces the identical fieldChanges applyOnboardingPlan computes, and never writes",
    "a dry-run against a fresh project creates no config file at all",
    "writes a fresh config.json with every plan field, verified=true",
    "merges onto an existing config — preserves unrelated keys, only touches plan fields",
    "leaves no leftover .tmp file after a successful write",
    "a field absent from the plan (blocked provider selection) never appears in fieldChanges or on disk",
    "carries mcpAttachActions through without executing them",
    "restores the exact prior config, deleting keys that did not exist before the apply",
    "reverting a fresh-project apply removes every plan-written key entirely"
   ],
   "cli/onboarding-chat-flow.test.ts": [
    "recognizes yes words in tr and en",
    "recognizes no words in tr and en",
    "recognizes skip words in tr and en, and an empty reply",
    "matches a choice by value, case-insensitively",
    "matches a choice by 1-based index",
    "is deterministic — no LLM: unrecognized text with no matching choice is unknown",
    "an out-of-range index is unknown, not clamped",
    "walks provider_detect -> auth_status -> mcp_suggestion(yes) -> workspace_mode(scope,mode) -> config_plan",
    "wizard-reuse: the chat-driven result matches a plain runOnboardingWizard call with equivalent answers",
    "hayır declines MCP attach — mcpAttachActions empty even though hosts were suggested",
    "atla (skip) on mcp_suggestion also declines attach",
    "atla on scope/mode questions falls back to the wizard defaults (project/balanced)",
    "no MCP host suggested -> mcp_suggestion auto-advances straight to workspace_mode",
    "no authenticated provider anywhere -> honest blocked config-plan, flow still completes",
    "is deterministic — identical reply sequence yields deep-equal final results",
    "an unrecognized reply keeps the same question pending and records lastUnrecognizedReply",
    "a subsequent recognized reply clears lastUnrecognizedReply and proceeds",
    "the fallback seam is only invoked for unknown intents, and its resolution is applied — no real LLM wired by default",
    "the fallback seam is never invoked when the deterministic core already resolved the reply",
    "replyToOnboardingChat throws when there is no pending question",
    "a state serialized mid-flow resumes to the same final result as an uninterrupted run",
    "every state along the way is plain JSON data (no functions survive a stringify round-trip loss)",
    "suggests connecting a provider when the plan is blocked",
    "suggests attaching MCP later when declined, project-scope and balanced-mode nudges, and a fallback-provider nudge",
    "omits the project/balanced nudges once the user picks global scope + a non-default mode",
    "suggests adding a fallback provider when only one provider is authenticated",
    "every suggestion key is string-free (dotted identifier, never literal prose)"
   ],
   "cli/onboarding-wizard.test.ts": [
    "reuses discoverProviders — injected version probe drives present/version",
    "preserves ONBOARDING_PROVIDERS / DISCOVERABLE_PROVIDERS order",
    "merges the real auth probe result onto each discovered provider, preserving order",
    "reports unknown auth state without inventing a confident answer",
    "CLI not installed → host_not_installed, attach probe never called",
    "installed but host CLI does not support mcp subcommand → unsupported",
    "supported and already attached → already_attached, no suggestion",
    "supported, not attached → attach_suggested with real getAttachCommand reuse",
    "is string-free — no descriptionKey/label ever contains a space (proxy for \"not literal prose\")",
    "scope question offers exactly project/global, defaulting to project",
    "mode question offers every ALL_PLAN_MODES value, defaulting to balanced",
    "is string-free — every choice/prompt key is a dotted identifier, never literal prose",
    "defaults to project scope + balanced mode when no answers are given",
    "honestly reports undefined modePreset for a subscription-tier mode with no MODE_PRESETS entry",
    "global scope resolves via the real global-scope-resolver (real reuse, not invented)",
    "global scope on an unresolvable home falls back to projectRoot with an honest error, never a throw",
    "an unsupported platform falls back to projectRoot with an honest error, never a throw",
    "auto-picks the single authenticated provider for both brain and worker",
    "auto-picks in DISCOVERABLE_PROVIDERS order, next authenticated becomes fallback",
    "zero authenticated providers → honest blockedReasonKey, no invented provider",
    "a caller-supplied answer always wins over auto-pick",
    "never writes anything — applied is always the literal false",
    "computes configPath under DECKENT_DIR beneath workspace.root",
    "carries mode/language/projectName/providers/model_strategy into fields",
    "only suggested mcp attachments become mcpAttachActions",
    "propagates blockedReasonKey from provider selection",
    "produces the 5-step trace in order with no real I/O (every seam injected)",
    "end-to-end: healthy matrix picks claude as brain/worker, codex as fallback, plans 3 mcp attaches",
    "defaults projectName to basename(projectRoot) when omitted",
    "honors caller answers for scope/mode/provider selection end-to-end",
    "no authenticated provider anywhere → honest blocked config-plan, mcp suggestions still computed",
    "is deterministic — identical input yields deep-equal output on repeat calls"
   ],
   "cli/openrouter-probe.test.ts": [
    "prints an honest unavailable message and leaves process.exitCode at 0/undefined",
    "never calls fetchImpl when the key is absent (no live network attempt)",
    "never writes the cache file when the key is absent",
    "--json emits {available:false, reason} and stays at exit 0",
    "renders the Turkish unavailable message when getLangFn returns tr",
    "calls the injected fetchImpl exactly once against the OpenRouter models URL",
    "writes the free-model cache to .deckent/settings/openrouter-models.json (only \":free\" entries)",
    "prints a table summary with the free-model count and cache path",
    "--json emits available:true + count + models + cacheFile",
    "truncates the human-readable listing to MAX_LISTED_MODELS (10) and prints an \"N more\" line",
    "does NOT truncate the --json model array even past MAX_LISTED_MODELS",
    "resolves the key via the DECKENT_-prefixed convention too",
    "leaves process.exitCode at 0/undefined on a successful live probe",
    "sets process.exitCode = 1 and prints an honest fetch-failed message on a network error",
    "never writes the cache file when the live fetch fails",
    "--json emits {available:true, error} on a fetch failure",
    "registers a working `openrouter-probe --json` command",
    "buildProgram() registers openrouter-probe alongside the rest of the CLI surface"
   ],
   "cli/output.test.ts": [
    "writes message + newline to stdout",
    "writes empty string with newline",
    "writes special characters correctly",
    "writes Error instance message to stderr with prefix",
    "writes string error to stderr",
    "writes non-Error object as string",
    "writes number error correctly",
    "renders provider alias conflicts through the localized CLI message map",
    "returns all dots for 0%",
    "returns all hashes for 100%",
    "returns half filled for 50%",
    "uses default width of 8",
    "respects custom width",
    "clamps values below 0 to 0",
    "clamps values above 100 to 100",
    "handles width of 1",
    "renders headers and a single row",
    "includes separator line",
    "aligns columns by widest cell",
    "handles empty rows",
    "handles single column",
    "handles missing cells gracefully",
    "renders sprint number",
    "renders progress info",
    "renders alert count",
    "includes unicode box-drawing characters",
    "renders agent info when agents present",
    "shows DONE agent with full progress bar",
    "shows IDLE agent with empty progress bar",
    "uses --:-- when updatedAt is missing",
    "shows checkmark for passed checks",
    "shows cross for failed checks",
    "shows pass count in summary",
    "shows failure count when checks fail",
    "includes check name and message",
    "handles empty checks array",
    "includes sprint number in title",
    "shows results summary with metrics",
    "shows time duration",
    "includes next steps",
    "shows what went well section when no boundary violations",
    "shows task count when no metrics",
    "returns muted (plain, never dim) \"generic\" for undefined agent",
    "returns muted (plain, never dim) \"generic\" for explicit generic value",
    "returns cyan-highlighted label for specialized agent",
    "returns cyan-highlighted label for test-writer agent",
    "includes ANSI reset code",
    "shows agent label for worker with assignedAgent",
    "shows generic label for worker without assignedAgent",
    "shows generic label for worker with explicit generic agent",
    "uses cyan ANSI for specialized agent",
    "uses dim ANSI for generic agent",
    "shows stale warning when dashboard data is older than 60 seconds",
    "does not show stale warning when dashboard data is fresh",
    "shows Budget OK when entries are below warning zone",
    "resolves the budget from the canonical constants budget when input.memoryBudget is absent (owner finding 2026-08-27: no 600 literal)",
    "shows Budget warning percentage when entries exceed 80% of max",
    "shows Budget OVER with cleanup hint when entries exceed max",
    "shows no Budget line when projectRoot is not set",
    "shows Budget unreadable (not a false OK) when the DB exists but cannot be read (R6)",
    "shows CRITICAL alert with [!!] prefix and message text",
    "shows WARNING alert with [!] prefix and message text",
    "shows INFO alert with [i] prefix and message text",
    "truncates alert list at 10 with overflow count message",
    "shows all alert messages when 10 or fewer alerts",
    "returns false when no trigger is active",
    "trigger 1 — returns true when flagValue is explicitly true",
    "trigger 2 — returns true when NO_COLOR env is set",
    "trigger 3 — returns true when --no-color is in argv"
   ],
   "cli/pack-size-budget.test.ts": [
    "passes comfortably under budget (projected post-dead-asset-removal size)",
    "fails at a regression size over the 7 MB ceiling (7.5 MB)",
    "reports the largest packed files as topOffenders when the gate fails",
    "sorts descending by bytes and respects the limit",
    "does not mutate the input array",
    "captures fileSizes alongside the existing files list",
    "passes when entry/types/bin/dashboard files are all present in the tarball",
    "fails and names the missing bin file when an over-aggressive .npmignore strips it",
    "fails when the main entry point is declared but not packed",
    "fails when the dashboard JS bundle is missing from the tarball (assets/ narrowed away)"
   ],
   "cli/permission-parity.test.ts": [
    "a session grant recorded by a direct MCP call also skips the nested prompt",
    "full-auto runs confirm-tier MCP promptless but preserves deckent_kill always-floor",
    "a live explicit deny blocks both direct and nested MCP paths in full-auto"
   ],
   "cli/plan-interrogate.test.ts": [
    "asks 5 questions, confirm=true → writes revised draft to disk",
    "confirm=false → writeFileSync NOT called, original content returned",
    "all empty answers → original returned, confirmFn never called",
    "partial answers (some empty) → draft excludes empty answers",
    "uses i18n lang=tr for question text",
    "registers --interrogate option in the plan command",
    "--no-confirm skips interrogation even when --interrogate is set",
    "config.plan.interrogate=true triggers interrogation without --interrogate flag"
   ],
   "cli/plan-nl-adapter.test.ts": [
    "forwards goal and execution flags unchanged to the canonical do command",
    "fails honestly when the replacement command is not registered"
   ],
   "cli/plan-nl-cmd.test.ts": [
    "accepts arbitrary do arguments and forwards them without legacy planning or writes",
    "registers only the forwarding alias rather than legacy --write behavior"
   ],
   "cli/plan-preview-scope-gate-labels.test.ts": [
    "'fail': verdict header line first, then the gate message verbatim (line-split, '  ! ' prefixed)",
    "'fail': the message body is never re-worded — every source line survives byte-for-byte modulo the '  ! ' prefix",
    "overridden: a single notice line, no message lines",
    "clean (pass, not overridden): no lines at all — nothing invented",
    "en and tr verdict/notice labels genuinely differ (real localization, not passthrough)",
    "lang=${lang}: 'fail' fixture — the CLI preview contains every line formatScopeGateLines produces, verbatim",
    "lang=${lang}: 'overridden' fixture — the CLI preview contains the REPL's override notice verbatim",
    "lang=${lang}: clean fixture — neither surface renders any scope-gate text",
    "sanity: fail and overridden fixtures produce genuinely different CLI output (the verdict is not a static no-op)",
    "CLI dry-run preview of a scope-gate fail contains \"Scope gate: FAIL\" and \"--force-scope\"",
    "formatScopeGateLines degrades gracefully when scopeGateFailLabel is omitted: still renders the real message, no header",
    "formatScopeGateLines drops the overridden notice (not invented) when scopeGateOverriddenLabel is omitted"
   ],
   "cli/plan-surface-postadoption.test.ts": [
    "renders a Scope (write) column carrying the POST-adoption path, not the pre-adoption typo",
    "prints an honest post-adoption note after the table",
    "falls back to PRE-adoption scope + a \"final scope lives in task-JSON\" note when git ls-files fails",
    "warns the run would BLOCK (instead of inventing an adopted scope) when the write path is an unresolved suspect",
    "applies the SAN-2 auto-replace resolution to the previewed scope",
    "fails open to the pre-adoption scope when git ls-files throws",
    "returns the block message + pre-adoption scope (no fictitious adoption) when the gate would BLOCK",
    "never calls runSprint — preview-only, no spawn",
    "direction 1 — a new subdirectory under an ESTABLISHED tracked root is new-plausible (WARN, not BLOCK)",
    "direction 2a (typo-class) — a path that escapes the repo root still BLOCKs, even under an established root",
    "direction 2b (typo-class) — a suspicious character in the path still BLOCKs, even under an established root",
    "direction 2c (typo-class) — too many new directory levels still BLOCKs, even under an established root",
    "direction 2d (typo-class) — a new subdirectory under a THIN (barely-tracked) root still BLOCKs (regression guard)",
    "a fully invented top-level tree (no tracked ancestor at all) still BLOCKs"
   ],
   "cli/plan-yes-flag.test.ts": [
    "registers a --yes option",
    "--yes approves the exact plan and publishes PENDING tasks without prompting",
    "without --yes, the interactive confirm still gates approval (regression baseline)"
   ],
   "cli/planner-evidence-ingress.test.ts": [
    "${command} ${language} renders %s without raw evidence",
    "preserves the pre-existing unknown-error presentation"
   ],
   "cli/planner-evidence-presentation.test.ts": [
    "renders every allowlisted code and safe action in %s",
    "returns null for non-scope input"
   ],
   "cli/progress-reader.test.ts": [
    "summarizes valid lines with correct ordering + currentAction from the newest step",
    "currentAction falls back to the step name when detail is absent",
    "invalid JSON lines are skipped and counted, valid lines still summarized",
    "a JSON line missing required `step` or `ts` is corrupt, not fabricated",
    "blank / whitespace-only lines are dropped silently, not counted as corrupt",
    "keeps only the last tailSize valid events, oldest-first",
    "empty input -> empty summary, no throw",
    "missing dir -> empty result, no throw",
    "a readdirSync throw on an existing-looking dir degrades to empty result",
    "reads multiple worker progress files, keyed by taskId extracted from filename",
    "non-matching filenames in the dir are ignored",
    "an empty progress file yields an empty summary for that worker, not a throw",
    "corrupt lines within a real per-worker file are counted without breaking the summary",
    "tailSize option overrides the default of 5",
    "tails a large real file correctly while reading far fewer bytes than the file size"
   ],
   "cli/provider-authority-keyring.test.ts": [
    "reports an unprovisioned keyring as absent, not as a fault",
    "provisions a genesis revision and never prints key material",
    "refuses to overwrite an existing keyring",
    "rotates only against the exact expected revision hash",
    "refuses to rotate when nothing is provisioned",
    "doctor reports the keyring as a required pre-flight check",
    "offers the keyring remedy only for keyring-caused holds",
    "never writes into the project tree"
   ],
   "cli/provider-authority-process-runtime.test.ts": [
    "constructs one backend lazily from effective config and exposes only bounded invoke",
    "does not load config or open authority for unrelated commands",
    "opens once before %s, injects the exact result, and closes after settlement",
    "closes the process authority when command execution throws",
    "passes a healthy composition as ready for %s — never the empty-candidate hold",
    "binds canonical Brain identity and returns the shared authority HOLD"
   ],
   "cli/provider-limits-authoring-wire.test.ts": [
    "is a resolver view over the SAME registrations, not a second registry",
    "answers null for a canonical provider this host registered no source for",
    "reaches the proposal stage from the bootstrap-registered source, with nothing injected",
    "keeps the typed hold byte-identical when no registered source resolves",
    "still lets an injected resolver win over the host bootstrap",
    "wires the default Claude Docker source through effective config without eager dispatch",
    "turns default config/factory failure into the typed source refusal",
    "carries effective config through the lazy factory and exact registry slot to one bounded call",
    "keeps native negative outcome %s non-live",
    "does not dispatch for null transport, missing budget, or wrong execution profile",
    "lets limits init pass the default Claude Docker source gate without eager Docker work"
   ],
   "cli/provider-observation-adoption-receipt.integration.test.ts": [
    "exposes production adopt-runtime as a dry-run that fails closed without verified live-runtime authority",
    "proves inspect → dry-run → apply → fresh-process replay with a canonical durable receipt and zero database mutation",
    "fails closed for non-empty WAL evidence and a tampered durable receipt without database or temp mutation"
   ],
   "cli/provider-observation-messages.test.ts": [
    "%s has an exact EN/TR catalog pair",
    "%s interpolates every supplied field in both languages",
    "keeps the dry-run and already-v2 outcomes explicitly side-effect free",
    "preserves typed HOLD and error identifiers during interpolation",
    "%s is explicitly cataloged in both locales",
    "%s pins the complete placeholder contract in EN and TR",
    "%s does not expose raw provider identity fields",
    "%s has exact, distinct EN/TR entries",
    "%s pins its bounded placeholder contract",
    "%s never asks for a raw identity or path"
   ],
   "cli/provider-observations-migration.test.ts": [
    "is dry-run by default and forwards explicit apply, preimage, digest, and stable JSON",
    "renders only a bounded reason code when runtime adoption holds"
   ],
   "cli/provider-observations-reconciliation.test.ts": [
    "opens terminal approval authority without requiring API OIDC composition",
    "keeps inspect explicit and defaults reconcile to an all-eligible dry-run batch without a manual run ID",
    "collects repeatable optional run filters and forwards one digest plus one approval ID for apply and replay",
    "renders stable redacted JSON with only run, candidate, and HOLD aggregates",
    "turns typed reconciliation failures into a redacted, non-mutating HOLD"
   ],
   "cli/provider-observations.test.ts": [
    "consumes the core canonical database default without declaring a local path authority",
    "renders byte-stable redacted aggregate JSON without the absolute root or raw identity",
    "wires inspect and migrate to the exact typed handler and stays dry-run by default",
    "executes production inspect and dry-run handlers in real asynchronous child processes",
    "rejects %s database paths with stable redacted JSON",
    "keeps a valid explicit database override readable and side-effect free",
    "fails closed on corrupt and symlink databases without leaking exception paths",
    "runs production adoption through durable publish, exact fresh-read, and deduplicated replay",
    "refuses a plan made stale by a source change without writing a receipt",
    "returns a redacted HOLD for a non-empty WAL and performs zero writes",
    "fails closed with a redacted HOLD when the independently persisted receipt is malformed"
   ],
   "cli/provider-parity-robust.test.ts": [
    "send() rejects (not a silent empty success) on non-zero exit code",
    "send() error message includes the provider binary name and any collected stdout",
    "stream() rejects on non-zero exit code instead of yielding a done chunk",
    "send() still resolves normally on exit code 0 (no regression)",
    "send() still resolves normally on a null exit code (signal-terminated, ambiguous — not treated as failure)",
    "ollama adapter times out against an unresponsive server instead of hanging forever",
    "openai-compatible adapter times out against an unresponsive server instead of hanging forever",
    "DECKENT_CHAT_HTTP_TIMEOUT_MS env var overrides the default/opts timeout",
    "a fast-responding server is unaffected by the timeout wiring",
    "codex spawn env has OPENAI_API_KEY and DECKENT_OPENAI_API_KEY stripped",
    "claude/gemini spawns also have OPENAI keys stripped (unconditional, symmetric with gemini keys)"
   ],
   "cli/provider-switch-invalid.test.ts": [
    "switchTo to an unknown provider does not throw",
    "returns a switchError message instead of crashing",
    "leaves current() unchanged on a failed switch (no silent fallback)",
    "the proxy keeps routing to the previous (still live) adapter after a failed switch",
    "a subsequent valid switch still works after a prior failed attempt",
    "a non-Error throw (e.g. a thrown string) is caught and stringified"
   ],
   "cli/provider-switch.test.ts": [
    "delegates send/stream to the initial adapter",
    "switchTo rebuilds, tears down the previous adapter, and routes to the new one",
    "switchTo with only a model keeps the current provider",
    "proxy.stream falls back to send() for a non-streaming adapter",
    "exit tears down the active adapter"
   ],
   "cli/quick-start.test.ts": [
    "returns a string with the description in a Task 1 heading",
    "includes the description in the body",
    "starts with a top-level DIRECTIVES heading",
    "includes a Tests section",
    "handles description with special characters",
    "produces non-empty output for any description",
    "creates DIRECTIVES.md when it does not exist",
    "returns the correct directivesPath",
    "writes the description into the temporary DIRECTIVES.md",
    "writes valid DIRECTIVES format to the file",
    "does NOT create a file when DIRECTIVES.md already exists",
    "returns alreadyExisted=true when file exists",
    "preserves existing DIRECTIVES.md content when already exists",
    "handles descriptions with leading/trailing spaces",
    "creates the file with UTF-8 encoding",
    "removes DIRECTIVES.md when createdTemp is true",
    "does not remove DIRECTIVES.md when createdTemp is false",
    "does not throw when file was already deleted",
    "is a no-op when alreadyExisted is true",
    "returns the file content when DIRECTIVES.md exists",
    "returns null when DIRECTIVES.md does not exist",
    "returns empty string for an empty file",
    "creates, populates, and cleans up DIRECTIVES.md in full flow",
    "handles concurrent prepare calls (second call sees existing file)"
   ],
   "cli/rbac-command.test.ts": [
    "check: admin has admin permission → ALLOWED + exit 0",
    "check: viewer does not have write permission → DENIED + exit 1",
    "roles: lists admin, operator, viewer",
    "check: invalid role → error message + exit 1",
    "check: operator has sprint:read permission → ALLOWED",
    "roles: shows permissions in output"
   ],
   "cli/rbac-grant.test.ts": [
    "grant: assigns a valid role to a user → GRANTED + exit 0",
    "revoke: removes an existing role assignment → REVOKED + exit 0",
    "grant: invalid role → error message + exit 1",
    "revoke: no role assigned → WARN message + exit 0"
   ],
   "cli/recover-force-checkpoint-preservation.test.ts": [
    "reports identical preservation and canonical PAUSED remediation for %s",
    "renders the canonical commands through Turkish i18n",
    "projects the policy disposition and remediation in stable JSON"
   ],
   "cli/recover-resume.test.ts": [
    "delegates mutation to the canonical resume command with exact flags",
    "requires an exact child outcome artifact and preserves PAUSED as typed operator action",
    "refuses resume when no canonical resumable authority exists"
   ],
   "cli/recover.test.ts": [
    "rejects conflicting flag %s before operation",
    "rejects malformed dispatch identity",
    "forwards exact identity and projects the optional dry-run report in JSON",
    "forwards committed-unsettled retention as a distinct dry-run branch",
    "rejects combining the two retention modes",
    "does not claim cleanup while confirming %s retention",
    "keeps preview and settlement copy localized and count-driven"
   ],
   "cli/recovery-lifecycle-binary.integration.test.ts": [
    "inspects an isolated paused run read-only and never calls a provider"
   ],
   "cli/recovery-truth-real-binary.integration.test.ts": [
    "runs the compiled entrypoint asynchronously and proves replay/no-delete at the terminal JSON surface"
   ],
   "cli/reform-batch-integration.real.test.ts": [
    "passes the registration gate and the package-resolved CLI parity gate",
    "keeps deprecated aliases out of primary groups and exposes every new group member",
    "$command emits one typed warning and forwards an argument to $replacement"
   ],
   "cli/registration-harness.test.ts": [
    "${file}: ${fnName} is registered via parent subcommand (not index.ts direct)",
    "${file}: ${fnName} is imported and called in index.ts"
   ],
   "cli/remember.test.ts": [
    "prints error when memory.db does not exist",
    "inserts a basic note with default type",
    "inserts with custom type via --type flag",
    "inserts with tags via --tags flag",
    "uses custom title via --title flag",
    "truncates title to 60 chars + ellipsis when no --title given",
    "does not add ellipsis for short notes",
    "sets source as \"user\"",
    "ignores empty tags",
    "handles special characters in note content",
    "generates unique IDs for multiple inserts"
   ],
   "cli/render-region-clear.test.ts": [
    "single-row prompt (getCursorPos rows: 0) → clears the region then writes, no spurious cursor-up",
    "wrapped multi-row prompt (getCursorPos rows: 2) → moves cursor to the TOP of the region before clearing (full region, not just the current row)",
    "repeated writeAbove calls each fully re-clear their region — no leftover artifacts carry across calls",
    "back-compat: rl without getCursorPos (minimal test double) → treated as single-row, does not throw",
    "non-TTY → unaffected passthrough (no clearing escapes at all)"
   ],
   "cli/render-region-safeprompt.test.ts": [
    "real Node readline closed (ERR_USE_AFTER_CLOSE) → writeAbove/reprompt do not throw and log nothing",
    "expected teardown error (fake rl, exact ERR_USE_AFTER_CLOSE code) → swallowed silently, no log",
    "unexpected error (different error code) → does not throw, but is logged instead of silently eaten",
    "unexpected error (plain Error, no code) → reprompt does not throw but is logged",
    "unexpected non-Error throw (string) → does not throw, is logged via debugLog string-coercion path",
    "regression guard (born-540 intact): rl.prompt succeeds normally → full-region clear + prompt(true) unaffected by the narrowed catch"
   ],
   "cli/repl/active-turn-cancel-http.test.tsx": [
    "closes the active SSE response without a late terminal and accepts a fresh next turn"
   ],
   "cli/repl/app-keyboard-ownership.test.tsx": [
    "keeps startup recent-session discovery off by default but lets explicit /resume load it",
    "shows the startup teaser once when explicitly enabled",
    "native /status preserves run output and appends the full local chat context",
    "keeps runtime detail hidden without polling, then opens one fresh /status snapshot and closes it with Esc",
    "drops a late /status response after clear instead of reopening stale detail",
    "wraps long status identities losslessly and re-clamps the window to the measured row budget",
    "commits a typed native resume only after hydration succeeds",
    "suppresses a delayed pre-resume measurement callback while allowing the resumed chat to commit",
    "refuses a sprint projection without changing local chat identity",
    "never admits a recent sprint disk row to archive fallback when its exact ledger is malformed",
    "hydrates an exact native sprint-id ledger beyond the recent-five list instead of loading archive context",
    "loads exact sprint context without switching chat identity and injects it only into the next submitted request",
    "uses the same one-shot annotated request on the legacy provider path",
    "does not arm a late sprint load after /clear changes the generation",
    "binds concurrent loads to the newest generation and never carries the older sprint across it",
    "aborts a pending sprint load when a chat resume commits a new identity",
    "preserves an already loaded pending context when a later archive load holds",
    "picker consumes Ctrl-C exactly once, then idle Ctrl-C follows the normal arm policy",
    "an inactive picker does not suppress fallback Ctrl-C owned above it"
   ],
   "cli/repl/app-picker-mutex.test.ts": [
    "matches the bare forms only, case-insensitively, and never a typed argument",
    "is active only when no decision card or inbox owns stdin",
    "app.tsx mounts PickerCard after the inbox card, gates it with resolvePickerCardActive, keeps the input bar inactive while open, and shares runSwitch",
    "run.tsx injects pickerLabels, pickerSpecs (native + legacy), saveDefault and the ascii/noColor gates"
   ],
   "cli/repl/app.test.ts": [
    "emits the honest flag-off notice and touches nothing else",
    "bare goal → usage hint, proposeRun NEVER called (avoids controller throw)",
    "whitespace-only goal → usage hint",
    "proposeRun gets the trimmed goal and setPreview gets deriveRunFlowPreview(ctx)",
    "a non-AWAITING context after propose → setPreview(null) (honest, no stale card)",
    "a proposeRun throw (e.g. single-flow RunFlowTransitionError) → reportError",
    "both catalog labels are non-empty (the component owns no fallback)"
   ],
   "cli/repl/approval-card-async.test.tsx": [
    "presentation retirement removes only the exact local card without a durable decision",
    "locks repeated keys and emits no closure before durable acceptance",
    "keeps rejected promises and synchronous throws visible as truthful HOLD",
    "refuses an accepted callback whose durable id/action does not match the intent",
    "retires only the exact expired request as a non-actionable terminal outcome",
    "keeps cancellation visible and retryable without a success closure",
    "runs approve-similar as separate sequential ceremonies and stops truthfully on partial HOLD"
   ],
   "cli/repl/approval-card-facts.test.tsx": [
    "is minute-grained above a minute and second-grained only in the last minute; the past clamps to zero",
    "v1: action leads, then requester, tenant, policy, age, expiry→outcome (worded template), and honest not-declared rows",
    "undeclared consequence/rollback on a low-risk request is not emphasized; declared strings surface verbatim",
    "v2: adds the lifecycle row (origin · tier · blocking · SLA stage) and the timeout disposition on the expiry row",
    "an expired request says so (worded template) instead of a negative countdown",
    "shows the fact rows in en and tr, the full masked arguments, and never renders rawArgsRef",
    "catalog rows exist in en and tr for the new fact labels"
   ],
   "cli/repl/approval-card-render.test.tsx": [
    "Esc collapses expanded details without deciding the pending request",
    "renders the pending request summary + risk badge",
    "uses an ASCII frame/owned separators and preserves request Unicode",
    "pressing y → onDecide(allow) AND onClosure(request, \"allow\") both fire",
    "pressing n → onClosure(request, \"deny\")",
    "isActive=false → a keypress is ignored (mutex deference)"
   ],
   "cli/repl/approval-keyboard-transition.test.tsx": [
    "moves the first key from InputBar to native intent without double consumption, then back",
    "durable card ignores the transition key until it owns the mutex",
    "mounted ReplApp gives the first committed confirm key to the modal, then returns ownership"
   ],
   "cli/repl/approval-terminal-command.test.ts": [
    "lets the current readable callback unwind before crossing the public pause boundary",
    "runs only the fixed compiled approvals command and accepts only the fresh durable authorized match",
    "holds the matching local cross-decision until terminal restoration without delaying external decisions",
    "keeps later verification on HOLD when terminal restoration threw after a durable decision",
    "latches restoration HOLD when an expired child cannot be reaped",
    "revalidates expiry after a local ceremony wait instead of releasing a stale allow",
    "fails closed before spawn without an inherited interactive TTY",
    "reports nonzero-after-write as HOLD with the observed durable truth",
    "maps exit 130 to cancellation only without a durable winner",
    "rejects unsafe positional ids and request digest drift without spawning",
    "validates a cross-channel event against the exact durable row and authorization",
    "accepts canonical expiry only with an exact request-bound timeout receipt",
    "returns cancellation for a signalled child only when no durable decision exists",
    "expires and reaps a child at the request deadline without claiming a decision",
    "does not treat a long-lived request as expired by Node timer overflow",
    "does not spawn when the suspended terminal input cannot reach the pause boundary"
   ],
   "cli/repl/busy-controls.test.ts": [
    "starts idle, no interrupt requested, no steer notes",
    "markBusy transitions to busy and resets bookkeeping",
    "markIdle transitions to idle and drains steer notes exactly once, in order",
    "idle + /queue → queue-status busy:false, forwards size()",
    "busy + /queue → queue-status busy:true, forwards size()",
    "never mutates a real ChatTurnQueue — only size() is called (type + runtime proof)",
    "idle + /interrupt → no-op, informational result, canceller never invoked",
    "busy + first /interrupt → invokes canceller once, sets interruptRequested",
    "busy + double /interrupt (e.g. Esc then Ctrl-C) is idempotent — canceller invoked exactly once",
    "idle + /steer → no-op, nothing running to steer",
    "busy + blank /steer message → no-op",
    "busy + /steer appends to FIFO in submission order, reporting 1-based position",
    "parses /queue and /interrupt with no argument",
    "parses /steer with a message, trimming surrounding whitespace",
    "parses bare /steer as an empty message",
    "unrecognized input and non-slash lines → none",
    "escape and ctrl+c both resolve to interrupt",
    "unrecognized key → undefined",
    "BUSY_KEY_ACTIONS table only maps the documented cancel gestures"
   ],
   "cli/repl/context-slashes.test.tsx": [
    "both commands are listed with localized descriptions",
    "ignores other input; reports unavailable on an engine without the seam",
    "renders REPL selection vs last admitted measurement (not inference proof)",
    "renders native work budget lines when the snapshot includes them",
    "does not claim alignment for denied last measurement",
    "renders the cached last actual request, not a synthetic current-occupancy percentage",
    "renders retained tool-result pressure when the snapshot carries it",
    "says \"unknown\" (catalog) when no actual request exists instead of guessing",
    "reports each outcome from the engine seam and unavailable without it",
    "keeps the raw typed compact cause and its localized safe action in %s",
    "reports failed-compaction usage once without a normal engine turn in %s",
    "answers /context and /compact with aggregate usage %i/%i and forwards the live measurement callback",
    "buildShortcutsPanel resolves a title and ≥ 10 rows for en and tr, with distinct languages",
    "`?` on an empty composer toggles the panel and never enters the buffer; `?` inside text is a normal character",
    "localizes the %s trigger in EN and TR without a provider turn",
    "retains the actual cached request provenance across changed live snapshot fields in %s",
    "shows boot measurement authority on /context in %s",
    "does not invent a trigger or provenance before any actual request in %s",
    "shows measured preamble budget in %s",
    "shows the admitted preamble floor warning in %s"
   ],
   "cli/repl/f11-016-stab.test.tsx": [
    "approves on y AND Y (uppercase used to deny — the regression this fix closes)",
    "always-approves on a AND A",
    "denies on n AND N (the hint documents capital N)",
    "Enter and Esc keep their pre-fix deny-default behavior",
    "ignores navigation keys (arrows arrive as empty input) instead of denying",
    "ignores stray typed/pasted text (input bar is inactive during the modal)",
    "ignores ctrl/meta-modified keys (shortcuts never decide a card)",
    "drives createConfirmQueue safely through a noisy key stream (burst survives)",
    "emits only the segment on first push",
    "appends only the segment when headAlreadyPushed is true",
    "is deterministic — a re-invoked updater cannot duplicate segments",
    "REPRO — keeping the old segmenter resurfaces pre-clear text (the closed bug)",
    "FIX — a recreated segmenter starts empty; post-clear stream renders clean",
    "FIX — a pre-clear OPEN fenced block no longer swallows post-clear lines",
    "leaves short lines untouched (no ellipsis at or under the cap)",
    "truncates ASCII past the cap with a trailing ellipsis",
    "never bisects a surrogate pair (the pre-fix q.slice(0, 60) did)",
    "honors a custom max"
   ],
   "cli/repl/grapheme-caret.test.tsx": [
    "segments a buffer into user-perceived graphemes",
    "← / → / Backspace / Delete never split a cluster",
    "measures display width per cluster: ZWJ emoji 2, flag 2, combining sequence 1, CJK 2",
    "layoutWrapped never splits a cluster across rows",
    "graphemeIndexAtUtf16 snaps an offset inside a cluster to the cluster start",
    "three ← from the end of \"ab<family>cd\" land before the family; typing inserts there",
    "Backspace right after the family removes the whole cluster",
    "a legacy cursor inside a cluster snaps to the cluster start on the next edit",
    "with the caret ON an emoji the frame keeps the emoji intact (no lone surrogate)",
    "marker style places the marker before the whole ZWJ cluster after two ←"
   ],
   "cli/repl/history-ink-wire.test.tsx": [
    "starts empty when no history file exists yet",
    "loads pre-seeded on-disk history at construction",
    "returns the newest entry on the first ↑ with an empty buffer",
    "keeps walking older entries on repeated ↑",
    "only cycles through entries matching the typed prefix",
    "returns the buffer unchanged when nothing matches the prefix",
    "restores the empty live buffer after navigating up then down-through",
    "restores the originally-typed prefix buffer after down-navigating past the newest match",
    "is a no-op while already at the live line",
    "grows the in-session entries array",
    "persists to disk — a fresh loadHistory() read sees it (simulated restart)",
    "resets navigation back to the live line so the next ↑ re-enters fresh",
    "a newly recorded line is immediately reachable via prefix-filtered ↑"
   ],
   "cli/repl/inbox-card-render.test.tsx": [
    "open=false renders nothing (no card, no stdin ownership)",
    "open=true renders header + rows + nav hint, first row focused by default",
    "↓ moves the focus cursor to the next row",
    "↑ from the first row wraps to the last",
    "the focus cursor stays glued to its run across a live-refresh REORDER (D-2 headline)",
    "↵ opens the focused run detail in-card (list hidden)",
    "Esc from an OPEN detail returns to the list (does NOT close the card)",
    "Esc from the LIST invokes onClose (the card closes)",
    "isActive=false suppresses every key (mutex deference)",
    "decision keys are INERT in the list view (detail-only verbs)",
    "AWAITING_APPROVAL detail shows the decide hint; `a` fires approve and renders the outcome line",
    "verbs are state-gated: `s` is inert on AWAITING_APPROVAL, `a` is inert on APPROVED, `s` starts an APPROVED run",
    "a RUNNING detail offers no verbs and no hint; without onDecide the keys stay inert (D3b compat)",
    "the outcome notice clears on navigation (never sits under a different run)"
   ],
   "cli/repl/ink-palette.test.ts": [
    "ansi16: only named chalk colors (host-theme mapped), never a hex",
    "truecolor: the token hex flows for roles that have one; attribute roles stay attributes",
    "ansi256: the nearest xterm index in Ink syntax",
    "none: every role is an empty style (no SGR, textual carriers only)",
    "covers exactly the generated palette roles"
   ],
   "cli/repl/ink-stabilize.test.ts": [
    "emits a prose line immediately when a newline arrives",
    "flush() emits a trailing partial line (no trailing newline)",
    "flush() emits an unclosed fence block (yarım fence flush-race)",
    "flush() on a partial fence-opener emits the fence as a block",
    "fenceGuard fires at MAX_CODE_BLOCK_LINES and auto-flushes mid-stream",
    "properly closed fence emits once on the closing ``` line",
    "multiple prose lines each emit individually",
    "returns null head when queue is empty",
    "enqueue + answer in FIFO order — 3-item burst",
    "always-allow (a) auto-resolves same-tool pending items",
    "deny (n) does NOT cascade to remaining queue items",
    "concurrent Promise.all enqueue + sequential answer — no orphan",
    "head() index/total are correct across a burst",
    "mid-burst arrival: new enqueues while head is being answered grow total, 0 dropped",
    "5+ burst with concurrent enqueue race — 0 drop, all FIFO",
    "always (a) cascade with 5-item same-tool burst — all resolved, 0 dropped"
   ],
   "cli/repl/input-bar-caret-style.test.tsx": [
    "marker style renders an explicit `|` before the caret cell (mid-line and at the end)",
    "inverse style renders no marker glyph (the caret is the inverse attribute)",
    "renders a classic ASCII frame and prompt without changing typed Unicode"
   ],
   "cli/repl/input-debug-wire.test.tsx": [
    "disabled mode creates no diagnostic file and input still submits",
    "enabled mode records content-free actions, preserves editing/submission, and drains on unmount",
    "an inactive mounted bar neither consumes nor records a key",
    "StrictMode effect replay replaces the closed sink and the live instance still records",
    "repeated mounts own distinct exclusive sinks"
   ],
   "cli/repl/input-debug.test.ts": [
    "projects only the closed action/modifier vocabulary and retains no raw input or unknown key data",
    "uses a unique portable default destination while preserving the explicit override",
    "creates a private exclusive file, drains on close, and never persists content",
    "refuses pre-existing files and final symlinks without changing either",
    "does not close a pending write on limit, then drains all 256 accepted rows in order and closes once",
    "keeps close behind partial progress and ignores a duplicate callback without completing a newer write",
    "waits for an in-flight write failure callback before dropping unissued work and closing",
    "drains a close requested before open completes and closes the late descriptor once",
    "contains synchronous adapter and reporter exceptions with typed content-free status",
    "treats zero and invalid partial progress as terminal without retrying"
   ],
   "cli/repl/input-history.test.ts": [
    "returns [] when no history file exists yet",
    "round-trips a single appended line",
    "preserves append order across multiple lines",
    "skips empty and whitespace-only lines (no-op)",
    "flattens embedded newlines so each entry stays one line on disk",
    "writes to .deckent/settings/repl-history under the project root",
    "returns at most `cap` entries, most-recent-kept, oldest-first order",
    "uses DEFAULT_HISTORY_CAP (1000) when no cap is passed",
    "does not truncate the on-disk file — a larger cap later still sees all lines",
    "redacts an AWS access key before it ever touches disk",
    "redacts a Bearer token in an otherwise normal command",
    "leaves ordinary commands with no secrets untouched",
    "cycles from newest to oldest on repeated up, then back to live on down-through",
    "down navigates back toward the live/draft line",
    "down is a no-op while already at the live line",
    "up with an empty entries list returns the live line unchanged",
    "reset() returns navigation to the live line",
    "only cycles through entries matching the prefix captured at navigation entry",
    "returns the live line when no entries match the prefix",
    "empty prefix matches every entry (equivalent to plain navigation)",
    "converts CRLF line endings to LF",
    "converts lone CR to LF",
    "strips C0 control characters other than newline and tab",
    "strips DEL (0x7f)",
    "preserves tabs and newlines",
    "leaves plain printable/unicode text unchanged",
    "handles empty string",
    "two interleaved writers land all lines without corruption",
    "N concurrent async appenders (Promise.all) all land — none dropped, none merged"
   ],
   "cli/repl/interaction-flow.test.ts": [
    "%s: a text-delta emitted before a tool call reaches output() before the tool line lands",
    "the stream segmenter shows a newline-less narration line as the live partial and commits it on flush",
    "a shell tool line carries the command with credentials redacted and the read-only marker + elapsed",
    "%s: cancelTurn mid-stream keeps the streamed text and reports tool calls / seconds",
    "counts the tool calls that ran before the cancel",
    "a turn that ends normally never prints the interrupted line",
    "%s: two silent reads → typed notice rendered, model answers, /context shows the counters",
    "fires on the wall-clock bound through the injected engine clock",
    "/context renders the pending line while a host request is outstanding",
    "every 7114 catalog row exists in en and tr"
   ],
   "cli/repl/interrupt-policy.test.tsx": [
    "a draft is discarded, never the session (first press arms)",
    "while a turn is running the first press requests the interrupt",
    "idle + empty arms the exit; the second press inside the window exits",
    "an armed second press exits regardless of draft/working (the user asked twice)",
    "Ctrl-D (EOF on an empty composer) exits immediately",
    "honors a custom window",
    "tui.ctrl_c_* rows exist in en and tr and are carried by ReplLabels",
    "run.tsx mounts Ink with exitOnCtrlC: false so the policy owns Ctrl-C",
    "app.tsx routes InputBar interrupts through resolveCtrlC (no unconditional exit)",
    "Ctrl-C with a draft → (\"int\", true) and the composer is cleared",
    "Ctrl-C on an empty composer → (\"int\", false); Ctrl-D → (\"eof\", false)"
   ],
   "cli/repl/live-footer-labels.test.ts": [
    "maps every LiveFooterLabels field to a key that resolves in en AND tr",
    "every en field is the catalog row itself (the mechanism exports no default set)",
    "Turkish idle line is no longer the English default",
    "Turkish running/elapsed lines use the injected labels"
   ],
   "cli/repl/mcp-bridge.test.ts": [
    "isMcpClientEnabled: only an explicit true opts in (default off)",
    "flag off → returns null (no broker, backward-safe)",
    "flag on, default deps → real McpClientBroker composed; bridge non-null, NOT connected",
    "flag on → composing the bridge does NOT auto-connect the broker",
    "flag on → dispatch flows through buildMcpBridge (confirm + callTool + audit)",
    "flag on → reject path: callTool NOT invoked + cancelled audit event"
   ],
   "cli/repl/multiline-composer.test.tsx": [
    "Shift+Enter, Alt/Option+Enter and Ctrl-J (linefeed) insert a newline at the cursor",
    "a trailing backslash + Enter continues the line (the backslash becomes the newline)",
    "a plain Enter submits the whole multi-line buffer; a backslash NOT at the end does not continue",
    "a newline-only draft is not submitted (whitespace-only stays a no-op submit)",
    "Up/Down move between lines keeping the column; history only at the first/last line",
    "column clamps to a shorter target line and is measured in graphemes (emoji-safe)",
    "Home/End and Ctrl-A/Ctrl-E are line-local",
    "a single-line draft keeps the legacy behavior byte-identical (Up = history, Home = 0)",
    "maps shift/meta on return and a bare linefeed to the enter key",
    "Alt+Enter and a trailing backslash add lines; Enter submits the joined text"
   ],
   "cli/repl/native-permission-approval.test.ts": [
    "selects intent before publishing and verifies the canonical decision",
    "retires the local intent on abort without publishing a decision",
    "keeps a published durable request pending while abort retires only its local view",
    "keeps an untrusted cross-channel decision as HOLD",
    "maps trusted terminal disposition %s to a stable HOLD code",
    "does not release a late trusted decision after the turn was cancelled"
   ],
   "cli/repl/native-permission-card.test.tsx": [
    "gives simultaneous durable and native cards exactly one keyboard owner",
    "selects session with one keyboard owner in %s",
    "accepts the first key after a mounted inactive card becomes active",
    "Escape retires only the transient intent"
   ],
   "cli/repl/native-permission-round.test.tsx": [
    "ships every read-only / round key in EN and TR",
    "chooseRound settles the current intent as once and covers the other confirm-projected calls of the SAME round",
    "never covers elevated or foreign-round invocations and consumes each cover once",
    "chooseRound is refused when nothing else in the round is coverable",
    "lists every proposed call with scope/risk and offers key 4 in %s",
    "collapses rows past the bound and hides key 4 when nothing else is coverable",
    "read-only marker fires exactly once per silent read and never for asked/denied calls",
    "projects a round with engine-parity tiers and keys it per session/turn/round"
   ],
   "cli/repl/native-reasoning-indicator.test.tsx": [
    "shows \"~N hidden reasoning tokens\" on the phase anchor while thinking streams and clears it afterwards",
    "runNativeTurnLoop forwards reasoning activity only while its turn is open and clears at turn end"
   ],
   "cli/repl/native-request-metrics.test.tsx": [
    "renders an actual admitted request without inferring transport success or current occupancy",
    "keeps a denied checkpoint and provider reports separate from admission measurement",
    "uses the catalog-provided unknown state when no actual request exists",
    "discloses known provider reports even when no request measurement exists",
    "escapes terminal controls in dynamic identity and provenance fields"
   ],
   "cli/repl/native-runtime-truth.test.ts": [
    "pending when no last request exists (never aligned)",
    "not_admitted when measurement was denied",
    "pending_no_selection when REPL selection is missing",
    "unknown_default_model when session model is provider default",
    "aligns only when admitted, explicit model, provider and model match",
    "detects provider mismatch",
    "detects model mismatch",
    "carries checkpoint purpose on last measurement",
    "shows pending lines without fabricated inference",
    "labels denied measurement without match claim"
   ],
   "cli/repl/native-session-lifecycle-present.test.ts": [
    "includes idle disclaimer separate from work wall budget"
   ],
   "cli/repl/native-tool-activity.test.tsx": [
    "advances mounted tool duration monotonically across a backwards wall-clock change",
    "drops an old engine callback after that turn has closed before a later turn can own the anchor",
    "keeps compact state and elapsed before a long tool name in the 48-column anchor budget",
    "keeps an unabortable tool visible after Ctrl-C until its matching canonical completion",
    "does not render activity when repl_surface is disabled",
    "keeps an activity through a nonmatching clear and removes it only for its matching call id",
    "keeps an executing tool anchor when Ctrl-L clears the screen",
    "clears a thrown turn through the loop finally without treating it as a tool result"
   ],
   "cli/repl/native-tool-skill-dispatch.test.ts": [
    "registers a skill-dispatch tool (RED pre-fix: no such tool was registered)",
    "delegates to the injected skill executor seam and returns its result in tool-result shape",
    "maps an executor [mcp-error] string to ok:false (toolResultFrom reuse)",
    "guards a missing/empty skillId without invoking the executor",
    "default live path resolves SKILL.md guidance from the skill-pool (proof-of-function, hermetic)",
    "is additive — existing exec/CLI tool registrations are unchanged"
   ],
   "cli/repl/native-transport.test.ts": [
    "returns a mock adapter when DECKENT_NATIVE_MOCK is set",
    "returns ProviderError when no transport is detected",
    "emits completed prose lines immediately on newline",
    "buffers an unclosed fence block and emits it on flush() (the queue/flush race guard)",
    "correctly segments a multi-chunk stream with a complete fence block",
    "flush() on a trailing partial line (no newline) emits the partial as a line",
    "partial() returns only the in-progress line (no newline), not completed lines",
    "flush() after flush() is idempotent (no double-emit)",
    "picks the Anthropic adapter when ANTHROPIC_API_KEY is set",
    "picks an OpenAI-compatible adapter for OPENAI_API_KEY",
    "picks Ollama when only ollama_host is configured",
    "honors DECKENT_NATIVE_MODEL override",
    "returns an honest error (no adapter) when no transport is available"
   ],
   "cli/repl/nervous-bridge.test.ts": [
    "passes through whatever the store returns, unmodified",
    "returns an empty list when the store has nothing pending",
    "builds an accept plan with resolve-approval + clear-pending steps",
    "matches by id prefix",
    "matches by shortCode (case-insensitive)",
    "returns found:false with the requested id when nothing matches",
    "builds a reject plan and carries the reason as plan metadata",
    "omits reason from the plan when not given",
    "returns found:false when nothing matches",
    "builds an accept plan carrying modifiedPayload",
    "gotcha-parity: edit-accept plan includes the SAME clear-pending step as a plain accept",
    "matches by id prefix and shortCode like planAccept/planReject",
    "returns found:false when nothing matches",
    "accept plan: calls executor.resolveApproval once with the resolution + no opts",
    "reject plan: calls executor.resolveApproval with decision \"rejected\"",
    "edit-accept plan: forwards modifiedPayload to the injected executor",
    "regression (project_nervous_accept_pending_not_cleared parity): edit-accept apply clears pending",
    "plain accept and reject also clear pending (parity across all resolutions)",
    "returns the executor result verbatim (false when nothing was resolved)",
    "works without a pendingCleanup dependency (optional)"
   ],
   "cli/repl/onboarding-ui.test.tsx": [
    "walks provider_detect → auth_status → mcp → scope → mode → summary with honest progress",
    "selection changes the plan: global scope + economic mode re-derive via the machine",
    "skip (s) answers every question with its own defaultValue",
    "declining the mcp suggestion empties the plan attach actions",
    "Esc cancels from any active step and fires onCancel exactly once",
    "the summary cancel choice cancels without applying",
    "skip on the apply-confirm is a no-op — a config write never happens implicitly",
    "cursor wraps in both directions; up/down on an info card is a no-op",
    "renders step 3 as an info card with the MACHINE descriptionKeys when nothing is suggested",
    "surfaces the machine blockedReasonKey as a warn summary row when nobody is logged in",
    "maps arrows/enter/s/esc and treats everything else as a no-op",
    "escape wins over simultaneous flags (never a stray select)",
    "resolveOnboardingColors(true) carries no color and no attribute",
    "resolveOnboardingColors(false) provides the role palette (named colors, inverse focus, no dim)",
    "no screen/view-model ever contains an ANSI escape (string-free + key-only)",
    "buildProviderDetectRows/buildAuthStatusRows/buildMcpInfoRows key every provider",
    "buildMcpAttachQuestion lists the suggested hosts in the accept choice params",
    "ONBOARDING_UI_STEPS mirrors the machine 5-phase order"
   ],
   "cli/repl/operator-strip-dynamic.test.tsx": [
    "shows tool B after rerender (coalesced in-place update)",
    "shows withheld budget notice on operator strip without polluting prose",
    "Ink Static does not repaint an updated item at the same index (regression guard)"
   ],
   "cli/repl/operator-strip-lifecycle.test.tsx": [
    "isOperatorStripTurnLive matches clear-epoch stale guard",
    "isOperatorPhaseSignal is true only for hard failures",
    "A → prose → B keeps B visible in dynamic strip",
    "three routine tools in one turn keep only the last on the live strip until finalize",
    "phase signal tool is eligible for scrollback commit on finalize",
    "failed tool A commits to scrollback before success B replaces live strip",
    "stale tool after /clear does not update strip; new turn accepts fresh tool"
   ],
   "cli/repl/paste-composer.test.ts": [
    "collapses pastes over line or char threshold",
    "expands chip text to wire body while rawIntent keeps chips",
    "expands corrupted chip labels to full paste on submit",
    "strips terminal table copy artifacts from pasted text",
    "strips Ink unicode box borders including ┬ junctions (smoke table copy)",
    "allocates distinct chip text for duplicate dimensions"
   ],
   "cli/repl/picker-card-render.test.tsx": [
    "shows the title, the focused row with its facts and state word, and the pick hint",
    "↓ moves the cursor; the blocked row shows its typed reason under the cursor",
    "a credential-blocked row shows its localized detail in-row and on Enter, never the {detail} placeholder",
    "typing filters (the filter line shows the query); Esc clears it, a second Esc closes",
    "Enter → scope stage with textual markers; Tab moves the marker; Enter commits (id, scope)",
    "Enter on a blocked row commits nothing; a read-only reason blocks every commit",
    "isActive=false ignores keys (stdin mutex deference)",
    "NO_COLOR frame carries no SGR sequences and keeps every state word; ASCII glyphs render",
    "at 40 columns facts drop and the focused row reveals its full id on a second line",
    "renders Turkish labels when the tr set is injected"
   ],
   "cli/repl/picker-catalog.test.ts": [
    "is registered and every key is tui.picker.* with non-empty, distinct en and tr"
   ],
   "cli/repl/picker-closure.test.tsx": [
    "a digit types into the filter instead of jumping to an unnumbered row",
    "fitPickerRow pads the label to labelWidth so the facts/state column lines up",
    "pickerLinesFor drops facts under a width budget and can omit the typed hint",
    "an availability of 'unknown' yields state unknown (never a false ok); the legacy path uses it",
    "a provider with zero listed models is blocked NO_MODELS_LISTED and the models fact is localized",
    "term rows carry the localized posture label while the id stays the token",
    "the value picker names its key in the title subject",
    "formatConfigValue never yields [object Object] and bounds long values",
    "renders the title with the key, swaps the hint while a filter is active, and shows a blocked reason once on Enter",
    "Ctrl-C only asks the app to close (the app hook arms the two-press exit)",
    "bare /approve lists without the <n|id> hint and names the typed form; bare /provider appends the current provider",
    "the picker Ctrl-C path only closes the card, the card is keyed per opening, and config facts use formatConfigValue"
   ],
   "cli/repl/picker-config.test.ts": [
    "opens the key picker; typed sub-commands stay on the CLI-bridge path",
    "enumerable keys are ok, others blocked NOT_ENUMERABLE; facts carry category/current/default words",
    "one row per option with the current value marked; apply + cancel scopes",
    "buildConfigEntries widens AI-provider keys to VALID_PROVIDERS, keeps other option lists, and reads the project-level current value",
    "the config rows exist in en and tr and the labels carry them",
    "app.tsx keeps the chosen key, opens the value picker on a key commit, and applies through saveConfigValue",
    "run.tsx injects configEntries (metadata + current values, provider keys widened to VALID_PROVIDERS) and saveConfigValue → setConfigValues"
   ],
   "cli/repl/picker-degrade.test.ts": [
    "bare /model prints the numbered list with the typed hint; /model <n> resolves and switches; a blocked row is refused",
    "without a switchModel seam, /model <n> reports the honest unavailable line (no fake switch)",
    "bare /provider keeps the usage line first and adds the numbered list; /provider <n> resolves the number before switching",
    "bare /approve, /term and /config print their numbered lines and the typed hint (readline has no apply seam for them)",
    "resolvePickerSurfaceMode picks the card at 40+ columns and lines below",
    "uses ASCII separators and overflow while preserving raw candidate text",
    "entry.ts wraps the readline provider in createSwitchableProvider and injects switchProvider, switchModel, pickerSpecs and pickerLabels",
    "app.tsx prints pickerLinesFor into the transcript when the surface mode is lines"
   ],
   "cli/repl/picker-evidence-surfaces.test.ts": [
    "prints ok rows once the refresh resolved",
    "a refresh that overruns the bound leaves [unknown] rows and never blocks the surface",
    "rows are ok / typed blocked / unknown from the injected availability",
    "without a store every row stays unknown (never a false ok)",
    "NOT_LOGGED_IN and UNREACHABLE exist in every language and reach PickerLabels.blocked",
    "run.tsx and entry.ts create the store; app.tsx subscribes; chat-native awaits it"
   ],
   "cli/repl/picker-specs.test.ts": [
    "lists the current provider first, marks the current model, carries tier/ctx/status facts, offers session+default",
    "a provider without a credential blocks its models with the localized detail in-row",
    "owner policy: an inactive model is visible and blocked with the typed code; explicit-active providers get the set code",
    "policy lookups use the REGISTRY provider name for native providers that map to one (openai → codex)",
    "one row per provider with its model count, current marked, missing credential blocked in-row",
    "registryProviderFor maps native transports to registry owners",
    "lists registry models for claude/openai, ollama builtins, preset models for compat providers, discovered ids for local-llm",
    "mergeLocalLlmPublishedModelIds keeps config order then discovered extras sorted",
    "listNativeModelCandidates local-llm falls back to registry models when discovery is empty"
   ],
   "cli/repl/picker-surfaces.test.ts": [
    "maps bare /approve, /term and /resume; typed arguments stay direct",
    "lists the three modes once, in the documented order",
    "approval: one row per mode with its localized meaning, current marked, initialId current, scope apply",
    "term: one row per posture with the risk classes it admits, current marked",
    "lists records with status/time facts, marks the active session current, keeps a single apply scope",
    "approveFacts rows exist in en and tr",
    "shares runApprove / runTerm / applyResumeDecision between the typed forms and the picker, builds the three specs in-app, and handles /term outside the repl_surface flag"
   ],
   "cli/repl/picker-vocabulary.test.ts": [
    "are labeled by the registry owner, keep the transport id, and carry the via fact first",
    "the typed form accepts the owner word or the transport id",
    "without a transport seam the label is the id and no via fact is invented",
    "name the registry owner as the provider fact and add the via fact",
    "every native provider has a via kind; ollama and local-llm are local, the rest API",
    "host surface: the auth-probe (subscription CLI) providers are host-cli, native locals stay local, others API"
   ],
   "cli/repl/picker.test.ts": [
    "arrows/page/home/end move; Enter selects; Esc closes; Ctrl-C interrupts",
    "j/k and 1-9 navigate ONLY while the filter is empty; otherwise they type",
    "Tab / ←→ change the scope only in the scope stage; other keys are ignored",
    "initial nav focuses initialId (or the first row) with an empty filter",
    "filter is a case-insensitive substring over id, label and fact values",
    "realign keeps the selection when present, else falls to the first row",
    "resolveMenuWindow keeps the selected row visible (input-bar precedent)",
    "move wraps over the FILTERED rows and never uses an index as identity",
    "page clamps at the edges; edge jumps; jump targets the visible window",
    "typing narrows and realigns; backspace widens; Esc clears the filter before closing",
    "select on a blocked row reports the typed code and commits nothing",
    "select with two scopes enters the scope stage; Enter there commits the chosen scope; cancel closes",
    "select with one scope commits immediately; interrupt surfaces as an effect",
    "fits within columns by dropping trailing facts first, then truncating the label",
    "glyphs degrade to ASCII on request",
    "pickerLinesFor renders title, numbered rows with facts and state, and the typed hint",
    "resolvePickerArg accepts a 1-based number or an exact id/label (case-insensitive)",
    "pickerBlockedReason fills {detail} from the candidate detail and never leaks the placeholder",
    "buildPickerLabels resolves every field in en and tr; the guard throws on a missing one"
   ],
   "cli/repl/plan-preview-card.test.ts": [
    "maps y/n/d (case-insensitive) to approve/reject/details",
    "any other key is a no-op",
    "is 1-indexed and joins title/summary with an em dash",
    "truncates past 12 chars with an ellipsis",
    "leaves a short digest untouched",
    "sources all fields (including the 452-003 scope-gate labels) from real messages.ts keys, en and tr",
    "en and tr scope-gate labels genuinely differ",
    "renders the shared collision, edge, wave, concurrency, and finding projection",
    "'fail': verdict label + every scopeGateMessage line, each '  ! '-prefixed, in order",
    "'fail' with no scopeGateMessage: just the verdict label, no crash on missing message",
    "scopeGateOverridden=true (and result not fail): a single notice line",
    "'fail' takes precedence over scopeGateOverridden if both were somehow set",
    "pass/skipped, not overridden: no lines — nothing is invented when there is nothing to report",
    "degrades gracefully (never throws) when labels omit the optional scope-gate fields entirely"
   ],
   "cli/repl/provider-bootstrap.test.ts": [
    "empty registry → loads config ONCE, bootstraps ONCE with (config, root), returns the post-bootstrap list",
    "populated registry → neither config load nor bootstrap; returns the current list",
    "bootstrap fault is swallowed (best-effort, mirrors spawn.ts recovery) — returns whatever is registered",
    "config-loader fault is swallowed too — no throw escapes the seam",
    "second call after the registry became populated never double-bootstraps (idempotent at the seam)"
   ],
   "cli/repl/provider-evidence-http.test.ts": [
    "aborts and closes an actual hanging loopback request without caching reachability"
   ],
   "cli/repl/provider-evidence.test.ts": [
    "reports unknown before any evidence and never invents ok",
    "refresh turns the auth probe into ok / typed blocked / unknown per provider",
    "pings the Ollama host: reachable → ok, refused → typed UNREACHABLE naming the host",
    "contains a synchronous Ollama fetch throw as typed unreachable evidence",
    "a provider with no evidence source stays unknown without any probe call",
    "caches within the TTL and re-probes after it",
    "deduplicates an in-flight probe for the same provider",
    "a probe that exceeds the bound leaves the provider unknown (never blocks the surface)",
    "aborts a hung Ollama transport, clears pending, and rejects its stale late success",
    "a throwing probe is evidence of nothing: unknown, not blocked",
    "notifies subscribers once per refresh that changed something",
    "two overlapping refreshes that both waited on one changed probe each notify once; a late-landing result notifies on its own",
    "snapshot exposes the evidence with its age for a status surface"
   ],
   "cli/repl/readline-authority.test.ts": [
    "bare /term prints the status line and the numbered postures; a number applies through the authority",
    "/term <mode> applies by name; an unknown word gets the usage line and changes nothing",
    "applies by number or name through the authority and reports the set mode",
    "lists keys, then values for a chosen key, then writes through the config seam",
    "a value outside the key\\'s options is refused with the not-found line",
    "a bare listing asks through askChoice; a number applies, Enter cancels with zero side effects",
    "/help hides the enterprise group in Ask and lists it after /term control"
   ],
   "cli/repl/reduced-motion.test.tsx": [
    "suppresses only decorative 80ms churn for %s",
    "keeps normal animation and disposes it when the preference changes live"
   ],
   "cli/repl/reference-progress-bridge.test.ts": [
    "%s: emits one localized progress line and clears it when the turn ends",
    "repaints at the indicator cadence but never withholds a terminal phase",
    "records every phase transition in the privacy-safe audit with counts only",
    "emits nothing when large references are disabled",
    "scopes the activity to its own turn and always clears at the end"
   ],
   "cli/repl/reference-progress-view.test.ts": [
    "ships both catalogue languages for the digest surface",
    "%s: names the source, phase, verified sections, size and age on ONE line",
    "%s: an unknown section total renders as unknown, never as 0",
    "falls back to the source digest when the host has no path for the reference",
    "renders bytes in a stable unit that is never a token count",
    "%s: reports source bytes, coverage, child requests, retained tokens, deadline and journal",
    "%s: an unmeasured retained digest is unknown, not zero",
    "%s: a typed failure is a sentence, not a raw code",
    "%s: with no digest program the output is byte-identical to pre-7113",
    "%s: every phase and every typed failure has a real translation",
    "%s: the unavailable signal shows the localized reason, not the code",
    "an unknown reason code still reaches the user instead of vanishing"
   ],
   "cli/repl/reference-signal-localization.test.ts": [
    "%s: the user reads the cause, never the raw key",
    "an already-namespaced code resolves to its own key",
    "a bare code still gets the historical native. prefix",
    "an unknown code still falls back to the caller text (no regression)",
    "%s: every typed reference failure becomes a sentence in the signal vars"
   ],
   "cli/repl/renderer-i18n.test.tsx": [
    "renders the diagnostic probe in the explicitly selected language",
    "builds both approval demo fields from the same resolved language",
    "maps approval-demo owned punctuation with the resolved ASCII palette",
    "preserves raw native-error values with replacement syntax and placeholders in %s ascii=%s",
    "preserves raw injected-label values after owned template mapping in %s ascii=%s",
    "preserves the canonical unknown-key fallback and raw mechanism error"
   ],
   "cli/repl/rpc-client.test.ts": [
    "session.list — dispatches and returns a schema-shaped result",
    "run.status — dispatches with params and echoes runId",
    "approval.list — dispatches with an optional scopeId param",
    "limits.get — dispatches with empty params",
    "generates a fresh id per call when none is supplied, and honors an explicit id",
    "an unknown method resolves to UNKNOWN_METHOD, never throws",
    "a method absent from the handler map resolves to METHOD_NOT_IMPLEMENTED",
    "an empty method string is rejected as a malformed envelope, never throws",
    "roundtrip — the response returned is itself schema-shaped (survived serialize->parse)",
    "session.list maps ChatSessionSummary -> SessionSummary, marking the current session active",
    "session.list is absent from the map when listChatSessions is not supplied",
    "approval.list filters by scopeId when supplied, and passes everything through unfiltered otherwise",
    "limits.get passes through an available probe result",
    "limits.get honestly reports an unavailable probe, without fabricating numbers",
    "run.status is NEVER registered by this slice, regardless of deps supplied",
    "a non-/rpc line returns null (fall through to normal chat handling)",
    "bare \"/rpc\" with no method returns a usage error",
    "\"/rpc <method>\" with no params",
    "\"/rpc <method> <json>\" parses params",
    "malformed JSON params returns a parse error, never throws",
    "returns null for a non-/rpc line",
    "formats a successful dispatch as pretty-printed JSON",
    "reports an unknown method without invoking the transport",
    "surfaces a parse error (bad JSON params) as a formatted message"
   ],
   "cli/repl/run.test.ts": [
    "every label is a non-empty, genuinely-translated string (en !== tr)",
    "resolves the do.slash_* message keys (not the raw key back)"
   ],
   "cli/repl/session-authority-wire.test.ts": [
    "creates the authority from terminal.posture + the policy default and consults it in the tool confirm",
    "injects askChoice on a TTY through the answer arbiter, and the term/approve/config specs + config seam",
    "run.tsx creates the authority and the App mirrors /term and /approve into it",
    "run.tsx re-exports the same functions and the value rule parses JSON first",
    "tui.picker.choice_prompt exists in every language and reaches PickerLabels.choicePrompt"
   ],
   "cli/repl/session-authority.test.ts": [
    "starts from the given posture and approval, defaulting to the term-mode default and suggest",
    "setPosture applies the term-mode transition and reports whether anything changed",
    "setApproval switches the approval mode and refuses an unknown mode with a typed result",
    "gate() decides an action against the CURRENT posture (the same gate the Ink App uses)",
    "confirmPolicy mirrors the native engine: full-auto allows, auto-edit allows non-shell only, suggest asks",
    "subscribers hear every change and can unsubscribe"
   ],
   "cli/repl/session-ledger.test.ts": [
    "roundtrips N turn deltas into the exact transcript and summed usage",
    "writes only each delta, so equal-sized turns grow linearly rather than copying history",
    "skips corrupt, partial, missing-usage, and cross-session lines without crashing",
    "lists newest sessions with ChatSessionSummary-compatible previews and a limit",
    "keeps sanitized session files inside the injected cross-platform path and hardens permissions",
    "returns empty results for missing ledger data and non-positive limits",
    "keeps tolerant read compatibility while strict resume reads distinguish absence, empty, and unsafe files"
   ],
   "cli/repl/shell-confirm-once.test.tsx": [
    "ignores a/A for a one-time card and still decides y/n",
    "resolves \"a\" as \"y\" for a one-time request and leaves same-tool siblings pending",
    "confirmHintOnce and inputPaused exist in en and tr and the once-hint carries no \"a\"",
    "marks the `!` dispatch window, enqueues its confirm as one-time, hides the raw denial marker and shows the paused carrier"
   ],
   "cli/repl/slash-menu-more-labels.test.tsx": [
    "substitutes the count into the injected template",
    "tui.menu_more_above/below resolve in en AND tr and differ between the languages",
    "buildReplLabels(t) carries menuMoreAbove/menuMoreBelow for the session language",
    "typing `/` over a 12-command registry shows the injected Turkish \"below\" hint with the real count",
    "a missing injection surfaces through the REPL error boundary as the ${lang} catalog description — never English prose from the mechanism",
    "without a describer the boundary renders only the technical code (no prose) for a missing injection"
   ],
   "cli/repl/slash-menu-submit.test.ts": [
    "substitutes the selected command when buffer prefix-matches",
    "keeps typed buffer when nothing prefix-matches (fallback catalog)"
   ],
   "cli/repl/sprint-context-input.test.ts": [
    "keeps raw intent first and appends attributed untrusted data as one complete unit",
    "does not alter or truncate an empty or very long admitted artifact",
    "preserves Unicode and delimiter-like data with a bounded fence scan"
   ],
   "cli/repl/startup-health-lifecycle.test.tsx": [
    "renders an auth-only completion through subscribed state and ignores updates after unmount"
   ],
   "cli/repl/static-prose-batch.test.ts": [
    "joins prose lines into one push after debounce",
    "flushes prose before a fenced block"
   ],
   "cli/repl/status-row-render.test.tsx": [
    "renders one line with the brand/provider gap intact and the cwd tail visible at 60 columns",
    "shows the optional segments when they fit"
   ],
   "cli/repl/status-row.test.ts": [
    "fits everything when the terminal is wide enough (byte-identical segments)",
    "keeps the brand/provider spacing and tail-truncates the cwd with a leading ellipsis at 100 columns",
    "drops optional segments (tokens → approval → model) before active identity or cwd",
    "never exceeds the column budget for any width from 1 to 160",
    "reserves a grapheme-safe active-id abbreviation before any final whole-row clip",
    "preserves ordinary Unicode IDs and renders terminal controls explicitly",
    "renders %s explicitly",
    "keeps an ordinary full identity byte-for-byte in a fitting status row",
    "measures in display cells — a CJK / emoji path counts double-width glyphs",
    "exposes typed segments so the renderer colors roles, not substrings",
    "uses ASCII chrome while preserving provider, model and cwd content"
   ],
   "cli/repl/stream-segmenter-utf8.test.ts": [
    "reassembles a Turkish+emoji line fed ONE BYTE AT A TIME (every boundary mid-codepoint)",
    "reassembles when split at adversarial 2/3/4-byte mid-codepoint boundaries",
    "decodes a single 2-byte char (ş) split exactly between its two bytes",
    "preserves newline segmentation when multi-line Turkish is fed byte-by-byte",
    "buffers a fenced code block fed byte-by-byte and emits it whole, intact",
    "string feed of the same content is identical to the byte feed (whole-codepoint path unchanged)",
    "ASCII byte feed segments identically to ASCII string feed (no behavior change)"
   ],
   "cli/repl/string-free-closure.test.ts": [
    "${lang}: no field is empty, undefined or a key echo",
    "the reverse-search prompt is a catalog row (en+tr) carried by ReplLabels",
    "resolveModeLabel",
    "buildResumePickerLines / resolveResumeCommand",
    "renderBusyDecision",
    "formatApprovalClosure / resolveSwitchGate / formatTurnErrorLine",
    "throws the typed error naming the first missing field",
    "renders with the injected set (tr) and never the English mechanism words",
    "resolves ≥ 4 distinct verbs for en and tr, and the two languages differ",
    "createThinkingTicker requires a non-empty verb list (typed error, no built-in Turkish list)",
    "buildToolActivityVerbs resolves a verb for every built-in tool in en and tr, and renderToolActivity uses it",
    "${file}: no DEFAULT_*_LABELS export, no \\`labels.x ?? '…'\\` fallback, no THINKING_VERBS list",
    "entry.ts resolves the non-TTY spinner label and the ticker verbs from the catalog"
   ],
   "cli/repl/structured-action-authority.test.ts": [
    "confirms the canonical projection before one capture: %j",
    "runs read-classified audit action without a confirmation: %j",
    "denial returns the exact argv summary and performs zero capture",
    "rejects a forged request before confirmation or capture",
    "rejects null at the public runtime boundary without policy or capture work",
    "binds approval and capture to one immutable snapshot despite caller mutation while confirmation waits",
    "injects all action labels without mechanism-owned prose"
   ],
   "cli/repl/structured-action-ingress.test.tsx": [
    "picker close performs no action, selection captures once and exposes full detail (%s)",
    "audit gate selection requests an explicit target before dispatch",
    "posture denial precedes the authorized capture seam",
    "invalid actions cannot fall through to a legacy or read dispatcher",
    "confirmation denial restores input without installing a fabricated result",
    "closing a pending view cannot dispatch a concurrent write action"
   ],
   "cli/repl/structured-action-picker.test.ts": [
    "builds one localized picker without granting authority (%s)",
    "resolves %s through the same closed request contract",
    "requires an explicit gate target and keeps unrelated commands untouched",
    "rejects %s without selecting an action",
    "rejects unsafe or reserved gate id %j",
    "keeps terminal controls out of confirmation filters %j"
   ],
   "cli/repl/term-compat-matrix.test.ts": [
    "narrow width truncates a long single-field line with a trailing ellipsis",
    "wide width leaves a short line untouched (no truncation)",
    "width=1 edge case (resize to minimal size) truncates safely without throwing",
    "a simulated live resize (width change across two renders) truncates every field independently",
    "idle (empty) state single line also respects width after a resize",
    "a single-chunk paste (no embedded newline) inserts as one unit mid-buffer",
    "a paste at the very start of the buffer prepends and advances the cursor",
    "a paste at the very end of the buffer appends, cursor lands at the new end",
    "a stray unmapped raw control byte at the sequence head is dropped whole, not partially inserted",
    "a literal tab embedded inside a multi-char paste is preserved as-is (normalization only applies to a lone standalone Tab keypress)",
    "left/right move the cursor within bounds and clamp at both edges",
    "Home/End jump to the start/end of the buffer",
    "Backspace/Delete edit around the cursor and no-op safely at buffer boundaries",
    "up/down keys surface a history-navigation signal without mutating the buffer",
    "InputHistory older/newer navigation preserves the in-progress draft (↑↑↑ then ↓↓↓ returns to it)",
    "consecutive duplicate submits are deduped, non-consecutive duplicates are kept (most-recent-first)",
    "bounded history (max=200) evicts the oldest entry once the cap is exceeded",
    "Ctrl-C clears the buffer and signals an interrupt, regardless of content/cursor",
    "Ctrl-D signals eof only on an empty buffer; on a non-empty buffer it is a no-op",
    "Ctrl-U clears the whole line regardless of cursor position",
    "Ctrl-A/Ctrl-E (raw-mode emacs-style jump) are equivalent to Home/End",
    "a lone standalone Tab keypress (raw byte 0x09) inserts two spaces",
    "an unmapped Ctrl-combo (e.g. Ctrl-K, unbound in this app) passes through safely — no throw, no mutation"
   ],
   "cli/repl/term-gate.test.ts": [
    "a declared registry tag wins; exec tools map by name; CLI-bridge tools derive from their confirm tier",
    "Ask allows only reads; Run allows up to execution; Control allows everything",
    "resolveShellLine extracts the command; a bare `!` is not a shell line",
    "isDeniedShellOutput recognizes the exec dispatcher denial marker",
    "shell notes are bounded (count + output tail) and render as a prefix for the next prompt",
    "substitutes target, risk (risk-language), mode and the suggested mode in en and tr",
    "app.tsx gates slash dispatches, the confirm trigger and the `!` shell line, and prepends shell notes at the submit boundary",
    "run.tsx consults the registered action gate BEFORE the approval-mode shortcuts"
   ],
   "cli/repl/term-posture-default.test.ts": [
    "defaults to run; an explicit posture wins",
    "resolveConfiguredPosture accepts only the three tokens (case-insensitive) and falls back to the default",
    "has metadata with the three options and the run default, in both description languages",
    "validatePartialConfig accepts the tokens and refuses anything else",
    "run.tsx resolves the configured posture and app.tsx starts from it"
   ],
   "cli/repl/terminal-glyphs.test.tsx": [
    "keeps the exact rich Unicode default and exposes punctuation-only ASCII",
    "maps a known catalog template before interpolation and preserves raw Unicode bytes",
    "provides the same closed contract to mounted Ink consumers",
    "budgets one/two/three-cell markers and never splits CJK or emoji graphemes",
    "propagates the separator through transcript/card pure producers without rewriting data"
   ],
   "cli/repl/terminal-resize-mediator.test.ts": [
    "substitutes tool-read fields literally without replacement-pattern interpretation",
    "publishes target dimensions before releasing renderer rows and its resize listener",
    "coalesces rapid targets without releasing an intermediate row extent",
    "handles row-only and growing changes with the same sequenced contract",
    "exposes a real growing row extent before subscribers render the target frame",
    "retains the largest safe extent across a rapid grow then shrink until the latest compact frame flushes",
    "reports flush rejection or timeout without releasing renderer geometry",
    "disposes pending work without late render notification and restores only its own physical listener",
    "delegates stream writes/backpressure and non-resize listeners without patching the source",
    "keeps once and prepend resize listeners behind the flush boundary"
   ],
   "cli/repl/tool-read-card.test.tsx": [
    "reuses complete detail wrapping across page keys and recomputes for new geometry",
    "navigates immutable rows, exposes detail and peels Esc before closing",
    "defers every key when its caller-owned mutex is inactive",
    "renders loading distinctly and exposes full immutable capture metadata under its own section",
    "uses ASCII card chrome/state separators without rewriting projected data"
   ],
   "cli/repl/tool-read-ingress.test.tsx": [
    "budgets the card with its surrounding Workline and retains global Ctrl-C while input is paused",
    "keeps a full stderr capture separately pageable beyond its preview without another command",
    "preserves readable stdout when only the stderr detail reader is unavailable",
    "preserves every scalar exactly once across real overlapping byte windows",
    "uses one direct read invocation for a native doctor slash and keeps useful nonzero JSON",
    "shows a distinct loading state, then captures one immutable observation time and full capture metadata",
    "pages a complete large capture through opaque ranges without claiming partial capture",
    "aborts the exact pending opaque read on unmount and ignores late completion",
    "opens an unknown complete JSON document as bounded raw detail",
    "keeps a typed detail hold visible rather than treating it as empty"
   ],
   "cli/repl/tool-read-model.test.ts": [
    "keeps doctor diagnostic rows when the execution outcome is later nonzero",
    "distinguishes valid empty from malformed and unsupported schemas",
    "keeps model authority HOLD as unavailable rather than a successful empty catalog",
    "retains catalog metadata and rejects a loose model-shaped object",
    "uses the producer count and leaves it unknown when the count is invalid",
    "keeps the complete history record and its unknown counts without accepting a partial schema",
    "preserves skill activation, exposure, statistics, explicit null and additive fields",
    "keeps doctor aggregate and nested additive diagnostics in their original sections",
    "retains active authority and snapshot details in both ready and HOLD states",
    "sanitizes control and bidi code points only in the display projection",
    "preserves the canonical never-used agent null success rate without inventing zero success",
    "parses the optional canonical sync report without inventing aggregate success or a count",
    "prepends an aggregate summary row ahead of the raw sync report and derives the conflict count from it",
    "renders a dash for commits and omits the empty conditional fields when the aggregate summary has no git repository",
    "root-fallback: the `sync:aggregate` row surfaces a `detection` field carrying the mode",
    "unavailable (with an issue): the row's `commits` count still shows the raw number, plus a `detection` field",
    "malformed detection: an out-of-range mode, and a non-null issue with an unknown code, both fail closed to schema-unknown",
    "legacy gitChanges without `detection`: still valid, and the derived aggregate fails closed to `detection: \\'unavailable\\'` on the row",
    "range: the common case adds no `detection` field to the row",
    "unavailable + %s: the raw detection issue code round-trips through a matching summary as `valid`",
    "an unknown raw issue code (`GIT_FOO`) fails closed to schema-unknown",
    "incoherent raw: mode `range` paired with a non-null, otherwise well-formed issue object fails closed to schema-unknown",
    "the `sync:aggregate` row places `detectionIssue` immediately after `detection` for `unavailable`, omits `detectionIssue` (but keeps `detection`) for `root-fallback`, and carries neither for `range`",
    "a `commits: 0` raw with GIT_LOG_FAILED renders as `valid` with `commits: '0'` AND `detectionIssue: 'GIT_LOG_FAILED'` on the row — the REPL must show why a zero is untrusted",
    "fails closed to schema-unknown rather than partially rendering a malformed aggregate summary",
    "accepts a summary that is exactly buildSyncAggregate applied to the same raw report (real producer shape)",
    "sanity: the unmodified raw/summary pair is internally consistent and renders valid",
    "adapters: raw reports zero synced adapters while the forged summary claims 999",
    "agentPrompts: a forged created count disagrees with the raw report",
    "skills: a forged unchanged count disagrees with the raw report",
    "workspace: a forged unchanged count disagrees with the raw report",
    "git: a forged commits count disagrees with the raw report",
    "git: a forged `detection` disagrees with a raw report whose provenance was actually a clean range (7104: a git failure must never look like \"no changes\", nor the reverse)",
    "git: a forged `issueCode` disagrees with the raw report’s actual issue code (raw names `GIT_LOG_FAILED`, summary claims `GIT_DIFF_FAILED`)",
    "git: a forged null `issueCode` disagrees with a raw report that names a real issue",
    "git: a forged null block disagrees with a raw report that has real git changes",
    "git: a forged non-null block disagrees with a raw report that has no git changes",
    "missingBaseline: a forged count/agentIds pair disagrees with the raw report, despite being internally shape-consistent",
    "conflicts: an extra entry with no corresponding raw conflict record is rejected",
    "warnings: a forged warnings count disagrees with the raw report",
    "rejects a summary whose conflicts contain a missing-baseline entry even when it is otherwise shape-consistent",
    "leaves state and count unchanged for the canonical sync sample when no aggregate summary is present",
    "keeps producer error JSON visibly raw and never promotes it to a successful structured action",
    "parses audit gate verdict as data independently from the attached process exit truth",
    "keeps an empty audit query as an explicit scanned/matched summary, not an invented success",
    "parses compliance controls, empty actors, broken chains and additive evidence without inference",
    "agentCapabilitiesSync: array-like `migrated`/`issues` are rejected even when a hand-built summary claims matching counts (only the deep validator catches this — the equality gate alone cannot)",
    "agentPromptSync: array-like `created` is rejected",
    "agentPromptSync.conflicts: a non-record entry, and separately an entry with an out-of-range `kind`, are both rejected",
    "skillManifestSync.issues: a record missing `skillId`/`reason` is rejected",
    "agentCapabilitiesSync.issues: a record missing `code`/`message` is rejected",
    "agentManifestSync.keptLocal: a string instead of a string array is rejected",
    "accepts a real producer-shaped report with all four nested reports fully populated, including conflicts of both kinds and a non-empty capabilities.protected",
    "accepts a legacy conflict entry with no `kind` field at all (predates the field; buildSyncAggregate conservatively classifies it as local-edit)"
   ],
   "cli/repl/tool-target.test.ts": [
    "file read: path + inclusive line range / legacy offset+limit / outline+search modes",
    "shell: first line of the command only, credentials redacted",
    "content ref: digest prefix + byte window, never the whole digest",
    "grep/glob: pattern (+ path); other tools: url/query; nothing displayable → empty",
    "clips a long target to the glance budget with an ellipsis",
    "maps native read/exec tools to compact i18n verbs",
    "joins verb and target without exposing the raw tool id",
    "rounds and clamps at zero; undefined start → undefined"
   ],
   "cli/repl/transcript-turn-view.test.tsx": [
    "renders user lines on inverse panel without a speaker label",
    "renders user-ingress on the inverse panel without the user label",
    "renders assistant segments flush without deckent header chrome",
    "shows every compact-table cell on a narrow TTY including the last column sentinel",
    "renders tool lines as verb + target without internal tool id",
    "renders operator strip rows with the same shape as legacy tool rows"
   ],
   "cli/repl/turn-abort-wire.test.tsx": [
    "is false while idle; true during a turn, and the blocked provider stream ends promptly",
    "cancelTurn, hydrateTranscript, getContextBudgetTokens, setApprovalMode, close and renewBudgetEpoch survive the wrap",
    "a canceller returning true → interrupted+aborted; returning false → interrupted but not aborted",
    "renders honest lines: an aborted turn vs an engine without an abort seam (en + tr catalog rows)",
    "Esc with an empty composer calls onEscape",
    "Esc while the `/` menu is open closes the menu and does NOT call onEscape",
    "the interrupt canceller aborts the native turn (cancelTurn) — not only the queue",
    "Esc-interrupt is delivered through the composer (onEscape), not a flag-gated App-level Esc hook",
    "/interrupt is handled regardless of repl_surface.enabled",
    "routes /interrupt, Escape, and Ctrl-C through the same activity-aware interrupt helper"
   ],
   "cli/repl-agentic-enterprise-wire.test.ts": [
    "/cost → dispatchEnterpriseSlash invoked (provider NOT called)",
    "/cost --json → enterprise spawnFn gets the extra args appended",
    "/audit → enterprise dispatched (audit subcommand)",
    "\"durum ne\" (agenticDispatch: true) → classifyAgenticIntent + dispatchAgenticIntent fire",
    "plain chat \"merhaba\" → provider called, neither enterprise nor agentic dispatch fires",
    "/recall docker (registry agentic) → deckent_memory_query dispatched, enterprise NOT called",
    "/foobar (unknown slash) → falls through to provider, enterprise NOT called",
    "status intent → dispatcher.dispatch called, provider NOT called",
    "plain chat (\"hello\") → dispatcher NOT called, provider called",
    "recall intent → dispatcher.dispatch deckent_memory_query with extracted query",
    "risky intent (\"sprint planla\") + declined confirm → dispatcher NOT called, cancel echoed",
    "risky intent (\"sprint planla\") + approved confirm → dispatcher called",
    "agenticDispatch: false (default) → backward-compatible, agentic intents fall through to provider"
   ],
   "cli/repl-banner-wire.test.ts": [
    "entry.ts imports renderBanner from chat-banner",
    "entry.ts calls renderBanner (wire present)",
    "entry.ts injects the catalog hint (tui.banner.hint) into renderBanner — the mechanism has no fallback text",
    "returns non-empty banner string when TTY=true (banner is rendered)",
    "returns empty string when TTY=false (TTY-only, pipe context suppressed)",
    "reflects the active provider name in banner output",
    "renders status line with provider and dir by default (undefined config)",
    "reflects the provider name in the rendered output",
    "returns empty string when status_line config is false (config-kapalı→yok)",
    "shows dir correctly when status_line is true",
    "includes active sprint when present",
    "omits sprint when activeSprint is null"
   ],
   "cli/repl-clear-ansi.test.ts": [
    "writes the full clear-screen + scrollback + home sequence on a TTY stream",
    "the sequence erases the visible screen (2J), the scrollback (3J), and homes the cursor (H)",
    "never writes raw escape codes to a non-TTY stream (piped/redirected stdout)",
    "treats a missing isTTY (undefined) the same as non-TTY — no write",
    "a turn stamped at the current clear-epoch is live (normal turn, no /clear in flight)",
    "a turn stamped BEFORE a clear-bump goes stale — its stream is cancelled",
    "a turn started AFTER the clear renders normally under the new epoch",
    "a second /clear during the same still-stale turn does not resurrect it",
    "a fresh segmenter after clear renders post-clear tokens with no pre-clear residue"
   ],
   "cli/repl-confirm-queue.test.ts": [
    "3-confirm-sequential: each card is shown + resolved in arrival order",
    "pending-not-overwritten: a 2nd trigger queues behind the 1st (no orphan)",
    "deny-continue: denying one card does NOT cancel the rest of the queue",
    "always-applies-to-queue: an \"a\" decision auto-resolves the same-tool remainder",
    "always without a toolName does NOT auto-apply (ALWAYS-tier safety)",
    "answering an empty queue is a no-op (defensive)",
    "[i/N] grows when a card arrives mid-burst"
   ],
   "cli/repl-cursor-model.test.ts": [
    "splits by code point, defaulting cursor to end-of-buffer",
    "round-trips back to the original string",
    "clamps an out-of-range explicit cursor into [0, length]",
    "never bisects a surrogate pair into the graphemes array",
    "left/right move one code point at a time",
    "home/end jump to the buffer edges",
    "clamps left at position 0 as unchanged (no negative cursor)",
    "clamps right at buffer end as unchanged (no overrun)",
    "home/end are unchanged (not \"moved\") when already at that edge",
    "moves over an astral/surrogate-pair character as ONE atomic step (closes ADR-D-010 KALAN (a))",
    "a ZWJ-joined compound sequence is ONE grapheme: it moves as a single unit (TERMINAL-TOOLS-005 closed the former code-point gap)",
    "ASCII characters are width 1",
    "CJK ideographs are width 2",
    "common emoji (astral-plane pictographs) are width 2",
    "combining marks are width 0",
    "zero-width joiner is width 0",
    "displayWidth sums per-code-point widths across mixed text",
    "displayWidth of an empty string is 0",
    "does not wrap when the buffer fits within terminalWidth",
    "wraps narrow characters exactly at the column boundary",
    "never splits a wide glyph across two rows — it wraps whole to the next row",
    "reports the caret display position at an arbitrary mid-buffer cursor after a wrap",
    "reports the caret at the very end of a wrapped buffer",
    "reports the caret at the very start (row 0, column 0)",
    "handles an empty buffer as a single empty row with the caret at (0,0)",
    "guards against a non-positive terminalWidth without looping forever",
    "inserts text at an arbitrary mid-buffer cursor, advancing the cursor past the inserted text",
    "inserts multi-code-point text (incl. a surrogate pair) as one atomic block",
    "insert with empty/undefined text is a no-op",
    "backspace removes the whole code point before the cursor, never a lone surrogate",
    "backspace at position 0 is a no-op",
    "delete removes the whole code point at the cursor without moving it",
    "delete at end-of-buffer is a no-op",
    "a mid-buffer edit sequence (insert, then backspace) composes correctly"
   ],
   "cli/repl-deny-toolsink.test.ts": [
    "regression: denied confirm-tier tool calls toolSink with a failed=true block, not silence",
    "approved path is unaffected: confirm-tier tool runs cliDispatcher and reports the real outcome",
    "read-tier tool never prompts for confirmation (approved/no-gate path unaffected)",
    "existing EXEC_TOOLS [deckent-denied] path still renders its own toolSink block (regression guard)",
    "always-tier tool uses askConfirmAlways, and denial still renders the toolSink block"
   ],
   "cli/repl-dispatch-parity.test.ts": [
    "/nervous carries category/risk cross-referenced from command-registry.ts",
    "/autonomous carries category/risk cross-referenced from command-registry.ts",
    "/mcp carries category/risk cross-referenced from the mcp-bridge (REPL-surface) command-registry entry",
    "an untagged entry (e.g. /help) has category/risk undefined",
    "/nervous (bare) lists pending via listPendingNervous — read-only, no plan",
    "/nervous list is equivalent to bare /nervous",
    "/nervous accept <id> builds an unapplied accept plan that applies correctly via a fake executor",
    "/nervous reject <id> <reason...> builds a reject plan carrying the reason",
    "/nervous edit <id> <json> builds an accept-with-modifiedPayload plan that applies correctly via a fake executor",
    "/nervous edit <id> key=value ... parses key=value payload form",
    "/nervous edit <id> {bad json} → nervous.slash_edit_invalid_json message, no plan built",
    "/nervous edit <id> badtoken → nervous.slash_edit_invalid_kv message",
    "/nervous edit <id> (no payload) → nervous.slash_edit_payload_required message",
    "/nervous accept (no id) → nervous.slash_id_required message",
    "/nervous accept <unknown-id> → nervous.slash_not_found message",
    "/nervous frobnicate → chat.slash_unknown_subaction message",
    "resolveSlash(\"/nervous accept <id>\", registry, store) dispatches through resolveNervousSlash",
    "resolveSlash(\"/nervous\", registry) WITHOUT a store — backward-compatible, resolves to none",
    "/autonomous (bare) defaults to status",
    "/autonomous start",
    "/autonomous backlog list",
    "/autonomous backlog add <title>",
    "/mcp (bare) → honest not-wired notice (unchanged from before this task)",
    "/mcp list → same not-wired notice as bare (this fallback layer only fires with no live bridge)",
    "/mcp restart → honest unknown-subaction hint (no restart verb exists in the mcp bridge)"
   ],
   "cli/repl-do-slash-wire.test.ts": [
    "mount composed → /do drives proposeRun(goal) → shared preview seam",
    "mount returns undefined without building a controller (zero fs/planner)",
    "/do prints the real getMessage flag-off notice, no fs/preview side effect",
    "localizes the flag-off notice (tr)",
    "/exit breaks the loop without calling provider",
    "/quit breaks the loop without calling provider",
    "/clear empties the transcript and continues; subsequent line reaches provider",
    "normal line falls through to provider (slash does not swallow regular input)",
    "uppercase /EXIT is normalized and breaks the loop",
    "%s: reportError carries the localized template with {model}/{provider}/{registered} filled — never the raw message",
    "buildDoSlashLabels carries the noProviders template (en/tr pin)",
    "a non-hold controller failure still reports its raw message (unchanged edge)"
   ],
   "cli/repl-errorboundary-i18n.test.ts": [
    "run.tsx passes the label prop from getMessage(\\'tui.render_error\\', lang) — not left unset/hardcoded",
    "renders the label supplied via props, proving it is prop-driven and not a fixed hardcoded string",
    "lang=tr resolves tui.render_error to the Turkish message",
    "lang=tr → the boundary renders the Turkish fallback text",
    "lang=en → the boundary renders the English fallback text",
    "app.tsx carries no mechanism-owned English fallback for the boundary label",
    "run.tsx injects a catalog-backed error describer alongside the label",
    "lang=${lang} → an InjectedLabelMissingError renders the ${lang} catalog explanation, never the error's own text",
    "the describer passes an ordinary error message through unchanged (technical pass-through, no invented prose)",
    "without a describer the boundary renders the typed error\\'s technical code only"
   ],
   "cli/repl-i18n-flip.test.ts": [
    "mode indicator (/ask · /run · /control) renders Turkish",
    "resume-picker (header/hint/switched/not-found/ambiguous) renders Turkish",
    "busy-controls (/queue · /interrupt · /steer) render Turkish",
    "ApprovalCard labels (hint/details/no-args/risk badges) render Turkish",
    "mode indicator matches the old hardcoded English fallback",
    "resume-picker matches the old hardcoded English fallback",
    "busy-controls match the old hardcoded English fallback",
    "ApprovalCard labels match the pre-387-001 English strings exactly (the mechanism owns no default object since TERMINAL-TOOLS-002)",
    "${key} resolves to distinct, non-empty en/tr text (not the raw key)",
    "lang=\"en\" selects the English immutable core + project-info header",
    "lang=\"tr\" selects the Turkish immutable core (unchanged constant)",
    "omitting lang stays byte-identical to the pre-387-001 default (Turkish core)"
   ],
   "cli/repl-input-bar-menu-submit.test.ts": [
    "uncatalogued command + Enter → the buffer, never the menu selection (the /help bug)",
    "fallback mode ignores whatever row is selected",
    "prefix-matching buffer + Enter → the selected command name",
    "bare `/` prefix-matches everything → selection wins (browse-and-pick flow)",
    "matching is case-insensitive, mirroring filterSlashCommands",
    "out-of-range selection degrades to the buffer (never a crash)",
    "/term IS catalogued now → real match, Enter submits /term itself",
    "/run now prefix-matches the SURF-3 /runs inbox → Enter autocompletes to /runs"
   ],
   "cli/repl-input-queue.test.ts": [
    "starts empty",
    "dequeues in exact enqueue order",
    "snapshot reflects pending order oldest-first without mutating the queue",
    "interleaved enqueue/dequeue preserves every accepted line, in order",
    "a burst of rapid distinct lines all survive intact",
    "swallows an empty line without queuing it",
    "swallows a whitespace-only line (trimmed to empty)",
    "swallows an immediate duplicate resubmission of the same trimmed text",
    "treats surrounding-whitespace variants as the same duplicate text",
    "does NOT swallow a different line following a queued one",
    "allows the same text again once it has been dequeued (not a permanent block)",
    "removes all pending lines",
    "resets the duplicate guard so the same text can be queued again right after",
    "is a safe no-op on an already-empty queue"
   ],
   "cli/repl-input-terminal.test.ts": [
    "TTY → terminal: true (enables line-editing + arrow keys)",
    "TTY → output bound to process.stdout (required for terminal rendering)",
    "TTY → history enabled (↑/↓ recall), historySize 100",
    "non-TTY → line-only reader: no terminal, no output, no history",
    "both modes attach input to process.stdin"
   ],
   "cli/repl-legacy-loop-lang-wire.test.ts": [
    "the legacy loop call in entry.ts carries `lang` resolved from the project config",
    "lang: tr → Turkish header and description",
    "lang: en → English header and description"
   ],
   "cli/repl-mcp-dispatch.test.ts": [
    "/mcp listele → renderMcpSlashLines returns one entry per namespaced tool",
    "namespaced çağrı → confirm callback is invoked with the resolved action",
    "confirm-onay → broker.callTool fires and an audit event is written",
    "reddet → broker.callTool NOT invoked + cancelled audit event written",
    "unknown namespaced name → no callTool, no confirm, error output + unknown-tool audit"
   ],
   "cli/repl-model-busy-gate.test.ts": [
    "idle → apply (idle çalışır tıpkı öncesi gibi, byte-identical path)",
    "busy → rejected, with the injected en line naming the switch kind",
    "busy → rejected honors a caller-supplied localized {kind} template",
    "never returns rejected while idle regardless of labels",
    "busy /model <arg> → onSwitch is NEVER called; the reject line is returned instead",
    "busy /provider <arg> → onSwitch is NEVER called; the reject line is returned instead",
    "idle /model <arg> → onSwitch runs exactly once, no reject line (idle davranışı korunur)",
    "idle /provider <arg> → onSwitch runs exactly once, no reject line",
    "busy bare /model (no arg) → status query, NOT gated — onSwitch never applicable here",
    "rapid busy /model then /provider both reject independently — no partial splice of either"
   ],
   "cli/repl-nervous-wire.test.ts": [
    "/nervous lists pending notifications (non-TTY)",
    "/nervous renders the visible banner on TTY (ansi + ») when pending exists",
    "/nervous accept <id> removes the notification from pending",
    "/nervous reject <id> removes the notification from pending",
    "/nervous with no pending emits \"bekleyen yok\" — no \"Unknown command\""
   ],
   "cli/repl-prompt-echo.test.ts": [
    "interactiveTty: true → does NOT re-echo the user line (no double print)",
    "interactiveTty: true → suppresses the loop header (ticker owns `● deckent`), reply still streams",
    "non-TTY (interactiveTty omitted) → KEEPS the `› line` echo (pipe contract)",
    "layout off → no chrome regardless of interactiveTty"
   ],
   "cli/repl-provider-parity.test.ts": [
    "claude → adapter with send + stream",
    "codex → adapter uses codex binary with exec --full-auto args",
    "gemini → adapter uses gemini binary with -p flag",
    "ollama → zero-API HTTP adapter calls /api/generate (no ANTHROPIC_API_KEY needed)",
    "openai-compatible → HTTP /v1/chat/completions adapter",
    "all CLI providers produce the same ProviderResponse shape",
    "all HTTP providers produce the same ProviderResponse shape",
    "throws a descriptive error for unknown provider (no silent fallback)",
    "error message lists all valid providers",
    "ollama throws on HTTP error (not a silent empty response)",
    "openai-compatible throws on HTTP error",
    "ollama adapter never passes API keys to the fetch call",
    "claude spawn env has ANTHROPIC_API_KEY stripped",
    "returns adapter when opts is omitted (uses production spawn/fetch)"
   ],
   "cli/repl-provider-resolve.test.ts": [
    "chat_provider=ollama uses the HTTP ollama adapter via injected fetch (no claude-spawn)",
    "ollama posts to the /api/generate endpoint on localhost:11434 by default",
    "claude routes through host CLI spawn (no fetch, no skeleton)",
    "deepseek routes to the OpenAI-compatible HTTP adapter (POST /chat/completions)",
    "qwen + glm presets also route to OpenAI-compatible HTTP (provider-parity)",
    "deepseek without DEEPSEEK_API_KEY surfaces a clear ProviderError (no silent skeleton)",
    "connection-refused fetch is wrapped with a clear \"Ollama (...) is unreachable\" message",
    "NET error mentions \"ollama serve\" hint so the user knows the fix",
    "unknown name throws a clear error mentioning the widened valid set"
   ],
   "cli/repl-render-region.test.ts": [
    "non-TTY → plain passthrough (text + newline), no readline calls",
    "TTY → sets `› ` prompt, writes the text, then redraws prompt (preserves buffer)",
    "reprompt → redraws on TTY, no-op off-TTY",
    "yields buffered lines in order, then ends on close",
    "ends immediately on close with no lines",
    "non-TTY → start/stop are no-ops (no writes, no throw)",
    "TTY → start shows `● deckent` + a FIXED verb; only the braille frame animates",
    "TTY → stop finalizes to plain `● deckent` + newline and clears the timer",
    "coalesces a burst of lines (within window) into ONE message",
    "single line → one message after the window",
    "two bursts separated by > window → two messages",
    "flush() emits the buffered message immediately",
    "known tool → Turkish verb + target (non-TTY plain)",
    "bash → komut çalıştırıyor + cmd",
    "unknown tool → raw name",
    "TTY → dim-wrapped only when the color gate allows it",
    "emits a complete line only on \\\\n; holds the partial",
    "splits multiple newlines in one chunk",
    "flush emits the trailing partial line",
    "flush with empty buffer is a no-op",
    "NO_COLOR=1 → no SGR at all in the painted line or the finalize line; the erase-line control stays",
    "FORCE_COLOR=1 → brand SGR present (proves the colored path is reachable)"
   ],
   "cli/repl-slash-registry-wire.test.ts": [
    "/help renders the registry list and does NOT call the provider",
    "/runs (SURF-3 inbox) renders the read-only list and does NOT call the provider",
    "/status dispatches deckent_status through the MCP dispatcher (NOT provider)",
    "unknown slash /foobar falls through to the provider (not swallowed)",
    "/clear still empties the transcript (handleReplCommand path preserved)",
    "/recall <query> extracts trailing words as the deckent_memory_query arg",
    "bare /recall prompts locally, then dispatches the next full query without a provider turn",
    "bare /recall followed by /cancel performs no provider or tool call",
    "bare /recall does not capture canonical /clear or /exit controls as a query",
    "user mode filters the 4 enterprise slashes out of the live registry",
    "enterprise mode returns the full live registry unfiltered",
    "/help itself is always visible, in both modes",
    "user-mode result is a strict subset of the enterprise-mode result"
   ],
   "cli/repl-streaming.test.ts": [
    "yields one delta per assistant event and skips system/result events",
    "reconstructs a delta when one NDJSON line is split across multiple chunks",
    "emits an unparseable NDJSON line as raw text rather than swallowing it",
    "non-JSON stdout streams chunks verbatim (legacy --print batch mode)",
    "multi-chunk raw stdout preserves chunk boundaries (no buffering in raw mode)",
    "emits no text but still yields a final done event with empty text",
    "treats empty-string chunks as no-ops (skipped, done still fires)",
    "emits exactly one done chunk and it is the terminal yield",
    "done.text equals the concatenation of all yielded text chunks",
    "returns the text delta from an assistant event",
    "concatenates multiple text parts inside one assistant content array",
    "returns null for non-text event types (system / result)",
    "returns undefined for non-JSON lines (caller falls back to raw passthrough)",
    "returns \"\" for assistant events that have no text parts (tool_use only) — caller skips empty deltas"
   ],
   "cli/repl-surface-wire.test.tsx": [
    "maps known and unknown codes without losing the technical identity (%s)",
    "does not reflect an unknown or prototype-shaped reason: %s",
    "classifies the worker resource-limit reason as a known bounded failure",
    "keeps the default/explicit disabled slot inert before principal or archive access",
    "refuses an incomplete or invalid resolved limit before archive access",
    "requires an actual local os-user principal regardless of principal_enforce",
    "holds strict tenant isolation before exposing a reader for an unbound archive",
    "forwards the immutable root, exact sprint, limits, and AbortSignal to the canonical reader",
    "throws the typed guard error when no labels are supplied (no English fallback)",
    "uses caller-supplied labels when present (i18n-first seam)",
    "a partial override replaces only the supplied mode; a mode with no label is a typed error, never English",
    "composes with term-mode.ts parseTermCommand + applyModeTarget — end-to-end mode-switch flow",
    "empty payload list → empty text list",
    "one payload, one event → one text (no trailing newline)",
    "one payload, multiple coalesced events → newline-joined single text",
    "multiple payloads → one text per payload, order preserved",
    "integrates with the real ChatTurnQueue — mid-turn buffer, turn-end drain (Hermes rule)",
    "renders the teaser when the jobs source has sessions (fixture 1)",
    "renders NOTHING when the source is empty (fixture 2 — degrade-safe)",
    "numbers disk rows first, then chat rows, one shared number-space",
    "uses caller-supplied labels when present (i18n-first seam)",
    "maps id/preview/lastAt and marks the rows as chat sessions",
    "hydrates ledger messages byte-for-byte and never consults legacy on collision",
    "falls back to legacy history and converts only provider-compatible chat roles",
    "reports missing without invoking hydration",
    "permits archive eligibility only after exact ledger and legacy chat are both absent",
    "treats an unavailable memory adapter with an existing canonical DB as unknown, not absent",
    "turns malformed runtime history %j into a typed failure",
    "turns read and hydrate exceptions into a typed failed attempt",
    "deduplicates picker ids with ledger precedence",
    "no local sessions at all → passthrough for every form (loop behavior byte-identical)",
    "bare /resume → the numbered picker list (teaser-aligned)",
    "numeric pick of a disk row → switch, sessionId CHANGES, no loop forward",
    "numeric pick of a resumable row → switch for direct hydration (never the raw typed id)",
    "exact session id → switch (id match beats title matching)",
    "numeric out-of-range → reject (a number must NEVER pass through to the loop)",
    "unknown literal id → passthrough (the loop may know it — behavior-merge)",
    "ambiguous title prefix → reject listing every match",
    "switch/reject lines honor caller labels ({id}/{arg} templates)",
    "idle × /queue → idle status line with the background bucket count",
    "busy × /queue → busy status line, count from the REAL ChatTurnQueue",
    "idle × /interrupt → noop line, canceller NEVER invoked",
    "busy × /interrupt → interrupt line, canceller invoked exactly once (real abort seam)",
    "busy × /interrupt on an engine without an abort seam → the honest \"not available\" line",
    "double interrupt (Esc then Esc / Esc then /interrupt) is idempotent — canceller fires once",
    "idle × /steer → noop line, nothing queued",
    "busy × /steer <msg> → FIFO positions #1, #2; blank message → usage line",
    "renderBusyDecision honors caller labels ({count}/{state}/{position} templates)",
    "resolveKeyAction('escape') → 'interrupt' (the contract app.tsx's useInput wires)",
    "steer notes drain ONLY at turn end, ahead of pending input, exactly once"
   ],
   "cli/repl-tool-multi-tag-repro.test.ts": [
    "finds ALL tags regardless of surrounding prose (matchAll, /gi)",
    "SEQUENTIAL dispatch (engine for…of await): all 3 confirms reach the user",
    "CONCURRENT confirms: the 2nd overwrites the 1st resolver → 1st is orphaned",
    "CONTROL: tag streamed as a content_block_delta → found (happy path)",
    "H2-A: tag carried only in the `result` event is still dispatched (reconciled)",
    "H2-B: tag carried only in the `assistant` complete-message event is dispatched",
    "H2-C: tags split across delta + assistant blocks → all 3 found (no undercount)",
    "CONTROL: a single tool result is fed back to the model (regression guard)",
    "H3: 3 trailing tool results → all three reach the model"
   ],
   "cli/repl-turn-exception.test.ts": [
    "uses a monotonic default duration when the wall clock moves backwards",
    "surfaces a thrown turn as onTurnError instead of rejecting (görünür hata, sessiz-donma değil)",
    "keeps driving the loop after a crash — the NEXT turn runs normally (normal-tur bozulmaz)",
    "never turns the exception into a process-crash-shaped rejection (graceful kal)",
    "normalizes a non-Error throw (thrown string) into a string message",
    "completes cleanly (no callbacks) when no line ever throws",
    "persists each completed turn with the user line + streamed assistant text",
    "accumulates multi-chunk streamed output into one assistant string",
    "a tools-only turn (no visible text) still persists the user line with empty assistant text",
    "does NOT persist a turn that threw (nothing half-written)",
    "still delivers output to the display sink when persistTurn is wired",
    "throws the typed guard error when the label is missing — no English fallback",
    "substitutes {error} into a caller-supplied localized label",
    "always prefixes ⚠ regardless of label — the glyph is owned by the function, not the label"
   ],
   "cli/resources-command.test.ts": [
    "registers a \"resources\" command",
    "command has a description",
    "returns no_containers message for empty samples",
    "returns Turkish no_containers message for tr lang",
    "renders container and task columns",
    "renders memory usage and percentage",
    "renders CPU percentage",
    "renders multiple containers",
    "includes table headers",
    "returns log_empty message for empty summaries",
    "renders task summary row",
    "renders sprint peak line",
    "renders peak mem and cpu columns",
    "uses defaults when config is empty",
    "uses config values when present",
    "computes correct RAM ceiling",
    "outputs valid JSON with samples and config keys",
    "shows no_containers message when docker returns empty",
    "shows config line even when no containers",
    "shows log_not_found message when log file is missing",
    "renders log summary table from JSONL file",
    "renders --log --json with summaries and sprintSummary keys"
   ],
   "cli/resume-budget-reauthorization.test.ts": [
    "persists current owner policy for pending dynamic FIX tasks before SPAWN",
    "fails closed without rewriting task authority when policy is unavailable",
    "removes only a stale PLANNING projection after resume failure",
    "restores the pre-resume PAUSED state and dashboard as one authority lease"
   ],
   "cli/resume-pause-authority.test.ts": [
    "removes only the matching authority and restores it after failed resume",
    "refuses to remove another run authority",
    "does not overwrite a newer pause emitted by the resumed run",
    "leases provider holds and restores them when resume fails"
   ],
   "cli/resume-race.test.ts": [
    "deletes stale .hb and .partial-result for respawn-eligible tasks before spawning",
    "leaves artifacts of tasks that completed during the crash (.result on disk) untouched",
    "dry-run performs zero writes and never spawns",
    "resets artifacts BEFORE runSprint is invoked (ordering)",
    "delete strategy is consistent with the collector stale threshold",
    "pending task without artifacts does not crash resume and still spawns",
    "resets the interrupted worker to PENDING but never the one that completed during the crash",
    "dry-run resets nothing — task.json and checkpoint are byte-identical, no spawn",
    "resumes a PAUSED task only when its durable pause marker exists"
   ],
   "cli/retro-formatter.test.ts": [
    "returns English label by default",
    "returns Turkish label for lang=tr",
    "returns key for unknown label",
    "falls back to English for unknown language",
    "shows English labels by default",
    "shows Turkish labels when lang=tr",
    "shows 0% success for zero tasks",
    "includes sprint id",
    "computes positive delta",
    "computes negative delta",
    "shows Turkish labels when lang=tr",
    "returns empty string for empty rows",
    "formats agent performance table",
    "shows Turkish title when lang=tr",
    "returns empty string for empty rows",
    "includes skill name in output",
    "shows no trend message for empty entries",
    "formats trend entries",
    "shows Turkish title when lang=tr"
   ],
   "cli/retro-parser.test.ts": [
    "parses sprint-reporter \"Tasks completed | X/Y\" format",
    "parses NO_GO rate format with parenthesized count",
    "parses legacy \"| Total Tasks |\" format",
    "returns defaults for empty content",
    "counts GO_WITH_TECH_DEBT occurrences as techDebt fallback",
    "parses Sprint time format",
    "falls back to non-table format",
    "stores raw content",
    "parses agent performance table rows",
    "returns empty array when no section found",
    "parses multiple rows",
    "parses skill performance table rows",
    "returns empty array when no section found",
    "returns empty array when sprints dir does not exist",
    "loads trend entries from sprint files",
    "respects the n limit"
   ],
   "cli/review-active-settlement-reference.test.ts": [
    "returns an exit-0 typed view for ACTIVE, PAUSED, and terminal tasks without writes",
    "turns an authority parse failure into corrupt typed output rather than a stack trace",
    "rejects cross-task references and catalogs every new user-facing label in EN/TR"
   ],
   "cli/rich-output.test.ts": [
    "renders task table header with column names",
    "renders task rows with correct data",
    "falls back to evaluations map when taskRows not provided",
    "shows GO (DONE), GO_WITH_TECH_DEBT, and NO_GO on separate lines",
    "displays correct counts in evaluation summary",
    "shows config migration section when configMigrated is true",
    "does NOT show config migration section when configMigrated is false",
    "does NOT show config migration section when not provided",
    "renders brain insights when provided",
    "does NOT render brain insights when not provided",
    "does NOT render brain insights when empty string",
    "README contains at least 33 deckent commands in the command table",
    "README contains explain command",
    "README contains quick-start command",
    "README contains skill command",
    "README contains skill-marketplace command",
    "README contains agent command",
    "README contains review command",
    "README contains config migrate command"
   ],
   "cli/risk-language.test.ts": [
    "ladder is exactly the 4 CommandRisk classes in spec order",
    "renders %s with a real (non-fallback) en label + description",
    "renders %s with a real (non-fallback) tr label + description",
    "en and tr labels differ for every class",
    "every class has a distinct en label and a distinct tr description",
    "renderAllCommandRisks returns all 4 in ladder order for both languages",
    "maps read/confirm/always onto the 3 linear ladder rungs",
    "ADR § Decision item 4: kill is a correctly-matched entry — always-tier -> Çalıştır == registry tag",
    "ADR § Decision item 4: cleanup/recover are documented mismatches — always-tier target (Çalıştır) != registry tag (Değiştir)",
    "every entry in COMMAND_REGISTRY uses a CommandRisk value this module knows how to render",
    "ApprovalRisk.medium -> Değiştir (ADR-stated design-doc lean)",
    "nervous RiskLevel.medium -> Değiştir (same ADR lean, named alongside ApprovalRisk.medium)",
    "ApprovalRisk ladder: none->Oku, low/medium->Değiştir, high/critical->Çalıştır (critical clamps)",
    "nervous RiskLevel ladder: low->Oku, high->Çalıştır",
    "base ToolRiskLevel: safe->Oku, moderate->Değiştir, destructive->Çalıştır",
    "ToolCatalogRiskLevel extends the base ladder and clamps critical to Çalıştır",
    "maps low/medium/high/critical onto the ladder, clamping high+critical to Çalıştır",
    "Danger clamps to Çalıştır (risk-driven per classifyToolTrust)",
    "source-derived tiers (Core/Project/MCP/Enterprise) use the honest Değiştir default"
   ],
   "cli/root-help-generated.test.ts": [
    "renders the compact English-default surface instead of the flat command list",
    "keeps the deprecated block OUT of root help and lists every visible command with its summary",
    "prints the advanced and deprecated registry exactly once via a real command",
    "uses the registered Turkish catalog through the existing resolver"
   ],
   "cli/run-attempt-custody.test.ts": [
    "delegates without publishing a task and never turns worker self-assessment into DONE",
    "reports zero-work receipt identity without claiming that the task started",
    "distinguishes reconciliation-required from zero work and preserves both receipt refs",
    "retains a durable receipt when admission throws before dispatch"
   ],
   "cli/run-budget-contract.test.ts": [
    "persists the owner-policy snapshot and passes the exact ceiling to spawn",
    "forwards the runtime-wide attended authority to the shared final dispatch seam",
    "holds a remote run before Task JSON and spawn when policy is missing",
    "consumes an injected process authority HOLD before routing, Task JSON, prompt or spawn",
    "keeps a local Ollama run exempt without inventing a numeric ceiling"
   ],
   "cli/run-completion-watch-corr.test.ts": [
    "parses completionRecord.flowId when present (427-001 additive shape)",
    "a legacy record with no completionRecord parses flowId as undefined",
    "a non-string completionRecord.flowId parses as undefined, never throws",
    "parses completionRecord.taskSummary into per-task evidence",
    "a legacy job with no taskSummary parses without a tasks key (byte-identical)",
    "tolerates a partial/garbage taskSummary entry — drops the bad one, keeps the good",
    "pre-existing sprint-finalizer.ts / job-runner.ts shapes still parse identically (no flowId key added when absent)",
    "flowId option: only the matching flow’s completion fires",
    "a legacy job (no completionRecord at all) never fires when a flowId filter is set",
    "no flowId option supplied → fires for every terminal job regardless of flowId (filtresiz yol bit-eş)",
    "a job already-COMPLETE-with-matching-flowId at construction time still does not fire (baseline suppression unaffected by the filter)",
    "dedup is preserved under a flowId filter — fires exactly once across multiple scans",
    "the poll timer alone (fs.watch never firing) still delivers a matching-flowId completion — poll fallback untouched by the filter",
    "dispose() still clears the poll interval and closes fs.watch when a flowId filter is active"
   ],
   "cli/run-flow-controller-complete.test.ts": [
    "DETACHED_RUNNING + status COMPLETE -> COMPLETED via the reducer",
    "DETACHED_RUNNING + status FAILED -> FAILED via the reducer, failureReason from event.error",
    "FAILED with no event.error -> a synthesized, non-empty fallback failureReason",
    "missing/empty COMPLETE label throws before coordinator mutation (%s)",
    "missing/empty FAILED fallback label throws before coordinator mutation (%s)",
    "wrong-flow event: context unchanged, ignored with a loud console.error",
    "event with flowId unset while controller has no active flow: ignored with a loud console.error",
    "idempotent: applying the same COMPLETE event twice — the second call is a silent no-op",
    "idempotent: a stray FAILED event replayed against an already-COMPLETED flow is also a silent no-op (state-based guard, not event-identity)",
    "a genuinely invalid transition (context still AWAITING_APPROVAL) propagates the reducer\\'s typed RunFlowTransitionError, not swallowed",
    "existing propose/approve/reject/startApproved surface is unchanged by this addition"
   ],
   "cli/run-flow-controller-provider-hold.test.ts": [
    "awaits ensureProviders BEFORE planRunFlow (order pinned)",
    "ProviderNotFoundError in the cause chain → RunFlowProviderHoldError(code NO_PROVIDERS) with model, provider, registered, flowId",
    "a non-provider planner failure passes through UNCHANGED (no new error shape for unrelated faults)",
    "no ensureProviders seam (unit tests, legacy callers) → planning still runs; a provider hold reports registered=[]"
   ],
   "cli/run-flow-controller.test.ts": [
    "resolveRunFlowEnabled — only literal true enables (fail-closed)",
    "flag-off (omitted): no deckent_propose_run, set/plan/start descriptions carry no escape-hatch note",
    "flag-off (explicit enabled:false): identical tool list/descriptions/tiers to omitted — zero behavior change pin",
    "flag-on: registers deckent_propose_run (silent tier, builtin source) and appends the escape-hatch note to set/plan/start — tool-count pin adds exactly one",
    "deckent_propose_run handler requires intentSummary and delegates to the controller",
    "deckent_propose_run handler surfaces a controller rejection as ok:false (never throws)",
    "proposeRun drives COLLECTING -> AWAITING_APPROVAL with a REAL plan preview (real generatePlanPreview+compileRunProposal — only planSprint/readContext mocked)",
    "approve() -> APPROVED with an approvedSnapshot CAS-matched to the live preview; never reaches STARTING/handle",
    "approve() cannot override an undeclared shared-writer topology block",
    "startApproved persists the proposal into the durable snapshot — REAL controller write path (G1)",
    "reject() -> CANCELLED",
    "a second proposeRun on the same controller throws — no second plan-path",
    "approve() before any proposal throws a descriptive error, not a raw TypeError",
    "reject() before any proposal throws a descriptive error",
    "maps %s -> %s",
    "unmapped key %j is a no-op (null)",
    "formats a 1-indexed task summary line",
    "truncates a long digest, leaves a short one untouched",
    "every label is a non-empty, genuinely-translated string (en !== tr)",
    "falls back to en for an unknown lang (getMessage contract)"
   ],
   "cli/run-flow-inbox.test.ts": [
    "empty project → no rows",
    "lists a proposed flow with its intentSummary + store state",
    "jobs-dir COMPLETE overrides the store state (a finished do no longer shows as running)",
    "jobs-dir FAILED overrides to FAILED",
    "sorts newest-first by updatedAt and caps at the limit",
    "respects an explicit limit override",
    "lists a snapshot-only do-flow (cache-miss, disk-fold) as DETACHED_RUNNING with a bare short-id",
    "a snapshot-only do-flow that finished shows COMPLETED via the jobs-dir override (disk-fold path)",
    "renders a bare do-flow row with no intent (short-id + state only)",
    "a do-flow with a persisted proposal shows its intentSummary (not a bare UUID)",
    "a dead recorded pid shows FAILED + liveness dead — WITHOUT writing any store (pure reader)",
    "an alive recorded pid stays running with NO liveness mark (verified alive)",
    "a pid-less legacy handle (pre-698) marks liveness unknown, state unchanged",
    "a terminal jobs-dir record is never probed (jobs truth wins, no liveness field)",
    "renders the liveness marks in the row body and the detail lines",
    "empty rows → the single empty notice",
    "renders header + numbered rows + hint, with short-id, state, metrics, intent",
    "omits the metrics parenthetical when done/total are absent",
    "bare / whitespace arg → list",
    "a valid 1-based index → that row detail",
    "out-of-range / zero → not-found (honest, never a silent list)",
    "a non-numeric arg → list (a stray arg is not an error)",
    "finds a unique prefix beyond the default listing window and retains ambiguity refusal",
    "retires an approved, unstarted flow through the existing abort command",
    "keeps rejection for an awaiting-approval flow and refuses a terminal retirement",
    "renders header + full id + intent + progress + honestly-labeled updated line",
    "omits absent fields — a bare do-flow shows id + state only",
    "renders \"YYYY-MM-DD HH:mm (relative)\" for each age tier",
    "a future stamp gets the absolute part only (no relative claim)",
    "an unparsable stamp is returned verbatim (honest, never throws)",
    "renders m:ss under an hour and h:mm:ss above",
    "is undefined for unparsable or negative intervals",
    "a finished do-flow shows origin, REAL start (never updatedAt), closed + duration + progress",
    "a sweep-closed legacy flow shows real start, a \"closed\" label and the operator narrative — the exact view Alperen flagged",
    "duration renders only for COMPLETED (honest runtime)",
    "a jobs record with completedAt + summary drives closed, REAL duration and the summary line",
    "a legacy jobs record WITHOUT completedAt never shows a bogus 0:00 duration (updatedAt recycles startedAt)",
    "bare /runs → the list; /runs <n> → that flow detail; /runs <bad> → not-found",
    "empty project → the empty notice for a bare /runs",
    "resolves every label in en + tr (en !== tr), covering all states",
    "renders the SAME body buildInboxLines indents by two spaces (one source of truth)",
    "maps ↑↓ / Enter / Esc to their actions",
    "any unmapped key is a no-op (never an implicit move/decision)",
    "keeps a still-present selection unchanged (even after a reorder)",
    "falls back to the first row when the selection is null or vanished",
    "is null for an empty list",
    "↓ moves to the next row, ↑ to the previous — both wrap",
    "↵ opens the focused row detail (realigning a null selection to the first row)",
    "Esc collapses an open detail; from the list it is a no-op (caller closes the card)",
    "↑↓ still move the cursor while a detail is open (browse detail-by-detail)",
    "an empty list nulls the selection and cannot open a detail",
    "AWAITING_APPROVAL offers the three Telegraph verbs; APPROVED offers only start",
    "every other state offers NOTHING (running/terminal flows are not decidable)",
    "maps exactly a/f/r/s — every other key is null (disjoint from the nav map)"
   ],
   "cli/run-flow-mount.test.ts": [
    "AWAITING_APPROVAL with no preview on context (should not happen, but honest) -> null",
    "idle (no confirm, no approval pending): card may be active",
    "confirm modal open: card defers, regardless of approval state",
    "an ApprovalCard approval is pending (confirm modal closed): card defers",
    "idle (no confirm, no approval, no plan-preview): the inbox card may own stdin",
    "defers to the confirm modal",
    "defers to a pending ApprovalCard",
    "defers to a pending PlanPreviewCard (runFlowPending)",
    "defers when several higher-priority consumers are active at once",
    "started — substitutes {jobId}",
    "rejected — static line",
    "error — substitutes {error}",
    "honors a caller-supplied label override (i18n wiring, e.g. Turkish)",
    "%s mounted /do reports all refusal codes without raw evidence",
    "keeps unknown mounted errors on the established path",
    "%s production renderer binds catalog text for the App",
    "flag-off: returns undefined without invoking the controller factory",
    "flag-on: invokes the factory exactly once and returns its result",
    "every label is a non-empty, genuinely-translated string (en !== tr)",
    "startApproved() before approve() throws a descriptive error",
    "drives APPROVED -> STARTING -> DETACHED_RUNNING via a fake spawnStart — no real sprint spawn",
    "persists the approved snapshot durably via Task-1 run-flow-store.ts (not just in-memory)",
    "is idempotent: calling startApproved() twice does not spawn a second time",
    "approve() itself is UNCHANGED — still stops at APPROVED with no handle (dilim-3 pin)",
    "flag-on without a seam → factory receives deps with a callable ensureProviders (the ONE lazy bootstrap)",
    "flag-on with an injected seam → preserved by reference (tests/hosts stay in control)",
    "flag-off → factory never invoked, no seam constructed"
   ],
   "cli/run-flow-result-turn.test.ts": [
    "formats a COMPLETE info as a rich, flowId-bearing verdict summary",
    "formats a FAILED info with an error message",
    "a FAILED info with no error message falls back to the jobId",
    "missing metrics counts default to 0 rather than \"undefined\" in the summary",
    "honors a caller-supplied label override (i18n wiring, e.g. Turkish)",
    "appends per-task evidence lines below the aggregate header",
    "a bare NO_GO task with no files/tests stays a clean one-liner (no \"0 files\")",
    "caps the evidence block at MAX_EVIDENCE_TASKS and adds a \"… N more\" footer",
    "a COMPLETE info with no tasks (legacy job) is header-only — byte-identical to pre-evidence",
    "a FAILED info stays single-line even if it somehow carried tasks",
    "maps each verdict to its icon",
    "omits coverage when zero but still shows the test mark",
    "shows a failing test mark",
    "every label is a non-empty, genuinely-translated string (en !== tr)",
    "enabled=false: returns undefined without invoking the watch factory",
    "matching flowId: drives applyRunCompletion AND feeds onResult a rich localized event",
    "non-matching flowId: silently skipped — no applyRunCompletion call, no onResult call",
    "a legacy job with no completionRecord.flowId at all: silently skipped",
    "controller has no active flow (flowId undefined): every completion is silently skipped",
    "a FAILED matching-flow completion also drives applyRunCompletion + onResult",
    "uses the real createRunCompletionWatch as its default watchFactory when none is injected",
    "idle: produces the turn text immediately (idle REPL \"wakes\")",
    "mid-turn: buffers only, returns [] — Hermes no-inject rule preserved",
    "the mid-turn buffered event surfaces later via the queue\\'s own drainAsTurns() at turn-end"
   ],
   "cli/run-flow-scope-mirror.test.ts": [
    "ambiguous write-suspect → preview.scopeGateResult=fail with the gate message (the 449 death, now visible pre-approval)",
    "forceScope acknowledges the same suspect → pass + scopeGateOverridden",
    "clean write scope → pass, no message, no override",
    "git unavailable → fail-closed for exact authority",
    "do --run --yes on a scope-gate fail: honest reject BEFORE any start, exitCode=1 (no more silently-dead run)",
    "do --run --yes --force-scope on the same plan: starts (consent flows through)",
    "dry-run preview surfaces the scope-gate fail so the operator sees it before ever passing --run",
    "source pin: default spawnStart forwards --force-scope to the detached child"
   ],
   "cli/run-language-surface.test.ts": [
    "plan.sprint_planned bridges while keeping id/count interpolation intact",
    "plan.note_sprint_size bridges",
    "start.sprint_planned bridges while leaving unfilled placeholders intact",
    "finalize.complete bridges",
    "every bridged key has a distinct, non-empty en+tr pair",
    "renders \"Run N (sprint) — completed\" and preserves the W0-TRUTH retro-hint contract",
    "${key} contains \"Run\" + \"(sprint)\" bridge in both en and tr",
    "hint.COMPLETE stays \"Sprint complete\" verbatim — tests/cli/messages.test.ts hard-asserts this (out of write scope)",
    "status.sprint_active is now bridged to \"Run {sprintId} (sprint) active\" (450-004 RUN-RENAME dilim-3) — tests/cli/helpers/messages.test.ts was updated in the same atomic change",
    "status.no_active_sprint is now bridged to \"No active run (sprint)\" (450-004 RUN-RENAME dilim-3) — tests/cli/commands/i18n-integration.test.ts was updated in the same atomic change",
    "formatHumanStatus non-COMPLETE header stays \"Sprint <N>\" verbatim — tests/cli/helpers/human-status.test.ts hard-asserts this (out of write scope)"
   ],
   "cli/run-pre-dispatch-settlement.test.ts": [
    "declares then settles a budget HOLD without Task JSON or spawn",
    "closes the authority when declaration itself fails",
    "settles a provider-authority HOLD before routing, Task JSON, or spawn",
    "settles prompt construction failure as command_build_failed",
    "aborts before the external side effect when dispatch marking fails",
    "rejects a declared/actual backend mismatch before dispatch or provider work",
    "never asserts NOT_DISPATCHED after a persisted dispatch boundary",
    "localizes a backend that returns without publishing its dispatch boundary",
    "localizes and quarantines a duplicate dispatch boundary",
    "projects kept raw PENDING to effective %s through terminal receipt evidence",
    "keeps terminal receipt evidence content-addressed when raw files are cleaned",
    "keeps a timed-out dispatched receipt open for reconciliation",
    "quarantines a mismatched raw result identity without terminal settlement or cleanup"
   ],
   "cli/run-rename-alias.test.ts": [
    "run ${target} [flags] invokes the exact same handler+args as ${target} [flags] directly",
    "run ${target} with no flags still delegates cleanly",
    "run command exposes exactly the 4 reserved alias subcommands",
    "sets exit code 1 for invalid model (regression, unchanged from pre-alias behavior)",
    "a description that merely CONTAINS a reserved word as a substring is NOT captured by the alias",
    "spawns worker and reports DONE result for an ordinary description (unchanged end-to-end)",
    "returns a non-empty, distinct string for en vs tr"
   ],
   "cli/run-rename-smoke.test.ts": [
    "no run history: prints \"No run history found.\" and exits 0",
    "legacy \"sprint-NNN.md\" file is still recognized and parsed into a row (sprint-alias-compat)",
    "SKIP: dist/cli/commands/history.js predates src/cli/commands/history.ts:304 ",
    "a COMPLETE run renders \"Run N (sprint) — completed\" — sprint appears only as a parenthetical alias",
    "--json on the same COMPLETE run reports the canonical no-active contract",
    "SKIP: dist/cli/helpers/messages.js predates src/cli/helpers/messages.ts ",
    "a live ACTIVE run header is real, current, bare \"Sprint 999\" — documented pre-existing ",
    "SKIP: ${ENTRY} not found — run \\`npm run build\\` first"
   ],
   "cli/run-result-settlement.test.ts": [
    "ignores an early raw DONE until the exact host receipt publishes embedded NO_GO",
    "does not accept a stale receipt from another attempt of the same task",
    "returns the embedded snapshot even if the worker-writable raw result changes later",
    "preserves legacy raw-result behavior when no settlement reference exists",
    "rejects a raw result whose embedded task identity does not match the requested task",
    "rejects corrupt closure evidence instead of timing out or reading raw output"
   ],
   "cli/run-state-feed-corr.test.ts": [
    "a matching completionRecord.flowId produces a typed, populated completion event",
    "no flowId supplied -> no completion event, even with matching-shaped jobRecords present (legacy-yol)",
    "flowId supplied but jobRecords omitted -> no completion event",
    "flowId supplied but no jobRecords entry matches -> no completion event",
    "a legacy job record with no completionRecord at all never matches (legacy-yol, bit-eş)",
    "malformed verdictSummary is omitted from the event, but the event itself is still returned",
    "jobId falls back to sprintId, then to flowId, when job.jobId is missing/non-string",
    "a non-string completionRecord.flowId never matches a string flowId",
    "flowId option triggers a JOBS_DIR scan and surfaces the matching correlated completion",
    "flowId omitted -> JOBS_DIR is never touched at all (zero extra fs work, legacy-yol)",
    "flowId supplied but no matching job on disk -> no completion, no throw",
    "a missing JOBS_DIR (flowId set) degrades to no completion, never throws",
    "a malformed job file is skipped, a matching sibling is still found",
    "a readdirSync throw on JOBS_DIR degrades to no completion, other feed fields unaffected",
    "non-.json files under JOBS_DIR are ignored"
   ],
   "cli/run-state-feed.test.ts": [
    "returns an entirely empty state when nothing is known (idle collapse upstream)",
    "uses persisted IDLE authority to suppress stale sprint-state and heartbeat projections",
    "uses fresh ABORTED authority to veto a stale ACTIVE read model and running projection",
    "uses fresh ORPHANED/dead authority to veto a stale FIXING projection",
    "one active worker -> \"<taskId> · <phase>\"",
    "multiple active workers -> \"<N> tasks · <phase>\"",
    "a heartbeat with a matching .result is finished, not active",
    "sprint active with zero active workers -> \"<sprintId> · <phase>\"",
    "next = first taskIds[] entry with neither .hb nor .result",
    "next is omitted when every taskId is started or finished",
    "provider/auth pass through only when the cache is well-formed",
    "malformed provider cache entries are omitted, not fabricated",
    "provider.healthy normalizes any non-boolean to \"unknown\"",
    "heartbeats with a non-string taskId are ignored",
    "missing sprint-state.json + missing .tasks dir -> entirely empty (idle upstream)",
    "reads a real hb+state fixture into the correct footer-state",
    "marks a stale persisted model and projects fresh terminal authority",
    "a corrupt sprint-state.json degrades to absent, not a thrown error",
    "a malformed individual .hb file is skipped, siblings still read",
    "a readdirSync throw on an existing-looking .tasks path degrades to no heartbeats",
    "provider-health cache populates provider/auth only when present + well-formed",
    "never reads or requires the provider-health cache to be present (probe-tetiklemez)",
    "returns a () => LiveFooterState that re-reads the seam on every call"
   ],
   "cli/run.test.ts": [
    "returns a string starting with run-",
    "returns unique-format ids on each call",
    "creates a task with correct structure",
    "truncates title to 80 characters",
    "uses the provided model",
    "sets createdAt as ISO string",
    "has correct goNogo defaults",
    "sets empty filesRead and filesWrite",
    "deletes all task file extensions",
    "skips deletion if file does not exist",
    "does not throw if unlinkSync fails",
    "returns result immediately if file exists",
    "returns null if timeout expires without result",
    "returns null if result file has invalid JSON",
    "registers a \"run\" command on the program",
    "run command has --model option",
    "run command has --scope option",
    "sets exit code 1 for invalid model",
    "spawns worker and reports DONE result",
    "sets exit code 1 for NO_GO result",
    "accepts a known exact ID (gpt-5.6-sol) and spawns with it unchanged",
    "accepts a pricing-verified versioned ID with --provider codex; Task JSON + spawn preserve it",
    "omitted --model resolves from the canonical config default (never a literal alias)"
   ],
   "cli/runs-inbox-hygiene.test.ts": [
    "classifies exactly the superseded set and writes nothing in a dry-run",
    "retires exactly the superseded set with a persisted supersededBy; the newest survives",
    "never touches a started or terminal flow, nor an ungroupable one",
    "still parses a legacy abort record — no supersededBy folds to \"aborted\"",
    "renders the CLI report through getMessage in en and tr"
   ],
   "cli/runtime-floor.test.ts": [
    "the manifest declares an engines.node floor",
    "checkNode() derives its message from the manifest engines.node range, not a source literal",
    "checkNode() passes on the real, currently-running Node binary (which meets the manifest floor)",
    "runDoctorChecks() surfaces the same manifest-derived floor for the Node.js check"
   ],
   "cli/runtime-hygiene-cleanup.test.ts": [
    "registers explicit preview, apply, CAS and JSON options",
    "defaults history cleanup to one path-free JSON plan and performs no mutation",
    "requires an exact digest before calling the canonical apply service",
    "never lets runtime-only switches fall through to destructive legacy cleanup",
    "applies only the freshly reproduced exact plan and emits one receipt projection",
    "replays an existing immutable receipt without rebuilding the mutated live tree",
    "refuses apply while terminal authority reports an active run"
   ],
   "cli/runtime-hygiene-messages.test.ts": [
    "%s resolves directly in EN and TR",
    "%s has exact placeholder parity",
    "%s uses only bounded, non-sensitive placeholders",
    "keeps HOLD typed and explicitly side-effect free in both locales",
    "renders every state without unresolved placeholders when safe aggregates are supplied"
   ],
   "cli/runtime-hygiene.integration.test.ts": [
    "inventories read-only, retires losslessly, then replays the durable receipt in a fresh process"
   ],
   "cli/serve-first-run-banner.test.ts": [
    "prints the ready line with host and port",
    "prints the token auto-injected line",
    "prints terminal-enabled message when terminal is active",
    "prints terminal-disabled message when --no-terminal is passed",
    "prints the stop (Ctrl+C) hint",
    "prints the port/host tips line",
    "uses getMessage — all banner strings routed through i18n"
   ],
   "cli/serve-provider-authority.test.ts": [
    "opens before provider bootstrap, injects the exact authority, and closes it at shutdown",
    "closes the opened authority when HTTP server construction fails"
   ],
   "cli/serve-terminal.test.ts": [
    "exposes --host and --no-terminal",
    "warns via stderr when host is non-localhost and terminal is not explicitly disabled",
    "does not warn when --no-terminal is explicitly set with non-localhost host"
   ],
   "cli/serve.test.ts": [
    "registers serve command",
    "starts server with default port",
    "starts server with custom port",
    "registers a shutdown hook instead of dead process.on signal listeners",
    "writes the daemon handshake meta on startup (adopt-vs-spawn hint)",
    "shutdown hook clears the handshake meta FIRST, then closes the api",
    "shutdown hook still clears the meta even when api.close rejects"
   ],
   "cli/session-registry.test.ts": [
    "starts empty when no file exists yet",
    "registers a session and lists it back (alive)",
    "accepts a null tty",
    "writes to the default path under SETTINGS_DIR",
    "re-registering the same pid replaces its entry, not duplicates it",
    "multiple distinct pids all survive independent register calls (multi-writer atomicity)",
    "filters out dead pids by default",
    "includeDead:true bypasses the liveness filter",
    "removes dead entries, persists the pruned set, and returns the removed entries",
    "is a no-op (skips the write) when nothing is stale",
    "round-trips register -> cleanupStale -> listActive with a fake-pid-probe",
    "is fail-soft to an empty set when the file contains invalid JSON",
    "is fail-soft to an empty set when the top-level JSON is not an array",
    "drops malformed entries (missing pid/startedAt) but keeps valid ones",
    "coerces a non-string tty to null"
   ],
   "cli/session-resume.test.ts": [
    "parses a full modern job record",
    "falls back to jobId for id and title, and startedAt for date, when minimal (RUNNING)",
    "prefers sprintId over jobId for id when both present but summary is absent",
    "prefers completedAt, then endedAt, then startedAt for date",
    "returns null on malformed JSON",
    "returns null when JSON is not an object",
    "returns null when neither sprintId nor jobId is present",
    "returns null when status is missing",
    "returns null when no valid date field is present",
    "returns null when the only date field is an unparseable string",
    "returns [] when the jobs directory does not exist (fresh checkout / pre-init)",
    "returns [] when n <= 0",
    "lists sessions newest-first",
    "caps the result at n",
    "skips corrupted records and non-json files without throwing",
    "ignores an unreadable directory gracefully",
    "picks by 1-based number matching list position",
    "reports not-found for an out-of-range number",
    "picks by exact id",
    "picks by unique case-insensitive title prefix",
    "reports ambiguous when a title prefix matches multiple sessions",
    "reports ambiguous when the session list itself has a duplicate id",
    "reports not-found for empty input and no match",
    "returns not-found against an empty session list"
   ],
   "cli/session-warn.test.ts": [
    "1 active session stays ok, below the parallel-usage warn threshold",
    "4 active sessions crosses the threshold — warn, all sharing one usage limit",
    "stale (dead-pid) sessions are excluded from the count via the registry liveness filter",
    "a throwing session-count reader degrades to unknown — never crashes the whole snapshot",
    "no registry file on disk yet resolves to 0 active sessions, not an error",
    "defaults listActiveSessionsFn to the real listActive() when not injected",
    "omits the warning line entirely when sessions is absent (backward compatible)",
    "omits the warning line when sessions.status is ok",
    "appends a second warning line when sessions.status is warn",
    "localizes the warning line — en and tr differ",
    "interpolates the real active-session count into the warning line",
    "defaults getMessageFn to the real getMessage() when no deps are injected — fails soft (raw key, no crash) until messages.ts adds the companion key"
   ],
   "cli/shutdown-hooks.test.ts": [
    "command-local ownership is acquired before action invocation and unwinds on success and rejection",
    "nested command-local scopes retain ownership until the outer scope releases",
    "hasShutdownHooks reflects registration and unregistration",
    "unregister is idempotent and removes only its own hook",
    "runs every hook to settlement — a rejecting hook does not break siblings and never rejects the run",
    "is collectively bounded — a never-resolving hook cannot hang the run"
   ],
   "cli/sigterm-cleanup.test.ts": [
    "SIGTERM invokes interruptActiveSprint() and killAllSessions() (same as SIGINT)",
    "SIGINT still invokes the same cleanup calls (parity, no regression)",
    "cleanup call order is unchanged: interruptActiveSprint() before killAllSessions()",
    "a thrown interruptActiveSprint() does not prevent killAllSessions() or exit (non-fatal try/catch preserved)",
    "%s under command-local ownership awaits owned hooks and skips sprint/tmux teardown",
    "maps a declared Windows SIGBREAK number and reports unsupported host signals honestly"
   ],
   "cli/sigterm-teardown.test.ts": [
    "SIGTERM still runs interrupt/killSessions/exit synchronously in the same tick",
    "warm-child persistent session (real createPersistentClaudeSession, async spawn) is killed and AWAITED before sprint/tmux cleanup",
    "MCP client broker (real McpClientBroker + SDK InMemoryTransport) is fully disconnected — no orphaned connection survives SIGTERM",
    "unregister() removes the hook — a later signal does not re-run stale REPL cleanup",
    "a rejecting REPL teardown hook does not prevent sprint/tmux cleanup or exit (best-effort, non-fatal)",
    "a hanging REPL teardown hook does not block shutdown forever (bounded overall timeout)",
    "POSIX (linux/darwin) registers SIGINT + SIGTERM",
    "win32 registers SIGINT + SIGBREAK, NOT SIGTERM (Node has no real Windows SIGTERM event)",
    "unmounts Ink, disconnects the MCP broker, then exits the warm-child switcher, in order",
    "restores the alt-screen only when altScreen=true",
    "is idempotent — calling teardown() twice only runs the cleanup once",
    "one throwing dependency (memory.close) does not skip the remaining steps (best-effort)",
    "a rejecting mcpBroker.disconnectAll() still allows switcherExit() to run (independent best-effort steps)"
   ],
   "cli/skill-create-gate.test.ts": [
    "persists the generated canonical profile before writing the skill",
    "fails before every filesystem write with typed, localized guidance (%s)"
   ],
   "cli/skill-list-v2-manifest.test.ts": [
    "does not throw for a v2 manifest with no triggers array",
    "always returns a triggers array, derived from activation.rules when missing",
    "renders skill.triggers.slice(0, 3) safely — the exact call site that crashed",
    "safe-defaults category, enabled, priority, name, description for a bare v2 manifest",
    "falls back triggers to [] when there is no activation config at all",
    "falls back name to id when a v2 manifest omits it",
    "leaves a well-formed v1 manifest unchanged (no regression)",
    "loads a mix of v1 and v2-bare manifests without throwing",
    "exits clean (process.exitCode stays unset) for a v2 manifest missing triggers",
    "renders the derived triggers and safe defaults in the printed table"
   ],
   "cli/skill-publish.test.ts": [
    "rejects sandbox-unsafe skill",
    "generates signature for valid skill",
    "produced signature is verifiable with public key",
    "errors when manifest.json is missing",
    "overwrites existing signature when re-publishing"
   ],
   "cli/skill-surface-readmodel.test.ts": [
    "snapshot carries ordered entries, invalid records and a sha256 digest",
    "digest is deterministic on an unchanged tree and moves on catalog mutation",
    "disposition, masked and profileState travel through the snapshot entries",
    "an invalid manifest is REPORTED, never silently dropped",
    "S8: excludeSidecarStats keeps the catalog-only digest stable across a sidecar mutation, while the full-state digest moves and diverges from it",
    "both read surfaces resolve through the snapshot (no raw scan symbols survive)"
   ],
   "cli/slash-case-translit.test.ts": [
    "/Help resolves identically to /help (task goCriteria example)",
    "meta-commands (/Exit, /QUIT, /Clear) resolve regardless of case",
    "generic catalog entries (/STATUS, /Recall) dispatch to their agenticTool regardless of case",
    "structured subaction commands (/Autonomous Status, /MCP) fold case on both name and subaction",
    "/Queue and /QUEUE resolve like /queue",
    "/Interrupt resolves like /interrupt",
    "/Steer <msg> resolves like /steer <msg>, preserving the message casing",
    "still rejects unrelated input regardless of case",
    "/St and /ST match /status just like /st",
    "returns the line as-typed (completer contract), not case-folded",
    "no case-insensitive match still falls back to the full list",
    "transliterates ç/ş/ı/ğ/ö/ü to their ASCII equivalents",
    "transliterates the dotted capital İ correctly",
    "ASCII-only titles are unaffected (no regression, matches prior expectations)"
   ],
   "cli/slash-completer.test.ts": [
    "non-slash line → no completions (normal chat)",
    "bare `/` → lists all commands (menu), /quit alias hidden",
    "prefix → filters matching commands",
    "no prefix match → falls back to full list (so the menu still shows)"
   ],
   "cli/spawn-error-listener.test.ts": [
    "does not crash and rejects stdoutLines.next() instead of hanging forever",
    "resolves `wait` even though close is not guaranteed to fire after error",
    "isAlive() reports true after exit() followed by a fresh respawn (kill+restart)",
    "resolves with an [enterprise-error] tag instead of hanging when spawn errors",
    "still resolves normally via close when no error occurs",
    "runChatNativeLoop is called with gracefulErrors: true (mirrors the --once branch)"
   ],
   "cli/spawn-final-only-parity.test.ts": [
    "returns the same exact task-stamped grant for both ingress families",
    "resolves auto before containment and rejects its non-Docker platform result",
    "makes replay settlement first-writer-only and rejects a stale foreign receipt"
   ],
   "cli/spawn-final-only-real-binary.test.ts": [
    "fails a canonical task with a missing owner grant before Docker/provider work"
   ],
   "cli/spawn-lifecycle.test.ts": [
    "passes a valid claude level to the config backend spawn as reasoningEffort",
    "does NOT emit reasoningEffort for an invalid level (resolveReasoningEffort gate)",
    "keeps opt-in semantics: no modelEffort → reasoningEffort undefined",
    "holds the unmetered tmux fallback before provider work",
    "forwards task.modelEffort from the task JSON to the backend spawn",
    "emits no reasoningEffort when the task JSON has an invalid modelEffort",
    "retains the invocation receipt when common ingress throws before dispatch",
    "renders exact zero-work and ambiguous dispatch as distinct evidence-bearing outcomes",
    "rejects forced exact respawn until a new generation identity is available",
    "fails closed before provider work when a final-only task has no grant",
    "fails closed before provider work when a final-only grant resolves to non-Docker",
    "forwards --model-effort through buildExecutionRequest to the backend spawn",
    "emits no reasoningEffort for an invalid --model-effort level",
    "DONE result → task JSON status DONE",
    "GO_WITH_TECH_DEBT result → task JSON status DONE (ADR-045 §1 mapping)",
    "NO_GO result → task JSON status NO_GO",
    "missing result file → null, task JSON untouched",
    "unknown selfAssessment → null, task JSON untouched",
    "finalizes task JSON to DONE when the worker result is DONE",
    "finalizes task JSON to NO_GO when the worker result is NO_GO",
    "ignores a STALE pre-spawn result on --force respawn (mtime guard)"
   ],
   "cli/spawn-multiprovider.test.ts": [
    "routes ollama to host adapter even when spawnBackend=docker is set",
    "awaits refreshSupportedModels before calling adapter.spawn",
    "routes ollama to host adapter even without spawnBackend option",
    "rejects a live budget before adapter refresh or provider work when evidence is unsupported",
    "rejects an unregistered OpenRouter model before provider work",
    "forwards allowedTools to adapter.spawn",
    "forwards autoApprove=true to adapter.spawn",
    "calls ensureOllamaModelRegistered when opts.provider=ollama (dynamic tag pre-registration)",
    "does NOT call ensureOllamaModelRegistered when opts.provider is absent",
    "claude model with spawnBackend=docker still uses docker backend (not adapter)"
   ],
   "cli/spawn-settlement-attempt.test.ts": [
    "serializes dispatch and settlement for the same task with a unique-owner fence",
    "refuses a later dispatch after NOT_DISPATCHED even when no Task JSON was published",
    "fails closed when historical receipts make task execution authority ambiguous",
    "allows only the caller that owns the exact open invocation to dispatch",
    "durably journals the exact attempt before backend.spawn can perform provider work",
    "persists no backend attempt or provider work when the final dispatch boundary rejects",
    "fails before provider work when a terminal-result contract resolves to a non-settlement backend",
    "fails before host-adapter bootstrap when the adapter cannot return a settlement",
    "routes a container-capable adapter provider to the settlement backend instead of its host adapter",
    "keeps failing closed for a container-capable adapter provider when no backend is configured",
    "projects the exact immutable receipt into task status with the real finalizer"
   ],
   "cli/sprint-complete.test.ts": [
    "shows sprint number in title with zero-padded format",
    "shows success/failure count clearly",
    "does not show \"needs attention\" when no failures",
    "shows time duration in human-friendly format",
    "shows code stats when results are provided",
    "shows \"What went well\" section",
    "shows no boundary violations in \"What went well\"",
    "shows first-try task count in \"What went well\"",
    "shows self-healed tasks in \"What went well\"",
    "shows \"What needs attention\" for NO_GO tasks",
    "shows self-healing rate when retries occurred",
    "does not show self-healing rate when no retries",
    "shows next steps with actionable commands",
    "shows debt command in next steps when open debt exists",
    "omits debt command when no open debt",
    "shows task count when no metrics",
    "returns empty string for undefined",
    "returns empty string for zero",
    "formats seconds",
    "formats minutes",
    "formats minutes with seconds",
    "formats hours",
    "singular minute",
    "includes first-try count",
    "includes self-healed count",
    "includes no boundary violations",
    "does not include no boundary violations when there were violations",
    "lists NO_GO tasks",
    "includes result notes for NO_GO tasks",
    "flags tasks with many retries",
    "returns empty for clean sprint",
    "returns null when no results",
    "returns null when no retries occurred",
    "calculates 100% when all retries succeeded",
    "calculates partial rate correctly",
    "counts GO_WITH_TECH_DEBT as healed",
    "returns 0 for undefined",
    "counts tasks with no feedbackLoop as first-try",
    "excludes NO_GO tasks",
    "excludes tasks with retries",
    "counts tasks with feedbackLoop but no retries as first-try",
    "returns 0 for undefined",
    "counts tasks that had retries and succeeded",
    "excludes NO_GO tasks even with retries",
    "excludes tasks with no retries"
   ],
   "cli/stale-adr-surface.test.ts": [
    "exposes both languages for every key (scan precondition)",
    "no user-facing CLI help text cites a legacy numeric ADR id (no allowlist)",
    "pins the only catalog keys still citing a legacy ADR id to generated mechanism text",
    "no command description cites a legacy numeric ADR id",
    "no option description cites a legacy numeric ADR id",
    "keeps the functional sentence on the options this task rewrote",
    "cli.process.desc still describes the process-mode surface in en+tr",
    "cli.nervous.recommendations.desc still describes the Brain inbox in en+tr"
   ],
   "cli/start-approved-flow-guard.test.ts": [
    "refuses to silently replan while an approved, unconsumed flow exists",
    "--force-replan consciously overrides the guard and the sprint proceeds",
    "a CONSUMED approved flow (run handle exists) does not trip the guard",
    "run_flow_v2 ON + single approved flow: bare start CONSUMES it canonically (no replan)",
    "run_flow_v2 ON + MULTIPLE approved flows: typed list + --consume-approved yönlendirmesi",
    "run_flow_v2 ON + --consume-approved picks the named flow among several",
    "run_flow_v2 OFF: the B1a refusal stands and now names the enable path",
    "an empty store leaves bare start byte-identical (baseline)"
   ],
   "cli/start-cost-gate.test.ts": [
    "calls evaluateCostGate before spawning workers (gate active by default)",
    "keeps the pre-sprint cost plan read-only until runSprint owns recovery and materialization",
    "blocks before cost planning and worker spawn when the subscription limit gate blocks",
    "surfaces a subscription warning and continues to the cost gate",
    "blocks the sprint with exitCode=1 when the gate returns COST_GATE_EXCEEDED",
    "runs the cost gate with numeric acknowledgement when --force is passed",
    "blocks unknown pricing even when --force is passed",
    "fails closed when the cost pre-plan is unavailable",
    "shows a would-block verdict in dry-run without spawning a worker",
    "prompts the user when realistic cost is above the auto-confirm threshold",
    "does not prompt the user when autoConfirm=true (cost below threshold)",
    "aborts when the user declines the confirm prompt"
   ],
   "cli/start-environment-orphan-preflight.test.ts": [
    "clears stale liveness when durable disposition is terminal",
    "blocks when disposition is %s"
   ],
   "cli/start-gate-exit.test.ts": [
    "sets exit code 1 when the pre-spawn scope gate blocks the sprint (BrainError from runSprint)",
    "sets exit code 1 for a gate-blok BrainError with no explicit phase (falls back to \"unknown\")",
    "preserves exit code 0 (unset) on normal sprint completion — no over-firing"
   ],
   "cli/start-notify-lifecycle.test.ts": [
    "delivers a queued sprint-finalized notification before connector teardown"
   ],
   "cli/start-prompt-gate-flag.test.ts": [
    "registers a --force-prompt-gate option on the start command",
    "omitting --force-prompt-gate threads acknowledgePromptGate: false into runSprint()",
    "--force-prompt-gate threads acknowledgePromptGate: true into runSprint()",
    "--force-prompt-gate is independent of --force-scope — both can be set together",
    "registers deckent_start with an inputSchema (acknowledgePromptGate included)",
    "omitting acknowledgePromptGate writes acknowledgePromptGate: false into the IPC config.json",
    "acknowledgePromptGate: true is threaded into the IPC config.json for the forked runner",
    "acknowledgeScopePaths and acknowledgePromptGate are threaded independently",
    "declares --force-prompt-gate as a commander option (mirrors --force-scope)",
    "StartCommandOpts declares forcePromptGate?: boolean",
    "runSprint() call includes acknowledgePromptGate: opts.forcePromptGate === true — call-site pin",
    "the acknowledgePromptGate line sits in the same runSprint() options object as acknowledgeScopePaths",
    "inputSchema declares acknowledgePromptGate as an optional boolean (default false)",
    "the acknowledgePromptGate schema field sits next to acknowledgeScopePaths",
    "the handler destructures acknowledgePromptGate from its input",
    "acknowledgePromptGate is threaded into runnerConfig (IPC config boundary)"
   ],
   "cli/start-sandbox.test.ts": [
    "accepts --sandbox-mode flag without error",
    "sandbox mode prints sandbox context message",
    "sandbox mode DOES call runSprint (git stash + restore mechanism)",
    "sandbox mode passes sandboxMode:true to runSprint",
    "without --sandbox-mode, sprint runs normally with autoApprove false by default (K3 fail-closed)",
    "sandbox mode with description cleans up zero-config",
    "normal start without sandbox passes sandboxMode undefined",
    "sandbox + auto-approve: both flags work together",
    "sandbox + dry-run: planSprint used (dry-run takes priority over sandbox)",
    "sandbox mode loads config to get language",
    "multiple flags can coexist: --sandbox-mode --force --watch",
    "without --sandbox-mode and --force, sprint completes",
    "--sandbox flag selects SandboxSpawnBackend (name=claude-sandbox)",
    "--sandbox calls createSandboxBackend with project root",
    "without --sandbox, spawnBackend is undefined (byte-identical behavior)",
    "--sandbox + --sandbox-mode: both flags coexist",
    "--sandbox does not call runSprint in dry-run mode"
   ],
   "cli/start-snapshot-branch.test.ts": [
    "calls runSprint with the approved snapshot as preplannedSprint (no fresh planSprint replan)",
    "blocks unknown snapshot pricing under --force before persisting or running",
    "persists a run handle durably via the real run-flow-store (not just in-memory)",
    "prints the formatted sprint summary on success (no error, no non-zero exit)",
    "persists bounded recovery hold evidence in both exact attempt and flow failure",
    "publishes terminal run status after an exact detached child crashes",
    "a second identical start is refused as a typed attempt-mismatch — spawnStart-equivalent runSprint is NOT called twice",
    "revision mismatch -> printError + exitCode=1, runSprint never called",
    "planDigest mismatch -> printError + exitCode=1, runSprint never called",
    "no approved snapshot at all -> RunJobFlowNotApprovedError, exitCode=1, runSprint never called",
    "--flow-id without --revision/--plan-digest -> validation error, exitCode=1",
    "flow flags without the detached-child capability triple -> typed capability refusal, exitCode=1",
    "flags given but config.terminal.run_flow_v2 is not true -> validation error, exitCode=1",
    "calls runSprint WITHOUT a preplannedSprint key (legacy call-shape)",
    "never touches the run-flow-store — no store directory is created",
    "prints the formatted sprint summary on success (legacy success path unaffected)"
   ],
   "cli/startup-performance.test.ts": [
    "paints before authority validation and stops before provider/session work on HOLD",
    "uses supplied config and MemoryStore projection without duplicate reads",
    "treats an unavailable owned memory projection as final and never opens a second store",
    "settles native unknown auth once and suppresses a late update after disposal",
    "clears a switched provider boot projection and suppresses its in-flight result"
   ],
   "cli/status-authority-closure-race.test.ts": [
    "one deterministic fixture sequence: canonical lifecycle authority never competes across surfaces, progress never exceeds total, progress never regresses, and no active worker survives terminal COMPLETE authority"
   ],
   "cli/status-comms.test.ts": [
    "returns null when worker_comms.enabled is false",
    "returns null when config file does not exist",
    "shows no-shared message when shared dir is empty",
    "shows shared entries when worker_comms.enabled and entries exist",
    "shows handoff counts when handoffs exist",
    "uses Turkish strings when lang=tr",
    "section header is always present when enabled",
    "skips expired entries based on TTL"
   ],
   "cli/status-follow.test.ts": [
    "--follow flag is recognized by commander",
    "-f short alias is recognized",
    "follow mode writes initial snapshot to stdout",
    "follow mode subscribes to eventBus",
    "subscribed events enqueue ANSI redraw frames",
    "registers central shutdown cleanup and no raw signal listeners",
    "central shutdown cleanup is idempotent and unregisters its hook",
    "coalesces event bursts to one in-flight plus the latest frame and drains on shutdown",
    "JSON shutdown drains an empty sentinel without cursor or ANSI output",
    "runtime stdout failure performs bounded cleanup and publishes exit code 1",
    "hideCursor returns ANSI escape",
    "showCursor returns ANSI escape",
    "clearScreen returns ANSI escape"
   ],
   "cli/status-json-contract.integration.test.ts": [
    "dist/cli/entry.js is present (build artifact required for this suite)",
    "real binary, isolated empty-state workspace: stdout is exactly one canonical IDLE object, exit 0",
    "real binary ignores a fresh ACTIVE dashboard when no lifecycle authority exists",
    "real binary cleanup HOLD preserves projections while coordinator authority is live"
   ],
   "cli/status-json-contract.test.ts": [
    "--json with no dashboard and no tasks: stdout is exactly one canonical IDLE object, exit 0",
    "--json with a parked nervous approval: pendingApprovals carries the full PendingApproval shape",
    "without --json, no dashboard/tasks: preserves the existing human-text + exit-code contract (not JSON)",
    "without --json + a parked approval: the pending-approvals text block still renders unchanged",
    "--json with a COMPLETE dashboard: emits the honest no-active shape (JSON twin of the human completed-gate)",
    "--json ignores a fresh ACTIVE dashboard when no lifecycle authority exists",
    "--json projects stale dashboard lifecycle and impossible progress through one authority snapshot",
    "COMPLETE dashboard: human + JSON agree it is not live (no unqualified Complete-as-active)",
    "renders a PID-proven ACTIVE authority without requiring a republished read model",
    "keeps an ACTIVE claim without PID proof fail-closed"
   ],
   "cli/status-live-activity.test.ts": [
    "returns the LATEST line per task, in event order",
    "missing file / empty sprint → empty list (fail-soft)",
    "caps at maxTasks (feed stays box-sized)"
   ],
   "cli/status-liveness-truth.test.ts": [
    "keeps a frozen one-write activity row active when host authority says alive",
    "marks only an explicit host-authority dead worker stale with i18n text",
    "never renders host-dead workers as writing or active",
    "keeps unavailable or HOLD authority separate from dead"
   ],
   "cli/status-output.test.ts": [
    "renders the neutral-honest blocked line in English (no single-cause claim)",
    "renders the neutral-honest blocked line in Turkish",
    "does not show the blocked section when blocked count is 0",
    "renders the \"Next\" line via i18n in English",
    "renders the \"Next\" line via i18n in Turkish",
    "does not show the \"Next\" section when there are no waiting tasks",
    "renders the stale-dashboard warning via i18n in English",
    "renders the stale-dashboard warning via i18n in Turkish",
    "still renders the sprint header alongside the localized lines"
   ],
   "cli/status-paused-render.test.ts": [
    "a PAUSED authority NO LONGER requires the persisted read-model",
    "ORPHANED still requires the read-model (contested state, born-688 safety kept)",
    "ACTIVE still requires the read-model",
    "the paused banner names the paused state AND the exact recover command",
    "keeps PAUSED as the top-level lifecycle with self-sufficient readiness",
    "the banner falls back to a derived recover command when the authority omits one"
   ],
   "cli/status-pending-approvals.test.ts": [
    "returns null when nothing is parked (no noise)",
    "renders the count header + the exact accept command per parked approval",
    "localizes the header (TR)"
   ],
   "cli/status-read-model-wire.test.ts": [
    "uses the persisted IDLE revision and never re-counts unresolved historical observations",
    "does not promote ambient task artifacts when IDLE read-model evidence is stale"
   ],
   "cli/status-renderer-pending.test.ts": [
    "renders the parked approval title + exact accept command",
    "omits the pending section when nothing is parked (no noise)"
   ],
   "cli/status-renderer-polish.test.ts": [
    "PLAN → gray (ANSI 90)",
    "EXECUTE → blue (ANSI 34)",
    "EVALUATE → yellow (ANSI 33)",
    "RETRO → green (ANSI 32)",
    "noColor=true → returns plain string",
    "PLAN phase renk snapshot",
    "fresh heartbeat (< 2min) → 🟢",
    "stale heartbeat (> 2min < 5min) → 🟡",
    "stale heartbeat (> 5min) → 🔴",
    "no heartbeat → 🔴",
    "exactly 2min → 🟡 (boundary)",
    "RESULT-DONE → ✅",
    "SPAWN → 🔁",
    "NO_GO → ❌",
    "ALERT → ⚠️",
    "NOTIFY → 🔵",
    "unknown type → •",
    "case insensitive matching",
    "60-col → ASCII fallback box drawing",
    "80-col → Unicode box drawing",
    "ASCII fallback snapshot (55 cols)",
    "< 50% → green (ANSI 32)",
    "50-80% → yellow (ANSI 33)",
    "85% → red (ANSI 31)",
    "exactly 80% → red (boundary: >=80 is red)",
    "noColor=true → plain text",
    "cost 85% → red color snapshot",
    "first partialRedraw → full write (clears screen)",
    "second partialRedraw with 1 changed line → only that line rewritten",
    "partialRedraw with no changes → 0 lines redrawn",
    "partialRedraw with multiple changes → correct count",
    "resetRedrawState → next call does full write",
    "low progress → green filled region",
    ">= 80% → yellow filled region (warning)",
    "noColor=true → no ANSI codes",
    "total=0 → empty bar (all░)",
    "done > total → capped at 100%",
    "renders events with correct icons",
    "no events → \"Recent Events: none\""
   ],
   "cli/status-terminal-publication-race.test.ts": [
    "keeps JSON, human/watch projection inputs bounded while stale archive residue lingers"
   ],
   "cli/status-terminal-receipt.test.ts": [
    "keeps canonical COMPLETE and its receipt when stale dashboard and task residue remain",
    "projects logical progress from task lineages rather than stale dashboard progress",
    "reports a foreign receipt as a conflict without changing canonical lifecycle"
   ],
   "cli/status-truth-gate.test.ts": [
    "renders the honest completed block, not live progress (the 2026-07-06 live lie)",
    "a genuinely ACTIVE dashboard still renders the live view (zero regression)",
    "a PAUSED (parked after a FIX spawn failure) dashboard does NOT print \"completed\"",
    "a FIXING dashboard is not rendered as completed either (no false Complete mid-FIX)",
    "stale + no live sprint + no task files → orphaned (honest no-sprint view)",
    "fresh dashboard is never orphaned",
    "a live sprint keeps the dashboard authoritative even when stale"
   ],
   "cli/stream-markdown.test.ts": [
    "TTY under NO_COLOR → markers stripped, no ANSI (the gate applies to streaming too)",
    "non-TTY → passthrough (no ANSI, flush empty)",
    "TTY → **bold** in one chunk renders BOLD…RESET (no literal **)",
    "TTY → ** split across chunk boundary still toggles correctly",
    "TTY → single * at chunk end held then flushed (not a bold marker)",
    "TTY → `code` renders the code role…RESET (never dim)",
    "TTY → flush closes an unclosed bold (no style leak)"
   ],
   "cli/stream-segmenter.test.ts": [
    "discard clears partial without emitting",
    "emits prose lines as they complete (real-time flow)",
    "buffers a fenced code block and emits it whole",
    "buffers a table (with separator) and emits it whole",
    "a pipe-run without a separator is emitted as prose lines (not a table)",
    "flush emits a trailing partial line",
    "flush emits an unclosed code block",
    "caps a runaway unclosed code block — emits mid-stream instead of swallowing the rest of the reply",
    "a normal-sized closed code block is unaffected by the cap (still emits whole on close)",
    "after a force-flush the REAL closing fence closes the block and prose after it is NOT swallowed",
    "every force-flushed chunk of a giant block is a balanced fenced block (opens + closes)"
   ],
   "cli/subcommand-folds.test.ts": [
    "declares every canonical folded command path"
   ],
   "cli/surface-contract-catalog.test.ts": [
    "has bilingual rows for every batch key",
    "interpolates the shared deprecation template"
   ],
   "cli/surface-contract.test.ts": [
    "declares all deprecated forwarding mappings",
    "declares option and subcommand groups"
   ],
   "cli/surface-registry.test.ts": [
    "has unique, well-formed typed rows and safe accessors",
    "implements the owner-approved v2.1 visible groups",
    "covers the complete live Commander top-level universe, including hidden entries",
    "keeps every deprecated replacement resolvable and complete",
    "points every summary at a real English catalog row"
   ],
   "cli/sync-aggregate.test.ts": [
    "reduces an empty sync output to all-zero counts, a null git block and empty conflict state",
    "dedupes missing-baseline agent ids across prompt and manifest reports and sorts local-edit conflicts by scope then agentId",
    "orders multiple local-edit conflicts within the same scope by agentId ascending",
    "treats a legacy conflict record lacking `kind` as local-edit (fail-closed)",
    "reports git as null when gitChanges is explicitly null, and populated when present",
    "propagates a `root-fallback` detection mode from the raw report onto `git.detection`",
    "propagates an `unavailable` detection mode from the raw report onto `git.detection`, carrying the issue code onto `git.issueCode` (the free-text `detail` is what gets dropped)",
    "fails closed to `detection: \\'unavailable\\'` when a legacy raw report omits `detection` entirely",
    "distinguishes a failed log (commits: 0, `unavailable`, `GIT_LOG_FAILED`) from a successful zero (commits: 0, `range`, `issueCode: null`) — the two must never be deep-equal",
    "freezes the returned summary and every nested object/array so it cannot be mutated",
    "accepts a summary produced by buildSyncAggregate, including the empty-output shape",
    "rejects a non-object, null, or array value",
    "rejects a wrong schemaVersion",
    "rejects a negative count field",
    "rejects a non-integer count field",
    "rejects a non-string agentId inside missingBaseline.agentIds",
    "rejects an unknown conflict kind",
    "rejects an unknown conflict scope",
    "rejects a mismatched missingBaseline.count / agentIds.length pair",
    "rejects a git block with a non-numeric field, but accepts a null git block",
    "rejects a git block whose detection is outside the three closed modes, and rejects one missing detection entirely",
    "accepts each of the four closed issue codes paired with `unavailable`",
    "accepts `issueCode: null` paired with each of the three closed detection modes",
    "rejects an unknown issue code string",
    "rejects a valid issue code paired with a clean detection mode (`range` or `root-fallback`) — a code names a failure, so it can only ever accompany `unavailable`",
    "rejects a git block missing `issueCode` entirely (the field is required, not merely optional-and-null)",
    "rejects a non-null, non-string `issueCode` (explicit undefined, a number, an object)",
    "rejects a conflicts entry whose kind is missing-baseline (contract: conflicts is local-edit only)",
    "accepts buildSyncAggregate output for a raw report mixing missing-baseline and local-edit records, with missing-baseline kept out of conflicts"
   ],
   "cli/sync-async-liveness.test.ts": [
    "(1) awaiting a slow child does not block the event loop: a parallel 50ms interval keeps ticking",
    "(2) timeout: a child that ignores nothing dies on the first SIGTERM",
    "(3) timeout with a SIGTERM-ignoring child: SIGKILL escalation fires and reaps it",
    "(4) stdout beyond maxOutputBytes is an overflow failure; the (truncated) output is never handed back",
    "(5) a child that signals itself SIGTERM is reported as a signal failure, not a plain non-zero exit",
    "(6) non-zero exit carries the first stderr line as detail, verbatim",
    "(7) a non-existent command is a spawn_error with no live child ever observed",
    "(8) success carries exact stdout, including embedded NUL bytes, byte for byte",
    "(9) collectGitChanges on a small real repo does not block the event loop while it awaits git",
    "(10) root repro 1043: parent exits 0 while a same-group descendant holds stdout — the anchor keeps authority, the descendant is REALLY terminated, typed timeout with proven-empty group",
    "(11) a descendant that escaped to its own session holds stdout: the owned group is proven empty, the streams stay held from outside it — child_tree_unresolved, the escaped process is NOT signalled",
    "(12) a deadline declared before a late exit 0: SIGTERM ignored by the live target, it exits on its own — the result stays a timeout failure carrying the real exit record, tree proven empty",
    "(15) root repro 1046: parent exits 0; the descendant catches SIGTERM, closes only its stdio and lives — the anchor still owns the group, SIGKILL after grace REALLY terminates it, typed timeout with proven-empty group",
    "(16) target alive at the deadline, its descendant survives the group SIGTERM by closing only stdio — streams close, the group still has that member, the anchor-held authority SIGKILLs it: typed timeout, descendant dead",
    "(17) chain stop: after an unresolved tree (escaped session), the next Git call for the SAME repository is refused before spawning; another repository is unaffected; an explicit reset re-enables the repository",
    "(18) success custody: a plain exit-0 target carries the three separate records — IPC target exit, stream EOF and a proven-empty group — and no SIGTERM was ever needed",
    "(19) root probe 1051: the REAL anchor is SIGSTOPped through its own handle right after ready — the deadline still fires, the group is SIGKILLed, typed timeout with proven-empty group, no external intervention",
    "(20) absent spawn acknowledgment: an anchor that says ready and then ignores every message is contained by the lifetime deadline; cleanup verified",
    "(21) delayed spawn acknowledgment inside the deadline: an anchor that acknowledges late still yields a normal success with the three custody records",
    "(22) IPC channel closed while the spawn is pending: the pending acknowledgment settles as ipc-closed, the call ends as a typed spawn_error with the anchor group SIGKILLed — not a strand, not a timeout",
    "(23) handshake bounded by the lifetime deadline: an anchor that never says ready is SIGKILLed through its direct handle, cleanup verified, typed timeout in the handshake phase",
    "(13) stderr beyond maxOutputBytes is dropped and counted, never overshoots the cap, and never fails a successful command",
    "(14) with a flooded stderr, a non-zero exit still reports the first stderr line verbatim"
   ],
   "cli/sync-capabilities-migrate.test.ts": [
    "migrates a lacking-capabilities builtin manifest and dual-carries activation.rules",
    "leaves an already-v3 manifest byte-for-byte untouched",
    "leaves a non-builtin-source manifest untouched and out of every report bucket",
    "records a typed issue for malformed JSON and still migrates a sibling valid manifest",
    "skips the archive/ subdirectory entirely",
    "dryRun reports what would migrate but writes nothing to disk",
    "returns an empty report when .deckent/agents does not exist (fresh/uninitialized project)",
    "report shape always exposes migrated/alreadyV3/issues arrays"
   ],
   "cli/sync-eisdir.test.ts": [
    "does not throw when .cursor/rules is a directory (live repro)",
    "still syncs the other adapter files when one entry is a directory",
    "heals the directory entry: syncs .cursor/rules/deckent.mdc with no typed error",
    "does not write anything and does not throw in dry-run mode, even with the directory collision",
    "reports the same labels as before, with no filesystem writes",
    "report form matches the plain form for a clean fixture"
   ],
   "cli/sync-format.test.ts": [
    "EN: returns sync.no_changes",
    "TR: returns sync.no_changes",
    "EN: matches the getMessage-composed output and today's legacy literal text byte-for-byte",
    "TR: matches the getMessage-composed output",
    "EN: uses sync.format_last_sprint",
    "TR: uses sync.format_last_sprint",
    "EN: MAX_FILE_LIST+3 files → \", and 3 more...\" suffix with the right remainder",
    "TR: MAX_FILE_LIST+3 files → \" ve 3 tane daha...\" suffix",
    "does not truncate when file count is at or below MAX_FILE_LIST",
    "omitting lang falls back to getLanguage() without throwing (below-threshold path)",
    "omitting lang falls back to getLanguage() without throwing (over-threshold path, either language suffix)"
   ],
   "cli/sync-git-detection.test.ts": [
    "(a) single commit, commitCount 1 → root-fallback; added includes the committed file",
    "(b) N == history depth (2 commits, count 2) → root-fallback; added has files from BOTH commits, modified empty",
    "(c) N < history depth (3 commits, count 2) → range; modified/added exact",
    "(d) rename: commit a file, then git mv + commit, count 1 → renamed has the new path, not modified/added",
    "(e) non-repository directory, count 1 → unavailable / GIT_REV_LIST_FAILED, lists empty",
    "(f) empty repository (git init, no commits), count 1 → unavailable / GIT_REV_LIST_FAILED",
    "(h) 2 commits, count 2 → root-fallback; added has both files; show-object-format reports sha1",
    "(i1) repo with 2 commits, since far in the past → commits.length 2, issue null",
    "(i2) repo with 2 commits, since far in the future → a SUCCESSFUL zero: commits [], issue null",
    "(i3) non-repository directory → commits [], issue.code GIT_LOG_FAILED, detail nonzero_exit-prefixed",
    "(i4) unborn branch (git init, no commits) → issue.code GIT_LOG_FAILED, detail nonzero_exit-prefixed",
    "(j1) equals probeCommitsSince(...).commits on success",
    "(j2) returns [] on the non-repository failure (explicit compat: cannot tell a failure from zero)",
    "(k1) non-repository directory → commits 0, lists empty, detection unavailable/GIT_LOG_FAILED",
    "(k2) repo with 3 commits + far-future since → a SUCCESSFUL zero: {commits:0, detection: range/null} — distinct from (k1)\\'s failure",
    "(k3) repo with 3 commits + since 2020 → commits 3, root-fallback, added has all three files",
    "(l1) rejects with RangeError for commitCount=%p in a real repo",
    "(l2) rejects with RangeError before any git call on a non-repository path (validation precedes git)",
    "(m) unicode+space paths (incl. a nested unicode+space directory) list exact raw paths in BOTH root-fallback and range mode",
    "(n) git mv of a unicode+space path in range mode → renamed has the exact new path; modified/added/deleted do not contain it",
    "(o1) since='not-a-date' on a real 2-commit repo → resolves (never throws); observed on git 2.43.0: issue null, commits [] (git silently matches nothing rather than failing the log)",
    "(o2) since='' (empty) on a real 2-commit repo → resolves (never throws); observed on git 2.43.0: issue null, commits [] (same silent-no-match behavior as (o1))",
    "(p) git rm + commit, count 1 → deleted exact",
    "(q) a directory that does not exist at all → resolves to commits 0, detection unavailable/GIT_LOG_FAILED, detail spawn_error-prefixed"
   ],
   "cli/sync-skill.test.ts": [
    "materializes builtin manifests and bodies, then is idempotent",
    "preserves a user-edited builtin body and reports it as kept local",
    "persists and preserves an authored builtin profile with manifest provenance",
    "does not overwrite a project-authored manifest with a builtin of the same id",
    "repairs a stale builtin observability manifest while preserving disabled state",
    "reports dry-run changes without writing manifests"
   ],
   "cli/sync-truth.test.ts": [
    "fresh project, no git: creates every builtin agent prompt/manifest, no conflicts, git summary is null",
    "is idempotent: reruns create/update nothing and the third run reproduces the second run\\'s summary exactly",
    "missing-baseline shadow is reported once, is NOT a conflict, and its content is preserved byte-for-byte",
    "a shadow that diverges from BOTH the recorded baseline and the builtin is a real local-edit conflict",
    "the text-mode aggregate footer is numerically identical to an equivalent fresh run\\'s JSON summary",
    "prints Turkish-localized lines when DECKENT_LANGUAGE=tr",
    "manifest shadow with missing baseline is kept byte-identical and never migrated",
    "manifest shadow with a real local edit is kept byte-identical and never migrated",
    "(a) --json: gitChanges + summary report GIT_LOG_FAILED as unavailable, not a successful zero",
    "(b) text (EN): prints the unavailable warning with the exact detail from the same repo state, never \"no changes\"",
    "(c) text (TR): same, localized"
   ],
   "cli/sync-workspace.test.ts": [
    "repairs a stale managed contract, preserves user content, and is idempotent",
    "reports dry-run changes without changing workspace bytes"
   ],
   "cli/sync.test.ts": [
    "errors when DECKENT.md does not exist",
    "calls ensureDeckentImport for CLAUDE.md and AGENTS.md when DECKENT.md exists",
    "does not set error exitCode when DECKENT.md exists"
   ],
   "cli/task-settlement.test.ts": [
    "is dry-run by default and appends no immutable receipt",
    "evaluates an explicit attestation without mutation when --apply is absent",
    "requires both attestation flags before --apply and never opens authority",
    "rejects an untyped or sentinel pre-dispatch reason before opening authority",
    "requires a typed reason only for a declared eventless receipt",
    "settles and idempotently replays a declared eventless receipt with a typed reason",
    "atomically settles legacy absence while preserving raw task JSON",
    "canonically settles planned %s without mutating its raw task projection",
    "optionally preserves a precise typed cause for legacy settlement without raw operator text",
    "uses one attestation snapshot and one apply re-check without a third probe",
    "fails closed when absence evidence drifts during the apply re-check",
    "rejects traversal-like task identifiers before any file read",
    "never settles a normal Docker dispatch from a public result projection"
   ],
   "cli/term-flow-composition.test.ts": [
    "NL -> typed-proposal -> builder-validation -> actual-preview -> digest-bound-approval -> ",
    "a finalizer completion record built WITHOUT a flowId reads back undefined — no throw, absent semantics + sprintId fallback preserved",
    "compileRunProposal folds a reserved-label-looking intentSummary safely (no parser fracture, no label hijack)"
   ],
   "cli/term-mode.test.ts": [
    "starts in run by default (owner decision 2026-09-03); ask stays an explicit posture",
    "exports /term as the single transition command",
    "non-/term lines → none (fall through to chat / other dispatch)",
    "bare /term → status (case-insensitive, whitespace-tolerant)",
    "/term ${mode} → switch:${mode} (case-insensitive)",
    "unrecognized or extra arguments → usage (never a silent no-op)",
    "${from} + /term ${target} → ${target} (changed:${expectChanged})",
    "Oku is allowed in ask",
    "${risk} is rejected in ask, with a suggestion that actually allows it",
    "${risk} is allowed in run",
    "Otonom is rejected in run, suggesting control",
    "${risk} is allowed in control",
    "every risk value actually used in the live COMMAND_REGISTRY is covered by control mode",
    "mode ladder is strictly cumulative (ask ⊆ run ⊆ control)"
   ],
   "cli/term-simple-mode.test.ts": [
    "defaults to false (off) with an empty config",
    "defaults to false when terminal block is absent",
    "defaults to false when simple_mode key is absent",
    "returns true when terminal.simple_mode === true",
    "returns false when terminal.simple_mode === false",
    "treats truthy-but-non-boolean values as off (strict === true check)",
    "roundtrips: setting true then false resolves independently each time (pure, no hidden state)",
    "narrows the full fixture registry down to ≤7 commands",
    "core-set is complete: exactly the 7 expected commands, nothing else",
    "hides advanced user-mode commands (/recall /agents /config)",
    "hides enterprise commands too (/audit /rbac) — simple hides BOTH advanced and enterprise",
    "empty registry returns empty array",
    "tolerates a registry missing some core names (no throw, just fewer results)",
    "mode=\"${mode}\" with simpleMode omitted equals the pre-existing filterRegistryByMode(mode) result",
    "mode=\"${mode}\" with simpleMode=false explicitly equals the same pre-existing result",
    "user mode + simple=true: visible-command count is ≤7",
    "user mode + simple=true: every visible command is in the declared core set",
    "enterprise mode + simple=true: STILL narrows to the core set (simple hides enterprise regardless of mode)",
    "core commands still resolve via the FULL registry when simple-mode hides them from /help (capability always present)",
    "disk-verify: chat-mode.ts does not import ../repl/term-mode.ts (simple-mode is a visibility filter, not a term-mode state)",
    "disk-verify: term-mode.ts does not import chat-mode.ts (no reverse coupling either)"
   ],
   "cli/term-slice-541-544-e2e.test.ts": [
    "SKIP: dist/ predates ${STALE_PAIRS.length} source file(s) this integration proof covers ",
    "SKIP: ${ENTRY} not found — run \\`npm run build\\` first"
   ],
   "cli/term2-wire.test.ts": [
    "prefixes each coalesced event summary with \"[bg] \", newline-joined",
    "flag-off (no bgQueue): single turn, output/onTurnEnd unchanged — byte-identical",
    "flag-off (bgQueue supplied but bgTurnsEnabled false): still a single turn, queue left untouched",
    "bgTurnsEnabled true but no bgQueue: falls back to the base engine (inert, no throw)",
    "flag-on (fake-loop): a bg event enqueued mid-turn is NOT injected until the turn ends, then drains as a synthetic \"[bg] ...\" turn",
    "flag-on: two different-source bg events enqueued mid-turn drain as two ordered synthetic turns"
   ],
   "cli/terminal-links-setting.test.ts": [
    "has metadata with the three options, the auto default and a Turkish description",
    "validatePartialConfig accepts the tokens and refuses anything else",
    "run.tsx resolves the hyperlink decision from env + setting and app.tsx renders markdown with it"
   ],
   "cli/test-run.test.ts": [
    "registers a \"test\" command on the program",
    "test command has --keep option",
    "test command has --timeout option",
    "sets exit code 1 if DIRECTIVES.md is missing",
    "calls runSprint with testMode=true",
    "passes skipCleanup=true when --keep is set",
    "passes custom timeout when --timeout is set",
    "sets exit code 1 for invalid timeout",
    "exit code 0 when all tasks are DONE",
    "exit code 1 when any task has NO_GO status",
    "exit code 1 when metrics report noGoTasks > 0",
    "handles BrainError with phase info",
    "handles generic error gracefully",
    "prints sprint summary on success"
   ],
   "cli/tool-bridge-err-class.test.ts": [
    "spawnFn rejects with EACCES → tagged [deckent-denied], not [mcp-error]",
    "spawnFn rejects with EPERM → also classified as permission-denied",
    "spawnDetachedFn throws EACCES (deckent_start path) → [deckent-denied]",
    "spawnFn rejects with a plain Error (no errno code) → stays [mcp-error] with detail (unchanged)",
    "spawnFn rejects with an unrelated errno code (ENOSPC) → still [mcp-error], not denied",
    "permissionDeniedLabel override replaces the English default (i18n seam)",
    "the two classes are mutually exclusive and distinguishable by prefix (matches run.tsx isDenied/isError)"
   ],
   "cli/tool-bridge-timeout.test.ts": [
    "gives audit/plan (documented long-running) a budget past the 30s default",
    "keeps the conservative 30s default for short read-only tools",
    "falls back to the default for an empty argv",
    "short tool (status) is still killed at the default 30s budget if it hangs",
    "a deckent_audit spawn legitimately exceeding 30s is NOT killed and completes normally",
    "the long-running budget is still finite — an audit spawn that never closes is eventually killed",
    "a spawn-level error (e.g. ENOENT) rejects immediately — no hang until the timeout fires",
    "short tools still resolve normally well within their 30s budget"
   ],
   "cli/tool-exec-labels.test.ts": [
    "en → English summaries with interpolated path/chars/cmd",
    "tr → Turkish summaries with the same interpolation",
    "unknown lang falls back to English (getMessage normalization)",
    "583/N4 — git confirm summaries resolve in en AND tr (the human-seal prompts)"
   ],
   "cli/tool-exec-symlink.test.ts": [
    "symlink FILE inside cwd pointing to an existing outside target → write rejected",
    "BROKEN symlink FILE inside cwd pointing outside (target does not exist) → write rejected",
    "symlink DIRECTORY inside cwd pointing outside → write through it rejected",
    "legitimate in-scope symlink (target stays inside cwd) → write still succeeds",
    "plain new file with no symlink involved → still succeeds (regression guard)"
   ],
   "cli/tool-permission-tier.test.ts": [
    "deckent_start → always (spawns full sprint lifecycle)",
    "deckent_run → always (spawns a worker process immediately)",
    "deckent_process: submit (default) → always, status/result → read",
    "does not regress other tools already classified (spot-check)"
   ],
   "cli/tool-permissions.test.ts": [
    "read-only tools → read (no confirm)",
    "config show / get / list → read",
    "config set / import / migrate → confirm (mutates config.json)",
    "write tools → confirm (once, \"a\" remembered)",
    "destructive tools → always (a/allow-list/full-auto overridden)",
    "deckent_autonomous: read actions silent, mutating actions confirm (Sprint 269)",
    "deckent_audit: gate confirms (writes gate file), query/compliance read (Sprint 269)"
   ],
   "cli/tool-registry-empty-desc.test.ts": [
    "registers without throwing when descriptor.description is an empty string (REPL launch survives)",
    "falls back to \"MCP tool <namespacedName>\" for an empty-string description",
    "falls back for a whitespace-only description too (matches validateToolDefinition trim semantics)",
    "falls back when descriptor.description is entirely absent (undefined) — pre-existing path stays intact",
    "passes a normal non-empty description through unchanged (no behavior change for the happy path)"
   ],
   "cli/tool-telemetry.test.ts": [
    "valid tags: tagCount = calls.length, malformedCount = 0",
    "malformed JSON body: malformedCount = 1, tagCount = 2",
    "missing name field: malformedCount incremented",
    "empty body: malformedCount incremented",
    "counter-silent: no warning when all tags are valid",
    "mismatch-warning: 1 valid + 1 malformed → warning with correct counts",
    "malformed-warning: all tags malformed → warning, 0 executed",
    "no warning with plain text reply (no tags)"
   ],
   "cli/trace-migrate.test.ts": [
    "keeps migrate dry-run side-effect free and returns stable machine-readable evidence",
    "publishes an idempotent migration, builds a canonical corpus, and lints its manifest",
    "fails closed when a positive weight is requested without training admission"
   ],
   "cli/trace-wire.test.ts": [
    "returns undefined when disabled",
    "returns a recorder that appends a JSONL example when enabled",
    "resolves a lazy system getter per write, so a post-open prompt is recorded (not the boot snapshot)",
    "writes only the DELTA per turn and marks the schema transition",
    "trace growth is O(N), not O(N²) — 20 turns cost ~20 deltas, not 210 message copies",
    "a context-epoch compaction is reported as a new epoch record, never a wrong delta",
    "skips a write when the turn produced no new messages",
    "emits each message exactly once while the transcript extends",
    "returns defensive copies — a caller cannot mutate the source transcript",
    "appends one ledger row per turn with the turn usage, and totals replay",
    "stores only the delta per row — the ledger is O(N), not O(N²)",
    "fails soft — an unwritable root never throws out of the recorder",
    "writes the ledger even when the training trace is disabled (buildTurnRecorder → undefined)",
    "fans one call out to every active recorder",
    "returns undefined when nothing is active",
    "carries the turn usage, live model/provider and turn index into recordTurn",
    "falls back to an honest sentinel provider when no resolver is supplied",
    "the usage → ledger disk chain is end-to-end: engine turn → JSONL row",
    "hydrateTranscript loads a prior transcript VERBATIM ahead of the live turn",
    "hydrated history is not re-recorded — only this session\\'s own turn reaches the recorder",
    "an empty hydration leaves the request untouched",
    "redacts a secret found in message content before writing the trace",
    "redacts a secret found inside tool-call arguments before writing the trace",
    "leaves ordinary (non-sensitive) content untouched",
    "fails soft — a write error inside the recorder never throws out of the closure",
    "redacts a secret that first appears in a later DELTA record (7089 delta strategy)",
    "flag OFF stays byte-identical — no recorder, no writes"
   ],
   "cli/trn3-trace-extract.test.ts": [
    "returns the file itself when given a single file",
    "recursively collects *.jsonl under a directory, sorted",
    "redacts message content and tool_call arguments, reports whether anything changed",
    "extracts a single fixture transcript to aligned/general JSONL with redaction applied",
    "walks a directory of transcripts and aggregates honest counts (aligned excludes non-mappable)",
    "running twice APPENDS rather than overwriting (accumulating corpus)",
    "registers `trace extract` and --help renders (no real spawn)",
    "reports an error and sets exitCode=1 when the input path does not exist"
   ],
   "cli/truth-command.test.ts": [
    "returns null when the manifest is absent",
    "returns [] when the manifest exists but declares no truth-blocks",
    "returns the truth array when present",
    "maps label→title and omits undefined optional keys",
    "carries every declared field through to the def",
    "resolves the wired feature green and flags the orphan as half-wire",
    "never reimplements the engine — undefined prodCallsitePattern → wired=undefined, not half-wire",
    "renders headers + status cells with no ANSI under NO_COLOR",
    "empty run → \"no truth-blocks\" message",
    "half-wire section lists the candidate; reports none when all wired",
    "projects features + halfWireCandidates + summary",
    "returns en + tr variants and interpolates {vars}",
    "every key defines both en and tr",
    "diffRatchet computes new + resolved sets",
    "exit 2 — no baseline present → propose creation",
    "exit 1 — a NEW half-wire candidate not in the baseline",
    "exit 0 — baseline matches the live candidate set",
    "--write pins the baseline (sorted), then a re-check passes (exit 0)",
    "malformed baseline degrades to empty set (fail-soft), not \"everything is new\"",
    "registers a tool named deckent_truth (real registration, not a claim)",
    "handler returns the same engine results as the CLI computeTruth (parity)",
    "check=true includes a read-only ratchet diff vs the pinned baseline",
    "reports isError when the manifest is missing"
   ],
   "cli/truth-proof-render.test.ts": [
    "uses the typed catalog dash when proof is absent",
    "sanitizes raw nullish spellings in every cell"
   ],
   "cli/turn-stats.test.ts": [
    "elapsed only when usage absent",
    "elapsed + token count when usage present",
    "formats large token counts as k",
    "is dim-wrapped only when the color gate allows it (FORCE_COLOR=1); plain under NO_COLOR / off-TTY",
    "pulls input/output tokens off the result event",
    "result without usage → usage undefined (footer falls back to time-only)"
   ],
   "cli/usage-command.test.ts": [
    "registers usage command with expected options",
    "renders model-level table for default 7-day window",
    "outputs JSON array with expected shape for --json flag",
    "shows per-task sprint table when --sprint N is given",
    "prints no_data message when no records found",
    "passes --since and --until to parseFn opts",
    "outputs SprintUsageSummary JSON for --sprint --json",
    "shows weekly budget reference line when config has weekly_budget_equiv",
    "omits budget reference line when config lacks weekly_budget_equiv",
    "outputs [] JSON for --json when no data"
   ],
   "cli/usage-comparison.test.ts": [
    "emits stable dry-run JSON with exact denominators, USD and parity and no raw identities or paths",
    "holds with a bounded reason when exact provider USD is unavailable",
    "does not represent an empty or rejected archive cohort as provider-reported zero USD",
    "binds an intentional baseline/candidate feature toggle without treating A/B as incomparable",
    "uses the terminally selected FIX lineage measurement without exposing its exact identity",
    "requires the dry-run digest for apply and publishes only an opaque receipt projection",
    "validates paired and mutually exclusive comparison options and rejects unbound apply"
   ],
   "cli/version-enhanced.test.ts": [
    "buildVersionString returns formatted string with version",
    "buildVersionString shows tmux status",
    "buildVersionString handles missing tmux gracefully",
    "buildVersionString handles missing claude gracefully",
    "buildVersionJson returns structured JSON object"
   ],
   "cli/watch-follow.test.ts": [
    "spawns docker with logs -f and the correct container name",
    "prints the container name and Ctrl+C hint before spawning",
    "handles spawn error gracefully by calling printError",
    "streams stdout lines to the onLine callback",
    "streams stderr lines to the onLine callback",
    "stop() calls proc.kill() to terminate the stream",
    "skips blank and whitespace-only lines",
    "passes docker logs -f and containerName to the spawn function",
    "handles partial chunks across multiple data events (buffering)"
   ],
   "cli/watch-output.test.ts": [
    "registers the real child and preserves exact selectors without dashboard checks",
    "renders ambiguous identity distinctly and never starts an observer",
    "honors command-local Turkish under opposite ambient language",
    "projects the actual OS principal to the reader exact caller shape",
    "sanitizes sealed content before tailing and never serializes raw content or capability",
    "keeps legacy --follow stdout as NDJSON and sends its sole deprecation warning to stderr sink",
    "rejects non-decimal tail %j before the reader",
    "keeps invalid local language machine output valid NDJSON",
    "sets failure for an unavailable live observer without inferring task failure",
    "aborts only the observer client on SIGINT and reports no worker cancellation"
   ],
   "cli/watch.test.ts": [
    "errors when no active sprint (.dashboard missing)",
    "errors when no tmux session",
    "creates watch layout when sprint is active",
    "attaches to specific worker with --follow",
    "handles TmuxError from attachToWorkerPane gracefully",
    "re-throws non-TmuxError errors"
   ],
   "cli/web-removal.test.ts": [
    "buildProgram() no longer registers a `web` command",
    "does not expose `web` as an alias of any surviving command",
    "still registers a healthy command set after the removal",
    "errors with commander.unknownCommand naming the typed word",
    "emits no suggestion for `web` — too far from any surviving command",
    "positive control: a near-miss of `serve` IS suggested",
    "is still registered with a non-empty description",
    "keeps its full option surface (no flag migrated from or lost to `web`)",
    "COMMAND_REGISTRY has no `web` entry",
    "leaves no MCP parity hole — `web` never had an MCP tool",
    "drops `cli.web.desc` from the bilingual catalog",
    "leaves no orphaned `web.*` runtime message keys",
    "keeps `serve` catalog coverage intact"
   ],
   "cli/wizard.test.ts": [
    "returns defaults for all steps",
    "uses first choice for select without default",
    "uses specified default for select",
    "confirm defaults to false when no default specified",
    "input defaults to empty string when no default",
    "returns empty result for empty steps",
    "reads input for text step",
    "uses default when input is empty",
    "handles confirm step with y",
    "handles confirm step with empty (uses default)",
    "handles select step with numeric choice",
    "handles multiple steps sequentially"
   ],
   "cli/wlt-feed-wire.test.ts": [
    "fixture a: progress data present -> progress-reader currentAction wins",
    "fixture b: no progress data, hb.currentAction present -> hb fallback wins",
    "fixture c: neither progress nor hb has data -> sentinel 'unknown'",
    "an empty progress currentAction (no valid steps yet) falls through to hb, not blank",
    "a finished worker (has a .result, i.e. not in activeTaskIds) gets no workers entry",
    "zero active workers -> workers field entirely omitted (backward-compat with toEqual({}))",
    "multiple active workers each resolve independently",
    "reads a real progress.jsonl fixture and prefers it over hb.currentAction",
    "no progress file for a worker -> hb.currentAction fallback via the fs seam",
    "neither progress file nor hb.currentAction -> 'unknown', feed still returns cleanly",
    "missing .tasks dir entirely -> no workers, no thrown error (honest idle)"
   ],
   "cli/worktree-binary-authority-live.test.ts": [
    "holds a mismatched %s-shaped start before any runtime state is created",
    "keeps help reachable from a mismatched Deckent checkout",
    "requires an explicit override and surfaces that override before continuing"
   ],
   "cli/worktree-binary-authority.test.ts": [
    "does not impose dogfood identity checks on an ordinary user project",
    "resolves a src-less user project to allow instead of crashing on eager identity",
    "allows a compiled binary bound to the exact Deckent checkout root",
    "warns instead of holding when a same-root build input changes after the manifest was minted",
    "reports same-root source drift as warn with or without the diagnostic override",
    "holds when a Deckent checkout is driven by another worktree runtime",
    "allows direct same-root source execution without manufacturing a dist identity",
    "turns a mismatch into a visible override decision only with the exact env opt-in",
    "compares Windows roots case-insensitively while preserving POSIX case",
    "strictly parses the four-field manifest and rejects corruption/unknown fields",
    "keeps diagnostic invocation %j reachable",
    "checks operational invocation %j",
    "returns a normalized binding to the exact manifest, source tree, and entrypoint bytes",
    "never manufactures a dist identity for direct source execution",
    "holds when the executing module is another checkout or current sources drift"
   ],
   "cli/xverify-authority-unlock.test.ts": [
    "resolves the verifier scope and selects the configured verifier model, with authority no longer unavailable",
    "still holds typed when no policy has been authored — selector and full composition agree",
    "refuses a below-floor verifier request typed, even inside the full-composition call shape"
   ],
   "cli/xverify-claim-operation.test.ts": [
    "carries the owner-authored cross_verify block through, overriding only what the invocation forces",
    "routes CLI/MCP session claims as adjudication and keeps the full claim out of Title",
    "does not infer an automatic verifier candidate when --verifier is omitted",
    "carries --diff as untrusted evidence context instead of a second Description authority"
   ],
   "cli/xverify-evidence-scope.test.ts": [
    "passes changed paths from --diff into bootstrap evidence scope",
    "filters broker-inadmissible (oversize) files out of the derived diff paths",
    "treats --files as the exact --diff evidence boundary",
    "does not suppress the remedy when neither --files nor --diff is present"
   ],
   "cli/xverify-tier-floor.test.ts": [
    "reads the fixture ladder straight out of the registry",
    "refuses below-tier, admits equal and above-tier, in the pure seam",
    "fails closed — an identity absent from the registry proves no floor",
    "refuses a below-tier verifier requested explicitly, before any spawn",
    "admits an equal-tier verifier",
    "admits an above-tier verifier — the floor is a floor, not an equality",
    "refuses a below-tier identity that came from config, not from a flag",
    "enforces the floor against the AUTHORITATIVE author model, not the task stamp",
    "refuses when the author identity is unknown to the registry",
    "leaves the same-provider prohibition exactly as it was",
    "refuses an id absent from the model registry, before any dispatch",
    "refuses an id owned by a provider other than --author",
    "carries the stated author model to the runner and leaves the claim envelope alone",
    "records the substituted default as low-confidence when the flag is omitted",
    "renders the runner typed refusal in the operator language, not as a wire code",
    "keeps the self-verification prohibition unchanged alongside the new flag",
    "ships every new operator string in both catalog languages"
   ],
   "cli/xverify-ux.test.ts": [
    "scopes captureDiffFn to exactly the trimmed --files paths, duplicates included",
    "dedups --files into task.scope.filesRead without dropping any distinct path",
    "reports a typed i18n remedy when no --files/--diff/--target were given",
    "leaves remedy null once any evidence (files/diff/target) is attached",
    "still surfaces the remedy when the runner independently flags evidence as missing, even though evidence was attached",
    "extracts an exact 1-based inclusive line range",
    "extracts a brace-balanced symbol block",
    "extracts a brace-less one-liner symbol as a single line",
    "dedups a target path already present via --files into a single scope entry",
    "rejects a malformed --target spec with the exact i18n catalog message, before any spawn",
    "rejects a --target pointing at a nonexistent file with the exact i18n catalog message",
    "rejects an out-of-range --target line range with the exact i18n catalog message",
    "rejects a --target symbol that does not appear in the file with the exact i18n catalog message"
   ],
   "cli/xverify-waiting-signal.test.ts": [
    "announces each pending request exactly once, however many poll ticks pass",
    "announces a request that only becomes pending on a later tick",
    "writes the typed line to stderr only, in both supported languages",
    "takes the minimum of the authored window and the operator timeout",
    "leaves the mechanism default untouched when neither bound exists",
    "keeps the authored window when no --timeout was given",
    "bounds the decision window with --timeout and holds typed undecided with the request id",
    "prints no waiting line when the decision is already there (fast path)",
    "prepares the exact --verifier-model instead of the configured provider default",
    "projects an exact runner HOLD detail without requiring public result mutation",
    "emits exactly one parseable document on stdout while the waiting signal goes to stderr"
   ]
  },
  "config": {
   "config/isolation.test.ts": [
    "excludes src/dashboard from compilation",
    "still excludes node_modules, dist, and tests",
    "excludes src/dashboard/** from coverage",
    "ignores src/dashboard/node_modules",
    "ignores src/dashboard/dist"
   ],
   "config/nervous-enabled.test.ts": [
    "enabled is true",
    "mode is balanced",
    "safety-floor locked_actions preserved and bypass_allowed is false",
    "writes and reads config fixture via tmpdir without touching live state"
   ],
   "config/nervous-faz1-smoke.test.ts": [
    "Zod schema parse round-trip passes",
    "nervous_system.mode is a valid authority preset",
    "notifications.severity_min is info",
    "stale_worker is enabled with the canonical 120000ms threshold",
    "dead_event_stream is available but default-off",
    "directives_protection is enabled",
    "exactly five base detectors are enabled by default",
    "W0 6 new detectors are all disabled"
   ],
   "config/nervous-reenable-safe.test.ts": [
    "config enabled: nervous_system.enabled is true after re-enable",
    "advisory-mode: evaluatePanicGate returns PROCEED immediately for non-locked action (non-blocking)",
    "safety_floor preserved: bypass_allowed is false and locked actions are intact",
    "safety_floor preserved: evaluatePanicGate returns REJECTED for locked action (never bypassed)",
    "isLockedPanicAction correctly identifies locked actions"
   ]
  },
  "connectors": {
   "connectors/approval-clients-wire.test.ts": [
    "attaches slack when enabled with a resolved token + channel_id + transport",
    "attaches teams when enabled with a resolved token + channel_id + transport",
    "attaches telegram when enabled with chat_id + transport",
    "silently skips telegram when no transport is provided",
    "attaches both slack and teams from one config + transports call",
    "attaches nothing when approval_channels is absent entirely",
    "attaches nothing when config itself is undefined",
    "does not attach a channel whose enabled flag is false",
    "does not attach a channel whose enabled flag is absent (default-off)",
    "skips slack when the token is still the unresolved $DECK: placeholder",
    "skips teams when the token is missing entirely",
    "skips a channel when channel_id is missing, even with a resolved token",
    "skips a channel enabled+resolved but with no transport provided (never throws)",
    "one misconfigured channel (unresolved token) never blocks the other (valid) channel",
    "never logs the resolved secret value, on a successful attach",
    "never logs the resolved secret value, on a skip path (missing channel_id)",
    "slack: a wired button press resolves the broker awaitDecision end-to-end",
    "teams: a wired card action resolves the broker awaitDecision end-to-end",
    "fans one stable event to every configured client and durable per-client ACKs suppress restart replay",
    "does not ACK a failed transport send, so the same event remains retryable",
    "renders expiry through the injected i18n formatter as a view-only message on every client"
   ],
   "connectors/approval-clients.test.ts": [
    "sends masked-args + risk/scope summary as a section+actions Block Kit payload, never a raw value",
    "falls back to postMessage (no ts captured) when the transport lacks postMessageReturningTs",
    "updates the original card in place when a message ts was captured",
    "sends a plain follow-up message when no ts was captured (or no updateMessage support)",
    "maps an approve press to an allow decision",
    "maps a reject press to a deny decision",
    "ignores a non-approval action value",
    "a transport failure reports channel-error without blocking another attached channel",
    "a button press resolves the broker awaitDecision end-to-end",
    "sends masked-args + risk/scope summary as an Adaptive Card attachment, never a raw value",
    "falls back to sendActivity (no id captured) when the transport lacks sendActivityReturningId",
    "updates the original card in place when an activity id was captured",
    "sends a plain follow-up message when no activity id was captured (or no updateActivity support)",
    "maps an approve press to an allow decision",
    "maps a reject press to a deny decision",
    "ignores a non-approval action value",
    "a transport failure reports channel-error without blocking another attached channel",
    "a button press resolves the broker awaitDecision end-to-end"
   ],
   "connectors/approval-resolve.test.ts": [
    "approve: calls editMessage with approved outcome when approvalMessageId is set",
    "reject: calls editMessage with rejected outcome when approvalMessageId is set",
    "no editMessage call when approvalMessageId is absent",
    "expired approve: calls editMessage with refusal text and does NOT execute action",
    "best-effort: editMessage throwing does not crash the resolve path"
   ],
   "connectors/approval-slack-risk-tier.test.ts": [
    "omits actions for high+critical and rejects a forged button value",
    "keeps explicit elevated tier interactive"
   ],
   "connectors/approval-slack.test.ts": [
    "renders the source/reason/code card triple with brk values, a nonce, and no raw id",
    "resolves a valid brk callback to its raw broker id and rejects unknown or wrong-namespace payloads",
    "renders critical requests view-only with approve/reject command hints"
   ],
   "connectors/approval-teams-risk-tier.test.ts": [
    "omits card actions for high+critical and rejects a forged submit",
    "keeps explicit elevated tier interactive"
   ],
   "connectors/approval-teams.test.ts": [
    "renders the source/reason/code triple and versioned Action.Submit values with a per-card nonce",
    "makes critical cards view-only and includes the explicit decision-command hint",
    "validates and decodes a versioned card action into the relay decision shape"
   ],
   "connectors/approval-telegram-risk-tier.test.ts": [
    "renders high+critical view-only and ignores a forged legacy callback",
    "keeps an explicit elevated tier interactive despite legacy high"
   ],
   "connectors/approval-telegram.test.ts": [
    "sends the source/reason/short-code triple with versioned buttons and no raw request id in payloads",
    "generates one fresh 8-hex nonce per card",
    "renders critical requests view-only with the short-code CLI decision hint",
    "falls back to sendMessage (no id captured) when the transport lacks sendMessageReturningId",
    "edits the original card in place when a platform message id was captured",
    "sends a plain follow-up message when no message id was captured (or no editMessage support)",
    "maps an approve press to an allow decision for the raw broker request",
    "maps a reject press to a deny decision for the raw broker request",
    "ignores a non-approval callback payload",
    "a transport failure reports channel-error without blocking another attached channel",
    "a button press resolves the broker awaitDecision end-to-end"
   ],
   "connectors/base-connector.test.ts": [
    "start/stop lifecycle — enabled connector becomes started, stop resets",
    "start with disabled config — no-op, connector stays stopped",
    "handler registration — multiple handlers receive incoming messages",
    "unhealthy state detection — not started connector is not healthy",
    "error propagation — start failure throws, connector stays stopped",
    "handler error isolation — one handler throwing does not prevent others",
    "IncomingMessage schema — all required fields present"
   ],
   "connectors/bootstrap-wiring.test.ts": [
    "bootstrapConnectorCommands uses deps.artifacts when provided, artifact is resolvable from the same instance",
    "when deps.artifacts is absent, bootstrap creates an INTERNAL store (negative proof: external artifact not found via fresh store at same root)",
    "bootstrap creates its own store when deps.artifacts is absent (backward-compat)",
    "transcribe is called when voiceAdapter + stt enabled + voice message arrives",
    "voiceAdapter null → transcribe not called (default-off stays byte-identical)",
    "sends honest degrade message when transcribe throws"
   ],
   "connectors/bot-action-store.test.ts": [
    "park writes a durable action and returns an id; list reflects it",
    "take returns the parked action and CONSUMES it (idempotent — second take is null)",
    "take of an unknown id → null",
    "distinct parks get distinct ids and coexist",
    "no store dir yet → list is empty, take is null (no throw)",
    "prefix match: take resolves an id by its unique prefix (CLI/phone UX)",
    "park stores a TTL (expiresAt) on every action",
    "park binds the active sprint for sprint-scoped destructive tools only",
    "kill / cleanup / recover are sprint-scoped destructive",
    "plan / sync / status are NOT (TTL only, no sprint binding)",
    "within TTL + bound sprint still active → ok",
    "🔴 expired → refused (backlog-replay / forgotten approval)",
    "🔴 bound sprint no longer active (different sprint) → refused (the wrong-kill scenario)",
    "🔴 bound sprint but NOTHING active now (user killed it manually) → refused",
    "unbound action (e.g. plan) → only TTL matters, sprint id ignored"
   ],
   "connectors/bot-agentic-capabilities.test.ts": [
    "auto → runs capability (not parked, not inner)",
    "confirm → parks (existing approve flow), does NOT run",
    "deny → refusal, nothing runs/parks",
    "unavailable → not-available message",
    "non-capability tool path is unchanged (read-only auto-exec)"
   ],
   "connectors/bot-agentic-sendapproval.test.ts": [
    "confirm: parks, calls sendApproval, returns short ack (not the type-approve text)",
    "confirm fallback: no sendApproval (or returns false) → legacy parked text",
    "confirm fallback: sendApproval returns false → legacy parked text (not short ack)",
    "confirm fallback: sendApproval throws → exception swallowed, legacy parked text returned"
   ],
   "connectors/bot-agentic-tool-approval.test.ts": [
    "risky tool + sendToolApproval=true: parks, sends buttoned approval, returns short ack (not \"approve <id>\")",
    "risky tool + no sendToolApproval → legacy parked text (unchanged)",
    "risky tool + sendToolApproval=false → legacy parked text (not short ack)",
    "risky tool + sendToolApproval throws → swallowed, legacy parked text returned",
    "cost/usage/kpi are read-only (not risky)",
    "deckent_cost auto-executes via inner (no park)",
    "start/run/config/autonomous/process/set_directives are risky (gated)"
   ],
   "connectors/bot-agentic.test.ts": [
    "read-only tools are safe (auto-exec)",
    "state-changing / destructive tools are risky (gated)",
    "unknown tools default to risky (fail-safe)",
    "safe tool → inner.dispatch executes, result returned",
    "🔴 risky tool → inner NOT called; parks with {tool,args}; returns informed NOT-EXECUTED + approve <id>",
    "a throwing inner dispatch is surfaced as a tagged string, never thrown (loop stays alive)",
    "advertises CLI sprint tools, NOT raw shell/file (no RCE surface)",
    "answers benignly (no park) when NO checkpoint is pending",
    "still PARKS deckent_checkpoint when a checkpoint IS pending (gate preserved)",
    "without a hasPendingCheckpoint probe, parks as before (backward compatible)",
    "the guard is checkpoint-specific — other risky tools are still parked",
    "emits the benign message in Turkish when lang=tr",
    "returns false when the checkpoints dir does not exist",
    "returns true when a checkpoint file has status pending",
    "returns false when all checkpoints are approved/rejected (none pending)",
    "skips malformed checkpoint files without throwing",
    "no canonical grounding → returns the bare tool prompt (back-compat)",
    "injects only the caller-supplied canonical scoped memory view",
    "surfaces ABSENT without inventing fallback project facts",
    "surfaces HOLD as unavailable rather than empty context"
   ],
   "connectors/bot-commands.test.ts": [
    "detects and normalizes a slash command (trim + lowercase verb)",
    "non-slash text is not a bot slash",
    "a bare slash is still slash territory (handled, not leaked)",
    "lists bot read-only commands + approve/reject + the natural-language hint",
    "does NOT leak destructive CLI slashes",
    "every advertised tool-backed command is READ-ONLY (no gate bypass)",
    "/help → curated help (no dispatcher call)",
    "/status → maps to read-only deckent_status",
    "unknown slash → default-deny guidance, never falls through to CLI/chat",
    "/pending with no parked actions → friendly empty message",
    "/pending lists parked actions with their ids",
    "/pending surfaces nervous + autonomous parked approvals"
   ],
   "connectors/bot-daemon.test.ts": [
    "write → read returns the live pid; clear removes it",
    "returns the listener-published runtime identity only for verified ownership",
    "does not promote a verified schema-v1 record without runtime identity",
    "a stale pidfile (dead pid) reads as null and is cleaned",
    "does not let an older process generation clear a newer record",
    "retires a provably foreign live legacy pid without treating it as a bot",
    "no pidfile → not-running",
    "running → SIGTERMs the process and reports stopped",
    "never signals a live pid whose kernel start token was reused",
    "fails closed and never signals when ownership evidence is unavailable",
    "already running → returns already-running, does NOT spawn",
    "not running → spawns detached and reports started",
    "reports started only after the listener publishes its ownership record",
    "never reports started when the spawned listener dies before readiness",
    "spawn failure → spawn-failed",
    "does not spawn when existing ownership is ambiguous",
    "refuses listener admission when the platform lacks a start-token adapter"
   ],
   "connectors/bot-lifecycle-honesty.test.ts": [
    "removes this listener\\'s pid file even when connector disposal rejects",
    "removes the pid file on a real SIGTERM delivered to the process",
    "still writes and clears the pid file when disposal succeeds cleanly",
    "stopBot stays fail-closed and signals nothing when ownership is ambiguous",
    "startBotDaemon stays fail-closed and never spawns when ownership is ambiguous"
   ],
   "connectors/bot-memory-read.test.ts": [
    "honors the explicit bot consumer profile instead of the generic read limits",
    "grounds and queries through the per-turn tenant authority without the global summary projection",
    "rejects model-supplied tenant or limit authority instead of delegating to the CLI bridge",
    "reuses one child only for the same scope/session/context revision and rotates after a source revision",
    "serializes different agentic sessions because they share exactly one child slot",
    "binds durable chat history to scope, session and principal without changing the external channel id",
    "surfaces unavailable memory as a typed HOLD in both grounding and the tool result"
   ],
   "connectors/callback-router.test.ts": [
    "round-trips a versioned payload",
    "parses an approve payload as legacy",
    "parses a reject payload as legacy",
    "keeps a triggerId that itself contains colons",
    "returns the invalid state for an unknown prefix",
    "returns the invalid state for a missing/empty id",
    "rejects malformed namespaces, actions, short codes, and nonces",
    "round-trips with parseApprovalCallback",
    "stays safely below Telegram callback_data\\'s 64-byte limit",
    "enforces Crockford short-code and hexadecimal nonce character sets"
   ],
   "connectors/capabilities/artifacts.test.ts": [
    "registers + retrieves per chatKey; isolates chats; rejects unknown",
    "TTL prune: expired artifact returns null on get",
    "mime is preserved on register and returned on get",
    "SEC: get() with path-traversal id returns null (id format guard)",
    "SEC: chatKey=\"..\" sanitizes to \"__\" — artifact path stays inside artifacts dir"
   ],
   "connectors/capabilities/chat-turn-media.test.ts": [
    "per-turn mediaConnector.sendMedia is called when capability produces a photo",
    "WITHOUT per-turn mediaConnector (2-arg call), sendMedia is not called — honest text fallback",
    "connector-bootstrap onChat passes the live connector as mediaConnector"
   ],
   "connectors/capabilities/enablement-wire.test.ts": [
    "enabled=true → non-empty catalog containing screenshot and send_mail",
    "enabled=false → empty catalog (nothing advertised)",
    "capConfig absent (undefined) → disabled fallback → empty catalog",
    "🔴 capConfig: { enabled: true } — gate routes screenshot to runAuto, NOT \"not available\"",
    "capConfig absent — gate returns \"not available\" for screenshot",
    "capConfig: { enabled: false } explicit — same as absent, gate returns \"not available\"",
    "contains screenshot (read/auto) and send_mail (external/confirm)",
    "lists only available capabilities (enabled), with ids",
    "returns empty string when master disabled (nothing advertised)",
    "uses connector.sendMedia when present",
    "falls back to honest text when connector lacks sendMedia"
   ],
   "connectors/capabilities/execute-principal-gate.test.ts": [
    "DENIES when principal lacks the required permission (and does not run)",
    "ALLOWS when principal has the permission",
    "ALLOWS (no gate) when capability declares no requiredPermission — back-compat",
    "ALLOWS (no gate) when no principal in context — opt-in / back-compat"
   ],
   "connectors/capabilities/execute.test.ts": [
    "runs capability, sends media via sink out-of-band, returns text-ack, writes audit",
    "unknown capability → honest error",
    "invalid args → honest validation error, run not attempted",
    "text-ack includes artifact ids when result.artifacts is non-empty"
   ],
   "connectors/capabilities/pillar1-e2e.test.ts": [
    "screenshot artifact is attached to send_mail (transport spy sees attachments)",
    "default-off: no artifacts dep → send_mail cannot resolve artifact id (honest error)",
    "bootstrap wiring: capCtx.artifacts is the same store instance as the inbound media path"
   ],
   "connectors/capabilities/platform.test.ts": [
    "win32 → win-native",
    "darwin → darwin",
    "linux + microsoft in /proc/version → win-wsl",
    "plain linux → linux",
    "other (e.g. aix) → unsupported"
   ],
   "connectors/capabilities/policy.test.ts": [
    "master disabled → unavailable",
    "enabled → capability defaultPolicy",
    "global override beats default",
    "per-chat override beats global",
    "destructive can NEVER be auto — clamped to confirm (global override source)",
    "destructive can NEVER be auto — clamped to confirm (per-chat source)",
    "destructive can NEVER be auto — clamped to confirm (defaultPolicy source)",
    "enterprise capability is unavailable on solo edition"
   ],
   "connectors/capabilities/registry.test.ts": [
    "registers and retrieves by id",
    "returns undefined / false for unknown id"
   ],
   "connectors/capabilities/screenshot.smoke.test.ts": [
    "captures a real PNG on this host (or skips honestly)"
   ],
   "connectors/capabilities/screenshot.test.ts": [
    "darwin: builds `screencapture -x -t png <tmp>` and returns PNG media",
    "nonzero exit → honest error text, no media",
    "unsupported platform → honest \"not supported\", never throws",
    "registers artifact when ctx.artifacts present, result includes artifacts array with id",
    "no artifact registered when ctx.artifacts absent (backward-compat)"
   ],
   "connectors/capabilities/send-mail.smoke.test.ts": [
    "opens a real SMTP connection and transmits the envelope",
    "transmits Content-Disposition: attachment when an artifact is attached"
   ],
   "connectors/capabilities/send-mail.test.ts": [
    "exact + domain rules",
    "TR: preview is multi-line with bold *Kime* / *Konu* / *Gövde*",
    "EN: preview is multi-line with bold *To* / *Subject* / *Body*",
    "preview includes body up to 200 chars (not truncated at 120)",
    "attaches artifacts by id; unknown id → honest error, no send",
    "preview with attachIds lists attachment filenames",
    "missing SMTP config → honest error, no send",
    "recipient outside allowlist → denied before send",
    "multi-recipient: any recipient outside allowlist → denied before send",
    "multi-recipient: all allowed → transport called once",
    "valid → calls transport with correct envelope"
   ],
   "connectors/chat-bridge-approval.test.ts": [
    "sends a buttoned HTML preview via sendMessageReturningId and returns the message id",
    "falls back to sendMessage when sendMessageReturningId is absent — returns empty string (sent, no id)",
    "returns false when connector has no sendMessage",
    "uses default preview fallback when capability not in registry — returns \"\" (sent, no id)",
    "i18n: Turkish header appears when lang=tr"
   ],
   "connectors/chat-bridge-onpartial.test.ts": [
    "invokes onPartial with cumulative text as the reply streams",
    "does not affect existing callers when onPartial is absent"
   ],
   "connectors/chat-bridge.test.ts": [
    "makes capability approval buttons with bot namespace, short code, and nonce",
    "makes risky-tool approval buttons with bot namespace, short code, and nonce",
    "returns the model reply for a plain chat message",
    "🔴 serializes turns per session — a second turn never overlaps the first",
    "different sessions run concurrently (no cross-session head-of-line blocking)",
    "a provider failure resolves to a non-empty error string, never rejects (bot must reply)",
    "🔴 model-driven RISKY tool → parked durably + inner NOT executed (dispatcher chokepoint)",
    "model-driven READ-ONLY tool → inner executes (grounded), nothing parked",
    "returns a single chunk when under the limit",
    "splits oversized text into chunks each within the limit",
    "hard-splits a single oversized line with no newline boundary"
   ],
   "connectors/connector-bootstrap-gate-e2e.test.ts": [
    "authorized operator runs the capability; known viewer is denied; unknown sender is dropped",
    "REGRESSION (escaped bug): a bound GROUP whose chat_id ≠ notify chat_id is admitted — not dropped by the per-channel gate",
    "opt-out: identity absent → no binding seeded, sender flows to chat with NO principal (gate no-op)"
   ],
   "connectors/connector-bootstrap-identity.test.ts": [
    "resolves a stored sender to a principal within the bound tenant",
    "returns null for an unknown sender on a tenant-locked binding (fail-closed)"
   ],
   "connectors/connector-bootstrap-requester-principal.test.ts": [
    "stores the per-action requester principal; a later park by A does NOT overwrite B",
    "omits requesterPrincipal when none is supplied (identity-disabled path byte-for-byte unchanged)",
    "the principal passed to the chat turn is carried onto the parked action",
    "no principal (identity disabled) → parked action has no requesterPrincipal (unchanged)"
   ],
   "connectors/connector-bootstrap.test.ts": [
    "starts (outbound) an enabled connector and returns its target + chat_id",
    "skips a disabled connector",
    "skips an unresolved $DECK: token (no real send attempted) — fail-safe",
    "skips a connector whose startOutbound throws — fail-safe, no crash",
    "returns empty for undefined config",
    "routes a versioned bot callback as the correct synthetic command",
    "stubs a broker callback with an i18n reply and never reaches the gate resolver",
    "injects a broker decider and sends its typed reply through the existing transport",
    "keeps routing a legacy callback with its original id",
    "silently ignores an invalid callback and emits only a debug log"
   ],
   "connectors/connector-commands-bootstrap.test.ts": [
    "starts the connector INBOUND (full start) and registers a command handler",
    "authorized \"approve <id>\" → resolve called + ack replied via the same connector",
    "unauthorized sender → resolve NOT called, no reply",
    "returns a NotificationAdapter over the SAME instance (one instance, both directions)",
    "dispose() stops every started connector",
    "authorized non-command → chat responder called + reply chunks sent (full conversation)",
    "/help → curated bot help, chat responder NOT called (no CLI leak)",
    "/status slash → read-only dispatcher, NOT the chat engine",
    "unauthorized non-command → chat responder NOT called (RCE chokepoint)",
    "approve <id> of a parked bot-action → executes it + replies the result (slice 2b)",
    "approve a parked bot-action TWICE → executes once (idempotent, consume-once)",
    "reject <id> of a parked bot-action → NOT executed, discarded",
    "🔴 approve a kill BOUND to a sprint that is no longer active → REFUSED, kill not run",
    "approve a kill bound to the ACTIVE sprint → routes to killSprintById (not the generic dispatcher)",
    "🔴 approve an EXPIRED parked action → REFUSED, not executed",
    "chat reply with Markdown → sent as Telegram HTML (parseMode HTML, bold rendered)",
    "chat reply HTML send throws → plain fallback fires (no crash, no lost reply)",
    "unresolved $DECK token → skipped, nothing started, adapter null",
    "streaming path: connector with 3 caps + onChatStreaming → editMessage called, sendMessage NOT called for reply"
   ],
   "connectors/connector-notify-adapter.test.ts": [
    "renders callbackData actions as inline buttons on the message",
    "omits buttons when actions carry no callbackData (text-only fallback)",
    "sends to each connector at its own chat id, with the notification text",
    "renders actionable commands (approve/reject short codes) into the sent text",
    "no actions → no command footer; Telegram gets HTML rich text + parse_mode",
    "non-Telegram connectors keep plain text (no HTML tags, no parse_mode)",
    "HTML-escapes dynamic content in the Telegram rich format",
    "isAvailable reflects whether any target is configured",
    "fail-safe: a throwing connector is swallowed; other connectors still receive",
    "fail-safe: a hanging connector is timed out, not awaited forever",
    "close stops every connector exactly once and makes later sends a no-op",
    "close attempts every stop and propagates shutdown failure",
    "lossless: a notification over the Telegram limit is split, never cut"
   ],
   "connectors/connector-pool.test.ts": [
    "should register connectors and broadcast in parallel",
    "should handle partial failure — failed connector does not block others",
    "should return error for not-registered connector",
    "should manage startAll and stopAll lifecycle",
    "should fan out onAnyMessage to all registered connectors",
    "should return empty results for empty targets",
    "should handle startAll with connector start failure gracefully",
    "should support get, has, and getAll accessors",
    "sends to ALL registered connectors without explicit targets",
    "returns empty array when no connectors are registered",
    "per-connector error isolation: a failing connector does not block others"
   ],
   "connectors/discord.test.ts": [
    "starts with token and logs in successfully",
    "skips start when disabled",
    "triggers incoming message handler on MessageCreate",
    "filters bot messages",
    "sends message to channel",
    "destroys client on stop",
    "reports healthy when WebSocket status is READY (0)",
    "throws on sendMessage when not started",
    "throws instead of silently dropping when the channel cannot be resolved (R6)",
    "throws instead of silently dropping when the channel is not text-based (R6)"
   ],
   "connectors/email-nodemailer-major.test.ts": [
    "nodemailer is referenced in exactly the known usage surface (mail-transport.ts) — no undiscovered call sites",
    "createTransport() is only called with fields unaffected by any 7.x/8.x/9.x breaking change",
    "the MailMessage contract never carries nodemailer\\'s raw option or a remote-URL attachment",
    "send_mail capability only builds attachments from the local artifact store, never a remote URL",
    "no code path matches nodemailer\\'s legacy \\'NoAuth\\' error-code string (renamed to ENOAUTH in 8.0.0)",
    "no SES transport usage anywhere under src/connectors (7.0.0 removed the legacy SES SDK)"
   ],
   "connectors/gateway/gateway-access-bindings.test.ts": [
    "returns null for an unbound channel",
    "persists and reads back a binding",
    "survives a reload from disk"
   ],
   "connectors/gateway/gateway-access-lifecycle.test.ts": [
    "persists opaque identity, distinct alias, exact scope and resolved 10m critical snapshot privately",
    "uses one parser for the legacy array and production object-map, quarantining unsafe legacy decisions",
    "fresh-reads under one CAS: a CLI-like peer grant is visible and a racing loser observes closure",
    "expires at the immutable 10m boundary and a late approve never grants access",
    "blocks new requests when lifecycle is disabled and rejects mismatched approval scope"
   ],
   "connectors/gateway/gateway-access.test.ts": [
    "denies by default, authorizes, persists across reload",
    "pairing: request → approve moves chat onto the project allowlist",
    "requestPairing reuses the pending code for the same chat",
    "rejectPairing removes a pending pairing"
   ],
   "connectors/gateway/gateway-daemon-access-reload.test.ts": [
    "observes a CLI-process approval from disk without daemon restart",
    "resolves explicit tenant scope and refuses synthesis under strict isolation"
   ],
   "connectors/gateway/gateway-ipc.test.ts": [
    "encodes one frame per line",
    "decodes complete frames and keeps a trailing partial line",
    "skips malformed lines without throwing"
   ],
   "connectors/gateway/gateway-paths.test.ts": [
    "honors DECKENT_GATEWAY_HOME override",
    "falls back to ~/.deckent/gateway when unset"
   ],
   "connectors/gateway/gateway-router-lifecycle.test.ts": [
    "passes the resolved tenant and canonical project scope to pairing creation",
    "fails closed without tenant lifecycle authority and never creates an unscoped pairing"
   ],
   "connectors/gateway/gateway-router.test.ts": [
    "guides an unbound chat instead of routing",
    "/use binds the chat to a project",
    "routes a natural-language message to the bound runtime",
    "drops a message from an unauthorized chat",
    "/use by an unauthorized chat returns a pairing code and does NOT bind"
   ],
   "connectors/gateway/gateway-runtime.test.ts": [
    "answers a message request with a final response frame"
   ],
   "connectors/gateway/pairing-no-access-grant.integration.test.ts": [
    "keeps parser, inbox, child-process timeout/late decision and daemon on one terminal truth"
   ],
   "connectors/gateway/project-registry.test.ts": [
    "adds and resolves by name OR path",
    "persists across reloads and dedupes by name"
   ],
   "connectors/gateway/runtime-supervisor.test.ts": [
    "spawns once per project and round-trips a request",
    "strips ANTHROPIC_API_KEY from the spawned child env",
    "respawns after the child exits"
   ],
   "connectors/gateway/session-registry.test.ts": [
    "binds, resolves, persists, and reloads",
    "unbinds",
    "treats a corrupt file as empty (fail-safe)",
    "writes atomically (no leftover temp file in the json)"
   ],
   "connectors/identity/factory-faz3.test.ts": [
    "creates a ScimIdentityProvider for kind=\"scim\"",
    "creates an OidcClaimsIdentityProvider for kind=\"oidc-claims\"",
    "local path remains backward-compatible",
    "throws E_UNKNOWN_IDENTITY_PROVIDER for unknown kind (csv = Faz-2)",
    "scim provider resolve() is fail-closed for unknown sender (no network)",
    "oidc-claims provider resolve() is fail-closed for unknown sender"
   ],
   "connectors/identity/identity-store.test.ts": [
    "upserts and reads back an identity",
    "returns null for a different tenant (tenant isolation)",
    "upsert overwrites (idempotent on PK)",
    "deletes an identity",
    "exports and re-imports a bundle round-trip",
    "does not collide cache keys across segment boundaries",
    "stores and reads a pending-verify with attempts"
   ],
   "connectors/identity/local-provider.test.ts": [
    "resolves the owner as admin without a store record",
    "resolves a stored identity with role-map permissions",
    "returns null for an unknown sender (fail-closed)",
    "factory builds a local provider",
    "factory throws on unknown provider kind",
    "owner shortcut always returns full permissions even when roleMap narrows admin"
   ],
   "connectors/identity/oidc-claims-provider.test.ts": [
    "maps operator group to operator role with explicit permissions",
    "maps admin group to admin role",
    "maps viewer group to viewer role",
    "uses tid claim as tenantId",
    "maps operator group via tenantFallback (no tid claim)",
    "maps admin group correctly",
    "picks the highest-privilege role when a user is in multiple mapped groups",
    "ignores unmapped groups and picks the best match from the rest",
    "returns null when email claim is missing",
    "returns null when email is not a valid email string",
    "returns null when tenantId claim is absent and no tenantFallback provided",
    "returns null when groups claim is absent",
    "returns null when groups is an empty array",
    "returns null when no group matches the roleMap",
    "returns null when groups is not an array (scalar)",
    "returns null when roleMap is empty (no whitelist entries)",
    "uses roleClaim when groupsClaim is absent",
    "uses roleClaim when groups array has no matching entries",
    "uses groupsClaim result over roleClaim (groups take precedence)",
    "returns null when roleClaim value is not in the roleMap (no raw role bypass)",
    "supports custom roleClaim key via OidcClaimsConfig",
    "reads email from a custom emailClaim key",
    "exposes oidc-claims id and enterprise edition by default",
    "resolve() returns null for an unknown sender (fail-closed, no network)",
    "resolve() returns the stored principal for a previously bound OIDC identity",
    "resolve() is tenant-scoped — same externalId in a different tenant returns null",
    "accepts a custom edition"
   ],
   "connectors/identity/principal-resolver.test.ts": [
    "maps connectors to their native ref kind",
    "resolves a known sender within the binding tenant",
    "returns null for an unknown sender with no guest role (fail-closed)",
    "returns a guest principal when binding.guestRole is set"
   ],
   "connectors/identity/role-map.test.ts": [
    "falls back to built-in defaults when no role-map",
    "uses role-map entry by role key",
    "prefers explicit groupKey entry over role entry",
    "DEFAULT_ROLE_PERMISSIONS covers all three roles"
   ],
   "connectors/identity/scim-provider.test.ts": [
    "exposes scim id and enterprise edition by default",
    "sync() upserts every active user and maps external groups → deckent roles",
    "picks the highest-privilege role when a user is in multiple mapped groups",
    "resolve() is pure-local — never calls fetch and reads only the store",
    "resolve() is fail-closed for an unknown sender (null, no network)",
    "paginates SCIM /Users across multiple pages (startIndex/count)",
    "a sync failure leaves the existing store untouched",
    "a non-2xx response throws E_SCIM_SYNC without corrupting the store",
    "deprovisions directory users removed from the IdP, keeping non-directory records",
    "treats active:false users as deprovisioned"
   ],
   "connectors/identity/verify-bind.test.ts": [
    "rejects an invalid email at start",
    "happy path: start then confirm binds a verified identity",
    "rejects an expired code",
    "rejects a wrong code and counts attempts, then locks out",
    "rejects confirm with nothing pending",
    "rejects confirm from a different tenant and clears the pending row (tenant-mismatch)"
   ],
   "connectors/identity-e2e-smoke.test.ts": [
    "authorized sender (operator with order:write) → capability RUNS",
    "unauthorized sender (viewer, order:read only) → capability DENIED"
   ],
   "connectors/identity-faz3-e2e.test.ts": [
    "T1: scim sync (mock-fetch) fills the store → inbound resolves per-user → real gate ALLOW/DENY",
    "T2: role-map groupKey is LIVE — a viewer in an order-admins group is granted order:write",
    "T3: sync failure is fail-safe — connector keeps serving, unknown senders fail closed",
    "T5: scim kind configured WITHOUT a provider seam → identity disabled, NOT silently downgraded to local",
    "T4: local provider has no sync() → triggerBackgroundSync is a no-op (disabled/local path unchanged)"
   ],
   "connectors/inbound-media.test.ts": [
    "photo message: getFileBuffer called, chat receives [attached: id, photo.jpg] prefix",
    "document message: getFileBuffer called with doc fileId, [attached] includes filename",
    "Turkish lang: uses [ek: id, filename] prefix",
    "message without raw.media passes text through unchanged to chat",
    "getFileBuffer missing on connector: message still reaches chat (graceful degrade)"
   ],
   "connectors/incoming-approval-lifecycle.test.ts": [
    "returns not-found for a closed row and persists only system timeout authority",
    "threads lifecycle and clock through the connector bootstrap default resolver"
   ],
   "connectors/incoming-command-resolver.test.ts": [
    "approve an owned trigger → resolved ack carries WHAT was approved + decisions.json records approved",
    "reject an owned trigger → resolved + decisions.json records rejected",
    "fails closed on a replay after the first durable resolution",
    "localizes the ack (TR)",
    "real disk: nervous id → resolved ack shows the title + an IPC pending file is written",
    "injected: routes to writeNervousApproval with the full id + action",
    "short code: `approve <shortCode>` resolves the notification and writes the FULL id",
    "short code is case-insensitive (operator may type it any case)",
    "ambiguous persisted short code is rejected with typed candidates and never written",
    "autonomous owns the id → nervous is NOT consulted",
    "neither gate owns the id → not-found"
   ],
   "connectors/incoming-command-router-identity.test.ts": [
    "forwards channelId, text, and the full IncomingMessage (incl. fromUser)",
    "still drops unauthorized senders (gate unchanged)"
   ],
   "connectors/incoming-command-router.test.ts": [
    "parses \"approve <id>\" and \"reject <id>\"",
    "is case-insensitive and tolerates a leading slash + extra whitespace",
    "accepts \"accept <id>\" as an alias for approve",
    "normalizes short y/n verbs to approve/reject",
    "default-denies anything that is not exactly verb + one id",
    "🔴 unauthorized sender + valid command → resolver NOT called, no reply (silent)",
    "authorized \"approve <id>\" → resolve(id,\"approve\") + success ack",
    "authorized \"reject <id>\" → resolve(id,\"reject\")",
    "authorized y/n commands dispatch their normalized actions",
    "resolver returns \"not-found\" → authorized sender still gets a (distinct) ack",
    "authorized chatter (non-command) → resolver not called, silent (no reply spam)",
    "a throwing resolver never escapes the handler (fail-safe)",
    "a throwing resolver sends a failure reply instead of silently swallowing (R6)",
    "a resolver throw with a reply that also throws still never escapes the handler",
    "🔴 a stale message (timestamp before acceptFrom) is DROPPED — no resolve, no chat, no reply",
    "a fresh message (timestamp at/after acceptFrom) is processed normally",
    "no acceptFrom configured → no age filtering (back-compat)",
    "authorized non-command → onChat(channelId, text), resolve NOT called",
    "🔴 UNAUTHORIZED non-command → onChat NOT called (chat inherits the same auth chokepoint)",
    "a command still routes to resolve, NOT onChat",
    "no onChat configured → authorized non-command is silently ignored (back-compat)",
    "a throwing onChat never escapes the handler (fail-safe)"
   ],
   "connectors/incoming-router.test.ts": [
    "should emit event on eventBus when routing a message",
    "should include connectorId, fromUser, and text in event payload",
    "should emit separate events for multiple messages",
    "should return true for matching keys",
    "should return false for mismatched keys",
    "should return false for empty provided key",
    "should return false for empty expected key",
    "should return false for different length keys",
    "should parse valid Discord webhook payload",
    "should return null for missing content",
    "should return null for null/undefined payload",
    "should parse valid Telegram webhook payload",
    "should return null for missing message field",
    "should return null for missing text",
    "should dispatch to Discord parser for discord connector",
    "should dispatch to Telegram parser for telegram connector",
    "should use generic parser for unknown connectors",
    "should return null for invalid payload",
    "should accept valid connector ids",
    "should reject invalid connector ids"
   ],
   "connectors/kpi-sprint-summary.test.ts": [
    "en: contains sprint id and all four headline KPI numeric values",
    "en: uses English KPI labels from definition.title.en",
    "tr: uses Turkish KPI labels and title",
    "tr: uses Turkish i18n title via kpi.title getMessage key",
    "en: uses English i18n title via kpi.title getMessage key",
    "empty views → returns kpi.no_data message (no crash)",
    "empty views tr → returns Turkish no_data message",
    "views without any headline KPIs → returns no_data (no crash)",
    "result=null view → shows dash for the null value (no crash)",
    "warning status → status line with warning indicator",
    "is deterministic: same input → same output",
    "sends KPI summary as a follow-up message on sprint-finalized",
    "does NOT call kpiSummaryFn for non sprint-finalized events",
    "non-blocking: a throwing kpiSummaryFn does NOT break the notify path",
    "non-blocking: a kpiSummaryFn returning null sends no follow-up",
    "hook does not break without kpiSummaryFn option (opt-in)"
   ],
   "connectors/kpi-summary-dispatch.test.ts": [
    "formats a seeded finalized sprint (en): non-empty, names a KPI, closes the store",
    "reuses buildKpiSprintSummary i18n — renders Turkish titles when lang=tr",
    "returns null for an unknown sprint (no data) without throwing, still closing the store",
    "returns null (no throw) when the .brain/memory.db is missing",
    "returns null when no connectors are configured (byte-for-byte default no-op)",
    "forwards kpiSummaryFn: broadcasts the KPI summary to the connector on sprint-finalized",
    "does not broadcast a summary when the sprint has no KPI data (kpiSummaryFn → null)",
    "closes earlier started connectors when a later connector fails to start"
   ],
   "connectors/markdown-to-html.test.ts": [
    "renders bold, italic, strikethrough",
    "renders inline code + code blocks without inner formatting, escaping inside",
    "escapes HTML-special chars in plain text (no injection)",
    "renders links and escapes the url/text",
    "converts headings to bold and list bullets",
    "leaves unbalanced markdown as escaped literal text (no broken tags)",
    "handles a realistic mixed reply",
    "does not corrupt when model text contains a code-placeholder-like pattern",
    "does not linkify non-http(s)/tg schemes (javascript: etc.)"
   ],
   "connectors/notification-delivery.test.ts": [
    "keeps append-only events pending while the bot is offline",
    "acknowledges after send and does not redeliver acknowledged ids",
    "retries one transient failure with the same idempotency key",
    "does not retry client errors and leaves the ordered suffix pending",
    "delivers the pending records and writes the ack receipt in one tick",
    "fires the drain on the configured interval and never after stop",
    "skips with a typed log and never sends when the bot is disabled",
    "skips with a typed log when no connector is configured",
    "leaves the record pending — and writes no receipt — when the send fails"
   ],
   "connectors/perturn-connector.test.ts": [
    "round-trips approvalMessageId through park/take"
   ],
   "connectors/stream-throttle.test.ts": [
    "coalesces rapid pushes and always flushes the final text"
   ],
   "connectors/telegram-getfile.test.ts": [
    "returns buffer + mime for a JPEG file (extension → image/jpeg)",
    "builds the correct download URL from the stored token",
    "throws when fetch returns a non-ok HTTP status (e.g. 404)",
    "handles PNG extension → image/png mime",
    "falls back to application/octet-stream for unknown extension",
    "throws when bot is not started"
   ],
   "connectors/telegram-sendmedia.test.ts": [
    "photo → sendPhoto(channelId, InputFile(data,filename), {caption})",
    "document → sendDocument(channelId, InputFile(data,filename), {caption})"
   ],
   "connectors/telegram-streaming.test.ts": [
    "sendMessageReturningId returns the message_id; editMessage + sendChatAction call bot.api"
   ],
   "connectors/telegram-voice.test.ts": [
    "calls api.sendVoice(channelId, InputFile(data))",
    "throws if bot not started",
    "emits IncomingMessage with raw.voice.fileId + mime",
    "falls back to audio/ogg when mime_type is missing"
   ],
   "connectors/telegram.test.ts": [
    "start with token — bot launched, text handler registered",
    "start does NOT await bot.start() — resolves even though long-poll start never settles (BOT-002)",
    "start disabled — no-op, bot not created",
    "incoming text handler — message emitted to registered handlers",
    "sendMessage — delegates to bot.api.sendMessage",
    "sendMessage with buttons — renders reply_markup.inline_keyboard with callback_data",
    "sendMessage with parseMode — passes parse_mode (rich text), combinable with buttons",
    "callback_query:data press — forwards callback_data to onCallback and acks the press",
    "stop — bot stopped, connector becomes unhealthy",
    "sendMessage before start — throws error"
   ],
   "connectors/voice/health.test.ts": [
    "returns explicit health_url when set (wins over derivation)",
    "derives health URL from stt_url origin + /health",
    "derives health URL from tts_url origin + /health when stt_url absent",
    "returns null when neither stt_url nor tts_url present",
    "returns null when local object is empty",
    "returns ok:true with provider:none and does NOT call fetch",
    "returns ok:true with provider:none when enabled is absent",
    "returns ok:true when health endpoint responds 200",
    "returns ok:false + detail when health endpoint responds 503",
    "returns ok:false + detail when fetch throws (network error)",
    "returns ok:true + provider:local when health_url is null (no url configured)",
    "resolves ok:false with timeout detail and does NOT hang when fetch never responds",
    "returns ok:true when OPENAI_API_KEY is present in deck",
    "returns ok:false + detail when OPENAI_API_KEY is absent",
    "returns ok:false + detail when OPENAI_API_KEY is empty string"
   ],
   "connectors/voice/language.test.ts": [
    "returns forced cfg.language when it is a concrete tag (config wins over turnLang)",
    "returns forced cfg.language even when turnLang is absent",
    "treats cfg.language \"Auto\" (capital) as auto — falls through to turnLang",
    "treats cfg.language \"Auto\" (capital) as auto — mirror when no turnLang",
    "trims whitespace from cfg.language \" tr \" and treats as forced tag",
    "mirrors turnLang when cfg.language is \"auto\" and turnLang is present",
    "mirrors turnLang when cfg.language is \"auto\" and turnLang is \"tr\"",
    "returns mirror mode when cfg.language is \"auto\" and no turnLang",
    "mirrors turnLang when cfg.language is absent and turnLang is present",
    "returns mirror mode when cfg.language is absent and no turnLang",
    "resolves correctly even when voice is disabled (pure — no side effects)",
    "resolves mirror when disabled and no turnLang"
   ],
   "connectors/voice/local-voice.test.ts": [
    "returns { text, language } from a stubbed {text, language} STT response",
    "appends ?language=<hint> to stt_url when stt_language is set",
    "uses URL API — no double-? when stt_url already has a query string",
    "does NOT append ?language= when stt_language is absent (auto-detect)",
    "throws when stt endpoint returns non-ok status",
    "POSTs {text, voice} to tts_url and returns audio bytes + content-type",
    "falls back to tts_voice config when no opts.voice supplied",
    "throws when tts endpoint returns non-ok status",
    "uses audio/wav as fallback mime when content-type header is absent",
    "includes \"language\" in POST body when opts.language is provided",
    "omits \"language\" key from POST body when opts.language is absent",
    "returns null when enabled is false",
    "returns null when local urls are missing",
    "returns null when local block is absent",
    "returns a VoiceAdapter when local is properly configured"
   ],
   "connectors/voice/modality.test.ts": [
    "ttsMode=off, voiceOrigin=false → text, overridden=false",
    "ttsMode=off, voiceOrigin=true → text, overridden=false",
    "ttsMode=always, voiceOrigin=false → voice, overridden=false",
    "ttsMode=always, voiceOrigin=true → voice, overridden=false",
    "ttsMode=reply-in-kind, voiceOrigin=true → voice, overridden=false",
    "ttsMode=reply-in-kind, voiceOrigin=false → text, overridden=false",
    "empty text, ttsMode=off → text, overridden=false",
    "\"bunu bana sesli anlat\" + reply-in-kind + voiceOrigin=false → voice, overridden=true",
    "\"sesli cevap ver\" + ttsMode=off → voice, overridden=true (off beaten)",
    "\"sesli yanıt ver\" + ttsMode=off → voice, overridden=true",
    "\"sesli söyle\" + ttsMode=off → voice, overridden=true",
    "\"ses olarak\" + ttsMode=off → voice, overridden=true",
    "\"bana oku\" + ttsMode=off → voice, overridden=true",
    "\"sesli olarak\" + ttsMode=off → voice, overridden=true",
    "\"reply by voice\" + ttsMode=off → voice, overridden=true",
    "\"in voice\" + ttsMode=off → voice, overridden=true",
    "\"read it aloud\" + ttsMode=off → voice, overridden=true",
    "\"say it aloud\" + ttsMode=off → voice, overridden=true",
    "\"say it aloud\" + ttsMode=reply-in-kind + voiceOrigin=false → voice, overridden=true",
    "\"please say it\" (bare, no aloud) → NOT overridden — falls back to default",
    "\"can you say it again\" → NOT overridden (UX-footgun guard)",
    "\"say italy\" must NOT trigger voice override — no \"say it aloud\" match",
    "case-insensitive: \"SESLİ CEVAP\" + ttsMode=off → voice, overridden=true",
    "\"ekran görüntüsü al ve bana yaz\" + reply-in-kind + voiceOrigin=true → text, overridden=true",
    "\"yazarak cevap ver\" + ttsMode=always → text, overridden=true (always beaten)",
    "\"yazılı cevap\" + ttsMode=always → text, overridden=true",
    "\"metin olarak\" + ttsMode=always → text, overridden=true",
    "\"yazıyla\" + ttsMode=always → text, overridden=true",
    "\"reply in text\" + ttsMode=always → text, overridden=true",
    "\"in text\" + ttsMode=always → text, overridden=true",
    "\"as text\" + ttsMode=always → text, overridden=true",
    "\"write it\" + ttsMode=always → text, overridden=true",
    "bare \"yaz\" as word boundary + ttsMode=always → text, overridden=true",
    "case-insensitive: \"YAZARAK\" + ttsMode=always → text, overridden=true",
    "\"bana yaz\" (standalone) + ttsMode=always → text, overridden=true (leading boundary)",
    "\"Xbana yaz\" (contrived leading-letter glue) → text still via bare \"yaz\" pattern",
    "\"yazılım güncellemesi yap\" — \"yazılım\" must NOT trigger text override (reply-in-kind, voiceOrigin=false)",
    "\"yazıyorum\" must NOT trigger override — word-boundary on bare \"yaz\"",
    "\"yazılımcı\" must NOT trigger text override",
    "\"ses\" alone in a sentence does NOT trigger voice override",
    "\"seslemek\" must NOT trigger voice override",
    "\"voice\" alone in a sentence does NOT trigger voice override",
    "\"text\" alone does NOT trigger text override",
    "text phrase last → text wins (last-occurring wins)",
    "voice phrase last → voice wins (last-occurring wins)"
   ],
   "connectors/voice/openai-voice.test.ts": [
    "calls openai.audio.transcriptions.create with verbose_json and returns { text, language }",
    "returns { text, language } — language may be undefined when not in response",
    "calls openai.audio.speech.create and returns buffer + mime",
    "uses default voice when none supplied",
    "returns null when OPENAI_API_KEY is absent",
    "returns null when OPENAI_API_KEY is empty string",
    "transcribe(buf, audio/ogg) → \"hi\": file-shim name set, no undefined iterator",
    "returns null when enabled is false",
    "returns null when OPENAI_API_KEY is absent from deck",
    "returns a VoiceAdapter (lazy) when key is present — does NOT attempt to load openai in sync path"
   ],
   "connectors/voice-bodyinit.test.ts": [
    "forwards the exact same Buffer reference as fetch body (no copy)",
    "sends identical bytes for binary audio payloads (including 0x00 / 0xff edge bytes)",
    "does not mutate the Buffer content-type/byteLength when handed to fetch"
   ],
   "connectors/voice-wire.test.ts": [
    "voice message: getFileBuffer called, transcribe called, chat receives transcribed text",
    "voice + stt=false → no getFileBuffer, no transcribe, text passed unchanged",
    "voiceAdapter absent → voice message passes through (no download, backward-compat)",
    "transcribe failure → fall back to text path, no crash, no sendVoice",
    "reply-in-kind + voice-origin → sendVoice called, no text sendMessage for reply body",
    "tts=always + voice-origin → sendVoice called",
    "tts=always + text-origin → sendVoice called AND text reply NOT sent",
    "tts=always + voice-origin → sendVoice called AND text reply NOT sent",
    "tts=off → never sendVoice even for voice-origin",
    "synthesize failure → fall back to text reply, no crash",
    "sendVoice failure → fall back to text reply, no crash",
    "inbound voice with language=\"tr\" → chat receives detectedLang=\"tr\" as 4th arg",
    "inbound voice with language=undefined → chat receives detectedLang=undefined (no injection)",
    "text-origin turn → detectedLang not set (pendingVoiceLang map stays clean)",
    "forced-tr: voice detects tr → chat text has forced-tr instruction prepended + synthesize called with language:tr",
    "forced-tr: config language=tr + no detectedLang → chat text has forced instruction + synthesize language:tr",
    "mirror: no detectedLang AND no config language → mirror instruction prepended + synthesize language:undefined",
    "default-off: no voice config → no instruction prepended (text-origin, backward-compat)",
    "voice-enabled turn → turnText contains voice-capability instruction marker",
    "voice-enabled text-origin turn → turnText contains both preamble instructions",
    "default-off: no voiceCfg → turnText === original text, byte-identical",
    "voice-origin + reply-in-kind + \"bana yaz\" in transcript → text reply, sendVoice NOT called",
    "text-origin + reply-in-kind + \"sesli cevap ver\" → sendVoice called",
    "regression: voice-origin + reply-in-kind + no phrase → still voice (default unchanged)",
    "modality-override=voice but synthesize throws → honest text fallback, no crash",
    "streaming + reply-in-kind + voice-origin → sendVoice called, NO streamed text, NO sendMessage for reply body",
    "streaming + reply-in-kind + voice-origin + synthesize failure → text reply sent as fallback (no crash)"
   ],
   "connectors/whatsapp-bootstrap.test.ts": [
    "selects whatsapp connector when enabled in config",
    "skips whatsapp when disabled",
    "skips whatsapp with unresolved $DECK: token — fail-safe",
    "skips whatsapp whose startOutbound throws — fail-safe, no crash",
    "whatsapp and telegram can run simultaneously"
   ],
   "connectors/whatsapp-scaffold.test.ts": [
    "disabled start → no-op (connector stays stopped)",
    "enabled start → throws activation error with guidance",
    "isHealthy → always false in scaffold mode",
    "sendMessage → throws not-activated error",
    "stop → no-op (nothing to clean up in scaffold mode)",
    "connector identity — correct id and name"
   ]
  },
  "core": {
   "core/acceptance-confirmation-contract.test.ts": [
    "pins canonical JSON and deterministic confirmation identity to every lineage byte",
    "creates and independently verifies a digest-bound terminal event",
    "defines the only receipt lifecycle as immutable PREPARED then APPLIED evidence",
    "strictly rejects unknown fields, lossy identifiers, non-UTC time, and uppercase digests",
    "rejects receipt tampering and timestamp regression",
    "rejects host-dependent or non-JSON canonicalization hooks",
    "round-trips only the full v2 lineage and fails closed on omission or downgrade",
    "digests exact source bytes without whitespace or encoding normalization"
   ],
   "core/acceptance-confirmation-race-scale.integration.test.ts": [
    "isolates tenants that reuse every caller-controlled confirmation identifier",
    "fails closed on a corrupt durable receipt and never converts it into replay authority",
    "measures 10k canonical rows within the host-class budget and a digest-stable replay"
   ],
   "core/acceptance-decision-authority.test.ts": [
    "indexes an exact FWW binding and verifies it after a restart-style fresh read",
    "fails closed for corruption, foreign lineage, mismatch, and replay substitution"
   ],
   "core/acceptance-exact-decision-authority.test.ts": [
    "publishes and freshly verifies one exact lineage with real closed usage evidence",
    "requires an independent fresh binding on every factory verification, never its own stored expected value",
    "cannot rebind a genuine CONFIRMED receipt to changed %s",
    "rejects %s output behind a genuine but misleading CONFIRMED receipt",
    "rejects fresh %s corruption after a valid first decision",
    "rejects immutable-source omission on capture replay and reads exact envelope bytes",
    "cannot add immutable custody authority to a legacy project-file claim on replay"
   ],
   "core/acceptance-matrix.test.ts": [
    "derives the policy vocabulary from the canonical verdict authority, excluding HOLD",
    "covers every canonical kind with the base row; security is stricter",
    "resolves default vs override with per-cell source attribution",
    "drops invalid override rules with typed reasons — never silently",
    "stamps kind × verdict policy on the persisted record (security → human route)",
    "leaves procedural evaluations unstamped (DEFERRED → HOLD, outside the policy)"
   ],
   "core/acceptance-reconciliation-store.test.ts": [
    "does not create missing storage",
    "reads durable authority without publication while writer-open=%s",
    "appends PREPARED/APPLIED atomically and replays after restart while holding conflicts",
    "rolls a conflicting transaction batch back completely",
    "commits a receipt with no mutable pending-digest phase",
    "uses an indexed tenant/project keyset cursor",
    "checks integrity and returns corruption in a quarantine view rather than as missing",
    "adopts legacy source bytes losslessly and idempotently without deletion",
    "adopts a valid legacy receipt and archives its exact source bytes once",
    "uses SQLite durability on win32 without relying on unsupported file fsync",
    "closes idempotently and keeps committed authority restart-readable",
    "rejects APPLIED without PREPARED and keeps the receipt projection empty"
   ],
   "core/acceptance-settlement.test.ts": [
    "resolves only routed UNDECIDABLE with authenticated applied non-expired acceptance",
    "pins the complete 4 verdict x 3 action x 6 evidence truth table with typed reasons",
    "pins every source/matrix mismatch and never rewrites source truth",
    "preserves the typed authentication conflict and does not mutate its input"
   ],
   "core/activation-engine.test.ts": [
    "scores matching rules",
    "returns 0 for non-matching rules",
    "excludes when exclusion rule matches",
    "does not exclude when exclusion rule does not match",
    "accumulates scores from multiple matching rules",
    "handles empty config gracefully",
    "default-off: omitting the affinity context leaves the score unchanged",
    "default-off: enabled=false leaves the score unchanged (no affinity applied)",
    "enabled + matching skill→agent: adds SKILL_AGENT_AFFINITY_BONUS to the score",
    "enabled but agent is NOT the affinity target (refactorer): no bonus, no penalty",
    "caps at one application even when multiple assigned skills map to the agent",
    "enabled with empty / undefined assigned skills: no bonus",
    "affinity never resurrects an excluded agent (exclusion wins, score stays 0)",
    "matches simple intent condition",
    "does not match when condition fails",
    "matches complex multi-field condition",
    "returns true when exclusion matches",
    "returns false when exclusion does not match",
    "supports complex exclusion conditions",
    "converts security keywords to security intent rules",
    "converts scope paths to domain rules",
    "handles empty inputs",
    "converts language skill triggers",
    "converts domain skill triggers",
    "adds stack detection bonus rule",
    "returns 50% score when rule matches via secondary intent",
    "returns 0 when value not in secondary intents",
    "returns 0 for non-string primary conditions",
    "returns 0 when intent already matches primary (no double-counting)",
    "evaluateActivation scores via secondary when exclude does not match",
    "evaluateActivation excludes before secondary scoring for implementation intent"
   ],
   "core/active-workers-pending.test.ts": [
    "markPending adds taskId to active list even without .hb file",
    "markActive transitions taskId out of pending Set (.hb is now authoritative)",
    "getActiveWorkerIds returns deduped union(pending, fromHeartbeats)",
    "clearPending removes a pending taskId (spawn-fail recovery path)"
   ],
   "core/active-workers.test.ts": [
    "returns taskIds from .hb files",
    "returns empty array if no .hb files",
    "handles malformed .hb files gracefully",
    "returns empty array if .tasks directory does not exist"
   ],
   "core/adr-046-step-ordering-invariant.test.ts": [
    "Step 3 → Step 4: freshly inserted ADR appears in regenerated rules",
    "does not contain ADR before insert (sanity check)",
    "multiple ADRs inserted in sequence all visible in one regen"
   ],
   "core/adr-constraints.test.ts": [
    "table stays deliberately small (amendment: revisit selection past ~10)",
    "flags the exact 440-001 demand: tie-break making intent.primary literal 'test'",
    "flags a spawnSync demand (ADR-D-002 family)",
    "silent on ADR-compliant specs (test-coverage tag wording)",
    "zero-config planner prompt includes the constraints block",
    "brain plan prompt includes the constraints block",
    "block renders one line per constraint"
   ],
   "core/adr-file-sync.test.ts": [
    "parses a valid MADR v3 file with title, status, sprint",
    "returns null for a file missing the H1 ADR title",
    "returns null for a file missing **Status:**",
    "returns null for a filename not matching NNN-*.md",
    "extracts sprint_id from inline `Sprint NNN` when **Sprint:** missing",
    "parses canonical taxonomy and discrete enforcement metadata",
    "marks `accepted` ADRs as decay_exempt",
    "does not mark `proposed` ADRs as decay_exempt",
    "happy path — inserts 3 valid ADRs and returns inserted=3",
    "idempotent — second sync with identical content yields 0 inserted/0 updated",
    "malformed skip — counts skipped + records error without aborting run",
    "status transition — updates existing entry when status changes",
    "sprint_id extraction — `**Sprint:** Sprint 163` becomes sprint-163 on DB row",
    "updates taxonomy fields even when title/content/status/sprint are unchanged",
    "updates an uppercase historical ID without inserting a case-variant duplicate",
    "reports error and returns empty result when adrDir does not exist"
   ],
   "core/adr-fs-export.test.ts": [
    "writes one .md file per ADR with MADR v3 header",
    "emits `_To be backfilled_` for missing sprint/content",
    "skips byte-identical files and overwrites filesystem drift",
    "projects canonical G/D ids to taxonomy filenames",
    "reuses an existing ID-matched projection even when its slug differs from the title",
    "surfaces a typed error for a non-canonical ADR id instead of inventing a filename"
   ],
   "core/adr-operative-state.test.ts": [
    "round-trips through object metadata",
    "round-trips through string (JSON) metadata — DB form",
    "round-trips when metadata is absent (defaults to object form)",
    "round-trips a hard rule without flagGating",
    "preserves unrelated metadata keys on write",
    "does not mutate the caller-supplied exceptions array",
    "overwrites a prior operative-state on re-write",
    "returns null when metadata is absent / null / empty string",
    "returns null when metadata has no operative key",
    "returns null on malformed JSON string (never throws)",
    "returns null when enforcementLevel is missing or invalid",
    "tolerates a missing/non-array exceptions field → []",
    "filters non-string entries out of exceptions",
    "ignores an empty/blank flagGating",
    "builds a bare metadata blob readable by readOperativeState",
    "extracts and trims the section between markers",
    "returns null when markers are absent",
    "returns null when only one marker is present",
    "returns null when end precedes start",
    "renders a SOFT enforcement line (no exceptions / no flag)",
    "renders a HARD enforcement line",
    "prefixes the enforcement line with the supplied label",
    "lists sanctioned exceptions as bullet lines under a header",
    "emits a flag-gate line only when flagGating is present",
    "filters out empty / whitespace-only exceptions and trims the rest",
    "returns \"\" for null / undefined / structurally-invalid state (no stranded label)",
    "is deterministic — identical output across repeated calls (prompt-determinism guard)",
    "integrates with readOperativeState on a seed ADR (read → render → non-empty parity block)",
    "adr-037 (RBAC) carries soft enforcement + a non-empty residual exception",
    "adr-008 (Brain import direction) carries soft enforcement + residual exceptions",
    "an un-annotated seed ADR returns null (no default injected)"
   ],
   "core/agent-catalog-schema.test.ts": [
    "classifies every observed record exactly once — zero silent skips",
    "sees the 21 shipped built-ins and their 21 project shadows",
    "classifies every built-in as routable, on the current schema version",
    "reports undeclared additive fields as warnings that still load and stay routable",
    "keeps a synced built-in shadow in the project layer without a provenance disagreement",
    "classifies the archived records — and the id/directory mismatch is a warning, not a clean load",
    "classifies each present learned record as enabled but definitively non-routable (D4)",
    "exposes exactly the versions this runtime knows",
    "defaults an absent version on read and says so, without downgrading validity",
    "treats a future version as typed invalid — never coerced, never dropped",
    "treats a non-numeric version as unknown rather than parsing it",
    "makes a missing capabilities block definitively non-routable",
    "never blocks routing on an unresolvable preferredModel",
    "keeps a degraded inline persona routable but records it",
    "blocks an agent with no resolvable persona at all",
    "reports owner intent separately from every other facet",
    "lists every blocker in a fixed order",
    "classifies an unreadable manifest instead of dropping it, and claims no owner intent",
    "classifies a non-object manifest",
    "rejects a missing or malformed id",
    "warns when a declared source disagrees with the observed layer",
    "records an absent source without inventing one",
    "separates a malformed core field from a malformed additive field",
    "classifies a malformed promptSha256 as additive-field-invalid, still loading (524-012)"
   ],
   "core/agent-layer-precedence.test.ts": [
    "L0 only: builtin record loads",
    "L1 only: project override record loads",
    "L2 only: learned/runtime record loads",
    "L0 + L1 (no L2): L1 wins whole-record — unchanged pre-existing behavior",
    "L0 + L2 (no L1): L2 wins whole-record — unchanged pre-existing behavior",
    "L1 + L2 (no L0): L1 wins identity/prompt/tools; L2 composes only stats + capabilitiesProvisional",
    "L1 + L2 collision: L2 without capabilitiesProvisional leaves L1 value untouched",
    "L0 + L1 + L2: L0 is irrelevant once the gap is filled — same result as L1 + L2"
   ],
   "core/agent-manifest-canonical.test.ts": [
    "keeps every source built-in pool-visible with exact registered API model IDs",
    "keeps the project mirror model-identical to builtins, including nested routing models"
   ],
   "core/agent-manifest-sync.test.ts": [
    "returns an empty report and never lists dirs when .deckent/config.json is missing",
    "returns an empty report when readdirSync throws",
    "returns an empty report when readdirSync returns a non-array (unmocked mock default)",
    "skips non-directory entries in the builtin agents dir",
    "skips a builtin agent directory with no agent.json",
    "creates the shadow agent.json from the builtin and reports it",
    "dry-run: reports the create but never writes to disk",
    "overwrites the shadow with the new builtin content and reports \"updated\"",
    "dry-run: reports the update but never writes the shadow",
    "keeps a shadow that differs from both the baseline and the current builtin, and reports a conflict",
    "treats a differing shadow with no recorded baseline as unknown-provenance and never overwrites",
    "reports nothing and does not touch the shadow, but stamps a missing baseline",
    "does not rewrite the state file when the baseline already matches",
    "always returns the four-bucket report shape, even on an empty builtin dir"
   ],
   "core/agent-pool.test.ts": [
    "emits a typed warning and marks the pool degraded when config.json is missing",
    "emits a typed warning and marks the pool degraded when builtin dir is missing",
    "returns empty pool when no directories exist",
    "loads agents from .deckent/agents/",
    "loads agents from .tasks/agents/ (temp agents)",
    "skips non-directory entries",
    "skips directories without agent.json (readJsonSafe returns null for missing file)",
    "skips directories with invalid JSON",
    "skips agents that fail validation",
    "handles readdirSync throwing",
    "creates directory and writes agent.json",
    "writes valid JSON with trailing newline",
    "returns false when agent dir does not exist",
    "removes the directory and returns true",
    "returns undefined when agent not found",
    "returns the agent when found",
    "returns empty array when no agents",
    "returns all agents as array",
    "filters out disabled agents",
    "creates temp agent directory with sprint prefix",
    "saves agent to .deckent/agents/temp-{id}/ directory",
    "adds temp- prefix when agent id does not already have it",
    "saves to .deckent/agents/ path (not .tasks/agents/)",
    "returns 0 when .deckent/agents/ does not exist",
    "removes all directories starting with temp-",
    "returns count of removed agents",
    "does nothing when .tasks/agents does not exist",
    "removes only directories matching the sprint prefix",
    "handles readdirSync throwing gracefully",
    "does nothing if agent not found",
    "increments totalUses on DONE",
    "counts GO_WITH_TECH_DEBT as success",
    "counts NO_GO as failure (successRate goes down)",
    "returns valid for a complete AgentDefinition",
    "rejects null",
    "rejects array",
    "rejects non-object",
    "rejects missing id",
    "rejects missing name",
    "rejects empty string id",
    "rejects invalid preferredModel",
    "accepts canonical registered preferredModel values",
    "rejects invalid source",
    "rejects effortMultiplier out of range (too low)",
    "rejects effortMultiplier out of range (too high)",
    "rejects non-number effortMultiplier",
    "rejects non-boolean persistent",
    "rejects non-array triggerKeywords",
    "rejects triggerKeywords with non-string items",
    "rejects invalid stats (not an object)",
    "rejects stats with non-number totalUses",
    "rejects an object without canonical preferredModel",
    "returns multiple errors at once",
    "rejects non-string description",
    "returns true when lastUsedInSprint is empty",
    "returns false when difference equals maxAge exactly (boundary)",
    "returns true when difference exceeds maxAge by 1",
    "returns false when agent was used recently (within maxAge)",
    "returns false when sprint IDs cannot be parsed (safe default)",
    "returns false when currentSprintId cannot be parsed",
    "handles zero-padded sprint IDs correctly",
    "handles maxAge=0 (evict everything not used in current sprint)",
    "loads all temp agents when count <= maxTempAgents",
    "keeps only the maxTempAgents most recent agents when over limit",
    "DEFAULT_MAX_TEMP_AGENTS is 50",
    "DEFAULT_MAX_AGENT_AGE is 5",
    "persistent agents are never evicted by LRU",
    "returns 0 when .tasks/agents does not exist",
    "returns 0 when readdirSync throws",
    "removes stale temp agents and returns count",
    "never removes builtin agents",
    "infers current sprint from highest lastUsedInSprint when not provided",
    "returns 0 when no agents have parseable sprint IDs and no currentSprintId given",
    "skips directories without agent.json (readJsonSafe returns null for missing file)",
    "skips invalid agent.json files during cleanup",
    "removes all stale agents when many are old",
    "calls readdirSync exactly once per directory (O(N+1) pattern)",
    "does NOT call existsSync for individual agent.json files",
    "loads multiple agents from a single directory in one batch",
    "handles mixed valid/invalid agent.json files in the same directory batch",
    "handles empty directory without calling readFileSync",
    "preserves all LRU eviction logic while using batch read pattern",
    "batch read result matches pre-refactor behavior for 0-agent case",
    "loadAgent(architect) returns full PROMPT.md content (>1000 lines mock)",
    "loadAgent(test-writer) returns full content without truncation",
    "loadAgent(doc-writer) returns full content without truncation",
    "cache invalidate returns fresh full content",
    "buildWorkerPrompt() output does not contain \"Clean up fil\" truncation pattern"
   ],
   "core/agent-prompt-resolution.test.ts": [
    "L1 project PROMPT.md wins over both the runtime and the builtin layer",
    "L2 runtime PROMPT.md wins over the builtin layer when the project layer has no record",
    "L0 builtin PROMPT.md resolves only when no project/runtime record exists at all",
    "a project agent.json with no PROMPT.md BLOCKS the builtin tier and degrades instead",
    "a runtime agent.json alone also blocks the builtin tier",
    "the degraded tier honors the SAME layer precedence: project systemPrompt beats runtime",
    "a PROMPT.md in ANY layer outranks an inline systemPrompt in the project layer",
    "a whitespace-only PROMPT.md is not a hit — resolution falls through to the next layer",
    "the builtin tier stays gated on an initialized project (no .deckent/config.json → no leak)",
    "classifies a systemPrompt fallback as degraded but NOT a routability blocker",
    "emits the degraded warning exactly once per resolution",
    "does not warn when a PROMPT.md resolves in any layer",
    "agrees with classifyAgentManifest (S1) on the availability facet for both prompt states",
    "returns the typed unresolvable record when no layer holds a persona",
    "an agent.json with an empty systemPrompt and no PROMPT.md is unresolvable, not empty-degraded",
    "getAgentPrompt() is a byte-identical projection of resolvePrompt() — ${scenario.name}",
    "AgentPoolManager.resolvePrompt() returns the same record — ${scenario.name}",
    "pool synthesis and prompt resolution read the same builtin PROMPT.md bytes"
   ],
   "core/agent-prompt-sync.test.ts": [
    "returns an empty report and never lists dirs when .deckent/config.json is missing",
    "returns an empty report when readdirSync throws",
    "returns an empty report when readdirSync returns a non-array (unmocked mock default)",
    "skips non-directory entries in the builtin agents dir",
    "skips a builtin agent directory with no PROMPT.md",
    "creates the shadow PROMPT.md from the builtin and reports it",
    "dry-run: reports the create but never writes to disk",
    "overwrites the shadow with the new builtin content and reports \"updated\"",
    "dry-run: reports the update but never writes the shadow",
    "keeps a shadow that differs from both the baseline and the current builtin, and reports a conflict",
    "treats a differing shadow with no recorded baseline as unknown-provenance and never overwrites",
    "reports nothing and does not touch the shadow, but stamps a missing baseline",
    "does not rewrite the state file when the baseline already matches",
    "always returns the four-bucket report shape, even on an empty builtin dir"
   ],
   "core/agent-type-extensions.test.ts": [
    "is optional and defaults to undefined",
    "accepts an agent ID string",
    "accepts \"generic\" as a value",
    "is optional and defaults to undefined",
    "accepts an agent ID string",
    "is optional and defaults to undefined",
    "accepts an agent ID string"
   ],
   "core/agent-types.test.ts": [
    "returns an AgentStats with all fields zeroed",
    "returns a new object on every call (no shared reference)",
    "stats fields are independently mutable",
    "creates an agent with only id and name, rest defaults",
    "allows overriding description",
    "allows overriding preferredModel to a canonical premium model",
    "allows overriding preferredModel to a canonical economy model",
    "allows overriding effortMultiplier",
    "allows overriding source to builtin",
    "allows overriding source to learned",
    "allows overriding triggerKeywords",
    "allows overriding triggerScopes and triggerFilePatterns",
    "allows overriding persistent and enabled",
    "allows overriding stats",
    "allows overriding expertise, allowedTools, deniedTools",
    "returns a new object each time (no shared reference)",
    "is a Map<string, AgentDefinition>",
    "supports iteration",
    "accepts agent as null with score 0",
    "accepts agent with a positive score",
    "holds agentId and phase",
    "can form a pipeline array"
   ],
   "core/allowscope-compose.test.ts": [
    "resolves allow without waiting on an external decide()",
    "still submits AND decides the request — the audit trail is never skipped",
    "short-circuits even an explicit deny/notify policy verdict",
    "never allowscope-matches a critical-risk action, even if the grant would otherwise match",
    "falls through to awaitDecisionOrFallback when scopeId does not match",
    "falls through to an external decide() when no grant exists at all",
    "auto-approve still settles instantly, unaffected by the new seam",
    "require-approval still waits on an external decide() when no allowStore is given"
   ],
   "core/analyzer-overhaul.test.ts": [
    "M) falls back to fs count when git ls-files fails",
    "M) uses git file count when git succeeds",
    "M) author count returns 0 when git log fails",
    "N) returns cached result on second call with same mtime",
    "N) re-detects after clearAnalyzeCache()",
    "classifies small size for few files",
    "classifies medium size for 50-499 files",
    "clearAnalyzeCache() is exported and callable"
   ],
   "core/analyzer.test.ts": [
    "detects next (takes priority over react)",
    "detects react",
    "detects express",
    "detects nest",
    "detects vue",
    "detects angular",
    "detects svelte",
    "returns unknown when no framework found",
    "detects typescript",
    "detects rust",
    "detects python via pyproject.toml",
    "detects javascript (package.json without tsconfig)",
    "detects mixed (multiple language markers)",
    "returns unknown when no language markers",
    "detects vitest",
    "detects jest",
    "detects mocha",
    "detects pytest from pyproject.toml",
    "detects vite",
    "detects webpack",
    "falls back to tsc when tsconfig exists",
    "detects github-actions",
    "detects gitlab-ci",
    "detects circleci",
    "small project with 1 author → micro-sprint",
    "medium project → sprint",
    "large project under 2000 files → agile",
    "large project with 2000+ files → hybrid",
    "handles missing package.json gracefully",
    "handles malformed package.json",
    "handles git command failure"
   ],
   "core/anthropic-http-client.test.ts": [
    "parses all 13 rate limit headers",
    "handles missing headers (returns null)",
    "parses partial headers",
    "calls count_tokens endpoint with correct URL and headers",
    "throws AnthropicApiError on 429",
    "throws AnthropicApiError on 500",
    "returns retry-after when present",
    "returns wait when RPM exhausted",
    "returns wait when ITPM near estimated task size",
    "returns 0 when plenty of quota remaining",
    "returns seconds until future time",
    "returns 0 for past time",
    "returns 0 for invalid time",
    "follows 5s, 20s, 80s, 320s schedule",
    "caps at 600s (10 minutes)",
    "supports custom base"
   ],
   "core/approval-allowscope-risk-tier.test.ts": [
    "never grants high+critical or malformed tiers and preserves elevated matching"
   ],
   "core/approval-allowscope.test.ts": [
    "starts empty when no file exists yet",
    "defaults filePath to <projectRoot>/.deckent/settings/approval-allows.json",
    "is fail-soft to an empty set when the file contains invalid JSON",
    "is fail-soft to an empty set when the top level is not an array",
    "is fail-soft and skips only the malformed entries in a mixed-validity array",
    "rejects a grant whose scopeId is the reserved wildcard token %j",
    "does not persist anything to disk when a wildcard grant is rejected",
    "accepts a concrete (non-wildcard) scopeId",
    "rejects an empty scopeId (schema min-length, not just the wildcard set)",
    "generates id + grantedAt, defaults reason, and returns the full rule",
    "persists the grant atomically as a JSON array on disk",
    "accumulates multiple grants",
    "rejects an invalid ApprovalScope value",
    "rejects an invalid maxRisk value",
    "rejects a non-ISO expiresAt",
    "matches when scopeId + scope are identical and risk is exactly maxRisk",
    "matches when request risk is BELOW maxRisk",
    "matches the none risk tier against any maxRisk",
    "does not match a different scopeId",
    "does not match a different scope (out-of-scope)",
    "does not match a scope that is merely adjacent (credential vs shell-exec)",
    "does not match when request risk exceeds maxRisk (over-risk)",
    "does not match critical risk even against a maxRisk of high",
    "returns null against an empty store",
    "does not match a request that satisfies risk+scope but not scopeId, and vice versa",
    "does not match an already-expired grant",
    "purges an expired grant from the in-memory set as a side effect of matchesAllow",
    "persists the purge — a fresh store re-reading the same file no longer sees the expired grant",
    "only purges the expired grant, leaving a still-live grant for a different scopeId intact",
    "treats a grant expiring exactly at \"now\" as expired (boundary is exclusive)",
    "completes the full round-trip",
    "persists the revoke — a fresh store re-reading the same file no longer sees it",
    "revokeAllow on an unknown id returns false and does not disturb existing grants",
    "revoking one grant leaves sibling grants untouched"
   ],
   "core/approval-authority-bootstrap.test.ts": [
    "stays disabled without an owner opt-in and never opens custody",
    "returns a typed HOLD when OIDC is not fully configured",
    "opens one approval-only runtime and pinned OIDC verifier without provisioning",
    "requires an enabled API OIDC verifier with a non-empty audience"
   ],
   "core/approval-authority-keyring.test.ts": [
    "opens a contiguous approval-only revision chain and verifies exact active and retired keys",
    "cannot validate a provider-domain MAC with the dedicated approval key",
    "fails closed when the keyring is missing, project-local, permissive, symlinked, or hash-broken",
    "returns an explicit unsupported adapter on native Windows"
   ],
   "core/approval-authority-runtime.test.ts": [
    "reopens one durable OIDC decision and validates the exact attended dispatch after restart",
    "keeps another tenant, revoked session and stale identity fail-closed",
    "returns typed HOLD for missing custody and native Windows without provisioning anything"
   ],
   "core/approval-broker-timeout-receipt.test.ts": [
    "publishes governed v2 bytes privately and blocks them when the gate is off",
    "makes the central direct decide path expiry-aware and never grants critical default allow",
    "returns an expired typed result from decideChecked after a fresh cross-process sweep",
    "produces byte-identical timeout receipts through broker and store sweep paths",
    "uses an existing valid receipt as read-only restart authority",
    "has a single durable winner under a human-versus-timeout race"
   ],
   "core/approval-broker.test.ts": [
    "persists a valid request atomically and emits pending",
    "rejects an invalid request without writing a file or emitting pending",
    "rejects a duplicate id",
    "rejects path-traversal ids before any store write",
    "rejects the same request id from a fresh broker without overwriting the first request",
    "cleans up the tmp file and leaves no partial request file when publish fails",
    "aborts only the exact waiter and cleans its listener without creating a decision",
    "does not park a pre-aborted waiter and keeps an already-settled result canonical",
    "binds listener cleanup to the signal captured at wait ingress",
    "removes the captured abort listener when a pending wait durably settles",
    "exposes exact read-only request and durable-winner lookups",
    "rejects a decision without a durable request",
    "resolves the awaiting promise and emits decided",
    "awaitDecision resolves immediately when already decided",
    "rejects deciding an already-decided id",
    "a fresh broker cannot overwrite the durable winner and hydrates it for waiters",
    "hydrates durable requests and decisions on process restart",
    "hydrates and decides a path-safe legacy v1 request without permitting a new legacy submit",
    "rejects an invalid decision (unknown decision value)",
    "auto-decides an expired pending request using its defaultAction",
    "leaves a not-yet-expired request untouched",
    "skips an already-decided request",
    "expires a hydrated path-safe legacy v1 request with the same lifecycle semantics",
    "filters by pending/decided/all, defaults to pending",
    "discovers a decision written directly to the store by another process",
    "a decision made by a second broker instance (simulated second process) is picked up by the first",
    "marks a malformed foreign decision file as seen so it never retry-loops",
    "does not mark a torn (invalid JSON) decision file as seen — retries once the write completes",
    "ignores a valid decision published under a non-canonical filename",
    "submit()/decide() output validates directly against the approval-contract schemas"
   ],
   "core/approval-channel-authenticator.test.ts": [
    "LIVE-DEFECT PIN (2026-08-21): the channel principal need NOT equal the request userId",
    "mints the pinned person actor and digest-bound live proof",
    "returns null for critical risk without consuming the nonce",
    "kills an in-flight proof when the chat falls off the live allowlist",
    "rejects a second use of the same nonce",
    "END-TO-END: decideChannel writes a channel envelope and only a wired validator trusts it",
    "decideChannel rejects a critical request when the authenticator returns null"
   ],
   "core/approval-channel-risk-tier.test.ts": [
    "uses explicit riskTier authoritatively and delegates missing legacy risk centrally",
    "rejects risk=high+riskTier=critical before nonce use and kills an in-flight proof"
   ],
   "core/approval-command-classification.test.ts": [
    "preserves the agentic-only package manager patterns",
    "preserves common destructive and network classifications",
    "classifies a proven read-only command as file-read with a typed READ_ONLY reason code",
    "read-only recognition wins over the heuristic pattern ladder only when proven",
    "keeps the context-free (worker gate) contract byte-identical: every shell command stays at least shell-exec",
    "follows the PowerShell dialect when the host runs deckent_bash through powershell.exe"
   ],
   "core/approval-contract-lifecycle.test.ts": [
    "keeps the new-write v1 compatibility default",
    "reads stored v1 source shape without injecting enumerable defaults or changing its digest",
    "accepts the strict v2 lifecycle envelope and preserves source lineage exactly",
    "rejects incomplete, malformed and raw-secret-bearing v2 envelopes"
   ],
   "core/approval-contract.test.ts": [
    "requester role is the 5-value enum",
    "scope is the 7-value enum",
    "risk is the 5-value enum",
    "policy is the 4-value enum",
    "defaultAction/decision is the 4-value enum",
    "accepts only bounded lowercase-ASCII opaque ids",
    "reads path-safe legacy v1 ids without authorizing them for new writes",
    "rejects an empty object and lists every required field as missing",
    "accepts a full valid request and stamps the contract version",
    "rejects an unknown requester.role (enum-guarded, nested path)",
    "rejects an unknown scope value",
    "rejects an unknown risk value",
    "rejects an unknown policy value",
    "rejects an unknown defaultAction value",
    "rejects expiresAt equal to createdAt",
    "rejects expiresAt before createdAt",
    "rejects a non-ISO createdAt/expiresAt",
    "flags a missing required sub-field with its dotted path",
    "accepts maskedArgs + rawArgsRef when args are present",
    "never lets a rawArgs (raw value) key survive validation — strict() rejects it",
    "has no rawArgs property on the inferred type (structural proof via keys of a valid parse)",
    "round-trips losslessly through JSON",
    "isApprovalRequest type guard: true for valid, false for garbage",
    "validateApprovalRequest never throws on non-object input",
    "applies the same cross-platform opaque-id contract to requestId",
    "reads a legacy decision id only through the persisted compatibility validator",
    "requires tombstone id and embedded winner requestId to match",
    "rejects an empty object and lists every required field as missing",
    "accepts a full valid decision and defaults reason to empty string",
    "accepts an explicit reason",
    "round-trips the strict live-session authorization envelope without requiring it on legacy decisions",
    "rejects malformed digests, raw session fields, and non-forward auth expiry",
    "rejects an unknown decision value",
    "accepts free-form channel values (not a fixed enum)",
    "rejects a non-ISO decidedAt",
    "round-trips losslessly through JSON",
    "isApprovalDecision type guard: true for valid, false for garbage",
    "approvalRequestSchema.safeParse matches validateApprovalRequest",
    "approvalDecisionSchema.safeParse matches validateApprovalDecision"
   ],
   "core/approval-decision-ingress-versioned-digest.test.ts": [
    "projects v1 exactly while binding every v2 lineage field",
    "mints and validates against the durable v2 envelope, not the plain source"
   ],
   "core/approval-decision-ingress.test.ts": [
    "accepts an interactive re-auth minted seconds after decide() entry, and still fails closed on mid-prompt request expiry",
    "derives identity from live auth, binds the exact request, and persists no raw session secret",
    "reauthenticates every replay, returns the immutable winner for the same command, and rejects nonce reuse drift",
    "rejects wrong actor or tenant and never writes a decision",
    "fails closed on expired request, expired auth, and authenticator failure",
    "rejects request/decision tampering, session revocation, and legacy allow"
   ],
   "core/approval-direct-surface-parity.integration.test.ts": [
    "keeps every retained surface terminal: no replay, revival, or access grant"
   ],
   "core/approval-eventstream.test.ts": [
    "two clients with different filters each see only their own matching notifications",
    "a client subscribing after requests are already pending receives them immediately, then live events",
    "a decided request is retired from the pending cache and is not backfilled",
    "drops the oldest events beyond maxBuffer and marks the drop with a coalesced counter",
    "unsubscribe() terminates the iterator, stops further delivery, and drops the client entry",
    "breaking a for-await loop early (iterator.return()) also cleans up, same as explicit unsubscribe",
    "rejects a duplicate client id",
    "detaches from the relay via its public API only and closes open subscriptions"
   ],
   "core/approval-expiry-driver-lifecycle.test.ts": [
    "runs a startup sweep, emits the durable receipt and never repeats it on a no-op tick",
    "redelivers a durable timeout receipt once after process restart for idempotent settle-back",
    "runs async legacy-origin sweep at startup and exposes graceful in-flight settlement"
   ],
   "core/approval-expiry-driver.test.ts": [
    "TTL-expires a due-pending request via broker.expire, visible in store after tick",
    "leaves a not-yet-due pending request untouched",
    "prunes a decided entry older than pruneOlderThanMs",
    "respects a custom pruneOlderThanMs",
    "re-indexes the store so a just-expired request is not immediately pruned",
    "is fail-soft: a throwing broker/store does not throw and reports via onTickError",
    "defaults to console.error when onTickError is not supplied",
    "running is false before start()",
    "start() creates an unref\\'d interval (hasRef === false)",
    "start() is a no-op when already running (single setInterval call)",
    "stop() clears the interval and is idempotent",
    "stop() before any start() is a no-op (does not throw)",
    "tick() runs periodically once started (fake timers, injected clock)"
   ],
   "core/approval-fallback.test.ts": [
    "expiry boundary is inclusive (request.expiresAt === ctx.expiresAt counts as expired)",
    "critical risk WITH a reachable channel escalates while live and denies after expiry",
    "prefers dashboard over api when both are alive",
    "falls back to api when dashboard is not alive",
    "ignores unrecognized channel names for escalation purposes",
    "only recognizes dashboard and api as escalation channels",
    "parks pending rather than hanging",
    "returns the same decision for the same inputs across repeated calls",
    "never mutates the channelsAlive input array",
    "${label} → finite decision matching the documented precedence"
   ],
   "core/approval-file-cas-platform.test.ts": [
    "proves privacy before first-writer-wins publication",
    "publishes a revisioned file durably with owner-only POSIX mode",
    "rejects a stale revision instead of clamping or replacing the winner",
    "serializes concurrent writers and requires reload under the lock",
    "returns typed Windows ACL outcomes using async icacls",
    "fails honestly on a platform whose private ACL cannot be proven"
   ],
   "core/approval-inbox-federation-parity.test.ts": [
    "reads the real revisioned object-map and projects opaque lifecycle authority",
    "uses identical compatibility parsing for legacy array and object-map rows",
    "does not surface an elapsed pending record as decidable"
   ],
   "core/approval-inbox-federation.test.ts": [
    "federates every store into origin-tagged rows, sorted by requestedAt",
    "is fail-soft: missing stores are empty, corrupt stores yield a typed unreadable row",
    "never writes: store bytes are identical after listing (read-only contract)"
   ],
   "core/approval-lifecycle-closure.integration.test.ts": [
    "preserves source/evidence/revision and policy lineage through SLA, audit, and expiry",
    "correlates a broker-native producer through its durable receipt and expiry audit",
    "correlates an autonomous producer through broker mirror, timeout settle-back, and audit",
    "correlates a gateway producer through broker mirror, timeout settle-back, and no-grant audit"
   ],
   "core/approval-lifecycle-concurrency-scale.integration.test.ts": [
    "makes human-vs-timeout one FWW terminal truth and never changes that truth on retry",
    "serializes two real processes through reload-under-lock and preserves only the first decision",
    "restart catch-up writes exactly one timeout receipt and a no-op restart keeps bytes stable",
    "measures one restart sweep over 10k canonical pending rows with stable unique receipts"
   ],
   "core/approval-lifecycle-config-resolver.test.ts": [
    "resolves the gate off and all four canonical profiles exactly",
    "accepts only monotonic tightening",
    "rejects lengthening, delayed/non-monotonic SLA and risk/blocking weakening",
    "rejects a project layer that weakens a tighter global layer"
   ],
   "core/approval-lifecycle-config-types.test.ts": [
    "pins the closed origin and policy vocabularies",
    "supports partial authored profiles and a complete resolved snapshot"
   ],
   "core/approval-lifecycle-migration.test.ts": [
    "derives expiry from the immutable source timestamp and is byte-deterministic",
    "never resets a shorter producer expiry to the profile ceiling",
    "quarantines an invalid original clock %s",
    "isolates identical legacy ids by tenant in deterministic successor identity",
    "quarantines a producer expiry at or before source creation"
   ],
   "core/approval-lifecycle-platform.integration.test.ts": [
    "publishes POSIX authority at 0600",
    "requires a successful Windows icacls proof before publication",
    "publishes nothing when the platform cannot prove privacy"
   ],
   "core/approval-lifecycle-policy.test.ts": [
    "maps legacy risk once and applies origin floors/sensitive promotion",
    "uses the injected clock and caps producer expiry without resetting createdAt",
    "resolves exact origin timeout safety and never trusts defaultAction alone",
    "produces a stable digest and rejects in-flight weakening",
    "ignores per-field weakening while retaining tightening from the same transition"
   ],
   "core/approval-live-session.test.ts": [
    "persists only a session hash and validates the exact context after restart",
    "revokes first-writer-wins and remains inactive across restart",
    "cannot mint a lease whose authentication is future-dated or already expired"
   ],
   "core/approval-masking.test.ts": [
    "redacts an sk- style API key inside a string leaf",
    "redacts a Bearer token",
    "redacts an API_KEY= assignment (env-value shape)",
    "redacts a password= assignment",
    "redacts a credential embedded in a URL path (user:pass@host)",
    "redacts a GitHub PAT inside an array element",
    "preserves non-string leaves untouched",
    "preserves object/array shape for nested structures",
    "preserves non-sensitive strings verbatim",
    "does not mutate the input object",
    "writes the raw args file under .deckent/approvals/raw/<id>.json",
    "persists the exact raw content (round-trip via resolveRawArgs)",
    "sets file permissions to 0600",
    "is atomic — no leftover .tmp file after a successful write",
    "sanitizes a path-traversal id into a safe filename (stays inside the raw dir)",
    "resolveRawArgs returns null for a ref that does not exist",
    "resolveRawArgs rejects a traversal ref (defense in depth)",
    "resolveRawArgs returns null for a malformed JSON file",
    "resolveRawArgs is explicit-call-only — storeRawArgs alone does not expose raw content anywhere else",
    "a request built from maskArgs()/storeRawArgs() output serializes with no trace of the raw secret"
   ],
   "core/approval-oidc-authenticator.test.ts": [
    "verifies a fresh step-up assertion and persists a bounded exact-context lease",
    "rejects a verifier whose authority does not match the configured policy"
   ],
   "core/approval-policy-fallback-risk-tier.test.ts": [
    "clamps high+critical auto-approval to deny while explicit elevated remains authoritative",
    "never applies an allow timeout default to an expired critical tier, even with API reachable",
    "fails malformed explicit tiers closed instead of treating them as missing legacy input"
   ],
   "core/approval-policy.test.ts": [
    "returns the first matching rule action, ignoring later matches",
    "is positional, not specificity-based — a broader earlier rule beats a narrower later one",
    "skips non-matching rules and applies the next matching one",
    "an empty match object matches every request",
    "matches on risk alone regardless of scope/requester/tenantId",
    "matches on requester role",
    "matches on tenantId",
    "carries the matched rule timeoutMs through to the result",
    "omits timeoutMs when the matched rule does not specify one",
    "defaultAction=%s -> policy=%s",
    "never elevates: an empty rule set always resolves via defaultAction, never a rule default",
    "clamps a matched rule that says auto-approve to deny",
    "clamps the no-match fallback when defaultAction=allow maps to auto-approve",
    "does NOT clamp a matched rule that already says require-approval",
    "does NOT clamp a matched rule that says notify",
    "does NOT clamp a matched rule that says deny (already safe)",
    "non-critical risk is unaffected by the clamp — auto-approve passes through",
    "is a pure function: identical inputs always produce identical outputs",
    "does not mutate the request or the rules array"
   ],
   "core/approval-probe-subject.test.ts": [
    "progresses request to terminal-authenticated decision, verification, and one host-global claim",
    "refuses a claim after the live re-authentication expires",
    "keeps a decision without live authorization fail-closed"
   ],
   "core/approval-relay-sla.test.ts": [
    "routes a stable SLA event once across restart-aware dedup state",
    "rejects an expired channel decision through the shared broker guard",
    "rejects SLA evidence bound to another request"
   ],
   "core/approval-relay.test.ts": [
    "notifies every attached channel with maskedArgs, never a raw value",
    "channel-A deciding resolves awaitDecision and notifies only channel-B",
    "uses an injected formatCrossDecided builder (string-free, i18n-first — born-697 D3)",
    "rejects a duplicate channel name",
    "detachChannel removes a channel and reports whether one existed",
    "a channel whose send() throws does not block the other channel from being notified",
    "a channel whose send() rejects (async) is reported without an unhandled rejection",
    "a losing decide() race (already-decided) is reported via channel-error, not thrown",
    "stops reacting to broker events after dispose()"
   ],
   "core/approval-rules-engine.test.ts": [
    "automation allowlist: probes are routine; mirrors/unknown kinds are untouchable",
    "liveRuleFor fresh-loads; a fault-flagged file automates nothing",
    "the authenticator mints rule:<id> under the engine authorityRef and dies with the file",
    "END-TO-END: a rule decision flows through the real ingress to a decided broker envelope"
   ],
   "core/approval-rules-load.test.ts": [
    "loads a fully valid rule set in original order",
    "accepts every match field (scope, risk, requester, tenantId) combined",
    "omits timeoutMs from the output when not provided on input",
    "skips a rule with an unknown field and records a warning",
    "skips a rule with an unknown enum value on match.risk",
    "skips a rule with an unknown enum value on action",
    "skips a rule with a missing required field (action)",
    "skips a rule with an unknown field nested inside match",
    "skips a rule with a non-positive timeoutMs",
    "never throws — a rule entry of the wrong type (string) is skipped, not fatal",
    "an all-invalid array does NOT silently fall back to safe defaults",
    "keeps valid entries, skips invalid ones, warns once per skip, preserves valid order",
    "returns safe defaults with no warnings when config has no approval field",
    "returns safe defaults with no warnings when approval is null",
    "returns safe defaults with no warnings when approval has no rules field",
    "returns safe defaults with no warnings when approval.rules is null",
    "returns safe defaults with no warnings when approval.rules is an empty array",
    "returns safe defaults + a warning when approval.rules is not an array",
    "returns safe defaults + a warning when approval is not an object",
    "returns safe defaults with no warnings for a non-object root config: %p",
    "has exactly 5 rules, risk-ordered critical -> high -> medium -> low -> none",
    "maps critical and high to require-approval",
    "maps medium to notify",
    "maps low and none to auto-approve",
    "default rules resolve every risk tier through decidePolicy as documented",
    "a loaded custom rule set resolves through decidePolicy identically to a hand-built one",
    "does not mutate the input config object",
    "is a pure function: identical input always produces an equivalent result"
   ],
   "core/approval-rules-risk-tier.test.ts": [
    "never automates critical or malformed explicit tiers",
    "uses explicit tier and central legacy normalization instead of a kind-local tier"
   ],
   "core/approval-rules.test.ts": [
    "loads fail-soft and saves atomically (round-trip)",
    "advisory matcher: prefix + summary + disabled + expiry, first match wins",
    "promotion scopes to the id-prefix family with provenance, routine-tier always"
   ],
   "core/approval-short-code.test.ts": [
    "is deterministic, 5 chars, confusion-resistant alphabet",
    "normalizes confusables and recognizes code-shaped input only",
    "resolves only against the current pending set — fail-closed and typed",
    "reports collisions as ambiguous with every match (never a guess)"
   ],
   "core/approval-sla.test.ts": [
    "emits initial once with stable lineage identity",
    "coalesces restart catch-up outbound to the highest actionable stage",
    "emits exactly one terminal expiry and never replays it",
    "records typed skipped stages for a short producer TTL without extending it",
    "binds persisted state to the exact request generation and clock lineage",
    "retries an unacknowledged stable event and never replays it after ACK"
   ],
   "core/approval-store-lifecycle.test.ts": [
    "does not create an empty store from a read-only constructor",
    "blocks new governed writes while lifecycle is disabled",
    "creates private v2 bytes and rejects duplicate ids first-writer-wins",
    "applies a later per-field tightening and lets expiry win a late human decision",
    "continues draining a durable request after gate-off and writes one idempotent receipt",
    "surfaces corrupt request bytes as quarantine instead of hiding the origin"
   ],
   "core/approval-store-watch.test.ts": [
    "reports a pending request already on disk BEFORE the watcher attached, exactly once",
    "does not report an already-decided request via onPending",
    "onPending fires exactly once across multiple scans for the same unchanged record",
    "onDecided fires exactly once when a decision appears, even across repeated scans",
    "onPending fires on attach, then onDecided fires once the decision file appears",
    "a torn (invalid JSON) request file triggers no onPending",
    "an atomic-write tmp file (mid-rename artifact) triggers no handler at all",
    "a decision file with no matching request file triggers no onDecided",
    "a change is detected via the poll timer alone when fs.watch never fires",
    "the fs.watch path reacts independently of the poll timer",
    "the poll interval is created unref\\'d (never pins the host process alive)",
    "dispose() clears the poll interval and closes the fs.watch handle",
    "dispose() is idempotent — calling it twice does not throw or double-close",
    "no handler fires for an event that arrives after dispose()",
    "fs.watch construction failure degrades gracefully — poll fallback still works",
    "detects an externally-written pending request without any injected watch/scan"
   ],
   "core/approval-store.test.ts": [
    "defaults storeDir to the same path ApprovalBroker defaults to",
    "keeps a missing storeDir lazy and starts with an empty snapshot",
    "categorizes a still-open request as pending",
    "categorizes an overdue, unswept request as expired (time-only, no decision file)",
    "categorizes a broker.decide(\"allow\") result as approved",
    "categorizes broker.decide(\"deny\"/\"defer\"/\"escalate\") results as denied",
    "categorizes a broker.expire() TTL sweep as expired (channel ttl-expire)",
    "static load(dir) is a pure one-shot equivalent of index()",
    "skips a torn (invalid JSON) request file instead of throwing",
    "skips a decision file with no matching request file",
    "a brand-new instance recovers the exact same categorized state purely from disk",
    "index() re-syncs an existing instance after an external writer touches storeDir",
    "writes an \"approved\" decision atomically at the broker-compatible path",
    "a decision transition()-written by the store is discovered by a live broker sharing storeDir",
    "writes an \"expired\" decision (channel ttl-expire) for an overdue pending request",
    "rejects transition() to \"expired\" without channel ttl-expire (category mismatch)",
    "rejects transition() to \"approved\" with a non-allow decision (category mismatch)",
    "rejects transition() for an unknown id",
    "rejects transition() for an already-decided id",
    "two stale store instances preserve the first terminal decision",
    "rejects an invalid decision payload",
    "retires an old decision with a permanent tombstone before removing its files",
    "prevents a stale broker/store snapshot from resurrecting a pruned id",
    "hydrates a pruned tombstone winner for existing and restart waiters",
    "fails loud and preserves active files when an invalid tombstone occupies the id",
    "leaves a decided entry newer than the cutoff untouched",
    "never prunes a pending or overdue-unswept entry regardless of age"
   ],
   "core/approval-terminal-authenticator.test.ts": [
    "mints a durable authorization envelope and fails closed after re-auth expiry"
   ],
   "core/approval-v1-mac-restart.integration.test.ts": [
    "validates the persisted v1 MAC after a fresh broker hydration"
   ],
   "core/approval-worker-gate-risk-tier.test.ts": [
    "settles high+critical auto-approve as deny without granting",
    "overrides an allow fallback for a critical tier",
    "preserves noncritical auto-approve behavior"
   ],
   "core/approval-worker-gate.test.ts": [
    "settles instantly via allow, without waiting for an external decision",
    "resolves allow once the broker is externally decided",
    "resolves deny once the broker is externally decided deny",
    "treats defer/escalate decisions as deny (fail-closed)",
    "defaults to deny when no fallback resolver is injected",
    "DENY_FALLBACK_RESOLVER always resolves deny",
    "uses a custom injected fallback resolver on timeout",
    "never overrides a decision that lands during fallback resolution (race-safe)",
    "does not fire the fallback if the broker decides before the timeout",
    "submits only maskedArgs; the raw value never appears on the request",
    "submits maskedArgs: null when the action carries no rawArgs"
   ],
   "core/as4-p1-realize.test.ts": [
    "returns empty extraArgs for an empty spec object",
    "returns empty extraArgs when all arrays are empty",
    "single skill with content → --append-system-prompt <content>",
    "multiple skills → two --append-system-prompt pairs in order",
    "skill without content and no projectRoot → placeholder arg",
    "single subagent → --agents <agentId>",
    "multiple subagents → multiple --agents pairs",
    "mcp entry → --mcp-config <configPath>",
    "skills before subagents before mcp in output",
    "skill with content → textFallback contains content and skillId, no extraArgs",
    "subagent → textFallback contains agentId",
    "mcp entry → textFallback contains serverName and configPath",
    "codex: skill → textFallback, no extraArgs",
    "gemini: subagent → textFallback with agentId",
    "claude always produces extraArgs (no textFallback)",
    "non-claude always produces textFallback (no extraArgs)",
    "provider field is echoed back in the result",
    "extraArgs is readonly (non-mutating)"
   ],
   "core/as4-p2-skills.test.ts": [
    "no useNativeSkills flag → no --setting-sources args (empty spec)",
    "useNativeSkills: false → no --setting-sources args",
    "useNativeSkills: true but no projectRoot → no --setting-sources (safe)",
    "useNativeSkills: true but missing .claude/skills dir → no crash, no args",
    "single skill dir → one --setting-sources entry",
    "two skill dirs → two --setting-sources entries (in order)",
    "empty skills dir → no --setting-sources entries (dir exists but empty)",
    "path contains projectRoot + .claude/skills/<id>",
    "no extraArgs for non-claude even with useNativeSkills",
    "useNativeSkills: true + ollama → textFallback contains skill id, no extraArgs",
    "useNativeSkills: true + codex → textFallback contains all skill ids",
    "useNativeSkills: false + ollama + skills dir → no textFallback",
    "claude: spec skill + native skill → both --append-system-prompt and --setting-sources"
   ],
   "core/attended-execution-approval.test.ts": [
    "authors only the bounded provider probe at the canonical routine risk",
    "binds a live-session allow to the exact final dispatch and writes one immutable receipt",
    "rejects legacy/raw evidence, exact-binding drift, and receipt replay before dispatch",
    "does not expose a constructible verified-grant shortcut",
    "rejects unsigned, denied, expired, and revoked-session decisions"
   ],
   "core/attended-execution-proposal.test.ts": [
    "persists one exact digest-only proposal and reopens it after restart",
    "rejects first-writer conflict and project-local custody"
   ],
   "core/audit-export.test.ts": [
    "returns valid JSON with correct entry count",
    "returns empty JSON array when no events",
    "returns CSV with header and one data row per entry",
    "escapes commas in CSV cell values",
    "applies tenantId filter and returns only matching entries",
    "applies time-range filter",
    "verifies chain produced by exportAuditLog",
    "returns false when a chain entry is tampered",
    "returns false when chain length differs from entries length",
    "returns false when wrong secret is used for verification"
   ],
   "core/audit-query-rbac.test.ts": [
    "viewer role (has READ permission) can query audit events",
    "viewer role without tenantId in query defaults to local tenant",
    "viewer role does NOT have WRITE permission (can() returns false)",
    "invalid role is rejected by RBAC gate — queryAudit returns empty",
    "admin role can query audit events (has all permissions)",
    "admin has READ, WRITE, EXECUTE, AUDIT and ADMIN permissions",
    "invalid tenantId in query is rejected by RBAC gate (fail-closed)",
    "no role (undefined) bypasses gate — existing behavior preserved"
   ],
   "core/audit-query.test.ts": [
    "returns only events matching tenantId",
    "returns empty array when no events match tenantId",
    "returns only events matching the specified channel",
    "returns only events within from/to range",
    "returns empty matched array when event stream does not exist",
    "returns empty matched array when no events match combined filters",
    "returns only events with matching correlationId",
    "returns empty array when no events match",
    "returns empty array for empty input",
    "returns only events with matching causationId",
    "returns empty array for empty input",
    "returns empty array for empty input",
    "returns empty array when no events match correlationId",
    "places root event (no causationId) first",
    "orders a 3-event chain root → middle → leaf",
    "excludes events from other correlationIds",
    "returns empty Map for empty input",
    "groups events by actor field",
    "all events for an actor are returned under the same key",
    "returns only audit-channel payloads, in order, excluding other channels",
    "reads back exactly what writeAuditEvent appended (hermetic round-trip)",
    "returns [] when the sprint stream is absent",
    "uses the AUDIT_EVENT_CHANNEL constant for filtering"
   ],
   "core/audit-retention.test.ts": [
    "returns three empty arrays when input is empty",
    "returns three empty arrays with no policy",
    "keeps all entries when no policy fields are set",
    "prunes entries older than maxAgeMs",
    "prunes multiple consecutive old entries from head",
    "keeps all entries when all are within maxAgeMs",
    "prunes all entries when all are beyond maxAgeMs",
    "archives entries beyond the maxCount window",
    "keeps all when maxCount >= entries.length",
    "archives all when maxCount is 0",
    "prunes oldest (age), archives middle (count), keeps newest",
    "maxCount cannot \"unprune\" age-expired entries (prune boundary respected)",
    "prune + archive + keep reconstructs the original ordered array",
    "no gaps: slices are contiguous and cover all entries",
    "does not prune entries without a timestamp (conservative)",
    "prunes timestamped head entries then stops at first legacy entry"
   ],
   "core/audit-writer-approval-lifecycle.test.ts": [
    "attributes timeout to system:expiry with full digest lineage",
    "records short-TTL skipped evidence without pretending it was delivered",
    "rejects mismatched request lineage before writing"
   ],
   "core/audit-writer.test.ts": [
    "returns true and writes an event readable by queryAudit",
    "payload contains actor and action fields",
    "multiple events are filterable by tenantId",
    "events are filterable by channel using the AUDIT_EVENT_CHANNEL constant",
    "returns false and writes no event when tenantId is missing",
    "returns false when tenantId is an empty string",
    "returns false when actor is missing",
    "returns false when action is missing",
    "returns true for a fully valid event",
    "returns true with optional fields present",
    "returns false for whitespace-only tenantId",
    "returns false for whitespace-only actor",
    "returns false for whitespace-only action",
    "single event forms a valid chain",
    "three consecutive events form a valid chain",
    "each event links to the previous via prevHmac",
    "modifying a payload field breaks the chain at that index",
    "modifying the first event breaks the chain at index 0",
    "injecting a foreign event with wrong prevHmac breaks the chain",
    "empty array returns intact: true",
    "legacy events without hmac are skipped (intact)",
    "mix of legacy and chained events: chain portion is verified",
    "a second sprint stream written after a first still anchors at GENESIS",
    "a non-sprint audit partition interleaved with a sprint keeps each chain intact",
    "restart that appends to an existing sprint stays contiguous (disk-seed)",
    "written hmac is keyed HMAC-SHA256 with the shared export secret, not unkeyed sha256",
    "the writer-shared secret matches audit-export.ts default secret",
    "an independent verifier recomputes a multi-event chain entirely from the keyed HMAC",
    "a legacy v1 (unkeyed sha256) record still verifies intact",
    "a stream that migrated v1 → v2 mid-chain verifies end-to-end",
    "tampering a migrated v2 record still breaks the chain (tamper-evidence preserved)",
    "relabeling a v2 record as chainVersion:1 fails (algorithm is bound to the authenticated version)"
   ],
   "core/auth-jwks.test.ts": [
    "rejects an http URL without ever calling fetch (HTTPS-only)",
    "rejects a malformed URL",
    "throws on a non-2xx response with the status in the message",
    "throws on malformed JSON",
    "throws when the body has no keys array",
    "returns the keys of a valid document, dropping non-object entries",
    "resolves a kid to a PEM spki key that verifyJwt accepts (round-trip)",
    "serves a second resolve from the TTL cache — fetch is NOT called again",
    "re-fetches once the TTL has elapsed (injected clock)",
    "handles key rotation: unknown kid on a fresh cache triggers exactly one re-fetch",
    "throws JWKS_UNKNOWN_KID after the single rotation re-fetch still misses",
    "does not double-fetch when the kid is missing right after a fresh fetch",
    "filters out non-RSA and non-RS256 keys (algorithm-confusion guard)",
    "exposes JwksError instances (typed error surface)",
    "verifies a real RS256 token end-to-end via the resolver",
    "rejects an HS256 token outright (JWKS keys are asymmetric only)",
    "rejects an alg:none token",
    "rejects an RS256 token without a kid header",
    "fails CLOSED when the kid cannot be resolved",
    "forwards claim constraints to verifyJwt (issuer mismatch surfaces)",
    "rejects a token signed by a key OTHER than the one the kid resolves to",
    "returns malformed_token for garbage input"
   ],
   "core/auth-matrix.test.ts": [
    "each provider override map carries ONLY its own credential (zero cross-leak)",
    "subscription mode (empty secrets) → empty overrides → Claude worker gets NO ANTHROPIC_API_KEY",
    "only the provided provider produces an override; ollama (host-HTTP) never appears",
    "matrix: the 4 auth combinations resolve to isolated/empty credential sets",
    "empty-string keys are ignored (no override, no env mutation)"
   ],
   "core/auth-oidc.test.ts": [
    "accepts a valid HS256 token",
    "rejects a tampered (bad-sig) HS256 token",
    "rejects when the algorithm is not in the explicit allow-list",
    "rejects when no key material is supplied",
    "accepts a valid RS256 token (round-trip validates the algorithm string)",
    "rejects an RS256 token signed by a different key",
    "rejects when an RS256 token is presented but no public key is supplied",
    "returns invalid_signature for a garbage signature segment",
    "rejects an alg:none token even when key material is present",
    "rejects alg:none case-insensitively (NoNe)",
    "rejects an expired token",
    "honors clock tolerance for a just-expired token",
    "rejects a not-yet-valid (nbf) token",
    "rejects an issuer mismatch",
    "accepts a matching issuer",
    "rejects an audience mismatch",
    "accepts when the token aud array contains the expected audience",
    "does not enforce iss/aud when the options are absent",
    "rejects a token without three segments",
    "rejects a non-string token",
    "rejects an unsupported algorithm",
    "decodes claims without verifying the signature",
    "decodes claims even for an alg:none token (introspection only)",
    "returns null for a malformed token",
    "exposes the JwtAlgorithm union"
   ],
   "core/auth-session.test.ts": [
    "create returns a non-empty token",
    "resolve returns the correct session for a valid token",
    "resolve returns null for unknown token",
    "each create produces a unique token",
    "resolve returns null after session expires",
    "resolve is valid one millisecond before expiry",
    "revoke causes resolve to return null",
    "revoking a non-existent token is a no-op",
    "prune removes expired sessions and returns the count",
    "prune with explicit now argument",
    "prune on empty store returns 0",
    "prune returns 0 when no sessions have expired",
    "persistence.save is called on create",
    "persistence.save is called on revoke",
    "persistence.save is called on prune when entries removed",
    "persistence.save is NOT called on prune when nothing removed",
    "persistence.load populates store on construction",
    "ActorContext fields map correctly to Session fields",
    "ActorContext with only id (no role/tenantId) is accepted"
   ],
   "core/branch-coverage.test.ts": [
    "falls back to 0.0.0 when package.json cannot be read",
    "falls back to 0.0.0 when package.json has no version field"
   ],
   "core/builtin-skills-quality.test.ts": [
    "discovers the full builtin skill set",
    "has an Anti-Patterns section",
    "has a Karpathy Notes section",
    "contains no stale or hallucinated model IDs",
    "matches the .deckent dogfood mirror in authored content (when present)",
    "anthropic-sdk teaches tier-based model selection, not a hardcoded dated ID"
   ],
   "core/builtins/agent-catalog-agsk2.test.ts": [
    "has agent.json and PROMPT.md on disk",
    "agent.json passes AgentPoolManager.validateAgentDefinition",
    "has correct id/name/manifestVersion/source/enabled",
    "has a zeroed stats object shaped like every other builtin",
    "is a superset of the api-builder (disk-verified reference) field set",
    "sets role=implementer, resolved live via getAgentRole (not a fixture)",
    "sets domain, resolved live via getAgentDomain (not a fixture)",
    "carries activation.rules whose domains.$contains values are routing-consistent",
    "PROMPT.md core body stays within the 4KB rubric cap and covers its rubric theme",
    "has unique ids and names across the two new agents",
    "agent-pool load-smoke: AgentPoolManager.loadAgents() picks up both new agents from the real .deckent/agents pool",
    "no existing built-in agent claims the new agents domain names (zero routing collision)"
   ],
   "core/builtins/agent-catalog-agsk6.test.ts": [
    "has a PROMPT.md on disk under src/core/builtins/agents/<id>/",
    "PROMPT.md core body stays within the 4KB rubric cap and is non-trivial",
    "opens with an H1 title and covers its rubric-theme keyword",
    "has no YAML frontmatter (matches the most recent lineage: integration-engineer, ",
    "carries a Skill Affinity note naming its paired 368-001 builtin skill",
    "has unique ids across the three new agents",
    "no existing built-in agent directory claims a new agent id (disk-driven, zero routing collision)"
   ],
   "core/builtins/agent-catalog-capabilities.test.ts": [
    "discovers at least the 18 builtin agents known to carry a capabilities block",
    "${id}/agent.json: capabilities is present and schema-valid",
    "refactorer carries build:never (the old catch-all must never return)",
    "architect carries writeAuthority:false (advisor, Write is in its deniedTools)",
    "finds at least the 2 known -auditor agents",
    "${id}: workTypes declares build:never",
    "ci-guardian and devops-engineer differ in at least 2 capability axes",
    "referential integrity: every declared domain id in the real tree exists in the builtin vocabulary",
    "referential-integrity check demonstrably fails on a fixture with an unknown domain id",
    "${id}/agent.json: activation.rules is a non-empty array"
   ],
   "core/builtins/catalog-materialize.test.ts": [
    "skill '${id}' is now pool-visible, enabled, source=builtin",
    "agent '${id}' is now pool-visible, enabled, source=builtin",
    "agent '${id}' resolves real PROMPT.md content via getAgentPrompt() builtin fallback",
    "all pre-existing .deckent/skills entries still load with identical ids/enabled/source",
    "all pre-existing .deckent/agents entries still load with identical ids/enabled/source",
    "an explicit .deckent/skills/api-design override shadows the builtin-derived fallback",
    "an explicit .deckent/agents/api-designer override shadows the builtin-derived fallback",
    "a completely unknown id (no .deckent record, no builtin) still resolves to source=none",
    "a bare directory (no config.json) does not inherit any real builtin agents/skills",
    "an initialized project (config.json present) sees the FULL builtin agent catalog via the fallback"
   ],
   "core/builtins/catalog-sync-parity.test.ts": [
    "src/core/builtins carries all 6 new items on disk (SKILL.md / PROMPT.md), read live off the real tree",
    "pool membership follows the two-layer contract: .deckent manifest OR builtin-tree presence (371-001 D-004 fallback — supersedes the manifest-only invariant)",
    "pool membership follows the two-layer contract: .deckent agent.json OR builtin-tree presence (371-001 D-004 fallback — supersedes the json-only invariant)",
    "the directory-level copy is unconditional: all 6 new items land in the fresh .deckent tree regardless of manifest/agent.json",
    "even freshly seeded, the pool loaders only recognize an item once its manifest/agent.json exists (proves the gate is the manifest, not the seeding path)"
   ],
   "core/builtins/skill-catalog-agsk3.test.ts": [
    "has manifest.json and SKILL.md on disk",
    "manifest.json passes SkillPoolManager.validateSkillDefinition",
    "has correct id/name/manifestVersion/source/enabled",
    "has a zeroed stats object shaped like every other builtin",
    "is a superset of the api-builder (disk-verified reference) field set",
    "sets the expected category",
    "carries the expected activation rule (domain or intent — post-601 contract)",
    "SKILL.md stays within the 4KB rubric cap and covers its lineage theme",
    "has unique ids and names across the two new skills",
    "the two trees match for both new skills: SKILL.md byte-eş, manifest stats-hariç eş",
    "skill-pool load-smoke: SkillPoolManager.loadSkills() picks up both new skills from the real .deckent/skills pool",
    "no existing built-in skill claims the new skills domain names (zero routing collision)"
   ],
   "core/builtins/skill-catalog-agsk4.test.ts": [
    "has manifest.json and SKILL.md on disk",
    "manifest.json passes SkillPoolManager.validateSkillDefinition",
    "has correct id/name/manifestVersion/source/enabled",
    "has a zeroed stats object shaped like every other builtin",
    "is a superset of the api-builder (disk-verified reference) field set",
    "sets the expected category",
    "carries activation.rules whose domains.$contains includes the expected domain",
    "SKILL.md stays within the 4KB rubric cap and covers its lineage theme",
    "SKILL.md documents all three providers and the silent-fallback ban",
    "the two trees match: SKILL.md byte-eş, manifest stats-hariç eş",
    "skill-pool load-smoke: SkillPoolManager.loadSkills() picks up the new skill from the real .deckent/skills pool",
    "no existing built-in skill claims the new skill domain name (zero routing collision)"
   ],
   "core/builtins/skill-catalog-agsk5.test.ts": [
    "has a SKILL.md on disk under src/core/builtins/skills/<id>/",
    "SKILL.md stays within the 4KB rubric cap and is non-trivial",
    "opens with an H1 title and covers its lineage theme + Karpathy Notes",
    "has no YAML frontmatter (matches the most recent lineage: rpc-protocol, ",
    "has unique ids across the three new skills",
    "no existing built-in skill directory claims a new skill id (disk-driven, zero routing collision)"
   ],
   "core/capability-audit-bridge.test.ts": [
    "returns the original handler result unchanged",
    "emits a success record with correct fields",
    "emits AFTER invoke completes (sequencing)",
    "preserves requiredCapability and description from the original handler",
    "emits record once per invocation across multiple calls",
    "re-throws the original error after emitting",
    "emits error record BEFORE re-throwing (sequencing)",
    "error record has outcome:error, error message, and actor",
    "handles non-Error throws (string)",
    "works without emit argument (success path — no crash)",
    "works without emit argument (error path — re-throws, no crash)",
    "emits record with undefined actor when ctx has no actor",
    "emits error record with undefined actor when ctx has no actor"
   ],
   "core/capability-authority-resolution.test.ts": [
    "allows and reports every part of the scope in a single decision",
    "reports denial-audit wiring on the decision when it is armed",
    "denies cross-tenant: the resource is owned by another tenant",
    "denies when the operation needs a capability the role does not grant",
    "denies an operation id that is not in the catalog (no decision from a guess)",
    "denies an unknown role (safe-deny, never permissive)",
    "escalates a high-risk operation from an unverified principal, and denies it under strict",
    "denies with DENIED_UNRESOLVED_INPUT naming '${input}' (${JSON.stringify(hole)})",
    "names every input when the request itself is absent",
    "projects an unresolved request to an EMPTY grant set under enforcement",
    "carries the decision grants and the resolved principal/tenant into the broker context",
    "a needs_approval decision is not an allow: the projected grant set is empty",
    "the armed registry enforces the decision through its own existing gate",
    "leaves grantedCapabilities unset, so invoke takes the same permissive branch",
    "produces an identical CapabilityResult to the pre-existing plain-context call",
    "does NOT block even when the decision denies (no default flip)",
    "keeps the registry itself unarmed — the flip stays owner-gated"
   ],
   "core/capability-broker.test.ts": [
    "register / has / get a handler",
    "list returns sorted, stable names",
    "re-registering the same name overwrites (last writer wins)",
    "unregister removes; clear empties",
    "resolves by capability verb",
    "prefers an explicit connector over the verb",
    "falls back to the verb when the connector is not registered",
    "returns CAPABILITY_NOT_FOUND when nothing matches",
    "passes target.args through to the handler",
    "denies when the granted set excludes the required capability",
    "allows when the granted set includes the required capability",
    "is permissive when no granted set is provided (opt-in enforcement)",
    "surfaces a handler throw as CAPABILITY_FAILED (never throws)",
    "echoHandler returns args verbatim and declares mcp-tool",
    "reads a real file under projectRoot",
    "throws on a missing file (→ CAPABILITY_FAILED via registry)",
    "throws on a non-string path",
    "refuses a path that escapes projectRoot (traversal containment)",
    "integrates through the registry as CAPABILITY_FAILED on bad path",
    "ships echo + fs.read preinstalled",
    "registerCapability mutates the default registry; invokeCapability uses it",
    "resetDefaultCapabilityRegistry restores the baseline (no leak across tests)",
    "getDefaultRegistry tracks the live instance after reset",
    "installReferenceHandlers populates an arbitrary registry",
    "returns CAPABILITY_NOT_FOUND when the request has no capabilityTarget",
    "extracts the target and invokes the matching handler",
    "derives the least-privilege grant set from requirements.capabilities",
    "lets an explicit ctx override the request-derived grant set",
    "routes against a provided registry instead of the default",
    "single backend registered with no opts: default priority, always available",
    "picks the highest-priority backend when several share a name",
    "skips an unavailable higher-priority backend, falls back to an available lower one",
    "returns CAPABILITY_NOT_FOUND when a registered name has no available backend",
    "equal priority → most-recently registered wins (last-writer-wins preserved)",
    "get() returns the top backend ignoring availability (has ⟺ get)",
    "a throwing isAvailable() predicate counts as unavailable (registry never throws)",
    "connector falls back to the verb when the connector backend is unavailable",
    "returns [] for an unregistered name",
    "lists labels in resolution order (priority desc, ties most-recent first)",
    "falls back to requiredCapability when a backend has no description",
    "forwards priority so the higher-priority backend is selected",
    "forwards isAvailable so an unavailable backend is skipped"
   ],
   "core/capability-handlers-data.test.ts": [
    "declares least-privilege requiredCapability values",
    "uses hyphen notation for requiredCapability (no dot, matches Capability namespace)",
    "gates against the canonical hyphen grant tag (dot tag never matched)",
    "installDataHandlers registers handlers without editing the broker",
    "keeps broker least-privilege gating active for installed handlers",
    "allows one SELECT statement and passes params through to injected queryImpl",
    "rejects write and admin SQL before queryImpl runs",
    "rejects non-SELECT and semicolon multi-statement SQL",
    "searches through injected searchImpl and maps normalized headers",
    "validates query and limit before searchImpl runs"
   ],
   "core/capability-handlers-erp.test.ts": [
    "rejects a missing entity with an explanatory failure",
    "rejects a non-string entity",
    "rejects non-array filters",
    "rejects a filter with an unknown op",
    "rejects a filter without a value",
    "rejects non-string-array fields and non-number limit",
    "surfaces connector validation (unregistered entity) as CAPABILITY_FAILED — SSOT, not re-validated",
    "is DENIED when the grant set lacks erp.read",
    "is allowed when erp.read is granted",
    "returns all rows and declared fields for a bare entity query",
    "applies eq and projects only the requested fields",
    "applies ne",
    "applies gt / gte / lt / lte over numbers",
    "applies in over a parameter list",
    "applies like with % and _ wildcards (anchored)",
    "combines multiple filters with AND semantics and honors limit",
    "throws an explanatory error for an unknown source table",
    "maps an entity to its declared physical source",
    "omits fields that are absent from a row instead of emitting undefined",
    "declares requiredCapability 'erp.read' and forwards ctx.actor to the connector"
   ],
   "core/capability-handlers.test.ts": [
    "declares least-privilege requiredCapability values",
    "installExtendedHandlers registers handlers without editing the broker",
    "keeps broker least-privilege gating active for installed handlers",
    "performs a hermetic GET through injected fetch and returns status/body",
    "rejects missing, invalid, and unsupported URLs before fetch",
    "reads only allow-listed variables",
    "returns null for an allow-listed variable that is absent",
    "runs through injected async spawn with shell disabled and returns output",
    "uses ctx.projectRoot as cwd when args.cwd is absent",
    "validates command, args, allowlist, and spawn errors"
   ],
   "core/capability-runtime.test.ts": [
    "preinstalls the full handler set (reference + extended + data)",
    "emits a success audit record on a successful invocation",
    "emits an error audit record when a handler throws (and the broker degrades to CAPABILITY_FAILED)",
    "a throwing emit never breaks the invocation (fail-safe)",
    "forwards handler options (env allowlist) to the underlying handlers",
    "omitting emit yields a plain working registry (no audit, no crash)",
    "does not admit the capability by name alone",
    "omitting the erp option keeps today's behavior — has('erp.read') is false",
    "registers 'erp.read' and round-trips a query when the erp option is provided",
    "erp invocations flow through the audit bridge",
    "gate DISABLED by default (no config) → ADVISORY_GATE_DISABLED, not enforced",
    "config.enforce_least_privilege:false → still ADVISORY_GATE_DISABLED",
    "config.enforce_least_privilege:true → ENFORCED_LEAST_PRIVILEGE, enforced",
    "denialAudited reflects options.denialAudit presence, independent of the gate",
    "production default (no config) → advisory debugLog fires with the dead-gate truth",
    "gate armed (enforce_least_privilege) → NO advisory (the gate is real)",
    "emits at most ONCE per process even across many gate-off constructions"
   ],
   "core/cascade-detector.test.ts": [
    "continues on single NO_GO",
    "pauses sprint after 5 consecutive NO_GO",
    "resets consecutive count on DONE",
    "resets consecutive count on GO_WITH_TECH_DEBT",
    "halts after 3 consecutive rate-limited events",
    "resets on successful request",
    "throttles when NO_GO rate >= 30% (after min tasks)",
    "does not throttle before min task threshold",
    "does not throttle when NO_GO rate below threshold",
    "reset clears consecutive counts but preserves totals",
    "fullReset clears everything",
    "getStats returns accurate counts",
    "halted state is sticky",
    "honors custom max consecutive NO_GO",
    "honors custom NO_GO rate threshold",
    "detects Sprint 140 cascade within first 5 failures",
    "prevents 197 worker cascade from ever happening"
   ],
   "core/catalog-apiid-merge.test.ts": [
    "remote metadata refreshes a bundled entry with the same canonical API identity",
    "bundled apiId is preserved when no remote entry matches",
    "merge is idempotent — applying twice gives same result",
    "unmatched remote models are appended; existing bundled entries stay intact",
    "updates bundled entry apiId via remote catalog through bootstrapFromCatalog",
    "offline mode: bundled apiId preserved when no network"
   ],
   "core/catalog-merge-id.test.ts": [
    "matches via remote.apiId === bundled.apiId and updates apiId + cost + contextWindow",
    "preserves the exact canonical id when a matching remote row refreshes it",
    "appends unmatched remote entry as a new registry entry",
    "keeps equal apiIds from different providers as distinct logical models",
    "is idempotent — second pass produces equal result",
    "each remote entry is consumed at most once (no double-update across bundled entries)"
   ],
   "core/catalog-stats-read-model.test.ts": [
    "projects exact ratios, rounded percentages, counts, recency, and provenance",
    "represents zero uses as never measured rather than a fake zero percent",
    "skips malformed entity records without rejecting valid siblings",
    "skips malformed exposure rows and rejects malformed attribution authority without hiding valid efficacy stats"
   ],
   "core/cfg-apr-wire.test.ts": [
    "absent block produces no approval errors (default off)",
    "accepts an empty approval object",
    "accepts gate_enabled / relay_enabled set to valid booleans",
    "accepts a well-formed rules array alongside the flags",
    "returns error when gate_enabled is a non-boolean value",
    "returns error when relay_enabled is a non-boolean value",
    "never throws for a malformed rules array — rule validation is NOT this layer",
    "absent approval -> safe default rules, gate/relay both false",
    "passes through a valid custom rule set unchanged",
    "resolves gate_enabled / relay_enabled from config, defaulting absent to false",
    "a malformed rule entry is skipped (fail-soft) — never throws, never blocks",
    "an all-invalid rules array resolves to an empty list, NOT silently substituted defaults",
    "accepts a valid approval partial",
    "rejects an invalid gate_enabled type",
    "valid config.json: rules + gate/relay flags resolve correctly",
    "absent approval block: default-safe (SAFE_DEFAULT_APPROVAL_RULES, gate/relay off)",
    "broken config.json (malformed rule entry): loadConfig does NOT throw — bad entry dropped",
    "broken config.json (approval.rules not an array): loadConfig does NOT throw — safe defaults used",
    "flows the real producer through the persisted first owner rule, signed ingress, and immutable one-shot claim",
    "fails closed for elevated/critical requests and a disabled owner rule",
    "does not turn an expired produced request into a rule authorization or claim"
   ],
   "core/cfg1-legacy-mode.test.ts": [
    "does not throw for legacy mode '${alias}' combined with an unrelated key",
    "mutates partial.mode from '${alias}' to canonical '${canonical}' in place",
    "still rejects a genuinely invalid mode (not a known legacy alias)",
    "leaves an already-canonical mode untouched",
    "sets an unrelated key without throwing when on-disk mode is legacy '${alias}'",
    "persists canonical mode '${canonical}' on disk after the set (read-time coerce persisted)",
    "resolves project config mode '${alias}' to '${canonical}'",
    "flags mode '${alias}' as needing migration",
    "migrateConfig renames '${alias}' → '${canonical}' and persists it",
    "does not flag mode 'unlimited' as needing migration (config-migration.ts gap)",
    "migrateConfig leaves 'unlimited' unchanged on disk when no other field is missing"
   ],
   "core/chat-config-schema.test.ts": [
    "parses a valid full chat config object",
    "accepts empty object — all fields are optional (sade default)",
    "accepts status_line as a string array (field list)",
    "rejects invalid mode value",
    "rejects invalid provider value",
    "rejects unknown extra fields (strict mode)",
    "returns parsed ChatConfig when chat block is valid",
    "returns empty object when config is null",
    "returns empty object when config is undefined",
    "returns empty object when chat block is absent",
    "returns empty object when chat block is invalid (invalid mode)",
    "returns empty object when chat block is an array (non-object)",
    "project chat block overrides global chat block (deepMerge semantics)",
    "enterprise chat.mode visible only when explicitly set in config",
    "slash_extra is replaced by project layer (not concatenated)"
   ],
   "core/chat-provider-fallback.test.ts": [
    "chat_provider takes priority over brain_provider",
    "brain_provider used when chat_provider absent",
    "defaults to claude when both are absent",
    "returns claude for unknown/invalid provider values",
    "handles ollama as a valid provider",
    "returns primary provider when available probe returns true",
    "falls back to ollama when primary unavailable and local_fallback=ollama",
    "returns primary when unavailable but no local_fallback configured",
    "skips isAvailable probe when not provided",
    "does not throw when provider is available",
    "throws ChatProviderError with clear message when unavailable",
    "error message contains provider name and actionable guidance",
    "error has correct name and provider field"
   ],
   "core/ci-after-sprint.test.ts": [
    "writes CI report to .brain/ci-report-{sprintId}.json",
    "creates .brain directory if missing",
    "serializes all report fields correctly",
    "returns null when report file does not exist",
    "returns null when report file is malformed JSON",
    "reads back a written report correctly",
    "writes CI report to disk and returns it",
    "computes delta.newTests from baseline when baseline exists",
    "returns tscPassed=false when tsc fails",
    "counts regressions from failed tests",
    "skips vitest run when track_test_count is false"
   ],
   "core/ci-learning.test.ts": [
    "returns empty array when .brain does not exist",
    "reads valid CI reports",
    "limits to maxSprints",
    "skips malformed files gracefully",
    "returns empty patterns for empty reports",
    "detects tsc failure pattern",
    "detects high severity tsc pattern with 3+ failures",
    "detects regression pattern",
    "detects coverage drop pattern",
    "detects build failure pattern",
    "returns no patterns when all green",
    "returns empty for no reports",
    "generates tsc warning for repeated failures",
    "generates regression warning",
    "generates coverage decline warning from report trend",
    "generates coverage improvement recommendation",
    "returns empty for no reports",
    "suggests block_on_test_fail for high regression rate",
    "suggests min_coverage for coverage decline",
    "suggests block_on_tsc_fail for repeated tsc failures",
    "keeps pre_sprint_check when all sprints green",
    "builds a summary line for a clean sprint",
    "includes tsc failed indicator",
    "includes regression pattern when detected",
    "returns empty string for no reports",
    "builds section with CI Learnings header",
    "returns empty result when no reports exist",
    "runs full analysis with reports",
    "writes ci-learnings.json to .brain/",
    "returns null when no reports exist (graceful)",
    "analyzes and writes results",
    "does nothing when MEMORY.md does not exist"
   ],
   "core/ci-pre-sprint.test.ts": [
    "parses all-passed output",
    "parses output with failures",
    "returns zeros for empty/unparseable output",
    "handles multiline output with summary at end",
    "returns passed=true with parsed counts on success",
    "returns passed=false with failure counts on fail",
    "skips validation when ci_guardian is disabled",
    "skips validation when pre_sprint_check is false",
    "blocks sprint when tsc fails and block_on_tsc_fail=true",
    "warns but does not block when tsc fails and block_on_tsc_fail=false",
    "blocks sprint when tests fail and block_on_test_fail=true",
    "does not block when tests fail and block_on_test_fail=false",
    "saves baseline on successful validation",
    "reads config from .deckent/config.json when no override",
    "configOverride takes precedence over file config",
    "skips vitest when track_test_count=false",
    "includes tscPassed=false but passed=true when tsc fails, block_on_tsc_fail=false, no tests",
    "DEFAULT_CI_GUARDIAN_CONFIG has pre_sprint_check=true"
   ],
   "core/ci-regression.test.ts": [
    "maps src/ file to corresponding tests/ file",
    "finds wildcard-matching test files (prefix match)",
    "returns empty array for files without test directory",
    "skips non-src files",
    "skips test files in filesChanged",
    "deduplicates test files across multiple source files",
    "returns passed=true when tsc succeeds",
    "returns passed=false when tsc fails",
    "returns passed=true with test count when vitest succeeds",
    "returns passed=false when vitest fails",
    "skips execution when no test files provided",
    "writes and reads baseline correctly",
    "returns null when no baseline file exists",
    "returns null for invalid JSON baseline",
    "returns defaults when no config exists",
    "returns defaults when config has no ci_guardian section",
    "merges partial ci_guardian config with defaults",
    "detects regression when tsc fails",
    "detects regression when targeted tests fail",
    "reports no regression when tsc and tests pass",
    "skips tsc check when block_on_tsc_fail is false",
    "detects test count decrease from baseline",
    "handles no filesChanged gracefully",
    "returns targeted test files list in result",
    "skips test count comparison when tracking disabled"
   ],
   "core/cli-command-contract.test.ts": [
    "keeps interactive model activation targets optional and offline-capable",
    "preserves the task settlement reason-code authority flag",
    "catalogs the complete command family and exact watch flags",
    "keeps the run contract applying by default while exposing dry-run",
    "shares the scheduled flow capability id"
   ],
   "core/compliance-report.test.ts": [
    "rbacStatus is ON when rbacEnabled is true",
    "rbacStatus is OFF when rbacEnabled is false",
    "tenantIsolationStatus is ON when tenantIsolation is true",
    "tenantIsolationStatus is OFF when tenantIsolation is false",
    "intact chain (legacy events) → auditChainIntact ON",
    "intact chain (empty events) → auditChainIntact ON",
    "broken chain → auditChainIntact OFF and brokenAt set",
    "eventCount matches number of provided events",
    "actorBreakdown counts events per actor",
    "empty events → eventCount 0, empty actorBreakdown",
    "report contains all required fields",
    "all controls OFF when both flags false and no events"
   ],
   "core/computer-use-contract.test.ts": [
    "accepts a minimal screenshot action",
    "accepts a screenshot action with a region",
    "accepts a click action and applies defaults",
    "accepts a fully-specified click action",
    "accepts a type action and applies defaults",
    "accepts a navigate action and applies defaults",
    "accepts a navigate action with an explicit waitUntil",
    "rejects an unknown kind",
    "rejects a click action missing coordinates",
    "rejects a click action with non-numeric coordinates",
    "rejects a click action with an invalid button",
    "rejects a type action with empty text",
    "rejects a type action with a negative delay",
    "rejects a navigate action with a non-URL string",
    "rejects a navigate action with an invalid waitUntil",
    "rejects a bare object with no kind",
    "exposes exactly the Oku/Değiştir/Çalıştır ladder (Otonom intentionally omitted)",
    "maps every action kind to a fixed class matching the taxonomy",
    "classifies screenshot as Oku (read-only)",
    "classifies click and type as Değiştir (mutating)",
    "classifies navigate as Çalıştır (highest risk — arbitrary execution downstream)",
    "securityClassForAction derives the class from a validated action, ignoring any extra input",
    "accepts a valid ok result",
    "accepts a valid error result",
    "accepts a valid unavailable result",
    "rejects an error result missing errorMessage",
    "rejects an unavailable result missing reason",
    "rejects an unknown status",
    "rejects a securityClass outside the fixed taxonomy",
    "is unavailable with a reason when the config block is entirely absent",
    "is unavailable when enabled is explicitly false",
    "is unavailable when enabled but allowed_capabilities is absent (fail-closed)",
    "is unavailable when enabled but allowed_capabilities is empty",
    "is unavailable when allowed_capabilities only contains unknown strings",
    "is available with the exact allowlisted subset when enabled with a valid allowlist",
    "filters out unknown capability strings rather than trusting them",
    "grants all four capabilities when fully allowlisted"
   ],
   "core/computer-use-exec.test.ts": [
    "config entirely absent → unavailable, spawn never called",
    "enabled: false → unavailable, spawn never called",
    "allowlist-dışı: enabled but action kind not in allowed_capabilities → unavailable, spawn never called",
    "unknown platform → unavailable, spawn never called",
    "region string in an unrecognized format → unavailable naming both accepted formats, spawn never called",
    "region-scoped screenshot on win32/wsl is honestly unavailable (no scripted sub-rectangle path yet), spawn never called",
    "linux: grim present → ok result with base64-decodable PNG bytes, file cleaned up",
    "linux: grim absent (ENOENT) + scrot present but fails + import succeeds — tries in order, skips gnome-screenshot",
    "linux: every candidate tool absent (ENOENT) → unavailable naming all 4 tools",
    "linux: every candidate tool present but fails (non-zero) → error (tool found, not \"unavailable\")",
    "darwin: screencapture present → ok result with correct bytes",
    "darwin: screencapture absent (ENOENT) → unavailable",
    "darwin: screencapture present but fails (non-zero) → error",
    "win32: powershell.exe writes+returns a path → ok result read from that path",
    "win32: powershell.exe absent (ENOENT) → unavailable",
    "win32: powershell.exe present but fails (non-zero) → error",
    "wsl: powershell.exe + wslpath translation both consulted → ok result",
    "wsl: wslpath missing after a successful powershell capture → error (tool was found, translation failed)",
    "linux: grim present → ok result, invoked with -g in grim/slurp \"X,Y WxH\" geometry form",
    "linux: comma-only region format (\"X,Y,W,H\") is normalized to grim\\'s own geometry form",
    "linux: grim absent (ENOENT), scrot present → falls back to scrot -a x,y,w,h, never tries import/gnome-screenshot",
    "linux: both region-capable tools absent → unavailable naming grim/scrot, import/gnome-screenshot never attempted",
    "darwin: screencapture -R present → ok result with correct region-rect args",
    "darwin: screencapture absent → unavailable",
    "linux: builds xdotool mousemove+click with button/repeat mapping",
    "linux: xdotool absent → unavailable",
    "linux: xdotool present but fails → error",
    "darwin: left click → osascript \"click at\" script, no modifier",
    "darwin: right click → Control-click modifier included",
    "darwin: middle click → honestly unavailable, osascript never invoked",
    "win32: builds a powershell SetCursorPos/mouse_event script with the correct button flags",
    "wsl: click routes through the same powershell.exe path as win32",
    "linux: builds xdotool type --delay with the literal text",
    "linux: xdotool absent → unavailable",
    "darwin: text is passed as a distinct osascript argv element (parametrized, no script-string interpolation)",
    "darwin: crafted-arg injection attempt safely fails — payload never enters the AppleScript source, only ever a literal keystroke argument",
    "darwin: osascript present but fails → error",
    "win32: invokes powershell.exe via -File with a STATIC param($Text) script; text passed as a distinct -Text argv element (no string-concat)",
    "win32: crafted-arg injection attempt safely fails — payload never enters the PowerShell script source, only ever a literal $Text parameter value",
    "win32: type cleans up the temp .ps1 script file after invocation",
    "wsl: type routes through the same powershell.exe -File SendKeys path as win32",
    "linux: xdg-open invoked with the URL as a plain positional argv element (no script at all)",
    "linux: xdg-open absent → unavailable",
    "linux: xdg-open present but fails → error",
    "darwin: open invoked with the URL as a plain positional argv element",
    "darwin: open absent → unavailable",
    "win32: invokes powershell.exe via -File with a STATIC param($Url) Start-Process script; url passed as a distinct -Url argv element",
    "win32: crafted-arg (query-string) injection attempt safely fails — payload never enters the PowerShell script source, only ever a literal $Url parameter value",
    "win32: powershell.exe absent → unavailable",
    "wsl: navigate routes through the same powershell.exe -File Start-Process path as win32",
    "${label} parses against computerUseResultSchema",
    "resolves code/stdout for a real successful process",
    "resolves a non-zero code + stderr for a real failing process",
    "rejects when the binary does not exist (ENOENT) — matches the fake-spawn \"throws on absent tool\" contract",
    "createRealComputerUseExecDeps returns a real spawn + a platform string, and executeComputerUseAction never touches it when the flag is off"
   ],
   "core/computer-use-platform.test.ts": [
    "exposes exactly the 4 known platform ids",
    "recognizes each of the 4 known ids",
    "rejects an unknown platform string",
    "never calls the prober when config is entirely absent",
    "never calls the prober when enabled is explicitly false",
    "never calls the prober when enabled but allowed_capabilities is empty (fail-closed)",
    "marks every capability unavailable with a platform-naming reason, without probing",
    "${platform}: screenshot/click/type honestly flip on prober output",
    "${platform}: prober is actually consulted (per-tool) for screenshot/click/type",
    "linux: mixed prober result (only gnome-screenshot present) still finds screenshot but not click/type",
    "${platform}: navigate is unavailable regardless of prober output",
    "does not probe click/type tools when only screenshot is allowlisted (linux, disjoint tool sets)",
    "grants exactly the allowlisted subset on darwin"
   ],
   "core/condition-evaluator.test.ts": [
    "resolves simple path",
    "resolves nested path",
    "resolves deeply nested path",
    "returns undefined for missing path",
    "returns undefined for null object",
    "returns undefined for empty path",
    "handles arrays in path",
    "matches string value",
    "rejects non-matching string",
    "matches number value",
    "matches boolean value",
    "matches nested path",
    "$gt operator",
    "$gte operator",
    "$lt operator",
    "$lte operator",
    "$contains on array of strings",
    "$contains on string",
    "$contains on array of objects with name field",
    "$in operator",
    "$not operator",
    "$exists operator",
    "multiple operators on same field",
    "$and — all must match",
    "$or — any must match",
    "nested $and/$or",
    "all conditions must match",
    "fails if any condition mismatches",
    "empty condition always matches",
    "handles non-numeric comparison gracefully",
    "handles undefined actual value"
   ],
   "core/config-backup-rotation.test.ts": [
    "keeps 3 newest timestamped backups, deletes older ones",
    "preserves legacy timestamp-less config.json.bak",
    "is a no-op when no backups exist",
    "is a no-op when backup count <= keepCount",
    "ignores unrelated files matching different prefixes",
    "prunes old backups after a migration write"
   ],
   "core/config-bot-agent-passthrough.test.ts": [
    "preserves ollama_host / native_model / openai_base_url / bot_agent (not stripped)"
   ],
   "core/config-bot-capabilities.test.ts": [
    "typechecks on DeckentConfig and interpolates $DECK SMTP secrets"
   ],
   "core/config-cache.test.ts": [
    "first call performs full disk I/O (cold load)",
    "second call returns cached result without re-reading config files",
    "cache is invalidated when project config mtime changes",
    "cache is invalidated when effective global config mtime changes",
    "force: true bypasses cache even when mtime is unchanged",
    "DECKENT_CONFIG_RELOAD=1 env var bypasses cache",
    "different projectRoot invalidates cache",
    "clearConfigCache() resets cache state"
   ],
   "core/config-corrupted-recovery.test.ts": [
    "recovers from JSON syntax error — renames corrupted file, writes fresh default",
    "recovers from empty file",
    "recovers from binary garbage",
    "strike-5: io-error (EISDIR) does NOT quarantine — file untouched, no backup, defaults used",
    "uses defaults when config file does not exist (no backup created)",
    "persists a flat-only provider alias in canonical grouped form",
    "removes claude_backend when spawn_backend exists",
    "preserves claude_backend when spawn_backend is absent",
    "removes flat brain_provider when providers.brain exists",
    "promotes flat providers when grouped providers is absent",
    "preserves top-level max_workers (Decision 1+2)",
    "removes fallback_provider when providers.fallback exists",
    "returns empty array when no duplicates found",
    "canonicalizes provider_overrides and compares maps independent of key order",
    "does not mutate any key when a provider alias conflicts",
    "getModePreset returns correct max_workers for performance",
    "getModePreset returns correct max_workers for balanced",
    "getModePreset returns correct max_workers for economic",
    "getModePreset returns correct max_workers for api",
    "DEFAULT_MODES max_workers matches MODE_PRESETS (single-source)"
   ],
   "core/config-cross-verify.test.ts": [
    "absent block produces no cross_verify errors (default off)",
    "accepts cross_verify with only enabled: true",
    "accepts cross_verify with all fields set to valid values",
    "accepts custom verifier_priority values",
    "returns error when enabled is a non-boolean string",
    "returns error when high_stakes_only is a non-boolean value",
    "returns error when verifier_priority is not an array",
    "returns error when verifier_priority contains a non-string element",
    "accepts a provider → exact model API ID map",
    "rejects a bare string instead of a per-provider map",
    "names the offending provider when its model ID is empty",
    "leaves unknown model identity to registry resolution, not config shape"
   ],
   "core/config-defaults.test.ts": [
    "dependency_pipeline_enabled is true by default",
    "createDefaultConfig() and getDefaultConfig() return the same default value",
    "returns a fresh object each call (no shared reference)",
    "uses the four bounded-render defaults without constraining durable records",
    "deep-merges global and project overrides into one effective render config",
    "fills missing default-ON fields while preserving explicit false and extra fields",
    "project explicit false wins while an omitted sibling keeps its default",
    "runtime_extension_enabled is true by default",
    "explicit false override is preserved through deepMerge",
    "3-layer merge precedence: project override wins over global"
   ],
   "core/config-dep-pipeline-default.test.ts": [
    "createDefaultConfig() returns dependency_pipeline_enabled: true (default ON)",
    "mergeConfigs honors explicit user override to false (rollback path)",
    "mergeConfigs(null, null) preserves the true default (no-config case)"
   ],
   "core/config-edge.test.ts": [
    "merges flat objects — override wins",
    "deeply merges nested objects",
    "replaces arrays — does NOT deep-merge them",
    "ignores undefined override values",
    "handles null values in override",
    "does not mutate the base object",
    "handles empty override",
    "handles deeply nested three-level merge",
    "returns empty warnings array for a valid config",
    "throws ConfigValidationError for invalid mode",
    "throws for invalid language",
    "throws when a mode config is missing",
    "throws for invalid brain_model",
    "throws for invalid default_model",
    "throws for max_workers out of range",
    "throws for max_workers > 100",
    "accepts max_workers = \"auto\"",
    "returns warning (not error) when max_workers >= 20",
    "throws for invalid brain_planning value",
    "throws for non-boolean haiku_allowed",
    "throws for api mode budget_per_sprint <= 0",
    "collects multiple errors and throws once",
    "returns configured number when max_workers is a number",
    "uses systemProfile.recommendedMaxWorkers when max_workers = \"auto\"",
    "caps auto workers by planLimit when provided",
    "does not cap when recommendedMaxWorkers is less than planLimit",
    "ignores planLimit when max_workers is a numeric value",
    "returns defaults when no config file exists",
    "merges a partial project config over defaults",
    "returns defaults when config file contains malformed JSON",
    "includes projectRoot in the resolved config",
    "throws ConfigValidationError when api mode lacks ANTHROPIC_API_KEY",
    "accepts api mode when ANTHROPIC_API_KEY is set",
    "sets auto_docs defaults when not provided",
    "rejects invalid runtime artifact retention fail-closed: %s",
    "does not throw for an empty partial config",
    "does not throw for a valid partial mode override",
    "throws for an invalid mode in partial config",
    "throws for invalid language in partial config",
    "accepts valid language codes",
    "has the correct name property",
    "includes all errors in the message",
    "exposes errors array on the instance",
    "is instanceof Error",
    "message starts with descriptive prefix",
    "returns a config with default mode",
    "returns a config with all four modes defined",
    "returns a deep clone — mutating result does not affect DEFAULT_MODES",
    "getDefaultConfig returns same shape as createDefaultConfig",
    "getDefaultModes returns all four plan modes"
   ],
   "core/config-execution-budget-policy.test.ts": [
    "authors a visible default worker policy (cold-start can spawn; owner can edit)",
    "deep-merges global role defaults with project TaskKind profiles",
    "accepts structurally valid purpose_admission at config time and defers ceiling cross-checks to runtime",
    "surfaces malformed JSON policy through canonical config validation"
   ],
   "core/config-flag-roundtrip.test.ts": [
    "${name}: config.json → loadConfig → field returns AS-IS",
    "all 11 blocks round-trip together from a single config.json",
    "absent blocks resolve per-contract — opt-in blocks undefined, experience-layer default-ON",
    "partial block defaults missing fields while preserving explicit false",
    "preserves explicit false and unknown extra fields through the real disk resolver",
    "explicit tool_surface { enabled: false } still turns the meta-tool surface OFF (opt-out honored)",
    "[fixture] intentionally-missing field is detected — proves the comparison logic itself works",
    "every block field declared on the real ResolvedConfig interface either round-trips live or is a pinned pre-existing gap"
   ],
   "core/config-global.test.ts": [
    "returns null when global config file does not exist",
    "returns partial config when global config exists",
    "returns null for malformed JSON in global config",
    "returns null for a missing file path",
    "reads nested partial config including projectName",
    "returns defaults when both global and project are null",
    "applies global config over defaults",
    "project config overrides global config",
    "project config overrides global mode",
    "null global config with project config applies project",
    "null project config with global config applies global",
    "merge order: global defaults, project overrides",
    "returns a complete ResolvedConfig with activeModeConfig",
    "throws ConfigValidationError for invalid mode",
    "merges both global and project config fields",
    "canonicalizes global and project model aliases before precedence merge",
    "preserves canonical versioned model IDs byte-for-byte",
    "creates the directory and writes config",
    "overwrites existing global config",
    "writes valid JSON with trailing newline",
    "writes pretty-printed JSON",
    "round-trips with loadGlobalConfig",
    "returns empty object when config file contains {}",
    "reads config with all supported top-level fields",
    "succeeds when directory already exists",
    "saves and reloads an empty config object",
    "preserves nested modes in the written file",
    "both configs are empty objects (not null) — returns defaults",
    "global empty object + project with language — applies language",
    "deep merges nested mode config from global",
    "project config can override a nested mode field from global",
    "projectRoot is always a resolved string path",
    "auto_docs defaults are present when not provided",
    "version defaults to DECKENT_VERSION when not provided",
    "projectName defaults to \"deckent-project\" when not provided",
    "canonicalizes each layer before defaults and preserves precedence",
    "rejects conflicts within a single layer but not across layers",
    "uses defaults when no global and no project config",
    "applies project config when it exists",
    "project config mode is used in loadConfig",
    "sets projectRoot to the resolved directory"
   ],
   "core/config-heal-preimage.test.ts": [
    "backs up the exact preimage and publishes fresh 0600 defaults",
    "holds a valid concurrent revision unchanged and returns it for adoption",
    "holds a corrupt concurrent revision without touching the canonical file"
   ],
   "core/config-heal-race.test.ts": [
    "adopts a valid revision written between parse failure and the locked re-read",
    "keeps canonical JSON valid across a staged crash and unlinks the orphan on the next heal",
    "pins authority outputs to 0600 while a renamed backup preserves its pre-existing mode",
    "serializes two heal contenders so exactly one heals and the successor holds",
    "holds on an EMFILE read error without changing the config or leaving artifacts"
   ],
   "core/config-interrogate.test.ts": [
    "accepts plan.interrogate = true without plan.interrogate errors",
    "accepts plan.interrogate = false without plan.interrogate errors",
    "produces no plan.interrogate errors when plan block is absent (default off)",
    "returns error when plan.interrogate is a non-boolean string",
    "returns error when plan.interrogate is a number",
    "'${key}' has both en and tr translations"
   ],
   "core/config-live-trace.test.ts": [
    "exports the env-twin name (single source — writers import it, no literal drift)",
    "DECKENT_LIVE_TRACE=1 → true, even with no config at all",
    "DECKENT_LIVE_TRACE=1 → true, even when the config block explicitly disables",
    "no env + config { enabled: true } → true (the classic global opt-in still works)",
    "no env + absent/disabled config → false (pre-N5 default pinned: headless stays off)",
    "only the exact string '1' activates the twin — '0'/'true'/'yes' fall through to config"
   ],
   "core/config-metadata.test.ts": [
    "returns metadata for a known key",
    "returns undefined for an unknown key",
    "returns metadata with correct fields for brain_provider",
    "returns metadata for memory_budget with Memory category",
    "describes bounded memory_export without treating it as durable retention",
    "returns metadata for scan_interval with Auditor category",
    "returns metadata for output_mode with options",
    "returns metadata for rollback_policy",
    "returns an object with expected categories",
    "keys within each category are sorted alphabetically",
    "Memory category contains all memory fields",
    "Auditor category contains all auditor fields",
    "every CONFIG_METADATA key appears in some category",
    "returns a non-empty string",
    "contains a top-level heading",
    "contains a Table of Contents section",
    "contains category headings for Provider and Memory",
    "contains key headings for known config fields",
    "contains Description, Type, and Default fields for entries",
    "contains Options field for entries with options",
    "null defaults are shown as `null`",
    "undefined defaults are shown as (not set)"
   ],
   "core/config-migration.test.ts": [
    "returns empty array for a complete config",
    "detects missing top-level fields",
    "does not include modes key in missing fields",
    "preserves existing optional fields that happen to be null",
    "returns false for a full config",
    "returns true for a minimal config",
    "adds missing fields with default values",
    "preserves existing values",
    "returns empty addedFields for already-complete config",
    "does not add fields with undefined defaults",
    "returns error when file does not exist",
    "returns error for invalid JSON",
    "returns migrated=false and no backup for already-complete config",
    "adds missing fields and creates backup for minimal config",
    "preserves existing custom values during migration",
    "dry-run does not modify the file or create backup",
    "backs up legacy model aliases and writes canonical API IDs",
    "adds rollback_policy and fix_phase_enabled for sprint config",
    "detects and adds missing mode-level fields (G: modes migration)",
    "timestamp backup has valid ISO 8601 date format (H: backup naming)",
    "getMissingFields detects missing routing_engine in old config",
    "migrateConfigInMemory adds routing_engine = v3 for old config without it",
    "migrateConfigInMemory upgrades existing routing_engine = v2 to v3",
    "createDefaultConfig includes routing_engine = v3",
    "migrateConfig file adds routing_engine to on-disk config",
    "maps Claude models to correct tiers",
    "maps OpenAI models to correct tiers",
    "maps Gemini models to correct tiers",
    "fails loudly for an unknown migration model",
    "migrates brain_model to model_strategy.brain_tier",
    "migrates default_model to model_strategy.worker_tier",
    "migrates haiku_allowed false to min_tier standard",
    "migrates haiku_allowed true to min_tier economy",
    "migrates brain_provider and worker_provider to providers block",
    "does not overwrite existing model_strategy fields",
    "rejects differing legacy and canonical provider definitions before mutation",
    "deduplicates equal dual definitions and migrates remaining flat aliases",
    "returns migrated=false when no v1 fields to migrate",
    "handles flat v1 config (no modes object)",
    "reports all changes made",
    "returns true for v1 config with brain_provider",
    "returns true for v1 config with haiku_allowed in active mode",
    "returns false when model_strategy and providers already exist",
    "still detects a flat alias when grouped v2 fields already exist",
    "fails without writing or creating a backup",
    "migrateConfigFull applies v2 migration alongside field filling",
    "migrateConfigFull returns empty v2Changes for already-v2 config",
    "migrateConfigV1ToV2 is applied by migrateConfigFull but not by migrateConfigInMemory",
    "needsV2Migration detects v1 config needing v2 upgrade",
    "needsV2Migration returns false for full v2 config",
    "migrateConfigInMemory adds missing fields (simulates what loadConfig triggers)",
    "needsMigration returns false for already-complete config",
    "file-based: loadConfig auto-migrates missing fields on disk",
    "brain_model error uses \"Invalid value X for field Y. Valid: ...\"",
    "default_model error uses consistent format",
    "brain_planning error uses consistent format",
    "brain_provider error uses \"Invalid value X for field Y. Valid: ...\"",
    "worker_provider error uses consistent format",
    "rollback_policy error uses consistent format",
    "mode error message already consistent — unchanged",
    "all enum errors use \"Invalid value\" prefix",
    "needsMigration returns true for minimal config missing many fields",
    "needsMigration returns false for already-full default config",
    "migrateConfig (file-based) adds missing fields and creates backup",
    "migrateConfig dry-run does not write to disk",
    "migrateConfigInMemory preserves existing values while filling defaults",
    "brain_model validation message uses consistent format",
    "brain_provider validation message uses consistent format",
    "brain_planning validation message uses consistent format with valid options listed",
    "rollback_policy validation message uses consistent format",
    "mode validation error includes the invalid value",
    "all enum field errors start with \"Invalid value\" for recognizable format"
   ],
   "core/config-native-migration.test.ts": [
    "reports legacy selection-required without inventing a native target",
    "reports not-applicable when neither legacy nor native selection exists",
    "dry-runs an explicit pair without lock, backup, or mutation",
    "applies an explicit pair atomically and preserves legacy and Brain identities",
    "is byte-and-backup idempotent when the exact native pair is already present",
    "refuses invalid requested selection %j without mutation",
    "rejects runtime-incompatible aliases and fills an existing matching exact half",
    "preserves provider-only and model-only boot configs in ordinary generic migration",
    "refuses an explicit legacy opt-out without mutation",
    "never overwrites an existing same-timestamp backup",
    "reads the apply preimage only after entering the write lock"
   ],
   "core/config-nervous-schema.test.ts": [
    "default config has nervous_system.enabled=false",
    "project config mode override is applied via 3-layer merge",
    "global config overrides defaults, project overrides global",
    "throws ConfigValidationError for invalid nervous_system.mode",
    "throws ConfigValidationError for negative stale_worker.threshold_ms",
    "nervous_system.detectors has exactly 16 entries (5 active + 11 reserve)"
   ],
   "core/config-notification-outbox-fields.test.ts": [
    "(a) a config omitting both fields resolves to the documented defaults",
    "(b) a config setting both fields survives validation with the authored values intact",
    "(c) CONFIG_METADATA carries exactly one entry per new field"
   ],
   "core/config-persona-render.test.ts": [
    "is optional and undefined when not set",
    "accepts persona_render: \"full\"",
    "accepts persona_render: \"guidance\"",
    "ResolvedConfig accepts persona_render",
    "defaults to \"full\" (byte-identical legacy render)",
    "DEFAULT_PROMPT_CONFIG pins persona_render to \"full\"",
    "accepts \"full\" without errors",
    "accepts \"guidance\" without errors",
    "accepts absent persona_render without errors (default resolution applies)",
    "throws a typed ConfigValidationError for an invalid value",
    "a project-layer override merges over defaults, leaving siblings untouched",
    "an absent override resolves to the \"full\" default",
    "a global-then-project two-step merge lets project win"
   ],
   "core/config-reduced-motion.test.ts": [
    "resolves an absent preference as disabled without changing the pinned default shape",
    "deep-merges explicit true and preserves an explicit project false",
    "round-trips the preference through the canonical disk loader",
    "rejects invalid values with the resolved EN/TR catalog message",
    "publishes the optional preference through bilingual config metadata"
   ],
   "core/config-regen-guard.test.ts": [
    "merge preserves user fields — existing values are not overwritten by template",
    "backup is created at .deckent/config.json.bak.regen-{iso} before write",
    "missing-field add — template fields absent from user config are injected",
    "contains the four required safe defaults"
   ],
   "core/config-resource-monitor.test.ts": [
    "block absent → zero behavior change (no resource_monitor on resolved)",
    "valid block with enabled:true passes validation",
    "valid block with all optional fields passes validation",
    "interval_ms below 1000 → validation error",
    "enabled not boolean → validation error",
    "interval_ms not a number → validation error",
    "mergeConfigs passes resource_monitor block through to ResolvedConfig",
    "resource_monitor enabled:false passes validation"
   ],
   "core/config-terminal.test.ts": [
    "createDefaultConfig() provides secure terminal defaults",
    "DEFAULT_TERMINAL_CONFIG exposes the canonical secure defaults",
    "loadConfig() exposes terminal defaults on ResolvedConfig",
    "mergeConfigs() applies project overrides per-key (nested merge)",
    "mergeConfigs(null, null) preserves all default terminal values",
    "deep-merges the explicit startup teaser preference in both directions",
    "deep-merges partial sprint-context limits without introducing a default context",
    "gives a project sprint-context limit precedence over the global layer",
    "rejects a non-boolean startup teaser preference",
    "localizes malformed startup validation diagnostics from the resolved config language",
    "requires finite positive limits before sprint context can be enabled",
    "publishes the startup preference through localized config metadata",
    "publishes the bounded sprint-context contract through config metadata"
   ],
   "core/config-timeout.test.ts": [
    "createDefaultConfig returns correct timeout defaults",
    "throws when effort_base.high < effort_base.normal",
    "throws when effort_base.normal <= effort_base.low",
    "throws when docker_min_timeout < 300",
    "throws when tmux_min_timeout < 300",
    "throws when subprocess_min_timeout < 300",
    "throws when docker_max_timeout > 86400",
    "throws when docker_max_timeout <= docker_min_timeout",
    "accepts default timeout config without errors",
    "accepts custom valid timeout config",
    "partial timeout override merges with defaults",
    "loadConfig resolves timeout with defaults"
   ],
   "core/config-types.test.ts": [
    "accepts ai_planner_timeout as optional number",
    "ai_planner_timeout is optional (undefined when not set)",
    "ai_planner_timeout can represent the default 60000ms",
    "caller can derive effective timeout: config.ai_planner_timeout ?? 60000",
    "ResolvedConfig accepts ai_planner_timeout",
    "ai_planner_timeout is optional in ResolvedConfig",
    "a complete V2 config literal satisfies the interface",
    "is the type of DeckentConfig.nervous_system (single source of truth)",
    "ResolvedConfig.nervous_system uses the same V2 type",
    "NervousSystemConfigV1 is a derived VIEW of V2 — a V2 config is assignable to it",
    "is optional and undefined when not set",
    "accepts true",
    "accepts false",
    "ResolvedConfig accepts retry_transient_failures",
    "validateConfig throws when retry_transient_failures is not a boolean",
    "validateConfig accepts true without errors",
    "validateConfig accepts false without errors",
    "validateConfig accepts absent (undefined) without errors (default-off)",
    "routing is optional and undefined when not set",
    "accepts routing with effort_tiering: true",
    "accepts routing as empty object (effort_tiering default-off)",
    "ResolvedConfig accepts routing block",
    "validateConfig throws when routing is not an object",
    "validateConfig accepts valid routing block without errors",
    "validateConfig accepts absent routing without errors (default-off)"
   ],
   "core/config-validation.test.ts": [
    "returns empty warnings for a valid default config",
    "returns empty warnings when all fields are correct",
    "validates bounded memory_export units and rejects unknown or invalid fields",
    "throws ConfigValidationError for invalid mode",
    "throws ConfigValidationError for invalid language",
    "accepts undefined language (uses default)",
    "throws for max_workers = 0",
    "throws for max_workers = -1",
    "throws for max_workers = 101 (above limit)",
    "throws for max_workers as string (non-auto)",
    "accepts max_workers = \"auto\"",
    "returns warning (not error) for max_workers >= 20",
    "max_workers = 19 does not produce a warning",
    "throws for invalid brain_model",
    "throws for invalid default_model",
    "throws for non-boolean haiku_allowed",
    "throws for numeric haiku_allowed",
    "throws for invalid brain_planning value",
    "accepts valid brain_planning values (ai, structured, auto)",
    "throws for non-positive budget_per_sprint in API mode",
    "throws for negative budget_per_sprint in API mode",
    "throws for non-number budget_per_sprint in API mode",
    "accepts positive budget_per_sprint in API mode",
    "throws for missing mode config entry",
    "accumulates multiple errors in ConfigValidationError.errors",
    "ConfigValidationError contains all error strings in message",
    "accepts max_workers = 1 (lower boundary)",
    "accepts max_workers = 100 (upper boundary) with warning",
    "rejects routing_engine = v1 (ROUTE-V1-PURGE / ADR-G-006)",
    "accepts routing_engine = v3",
    "accepts routing_engine = undefined (omitted)",
    "throws ConfigValidationError for invalid routing_engine",
    "createDefaultConfig includes routing_engine = v3"
   ],
   "core/config-worker-comms.test.ts": [
    "absent block → undefined on resolved config (zero behavior change)",
    "accepts worker_comms with only enabled: true",
    "accepts worker_comms with all optional fields set to valid values",
    "accepts worker_comms enabled: false (disabled but valid block)",
    "returns error when enabled is a non-boolean string",
    "returns error when shared_memory_ttl_ms is not a number",
    "returns error when inject_handoffs is not a boolean",
    "returns error when inject_shared is not a boolean",
    "mergeConfigs passes worker_comms block through to ResolvedConfig",
    "createDefaultConfig base passes validation without worker_comms",
    "TaskResult accepts optional sharedNotes array",
    "TaskResult accepts optional handoffNotes string",
    "TaskResult is valid without sharedNotes or handoffNotes (backward compat)",
    "sharedNotes supports multiple key-value entries"
   ],
   "core/config-write-authority.test.ts": [
    "publishes the exact formatted JSON and leaves no temporary artifact",
    "opens the temporary file as r+ and fsyncs that descriptor before publication",
    "creates the published config with mode 0600 on POSIX",
    "removes the temporary file and rethrows when publication fails",
    "never exposes partial JSON to a parallel reader of a large replacement",
    "releases one owner before the next acquisition proceeds",
    "takes over a stale lock whose owner pid is dead",
    "times out with the typed error while a live owner holds the lock"
   ],
   "core/config.test.ts": [
    "returns valid DeckentConfig with mode === DEFAULT_MODE",
    "returns fresh copy on each call (mutation safe)",
    "ships disabled, bounded runtime artifact retention defaults",
    "contains 4 modes: performance, balanced, economic, api",
    "performance: max_workers=8, brain_model=opus",
    "api: budget_per_sprint=5.0, requires=ANTHROPIC_API_KEY",
    "returns defaults when no config files exist",
    "applies project config mode override",
    "merges global and project config",
    "deep-merges a project runtime artifact retention override and preserves defaults",
    "preserves the canonical frozen provider-limit authority envelope after interpolation",
    "deep merges nested mode config",
    "carries top-level max_workers through resolution (B-MAXWORKERS-WIRE not-a-dead-knob)",
    "throws ConfigValidationError for API mode without env var",
    "resolves successfully for API mode with env var set",
    "returns defaults for malformed JSON (readJsonSafeAsync returns null)",
    "resolves projectRoot from parameter",
    "default modes have brain_planning === \"auto\"",
    "accepts valid brain_planning values",
    "rejects invalid brain_planning value",
    "accepts { mode: \"economic\" }",
    "accepts empty object (merges with defaults)",
    "rejects invalid mode",
    "rejects max_workers=0",
    "accepts max_workers=100 (warn only, not error)",
    "rejects max_workers=101 (exceeds 100 limit)",
    "rejects invalid brain_model",
    "accepts max_workers=\"auto\"",
    "returns configured number when max_workers is numeric",
    "auto mode + 16GB RAM + 8 cores → ~7 workers",
    "auto mode + low RAM (1600MB) + 8 cores → 4 workers",
    "auto mode + very low RAM (400MB) + 4 cores → 1 worker",
    "auto mode respects planLimit when provided",
    "auto mode planLimit higher than recommended → use recommended",
    "numeric mode ignores systemProfile",
    "numeric mode ignores planLimit",
    "top-level config.max_workers (numeric) overrides activeModeConfig.max_workers",
    "top-level config.max_workers absent → activeModeConfig is read (behavior preserved)",
    "top-level numeric override ignores systemProfile and planLimit",
    "top-level config.max_workers === 'auto' takes the auto path",
    "top-level non-numeric / out-of-range value falls through to activeModeConfig",
    "max_workers=50 returns warning (not error)",
    "max_workers=19 returns no warning",
    "max_workers=101 throws ConfigValidationError",
    "max_workers=\"auto\" returns no warning",
    "maps max_plan to performance",
    "maps max5x_plan to balanced",
    "maps pro_plan to economic",
    "maps unlimited to api",
    "has exactly 4 aliases",
    "resolves legacy 'max_plan' to 'performance'",
    "resolves legacy 'max5x_plan' to 'balanced'",
    "resolves legacy 'pro_plan' to 'economic'",
    "resolves legacy 'unlimited' to 'api'",
    "passes through canonical name performance unchanged",
    "passes through canonical name balanced unchanged",
    "passes through canonical name economic unchanged",
    "passes through canonical name api unchanged",
    "returns unknown mode string as-is",
    "returns 'opus' for performance mode",
    "returns 'sonnet' for balanced mode",
    "returns 'sonnet' for economic mode",
    "returns 'opus' for api mode",
    "falls back to balanced brain_model for undefined config",
    "falls back to balanced brain_model for null config",
    "falls back to balanced brain_model for an unknown/corrupt mode string",
    "falls back to balanced brain_model when modes is present but missing the referenced mode key",
    "does not throw for a malformed config object",
    "returns 'opus' for performance mode",
    "returns 'opus' for balanced mode",
    "returns 'sonnet' for economic mode",
    "returns 'sonnet' for api mode",
    "falls back to balanced default_model for undefined config",
    "falls back to balanced default_model for null config",
    "falls back to balanced default_model for an unknown/corrupt mode string",
    "falls back to balanced default_model when modes is present but missing the referenced mode key",
    "does not throw for a malformed config object",
    "resolves legacy alias 'max_plan' in project config to 'performance'",
    "resolves legacy alias 'max5x_plan' in project config to 'balanced'",
    "resolves legacy alias 'pro_plan' in project config to 'economic'",
    "resolves legacy alias 'unlimited' in project config to 'api' (with API key)",
    "contains every provider represented by PROVIDER_MODEL_MAP",
    "default config has providers.brain=claude (grouped canonical)",
    "default config has providers.worker=claude (grouped canonical)",
    "default config has cost_optimization=false",
    "default config has no fallback_provider",
    "default config has no provider_overrides",
    "default config has no api_keys",
    "accepts valid brain_provider=codex",
    "accepts valid worker_provider=gemini",
    "accepts valid fallback_provider=claude",
    "rejects invalid brain_provider",
    "rejects invalid worker_provider",
    "rejects invalid fallback_provider",
    "accepts a complete role-aware provider fallback policy",
    "rejects malformed provider_fallback: %s",
    "accepts valid provider_overrides",
    "rejects invalid provider in provider_overrides",
    "accepts cost_optimization=true",
    "rejects non-boolean cost_optimization",
    "accepts api_keys as object",
    "rejects api_keys as non-object",
    "DECKENT_BRAIN_PROVIDER overrides config brain_provider",
    "DECKENT_WORKER_PROVIDER overrides config worker_provider",
    "invalid DECKENT_BRAIN_PROVIDER env var causes validation error",
    "invalid DECKENT_WORKER_PROVIDER env var causes validation error",
    "DECKENT_MODE overrides config mode",
    "DECKENT_MODE resolves aliases",
    "DECKENT_LANGUAGE overrides config language",
    "project config overrides brain_provider",
    "preserves a flat-only global provider through grouped defaults",
    "preserves a flat-only project provider through grouped defaults",
    "applies project grouped provider over a canonicalized global flat provider",
    "fails loudly with global-layer provenance for differing dual definitions",
    "fails loudly with project-layer provenance for differing dual definitions",
    "keeps environment provider override as the highest precedence",
    "default config has output_splash=true",
    "default config has output_mode=normal",
    "default config has output_theme=default",
    "default config has search_enabled=true",
    "default config has search_provider=context7",
    "default config has search_cache_ttl=3600",
    "default config has notify_on_complete=false",
    "default config has notify_channel=null",
    "default config has notify_url=null",
    "default config has telemetry_enabled=false",
    "default config has telemetry_anonymous=true",
    "default config has detected_env=null",
    "default config has multi_ide_mode=false",
    "default config has auth_mode=subscription",
    "default config has skill_routing=undefined",
    "default config has runtime_extension_enabled=true",
    "honors explicit false override via validatePartialConfig deepMerge",
    "explicit true override is preserved (idempotent flip)",
    "accepts output_mode=quiet via partial config",
    "accepts output_mode=verbose via partial config",
    "accepts output_theme=rich via partial config",
    "accepts output_theme=minimal via partial config",
    "accepts search_provider=web via partial config",
    "accepts search_provider=none via partial config",
    "accepts notify_channel=slack via partial config",
    "accepts notify_channel=null via partial config",
    "accepts auth_mode=api via partial config",
    "accepts auth_mode=hybrid via partial config",
    "accepts skill_routing with provider values",
    "accepts skill_routing with null values",
    "existing config files load without error when new fields absent",
    "detected_env accepts all valid enum values",
    "keeps both Codex prompt flags default-on after the owner canary decision",
    "continues to accept valid configs carrying either boolean value",
    "publishes localized help and generated-reference metadata for both keys",
    "default config has memory_budget=5000 (Sprint 140 pre-flight 5.5x increase)",
    "default config has decay_after_sprints=20 (Sprint 140 pre-flight 4x increase)",
    "default config has patterns_enabled=true",
    "default config has project_identity_enabled=true",
    "default config has scan_interval=30",
    "default config has heartbeat_timeout=120",
    "default config has boundary_enforcement=true",
    "default config has fix_phase_enabled=true",
    "default config has max_fix_retries=2",
    "default config has a scale-aware post-FIX circuit breaker",
    "default config has bounded lifecycle recovery containment",
    "default config has rollback_policy=never",
    "rejects memory_budget below 100",
    "rejects memory_budget above 10000",
    "accepts valid memory_budget=1000",
    "rejects decay_after_sprints=0",
    "rejects non-boolean patterns_enabled",
    "rejects scan_interval below 5",
    "rejects heartbeat_timeout below 30",
    "rejects non-boolean boundary_enforcement",
    "rejects non-boolean fix_phase_enabled",
    "rejects max_fix_retries above 10",
    "rejects invalid post-FIX circuit-breaker thresholds",
    "accepts a valid post-FIX circuit-breaker override",
    "accepts valid lifecycle recovery timings",
    "rejects lifecycle polling wider than its containment window",
    "rejects invalid rollback_policy",
    "accepts rollback_policy=on_failure",
    "accepts rollback_policy=always",
    "has entries for all new config fields",
    "has at least 30 entries",
    "every entry has description, type, default, category",
    "returns grouped config by category",
    "Provider category has brain_provider entry",
    "Memory category has memory_budget entry",
    "Auditor category has scan_interval entry",
    "Sprint category has fix_phase_enabled and rollback_policy",
    "resolved config includes memory_budget from defaults (Sprint 140 pre-flight)",
    "resolved config includes fix_phase_enabled from defaults",
    "resolved config includes the post-FIX circuit breaker from defaults",
    "resolved config includes lifecycle recovery containment from defaults",
    "resolved config includes rollback_policy from defaults",
    "project config overrides memory_budget",
    "A) default config has routing_engine=v3",
    "A) loadConfig returns routing_engine=v3 from defaults (no config file)",
    "A) project config migrates routing_engine v2 to v3",
    "A) validateConfig accepts routing_engine=v3",
    "A) validateConfig rejects routing_engine=v1 (ROUTE-V1-PURGE / ADR-G-006)",
    "A) validateConfig rejects invalid routing_engine value",
    "A) routing_config is propagated to ResolvedConfig when set",
    "A) cleanup_delay_ms is propagated with default 180000",
    "A) project config can override cleanup_delay_ms",
    "accepts a valid autonomous block",
    "rejects enabled=non-boolean",
    "rejects interval_ms=-1",
    "rejects pool_size=0",
    "rejects pool_size=1.5 (non-integer)",
    "accepts a valid reactive block",
    "rejects non-boolean reactive.enabled",
    "rejects non-string reactive.map_path",
    "accepts a valid work_generator block",
    "defaults work_generator to disabled",
    "rejects non-boolean work_generator.enabled",
    "rejects negative work_generator.interval_ms",
    "accepts a valid rbac_policy block",
    "defaults rbac_policy to disabled with role viewer",
    "rejects non-boolean rbac_policy.enabled",
    "rejects an unknown rbac_policy.role",
    "accepts a valid enabled block",
    "has no default — block absent means today\\'s behavior (no throw)",
    "rejects non-boolean enabled",
    "rejects enabled block with an empty issuer",
    "rejects enabled block with an empty key",
    "rejects an unknown algorithm",
    "rejects enabled block with algorithm missing",
    "rejects non-string audience",
    "accepts a DISABLED block with empty issuer/key (gating applies only when enabled)",
    "PlanMode type accepts new user-friendly tier names",
    "PlanMode type also accepts legacy tier names (backward compat)",
    "legacy aliases resolve to canonical names",
    "loadConfig migrates legacy alias to canonical name",
    "DEFAULT_MODES contains all 4 canonical tiers",
    "config merge preserves tier settings with canonical alias as mode",
    "accepts a valid cross_verify config with real provider names (regression pin)",
    "accepts a cross_verify config with no verifier_priority/verifier_model set",
    "rejects a typo\\'d provider name in verifier_priority ([\"cursro\"]) as a typed config error",
    "rejects a mix of valid and typo\\'d provider names in verifier_priority",
    "still rejects a non-string entry in verifier_priority (existing shape check preserved)",
    "rejects an unknown provider key in verifier_model",
    "still rejects an empty verifier_model value for a known provider (existing shape check preserved)",
    "validates exact-pair verifier tier authority carried by cross_verify",
    "rejects malformed verifier tier authority during config validation",
    "rejects unknown verifier tier authority fields during config validation"
   ],
   "core/confirmation-acceptance-index.test.ts": [
    "uses a durable tenant/project/status shard and fresh-reads exact lineage",
    "backfills losslessly and idempotently without making reads migrate or scan canonical directories",
    "bounds reads and projects corrupt or foreign pointers without writes or tenant leakage"
   ],
   "core/confirmation-acceptance-lineage.test.ts": [
    "pins tenant/attempt/generation/source parity and settlement integrity",
    "rejects incomplete acceptance authority without publishing while retaining the generic writer",
    "fresh-reads %s expiry as distinct canonical terminal truth"
   ],
   "core/confirmation-expiry-park.integration.test.ts": [
    "mints from EVALUATE, expires FWW to UNDECIDABLE, and prevents CLI/direct revival"
   ],
   "core/confirmation-lifecycle.test.ts": [
    "holds creation fail-closed when lifecycle policy is absent or disabled",
    "writes canonical v2 with embedded profile, digest, expiry and security risk floor",
    "keeps identical identity idempotent and requires an explicit successor after expiry",
    "parks timeout as UNDECIDABLE and rejects a late decision",
    "quarantines corrupt pending bytes with a typed visible receipt",
    "reads legacy 16-hex records side-effect-free and pins their exact source-byte digest"
   ],
   "core/constants.test.ts": [
    "are all non-empty strings",
    "SPRINT_STATE_FILE / SPRINT_ACTIVE_FILE are derived from DECKENT_DIR",
    "are all positive numbers",
    "AUDITOR_SCAN_INTERVAL_MS === 30_000 (Blueprint 5.2)",
    "HEARTBEAT_STALE_THRESHOLD_MS === 120_000 (Blueprint 5.2)",
    "are all positive integers",
    "MEMORY_MAX_LINES === 1500 (Sprint 140 pre-flight 5x increase)",
    "RETRO_MAX_LINES === 400 (Sprint 140 pre-flight 3.3x increase)",
    "SPRINT_LOG_MAX_LINES === 500 (Sprint 140 pre-flight 5x increase)",
    "BRAIN_TOTAL_LINE_BUDGET === 5000 (Sprint 140 pre-flight 5.5x increase)",
    "MEMORY_DECAY_SPRINTS === 20, PATTERN_DECAY_SPRINTS === 25 (Sprint 140 pre-flight 2.5x/2x increase)",
    "BRAIN_TOTAL_LINE_BUDGET >= sum of individual limits",
    "MEMORY_DECAY_SPRINTS < PATTERN_DECAY_SPRINTS",
    "decay sprints are reasonable for 35+ sprint projects",
    "individual file limits fit within total budget",
    "DEBT_HIGH_PRIORITY_SPRINTS === 2 (Blueprint 8)",
    "DEBT_CRITICAL_SPRINTS === 3 (Blueprint 8)",
    "SUPPORTED_LANGUAGES includes en and tr",
    "DEFAULT_MODE === performance",
    "DEFAULT_LANGUAGE === en",
    "DECKENT_VERSION matches package.json version",
    "DECKENT_VERSION is a valid semver string",
    "TMUX_SESSION_NAME === deckent",
    "TMUX_WORKER_PREFIX === w-",
    "other tmux constants are non-empty strings",
    "BRAIN_PLAN_TIMEOUT_MS === 900_000",
    "contains expected extensions"
   ],
   "core/cost-calculator-null-output.test.ts": [
    "api: outputTokens:null → cost from input/cache side + outputUnmeasured:true",
    "api: outputTokens:undefined is also treated as unmeasured (not a real 0)",
    "subscription: outputTokens:null → input/cacheWrite burn + outputUnmeasured:true",
    "real numeric output → NO under-count marker, value byte-identical to pre-fix arithmetic",
    "local regime carries the unmeasured flag too (value stays $0)",
    "unknown model carries the unmeasured flag too ($0, honest source)"
   ],
   "core/cost-calculator-regime.test.ts": [
    "maps subscription/api/local directly and free_tier → local",
    "counts cacheRead as ZERO weight (cacheRead never affects the burn)",
    "prices cacheWrite at 1.25×input",
    "full formula = in·$in + out·$out + cacheWrite·1.25·$in",
    "pulls per-model in/out price from the REGISTRY, not the cost-config",
    "charges cacheRead (discounted) — unlike subscription, it is NOT free",
    "measures the cache-hit ratio from real token counts (never assumed)",
    "falls back to archetype-B cache weights when the config has no per-model cache price",
    "local regime is always $0 regardless of tokens",
    "unknown model → $0 with an honest unknown-model pricing source (never silently priced)",
    "falls back to cost-config price for a model absent from the registry",
    "cost-calculator.ts contains neither DEFAULT_CACHE_HIT_RATIO=0.70 nor DEFAULT_CACHEABLE_CONTEXT=8000"
   ],
   "core/cost-calculator.test.ts": [
    "calculates Opus API cost with correct units",
    "calculates output cost",
    "uses gpt-5.6-sol explicit registry cache-read pricing with fallback-equivalent cost",
    "applies cache read discount (90% savings)",
    "applies cache creation when cache miss",
    "applies retry multiplier to all token counts",
    "naive is 70% of realistic",
    "worst case is 160% of realistic",
    "handles Opus subs + GPT-5 API + Gemini free",
    "aggregates task counts per provider",
    "warns when task exceeds context window",
    "passes when task fits in 1M Opus context",
    "marks withinBudget false when exceeds",
    "marks withinBudget true when under",
    "warns on unknown model",
    "still counts known models when some are unknown",
    "produces human-readable output with all sections",
    "shows subscription impact for subs billing",
    "estimates 409 task analysis sprint realistically",
    "resolves ollama task to billingMode=local with $0 cost",
    "renders \"(local)\" — not \"(subscription)\" or \"(free tier)\" — in the formatted estimate",
    "maps ollama → local regardless of auth mode",
    "maps subscription auth → subscription, api auth → api",
    "keeps unknown/custom providers remote-by-default and follows explicit auth",
    "defers to provider default (undefined) for hybrid/unknown auth",
    "subscription-auth codex task costs $0 even though cost-config default is api",
    "api-auth codex task still bills USD (no false zeroing)",
    "uses explicit per-attempt auth before the provider catalog default",
    "uses configured free-tier/local authority only when attempt auth is absent",
    "keeps hybrid and unknown-model billing unresolved instead of fabricating API spend"
   ],
   "core/cost-config-loader.test.ts": [
    "accepts valid per-token cost (Opus input)",
    "accepts valid per-token cost (Opus output)",
    "accepts zero (free tier)",
    "rejects per-MTok value (1,000,000× unit error)",
    "rejects per-MTok value at threshold",
    "rejects negative cost",
    "accepts cost at threshold (0.01)",
    "accepts valid config",
    "rejects missing _version",
    "rejects missing providers",
    "rejects missing cost_limits",
    "rejects negative sprint_max_usd",
    "rejects per-MTok pricing (unit safety)",
    "rejects invalid billing_modes_supported",
    "loads from .deckent/cost-config.json when present",
    "falls back to bundled baseline when user config missing",
    "bundled baseline ships ollama as a zero-cost local provider (fresh user sees \"(local)\") — İŞ4",
    "throws CostConfigError on malformed JSON",
    "finds model by direct ID",
    "finds model by alias",
    "returns null for unknown model",
    "skips disabled providers",
    "lists all enabled models",
    "excludes disabled models",
    "formats Opus input cost",
    "formats Sonnet output cost",
    "formats Haiku cache read cost",
    "creates .deckent/cost-config.json from baseline",
    "does not overwrite existing file without force",
    "overwrites with force",
    "baseline config passes unit safety for all models",
    "baseline has all 3 providers enabled",
    "baseline has subscription tracking documented for all providers"
   ],
   "core/cost-gate-spend.test.ts": [
    "returns 0 when the log is empty",
    "returns 0 when file contains only Docker-stats lines (no costUsd field)",
    "sums costUsd entries within the day window",
    "excludes entries from previous days when window=day",
    "sums all entries in the current month when window=month",
    "ignores entries with non-numeric or zero costUsd",
    "skips malformed JSON lines without throwing",
    "skips blank lines",
    "returns null when enforce_spend_gate is false (default-off)",
    "returns null when enforce_spend_gate is not set (default-off)",
    "returns null when projected spend is within daily limit (flag-on, under threshold)",
    "returns COST_LIMIT_WARN when projected daily spend exceeds daily_max_usd (flag-on)",
    "returns null when monthly_max_usd is not set (no monthly check)",
    "returns COST_LIMIT_WARN for monthly window when daily is within limit but monthly exceeds",
    "returns daily warn (not monthly) when both windows exceed limits",
    "sprint is never blocked — checkSpendGate only returns a warn, never throws or returns ok=false"
   ],
   "core/cost-gate.test.ts": [
    "passes (ok=true) when estimate is within budget",
    "returns COST_GATE_EXCEEDED when estimate is over budget",
    "bypasses the budget block when acknowledgeCost=true (overrideApplied set)",
    "sets autoConfirm=true when realistic cost is at or below the auto-confirm threshold",
    "sets autoConfirm=false when realistic cost exceeds the auto-confirm threshold",
    "falls back to DEFAULT_AUTO_CONFIRM_THRESHOLD_USD when not set in config",
    "handles an empty task list without crashing (estimate = 0, within budget)",
    "blocks unknown remote pricing even when acknowledgeCost is true",
    "admits a registry-known subscription model without charging the USD budget",
    "admits API billing for the same model with official pricing evidence",
    "estimates a dynamic remote model only with explicit pricing evidence",
    "emits a structured COST_GATE_EXCEEDED payload from an exceeded result",
    "defaults the override hint to \"acknowledgeCost\" when not specified",
    "supports an explicit \"force\" override hint for CLI parity",
    "surfaces pricing evidence as the only remedy for unknown remote cost",
    "blocks when estimate exceeds per-request budget even if within sprint budget",
    "uses sprint budget when it is smaller than per-request budget",
    "passes when estimate is within both sprint and per-request budgets",
    "acknowledgeCost=true bypasses per-request budget too (overrideApplied)",
    "no budget field → existing sprint-budget behavior unchanged",
    "ceilingTripped=sprint for pure sprint-budget block",
    "ceilingTripped=usd for per-request maxUsd block",
    "blocks when estimated tokens exceed per-request maxTokens",
    "passes when estimated tokens are within per-request maxTokens",
    "ceilingTripped=tokens even when USD is also over (tokens checked first)",
    "acknowledgeCost=true bypasses token ceiling (overrideApplied)",
    "no maxTokens field → existing behavior unchanged (backward-safe)",
    "estimatedTokens and budgetTokens are populated on token block",
    "all-ollama sprint passes the gate at $0 with auto-confirm, even on a $0.01 budget"
   ],
   "core/cost-model-label.test.ts": [
    "label preserves the exact canonical registry API ID",
    "uses an exact canonical cost-config key without alias normalization",
    "tier is correctly mapped from cost-config regardless of display label",
    "provider prefix is correct in model distribution output"
   ],
   "core/coverage-gate-split.test.ts": [
    "(a) defaults: coverage_hard_floor=50 + coverage_aspirational=90",
    "(b) adjustment mutates aspirational only — hard_floor untouched",
    "(c) immutable floor: aspirational clamped at hard_floor, never below",
    "(d) legacy coverage_threshold maps to coverage_aspirational"
   ],
   "core/cred-scrub-real-leak-site.test.ts": [
    "every provider identity sees ONLY its own key across a full 6-provider + config-driven mixed fleet",
    "a provider with no override map entry (e.g. subscription-mode / no .deck secret) gets NO credential key at all",
    "never mutates the hostEnv snapshot passed in",
    "removes every key in scrubKeys, keeps everything else",
    "never mutates the input hostEnv",
    "scrubs foreign keys then re-injects ownEnv on top",
    "with no ownEnv, the child gets NO credential key at all (subscription/session auth fallback)"
   ],
   "core/credential-decision-audit.test.ts": [
    "resolves outside an injected project root using a DECKENT_HOME-style override",
    "resolves under the platform global state dir (no project root involved at all)",
    "persists a redacted reference (env var name + sha256 prefix) and never the raw secret value",
    "rejects a granted decision with no secret reference",
    "persists no redactedRef for denial reason \"%s\"",
    "rejects a denied decision that carries a secret reference",
    "rejects a taskId that references a .deck path",
    "rejects a non-canonical occurredAt timestamp",
    "rejects an unrecognized reason",
    "requires a non-empty filePath at construction",
    "chains multiple appends and verifies intact across sink restarts (durable across processes)",
    "detects a corrupted record and reports the exact broken index",
    "returns an empty array when the file has not been created yet"
   ],
   "core/credential-encryption.test.ts": [
    "roundtrip: encrypts and decrypts correctly",
    "encrypted payload has iv, ciphertext, and tag as hex strings",
    "each encryption produces different ciphertext (unique IV)",
    "handles empty string",
    "handles unicode content",
    "rejects invalid key length for encrypt",
    "rejects invalid key length for decrypt",
    "decrypt with wrong key throws CredentialEncryptionError",
    "tampered ciphertext throws CredentialEncryptionError",
    "tampered tag throws CredentialEncryptionError",
    "auto-generates keyring file on first call",
    "returns same key on subsequent calls",
    "reads key from DECKENT_MASTER_KEY env var",
    "env var takes priority over keyring file",
    "throws on invalid env var length",
    "throws on invalid keyring file content",
    "creates parent directory for keyring if needed",
    "returns true for valid encrypted entry",
    "returns false for plaintext entry",
    "returns false for null/undefined",
    "returns false for malformed encrypted field",
    "stores encrypted credential and retrieves plaintext",
    "stored file does NOT contain plaintext key",
    "reads legacy plaintext entries (backward compat)",
    "save re-encrypts legacy entry on overwrite",
    "getCredentialEntry decrypts encrypted entries",
    "works with encryption disabled (plaintext mode)",
    "listCredentials works with encrypted entries",
    "deleteCredential works with encrypted entries"
   ],
   "core/credentials-harden.test.ts": [
    "getCredential throws (not the wrong secret) after two entries are swapped in the file",
    "happy path: new entries still round-trip normally (AAD does not break correct reads)",
    "an entry encrypted without AAD (pre-349-003 format) still decrypts correctly",
    "a mixed file (legacy + AAD-bound entries) reads both correctly",
    "leaves no .tmp leftover after a successful write",
    "an injected rename failure leaves the original store untouched and cleans up the tmp file"
   ],
   "core/credentials-per-project.test.ts": [
    "round-trip: get returns exactly what was set, within one project",
    "writes an encrypted file with no plaintext secret in the bytes",
    "getCredential returns null for a key that was never set",
    "getCredential returns null when the project has no credentials.enc yet",
    "stores multiple independent keys within the same project",
    "overwriting a key updates the value on subsequent get",
    "sibling-project cross-read FAILS: project B cannot decrypt project A credentials.enc",
    "rejects an empty credential value",
    "rejects an empty key name",
    "rejects a projectRoot that does not exist",
    "throws on a corrupt (non-JSON) credentials.enc file"
   ],
   "core/credentials.test.ts": [
    "stores a credential file for a provider",
    "writes the correct key to the file",
    "creates the credentials directory if it does not exist",
    "sets file permissions to 0600",
    "overwrites an existing credential",
    "throws CredentialStorageError for empty provider name",
    "throws CredentialStorageError for empty key",
    "sanitizes provider names with special characters",
    "stores storedAt as valid ISO 8601 timestamp",
    "writes valid JSON with trailing newline",
    "stores multiple providers independently",
    "returns null for a non-existent provider",
    "returns null for empty provider name",
    "returns the stored key after storeCredential",
    "returns null when credentials dir does not exist",
    "returns null when file contains invalid JSON",
    "returns null when file is missing key field",
    "returns empty array when no credentials stored",
    "returns empty array when credentials dir does not exist",
    "lists a single stored provider",
    "lists multiple providers",
    "excludes non-.json files",
    "skips files with invalid JSON and does not include null",
    "returns updated list after deletion",
    "returns false when provider does not exist",
    "returns false for empty provider name",
    "deletes an existing credential and returns true",
    "credential is no longer accessible after deletion",
    "does not affect other credentials when deleting one",
    "returns false when provider does not exist",
    "returns false for empty provider name",
    "returns true after storing a credential",
    "returns false after deleting a credential",
    "returns null for non-existent provider",
    "returns full entry with provider, key, storedAt",
    "returns null for empty provider",
    "storedAt in entry is a valid ISO date",
    "throws CredentialNotFoundError if credential does not exist",
    "updates the key for an existing credential",
    "preserves the provider name after update",
    "storeCredential is a function",
    "getCredential is a function",
    "listCredentials is a function",
    "listCredentials returns an array",
    "getCredential returns null for a non-existent global provider",
    "has correct name property",
    "includes provider name in message",
    "is an instance of Error",
    "has correct name property",
    "includes message",
    "is an instance of Error"
   ],
   "core/criteria-deriver.test.ts": [
    "does not project repository-wide commands into task acceptance",
    "does not project discovered test files into task acceptance",
    "does not project multiple discovered test files into task acceptance",
    "does not project a build command when no testFiles are known",
    "falls back to stack-neutral phrase when no commands at all",
    "uses generic phrase for generic stack with no commands",
    "does not include typecheck when both typecheck and build are discovered",
    "does not project build when typecheck is absent",
    "does not project test when typecheck is an empty string",
    "does not project typecheck when build is absent",
    "documentation kind ignores test commands entirely",
    "audit kind produces findings-based criteria",
    "returns true for non-build kinds",
    "returns false for code kinds",
    "strips well-formed **Kanıt:** prefix",
    "strips asymmetric *Kanıt:** prefix (single leading star — the artifact)",
    "strips list-marker + bold prefix: - **Proof:** content",
    "strips list marker without bold: - `grep foo`",
    "leaves plain content unchanged",
    "strips **Verify:** prefix",
    "trims surrounding whitespace"
   ],
   "core/cross-verify-adjudication.test.ts": [
    "creates a canonical, content-addressed, deeply immutable contract",
    "rejects duplicate assertions, dependency cycles, and unknown evidence references",
    "rejects contract digest substitution",
    "preserves the exact host parse rejection instead of reparsing null",
    "fails closed when a caller supplies both a parsed response and a parse error",
    "derives CONFIRMED only when every assertion has required host-bound evidence",
    "derives REFUTED from a cited contradiction and UNCLEAR from an exact missing map",
    "inverts NO-GO polarity without rewriting the authored statement",
    "rejects a provider-authored top-level verdict field instead of treating it as authority",
    "fails closed for missing, duplicate, and unknown assertion results",
    "fails closed on claim/manifest digest mismatch",
    "never confirms an incomplete execution: %s",
    "fails closed when the provider terminal token disagrees with the host derivation",
    "does not mutate caller-owned inputs while validating",
    "accepts the canonical evidenceSha256 unchanged",
    "accepts contentSha256 as a parse-boundary alias, normalizing to evidenceSha256",
    "accepts both keys when their digests are identical, emitting only the canonical field",
    "fails closed when evidenceSha256 and contentSha256 disagree",
    "re-exports the canonical reason limit and accepts its exact boundary only"
   ],
   "core/cross-verify-evidence-broker.test.ts": [
    "validates exact portable relative paths without normalizing aliases",
    "fails Windows and incomplete native adapters honestly with a typed error",
    "reads immutable evidence bytes instead of changed project bytes and rejects missing or corrupt blobs",
    "does not admit caller-forged immutable evidence capabilities or read their mutable fallback",
    "rejects JSON, accessor and proxy read ports without evaluating attacker hooks",
    "publishes a deterministic attempt-scoped snapshot outside the project",
    "fails closed when a reused host-decoded artifact is tampered",
    "enforces first-writer claim and manifest authority",
    "rejects traversal, symlink escapes, hard links, and bounded-byte overflow",
    "publishes only host-adjudicated verdict receipts bound to raw output digests",
    "shares the canonical raw byte ceiling at ASCII and worst-case Unicode boundaries",
    "publishes the host verdict receipt after the settlement is terminally closed",
    "fails closed on an open settlement and accepts idempotent replay once closed",
    "detects receipt tampering and refuses receipt-path symlink replacement",
    "keeps receipt JSON bounded independently of source content limits",
    "cuts a content-addressed slice from the pinned decoded blob",
    "rejects a range beyond the source line count with a typed error"
   ],
   "core/cross-verify-execution-contract.test.ts": [
    "produces one canonical content-addressed and deeply immutable contract",
    "changes content evidence without changing the stable attempt identity fields",
    "rejects integrity substitution and settlement-attempt drift",
    "fails loudly on absent budget authority and unsupported backend identity",
    "binds one typed claim, evidence snapshot, finite prompt, and output policy",
    "rejects prompt overflow and mutable evidence access",
    "detects semantic evidence substitution after contract creation"
   ],
   "core/cross-verify-prompt.test.ts": [
    "includes adversarial framing — instructs verifier to REFUTE not confirm",
    "injects the task goCriteria into the prompt",
    "injects the task noGoCriteria into the prompt",
    "injects files changed from the result",
    "includes the task title and description",
    "includes the mandatory VERDICT format instruction",
    "labels the verifier provider when opts.verifier is provided",
    "handles a task with no title or description gracefully",
    "handles an empty filesChanged array with a readable placeholder",
    "uses exact authored read files, dedupes them, and excludes self-reported extras",
    "renders a finite criteria-only protocol without repository-wide or repeated verification triggers",
    "fails host-truncated material fields directly to terminal UNCLEAR without tools",
    "defaults sprint callers to present-tense implementation verification",
    "renders claim adjudication without requiring future milestone behavior to exist now",
    "rejects competing criteria blocks leaked into Description or Worker Notes",
    "rejects unresolved render placeholders instead of sending them to a verifier",
    "treats evidence-file instructions and embedded verdicts strictly as data",
    "does not infer absence from an empty diff when committed exact-file evidence exists",
    "marks title truncation explicitly and forces UNCLEAR before evidence access",
    "binds the exact typed contract to a finite read-only broker protocol",
    "holds before dispatch when the immutable contract exceeds the prompt ceiling",
    "parses the exact two-line response while leaving verdict authority to the host",
    "re-exports the canonical complete-response ceiling and preserves supported CONFIRMED output",
    "accepts exact reason and response ceilings while rejecting each ceiling plus one",
    "rejects an overlong assertion reason with the exact schema path",
    "fails closed on extra prose, provider verdict injection, and oversize output",
    "parses a REFUTED verdict (standard form)",
    "parses a CONFIRMED verdict (standard form)",
    "parses an explicit UNCLEAR verdict as a terminal honest result",
    "is case-insensitive for the VERDICT keyword and status",
    "returns unclear when no VERDICT line is present",
    "returns unclear for empty output",
    "returns unclear for whitespace-only output",
    "extracts the reason text after the status word correctly",
    "includes a truncated output excerpt in unclear reason for debugging",
    "requires the verdict to be the last non-empty line",
    "accepts only the final Claude assistant line, not user prompt or usage echoes",
    "accepts a completed Codex agent_message and a Gemini response envelope",
    "rejects plain log text and assistant output that continues after the verdict",
    "requires an assistant role for OpenAI messages",
    "invalidates an earlier verdict when a later assistant message continues working",
    "invalidates an earlier verdict when a later assistant envelope contains only tool use",
    "keeps a terminal assistant verdict across later ineligible envelopes",
    "keeps a streamed OpenAI verdict across its empty finish marker",
    "reads the archived sprint-460 refusal as model-not-found, verbatim",
    "does not treat the non-fatal codex metadata warning as a refusal",
    "yields nothing when the verifier spoke — a run that died is not a refusal",
    "classifies auth and rate-limit refusals from status, not wording",
    "reads a Claude stream-json error result the same way",
    "refuses to classify without a status ≥ 400 or a named error class",
    "survives unparseable input without throwing",
    "reports the last refusal when a provider emits several"
   ],
   "core/cross-verify-response-limits.test.ts": [
    "defines the exact canonical budgets and their arithmetic invariant",
    "safely bounds UTF-8 output at the complete JavaScript-character limit",
    "exposes an immutable limits object"
   ],
   "core/cross-verify.test.ts": [
    "flags a security/auth keyword in the description (positive)",
    "flags a security keyword inside a scope directory (positive)",
    "flags assignedAgent === security-auditor (positive)",
    "flags forceAgent === security-auditor (positive)",
    "flags priority === CRITICAL (positive — P0 tier)",
    "flags a standalone \"P0\" marker in text (positive)",
    "flags policy === risk-tagged when present (positive)",
    "does NOT flag a plain documentation task (negative)",
    "does NOT false-positive on \"authority\"/\"author\" (boundary guard)",
    "does NOT flag a token-usage task lacking real security context (over-broad guard)",
    "ignores a non-risk policy value",
    "accepts a full Task object structurally (type compatibility)",
    "selects a different provider (claude task → codex)",
    "honors default priority order (codex before gemini)",
    "falls through to the next priority when codex is absent (claude → gemini)",
    "returns null for a single-provider environment (honest-skip)",
    "returns null when availableProviders is empty",
    "returns null when the only available provider equals the task provider",
    "selects claude when the task ran on codex",
    "dedups repeated providers in the available list",
    "respects a custom priority order argument",
    "falls back to availability order for a provider not in the priority list",
    "exposes a sane default priority order",
    "uses configured priority only among exact fresh eligible candidates",
    "rejects %s without falling through to registration truth",
    "rejects self-verification and ambiguous duplicate provider projections",
    "projects the exact admitted model through the combined decision",
    "verifies a high-stakes task when a second provider exists",
    "skips a non-high-stakes task under default high_stakes_only gate",
    "honest-skips a high-stakes task when no second provider is available",
    "verifies a non-high-stakes task when high_stakes_only is disabled",
    "passes a custom verifier priority through to selection",
    "always returns a non-empty reason string"
   ],
   "core/custody-read-snapshot.test.ts": [
    "releases payloads without forgetting native fences or accepting changed re-reads",
    "keeps evicted absence and discovery fences and permits an unchanged re-read",
    "detects an expired monotonic deadline even if wall time does not advance",
    "keeps consumer namespaces separate and expires them with the operation",
    "checks directory membership after all content observations",
    "rechecks missing observations and invalidates swallowed read failures"
   ],
   "core/daemon-hygiene.test.ts": [
    "flags a fabricated stale dist/mcp/server.js daemon",
    "ignores a FRESH deckent mcp server (younger than the age threshold)",
    "ignores non-deckent processes even when they carry a daemon-kind word",
    "ignores deckent-owned NON-daemon commands (e.g. `deckent doctor`)",
    "classifies bot / serve / watch deckent daemons",
    "normalizes Windows backslash paths so dist\\\\mcp\\\\server.js matches",
    "respects a custom minAgeSec threshold",
    "respects custom ownerMarkers",
    "returns multiple stale daemons in snapshot order",
    "parses \"%s\" → %i seconds",
    "parses `pid etime command` lines and skips garbage",
    "parses `pid|sec|command` lines and skips empty-command lines",
    "uses `ps` on linux and parses the snapshot",
    "uses `ps` on darwin too",
    "uses `powershell` on win32 and parses the snapshot",
    "returns an honest empty result (supported:false) on an unsupported platform",
    "degrades to empty (supported:true + error) when the lister tool exits non-zero",
    "degrades to empty when the spawn itself errors (binary missing)",
    "renders a single PASS line when nothing is stale",
    "renders WARN + per-daemon entry + a cross-platform kill hint",
    "localizes the header for tr",
    "flags stale daemons returned by an injected lister",
    "emits a PASS line (no stale) when the snapshot is clean",
    "degrades to a benign PASS note on an unsupported platform — never fails",
    "NEVER throws when the lister rejects — degrades to a PASS note",
    "honors minAgeSec passthrough"
   ],
   "core/debt-002.test.ts": [
    "DEBT.md exists and is non-empty",
    "parseDebtTable returns items when DEBT.md has entries",
    "all parsed items have a resolved field",
    "parseDebtTable can parse the full DEBT.md without crash"
   ],
   "core/debt-export-guard.test.ts": [
    "refuses to overwrite debt.md when render collapses to empty marker while DB has debt entries",
    "writes debt.md with minimal content when DB has no debt entries (0-legit-debt correct)",
    "writes debt.md normally when DB has debt entries and the renderer emits real content",
    "does not break existing guards for summary.md and decisions.md"
   ],
   "core/debt-tenant-cas.test.ts": [
    "creates once and makes only an exact replay idempotent",
    "updates status, metadata, and history atomically for the complete lineage",
    "fails closed for wrong tenant/project, NULL tenant, stale status, and lineage or metadata drift"
   ],
   "core/debug-log.test.ts": [
    "stays silent when DECKENT_DEBUG is unset",
    "outputs info/warn/error when DECKENT_DEBUG=1",
    "outputs all levels including debug when DECKENT_DEBUG=debug",
    "formats messages with ISO timestamp"
   ],
   "core/decay-catastrophic-small-db.test.ts": [
    "aborts when small DB has >50% non-exempt entries decaying (60% case)",
    "allows legitimate single-entry decay regardless of DB size",
    "allows legitimate two-entry decay (floor=3: batches of 1-2 always proceed)",
    "large DB normal decay proceeds without abort (regression guard)"
   ],
   "core/decay-safety.test.ts": [
    "preserves window-boundary entries (currentNum - decayAfterSprints <= entryNum)",
    "preserves entries with sprint_num == 0 (undated / parse-missing) — skipDelete guard",
    "aborts decay batch that would wipe more than 50% of non-exempt entries (catastrophic guard)",
    "keeps decay-exempt ADRs untouched even when batch crosses catastrophic threshold"
   ],
   "core/decision-config.test.ts": [
    "returns enabled by default",
    "sets agentSelectionThreshold to 3",
    "sets maxSkillsPerTask to 3",
    "sets learningEnabled to true",
    "sets learningMaxSprints to 10",
    "sets decisionLogging to true",
    "sets adaptiveAgentEnabled to false (opt-in)",
    "returns a new object each time",
    "returns enabled by default",
    "sets maxSprintsToKeep to 10",
    "sets minConfidenceForRecommendation to 0.6",
    "sets decayInterval to 5",
    "sets patternMigrationDone to false",
    "enables parallelPipelines by default",
    "disables sharedMemoryEnabled by default",
    "sets conflictStrategy to last_writer_wins",
    "validates a correct default config",
    "rejects null config",
    "rejects non-object config",
    "rejects non-boolean enabled",
    "rejects agentSelectionThreshold out of range",
    "accepts agentSelectionThreshold in range",
    "rejects maxSkillsPerTask out of range",
    "rejects non-boolean learningEnabled",
    "rejects learningMaxSprints out of range",
    "rejects learningMaxSprints above 100",
    "rejects non-boolean decisionLogging",
    "rejects non-boolean adaptiveAgentEnabled",
    "accepts empty object (all optional)",
    "collects multiple errors",
    "validates a correct default config",
    "rejects null",
    "rejects non-boolean enabled",
    "rejects maxSprintsToKeep out of range",
    "rejects minConfidenceForRecommendation out of 0-1 range",
    "accepts minConfidenceForRecommendation at boundaries",
    "rejects decayInterval out of range",
    "rejects non-boolean patternMigrationDone",
    "accepts empty object",
    "validates a correct default config",
    "rejects null",
    "rejects non-boolean parallelPipelines",
    "rejects non-boolean sharedMemoryEnabled",
    "rejects invalid conflictStrategy",
    "accepts valid conflictStrategy values",
    "accepts empty object",
    "DecisionEngineConfig type matches createDefaultDecisionConfig output",
    "LearningConfig type matches createDefaultLearningConfig output",
    "CollaborationConfig type matches createDefaultCollaborationConfig output"
   ],
   "core/decision-types.test.ts": [
    "returns a TaskAnalysis with type code",
    "returns complexity 0",
    "returns empty keywords",
    "returns scopeWeight 0",
    "returns estimatedDurationMs 0",
    "returns a new object on every call",
    "keywords array is independently mutable",
    "returns true for code",
    "returns true for test",
    "returns true for doc",
    "returns true for security",
    "returns true for refactor",
    "returns true for devops",
    "returns true for config",
    "returns false for empty string",
    "returns false for unknown type",
    "returns false for uppercase CODE",
    "returns false for number as string",
    "creates entry with given step number",
    "creates entry with given name",
    "creates entry with given reasoning",
    "defaults input to empty object",
    "defaults output to empty object",
    "defaults durationMs to 0",
    "returns a new object each time",
    "input object is independently mutable",
    "supports step numbers 1 through 6",
    "supports all TaskType values",
    "allows complexity range 0-10",
    "allows arbitrary input/output records",
    "can hold null agent",
    "can hold populated fields",
    "accepts in-range axis scores",
    "rejects an axis score above 1",
    "rejects a negative axis score",
    "rejects an unrecognized key (.strict())",
    "round-trips a valid DecisionStory example",
    "rejects an unrecognized key (.strict())",
    "rejects an empty summary",
    "round-trips a valid BrainEscalation example",
    "rejects an unrecognized key (.strict())",
    "rejects an invalid reason literal",
    "round-trips a valid example without escalation",
    "accepts a valid example WITH escalation",
    "rejects an unrecognized key (.strict())",
    "rejects an invalid provenance literal",
    "returns a decision with every input field intact",
    "is deep-frozen: top-level, nested story, nested steps array, axisScores",
    "throws on mutation attempt (strict-mode ESM)",
    "does not leak internal references — mutating the original input after construction does not affect the frozen decision",
    "accepts a valid decision WITH escalation and freezes it too",
    "throws InvalidRoutingDecisionV3Error on a missing required field",
    "throws InvalidRoutingDecisionV3Error on an out-of-range confidence",
    "round-trips a full valid example",
    "requires schemaVersion to be exactly 1",
    "rejects an unrecognized key (.strict())",
    "schemas round-trip through JSON serialization (replay-from-disk shape)",
    "captures %s (replay-sufficient)",
    "is deep-frozen: top-level, candidateOutcomes, nested decision",
    "does not leak internal references from the input requirementVector/candidateOutcomes",
    "throws InvalidJournalEntryV3Error on a missing required field",
    "throws InvalidJournalEntryV3Error on wrong schemaVersion"
   ],
   "core/deck-broker-audit-wire.test.ts": [
    "a granted decision produces exactly one durable record with decision:granted, reason:resolved, and the redacted secret source",
    "each denial reason (no-secret, already-consumed, expired) produces exactly one durable record with decision:denied and the matching reason",
    "resolveForTaskWithReason wires the same durable record set as the legacy resolveForTask path",
    "no sink configured: resolve behavior and in-memory audit log are identical to the pre-existing (unwired) contract",
    "a throwing sink does not change resolveForTaskWithReason\\'s return value or consumption semantics",
    "a throwing sink does not suppress or corrupt the in-memory getAuditLog() trail",
    "never leaks the .deck project root path into a durable record",
    "returns undefined when cfg is absent",
    "returns undefined when cfg.enabled is false",
    "builds a connector loading the ERP token from .deck file (mock driver 1 — orders)",
    "merges .deck secrets over baseEnv — deck key wins",
    "falls back to baseEnv when .deck is absent",
    "mock driver 1 (orders): entity+filter query returns safe rows",
    "mock driver 2 (products): entity query with numeric filter",
    "mock driver 3 (contacts): entity query with limit",
    "rejects an explicit request for a non-allow-listed field (orders.secret_field)",
    "rejects filtering on a non-allow-listed field (products.cost)",
    "unregistered entity is rejected (table does not leak)",
    "gate absent → plain erp.read (backward-safe, no approval required)",
    "gate returns true → query proceeds normally",
    "gate returns false → CAPABILITY_FAILED with denial message",
    "gate throws → treated as denial (fail-closed)",
    "gate receives entity name and actor from InvocationContext"
   ],
   "core/deck-broker-resolution.test.ts": [
    "returns state \"granted\" carrying ONLY the requested provider\\'s env",
    "narrows to `env` on the granted arm (compile-time discrimination)",
    "returns a fresh env copy per grant — mutating one cannot corrupt a later grant",
    "no-secret: an unconfigured provider is denied with reason \"no-secret\"",
    "already-consumed: a second resolve for the same taskId is denied with reason \"already-consumed\"",
    "expired: past the TTL window every resolve is denied with reason \"expired\"",
    "exposes all THREE denial reasons as typed values a caller can exhaustively handle",
    "a denial carries NO env, no secret value and no .deck path",
    "a denied resolve does NOT consume the taskId — a later valid resolve still grants",
    "still returns the env object on grant and null on every denial",
    "agrees with the typed API on every outcome, and consumption crosses both methods",
    "logs exactly ONE audit entry per legacy call — one resolve path, no double-audit",
    "the typed API audits identically to the legacy one (same outcome + reason)"
   ],
   "core/deck-broker.test.ts": [
    "resolves ONLY the requested provider\\'s credential, no cross-provider leak",
    "two different tasks resolving the same provider each get their own independent grant",
    "an unknown / unconfigured provider resolves to null (no secret)",
    "empty .deck (subscription mode) → every resolve denied, no ambient credential leaks in",
    "returns a fresh copy — mutating the returned env cannot corrupt a later resolve for a different task",
    "a second resolve for the SAME taskId is denied, even for the same provider",
    "a second resolve for the SAME taskId with a DIFFERENT provider is still denied (taskId is the handoff unit)",
    "a denied resolve (no-secret) does NOT consume the taskId — a later valid resolve for it still succeeds",
    "resolves normally within the TTL window",
    "denies every resolve once the TTL window has elapsed since construction",
    "defaults to a 5-minute TTL when unset",
    "records a granted entry with taskId/provider/outcome, and no secret value",
    "records denial reasons for no-secret, already-consumed, and expired",
    "getAuditLog returns a copy — mutating it does not affect the broker\\'s internal trail",
    "neither a successful nor a denied resolve, nor the audit log, ever mentions the project root path",
    "exposes no method that returns the project root or a filesystem path"
   ],
   "core/deck-file-secret-lifecycle.test.ts": [
    "never overwrites an existing .deck — content stays byte-identical (regression: pre-fix this erased live secrets on re-init)",
    "is a true no-op on repeat calls once the file exists — content never drifts across multiple re-inits",
    "creating a brand-new .deck sets owner-only 0600 permissions (POSIX; regression: pre-fix mode was unset → umask default, typically 0644)",
    "re-asserts 0600 via chmodSync after the write, closing the umask gap even under a permissive process umask",
    "writes atomically — no leftover .deck.tmp survives a successful create, and content is fully formed",
    "invokes icacls with /inheritance:r /grant:r <user>:F on the created file when platform is win32",
    "does not throw when icacls exits non-zero — degrades honestly with a stderr warning, file still created",
    "does not throw when spawn emits an error event (e.g. icacls not on PATH) — degrades honestly",
    "warns and skips the icacls attempt entirely when USERNAME is unset",
    "skips the icacls attempt entirely (no spawn call) once the .deck already exists (no-op-if-exists applies before the platform branch)",
    "does not throw and warns to stderr when the target path cannot be written (e.g. .deck is a directory)",
    "creates .deck + .gitignore normally with no warning on the happy path"
   ],
   "core/deck-file.test.ts": [
    "parses simple KEY=VALUE pairs",
    "skips comment lines starting with #",
    "skips blank lines",
    "trims whitespace around keys and values",
    "handles = in value (only first = splits)",
    "strips double quotes from values",
    "strips single quotes from values",
    "does not strip mismatched quotes",
    "handles empty value",
    "handles unicode values",
    "returns empty record for empty string",
    "skips lines without = separator",
    "skips lines with empty key before =",
    "returns empty record if .deck file is missing",
    "loads secrets from existing .deck file",
    "returns empty record if .deck file is unreadable",
    "returns valid for all known keys",
    "warns on unknown keys",
    "errors on invalid key format",
    "returns valid for empty record",
    "reports both warnings and errors together",
    "creates .deck file in project root",
    "template contains all known keys",
    "template contains comment header",
    "template is parseable and all values are empty",
    "creates .gitignore with .deck if not present",
    "appends .deck to existing .gitignore",
    "does not duplicate .deck entry",
    "handles .gitignore without trailing newline",
    "returns false when .deck is not tracked",
    "returns true when .deck is committed",
    "returns false in a repo without .deck file",
    "DECK_FILE_NAME is .deck",
    "KNOWN_DECK_KEYS contains 9 keys"
   ],
   "core/deck-interpolation.test.ts": [
    "resolves $DECK:KEY when .deck has the key",
    "keeps placeholder and warns when key is missing",
    "interpolates nested objects",
    "interpolates arrays",
    "does not modify non-matching strings",
    "falls back to DECKENT_ prefixed key"
   ],
   "core/deckent-style-config.test.ts": [
    "default config has deckent_style === \"sprint\"",
    "global config deckent_style: \"task\" overrides default via mergeConfigs",
    "project config \"task\" override wins over global \"sprint\" (3-layer merge)",
    "invalid value \"turbo\" throws ConfigValidationError",
    "DECKENT_STYLE=task env var overrides config",
    "type union is strict — only sprint and task are valid"
   ],
   "core/dep0190-shell-fix.test.ts": [
    "runTargetedTests passes shell=false on linux/darwin",
    "runFullVitest passes shell=false on linux/darwin",
    "captureVitestBaseline passes shell=true on win32",
    "captureVitestBaseline passes shell=false on linux/darwin"
   ],
   "core/directive-interrogator.test.ts": [
    "returns the five structural questions for a multi-task DIRECTIVES",
    "covers all five interrogation lenses with unique ids",
    "handles empty DIRECTIVES — still 5 questions with default params",
    "injects parsed key-names + task count into params (parametric)",
    "every question carries an i18n key from the public key contract",
    "resolves text through getMessage and honours the requested language",
    "does not throw on malformed / heading-less input",
    "preserves original content and adds a Refinements section",
    "is idempotent — re-applying replaces the section, never duplicates",
    "tolerates empty / missing answers — returns the original unchanged",
    "inserts the Refinements section inside the Goal block (before the first ---)",
    "appends refinements when there is no Goal heading (still content-preserving)",
    "exposes the five question keys plus intro + draft header",
    "every build question key is declared in the contract (no orphan keys)"
   ],
   "core/doc-tracking/code-drift.test.ts": [
    "expands globs against tracked files and keeps plain paths",
    "returns null when tracks is empty or null",
    "returns true when a tracked file is newer than the doc",
    "returns false when no tracked file is newer than the doc"
   ],
   "core/doc-tracking/config.test.ts": [
    "returns defaults when docs.json is absent",
    "merges a tracking block from .deckent/settings/docs.json over defaults"
   ],
   "core/doc-tracking/frontmatter.test.ts": [
    "parses managed scalars + tracks list and isolates body",
    "reports no front-matter when line 1 is not ---",
    "is stable across CRLF and trailing whitespace",
    "has sha256: prefix",
    "updates managed keys, preserves others (title/tracks), keeps body",
    "prepends front-matter when none exists",
    "is idempotent for identical fields",
    "writes <temp> when content_hash is null"
   ],
   "core/doc-tracking/git-date.test.ts": [
    "returns the git commit date for a tracked file",
    "falls back to mtime for an untracked file (no git)",
    "returns 0 for a missing file with no git"
   ],
   "core/doc-tracking/glob.test.ts": [
    "matches ** across path segments",
    "matches * within a single segment only",
    "matches exact literal paths",
    "matches node_modules anywhere"
   ],
   "core/doc-tracking/rank-resolver.test.ts": [
    "front-matter doc_rank overrides everything",
    "rankMap glob applies when no override",
    "most-specific (longest) pattern wins",
    "falls back to defaultRank",
    "ignores invalid override (negative / NaN)"
   ],
   "core/doc-tracking/scanner.test.ts": [
    "writes managed front-matter and records a fresh doc; ignores node_modules",
    "detects content_drift on second scan after body edit",
    "treats scratch/ and status:temp as EXEMPT (no hash)",
    "computes code_drift (non-null path) when a doc carries tracks, null otherwise"
   ],
   "core/doc-tracking/stale-scorer.test.ts": [
    "is rank-sensitive and clamped",
    "EXEMPT for draft/temp/frozen regardless of signals",
    "FRESH when no drift and within age threshold",
    "content_drift on a rank-0 doc escalates to CRITICAL_STALE",
    "content_drift on a high-rank doc is only DRIFT",
    "age beyond threshold yields STALE even without drift"
   ],
   "core/doc-tracking/store.test.ts": [
    "upserts and reads back a record (round-trip incl. JSON signals)",
    "upsert is last-write-wins on path PK",
    "pruneDeleted removes rows whose path is no longer present",
    "does not create or touch the entries table"
   ],
   "core/doc-tracking/sync.test.ts": [
    "scans docs into memory.db without writing front-matter"
   ],
   "core/effort-tiering.test.ts": [
    "covers every closed-core work-type exactly (no silent default holes)",
    "faithful V2 semantics: document/configure low, analyze high, construction normal",
    "subtype rolls up to the parent tier; unknown input degrades to normal"
   ],
   "core/ent2-deepen.test.ts": [
    "strict=true: getByType with tenantId returns only that tenant\\'s rows",
    "strict=false: getByType with tenantId returns that tenant + NULL-tenant rows",
    "admin bypass: no tenantId → returns ALL rows regardless of strict mode",
    "strict=true: getByTags with tenantId only returns that tenant\\'s tagged entries",
    "strict=false: getByTags with tenantId returns tenant + NULL-tenant tagged entries",
    "strict=true + no tenantId in query + role → fail-closed (empty result)",
    "strict=true + tenantId provided + role → RBAC passes, events filtered by tenant",
    "strict=false (default): no tenantId → uses \\'local\\' fallback, RBAC passes for viewer",
    "no role → no RBAC check, returns all events regardless of strict",
    "strict=true + tenantId provided + no role → returns filtered events",
    "SCHEMA string does not contain DEFAULT \\'local\\' for tenant column",
    "createMission with explicit tenant=acme → stores and retrieves correctly",
    "createMission with no tenant → falls back to local via code-level default",
    "listMissions filters by tenant correctly"
   ],
   "core/ent3-lineage.test.ts": [
    "returns all N events sharing the same correlationId",
    "returns empty array when no events have the given correlationId",
    "also returns events whose causationId matches the given id",
    "returned events carry prevHmac and hmac (chain intact)",
    "returns empty when events exist but none carry the looked-up id"
   ],
   "core/enterprise-config.test.ts": [
    "returns safe opt-in defaults when called with undefined",
    "returns safe opt-in defaults when called with null",
    "parses a fully-specified valid config",
    "parses partial config with missing sections using defaults",
    "throws on invalid rbac.defaultRole",
    "throws on negative flow.maxConcurrent",
    "throws on zero flow.maxConcurrent",
    "throws on non-object top-level value",
    "throws on non-boolean tenancy.enabled",
    "merges only the specified override fields",
    "full override replaces all fields",
    "empty override returns base unchanged",
    "does not mutate the base config",
    "all enterprise features are disabled by default (opt-in)",
    "defaultRole is the least-privileged role"
   ],
   "core/environment.test.ts": [
    "detects VS Code from VSCODE_PID",
    "detects VS Code from VSCODE_CWD",
    "detects VS Code from TERM_PROGRAM=vscode",
    "detects Cursor from CURSOR_SESSION",
    "detects Cursor from TERM_PROGRAM=cursor",
    "detects Codex from CODEX_SESSION",
    "detects Gemini from GEMINI_CLI",
    "detects tmux from TMUX env var",
    "falls back to shell when no env vars match",
    "vscode wins over cursor when both env vars are set (priority order)",
    "vscode wins over tmux when both are set",
    "cursor wins over codex when both are set",
    "return type satisfies DetectedEnv"
   ],
   "core/erp-connector.test.ts": [
    "queries an allowed entity/field, parameterizes filters, tags the actor",
    "defaults to all declared fields when none requested",
    "rejects an undeclared entity (allow-list)",
    "rejects a field not in the entity allow-list",
    "rejects mutation verbs smuggled as identifiers (no mutation verbs)",
    "always enforces a bounded limit (mandatory cap)",
    "validates filter value shapes",
    "tags the connector default actor when no per-call actor is passed, per-call overrides",
    "exposes entity introspection and a factory",
    "requires an injected driver function"
   ],
   "core/erp-driver-dynamics.test.ts": [
    "translates scalar op '%s' to OData operator '%s' with the 1-based-resolved param",
    "translates op 'in' to the NATIVE v4 in operator — f in ('a','b') — not an or-chain",
    "renders numeric members of an 'in' list raw (unquoted)",
    "translates op 'like' to the v4 string function contains(field,'value')",
    "escapes embedded single quotes as '' in string literals (OData injection guard)",
    "emits numbers and booleans raw (unquoted) and null as the keyword",
    "joins multiple predicates with ' and ' (AND semantics) across one shared params list",
    "omits $filter entirely when the compiled query carries no predicates",
    "throws on an out-of-range placeholder index (corrupt compiled query)",
    "issues a GET against <baseUrl>/api/data/v9.2/<EntitySet> with $select and $top (no v2 $format)",
    "honors an apiVersion override in the Web API path",
    "maps entity → entity set via entityModelMap, falling back to the entity name",
    "joins the Web API path onto baseUrl without doubling a trailing slash",
    "sends Bearer <token> plus OData-MaxVersion/OData-Version 4.0 and Accept application/json",
    "unwraps the OData v4 envelope { value: [...] } to ErpRow[]",
    "throws when the response carries no v4 value rows (v2 d.results is NOT accepted)",
    "throws when the 2xx response body is not valid JSON",
    "throws on a non-2xx HTTP response with the status and a redacted body snippet",
    "refuses a compiled query that is not read-only (defence in depth, before any network call)",
    "rejects non-http(s) and unparseable baseUrl at construction time",
    "rejects an empty bearer token and a non-bearer auth kind (bearer-only driver)",
    "rejects a malformed apiVersion at construction time",
    "throws an explanatory error when no fetch is available (no fetchImpl, no global)",
    "round-trips a registered-entity query: connector compiles, driver hits the Web API, rows come back"
   ],
   "core/erp-driver-ifs.test.ts": [
    "builds the IFS projection-REST URL with $select + $top and bearer auth",
    "translates eq / in / like predicates into one OData v4 $filter",
    "maps logical entity → IFS entity set via entityModelMap",
    "honours a custom apiVersion segment",
    "refuses a non-read-only compiled query (defence in depth)",
    "redacts the bearer token from error messages",
    "rejects an OData response without a `value` array",
    "eagerly validates wiring at construction time"
   ],
   "core/erp-driver-odoo.test.ts": [
    "translates scalar op '%s' to Odoo operator '%s' with the 1-based-resolved param",
    "translates op 'in' to Odoo 'in' collecting ALL placeholder values into an array",
    "translates op 'like' to Odoo 'ilike'",
    "resolves 1-based placeholders across MULTIPLE predicates sharing one params list",
    "throws on an out-of-range placeholder index (corrupt compiled query)",
    "POSTs a JSON-RPC 2.0 execute_kw/search_read envelope with db, uid, apiKey and an id",
    "passes compiled fields and limit through as search_read kwargs",
    "maps entity → Odoo model via entityModelMap, falling back to the entity name",
    "returns the JSON-RPC result array as ErpRow[]",
    "throws on an Odoo JSON-RPC error including error.data.message — WITHOUT the apiKey",
    "redacts a server-echoed apiKey out of the Odoo error message",
    "throws on a non-2xx HTTP response with the status — WITHOUT the apiKey",
    "throws when the JSON-RPC response carries no array result",
    "refuses a compiled query that is not read-only (defence in depth)",
    "rejects non-http(s) and unparseable endpoint URLs at construction time",
    "rejects empty db / non-integer uid / empty apiKey at construction time",
    "throws an explanatory error when no fetch is available (no fetchImpl, no global)",
    "round-trips a registered-entity query: connector compiles, driver hits Odoo, rows come back"
   ],
   "core/erp-driver-sap.test.ts": [
    "translates scalar op '%s' to OData operator '%s' with the 1-based-resolved param",
    "translates op 'in' to a parenthesized eq-or-chain (no native v2 'in')",
    "translates op 'like' to OData v2 substringof('needle', field)",
    "escapes embedded single quotes as '' in string literals (OData injection guard)",
    "emits numbers and booleans raw (unquoted) and null as the keyword",
    "joins multiple predicates with ' and ' (AND semantics) across one shared params list",
    "omits $filter entirely when the compiled query carries no predicates",
    "throws on an out-of-range placeholder index (corrupt compiled query)",
    "issues a GET with $select from compiled fields, $top from limit, and $format=json",
    "maps entity → OData entity set via entityModelMap, falling back to the entity name",
    "joins entity set onto baseUrl without doubling a trailing slash",
    "sends Basic base64(user:pass) for basic auth",
    "sends Bearer <token> for bearer auth",
    "unwraps the OData v2 envelope { d: { results: [...] } } to ErpRow[]",
    "unwraps the OData v4 envelope { value: [...] } to ErpRow[]",
    "throws when the response carries neither v2 d.results nor v4 value rows",
    "throws on a non-2xx HTTP response with the status and a redacted body snippet",
    "redacts a server-echoed bearer token out of the error body",
    "throws when the 2xx response body is not valid JSON",
    "refuses a compiled query that is not read-only (defence in depth)",
    "rejects non-http(s) and unparseable baseUrl at construction time",
    "rejects empty basic username/password, empty bearer token, and unknown auth kind",
    "throws an explanatory error when no fetch is available (no fetchImpl, no global)",
    "round-trips a registered-entity query: connector compiles, driver hits OData, rows come back"
   ],
   "core/erp-factory.test.ts": [
    "returns undefined when config is absent",
    "returns undefined when enabled is falsy (no erp.read handler installed)",
    "builds a connector with the entity allow-list and queries through the driver",
    "enforces the allow-list — an unregistered entity is rejected",
    "builds an IFS connector, reading the token from the named env var",
    "defaults the secret env var to DECKENT_ERP_TOKEN",
    "builds an Odoo connector from url/db/uid + api key env",
    "builds a SAP basic-auth connector (username in config, password in env)",
    "throws when a network driver is enabled but the credential env var is missing",
    "throws when a required connection field is missing",
    "throws when no entities are declared (empty allow-list)"
   ],
   "core/error-handling-unification.test.ts": [
    "${code} is registered",
    "${code} has message and suggestion",
    "${code} createError returns DeckentError",
    "${code} is registered",
    "${code} has message and suggestion",
    "${code} is registered",
    "${code} has message and suggestion",
    "loadAgentConfig throws DeckentError with E031 when config missing",
    "exportConfig throws DeckentError with E020 when file missing",
    "importConfig throws DeckentError with E021 when import file missing",
    "loadSkillManifest throws DeckentError with E023 when manifest missing",
    "definePipeline throws DeckentError with E040 for empty steps",
    "definePipeline throws DeckentError with E041 for invalid agentId",
    "definePipeline throws DeckentError with E042 for invalid phase",
    "definePipeline throws DeckentError with E043 for duplicate phase",
    "throws DeckentError with E049 for circular dependencies",
    "write throws DeckentError with E044 for empty key",
    "write throws DeckentError with E045 for empty writerId",
    "createHandoff throws DeckentError with E046 for missing task IDs",
    "createHandoff throws DeckentError with E047 for empty artifacts",
    "failHandoff throws DeckentError with E048 for missing handoff",
    "readTask throws DeckentError with E061 for missing task file",
    "write throws DeckentError with E062 for empty key",
    "write throws DeckentError with E063 for empty agentId",
    "recordResult throws DeckentError with E065 for nonexistent experiment",
    "analyzeExperiment throws DeckentError with E065 for nonexistent experiment",
    "completeExperiment throws DeckentError with E065 for nonexistent experiment",
    "CLI error with custom message preserves code",
    "Orchestra error with custom message preserves code",
    "Agent error with custom message preserves code",
    "getSkillDetail throws DeckentError with E039 for empty name",
    "getSkillDetail error has suggestion",
    "submitRating throws DeckentError with E053 for invalid rating (0)",
    "submitRating throws DeckentError with E053 for invalid rating (6)",
    "submitRating throws DeckentError with E053 for non-integer rating",
    "submitRating error has suggestion about 1-5 range",
    "E038 is registered for config file read failures",
    "E041 error message includes the invalid agentId value",
    "E042 error message includes the invalid phase value",
    "E043 error message includes the duplicate phase name",
    "DeckentError has name \"DeckentError\"",
    "DeckentError is an instance of Error",
    "DeckentError stores code, message, suggestion, docLink",
    "DeckentError without suggestion has undefined suggestion",
    "register() adds a new code",
    "createError for unknown code returns error with code",
    "createError with suggestion override",
    "getAll returns a Map with all registered entries",
    "${code} is registered and preserves dynamic context",
    "total registered codes >= 40 (E001-E010 + E020-E039 + E040-E053 + E060-E066)",
    "all CLI codes E020-E039 exist",
    "all Orchestra codes E040-E053 exist",
    "all Agent codes E060-E066 exist",
    "canonical raw-throw ratchet has no additions or stale baseline headroom",
    "${code} is registered",
    "${code} has message and suggestion",
    "${code} createError returns DeckentError carrying the code",
    "throws DeckentError E073 on a catalog-external formula identifier (message preserved)",
    "ModelsDevSource: produces DeckentError E072 on HTTP error yet still returns []",
    "OpenRouterSource: produces DeckentError E072 on HTTP error yet still returns []",
    "scanFile: detects throw new Error( as a violation",
    "scanFile: allows throw new DeckentError( — no violation",
    "runCheck: returns violations when throw new Error( found in orchestra dir",
    "runCheck: exit-0-clean — no violations in clean orchestra dir",
    "formatViolations: returns empty string when no violations",
    "formatViolations: includes file path, line number and fix suggestion",
    "collectTsFiles: only collects .ts files, skips node_modules"
   ],
   "core/error-registry-integrity.test.ts": [
    "gives every runtime-registered code a unique, well-formed code, message, and remediation",
    "fails closed for absent or blank registry metadata",
    "accepts an explicit typed no-remediation reason"
   ],
   "core/error-registry-lint.test.ts": [
    "package.json contains lint:errors script pointing to check-error-handling.mjs",
    "npm run lint:errors exits zero with visible known-debt count",
    "check-error-handling.mjs file exists",
    "detects throw new Error( on a single line",
    "detects multiple violations in same file",
    "returns empty array for file with no violations",
    "does NOT flag throw new DeckentError( as a violation",
    "does NOT flag throw new TypeError( as a violation (only Error)",
    "captures correct line number for violation in middle of file",
    "handles empty file gracefully",
    "ignores comments, strings and template literals containing the raw-throw text",
    "collects .ts files from a flat directory",
    "recursively collects .ts files from subdirectories",
    "excludes node_modules directory",
    "excludes dist directory",
    "returns empty array for non-existent directory",
    "returns empty array for empty directory",
    "current project exactly matches the canonical baseline",
    "filesScanned is positive when scanning real project",
    "detects violations in a mock project root",
    "fails when a governed file exceeds its explicit baseline count",
    "fails when a reduction leaves reusable baseline headroom",
    "fails a count-neutral same-file substitution",
    "does not fail when an unchanged throw moves to another line",
    "returns 0 violations for mock project root with no violations",
    "returns empty string for zero violations",
    "includes violation count in header",
    "includes relative file path (not absolute)",
    "includes line number for each violation",
    "includes suggested fix instruction",
    "handles multiple violations",
    "errors.ts exports ErrorRegistry class/object",
    "errors.ts exports DeckentError class",
    "ErrorRegistry has register() method",
    "ErrorRegistry has createError() method",
    "src/core/errors.ts does not contain raw throw new Error( at module level"
   ],
   "core/errors.test.ts": [
    "appends a critical-class entry to both error channels",
    "appends a non-critical entry only to the general channel",
    "trims the critical channel at its independent ceiling",
    "preserves the existing 600-line general-channel trim contract",
    "creates error with code and message",
    "includes suggestion when provided",
    "includes docLink when provided",
    "extends Error",
    "has a stack trace",
    "suggestion defaults to undefined",
    "docLink defaults to undefined",
    "includes whatHappened when provided",
    "includes why when provided",
    "includes howToFix when provided",
    "human context fields default to undefined",
    "has returns true for pre-populated codes",
    "has returns false for unknown codes",
    "get returns entry for known code",
    "get returns undefined for unknown code",
    "getAll returns all 10+ pre-populated entries",
    "getAll returns a copy (not the internal map)",
    "createError returns DeckentError for known code",
    "createError returns DeckentError with fallback for unknown code",
    "createError allows message override",
    "createError allows suggestion override",
    "register adds a new error code",
    "pre-populated E003 has correct suggestion about DIRECTIVES",
    "pre-populated E009 is about git",
    "pre-populated E010 is about node version",
    "all core error codes (E001-E010) have whatHappened",
    "all core error codes (E001-E010) have why",
    "all core error codes (E001-E010) have howToFix with at least 1 step",
    "CLI error codes (E020-E039) have human context",
    "Orchestra error codes (E040-E053) have human context",
    "Agent error codes (E060-E066) have human context",
    "createError populates human context fields from registry",
    "createError for unknown code has no human context",
    "includes error message and code in first line",
    "includes whatHappened section",
    "includes why section",
    "includes howToFix steps with numbering",
    "includes docLink when present",
    "omits sections when fields are missing",
    "handles empty howToFix array",
    "produces full human-friendly output for registry error",
    "produces complete output for E001 tmux error",
    "handles single howToFix step"
   ],
   "core/event-stream-location.test.ts": [
    "writeEvent/readEvents are importable from core/ and round-trip",
    "DeckentEvent type from core is structurally usable",
    "orchestra shim re-exports the SAME function references as core (single module instance)",
    "CHANNELS is identical across core and the orchestra shim",
    "write via core is readable via the orchestra shim (shared stream + module)",
    "core/audit-writer writes events readable via core/audit-query (rewired core-only import)",
    "core/audit-writer.ts source never imports from orchestra/event-stream (ADR-008)",
    "core/audit-query.ts source never imports from orchestra/event-stream (ADR-008)"
   ],
   "core/event-stream-monotonic.test.ts": [
    "returns distinct values to two competing nextSequence calls",
    "writes a rotation marker and preserves the sequence sidecar",
    "publishes sprint state atomically without leaving a tmp artifact"
   ],
   "core/event-stream-progress.test.ts": [
    "CHANNELS.PROGRESS exists with the expected value",
    "CHANNELS.PROGRESS is a key of the CHANNELS object",
    "emitProgress writes an event with channel=PROGRESS and correct payload shape",
    "pct is optional — omitting it leaves pct undefined in payload (not an error)",
    "writes only to the explicit target even when current state names another sprint",
    "source is NOT present in payload — only in event.source",
    "source override propagates to event.source; defaults to \"brain\"",
    "emitProgress event is readable back via readEvents",
    "emitProgress does not throw even when writeEvent errors internally"
   ],
   "core/event-stream-terminal-ownership.test.ts": [
    "keeps a stale terminal journal byte/hash stable and writes the exact live target"
   ],
   "core/event-stream.test.ts": [
    "(a) returns sprint-active.json sprintId when present — old core IGNORED this (pre-fix RED)",
    "(b) falls back to sprint-state.json when sprint-active.json absent",
    "(c) returns null when neither sprint-active.json nor sprint-state.json exist",
    "(d) falls back to sprint-state.json when sprint-active.json is malformed JSON",
    "(e) falls back to sprint-state.json when sprint-active.json sprintId is empty",
    "returns null when sprint-state.json is malformed JSON",
    "rotates to .1 when the file exceeds the cap",
    "does NOT rotate when under the cap (per-sprint small files)",
    "missing file → no-op, no throw",
    "continues from the max sequence in the event log instead of restarting at 1",
    "still starts at 1 when neither sidecar nor event log exists",
    "emits a debugLog record and a typed failure while writeEvent still returns null",
    "a healthy write is unaffected — same event object callers already receive"
   ],
   "core/event-trigger.test.ts": [
    "returns matching trigger on exact match",
    "returns empty array when eventType does not match",
    "excludes triggers from different tenantId",
    "skips disabled triggers",
    "returns empty array when source does not match",
    "matches multiple triggers for same event"
   ],
   "core/execution-admission.test.ts": [
    "publishes an allow manifest before returning an immutable permit",
    "is idempotent for the same identity/payload and conflicts on mutation",
    "stores HOLD durably without minting an execution permit",
    "allows attended unknown-limit work only with explicit approval evidence",
    "rejects USD-only, receipt-less, or reachability-unknown allow manifests",
    "verifies exact dispatch identity and ignores worker task projection mutation",
    "detects manifest corruption and permit tampering",
    "grants dispatch exactly once and returns the durable winner on replay"
   ],
   "core/execution-authority-adapter-parity.test.ts": [
    "keeps the raw-loader and typed facade surfaces byte-name identical",
    "exposes the identical frozen four-capability surface on both twins",
    "builds byte-identical stable fd paths",
    "exposes the identical frozen op surface on both twins",
    "exposes the identical frozen op surface as both existing twins"
   ],
   "core/execution-budget-derivation.test.ts": [
    "uses the task estimate when present, the config default when absent",
    "maps effort through the config table, defaulting to normal",
    "scales the output ceiling by retry × headroom — and derives NEITHER aggregate maxTokens NOR an input ceiling",
    "subscription-billed (USD 0 / absent) derives NO maxUsd — a 0-ceiling would block the task it contains",
    "API-billed derives maxUsd, hard-capped by the sprint budget",
    "the bundled baseline itself carries the estimator block (data, not code)",
    "a project cost-config that PREDATES the block loads with the baseline values resolved in",
    "a present-but-malformed estimator block fails CLOSED with a typed error"
   ],
   "core/execution-budget-policy.test.ts": [
    "resolves goal purpose admission as hold when the block is absent or configured to hold",
    "resolves allow-role-ceiling goal purpose admission with owner ceiling",
    "resolves goal-acceptance admission through auditor role ceiling mapping",
    "narrows a goal purpose under role authority and requires final-only containment",
    "rounds discrete turn reserve upward without changing exact allocations",
    "holds remote work when owner policy is absent",
    "uses the role default without fabricating missing ceilings",
    "holds remote work when the owner hard budget has no landing authority",
    "selects a canonical TaskKind profile over the role default",
    "lets a requested budget narrow or add ceilings but never widen authority",
    "holds before remote work when the effective integer turn reserve cannot finish the caller protocol",
    "checks the reserve after requested-budget narrowing and rejects invalid protocol requirements",
    "returns an immutable snapshot and stable digest across key order",
    "allows local execution without inventing a remote policy",
    "rejects malformed policy %#",
    "rejects ambiguous or duplicate backend reroute policy",
    "rejects an unbounded or role-less final-only usage authorization",
    "authorizes final-only usage only for the exact owner-named roles",
    "holds with a typed reason when no policy is provided",
    "holds when the profile is absent from the purposes block",
    "rejects a missing or non-positive total-token ceiling at config validation",
    "resolves the owner-authored ceilings when the profile is valid",
    "local cost-class decision carries finalOnlyUsage when the owner grant covers the role",
    "local-exempt carries the final-only grant NEVER without the owner policy"
   ],
   "core/execution-budget.test.ts": [
    "counts cache read and creation toward the token ceiling",
    "uses provider-authoritative result.cost for the USD ceiling",
    "returns unknown when a configured ceiling has no durable usage evidence",
    "does not enforce a ceiling from heuristic or worker-claimed token counts",
    "accepts host/provider measured usage for a within-budget decision",
    "treats unknown-model pricing as unknown even when the numeric placeholder is zero",
    "is inert when the owner supplied no ceiling",
    "excludes subscription/free-tier/local work from per-task USD ceilings while preserving token ceilings",
    "accepts the host runtime ledger and its lineage projection as measured usage",
    "uses the terminal live summary for turn and peak-context ceilings",
    "uses provider-final task cost against the configured sprint cap",
    "HOLDs unknown pricing evidence instead of treating it as zero",
    "HOLDs a locally-priced heuristic estimate instead of spending it as measured cost",
    "accepts locally-priced cost only when its token usage was measured",
    "excludes subscription, free-tier and local tasks from the sprint USD ledger",
    "fails closed when effective billing mode is unresolved"
   ],
   "core/execution-continuation-prompt.test.ts": [
    "compiles only durable checkpoint authority and remaining cumulative work",
    "cannot compile before a durable first-writer continuation claim exists",
    "rejects a cross-project or tampered checkpoint/claim identity",
    "fails before dispatch when bounded checkpoint fields still exceed the prompt ceiling"
   ],
   "core/execution-custody-subject.test.ts": [
    "preserves task bytes and derives goal custody from invocation identity",
    "rejects malformed or mixed identity %#",
    "rejects reservation mismatch %#"
   ],
   "core/execution-effect-containment.test.ts": [
    "reuses only validated output identity, never an untrusted clone or changed input",
    "keeps the expected phase gate when a validated object is reused",
    "allows a scoped canary edit when the protected inert mask target is unchanged",
    "still rejects a protected .deck %s rather than exempting mask paths",
    "binds the complete ordered native capture body before projecting a canonical manifest",
    "never treats path-based traversal as capture authority",
    "derives add, modify, delete and safe-mode effects from strict native manifests",
    "keeps capture-local root evidence separate from durable workspace identity",
    "keeps content-equal delete and add separate because content cannot prove rename",
    "binds exact safe directory mode into the manifest and MODE effect digest",
    "ignores worker-style extra claims and holds an outside-scope write",
    "treats empty filesWrite as read-only and protects control-plane paths",
    "allows only a derived new parent while exposing deletion of an existing non-empty parent",
    "recomputes phase, policy and manifest digest before evaluation",
    "rejects unsafe metadata, links, mount claims, deadlines and bounded-overflow manifests",
    "rejects self-digested manifests that raise implementation hard ceilings",
    "rejects portable aliases in native manifests even when their digest is recomputed",
    "propagates native hardlink, symlink, special-file, mount and cancellation HOLDs",
    "classifies every non-directory, non-regular and non-symlink object as special"
   ],
   "core/execution-effect-persistence-contract.test.ts": [
    "strictly binds result projection to terminal transaction and typed line applicability",
    "owns the one strict canonical landing-intent digest authority",
    "separates restart-stable workspace authority from capture-local root evidence",
    "binds one strict dependency resource into workspace seal and post-commit release",
    "represents an empty staged file as one canonical zero-length Store chunk",
    "verifies the complete reused-parent journal and reports no directory modification",
    "recomputes manifests, decision, transaction and every staged raw chunk",
    "rejects phase swaps, missing/orphan chunks and raw or aggregate digest drift",
    "rejects fake no-change terminals and non-canonical persisted JSON",
    "strictly round-trips terminal/workspace seals and accepted-result landing binding",
    "binds restart adoption to immutable journal refs, the prior lease and a fresh boundary"
   ],
   "core/execution-landing-checkpoint.test.ts": [
    "publishes an immutable digest-verified checkpoint outside the worker project",
    "publishes schema-validated authority atomically without leaving a sibling temporary file",
    "fails loudly on tampering, unknown fields and cross-project authority",
    "fails closed when the host authority resolves inside the worker-mounted project",
    "requires complete lineage and internally consistent cumulative counters",
    "derives cumulative remaining ceilings without consuming the per-call context ceiling",
    "keeps LANDED retirement separate from terminal product settlement",
    "admits exactly one continuation claimant after durable retirement",
    "enumerates validated retired checkpoints and their continuation intent",
    "does not admit continuation while predecessor runtime resources remain active",
    "persists and restart-rereads a path-free exact V2 context and checkpoint",
    "adopts the durable V2 context winner across a preparedAt and baseline race",
    "rejects V2 path fields, sibling artifacts, stale digests, wrong generation and policy",
    "first-writer persists V2 retirement and one exact generation-bound continuation claim"
   ],
   "core/execution-landing-proposal.test.ts": [
    "accepts only the path-free dispatch-bound V3 proposal",
    "emits a private-output exact protocol with no public attempt or result authority",
    "preserves the historical V1 helper protocol bytes unless V2 is explicit",
    "validates and atomically replaces a structured V2 proposal",
    "rejects non-serializable input and symlinked destinations",
    "accepts only the exact attempt-bound bounded schema",
    "accepts worker clock skew while binding freshness to host-observed file evidence",
    "rejects a proposal file that predates the host-prepared attempt",
    "surfaces malformed host input through the typed diagnostic",
    "emits one bounded attempt-specific volatile protocol segment",
    "combines finite adjudication checkpointing with the existing evidence tool call",
    "publishes valid structured input through the real Node entry without partial files"
   ],
   "core/execution-plan-digest.test.ts": [
    "persists the owner ceiling and retains a narrower request as provenance",
    "records a remote HOLD without treating a caller budget as owner authority",
    "keeps local Ollama work policy-exempt without inventing a ceiling",
    "binds final-only provider usage to a fail-closed plan HOLD before process birth",
    "binds an exact owner-authorized final-only containment grant into the task and digest",
    "binds shared-writer topology and configured concurrency while ignoring task-ID-only drift",
    "keeps v2 byte semantics independent from the new optional concurrency context",
    "dispatches persisted v2, v3 and v4 explicitly and rejects unknown versions",
    "binds criterion identity, polarity, statement and evidence without changing frozen v3 bytes",
    "treats evidence requirements as a canonical set but preserves criterion order",
    "binds planner and source authority while preserving explicit historical dispatch",
    "rejects missing or malformed V5 authority without changing legacy defaults",
    "is stable across volatile sprint/task identities, timestamps, scope order, and remapped internal dependency IDs",
    "binds configured fallback and owner-policy changes even when task summaries stay identical",
    "binds a normalized closed write allowlist only in the current V4 projection",
    "returns a versioned deep-frozen canonical projection",
    "blocks a tampered v2 stored Sprint before spawnStart"
   ],
   "core/execution-recovery.test.ts": [
    "takes a pure bounded healthy path from monotonic progress",
    "exports exactly the canonical decision vocabulary",
    "returns %s for its authoritative state",
    "treats stale wall-clock projection as stalled, never orphaned",
    "requires definitive process and fence evidence for orphaning",
    "never grants replay without an explicit exact-attempt resume permit",
    "allows an explicitly authorized paused attempt to resume only after process and fence absence",
    "exposes bounded abort/terminate operations for a stalled owned attempt",
    "requires durable completion and an explicit permit to finalize",
    "fails closed for incomplete or contradictory evidence",
    "requires exact task, attempt, and fence identity"
   ],
   "core/execution-termination-ledger-goal-subject.test.ts": [
    "connects real goal prepared metadata to both pre-dispatch binding producers",
    "persists %s goal identity without a fake task",
    "rejects goal/reservation mismatch %#",
    "rejects nullable task identity without a goal subject and mixed non-reservable identity",
    "binds non-reservable goal identity to mission, call and attempt",
    "rejects a different goal subject under the same binding id",
    "migrates a v2 task row to v3 without changing payload, MAC, digest or evidenceRef",
    "rejects goal subject envelope tampering even when its payload MAC is untouched",
    "rejects %s reserved-to-non-reservable envelope tampering"
   ],
   "core/execution-termination-ledger-non-reservable.test.ts": [
    "binds a non-reservable dispatch with NO reservation identity and reads it back",
    "is idempotent — the same non-reservable binding input returns created:false",
    "reserved and non-reservable arms coexist with distinct evidence refs",
    "the partial-unique index allows many NULL reservation_id rows",
    "the DB CHECK rejects a reserved row with NULL reservation_id",
    "the DB CHECK rejects a non-reservable row that carries a reservation_id",
    "migrates a legacy v1 binding to reserved without rewriting payload/hash",
    "rejects an unknown future schema version rather than silently migrating"
   ],
   "core/execution-termination-ledger.test.ts": [
    "resolves one host-global path across supported platform roots",
    "holds every backend without an equivalent durable adapter",
    "persists an exact pre-dispatch binding and released terminal across restart",
    "never releases a dispatched execution and records it only as consumed",
    "accepts LANDED retirement only as consumed after durable dispatch",
    "rejects post-dispatch binding, cross-reservation release and late ledger publication",
    "rejects a conflicting second terminal and cross-binding settlement evidence",
    "detects payload tampering after an immutable database trigger is bypassed",
    "keeps historical binding signatures verifiable across authority rotation",
    "acts as the exact ProviderLimitStore release verifier without a permissive callback"
   ],
   "core/execution-topology.test.ts": [
    "normalizes lexical aliases without consulting the filesystem",
    "rejects non-portable or root-escaping writer %s",
    "blocks three undeclared writers and serializes them in stable plan-slot order",
    "accepts an explicit dependency chain while keeping the safe serialized waves visible",
    "is task-ID invariant and caps every wave by configured maxWorkers",
    "fails loud on unresolved dependencies, invalid paths, and cycles"
   ],
   "core/execution-write-scope-policy.test.ts": [
    "produces a deterministic versioned exact manifest and removes write overlap from reads",
    "holds unsafe legacy path %s with typed evidence",
    "holds case-fold inventory collisions and declared symlink ambiguity",
    "does not widen an unrelated portable collision into the selected scope",
    "holds instead of silently compiling a trailing slash to exact-file",
    "holds for the read field on the same grounds",
    "still accepts the same path in the directories field",
    "leaves an ordinary file path untouched",
    "makes an empty exact write scope explicitly read-only",
    "canonicalizes only exact files and never inherits directory authority",
    "holds invalid, protected or portable-ambiguous authority %j",
    "does not rewrite observed paths into an authorized spelling",
    "bounds durable write-policy allocation before canonicalization"
   ],
   "core/export-empty-db-guard.test.ts": [
    "refuses to overwrite decisions.md when DB is empty but disk file has real content",
    "writes normally when DB is empty and no disk file exists",
    "writes normally when disk file already contains the emptyMarker (no real content to protect)",
    "writes decisions.md normally when DB has ADR entries and render is non-empty",
    "preserves richer ADR snapshots when the DB is non-empty but partial"
   ],
   "core/f10-001-policy.test.ts": [
    "score 30 < minScore 50 → suggest",
    "score 30 ≥ minScore 20 → permit",
    "condition-gate false → park",
    "condition-gate true → permit",
    "condition gate false → entry status=parked, outcome=failure",
    "low activation score → suggest (advisory) — execution proceeds",
    "policyEngine absent → backward-compat, no gate applied",
    "policyEngine.enabled=false → gate skipped even with builders provided"
   ],
   "core/f1012-config-registry.test.ts": [
    "accepts known built-in names",
    "accepts arbitrary custom names (not in ProviderName union)",
    "rejects empty string",
    "rejects whitespace-only string",
    "rejects names with special characters",
    "registers test-ai from config.providers.registry and makes it selectable",
    "registered config-driven provider is selectable via registry.getProvider",
    "accepts adapter key as alias for type",
    "completes without error when no registry configured (backward-compat)",
    "treats empty registry array as no-op (backward-compat)",
    "skips registry entry with empty name via validateProviderName",
    "skips registry entry with special-char name via validateProviderName",
    "registers multiple config-driven providers in one pass",
    "is idempotent — re-registering same name does not duplicate"
   ],
   "core/f1ad-integration.test.ts": [
    "does not execute an interactive cloud CLI as model enumeration",
    "bootstrapProviders resolves modelAutoDetectPromise without a cloud process birth",
    "replays Ollama inventory within TTL without a second process birth",
    "waits for close and leaves neither the parent nor its signal-ignoring child alive"
   ],
   "core/f1ad-model-detect.test.ts": [
    "parses JSON array of strings",
    "parses JSON { models: [{ id }] }",
    "parses JSON { data: [{ id }] }",
    "returns empty array for empty input",
    "returns empty array for invalid JSON",
    "returns model ids from the supported Ollama inventory command",
    "returns empty array when CLI exits with non-zero code",
    "returns empty array when spawn throws",
    "does not birth an interactive %s process without inventory capability",
    "declares Ollama inventory capability explicitly",
    "passes timeoutMs to the supported inventory spawn",
    "CLI ids come first",
    "deduplicates across sources",
    "returns catalog-only when CLI is empty",
    "returns builtin-only when CLI and catalog are both empty",
    "returns empty array when all sources are empty",
    "uses catalog authority for cloud providers without birthing a CLI process",
    "keeps existing builtin models when cloud inventory is unsupported",
    "ignores stale cloud auto-detect caches",
    "does not mint a cloud identity from injected fake inventory output",
    "gracefully handles Ollama inventory failure",
    "registers discovered local Ollama tags",
    "uses Ollama inventory cache when available and within TTL",
    "returns DetectResult with correct shape"
   ],
   "core/f8-003-deepen.test.ts": [
    "config enforce_least_privilege:true → registry.leastPrivilegeEnabled === true",
    "config enforce_least_privilege:false → registry.leastPrivilegeEnabled === false",
    "no config → registry.leastPrivilegeEnabled defaults to false (permissive)",
    "empty config object → registry.leastPrivilegeEnabled defaults to false",
    "operator role + gpu.compute + leastPrivilegeEnabled:true → CAPABILITY_DENIED",
    "admin role + gpu.compute + leastPrivilegeEnabled:true → ok (gpu in admin grants)",
    "viewer role + gpu.compute + leastPrivilegeEnabled:true → CAPABILITY_DENIED",
    "developer role + gpu.compute + leastPrivilegeEnabled:true → CAPABILITY_DENIED (no gpu in developer)",
    "operator + gpu.compute with leastPrivilegeEnabled:false → ok (permissive)",
    "no actor + gpu.compute with leastPrivilegeEnabled:false → ok (permissive)",
    "operator + gpu.compute → emitDenied called with correct info",
    "admin + gpu.compute → emitDenied NOT called (ok result)",
    "emitDenied receives grantedCapabilities for the denied actor",
    "emitDenied NOT called when leastPrivilegeEnabled:false (permissive)",
    "emitDenied NOT called on CAPABILITY_NOT_FOUND",
    "enforce_least_privilege:true + operator + gpu → CAPABILITY_DENIED",
    "enforce_least_privilege:true + admin + gpu → ok",
    "no enforce_least_privilege (flag-off) + operator + gpu → ok (permissive)",
    "enforce_least_privilege:true + emitDenied spy → denial audit triggered on CAPABILITY_DENIED",
    "operator ROLE_CAPABILITY_MAP does NOT include gpu",
    "admin ROLE_CAPABILITY_MAP DOES include gpu",
    "viewer ROLE_CAPABILITY_MAP does NOT include gpu"
   ],
   "core/f8-003-least-priv.test.ts": [
    "defines viewer, developer, operator, admin roles",
    "viewer has read-only capabilities",
    "operator has db-query but not db-write",
    "admin has all capabilities including db-write",
    "operator is a superset of viewer",
    "returns capabilities for known roles",
    "returns empty array for unknown role",
    "role \"viewer\" + db.write + enforceLeastPrivilege:true → CAPABILITY_DENIED",
    "role \"operator\" + db.read + enforceLeastPrivilege:true → ok (allowed)",
    "role \"admin\" + db.write + enforceLeastPrivilege:true → ok (allowed)",
    "unknown role + db.write + enforceLeastPrivilege:true → CAPABILITY_DENIED (empty grant set)",
    "flag-off: role \"viewer\" + db.write without enforceLeastPrivilege → ok (permissive)",
    "flag-off: no actor + db.write without enforceLeastPrivilege → ok (permissive)",
    "leastPrivilegeEnabled=true + role \"viewer\" + db.write → CAPABILITY_DENIED",
    "leastPrivilegeEnabled=true + role \"operator\" + db.read → ok",
    "leastPrivilegeEnabled=true + no role → CAPABILITY_DENIED (no grants)",
    "leastPrivilegeEnabled=false (default) → permissive (regression)",
    "explicit grantedCapabilities overrides role-derived grants",
    "explicit empty grantedCapabilities denies even for admin role",
    "role \"viewer\" + fs.read + enforceLeastPrivilege:true → ok"
   ],
   "core/failure-disposition-policy.test.ts": [
    "resolves the canonical disposition for %s",
    "projects a settled pre-dispatch result to NOT_DISPATCHED",
    "keeps an ordinary worker NO_GO eligible for FIX",
    "merges a narrow config override and excludes non-redispatchable settlements"
   ],
   "core/feature-truth.test.ts": [
    "missing entryModule file -> code=missing",
    "entryModule exists, no exportName requested -> code=ok",
    "exportName not present in module -> code=missing",
    "no prodCallsitePattern declared -> wired=undefined (undefined-honesty)",
    "call-site found in a non-test src file -> wired=ok with file:line evidence",
    "does not count the entry module function declaration as production wiring",
    "no call-site anywhere under src/ -> wired=none",
    "call-site present only under tests/ -> excluded -> wired=none",
    "no flagPath declared -> enabled=no-flag",
    "flagPath resolves to true -> enabled=on",
    "flagPath resolves to false -> enabled=off",
    "flagPath not present in config -> enabled=no-flag (honest, not a crash)",
    "no proof declared -> proof=undefined",
    "artifact-file exists and non-empty -> proof=ok",
    "artifact-file missing -> proof=missing",
    "artifact-file exists but empty -> proof=missing",
    "journal-recent last entry within maxAgeDays -> proof=ok",
    "journal-recent last entry older than maxAgeDays -> proof=stale",
    "journal-recent missing file -> proof=missing",
    "journal-recent torn/unparseable last line -> proof=missing (fail-soft, no throw)",
    "smoke-cmd is never executed here -> proof=declared",
    "code-ok + wired-none -> half-wire candidate (canonical export-var + no-callsite case)",
    "code-ok + wired-ok -> not a half-wire candidate",
    "code-missing + wired-none -> not a half-wire candidate (nothing shipped at all)",
    "flags all 6 historical half-wire shapes as candidates",
    "invalid regex in prodCallsitePattern -> per-def error, siblings unaffected, no throw",
    "malformed def reaching a runtime boundary -> error field, no throw, batch continues",
    "never reports a callsite from outside projectRoot/src (scan is scoped, not global)",
    "evidence paths are posix-normalized regardless of platform path.sep"
   ],
   "core/features-manifest.test.ts": [
    "manifest file exists at .deckent/features-manifest.json",
    "manifest is valid JSON",
    "has required top-level keys: _meta, active, lightly_used, dormant, dead",
    "_meta has required fields",
    "sourceAnalysis has sprintsChecked array and methodology string",
    "all categories are arrays",
    "every feature entry has required fields: id, label, files, description",
    "all feature IDs are unique across all categories",
    "active category has at least 10 entries (core features)",
    "ROUTE-V1-PURGE: decision-orchestrator-v1 is fully purged from the manifest",
    "purged decision-orchestrator-v1 leaves no adrRef residue",
    "active category contains sprint-controller",
    "active category contains event-stream",
    "active category contains dependency-scheduler",
    "dormant category contains heartbeat-daemon",
    "dormant category contains shared-memory",
    "no feature appears in multiple categories",
    "dead features have supersededBy or deprecatedSince field",
    "dormant features have blockedBy or roadmap field explaining dormancy",
    "_meta.usageWindow is set to last-10-sprints",
    "total feature count is at least 20 entries",
    "all feature files[] entries exist on filesystem (or are valid directories)",
    "active features have at least 1 file that is imported by other src/ files",
    "dead features are actually deprecated or superseded",
    "stale entry detection: learning-decay.ts should not be in manifest",
    "manifest _meta.generatedBy references sync-manifest.mjs",
    "pins routing truth to the canonical V3 entry instead of retired V2 code",
    "sync-manifest.mjs script exists",
    "dry-run produces valid output",
    "json output is valid JSON with correct structure",
    "features.ts CLI command file exists",
    "feature-query.ts MCP tool file exists",
    "features CLI is registered in src/cli/index.ts",
    "feature-query MCP tool is registered in src/mcp/tools/index.ts",
    "sprint-finalizer.ts contains features manifest sync hook"
   ],
   "core/file-lock-runtime.test.ts": [
    "creates a .spawnlock file under .locks/ with taskId metadata",
    "is idempotent: same taskId re-acquiring returns the existing lock",
    "throws SpawnLockError when a different task already holds the lock",
    "canonical scenario: two workers spawn for the same file — second fails",
    "hash-based filename keeps long paths safely encoded",
    "recovers from a corrupted spawnlock file (overwrites and acquires)",
    "removes the lock so a different task can acquire it",
    "is a no-op when the lock does not exist",
    "refuses to delete a lock owned by a different task",
    "acquires all files when none conflict",
    "rolls back previously-acquired locks when a later file conflicts",
    "empty file list returns empty array and creates no locks",
    "releaseSpawnLocks removes the specified batch",
    "releaseAllSpawnLocks removes only the calling task\\'s locks",
    "releaseAllSpawnLocks returns 0 when nothing matches the task",
    "releaseAllSpawnLocks tolerates a missing .locks/ directory",
    "cleanup-on-failure: releaseAllSpawnLocks unblocks a different task after a simulated failure",
    "checkLocks() does not surface .spawnlock entries",
    "both lock kinds can coexist on the same file system entry",
    "generic spawn cleanup cannot discover or remove .executionlock authority"
   ],
   "core/file-lock-spawnlock.test.ts": [
    "removes a stale spawn lock older than maxAgeMs",
    "keeps a fresh spawn lock younger than maxAgeMs",
    "removes only stale locks in a mixed set",
    "returns 0 when no locks directory exists",
    "skips corrupted spawnlock files without throwing",
    "removes a spawn lock whose taskId is not in the active set",
    "keeps a spawn lock whose taskId is in the active set",
    "handles mixed active and orphan locks correctly",
    "lists all active spawn locks",
    "returns empty array when no locks exist",
    "releases all spawn locks for the given taskId only"
   ],
   "core/file-lock.test.ts": [
    "should acquire a lock on a file",
    "should be idempotent for same worker",
    "should throw LockError for different worker",
    "should support optional TTL",
    "should release a lock",
    "should be no-op for non-existent lock",
    "should throw when releasing another worker\\'s lock",
    "should return null for unlocked file",
    "should return LockInfo for locked file",
    "should list all active locks",
    "should return empty array when no locks exist",
    "should release all locks for a worker",
    "should clear stale locks older than maxAgeMs",
    "should respect TTL over maxAgeMs",
    "should return 0 when no locks directory exists",
    "clearOrphanLocks: returns empty when no locks directory exists",
    "clearOrphanLocks: returns empty when all locks belong to active workers",
    "clearOrphanLocks: releases locks for dead workers",
    "clearOrphanLocks: releases all locks when active set is empty",
    "clearOrphanLocks: skips corrupted lock files gracefully",
    "inspects a bounded number of spawnlocks using filesystem mtime",
    "releases only the unchanged spawnlock inspected earlier"
   ],
   "core/final-only-usage-containment.test.ts": [
    "returns the exact canonical task-stamped grant at the complete intersection",
    "keeps maxUsd on the separate incremental-pricing fail-closed gate",
    "throws a typed HOLD before dispatch and returns undefined only when containment is unnecessary"
   ],
   "core/flow-dispatch.test.ts": [
    "returns scheduled due flows when nextRun ≤ now",
    "returns event-triggered dispatches when event matches trigger",
    "returns both scheduled and event-triggered dispatches combined",
    "returns empty array when no flows are due and no events match",
    "returns multiple event dispatches for multiple matching triggers",
    "does not match trigger from a different tenant"
   ],
   "core/flow-event-dispatch.test.ts": [
    "builds a pending entry stamped with the injected clock",
    "produces a kind:\"event\" dispatch when listTriggers/listEvents are supplied and match",
    "produces no event dispatch when the event does not match any trigger",
    "regression: default (no listTriggers/listEvents) behaves exactly as before — no event dispatches",
    "persists newly matched event dispatches to disk with unique ids",
    "ignores scheduled-kind dispatches and writes nothing when no event-kind items exist",
    "accumulates across calls (the persisted queue grows, ids stay unique)",
    "flips a pending entry to approved and stamps approvedAt",
    "returns null for an unknown id",
    "returns null when approving an already-approved entry (idempotent guard)",
    "an event-triggered dispatch stays pending (blocked) until flow approve <id> is called, then proceeds",
    "enqueues event dispatches and reports the queued count + path (en)",
    "reports in Turkish when lang=\"tr\"",
    "is a silent no-op when the tick has no event-kind dispatches (existing scheduled-only ticks unaffected)"
   ],
   "core/flow-registry-rbac.test.ts": [
    "operator can add a flow",
    "viewer is denied addFlow (throws)",
    "admin can add a flow",
    "no role provided bypasses RBAC check (backward compat)",
    "viewer can list flows for a tenant",
    "operator can list flows",
    "admin can list all flows without tenantId",
    "operator can remove a flow",
    "viewer is denied removeFlow (throws)",
    "operator can enable/disable a flow",
    "viewer is denied enableFlow (throws)",
    "admin can add, list, enable, and remove flows"
   ],
   "core/flow-registry-tenant.test.ts": [
    "persists flow at tenant-scoped path (.deckent/tenants/<id>/flows/)",
    "cross-tenant invisible: flow added in tenant-a not visible in tenant-b registry",
    "uses local default tenant when no withTenant context is active",
    "same flow id in different tenants is stored independently (no cross-contamination)"
   ],
   "core/flow-registry.test.ts": [
    "adds a flow and retrieves it by id",
    "returns undefined for unknown id",
    "lists all flows when no tenantId given",
    "filters by tenantId",
    "returns empty array when tenant has no flows",
    "removes a flow and returns true",
    "returns false when removing nonexistent flow",
    "round-trips timezone and leaves a legacy record without the field untouched",
    "persists flow to disk as JSON",
    "loads flows from disk on construction",
    "removes JSON file on removeFlow",
    "toggles enabled and persists updated state",
    "returns false for nonexistent flow"
   ],
   "core/flow-runtime.test.ts": [
    "dispatches due flows when registry has eligible flows",
    "calls callback with empty array for empty registry",
    "does not dispatch disabled flows",
    "starts and stops the tick loop",
    "start is a no-op when already running",
    "stop is a no-op when not running",
    "uses configured interval ms when starting"
   ],
   "core/flow-scheduler-timezone.test.ts": [
    "uses fixed UTC+3 for Istanbul in %s",
    "skips Berlin spring-forward nonexistent 02:30",
    "returns both occurrences of Berlin fall-back repeated 02:30",
    "keeps the omitted-timezone path exactly UTC"
   ],
   "core/flow-scheduler.test.ts": [
    "returns due flow when nextRun is in the past",
    "skips flow whose nextRun is still in the future",
    "skips disabled flows regardless of timing",
    "sorts multiple due flows by nextRun ascending",
    "updates lastRunAt so a due flow is not repeated on next tick",
    "triggers again after one minute has elapsed",
    "uses a flow timezone when computing the next run",
    "resets lastRunAt so the flow becomes due again",
    "returns every missed timezone-aware slot without mutating scheduler state",
    "fails closed instead of silently dropping occurrences at its bound"
   ],
   "core/framework-detection.test.ts": [
    "detects react when src/dashboard/package.json contains react dep",
    "does NOT detect react when src/dashboard/package.json has no react dep",
    "skips dashboard check when root package.json already has a framework",
    "does not set framework when src/dashboard/package.json is missing",
    "handles malformed src/dashboard/package.json gracefully",
    "returns react framework when dashboard has React",
    "returns vitest from stack-detector when vitest is in devDeps",
    "detects vite buildTool via stack-detector",
    "detects mixed language (TypeScript + Rust coexistence)",
    "CI detection remains in analyzer (not in stack-detector)",
    "framework from root package.json takes priority over dashboard"
   ],
   "core/global-config.test.ts": [
    "creates both directories when neither exists",
    "is idempotent — does not create when directories already exist",
    "creates only credentials dir when deckent dir already exists",
    "returns null when config file does not exist",
    "returns parsed config when file exists with valid JSON",
    "returns null for malformed JSON (readJsonSafe returns null)",
    "reads from GLOBAL_CONFIG_PATH",
    "returns partial config with only language set",
    "writes config as formatted JSON with trailing newline",
    "calls ensureGlobalDir before writing",
    "writes empty partial config correctly",
    "project config overrides global config",
    "uses global values when project has defaults",
    "project language overrides global language",
    "deep merges nested mode configs — project takes priority",
    "preserves global nested values not overridden by project",
    "returns a new object — does not mutate inputs",
    "returns the GLOBAL_CONFIG_PATH constant",
    "path contains .deckent/config.json",
    "returns true when config file exists",
    "returns false when config file does not exist",
    "checks the correct path"
   ],
   "core/global-precedence.test.ts": [
    "linux: platformPath is XDG config dir, legacyPath is the flat ~/.deckent dir",
    "linux: honors XDG_CONFIG_HOME override for platformPath only",
    "wsl: resolves identically to linux XDG rules (distinct platform tag, same paths)",
    "darwin: platformPath is Application Support, legacyPath is the flat ~/.deckent dir",
    "win32: platformPath is %APPDATA%\\\\deckent, legacyPath is the flat .deckent dir (win32 backend, deterministic on any host)",
    "win32: honors %APPDATA% override for platformPath",
    "DECKENT_HOME override collapses platformPath onto the override, independent of legacyPath",
    "falls back to GLOBAL_CONFIG_PATH for both candidates on an unsupported platform string",
    "falls back to GLOBAL_CONFIG_PATH when HOME is unresolvable on a supported platform",
    "never throws regardless of platform/env combination",
    "returns the legacy path when only the legacy file exists (existing-install back-compat)",
    "returns the platform-correct path when only it exists (fresh XDG-first install)",
    "prefers the platform-correct path when BOTH exist (\"yeni-yol tercih\")",
    "returns the legacy path when NEITHER exists — byte-identical to pre-migration behavior (both resolve to nothing)",
    "loadGlobalConfig(legacyPath) still reads an explicit path directly, bypassing dual-read (unchanged contract)"
   ],
   "core/global-scope-resolver.test.ts": [
    "resolves the four XDG defaults from HOME when no XDG_* vars are set",
    "honors each XDG_* base-dir override independently",
    "treats an empty-string XDG var as unset (falls back to the HOME default)",
    "computes legacyDir as <home>/.deckent (migration probe seam)",
    "throws a typed HOME_NOT_RESOLVED error when HOME is missing",
    "treats an empty-string HOME as unset (typed error, not a broken path)",
    "resolves identically to linux XDG rules but keeps the wsl platform tag",
    "honors XDG overrides on wsl too",
    "resolves config/data/state to Application Support and cache to Caches",
    "ignores XDG_* vars on darwin (Apple conventions win)",
    "computes legacyDir as <home>/.deckent",
    "resolves config/data to %APPDATA% and cache/state to %LOCALAPPDATA%",
    "derives APPDATA/LOCALAPPDATA from USERPROFILE when the vars are missing",
    "falls back to HOMEDRIVE+HOMEPATH when USERPROFILE is missing",
    "joins with backslashes (win32 path backend, regardless of host OS)",
    "computes legacyDir as <home>\\\\.deckent",
    "throws a typed HOME_NOT_RESOLVED error when no home source is available",
    "collapses all role dirs onto DECKENT_HOME on %s",
    "beats platform conventions even when XDG/APPDATA vars are also set",
    "treats an empty-string DECKENT_HOME as unset (platform convention wins)",
    "still computes legacyDir from home under env-override",
    "works without any home (home + legacyDir become null, no throw)",
    "throws a typed UNSUPPORTED_PLATFORM error for an unvalidated platform string",
    "maps darwin and win32 straight through",
    "maps plain linux (no WSL markers) to linux",
    "maps linux with WSL_DISTRO_NAME to wsl",
    "maps linux with WSL_INTEROP to wsl",
    "treats empty-string WSL markers as unset (stays linux)",
    "throws a typed UNSUPPORTED_PLATFORM error for platforms outside the matrix",
    "does not mutate the injected env object",
    "returns identical results for identical inputs (no hidden process/fs reads)"
   ],
   "core/global-store.test.ts": [
    "linux: cache-role store resolves under XDG_CACHE_HOME default",
    "linux: state-role store resolves under XDG_STATE_HOME default",
    "wsl: resolves identically to linux (same XDG rules)",
    "darwin: cache-role store resolves under Library/Caches, not Application Support",
    "darwin: state-role store resolves under Application Support (state coincides with data/config)",
    "win32: cache-role store resolves under %LOCALAPPDATA% with backslash joins",
    "win32 path join uses the win32 backend even when resolved on a non-win32 host",
    "auth-status-cache: missing file -> defaultData, then save -> load round-trips",
    "model-catalog-cache: save -> load round-trips through the cache role-dir",
    "limits-cache: save -> load round-trips through the state role-dir (not cache)",
    "save() creates the role-dir recursively when it does not exist yet",
    "invalid JSON text -> defaultData with a warning, never throws",
    "valid envelope shape but data fails schema validation -> defaultData fail-soft",
    "non-envelope JSON (missing version/data) -> defaultData fail-soft",
    "applies a registered fromVersion migration to upgrade a v1 payload to v2",
    "a version with no matching migration step fails soft to defaultData",
    "a version newer than the definition understands fails soft to defaultData",
    "a file already at the current version needs no migration and loads as-is"
   ],
   "core/gpt55-catalog.test.ts": [
    "is a canonical core model, not an alias shim",
    "declares gpt-5.5 with feed-verified fields",
    "is first-class and resolvable on a fresh registry",
    "is idempotent — calling twice does not duplicate the codex catalog",
    "does not register the removed gpt-5 alias",
    "declares all three pinned API IDs with official pricing evidence",
    "registerCodexParityModels() makes the family first-class on a fresh registry",
    "providers/codex.ts module-load registers the parity catalog on the GLOBAL registry (half-wire closed 2026-07-11)",
    "ships official Standard-pricing entries with per-model aliases",
    "is resolvable via findModel() by short alias (sol/terra/luna)",
    "ships a feed-verified gpt-5.5 entry (no user cost-config.json present)",
    "is resolvable via findModel() by canonical id and by the gpt55 alias",
    "passes the unit-safety pin (per-token, not accidentally per-MTok)",
    "leaves the existing gpt-5 baseline entry unchanged"
   ],
   "core/gpt6-astra-catalog.test.ts": [
    "ships exact official metadata without aliases or changing tier defaults",
    "accepts the exact configured performance Brain without changing the native model",
    "stays unique through repeated parity registration",
    "survives offline catalog loading without network or ambient cache",
    "requires explicit owner activation under explicit-active policy",
    "preserves the canonical model when a cached external catalog omits Astra",
    "keeps existing implicit-active behavior when no activation store exists",
    "ships per-token short-context pricing and documents the long-context limitation",
    "constructs exact Docker Codex argv with the exact model and supported high effort"
   ],
   "core/hook-dispatch-wire.test.ts": [
    "executed: identical result/telemetry shape to the seam-less call — no hookErrors key",
    "denied (fail-closed, no confirm): unaffected by the new seam",
    "vetoes before confirm/execImpl run, denies with a justified hookVeto",
    "a non-matching pre-hook does not veto — call proceeds to executed",
    "invalid/unknown_tool short-circuit takes precedence — hooks never run",
    "transformed args flow into the confirm() context",
    "transformed args flow into execImpl",
    "observe-only pre-hook (returns void) leaves args unchanged",
    "transformed result replaces execImpl output on the returned DispatchResult",
    "post-hook receives the (possibly pre-hook-transformed) args and the execImpl result",
    "post-hook does not run when execImpl throws — status stays error",
    "post-hook does not run when the call is denied",
    "a throwing pre-hook is isolated — dispatch still proceeds, error recorded in telemetry.hookErrors",
    "a throwing post-hook is isolated — dispatch still reports executed with the original result",
    "a rejected async pre-hook is isolated the same way",
    "emits DECKENT→USER:NOTIFY on event-bus when called",
    "is a no-op (fail-safe) when globalNotifyDispatcher is not initialized",
    "dispatches to all registered adapters when dispatcher is initialized",
    "handles all 5 NotificationEventName values",
    "assigns correct priority via createNotification mapping",
    "CliNotificationAdapter uses parent-PID path when DECKENT_PARENT_PID is set",
    "nervous dispatcher fires notify() via bridgeToUserNotify",
    "throttles non-critical notifications (respects 1s min-interval)",
    "dispatches critical notifications immediately (bypasses throttle)"
   ],
   "core/host-detector.test.ts": [
    "extracts MemTotal kB from a realistic meminfo body",
    "returns null when MemTotal line is absent",
    "returns null on malformed MemTotal value",
    "returns null on zero / negative readings",
    "parses /proc/meminfo and returns totalGB rounded to 1 decimal",
    "falls back to os.totalmem when /proc/meminfo throws (non-Linux / permission denied)",
    "falls back to os.totalmem when meminfo body has no MemTotal line",
    "clamps a 40 GB host to MAX_MAX_WORKERS=16 (raw would be 19)",
    "matches the spec example: 32 GB host with 2 GB workers → 15 (under cap)",
    "honours the workerMemGB override",
    "clamps below MIN_MAX_WORKERS when host RAM is tiny",
    "clamps above MAX_MAX_WORKERS even on enormous hosts",
    "returns MIN_MAX_WORKERS for pathological inputs (non-finite, zero, negative)",
    "exposes a default workerMemGB of 2 GB (Sprint 194 task 194-003 alignment)"
   ],
   "core/host-role-invocation-admission-runtime.test.ts": [
    "projects exact fresh verifier evidence without creating a reservation",
    "holds stale, unavailable and identity-mismatched verifier projections",
    "returns an explicit %s HOLD when host authorities are unavailable",
    "admits an exact known primary but does not grant dispatch by admission alone",
    "passes caller-authored concurrency authority through the host admission seam",
    "uses canonical exact-model fallback when the primary limit is held",
    "never builds a reservation when exact reachability evidence is missing",
    "treats a missing candidate authority as terminal HOLD instead of fallback spend",
    "projects expired reachability and limit evidence to stale HOLD",
    "keeps explicit unavailable reachability and limit truth fail-closed",
    "fails closed on candidate identity mismatch and truth-store failure",
    "binds reservation tenant/project/account/backend to the exact host authority",
    "never lets a reservation lease outlive selected reachability proof",
    "turns a host clock authority failure into terminal HOLD",
    "grants dispatch exactly once through the injected host store",
    "settles the exact dispatched reservation through the same scoped host authority",
    "settles an existing exact dispatch idempotently without issuing a new admission"
   ],
   "core/identity-config-faz3.test.ts": [
    "accepts minimal local provider config",
    "satisfies LocalIdentityProviderConfig",
    "is the default kind used in existing DeckentConfig consumers",
    "accepts full SCIM provider config",
    "accepts SCIM config without optional userFilter",
    "is assignable as DeckentConfig identity.provider",
    "accepts full OIDC-claims provider config",
    "accepts OIDC-claims config with only required issuer",
    "is assignable as DeckentConfig identity.provider",
    "narrows to correct type via switch on kind",
    "identity block with no provider is still valid",
    "identity block preserves all existing optional fields alongside new provider"
   ],
   "core/identity-generator-step-order.test.ts": [
    "Step 3 (adrInsert) runs before Step 4 (ruleRegen)"
   ],
   "core/identity-generator.test.ts": [
    "creates PROJECT-IDENTITY.md when file does not exist",
    "updates existing PROJECT-IDENTITY.md Current State section",
    "appends Current State section when missing in existing file",
    "is idempotent — calling twice with same metrics writes same content",
    "counts ADRs from summary.md when adrCount not provided",
    "handles errors gracefully",
    "writes all 4 export files when DB exists",
    "opens the export snapshot read-only and forwards caller render options",
    "returns error when memory.db not found",
    "reports guarded export warnings without declaring success",
    "closes the read-only snapshot when guarded rendering throws",
    "runs full hook chain successfully",
    "forwards memory export render options through the public hook",
    "skips memory export when skipMemoryExport=true",
    "skips identity regen when skipIdentityRegen=true",
    "does not call rule regen when no callback provided",
    "isolates errors — rule regen failure does not affect other results",
    "handles async rule regen callback",
    "identity-status AUTOGEN block exists in IDENTITY.md",
    "MCP Tools count in identity-status block is ≥ 27 (file-count lower bound)",
    "identity-status and identity-summary MCP counts are consistent",
    "MCP Tools count in a producer-generated identity fixture matches registered count",
    "## Project Status heading immediately precedes identity-status AUTOGEN block",
    "identity-status AUTOGEN start/end markers are properly paired",
    "returns ok=true for well-formed IDENTITY.md with MCP Tools=31",
    "returns ok=false when MCP Tools count is below 31",
    "returns ok=false when identity-status block is missing",
    "returns ok=false when IDENTITY.md does not exist",
    "returns ok=false when Project Status does not immediately precede AUTOGEN block",
    "all Project Status metric rows live inside the identity-status AUTOGEN block",
    "lint script exits non-zero when a managed metric is hand-edited",
    "lint --check reports no drift across two consecutive runs"
   ],
   "core/identity-regen-default-skip.test.ts": [
    "skipIdentityRegen default true — Step 2 not invoked when option omitted",
    "skipIdentityRegen: true — explicit opt-out preserves null result",
    "skipIdentityRegen: false — explicit opt-IN still invokes Step 2"
   ],
   "core/identity-sync.test.ts": [
    "creates the DB identity entry from IDENTITY.md when none exists",
    "records detector identity as a digest-bound system projection",
    "refreshes an existing (stale) identity entry in place — no duplicate",
    "is a no-op when IDENTITY.md is absent",
    "is a graceful no-op when memory.db is absent"
   ],
   "core/interaction-policy.test.ts": [
    "batch → non-interactive, auto-approve, no stream",
    "interactive → prompt user, no auto-approve, no stream",
    "streaming → like interactive plus stream output",
    "undefined mode → conservative interactive default",
    "absent mode (no arg) → conservative interactive default",
    "unknown/future mode → conservative interactive default"
   ],
   "core/invocation-output-artifact.test.ts": [
    "does not accept a transport output ref detached from the immutable artifact",
    "does not let an artifact row become a sibling invocation output",
    "returns absence for a read-only legacy receipt database without the artifact table",
    "turns a succeeded legacy replay without the artifact table into a typed hold",
    "rolls the artifact row back when its atomic transport event conflicts with a sibling event",
    "rejects unresolved usage before atomically publishing output or transport success",
    "does not accept a hash-consistent replay whose recovered usage is unresolved",
    "holds a replay when the supplied prompt differs from the persisted artifact",
    "holds a replay when the mission identity does not match the receipt reservation"
   ],
   "core/invocation-receipt-store.test.ts": [
    "atomically binds opaque invocation output to its receipt and transport settlement",
    "persists a non-reservable output artifact without reservation authority",
    "rejects non-reservable writes that still carry reservation authority",
    "rejects non-reservable writes when receipt auth mode is not subscription",
    "adds admission_mode on pre-column databases and reads legacy rows as reserved",
    "keeps a stable path-private project binding across restart",
    "persists an immutable hash-chained receipt and folds an open dispatch to unknown",
    "enforces tenant scope and conflicting invocation idempotency",
    "atomically gives one reconciliation authority receipt ownership of a task",
    "makes event retries idempotent and rejects an event-id payload conflict",
    "enforces monotonic event chronology and exact authored-timestamp replay",
    "rolls back an atomic suffix whose events are chronologically out of order",
    "enforces the receipt event FSM and keeps exact terminal replay idempotent",
    "fails closed when a receipt payload or event hash is tampered on disk",
    "fails closed on canonical-hashed malformed payloads and forged chronology",
    "cannot persist prompt, output, argv, or credentials through the typed receipt contract",
    "rejects malformed runtime variants across every event payload union",
    "keeps Mission V1 event shapes valid while enforcing called identity when supplied",
    "scans only bounded open dispatch heads oldest-first and tenant-scoped",
    "compares and orders valid offset timestamps by absolute time",
    "reconciles an exact open head once with durable evidence provenance",
    "refuses stale-head reconciliation and altered retry evidence",
    "validates bounded scan and reconciliation evidence inputs",
    "atomically rolls back declaration and rejection when the terminal suffix is invalid",
    "rejects unknown/generic pre-dispatch causes and attestation without canonical evidence",
    "rejects a canonical-hashed but structurally forged receipt",
    "scans exact tenant/project/task receipts and opens an existing store read-only",
    "bulk scans thousands of canonical task ids in bounded chunks without losing ambiguity",
    "scans high-cardinality tenant tuples once under one project snapshot",
    "validates bulk task receipt scans strictly and keeps empty input query-free"
   ],
   "core/kpi/breach-advisor.test.ts": [
    "one critical + one healthy → names ONLY the breached KPI (value/target/status + en labels)",
    "warning status is surfaced too (not only critical); null target → —",
    "unknown status (no data / div-by-zero) is non-healthy → surfaced",
    "multiple breaches → one bullet each, preserving deterministic definition order",
    "lang=\"tr\" localizes field labels and the KPI title",
    "defaults to en when lang is omitted",
    "all-healthy views → \"\" (no section)",
    "empty views array → \"\"",
    "all null-result (no data) views → \"\" (no data is not a breach)",
    "never throws on a non-finite value; renders — for it"
   ],
   "core/lazy-loader.test.ts": [
    "does not call loader until value is accessed",
    "calls loader on first access",
    "caches result after first access",
    "reset forces reload on next access",
    "handles loader returning undefined",
    "handles loader returning null",
    "registers and lazily loads a value",
    "returns undefined for unregistered key",
    "caches loaded value",
    "returns false before first access",
    "returns true after access",
    "returns false for unregistered key",
    "resets a specific key",
    "resets all keys",
    "preloads specified keys",
    "preloads all keys when no argument",
    "skips unregistered keys during preload",
    "returns true for registered key",
    "returns false for unregistered key",
    "returns all registered keys",
    "tracks size correctly",
    "overwrites loader on re-register"
   ],
   "core/limit-ledger-cache-gate.test.ts": [
    "returns N/A (applicable=false) for a single-session sprint",
    "returns N/A when no sessions have a taskMap entry",
    "PASS when all follower sessions read from cache (warm-share=1.0)",
    "FAIL when no follower sessions read from cache (warm-share=0.0)",
    "FAIL when warm-share is 0.75 (below 0.8 threshold)",
    "PASS when warm-share is exactly 0.8 (4 of 5 followers)",
    "selects the chronologically earliest session as the warmer",
    "correctly uses only first call of each session for the gate check",
    "skips sessions not present in taskMap"
   ],
   "core/limit-ledger-report.test.ts": [
    "extracts a base task ID from text",
    "returns the most specific match when fix-chain is present",
    "prefers single-fix over base when both appear",
    "returns null when no task pattern is present",
    "is case-insensitive for the pattern",
    "extracts task ID from message.content text block in JSONL",
    "extracts task ID from top-level content string",
    "only checks the first 6 lines",
    "finds task ID on line 6 (index 5 — last allowed)",
    "tolerates malformed JSON lines without throwing",
    "returns null for empty lines array",
    "aggregates records for a single task",
    "bootstrapCw equals the first call cacheWrite (by ts order)",
    "bootstrapCw uses insertion order when ts is null",
    "computes hitRate as cacheRead / (in + cacheRead)",
    "hitRate is 0 when in and cacheRead are both 0",
    "computes limitCost using provided prices",
    "sprint totals sum across all tasks correctly",
    "bootstrapShare = sum(bootstrapCw) / totals.cacheWrite",
    "bootstrapShare is 0 when totals.cacheWrite is 0",
    "excludes records whose sessionFile is not in taskMap",
    "applies durationMs from durationMap to matching tasks",
    "does not set durationMs when not in durationMap",
    "selects dominant model by call count",
    "returns empty tasks and zero totals for empty records",
    "keeps only the sprint\\'s tasks (prefix match, fix-suffixes included)",
    "accepts the \"sprint-NNN\" form",
    "returns an empty map when nothing matches",
    "maps session files to task IDs via injected seams (hermetic)",
    "skips unreadable files and returns {} for an unreadable root"
   ],
   "core/limit-ledger.test.ts": [
    "extracts usage records from a valid .jsonl file",
    "deduplicates records with the same message id",
    "includes two records with distinct message ids",
    "filters by since window — excludes records before since",
    "filters by until window — excludes records after until",
    "applies projectFilter to exclude directories",
    "skips <synthetic> model entries",
    "tolerates corrupt/malformed JSON lines without throwing",
    "returns empty array when projects directory is empty",
    "captures cacheRead and cacheWrite fields",
    "uses real tmpdir for file-based integration",
    "computes in·$in + out·$out + cacheWrite·1.25·$in with known numbers",
    "cacheRead does not contribute to cost (0 weight)",
    "cacheWrite costs 1.25× input rate",
    "returns 0 for models not in prices map",
    "sums across multiple records and models",
    "returns 0 for empty records array",
    "returns 0 for empty prices map",
    "prices drifted model IDs via family fallback (stale-key regression, 2026-06-11)",
    "returns exact match when the model ID is a key",
    "falls back to the longest contained key for drifted IDs",
    "ignores keys shorter than 4 chars in the fallback (no accidental \"o3\" hits)",
    "returns null when neither exact nor family match exists"
   ],
   "core/limit-preflight.test.ts": [
    "parses all three lines including reset text + timezone",
    "omits weekFablePct when the Fable line is absent",
    "tolerates a missing \"resets\" clause — pct still parses, resetAt is null",
    "returns unavailable when the \"Current session\" line is missing",
    "returns unavailable when the \"Current week (all models)\" line is missing",
    "returns unavailable, never throws, on garbage/empty input",
    "returns unavailable when the session line has an unparseable percentage",
    "silently omits an unparseable Fable line rather than failing the whole probe",
    "parses a successful spawn (exit 0) into a probe, never invoking real claude",
    "honors a custom claudeBin override",
    "uses the Windows wrapper and scrubs canonical plus config-defined credentials",
    "returns unavailable when spawn emits an error event (e.g. ENOENT)",
    "returns unavailable when spawnImpl throws synchronously",
    "returns unavailable when the process exits non-zero",
    "returns unavailable and kills the child when the timeout elapses",
    "bounds stdout and rejects a truncated usage snapshot",
    "rejects an invalid output bound without spawning",
    "never throws even when the CLI output format changes unexpectedly",
    "returns ok when every window is below the warn threshold",
    "returns warn at the exact warnPct boundary",
    "returns warn between warnPct and blockPct",
    "returns block at the exact blockPct boundary",
    "returns block above blockPct",
    "uses the worst window across session/weekAll/weekFable, naming it in the reason",
    "ignores weekFablePct in the comparison when absent",
    "returns unknown when the probe is unavailable — never fabricates a healthy state",
    "uses DEFAULT_LIMIT_GATE_THRESHOLDS when no thresholds are passed",
    "defaults to {warnPct:70,blockPct:90} for both windows when no override is given — byte-identical to the pre-362-004 min(70,block) formula",
    "recomputes the min(70,block) warn floor from a custom block override when no warn override is given",
    "honors an explicit sessionWarnPct override independently of weekly",
    "honors an explicit weeklyWarnPct override independently of session",
    "clamps a warn override down to the block ceiling when the override exceeds it",
    "matches evaluateLimitGate/min(70,block) semantics when no thresholds are passed",
    "evaluates session and weekly independently — a lenient session warn floor stays ok while a tighter weekly floor trips warn",
    "evaluates the reverse independently — a tight session warn floor trips warn while a lenient weekly floor stays ok",
    "applies the weekly thresholds to the week (Fable) window",
    "returns unknown when the probe is unavailable",
    "block always wins over warn even when a warn-triggering window has a higher raw percentage",
    "roundtrip: an explicit warn override actually moves the verdict boundary end-to-end"
   ],
   "core/lineage-causal-authority.test.ts": [
    "admits only the earliest repair born from the current unresolved head",
    "makes a successful admitted repair absorbing despite stale descendants",
    "pins sprint-488 successful 001/010/013 against unnecessary XFIX leaves",
    "does not let forcedByBlockedDependents bypass settled-DONE authority"
   ],
   "core/lineage-usage-authority.test.ts": [
    "resolves dynamic FIX attempts through the original logical root and retains all usage",
    "keeps %s executions at zero billed USD without discarding their reference cost",
    "uses the metered invoice, never reference cost, and fails closed when it is absent",
    "keeps %s billing typed unknown",
    "does not infer metered billing when the logical root is absent"
   ],
   "core/live-execution-budget.test.ts": [
    "requests owner-configured landing at 75% while preserving the hard ceiling",
    "rounds a fractional turn reserve upward before another indivisible provider call",
    "records applied deltas and neutral consecutive cache-read evidence without duplicate inflation",
    "restores v2 landing and repeated-read evidence without double counting",
    "deduplicates repeated Claude blocks by provider message id",
    "trips close to Sprint-455 call 32 without repeated-block inflation",
    "reconciles a final cumulative result as a snapshot instead of double-counting",
    "measures and deterministically deduplicates an id-less Codex terminal replay",
    "measures Gemini final-only usageMetadata with fresh input (cached subset separated)",
    "tracks per-call context separately from cumulative usage",
    "refuses usage without a stable provider call identity",
    "does not use a session id as an incremental call dedupe identity",
    "keeps maxContextTokens unknown for cumulative-only evidence",
    "replays a host-stamped canonical LogEvent without trusting arbitrary nesting",
    "does not claim maxUsd alone is enforceable before final billing",
    "rejects maxUsd before dispatch until immutable live pricing is supported",
    "rejects missing, empty, and unknown remote budgets before dispatch",
    "allows budgetless execution only for an explicitly local executor",
    "uses the typed admission error with an actionable unmetered-backend remedy",
    "keeps metering and landing capability independent at final admission",
    "codex inclusive schema: cached_input_tokens subtracts from input_tokens",
    "anthropic disjoint schema: input_tokens stays untouched",
    "openai nested prompt_tokens_details.cached_tokens also subtracts"
   ],
   "core/live-w1-staleHb.test.ts": [
    "DEFAULT_HEARTBEAT_TIMEOUT_MS equals config.heartbeat_timeout * 1000 (SSOT alignment)",
    "config nervous_system.detectors.stale_worker.threshold_ms defaults to DEFAULT_HEARTBEAT_TIMEOUT_MS",
    "StaleWorkerDetector detects a host-confirmed dead worker attempt",
    "StaleWorkerDetector does not alert while the host reports the attempt alive",
    "detector and auditor share the same SSOT value (both equal DEFAULT_HEARTBEAT_TIMEOUT_MS)",
    "APPROVE_TIMEOUT_ATTENDED_MS = 30000 (30s for interactive sessions)",
    "APPROVE_TIMEOUT_UNATTENDED_MS = 5000 (5s for unattended/CI)",
    "detectAttendedSession returns true when SSH_CONNECTION is set",
    "detectAttendedSession returns true when TERM is a real terminal",
    "detectAttendedSession returns false when TERM=dumb and no SSH_CONNECTION (non-TTY env)",
    "safety-floor: shouldArmAutoProceed(locked=true, …) is always false",
    "non-safety-floor with positive timeout: shouldArmAutoProceed returns true",
    "auto-proceed disabled (timeout ≤ 0) even for non-safety-floor actions"
   ],
   "core/local-llm-registry.test.ts": [
    "registers a fresh healthy local identity at zero cost without claiming ollama",
    "holds stale, unhealthy, or identity-mismatched endpoint evidence",
    "admits explicit local ownership parametrically and holds local-to-cloud equivalence",
    "retains a keyless candidate and bootstraps auth/cost metadata after live health and identity",
    "freshly probes a local primary and holds before consulting a cloud fallback"
   ],
   "core/log-event.test.ts": [
    "maps a Claude SDK-message tool_use → tool_use",
    "maps a direct Claude tool_use content-block → tool_use",
    "maps a Claude assistant text message → text",
    "maps a Claude user tool_result message → tool_result",
    "maps the Claude final result envelope (carries usage) → usage",
    "attaches canonical terminal semantics to an id-less Codex turn.completed envelope",
    "maps a Claude system/init event → lifecycle",
    "maps a raw Anthropic streaming content_block_start(tool_use) → tool_use",
    "maps an Ollama /api/generate streaming chunk → text",
    "maps an Ollama /api/chat streaming chunk → text",
    "maps the Ollama final stats chunk (eval_count) → usage",
    "maps an OpenAI-compatible delta chunk → text",
    "maps an OpenAI-compatible tool_calls delta → tool_use",
    "maps a Gemini candidates chunk → text",
    "maps a Gemini terminal envelope with usageMetadata → canonical usage",
    "NEVER drops an unknown-shape object → text, content preserved",
    "NEVER drops a non-JSON string chunk → text, raw preserved",
    "parses a JSON string chunk before classifying",
    "NEVER drops null/primitive raw → text",
    "always returns one of the declared LogEventType values",
    "appends parseable JSONL lines with the supplied monotonic seq and an ISO ts",
    "creates missing parent directories",
    "is fail-safe — never throws on a bad path",
    "round-trips normalized provider events to JSONL (never dropping any)",
    "persists canonical usage semantics beside the lossless provider payload"
   ],
   "core/logical-progress-projection.test.ts": [
    "folds original, FIX, and FIX-FIX into one logical task while retaining attempts",
    "counts exactly one status per logical task, including an empty projection",
    "rejects raw-attempt and conflicting logical denominators without replacing them",
    "rejects duplicate attempt identity instead of producing a distorted total",
    "treats exact attempt IDs as opaque and uses only explicit canonical identity",
    "rejects a repair edge with conflicting canonical logical identity",
    "rejects an empty canonical identity instead of inferring one from display identity"
   ],
   "core/maintenance-archive.test.ts": [
    "deduplicates concurrent identical publication without a mutable pointer",
    "preserves differing bytes as separate immutable objects",
    "restarts from a manifest, verifies fresh bytes, and replays bytes and mode exactly",
    "detects content and manifest tampering on fresh reads",
    "rejects path escape, absolute paths, and symlink sources or destinations"
   ],
   "core/manifest-migrator.test.ts": [
    "returns true for v1 (no manifestVersion)",
    "returns true for manifestVersion 1",
    "returns false for manifestVersion 2",
    "returns true for v2",
    "returns false for v1",
    "migrates v1 security-auditor agent",
    "no-ops on already-v2 agent",
    "handles agent with no trigger keywords",
    "migrates v1 typescript-expert skill",
    "migrates v1 ci-testing skill",
    "no-ops on already-v2 skill",
    "refactorer: refactor-only → refactor:primary, no build",
    "implementer: implementation → build:primary",
    "security-auditor: reviewer role → review:primary + security domain",
    "doc-writer: exclusions → 'never' entries",
    "maps 10→primary, 7-9→secondary, 5-6→able",
    "true when no write tool is denied, false when Edit/Write denied",
    "infers tier from the preferred model id",
    "devops → configure + devops/ci domain (registry-canonical id)",
    "performance → analyze + unknown 'performance' domain flagged but kept",
    "design/architecture → analyze + build:secondary",
    "flags an unmapped V2 intent as a typed issue (never throws)",
    "null / non-object manifest → invalid-manifest issue, valid fallback",
    "manifest with malformed activation → no-activation issue, no throw",
    "provisional flag is always set"
   ],
   "core/manifest-schema-lint.test.ts": [
    "defaults missing composableWith to []",
    "defaults missing triggers to []",
    "defaults missing category to a valid SkillCategory",
    "defaults an entirely-missing stackDetection to {files:[],dependencies:[],commands:[]}",
    "defaults a partially-populated stackDetection — missing sub-fields only",
    "defaults missing deniedTools to []",
    "defaults missing expertise to []",
    "skips manifest.json with invalid JSON (unchanged fail-soft behavior)",
    "rejects (does not silently normalize) a wrong-typed composableWith",
    "rejects (does not silently normalize) a wrong-typed expertise"
   ],
   "core/marketplace/dependency-resolver.test.ts": [
    "resolves skill with no dependencies",
    "resolves linear dependency chain A -> B -> C",
    "resolves diamond dependency (A -> B,C; B -> D; C -> D)",
    "throws CircularDependencyError for circular deps",
    "handles unknown dependencies gracefully",
    "records versions in resolved map",
    "returns null for no circular dependencies",
    "returns cycle path for circular dependency",
    "returns null for standalone skill",
    "picks highest version",
    "handles single version",
    "handles empty version list",
    "compares all semver components",
    "returns ordered install list",
    "returns single entry for no-dep skill",
    "reads manifest from skills directory",
    "handles missing manifest file"
   ],
   "core/marketplace/marketplace-auth.test.ts": [
    "stores a token",
    "sets file permissions to 0600",
    "creates credentials directory if not exists",
    "throws on empty token",
    "throws on whitespace-only token",
    "trims token before storing",
    "deletes token file",
    "returns false if no token file",
    "returns stored token",
    "returns null if no token file",
    "returns null on corrupted JSON",
    "returns true when token exists",
    "returns false when no token",
    "returns true for valid token (>= 8 chars, no spaces)",
    "returns false for short token",
    "returns false for token with spaces",
    "returns false for empty string",
    "returns false for non-string"
   ],
   "core/marketplace/rating-system.test.ts": [
    "returns 0 for zero stats",
    "returns max 5 for perfect stats",
    "weights success rate at 60%",
    "weights coverage at 30%",
    "weights frequency at 10%",
    "caps frequency at 100",
    "clamps negative values to 0",
    "persists rating to data file",
    "submits a valid rating",
    "throws for rating below 1",
    "throws for rating above 5",
    "throws for non-integer rating",
    "allows optional comment",
    "returns empty data when no file",
    "returns stored data",
    "returns null for unknown skill",
    "returns rating after calculateLocalRating",
    "returns empty array for no submissions",
    "formats rating as X/5",
    "clamps to 0-5 range",
    "rounds to 1 decimal"
   ],
   "core/marketplace/registry-client.test.ts": [
    "uses default registry URL",
    "accepts custom registry URL",
    "accepts custom timeout",
    "returns search results on success",
    "passes category as query parameter",
    "respects page and limit options",
    "clamps page to minimum 1",
    "clamps limit to 1-100 range",
    "throws RegistryRateLimitError on 429",
    "includes retryAfter in rate limit error",
    "throws RegistryNetworkError on 500",
    "throws RegistryNetworkError on network failure",
    "throws on invalid JSON response",
    "uses GET method for search",
    "returns skill detail on success",
    "throws on empty skill name",
    "encodes skill name in URL",
    "throws RegistryNetworkError on 404",
    "sends POST with auth header",
    "sends request body as JSON",
    "creates client with custom timeout"
   ],
   "core/marketplace/skill-sandbox.test.ts": [
    "returns safe for clean skill",
    "detects eval() usage",
    "detects child_process usage",
    "detects process.env access",
    "detects Function constructor",
    "returns unsafe for non-existent directory",
    "counts scanned files correctly",
    "detects AST violations in .ts files during safety scan",
    "does not run AST scan on .json files",
    "does not run AST scan on .md files",
    "clean SKILL.md passes both checks",
    "returns valid for correct manifest",
    "rejects missing id",
    "rejects invalid semver",
    "rejects non-existent manifest",
    "handles parse errors",
    "moves skill to .quarantine directory",
    "returns false for non-existent skill",
    "creates quarantine directory if needed",
    "built-in skills are trusted",
    "unknown skills are not trusted by default",
    "trustSkill adds skill to trusted set",
    "extraTrusted option pre-trusts skills",
    "returns list of built-in trusted skills",
    "detects eval() call",
    "detects Function() constructor call",
    "detects new Function() constructor",
    "detects require(\"child_process\")",
    "detects require(\"node:child_process\")",
    "detects require(\"fs\")",
    "detects require(\"os\")",
    "detects dynamic import(\"child_process\")",
    "detects dynamic import(\"node:fs\")",
    "detects bracket-access eval: globalThis[\"eval\"]()",
    "detects obfuscated eval via string concatenation: global[\"ev\"+\"al\"]()",
    "detects property access: global.eval",
    "detects property access: globalThis.Function",
    "detects setTimeout with string argument",
    "detects setInterval with string argument",
    "allows setTimeout with function argument",
    "returns empty for clean code",
    "returns empty for clean code with imports",
    "detects multiple violations in single file",
    "handles empty content",
    "handles syntax errors gracefully"
   ],
   "core/memory-backup-and-abort.test.ts": [
    "aborts when batch equals exactly 50% of non-exempt entries (>= boundary)",
    "aborts when batch is strictly greater than 50% of non-exempt entries",
    "does NOT abort when batch is less than 50% of non-exempt entries",
    "decay-exempt entries survive even when batch is below threshold",
    "backup creates a non-empty file that preserves entry count",
    "backup preserves data integrity after WAL checkpoint with active entries"
   ],
   "core/memory-export-compact.test.ts": [
    "preserves complete meaning and every source record without an entry cap",
    "orders ordinal sprints deterministically and keeps one truthful legacy plus one null group",
    "keeps every ID reachable while bounding a large human view",
    "spills one oversized single-line unit by bytes without cutting its source",
    "retains every MemoryEntryV2 provenance field in the details authority",
    "uses locale-independent structural guard metadata and replays deterministic bytes",
    "does not let a legacy no-options caller overwrite a configured compact projection",
    "holds a localized partial render by structural count rather than translated empty text",
    "finishes every render before the first output write",
    "preserves an existing output boundary when the atomic writer cannot create files",
    "holds without output while another writer owns the export lock"
   ],
   "core/memory-export-labels.test.ts": [
    "uses English by default and returns only resolved plain strings",
    "forwards the selected language without importing a catalog into core"
   ],
   "core/memory-export.test.ts": [
    "contains ADR IDs, titles, and status",
    "is under 5000 chars",
    "contains recent learnings",
    "contains active debt",
    "contains active patterns",
    "aggregates same-title patterns into one line with a sprint count",
    "contains total entry count and generation marker",
    "has Brain Summary header",
    "handles empty store gracefully",
    "has proper ## ADR-NNN: Title format with **Status:**",
    "includes full content of each ADR",
    "has auto-generated header",
    "separates ADRs with horizontal rule",
    "orders ADRs by ID",
    "returns header-only for empty store",
    "groups by sprint",
    "contains learning titles and content",
    "does not emit trailing whitespace from stored memory content",
    "orders sprints descending (newest first)",
    "has auto-generated header",
    "returns header-only for empty store",
    "contains active debt with priority",
    "has Active Technical Debt section",
    "has markdown table format with headers",
    "has auto-generated header",
    "shows resolved debt section when present",
    "returns header with empty tables for empty store"
   ],
   "core/memory-import.test.ts": [
    "extracts words > 3 chars (minLength: 4), lowercased, unique",
    "removes common stop words",
    "removes Turkish stop words",
    "limits to max 15 keywords (maxResults: 15)",
    "returns empty array for empty string",
    "deduplicates keywords",
    "default minLength is 2 — keeps 2/3-char tokens that minLength:4 would drop",
    "default is uncapped — returns more than 15 keywords when present",
    "default still filters the EN+TR base stopwords",
    "extraStopwords are unioned with the base set (consumer-specific filter)",
    "parses 2 ADRs with correct id, title, status, content",
    "handles accepted and deprecated statuses",
    "content does not include the header line itself",
    "generates tags from title + content keywords",
    "returns empty array for content without ADRs",
    "parses 2 sprint sections with correct sprint_id, sprint_num",
    "content includes bullet points",
    "handles both \"Sprint sprint-NNN\" and \"Sprint NNN\" header formats",
    "returns empty array for content without sprint sections",
    "generates tags from content keywords",
    "parses 2 debt rows from pipe table",
    "maps resolved vs active status correctly",
    "converts priority to lowercase",
    "metadata contains origin fields",
    "sets sprint_id from originSprintId column",
    "returns empty array for content without table"
   ],
   "core/memory-normalize.test.ts": [
    "converts Turkish I to ı then to i",
    "converts Turkish İ to i",
    "handles all Turkish special chars lowercase",
    "handles all Turkish special chars uppercase",
    "normalizes German umlauts",
    "preserves ASCII text unchanged",
    "handles mixed case",
    "handles empty string",
    "preserves technical terms lowercased",
    "handles Spanish accents",
    "handles French accents",
    "converts ÇÖKME to cokme",
    "normalizes öğrenci",
    "normalizes üzüm",
    "handles numbers and special chars"
   ],
   "core/memory-query.test.ts": [
    "finds entries by FTS query (docker heartbeat → mem-139-001)",
    "returns empty for non-matching query",
    "treats embedded quotes and punctuation as literal FTS input",
    "Turkish normalize: \"brain import\" finds ADR-008 (Turkish content)",
    "filters by type (text=\"spawnSync\", type=[\"adr\"] → only ADR results)",
    "filters by status (status=[\"accepted\"] → only accepted)",
    "returns ADR taxonomy fields on FTS results",
    "filters by sprint range (sprint_range.min=139 → only >=139)",
    "limits results (limit=2 → max 2)",
    "returns snippets with highlights for FTS results",
    "searches all types without filters (broad query)",
    "returns relevance scores for FTS results",
    "throws MemoryQueryError on FTS5 syntax error",
    "uses OR mode by default (broader recall)",
    "uses AND mode when mode=and (narrower, all tokens must match)",
    "AND mode finds entries containing all tokens",
    "explicit mode=or behaves same as default",
    "returns entries ordered by sprint_num DESC when no text",
    "filters by type without text",
    "filters ADRs by class and scope without text",
    "filters by tags_contain (entries must have ALL specified tags)",
    "tags_contain with no matching combo returns empty",
    "excludes soft-deleted entries by default",
    "includes soft-deleted entries when include_deleted=true",
    "filters by decay_exempt",
    "constructs correct params from keywords and scope",
    "omits tags_contain when scope is empty",
    "respects custom opts",
    "always sets mode to or for Brain auto-query",
    "wraps tokens in quotes (default OR join)",
    "joins with space in AND mode",
    "preserves OR/AND/NOT operators without duplication",
    "handles trailing wildcard",
    "doubles embedded quotes in literals and wildcard bases",
    "handles empty input",
    "handles single token",
    "is an instance of Error with correct name",
    "preserves cause",
    "fails closed when an explicit tenant query targets a schema without tenant_id"
   ],
   "core/memory-read-config.test.ts": [
    "shares a single default contract between config, metadata and the read service",
    "preserves project precedence and custom units through effective resolution",
    "rejects invalid runtime config %j without silently granting a different budget",
    "accepts positive one-unit views without turning them into a storage cap",
    "provides complete worker authority capacity without expanding other consumers",
    "does not materialize implicit read defaults through automatic config migration",
    "never treats a worker default as a floor over explicit shared caps",
    "resolves named overrides after shared settings with project precedence",
    "rejects invalid consumer profiles %j",
    "returns immutable effective limits without mutating authored profiles",
    "validates entry/candidate relationships after merging, not against invented intermediate defaults",
    "uses the same layer merge as resolved config without creating missing files",
    "preserves exact authored bytes and global/project overrides",
    "does not hide changes behind a cached default or mutate corrupted input",
    "matches effective config consumer profiles without changing either authored file",
    "fails explicitly on invalid view settings %j"
   ],
   "core/memory-read-labels.test.ts": [
    "injects the caller catalog and defaults to English",
    "resolves every label and state message in %s",
    "preserves every canonical memory-read catalog byte in %s",
    "matches getMessage explicit locale semantics for %s",
    "keeps an explicit English config locale when the host environment is Turkish",
    "keeps an explicit Turkish config locale when the host environment is English",
    "keeps memory-read consumers out of the CLI helper layer"
   ],
   "core/memory-read-planner-profile.test.ts": [
    "is declared and reaches the planner through the resolver",
    "admits the largest single ADR measured in this repo (287 lines / 23 KB)",
    "exceeds the shared default that used to apply to it",
    "carries a larger mandatory budget than the worker",
    "keeps the worker profile unchanged",
    "leaves a consumer without a declared profile on the shared default",
    "an authored shared limit wins over the declared default",
    "an explicit named override wins over the shared limit",
    "a partial override keeps the declared default for untouched limits",
    "resolveMemoryReadLimitsForConsumer agrees with the profile map"
   ],
   "core/memory-read-service.test.ts": [
    "turns large operator-bearing prose into a bounded literal discovery query",
    "returns whole content with raw UTF-8 digest under a bounded local-project selection",
    "uses exact verified-tenant scope while local-project preserves the existing all-project read",
    "defers an oversized optional meaning unit without loading a partial body",
    "invalidates same-second, same-length detail and cursor authorities through entry history",
    "defers giant metadata from SQL size evidence before a complete-row fetch is needed",
    "bounds visible opaque detail references and returns one continuation for the rest",
    "holds when a required whole unit is missing or exceeds either content budget",
    "includes only unresolved priority-critical context, not every accepted ADR",
    "selects the latest optional identity and retro whole within the exact tenant scope",
    "ignores an absent optional latest role but HOLDs a present role that cannot fit whole",
    "binds optional latest selectors into request validation and query authority",
    "binds continuation to consumer, scope, query, limits and the prior exact candidate page",
    "never renders HOLD as empty context and preserves each admitted content unit",
    "resolves only one accepted scoped ADR canonical ID and feeds exact requiredIds",
    "holds missing, non-ADR and ambiguous case-folded references",
    "treats missing preset references as optional but preserves ambiguity as HOLD"
   ],
   "core/memory-rebuild-safety.test.ts": [
    "returns every row in the relations table",
    "returns empty array when no relations exist",
    "is idempotent — second call inserts zero new rows",
    "skips orphan relations when referenced entry is missing",
    "keeps relations count equal before and after a benign rebuild",
    "strict mode throws DECKENT_MEMORY_RELATION_LOSS and rolls back when relations would be lost",
    "non-strict mode does not throw when relations are lost (accepts the loss)"
   ],
   "core/memory-relations-migration.test.ts": [
    "insertRelation: insert basic reference",
    "insertRelation: dedupe duplicate",
    "insertRelation: foreign key validation (skip orphan)",
    "insertRelation: 6 MADR v3 types"
   ],
   "core/memory-store-chat.test.ts": [
    "returns the provided session id verbatim when one is given",
    "generates a session id when none is provided",
    "treats blank session ids as missing and generates one instead",
    "persists turns in order with monotonic turn_index and ChatTurn shape",
    "returns only the most recent N turns when limit is provided",
    "isolates turns by session_id and rejects empty session ids",
    "stores chat turns as type=chat entries indexed by FTS5 for deckent recall",
    "round-trips role tag and metadata for getChatHistory after store reopen",
    "returns one summary per session with turn count and preview",
    "orders most-recently-active first and respects the limit",
    "truncates a long preview to ~60 chars with an ellipsis",
    "returns an empty array when there are no chat sessions"
   ],
   "core/memory-store-readonly.test.ts": [
    "reads complete source records without allowing writes or changing persisted database bytes",
    "never creates a missing database in read-only mode",
    "holds one WAL read revision while a different connection updates the source",
    "forbids mutations during nested snapshots and restores writer capability after an exception",
    "retains tenant isolation on read-only query paths",
    "rejects async callbacks before invoking them, and restores capability after rejected thenables"
   ],
   "core/memory-store.hermetic.test.ts": [
    "persists a row retrievable via getById with correct defaults",
    "computes content_norm on insert",
    "is immediately searchable via FTS5 after insert (entries_ai trigger)",
    "does not leak rows across independent tmpdir DB instances",
    "inserts when the id does not yet exist",
    "updates an existing row and recomputes content_norm",
    "keeps the updated content searchable via FTS5 (entries_au trigger)",
    "leaves an untouched row out of history on a no-op upsert",
    "orders results by sprint_num DESC and excludes soft-deleted rows",
    "isolates rows by tenant when a tenantId is supplied",
    "applies the ADR-G-019 adr_class taxonomy filter",
    "applies the ADR-G-019 scope taxonomy filter",
    "returns an empty array for a type with no rows",
    "finds an entry by a content keyword",
    "filters FTS results by type",
    "excludes soft-deleted rows from FTS results by default",
    "returns no results for a query matching nothing",
    "soft-deletes entries older than the decay threshold",
    "never decays decay_exempt entries regardless of age",
    "removes decayed entries from FTS search results",
    "aborts a catastrophic batch (>=3 entries, >=50% of non-exempt rows)"
   ],
   "core/memory-store.test.ts": [
    "insert + getById returns the entry",
    "insert computes normalized fields",
    "insert with defaults for optional fields",
    "insert with relations creates relation rows",
    "insert records a create history entry",
    "getById returns null for non-existent id",
    "getById excludes soft-deleted by default",
    "getById with includeDeleted returns soft-deleted",
    "getByType returns entries ordered by sprint_num DESC",
    "getByType excludes soft-deleted entries",
    "upsert inserts when entry does not exist",
    "upsert updates existing entry and records field history",
    "upsert replaces tags on update",
    "upsert does not record history for unchanged fields",
    "getTagsForEntry returns all tags",
    "getTagsForEntry returns empty array for no tags",
    "getByTags returns entries with ANY matching tag",
    "getByTags with multiple tags returns entries having ANY",
    "getByTags excludes soft-deleted entries",
    "getByTags returns empty for no matches",
    "getRelationsFrom returns outgoing relations",
    "getRelationsTo returns incoming relations",
    "getRelationsFrom returns empty for no relations",
    "getHistory returns all records ordered by id",
    "getHistory returns empty for non-existent entry",
    "softDelete sets deleted_at and records history",
    "restore clears deleted_at and records history",
    "soft-deletes old non-exempt entries",
    "preserves decay-exempt entries regardless of age",
    "does not double-delete already soft-deleted entries",
    "records decay history for each decayed entry",
    "countByType returns correct counts",
    "totalCount returns active entry count",
    "totalCount returns 0 for empty DB",
    "getSchemaVersion returns 1",
    "getRawDb returns a Database instance",
    "close does not throw",
    "stores true as 1 and reads back as true",
    "stores false as 0 and reads back as false",
    "inserts a relation between two entries",
    "ignores duplicate relations (INSERT OR IGNORE)",
    "allows different relation types between same entries",
    "returns both from and to relations",
    "returns empty array when no relations exist",
    "returns correct count",
    "returns 0 for empty relations table",
    "extracts single ADR reference",
    "extracts multiple ADR references",
    "deduplicates repeated references",
    "returns empty for no ADR references",
    "does not match partial patterns like ADR-06 or ADR-0001",
    "normalizes to lowercase",
    "auto-inserts references relation when content mentions ADR",
    "does not self-reference",
    "extracts from both title and content",
    "coexists with explicit relations",
    "getByType without tenantId returns all entries (backward compat)",
    "getByType with tenantId isolates rows for that tenant",
    "getById with tenantId returns entry when tenant matches",
    "getById with tenantId returns null for wrong tenant",
    "getByTags with tenantId isolates results",
    "getByTags without tenantId returns all matching entries (backward compat)",
    "tenant_id is persisted and retrievable on inserted entry",
    "getByType with tenantId also returns legacy NULL-tenant rows",
    "getById with tenantId returns legacy NULL-tenant entries",
    "default (no opts) MemoryStore is strict — NULL-tenant rows excluded (born-563 pinned default)",
    "strict mode never leaks rows across tenants in either direction (cross-tenant-SIZMAZ)",
    "permissive (opt-in strictTenantIsolation:false) getByType includes NULL-tenant rows",
    "strict getByType excludes NULL-tenant rows",
    "permissive (opt-in strictTenantIsolation:false) getById with tenantId includes NULL-tenant rows",
    "strict getById with tenantId excludes NULL-tenant rows",
    "strict getById with tenantId returns own-tenant rows",
    "permissive (opt-in strictTenantIsolation:false) getByTags with tenantId includes NULL-tenant rows",
    "strict getByTags excludes NULL-tenant rows",
    "strict getByType without tenantId still returns all entries (no tenant filter)",
    "upsert updates adr-010 title and content to amended version",
    "amendment records field-level history diff",
    "amended adr-010 is discoverable via tag query"
   ],
   "core/memory-stub-backfill.test.ts": [
    "backfill replaces stub content with archive content + clears stub_flag",
    "idempotent — already-filled entry skipped on re-run",
    "skips when archive file is missing",
    "skips when entry does not exist"
   ],
   "core/memory-tenant-isolation.test.ts": [
    "tenant-A insert + tenant-B query via getById → A row invisible",
    "tenant-A insert + tenant-B query via getByType → A row invisible",
    "tenant-A insert + tenant-B query via getByTags → A row invisible",
    "NULL-tenant row does not match an explicit-tenantId getById query",
    "NULL-tenant row does not match an explicit-tenantId getByType query",
    "NULL-tenant row does not match an explicit-tenantId getByTags query",
    "a NULL-tenant row never surfaces across ANY explicit tenant query (no all-tenant leak)",
    "existing single-tenant path is preserved: no-tenantId query is unaffected by the new default",
    "explicit opt-out (strictTenantIsolation: false) restores the legacy permissive NULL-fallback"
   ],
   "core/memory-tenant-scope.test.ts": [
    "tenantId=\"tenant-a\" returns only tenant-a rows",
    "tenantId=\"tenant-b\" returns only tenant-b rows",
    "fail-closed: a NULL-tenant row never matches an explicit tenantId query",
    "tenantId composes with other structured filters (type + tenantId)",
    "tenantId=\"tenant-a\" scopes FTS results to only tenant-a",
    "tenantId=\"tenant-b\" scopes FTS results to only tenant-b",
    "fail-closed: NULL-tenant row is excluded from FTS results under an explicit tenantId",
    "structured search without tenantId returns every row (all tenants + NULL)",
    "FTS search without tenantId returns every matching row (all tenants + NULL)",
    "existing memory-query.test.ts-style calls are unaffected (type filter, no tenantId)",
    "does not throw and returns unfiltered rows (structured path)",
    "emits an honest warn (not silent) when the column is missing",
    "req carrying tenant-a claim → only tenant-a rows returned",
    "req carrying tenant-b claim → only tenant-b rows returned",
    "no req at all (server.ts call site without req) → tenant-less legacy path, both rows",
    "req present but no resolvable tenant claim → tenant-less path, both rows"
   ],
   "core/metering-truth.test.ts": [
    "DISPROVES the \"sonnet-5 3/15 → 5/25\" premise: SSOT + registry agree at 3/15",
    "RED→GREEN: opus maxOutputTokens drift is now FIXED (128K, was unset)",
    "RED→GREEN: haiku maxOutputTokens drift is now FIXED (64K, was unset)",
    "SURFACES (not silences) the known haiku cost drift 0.8/4 vs SSOT 1/5",
    "after the maxOut fix, the ONLY residual claude drift is the known haiku cost",
    "prices EVERY entry incl off-task helper calls (haiku helper on-ledger)",
    "detects the 413-002/003 under-count (provider $8.48 vs ledger $5.08 = ~40%)",
    "LOUD-warns on drift and stays silent within tolerance (variance sessiz kalmaz)",
    "reconcile ties the bridge under-count to a loud alert",
    "RED→GREEN: naive visible-only estimate is 8–10× low; fixed loads close the gap",
    "fixed loads dominate a small task (the whole point of the under-count)",
    "RED→GREEN: coverage average never NaN when a result lacks coverage",
    "coverage guard returns 0 (not NaN) when no result has finite coverage",
    "RED→GREEN: distinguishes 5-attempt / 4-task instead of conflating to 5",
    "RED→GREEN: surfaces REAL files-changed/cost fields, not 0-placeholders"
   ],
   "core/model-activation-store.test.ts": [
    "an UNRECORDED model is active, so installing the store narrows nothing",
    "records a deactivation and reports it",
    "re-activating overwrites the decision rather than duplicating it",
    "clearing a decision restores the default (active)",
    "decisions are per-provider — the same model id under another provider is untouched",
    "refuses empty provider/model input instead of writing a junk row",
    "persists across connections (the decision is durable, not in-memory)",
    "returns one immutable strict snapshot with complete owner records",
    "gives absent storage the documented ready empty snapshot",
    "treats an absent store as the documented implicit-active default",
    "reads the exact project store instead of trusting process bootstrap order",
    "holds instead of opening every model when an existing store is unreadable",
    "holds when a structurally readable store contains an invalid policy decision",
    "is empty and NEVER throws when no store exists (fail-safe discovery)",
    "returns exactly the deactivated pairs, keyed by provider+model",
    "defaults to implicit-active for any unrecorded provider",
    "records explicit-active and reports it (durable, per-provider)",
    "rejects an unknown mode instead of writing a junk policy",
    "isExecutable under implicit-active == isActive (unrecorded stays eligible)",
    "isExecutable under explicit-active admits ONLY active=true records",
    "opens a v1 store, preserves model_activation rows, and enables policy writes",
    "is the all-executable fail-safe when no store exists",
    "reflects explicit-active semantics + a sorted active-set",
    "the snapshot digest is stable across identical stores and shifts on any change",
    "unregisters a deactivated model so nothing downstream can select it",
    "leaves every model registered when nothing is deactivated (default path)",
    "omitting projectRoot AND inactiveModels keeps the pre-store behaviour"
   ],
   "core/model-catalog-bootstrap.test.ts": [
    "(a) fetch success → mergeFromCatalog called with remote models",
    "(b) fetch fail + warm cache → mergeFromCatalog called with cached models",
    "(c) fetch fail + no cache → mergeFromCatalog called with bundled BUILTIN_MODELS",
    "(d) offline:true → network skipped, cache or bundled used"
   ],
   "core/model-catalog.test.ts": [
    "maps anthropic/openai/google aliases to canonical providers",
    "maps tier synonyms (max, flagship, pro, mini) to canonical tiers",
    "infers tier from average cost",
    "falls back to ga when status is missing or unknown",
    "produces a complete ModelDefinition from a well-formed remote entry",
    "returns null for unknown providers",
    "infers tier from cost when tier metadata is absent",
    "defaults sensible flags when capabilities are missing",
    "returns the CANONICAL_MODELS snapshot as a defensive copy",
    "returns parsed catalog on 2xx with valid shape",
    "throws DeckentError on non-2xx",
    "throws when payload shape is invalid",
    "throws a typed error before parsing a redirected response",
    "throws a typed error before parsing HTML content",
    "adapts the provider-keyed models.dev payload",
    "fetches, writes cache, returns source=remote",
    "serves cache without network when fetchedAt < TTL",
    "retains all Cursor definitions when a successful remote catalog omits them",
    "retains all Cursor definitions when a successful cached catalog omits them",
    "lets a fresh external definition override a matching canonical identity in stable order",
    "aggregates unsupported remote rows into one deterministic bounded warning",
    "re-fetches when cache older than TTL",
    "falls back to bundled when offline and no cache",
    "uses cache (not bundled) when offline and cache exists",
    "returns cache when remote fails but cache exists",
    "returns bundled when remote fails and cache absent",
    "does not overwrite a stale cache when a typed response guard fails",
    "treats malformed cache as missing and falls back",
    "fetches fresh even when cache is warm",
    "replace mode swaps the entire catalog",
    "merge mode overrides matching ids, preserves the rest",
    "merges the bundled catalog through the offline test seam"
   ],
   "core/model-config-canonicalizer.test.ts": [
    "canonicalizes every supported model-bearing config path with layer evidence",
    "does not mutate input and is idempotent for canonical IDs",
    "leaves unknown values for normal validation and detects only legacy aliases"
   ],
   "core/model-equivalence-lazy.test.ts": [
    "getByTier not called at import time (before any MODEL_TIERS access)",
    "premium tier returns correct model IDs on first access",
    "all tiers populated with correct model IDs",
    "cache idempotent: getByTier called at most 4 times across repeated accesses",
    "getModelsInTier delegates to MODEL_TIERS via Proxy correctly"
   ],
   "core/model-equivalence.test.ts": [
    "returns premium for opus",
    "returns premium for gpt-5",
    "returns premium for gemini-2.5-pro",
    "returns standard for sonnet",
    "returns standard for gpt-4.1",
    "returns premium_plus for o3",
    "returns standard for gemini-2.5-flash",
    "returns economy for haiku",
    "returns economy for gpt-5-mini",
    "returns economy for gpt-4.1-mini",
    "returns standard for o4-mini",
    "returns economy for gemini-2.0-flash",
    "throws for unknown model",
    "opus → gpt-5.5 for codex (premium tier)",
    "fable → gpt-5.6-sol for codex (premium_plus tier)",
    "sonnet → gpt-5.6-terra for codex (standard tier)",
    "haiku → gpt-5.6-luna for codex (economy tier)",
    "opus → gemini-2.5-pro for gemini",
    "sonnet → gemini-2.5-flash for gemini",
    "haiku → gemini-2.0-flash for gemini (economy tier)",
    "gpt-5.5 → opus-5 for claude (premium)",
    "gpt-4.1 → sonnet for claude (standard)",
    "o3 → fable for claude (premium_plus exact tier)",
    "gpt-5-mini → haiku for claude (economy)",
    "gpt-4.1-mini → haiku for claude (economy)",
    "o4-mini → sonnet for claude (standard)",
    "opus → opus for claude",
    "gpt-5 → gpt-5 for codex",
    "gpt-4.1 → gpt-4.1 for codex",
    "gpt-4.1-mini → gpt-4.1-mini for codex",
    "gemini-2.5-pro → gemini-2.5-pro for gemini",
    "gemini-2.0-flash → gemini-2.0-flash for gemini",
    "gemini-2.5-pro → gpt-5.5 for codex (premium)",
    "gemini-2.5-flash → gpt-5.6-terra for codex (standard)",
    "gemini-2.0-flash → gpt-5.6-luna for codex (economy)",
    "gpt-5 → gemini-2.5-pro for gemini (premium)",
    "gpt-4.1 → gemini-2.5-flash for gemini (standard)",
    "o3 → gemini-2.5-pro for gemini (premium_plus falls back to premium)",
    "gpt-5-mini → gemini-2.0-flash for gemini (economy)",
    "gpt-4.1-mini → gemini-2.0-flash for gemini (economy)",
    "o4-mini → gemini-2.5-flash for gemini (standard)",
    "gemini-2.5-pro → opus-5 for claude (premium)",
    "gemini-2.5-flash → sonnet for claude (standard)",
    "gemini-2.0-flash → haiku for claude (economy)",
    "opus is available on claude",
    "opus is NOT available on codex",
    "gpt-5 is available on codex",
    "gpt-5-mini is available on codex",
    "gpt-4.1 is available on codex",
    "gpt-4.1-mini is available on codex",
    "gpt-5 is NOT available on claude",
    "gemini-2.5-pro is available on gemini",
    "gemini-2.0-flash is available on gemini",
    "haiku is NOT available on gemini",
    "gemini-2.0-flash is NOT available on claude",
    "opus belongs to claude",
    "gpt-5 belongs to codex",
    "gpt-5-mini belongs to codex",
    "gpt-4.1 belongs to codex",
    "gpt-4.1-mini belongs to codex",
    "gemini-2.5-flash belongs to gemini",
    "gemini-2.0-flash belongs to gemini",
    "throws for unknown model",
    "premium tier exposes every registered canonical API id",
    "standard tier exposes every registered canonical API id",
    "economy tier exposes every registered canonical API id",
    "premium_plus tier exposes every registered canonical API id",
    "claude has 6 models (Fable 5.1 registered 2026-09-02, primary-source verified)",
    "codex exposes the base and versioned canonical API ids",
    "gemini has 4 models",
    "all tiers defined",
    "total model count includes the versioned 5.6 family, Opus 5, and Cursor Grok family",
    "every model in MODEL_TIERS has a provider",
    "every base tier has at least one model per provider",
    "every model can find an equivalent on every provider",
    "equivalent model always belongs to the target provider",
    "same-provider equivalence returns the same model",
    "premium models map to numeric tier 2",
    "standard models map to numeric tier 1",
    "economy models map to numeric tier 0",
    "premium_plus models map to numeric tier 3"
   ],
   "core/model-identity.test.ts": [
    "uses one identity for every bundled, parity, and canonical model",
    "removes runtime aliases while retaining explicit migration metadata",
    "owns V1 model-to-tier migration in the registry without a consumer dictionary",
    "resolves versioned Sol and Fable API IDs directly",
    "never treats an unowned unknown ID as Claude",
    "preserves an unambiguous or explicitly owned parametric API ID byte-for-byte",
    "rejects dual identity at parametric and registry mutation boundaries",
    "enforces canonical authoring ownership without runtime alias migration"
   ],
   "core/model-registry-apiid.test.ts": [
    "Claude Opus canonical id is current and identical to apiId",
    "Claude Opus 5 is a distinct exact API identity",
    "tier mapping is preserved after apiId update",
    "16-model invariant holds (6 Claude + 6 OpenAI + 4 Gemini — Claude Fable 5.1 added 2026-09-02)",
    "bundled fallback resolves the canonical Opus API ID unchanged",
    "singleton modelRegistry also returns canonical Opus unchanged"
   ],
   "core/model-registry-fable51.test.ts": [
    "is registered under the exact provider API id (pinned dateless snapshot, no alias)",
    "carries the primary-source specification (tier, context, output, price, capabilities, status)",
    "is first-class on a fresh registry and priced by the registry estimator",
    "resolves canonically for provider claude and refuses a foreign provider claim",
    "Fable 5 stays the designated claude/premium_plus generation; Fable 5.1 is selectable only by exact id",
    "no legacy alias points at Fable 5.1 and the `fable` alias still means Fable 5",
    "tier equivalence keeps XVerify provider separation: Fable 5.1 → codex gpt-5.6-sol; sol → claude stays Fable 5",
    "prices from the primary source: $10/$50, cache write 1.25×/2×, cache read 0.025×, no invented aliases",
    "ClaudeAdapter advertises it and pins the exact id + effort vocabulary on the spawn command",
    "under explicit-active the new catalog model is NOT executable until the owner activates it",
    "under the default implicit-active mode it is eligible but still not the tier default"
   ],
   "core/model-registry-ollama.test.ts": [
    "contains at least 3 Ollama entries (premium/standard/economy coverage)",
    "every entry declares provider=\"ollama\"",
    "preserves the provider wire tag byte-for-byte as the canonical identity",
    "every entry has zero cost (local inference)",
    "covers all three resolvable tiers (premium, standard, economy)",
    "returns undefined for ollama tier BEFORE registerOllamaModels is called",
    "resolves premium tier to a qwen-coder-32b-class model after registration",
    "resolves standard tier to a coding-tuned Ollama model after registration",
    "resolves economy tier to a llama3.2-class model after registration",
    "returns undefined for premium_plus (unmapped tier) even after registration",
    "is idempotent — calling registerOllamaModels twice does not duplicate entries",
    "preserves the 15-model BUILTIN invariant — Ollama models are NOT in BUILTIN_MODELS",
    "grows the registry by exactly OLLAMA_BUILTIN_MODELS.length after registration"
   ],
   "core/model-registry-parametric.test.ts": [
    "keeps all bundled builtin models available",
    "getOrThrow() stays strict for unknown ids (rigidity intact on the strict path)",
    "resolve() synthesizes a runtime-validated definition for a brand-new id",
    "resolve({ register: false }) resolves without mutating the registry",
    "infers provider from the id naming convention",
    "matches the cursor- namespace before the vendor branches",
    "cannot infer the cursor effort tier — why the catalog sets it explicitly",
    "infers tier from the id naming convention",
    "resolve() wires the inferred provider + tier onto the new model",
    "buildParametricModel honors explicit overrides over inference"
   ],
   "core/model-registry-pricing-invariant.test.ts": [
    "keeps every mutually priced registry model tier-aligned across all three sources",
    "keeps exactly one preferred Codex model in each preferred tier",
    "resolves Codex premium and premium_plus through their explicit preferences"
   ],
   "core/model-registry.test.ts": [
    "contains exactly 16 models (Claude Fable 5.1 added 2026-09-02, primary-source verified)",
    "has 6 Claude models",
    "has 6 OpenAI/Codex models",
    "has 4 Gemini models",
    "every model has a unique id",
    "every model has a non-empty apiId",
    "every model has valid tier",
    "every model has positive contextWindow",
    "every model has non-negative cost",
    "pins explicit cache-read prices at 0.1x uncached input for the gpt-5.6 family",
    "returns model for valid id",
    "returns undefined for unknown id",
    "returns true for known models",
    "returns false for unknown models",
    "returns model for valid id",
    "throws for unknown model",
    "returns 6 models for claude",
    "returns every canonical Codex identity exactly once, including Astra",
    "returns 4 models for gemini",
    "all returned models belong to the requested provider",
    "returns economy models",
    "returns standard models",
    "returns premium models",
    "returns premium_plus models",
    "returns the DESIGNATED GA model for claude+premium",
    "returns GA model for codex+standard",
    "returns undefined for provider+tier with no GA model",
    "resolves to the designated model even when it registered later",
    "falls back to first-match when the tier designates nothing",
    "rejects a second designation in the same tier at register() time",
    "rejects a catalog that designates two models in one tier",
    "lets a designation coexist with one in a different tier",
    "maps opus → gpt-5.5 (claude premium → codex premium)",
    "maps fable → gpt-5.6-sol (claude premium_plus → codex premium_plus)",
    "maps opus → gemini-2.5-pro (claude premium → gemini premium)",
    "maps gpt-5.5 → opus-5 (codex premium → claude premium)",
    "maps sonnet → gpt-5.6-terra (claude standard → codex standard)",
    "maps sonnet → gemini-2.5-flash (claude standard → gemini standard)",
    "maps haiku → gpt-5.6-luna (claude economy → codex economy)",
    "maps haiku → gemini-2.0-flash (claude economy → gemini economy)",
    "returns same model when target provider matches source",
    "maps premium_plus to claude premium_plus (fable)",
    "throws when no equivalent exists",
    "returns correct tier for each known model",
    "throws for unknown model",
    "orders economy < standard < premium < premium_plus",
    "returns 0 for same tier",
    "returns positive when first > second",
    "opus (premium) is at least economy",
    "opus (premium) is at least standard",
    "opus (premium) is at least premium",
    "opus (premium) is NOT at least premium_plus",
    "haiku (economy) is NOT at least standard",
    "o3 (premium_plus) is at least any tier",
    "adds a new model at runtime",
    "overwrites existing model with same id",
    "removes a model",
    "returns false for non-existent model",
    "returns correct API ID for Claude models",
    "returns correct API ID for OpenAI models",
    "returns correct API ID for Gemini models",
    "throws for unknown model",
    "calculates cost for opus with 1M input + 500K output",
    "calculates official Opus 5 cost with 1M input + 500K output",
    "calculates cost for haiku with 100K input + 50K output",
    "returns 0 for zero tokens",
    "calculates cost for gpt-4.1-mini (cheapest model)",
    "throws for unknown model",
    "returns 0 for economy",
    "returns 1 for standard",
    "returns 2 for premium",
    "returns 3 for premium_plus",
    "returns every canonical core and parity model identity exactly once",
    "returns all canonical model definitions",
    "returns 4 providers: claude, codex, gemini, cursor",
    "accepts custom builtins array",
    "empty array creates empty registry",
    "o3 has reasoning capability",
    "o4-mini has reasoning capability",
    "gemini-3.1-pro-preview has reasoning capability",
    "opus does not have reasoning capability",
    "all models support streaming and toolUse",
    "gemini-3.1-pro-preview has preview status",
    "all other models have ga status",
    "resolves all four ids off a fresh process without any bootstrap call",
    "registers exactly the four cursor-grok-4.6 effort ids",
    "assigns every tier explicitly rather than by inference",
    "designates exactly one preferred model per tier and resolves it",
    "keeps identity canonical and pricing evidence honestly absent",
    "joins CANONICAL_MODELS without entering the bundled builtin set",
    "re-registration is idempotent",
    "is an instance of ModelRegistry",
    "has the complete canonical offline catalog"
   ],
   "core/model-tier-guard.test.ts": [
    "treats code-development as code",
    "treats document-write / audit / documentation as NOT code",
    "treats other canonical code-like kinds (test/refactor/security/config/data/devops/design/generic) as code",
    "forbids economy for code-development",
    "allows economy for document-write and audit",
    "upgrades haiku → sonnet (standard) for a code-development task",
    "upgrades a code task classified from a .tsx scope (kind derived from scope)",
    "upgrades gpt-5-mini → gpt-4.1 (codex standard) honoring provider",
    "upgrades gemini-2.0-flash → gemini-2.5-flash (gemini standard) honoring provider",
    "leaves haiku untouched for a document-write task",
    "leaves haiku untouched for a doc scope (derived)",
    "leaves haiku untouched for an audit scope (derived)",
    "honors explicitOverride: keeps haiku on a code task but flags it",
    "leaves sonnet (standard) untouched on a code task",
    "leaves opus (premium) untouched on a code task",
    "leaves an unknown/ollama tag untouched (no registry tier → no guard)"
   ],
   "core/model-types.test.ts": [
    "maps claude to every canonical Claude API identity",
    "maps codex to canonical core and parity API IDs",
    "maps gemini to gemini-3.1-pro-preview, gemini-2.5-pro, gemini-2.5-flash, gemini-2.0-flash",
    "has every canonical provider including OpenRouter and local-llm",
    "contains every canonical Claude API identity",
    "is readonly (cannot mutate)",
    "contains the complete 22-model canonical offline catalog",
    "includes all Claude models",
    "includes OpenAI models",
    "includes Gemini models",
    "covers every model in PROVIDER_MODEL_MAP",
    "returns claude for opus",
    "returns claude for sonnet",
    "returns claude for haiku",
    "returns codex for gpt-4.1",
    "returns codex for o3",
    "returns codex for o4-mini",
    "returns gemini for gemini-2.5-pro",
    "returns gemini for gemini-2.5-flash",
    "throws UnknownModelError for invalid model",
    "returns true for opus",
    "returns true for sonnet",
    "returns true for haiku",
    "returns false for gpt-4.1",
    "returns false for gemini-2.5-pro",
    "returns true for gpt-4.1",
    "returns true for o3",
    "returns true for o4-mini",
    "returns false for opus",
    "returns false for gemini-2.5-flash",
    "returns true for gemini-2.5-pro",
    "returns true for gemini-2.5-flash",
    "returns false for opus",
    "returns false for o3",
    "tier 0 (economy): haiku, gpt-5-mini, gpt-4.1-mini, gemini-2.0-flash",
    "tier 1 (standard): sonnet, gpt-4.1, o4-mini, gemini-2.5-flash",
    "tier 3 (premium_plus): o3, gemini-3.1-pro-preview",
    "tier 2 (premium): opus, gpt-5, gemini-2.5-pro",
    "all 13 models have a defined tier",
    "returns true for all known models",
    "returns false for unknown strings",
    "ClaudeModel values are valid ModelType values",
    "OpenAIModel values are valid ModelType values",
    "GeminiModel values are valid ModelType values",
    "ProviderName has exactly 4 valid values (claude, codex, gemini, ollama)",
    "ALL_MODELS is accessible from types.ts",
    "CLAUDE_MODELS is accessible from types.ts",
    "getProviderForModel is accessible from types.ts",
    "isClaudeModel is accessible from types.ts",
    "getModelTier is accessible from types.ts",
    "isValidModel is accessible from types.ts",
    "PROVIDER_MODEL_MAP is accessible from types.ts",
    "maps Claude aliases to actual API model IDs",
    "maps OpenAI models to their API IDs",
    "maps Gemini models to their API IDs",
    "has an entry for every model in ALL_MODELS",
    "resolves Claude aliases to full API model IDs",
    "resolves OpenAI models (alias = API ID)",
    "resolves Gemini models (alias = API ID)",
    "throws UnknownModelError for invalid model",
    "each tier has a stable representative model per provider",
    "all canonical models have identity API IDs"
   ],
   "core/multi-ide.test.ts": [
    "acquireSprintLock creates .deckent/sprint.lock file",
    "lock file contains correct PID and env",
    "lock file contains valid ISO 8601 timestamp",
    "double acquire by same PID returns false (already locked)",
    "isSprintLocked returns locked=true when lock exists with live PID",
    "isSprintLocked returns locked=false when no lock file exists",
    "stale lock (dead PID) is cleared by isSprintLocked",
    "stale lock is cleared by acquireSprintLock allowing new acquisition",
    "takes over a live PID lock when its start token proves PID reuse",
    "preserves a live lock whose PID and start token match",
    "keeps a tokenless legacy live lock on the liveness-only path",
    "clears a planning lock during resume reconciliation only with stale evidence",
    "does not reconcile an unknown owner even with a dead coordinator projection",
    "releaseSprintLock removes the lock file",
    "atomically binds a planning leadership lock to the canonical execution id",
    "releaseSprintLock only works for owning PID",
    "releaseSprintLock is no-op when no lock exists",
    "missing .deckent/ directory is created by acquireSprintLock",
    "corrupt lock file is handled gracefully by isSprintLocked",
    "corrupt lock file is handled gracefully by acquireSprintLock",
    "releaseSprintLockForTerminatedSprint releases a foreign-PID lock for a terminated sprint but never a live sprint lock",
    "auto-detects env when not provided"
   ],
   "core/native-agent-interim-config.test.ts": [
    "defaults: progress note every 5, interim answer after 12 calls / 90 000 ms, 200-char floor",
    "owner overrides merge over the defaults",
    "rejects a non-positive / non-integer %s loudly",
    "rejects a progress-note cadence looser than the enforced deliverable bound",
    "a tighter session cap than the deliverable bound is legitimate (the budget terminates first)",
    "unknown keys next to the new ones still fail loudly"
   ],
   "core/nervous-config-schema.test.ts": [
    "DEFAULT_CONFIG.nervous_system.detectors 6 yeni alan içerir (enabled: false)",
    "dead_event_stream artık reserve_for taşımıyor (Sprint 165: kod hazır)",
    "NERVOUS_SYSTEM_SCHEMA default configi kabul eder ve invalid input reddeder"
   ],
   "core/nervous-enabled-integration.test.ts": [
    "createDefaultConfig() returns nervous_system.enabled === false (new project safe default)",
    "NervousObserver can be instantiated when config enabled=true (Brain boot readiness)"
   ],
   "core/nervous-types-runtime.test.ts": [
    "should enforce UUID v4 id and ISO 8601 timestamp structure",
    "should have correct sprintState snapshot structure",
    "should enforce category + defaultRisk consistency",
    "should enforce decision union types correctly",
    "should enforce safety floor VETO — autonomous never allowed for safety floor actions",
    "should compile without any type errors — readonly and structural checks"
   ],
   "core/nervous-types.test.ts": [
    "all valid modes compile and are assignable",
    "RiskLevel covers low/medium/high",
    "ApprovalPolicy covers all timeout variants",
    "Severity covers all 4 levels",
    "SafetyFloorAction covers all 5 locked actions",
    "valid notification object satisfies interface",
    "notification with no actions (informational) is valid",
    "safety floor action marked correctly",
    "AuthorityMatrix preset shape is valid",
    "NervousSystemConfigV1 defaults shape is valid",
    "NervousSystemConfigV1 derives its shared fields from the V2 NervousSystemConfig (single source)",
    "DetectorResult shape is valid",
    "simulates ADR-040 proposed entry structure",
    "getByType filter simulation returns adr-040",
    "adr-040 does not conflict with existing accepted ADRs"
   ],
   "core/non-null-safety.test.ts": [
    "parses a valid debt table row without crashing",
    "handles short columns gracefully (< 9 cols skipped)",
    "uses en locale for unknown language code",
    "formats date with known locale (en)",
    "formats date with known locale (tr)",
    "returns loaded value without non-null assertion",
    "caches the value across multiple accesses",
    "reset allows reloading",
    "returns undefined for unregistered key",
    "loads registered key safely",
    "registers and counts hooks without crash",
    "evicts oldest entry when budget exceeded",
    "resolves linear dependency chain",
    "resolves conflicts picking highest version",
    "resolveConflicts handles empty version list",
    "handles empty versions array",
    "handles single version array",
    "detects improving trend over multiple versions",
    "handles empty genealogy for descendants",
    "getParent returns null for missing agent",
    "registers message handlers safely",
    "builds prompt with skills without crash",
    "generates report for multiple conflicts",
    "returns no-conflict message for empty array",
    "increments count for duplicate alerts",
    "adds new alerts that do not match existing",
    "parses sprint log with table format",
    "parses retro with table format",
    "handles missing regex groups gracefully",
    "formats milliseconds to seconds",
    "formats milliseconds to minutes + seconds",
    "returns raw string if not matching ms pattern",
    "parses agent and skill info from content",
    "returns empty arrays for content without agent/skill info"
   ],
   "core/notification-config.test.ts": [
    "accepts https URLs",
    "accepts http URLs",
    "rejects ftp URLs",
    "rejects empty strings",
    "rejects malformed URLs",
    "rejects URLs without protocol",
    "returns empty array for valid config",
    "returns empty array for minimal config",
    "rejects non-boolean terminal",
    "rejects invalid webhook URL",
    "rejects non-string webhook",
    "rejects invalid discord URL",
    "rejects non-string discord",
    "rejects invalid slack URL",
    "rejects non-string slack",
    "rejects invalid event type",
    "rejects non-array events",
    "accumulates multiple errors",
    "returns terminal true by default",
    "returns default events",
    "does not include webhook by default",
    "returns defaults when config is undefined",
    "preserves webhook from config",
    "fills in defaults for missing fields",
    "respects terminal: false",
    "respects custom events list"
   ],
   "core/notification-dispatcher.test.ts": [
    "dispatches to available adapters",
    "skips unavailable adapters",
    "sends critical notifications immediately regardless of throttle",
    "throttles non-critical notifications (queues them)",
    "flushes queued notifications after throttle interval",
    "handles adapter errors gracefully (fail-safe)",
    "returns 0 when no adapters registered",
    "clearAdapters removes all adapters",
    "flush returns 0 when queue is empty",
    "close shares one promise, drains queued work once, then rejects post-close delivery",
    "drains every queued notification FIFO without duplicate timer delivery",
    "waits for an already-admitted delivery before draining and closing",
    "close attempts every adapter and preserves close failure authority",
    "assigns correct priority for each event type",
    "includes all fields in notification",
    "converts notification to event stream payload",
    "excludes timestamp from payload (carried by event envelope)"
   ],
   "core/notification-providers/discord.test.ts": [
    "sends POST request to webhook URL",
    "sends embed with correct title for sprint_complete",
    "sends embed with correct title for sprint_failed",
    "uses green color for sprint_complete",
    "uses red color for sprint_failed",
    "uses red color for task_nogo",
    "uses yellow color for usage_warning",
    "includes description from event summary",
    "includes details field when event has details",
    "omits fields when event has no details",
    "includes footer with version",
    "includes timestamp in embed",
    "uses default version 0.1.0 when not specified",
    "propagates HTTP errors"
   ],
   "core/notification-providers/slack.test.ts": [
    "sends POST request to webhook URL",
    "builds payload with header block",
    "builds payload with correct header for sprint_failed",
    "builds payload with correct header for task_nogo",
    "builds payload with correct header for usage_warning",
    "includes summary in section block",
    "includes details in a section block when present",
    "omits details section when no details",
    "includes context block with version",
    "includes fallback text for non-block clients",
    "uses default version 0.1.0 when not specified",
    "propagates HTTP errors from send",
    "sends JSON body with correct content type"
   ],
   "core/notification-providers/webhook.test.ts": [
    "sends POST request with correct JSON payload",
    "includes details in payload when present",
    "retries once on failure",
    "succeeds on retry after first failure",
    "logs successful webhook call",
    "logs failed webhook call with error message",
    "creates log directory if it does not exist",
    "appends to existing log entries",
    "uses 5000ms timeout for HTTP requests",
    "uses application/json content type"
   ],
   "core/notify-adapters/cli-adapter.test.ts": [
    "has correct adapter name",
    "isAvailable returns true when stderr is TTY",
    "isAvailable returns false when no TTY and no parent PID",
    "send writes to stderr as fallback when no parent PID",
    "send includes correct emoji for critical",
    "send includes correct emoji for info",
    "isAvailable returns false for invalid parent PID path"
   ],
   "core/notify-adapters/mcp-adapter.test.ts": [
    "has correct adapter name",
    "isAvailable returns true when server is set",
    "isAvailable returns false when no server",
    "isAvailable returns false when constructed without args",
    "sends info notification with correct MCP level",
    "sends warning notification with correct MCP level",
    "sends critical notification with correct MCP level",
    "includes full notification data in MCP message",
    "setServer allows lazy binding",
    "send does nothing when no server (no throw)",
    "handles server.sendLoggingMessage failure gracefully"
   ],
   "core/notify-adapters/webhook-adapter.test.ts": [
    "maps a canonical Notification onto the webhook payload (lossless event)",
    "is unavailable when no URL is configured",
    "returns the webhook option when channel=webhook and a URL is set",
    "defaults the project name when absent",
    "returns undefined for a non-webhook channel",
    "returns undefined when the URL is missing"
   ],
   "core/notify-bootstrap.test.ts": [
    "registers the dispatcher as the process-global instance",
    "wires CLI + file adapters by default (2), and appends extras in between",
    "sets DECKENT_PARENT_PID when unset and never overrides an inherited value",
    "delivers a notify() through the registered extra adapter AND the file log",
    "is fail-safe: notify() is a no-op (no throw) once the dispatcher is cleared",
    "W2: writes a notify-init breadcrumb with adapter names only under DECKENT_DEBUG"
   ],
   "core/notify-progress-type.test.ts": [
    "'progress' is accepted by createNotification without throwing",
    "'phase-change' is accepted by createNotification without throwing",
    "'progress' notification has priority 'info'",
    "'phase-change' notification has priority 'info'",
    "'progress' is dispatched to all 3 registered adapters",
    "'phase-change' is dispatched to all 3 registered adapters",
    "all 5 original event types still dispatch correctly",
    "notify('progress') emits on DECKENT→USER:NOTIFY with event='progress'",
    "notify('phase-change') emits on DECKENT→USER:NOTIFY with event='phase-change'",
    "notifyProgress helper dispatches a progress notification via global dispatcher"
   ],
   "core/notify-registry.test.ts": [
    "starts unregistered (returns null)",
    "round-trips a NotifyDispatcher instance",
    "clear resets to null",
    "starts unregistered (returns null)",
    "round-trips a dispatcher function",
    "dispatcher receives the full NotifyBusEvent shape",
    "clear resets to null",
    "passing null clears the dispatcher",
    "keeps notify-registry.ts free of orchestra imports (ADR-008)"
   ],
   "core/notify-self-describing.test.ts": [
    "createNotification defaults owningPid to the emitting process.pid",
    "createNotification accepts an explicit owningPid + actions via opts",
    "toEventPayload carries owningPid + actions so every surface can act",
    "cli-adapter prints the sprint+PID context line and the approval command line"
   ],
   "core/notify-webhook-bootstrap.test.ts": [
    "delivers a dispatched notification to the configured webhook URL",
    "registers the webhook adapter only when a webhook URL is configured",
    "does not register a webhook adapter for an empty URL"
   ],
   "core/notify.test.ts": [
    "src/core/notify.ts does not import from orchestra/",
    "invokes the registered dispatcher with the NOTIFY payload",
    "event payload shape stays identical for every NotificationEventName",
    "is fail-safe (no throw) when no event dispatcher is registered",
    "swallows dispatcher errors without throwing"
   ],
   "core/observability-instrument-points.test.ts": [
    "emits config.cache metric with result=miss on first loadConfig call",
    "emits config.cache metric with result=hit on cache hit",
    "claimTaskLock emits a lock.wait trace entry",
    "emits hb.stale metric when scanHeartbeats detects a stale non-completed agent",
    "emits honesty.check metric when result notes contain honesty-trigger phrases",
    "generateLoadReport includes config.cache, hb.stale, and honesty.check metric names"
   ],
   "core/observability-rotation-tenant.test.ts": [
    "lists only valid tenant ids that own an isolation directory",
    "returns an empty list when no tenant namespace exists",
    "derives an archive tenant scope from its isolation path",
    "attributes a non-partitioned archive to the root tenant scope",
    "discovers tenant-partitioned archives alongside project-scope archives",
    "names the tenant scope every pruned archive belonged to",
    "reports one scope outcome per evaluated tenant, with that scope`s ceilings",
    "attributes a project-scope prune to the root tenant scope",
    "never prunes tenant B archives because tenant A exceeded the count ceiling",
    "never prunes tenant B archives because tenant A exceeded the size ceiling",
    "keeps the project scope and a tenant scope independent of each other",
    "applies a per-tenant configured ceiling only to that tenant",
    "ignores a per-tenant block keyed by an id the tenant authority rejects",
    "an explicit caller override still outranks a per-tenant configured ceiling",
    "never prunes a legal-hold-marked archive, in any tenant scope",
    "a held archive does not count toward its own tenant`s count ceiling",
    "never prunes the last surviving archive of a tenant for the size ceiling alone",
    "the age ceiling may still empty a tenant scope completely",
    "returns an empty, throw-free receipt when nothing is archived"
   ],
   "core/observability-rotation.test.ts": [
    "should compress and archive metrics file then truncate original",
    "should return rotated=false when metrics file does not exist",
    "should return rotated=false when metrics file is empty",
    "should decompress archive and recover original content (gzip roundtrip)",
    "rejects redirected metrics namespaces without mutating hot metrics",
    "does not mutate a terminal-sealed archive or hot metrics",
    "737-001: actually prunes archives beyond the legacy count ceiling (no-op removed)",
    "should not prune when under limit",
    "should return empty when archive dir does not exist",
    "has typed defaults (keepLastN/maxAgeDays/maxSizeMB)",
    "returns a typed prune receipt with pruned/policy/legalHold",
    "never deletes without producing a receipt entry for exactly what was removed",
    "prunes oldest-first beyond the count ceiling",
    "prunes archives older than the age ceiling, keeps fresh ones",
    "prunes oldest survivors until the aggregate size fits the ceiling",
    "never prunes the last surviving archive purely for exceeding the size ceiling",
    "never prunes a legal-hold-marked archive under any ceiling",
    "legalHold is false when no archive is under hold",
    "never deletes the active metrics.jsonl file",
    "never rewrites bytes of a surviving archive (only whole-file unlink)",
    "resolves ceilings from .deckent/config.json observability.retention when no override is given",
    "falls back to defaults when config.json has no observability.retention block",
    "an explicit override always wins over the configured value",
    "returns empty pruned array and no throw when the archive dir does not exist",
    "should return true when file exceeds maxSizeMB",
    "should return false when file is under threshold",
    "should return false when file does not exist",
    "should inject sprintId tag into metric entries",
    "should not inject sprintId when not set",
    "should allow setting sprintId after initialization",
    "should preserve existing tags alongside sprintId",
    "should write to per-sprint file when enabled",
    "should also write to main metrics file when per-sprint is enabled",
    "should not write per-sprint file when disabled",
    "should handle entries without sprintId tag (legacy entries)",
    "should list archives in sorted order",
    "should return empty array when no archive dir",
    "should have correct defaults",
    "should return null when not set",
    "should return sprintId when set via init",
    "should return sprintId when set via setter",
    "should reset to null on resetObservability"
   ],
   "core/observability.test.ts": [
    "should write a metric entry and read it back correctly",
    "should append multiple metrics (append-only)",
    "should silently discard when not initialized",
    "should omit tags when empty or undefined",
    "should measure duration and record success",
    "should capture error on exception and re-throw",
    "should write pino-compatible JSON log entry",
    "should support all log levels",
    "should produce valid line-delimited JSON",
    "should have TELEMETRY_ENABLED hard-coded to false",
    "should not import or call net.connect",
    "should only write to local filesystem",
    "should generate markdown report from metrics data",
    "should return placeholder when no metrics file exists",
    "should record wave.start metric format correctly",
    "should record wave transition events",
    "should calculate p50/p95/p99 correctly for 100 samples",
    "should handle single element",
    "should handle empty array",
    "should handle two elements",
    "should distribute values into correct buckets",
    "should handle empty values",
    "should record per-task evaluation duration",
    "should return correct path for project root",
    "should throw when no root is available",
    "amortizes configured size-ceiling checks via the resolved policy",
    "evaluates the ceiling BEFORE the record is written",
    "rotates once the configured size ceiling is crossed, without waiting for sprint finalize",
    "recovers from a transient rotation failure in the same session without losing or duplicating records"
   ],
   "core/openrouter-bootstrap.test.ts": [
    "flag-off + key present: does NOT register openrouter",
    "flag-off is byte-identical to flag-absent, key present either way",
    "flag-on + $DECK:OPENROUTER_API_KEY (bare) present: registers openrouter",
    "flag-on + DECKENT_OPENROUTER_API_KEY (prefixed) present: registers openrouter",
    "flag-on + no key: does NOT register, records an honest skip reason",
    "is idempotent — already-registered openrouter is left untouched"
   ],
   "core/openrouter-model-registration.test.ts": [
    "registers an OpenRouter id that was previously unresolvable",
    "LANDMINE 1 — a \":free\" id resolves to openrouter, NOT ollama",
    "LANDMINE 2 — registers as status \"ga\" so tier resolution can see it",
    "keeps id === apiId verbatim (row 608 canonical-id rule — no Deckent alias)",
    "applies caller-supplied facts over the conservative defaults",
    "falls back to conservative defaults when no facts are supplied",
    "is idempotent and never clobbers an existing richer entry",
    "ignores an empty id instead of registering junk",
    "rejects an unknown paid id before registry mutation instead of pricing it as free",
    "rejects a deceptive suffix and invalid explicit zero pricing",
    "registers a paid exact API id only when explicit pricing is present",
    "applies the same paid-model gate to generic parametric resolution",
    "rejects a free suffix without fresh pricing evidence",
    "returns true for openrouter, so spawn prefers adapter.spawn() over the docker backend",
    "leaves the pre-existing contract untouched"
   ],
   "core/openrouter-models.test.ts": [
    "keeps only exact free ids with both prompt and completion prices equal to zero",
    "returns an empty array when no entry qualifies (never fabricates)",
    "calls the injected fetchImpl exactly once against the OpenRouter models URL",
    "throws OpenRouterProbeError when the underlying fetch rejects (network failure)",
    "throws OpenRouterProbeError on a non-OK HTTP status",
    "throws OpenRouterProbeError when the body is not valid JSON",
    "throws OpenRouterProbeError when the response shape has no \"data\" array",
    "throws OpenRouterProbeError when the body is null",
    "throws OpenRouterProbeError when \"data\" is present but not an array",
    "writes .deckent/settings/openrouter-models.json with generatedAt + models",
    "creates the .deckent/settings directory when it does not exist yet",
    "persists an empty models array without error",
    "overwrites (never appends) on a second write with different content",
    "returns pricing authority only while the integrity-bound cache is fresh",
    "rejects tampered cache payloads instead of authorizing zero price"
   ],
   "core/operation-catalog.test.ts": [
    "preserves Op strings and exposes immutable exact references",
    "distinguishes unknown IDs from supplied version mismatches without fallback",
    "keeps the effect-to-gate minimum contract",
    "converges equivalent declarations deterministically independent of input order",
    "fails closed with deterministic diagnostics for malformed, duplicate, unknown, wrong-version, and ambiguous declarations",
    "generates root-independent bytes, detects drift, and never writes in default/check mode",
    "rejects duplicate IDs, symbol collisions, invalid versions, audit-version drift, and incomplete bilingual titles",
    "accepts the checked-in six-entry projection in read-only check mode"
   ],
   "core/operation-invocation-async-scope.test.ts": [
    "reports frozen explicit absence and a distinct required-context error",
    "validates context and callback before entering storage",
    "preserves synchronous return and throw semantics, then restores absence",
    "preserves asynchronous resolution and rejection across awaited work",
    "refuses native promises created outside observation whether settled or pending",
    "invalidates queued continuations for an observed already-settled native promise",
    "rejects target, self, and mutual thenable cycles with bounded typed settlement",
    "reads each hostile then getter once and propagates a getter throw",
    "inspects Proxy-backed then values exactly once without triggering introspection traps",
    "adopts a deep finite synchronous chain without recursion and bounds an endless chain",
    "restores an active outer scope after nested success and rejection",
    "isolates concurrent scopes while their settlements interleave",
    "invalidates detached continuations after success or rejection without exposing context",
    "assimilates hostile thenables once and invalidates after their terminal result"
   ],
   "core/operation-invocation-child-process.test.ts": [
    "round-trips exact canonical bytes and digest through an isolated child",
    "returns a typed oversize refusal without accumulating unbounded authority bytes",
    "times out a hung child, escalates termination, and reaps it",
    "settles once when early exit races a large stdin write and leaves no child"
   ],
   "core/operation-invocation-conformance.test.ts": [
    "composes root, retry, child, sibling, and new-transaction retain/replace invariants",
    "round-trips every lifecycle context with exact bytes, digest, immutability, and subject facts",
    "composes validated transported contexts through nested, concurrent, and settled async scopes",
    "rejects cross-contract catalog, idempotency, identity, schema, and JSON adversaries",
    "keeps permission, approval, enforcement, dispatch, effect, and secret outcomes unrepresentable"
   ],
   "core/operation-invocation-context.test.ts": [
    "binds every required dimension in one exact policy-neutral envelope",
    "deep-copies and deeply freezes every nested authority-bearing value",
    "round-trips the canonical NATURAL binding and rejects operation drift",
    "constructs root, retry, child, sibling, and new-transaction contexts through exact lifecycle APIs",
    "separates structural transport validation from lifecycle construction authority",
    "rejects cycles, accessors, toJSON, prototypes, symbols, and unsafe numbers before copying"
   ],
   "core/operation-invocation-idempotency.test.ts": [
    "resolves only exact catalog operation references and preserves catalog error distinctions",
    "has one immutable exact binding shape for each catalog idempotency class",
    "brands only valid keyed authority data and forbids keys for NONE and NATURAL shapes",
    "rejects catalog-class mismatches and arbitrary binding material without policy or effects"
   ],
   "core/operation-invocation-identity.test.ts": [
    "validates caller-supplied namespace-distinct brands including the sole idempotency key",
    "rejects malformed, padded, C0/C1 control-bearing and oversized values without repair",
    "makes root causation null and rejects self-causation or ambiguous relation data",
    "retry replaces only the attempt and points at the exact previous attempt",
    "distinguishes child and sibling while retaining transaction and correlation lineage",
    "new transaction replaces exact identities, preserves prior causation, and accepts explicit same or new correlation",
    "rejects accessors, symbols, non-enumerable expected fields, prototypes, and extras"
   ],
   "core/operation-invocation-platform.test.ts": [
    "labels simulated contract coverage without claiming native execution proof",
    "round-trips deterministic frozen $platform bytes while preserving opaque path-looking IDs",
    "canonicalizes opposite insertion orders to identical transport bytes",
    "preserves Turkish and composed/decomposed Unicode distinctly without normalization",
    "preserves exact RFC3339 text and keeps Z and +03:00 representations distinct",
    "keeps exact codec bytes invariant across explicit locale and timezone environments",
    "rejects control-bearing opaque resource identity %j",
    "rejects LF and CRLF appended to otherwise canonical transport bytes",
    "rejects an unsupported platform instead of silently selecting a host fallback"
   ],
   "core/operation-invocation-subject.test.ts": [
    "brands every authority namespace distinctly at compile time and runtime",
    "round-trips explicit %s platform facts",
    "preserves every verified principal fact and freezes copied nested values",
    "accepts absent optional principal claims without synthesizing them",
    "keeps conflicting tenancy facts as lossless inputs without deciding them",
    "rejects accessors, symbols, hidden extras, and custom prototypes without invoking code"
   ],
   "core/operation-invocation-transport.test.ts": [
    "has deterministic golden bytes, an independent domain digest, and a frozen lossless round-trip",
    "measures UTF-8 bytes, preserves Unicode, and enforces one fixed hard ceiling",
    "changes the digest when every authority-bearing context dimension changes",
    "rejects malformed UTF-8 classes before JSON parsing",
    "rejects truncation and syntactically valid incomplete envelopes without repair",
    "rejects duplicate top-level and nested keys through original-byte canonical equality",
    "rejects BOM, whitespace, key order, alternate escapes, and numeric spellings",
    "rejects context and digest tampering without claiming authentication",
    "rejects inexact envelope schemas, versions, and digest representations",
    "rejects catalog drift even when an attacker recomputes a valid domain digest"
   ],
   "core/operation-invocation-worker-thread.test.ts": [
    "starts without inherited ALS while its parent scope stays active during byte-only reconstruction",
    "isolates distinct codec inputs across concurrent workers without cross-talk",
    "fails tampered codec bytes with a typed transport error and returns no context",
    "rejects malformed workerData and malformed evidence protocol without accepting context",
    "waits for uncaught-error and early-exit workers to close before rejecting once",
    "awaits termination of a timed-out worker and leaves no live worker"
   ],
   "core/orphan-cleaner-ipc.test.ts": [
    "should clean 5 orphan IPC dirs and protect the current one",
    "should ignore directories that do not match the IPC pattern",
    "should return 0 when no orphan IPC dirs exist",
    "should return 0 gracefully when .deckent dir does not exist",
    "removes IPC dir with a dead PID",
    "preserves IPC dir whose PID is alive (current process)",
    "removes IPC dir with no config.json (config-only leak)",
    "removes IPC dir with config.json but no pid field",
    "removes dead PID dirs while preserving live PID dirs",
    "returns empty array when .deckent dir does not exist",
    "ignores non-IPC directories",
    "concurrent isolation: two live-PID IPC dirs are both preserved",
    "uses default opts (checkLivePid=true) when no opts provided",
    "should delete IPC dir when process exits with code 0 (success)",
    "should preserve IPC dir when process exits with non-zero code (failure/debug)",
    "should preserve IPC dir when process exits with code 2 (fatal error)"
   ],
   "core/orphan-cleaner.test.ts": [
    "should archive DONE task files to the canonical sprint task namespace",
    "should archive NO_GO task files",
    "should preserve EXECUTING task files",
    "should preserve PENDING task files",
    "archives a raw PENDING task only with exact receipt-backed terminal projection",
    "uses the default read-only receipt projection and raw task tenant identity",
    "preserves a PENDING task when receipt authority is ambiguous",
    "preserves a PENDING task when the default receipt store is malformed",
    "preserves malformed canonical task identity without calling projection authority",
    "preserves projected terminal status when receipt/evidence proof is incomplete",
    "should clean stale locks (>5min)",
    "should handle missing .tasks/ gracefully",
    "should handle invalid sprintId gracefully",
    "preserves tasks with unknown status (missing .json)",
    "archives unprefixed artifacts for a terminal task without touching other or taskless files",
    "should move previous sprint files to archive",
    "archives unprefixed previous-sprint artifacts without touching current or taskless files",
    "should skip cleanup if another live sprint pid exists",
    "should proceed if pid file references a dead process",
    "should handle no orphan files gracefully",
    "should handle invalid sprintId",
    "should clean files from multiple previous sprints",
    "preserves a previous sprint PENDING task instead of treating age as terminal proof",
    "reports terminal files as archived + active as preserved, deleting NOTHING",
    "is sprint-scoped — never reports another sprint\\'s files",
    "preserves a pending fix task as an independent id (not archived)",
    "classifies xfix and chained fix artifacts as independent task ids",
    "reports the SAME set postFinalizeCleanup actually archives/preserves",
    "returns empty sets for an unextractable sprintId"
   ],
   "core/output-collector.test.ts": [
    "should store entries up to capacity",
    "should drop oldest entries when exceeding capacity",
    "should handle incremental pushes with overflow correctly",
    "should clear all entries",
    "should throw on non-positive capacity",
    "should return a snapshot copy from getAll()",
    "should handle exact capacity correctly",
    "should handle large batch overflow",
    "should call docker logs with correct args on poll",
    "should require containerName for Docker backend",
    "should call tmux capture-pane with correct args on poll",
    "should require tmuxTarget for tmux backend",
    "should read from task log file",
    "should handle missing log file gracefully",
    "should use active interval (1s) when output is flowing",
    "should switch to idle interval (5s) after 3 idle polls",
    "should flush output to disk with correct format",
    "should return null when buffer is empty",
    "should handle docker logs failure gracefully",
    "should handle spawnSync throwing an exception",
    "should prevent collect after dispose",
    "should stop polling when stop() is called",
    "should track active workers correctly",
    "should not add duplicate workers",
    "should return null for unknown worker snapshot",
    "should respect custom maxLines per worker",
    "should detect sprint ID from sprint-state.json",
    "should fallback to sprint-unknown when no state file",
    "should create an instance"
   ],
   "core/output-formatter.test.ts": [
    "renders ★ Insight header and footer",
    "includes sprintId in header line",
    "shows completed / total tasks",
    "shows tech-debt line only when techDebtTasks > 0",
    "shows fail line only when failedTasks > 0",
    "includes Türkçe labels",
    "includes emoji prefix (✅ for high coverage)",
    "uses ⚠ for medium coverage (70–89)",
    "uses ❌ for low coverage (<70)",
    "handles minimal data without crashing",
    "renders a single-line summary as first line",
    "renders a markdown table",
    "shows coverage in table",
    "handles missing coverage gracefully",
    "summary line excludes coverage when undefined",
    "includes VERBOSE SPRINT SNAPSHOT header",
    "includes ISO timestamp",
    "includes all metric fields",
    "includes extra non-standard fields",
    "ends with END SNAPSHOT footer",
    "renders valid JSON",
    "JSON contains all input fields",
    "pretty-prints with 2-space indent",
    "defaults to standart when mode is omitted",
    "accepts explainatory",
    "accepts standart",
    "accepts verbose",
    "accepts json",
    "maps legacy quiet → standart",
    "maps legacy normal → standart",
    "returns standart for unknown values",
    "returns standart when called with no argument",
    "returns standart for empty string",
    "returns 🚀 for rocket",
    "returns ✅ for success",
    "returns ❌ for error",
    "returns ⭐ for star",
    "returns empty string for unknown key",
    "formatStatus respects mode parameter (simulates config.output_render_mode)",
    "resolveOutputMode handles config value correctly for explainatory",
    "formatStatus with full data produces non-empty string for all modes",
    "handles empty StatusData object",
    "handles coverage = 0",
    "handles coverage = 100",
    "handles duration absent in explainatory (no duration line)",
    "phase icon resolves correctly for various phases"
   ],
   "core/parse-debt-md.test.ts": [
    "extracts sprint info from canonical debt-NNN-MMM ids",
    "handles double-prefix debt-debt-NNN-MMM shape (legacy import bug)",
    "returns null for malformed or empty ids",
    "uses originSprintId column when present",
    "falls back to id parsing when originSprintId column is \"-\"",
    "completes the atomic UPDATE pass in <100ms for 150 rows",
    "is idempotent — second call updates zero rows"
   ],
   "core/pending-approval-lifecycle-index.test.ts": [
    "projects only actionable runtime rows with finite expiry and normalized risk",
    "does not create an empty runtime store on a pure read"
   ],
   "core/pending-approvals-lifecycle.test.ts": [
    "removeNervousPending deletes exactly the named entry",
    "removeNervousPending is honest on a missing id and never throws on a missing file",
    "pruneExpiredNervousPending removes only expired entries and reports them",
    "handleNervousAccept clears the durable-hub entry (the 5-day-stale live lie)"
   ],
   "core/pending-approvals.test.ts": [
    "returns [] when no pending file exists (fail-safe)",
    "reads parked nervous approvals and emits the exact accept/reject commands",
    "is fail-safe on malformed JSON (returns [])",
    "reads parked autonomous triggers and emits the exact approve/reject commands",
    "merges nervous + autonomous parked approvals across surfaces",
    "is fail-safe on malformed autonomous JSON (nervous still surfaces)",
    "projects canonical PAUSED authority into actionable resume/finalize commands"
   ],
   "core/persona-channel-capability.test.ts": [
    "degrades when the descriptor is absent (D-D default)",
    "degrades when the descriptor explicitly declares unsupported",
    "is eligible only for a verified append channel",
    "is a HOLD candidate for a verified replace channel (D-H)",
    "is a HOLD candidate for an unknown-semantics channel (D-H)",
    "is a HOLD candidate for an unverified append channel"
   ],
   "core/persona-guidance.test.ts": [
    "extracts well-formed sections for known intents and default, trimmed, no issues",
    "returns an empty sections map and no issues when the document has no markers",
    "ignores an unknown intent key and reports it",
    "keeps the first pair on duplicate same-intent markers and reports the rest",
    "ignores a section whose start marker is never closed and reports it",
    "recovers a later well-formed pair when an earlier start for the same key is never closed",
    "reports a stray end marker with no matching start",
    "never throws on a document combining every malformed edge at once",
    "drops both sections and reports both issues when two different intents interleave",
    "drops all sections and reports every pair when THREE intents mutually interleave",
    "drops only the entangled sections in a chained interleave, keeping none of A-D corrupted",
    "handles empty and non-string-ish input without throwing",
    "picks the exact-intent slice when present (source: intent)",
    "falls back to default when the intent has no section (source: default)",
    "falls back to the full body when neither intent nor default exist (source: full-body)",
    "falls back straight to default for the unknown intent (no unknown-key section exists)",
    "is deterministic across repeated calls on the same (promptMd, intent) pair",
    "never mutates the input promptMd string",
    "removes valid marker blocks and cuts an emptied \"## Guidance Slices\" heading (real content-task shape)",
    "returns the input BYTE-IDENTICAL when there are no valid guidance blocks",
    "interleaved (heading-free) marker blocks are removed while body prose survives",
    "malformed (unclosed) markers are NOT deleted — fail-soft keeps non-block text intact",
    "a \"## Guidance Slices\" heading with REAL remaining content after block removal stays"
   ],
   "core/persona-integrity-detection.test.ts": [
    "a real persona above the floor with no declared digest is intact",
    "empty content is `empty`, not undersized",
    "below the config-resolved floor is `undersized`",
    "declared digest disagreeing with the actual digest is `digest-mismatch`",
    "NO declared digest never fabricates a mismatch — intact",
    "availability none is `unreadable` regardless of content",
    "the floor default is a config export, not a literal at the call site",
    "resolvePrompt carries the declared digest, and a wrong one classifies as digest-mismatch",
    "resolvePrompt carries a matching declared digest through as intact",
    "a manifest with no declared digest resolves declaredDigest null and never fabricates a mismatch"
   ],
   "core/pid-liveness.test.ts": [
    "returns false for negative pid",
    "returns false for zero pid",
    "returns false for NaN",
    "returns false for Infinity",
    "returns false for non-integer (decimal)",
    "returns true for the current process PID",
    "returns false for a virtually-guaranteed-dead PID (99999999)",
    "returns true for PID 1 (init) on POSIX",
    "returns the same answer for repeated calls with the same PID",
    "does not throw on any input"
   ],
   "core/pid-ownership.test.ts": [
    "alive + matching start token → owned",
    "🔴 alive + DIFFERENT start token → reused (pid was recycled; must NOT signal)",
    "dead process → dead (nothing to kill)",
    "alive but no stored token (old pid file) → unknown (cannot prove ownership)",
    "alive, stored token present but live token unreadable → unknown (no false reuse)",
    "null record → dead"
   ],
   "core/planner-evidence-refusal.test.ts": [
    "classifies %s without exposing diagnostics"
   ],
   "core/planner-invocation-binding.test.ts": [
    "binds and re-verifies an accepted fallback invocation without equating configured and called identity",
    "fails closed for missing stores, foreign scope, rejected settlement and wrong result evidence",
    "rejects %s receipt identity",
    "maps persisted receipt corruption into the safe binding taxonomy"
   ],
   "core/plugin-authenticity.test.ts": [
    "proves the vulnerability class: naive string-prefix comparison treats a sibling dir as contained",
    "validatePluginSecurity rejects a plugin loaded from an evil-prefix sibling directory",
    "true for the same directory",
    "true for a real descendant",
    "false for a sibling directory with a matching string prefix",
    "false for a \"..\"-escape back out of the parent",
    "true for a real descendant",
    "false for a sibling directory with a matching string prefix",
    "false for a \"..\"-escape",
    "false across drive roots",
    "an attacker who controls the file also controls its sha256 hash — legacy gate alone accepts it",
    "returns null when manifest.json has no publisherSignature block",
    "returns null when manifest.json does not exist",
    "returns null for a malformed publisherSignature block (missing fields)",
    "returns null for a non-ed25519 alg",
    "parses a well-formed publisherSignature block",
    "returns null when the key id is not in the trust list",
    "returns decoded 32-byte key material for a matching key id",
    "returns null for an undecodable hex value",
    "roundtrip: signs with a real keypair, configures the same pubkey as trust root, verifies",
    "roundtrip through validatePluginSecurity + resolvePluginSecurityConfig (full config passthrough)",
    "rejects a forged signature (payload signed by a keypair other than the trust root)",
    "rejects an untrusted publisher key id (valid crypto, signer not configured as trusted)",
    "rejects content tampered after a valid signature was produced",
    "unsigned plugin: default policy loads with a loud warning (never silent) once a trust root is configured",
    "unsigned plugin: require_signature=true rejects (fail-closed, enterprise profile)",
    "dormant (byte-identical to pre-born-612 behavior) when no trust root is configured",
    "validatePluginSecurity stays byte-identical for legacy sha256-only fixtures with no trust root"
   ],
   "core/plugin-hooks.test.ts": [
    "reads total.lines.pct from coverage-summary.json (was hardcoded 0)",
    "returns 0 when no coverage report exists (honest absence, e.g. fresh checkout)",
    "returns 0 on a malformed report instead of throwing",
    "registers a callback for a hook",
    "registers multiple callbacks for the same hook",
    "registers callbacks for different hooks independently",
    "allows registering the same callback function multiple times",
    "calls registered callback with correct context",
    "calls all registered callbacks in order",
    "does nothing when no callbacks registered",
    "calls callback with sprint context",
    "awaits async callbacks",
    "calls callback with task context",
    "calls callback with task and result context",
    "continues calling subsequent callbacks when one throws",
    "logs error to stderr when callback throws",
    "handles async callback rejection without throwing",
    "includes hook name in stderr log message",
    "removes all registered hooks",
    "after clear, runHooks does nothing",
    "clears only the specified hook",
    "no-op when hook has no registered callbacks",
    "returns 0 when no callbacks registered",
    "returns correct count after multiple registrations",
    "returns correct count for each hook independently",
    "callback receives context with correct hook field",
    "afterSprint context contains sprint object",
    "afterTask context contains both task and result",
    "returns null when hook file does not exist",
    "logs error for nonexistent hook file",
    "returns 0 when plugin has no hooks",
    "returns 0 when hooks object is empty",
    "logs error and returns 0 for nonexistent hook files",
    "returns 0 when plugins directory does not exist",
    "returns 0 when plugins directory is empty",
    "clears previously registered hooks before scanning",
    "skips disabled plugins",
    "loads hooks from enabled plugin with valid hook module",
    "loads multiple hooks from a single plugin",
    "loads hooks from multiple plugins in registration order",
    "handles invalid hook module gracefully",
    "hook callback receives correct context when executed"
   ],
   "core/plugin-install.test.ts": [
    "returns \"npm\" for plain package name",
    "returns \"npm\" for scoped package name",
    "returns \"git\" for https:// URL",
    "returns \"git\" for http:// URL",
    "returns \"git\" for git@ URL",
    "returns \"git\" for URL ending with .git",
    "returns \"local\" for relative path starting with ./",
    "returns \"local\" for relative path starting with ../",
    "returns \"local\" for absolute path starting with /",
    "returns true for https URL",
    "returns false for plain name",
    "returns true for ./ path",
    "returns true for / path",
    "returns false for plain name",
    "installs a valid local plugin and returns Plugin",
    "calls fsp.mkdir to ensure pluginsDir exists",
    "calls fsp.cp to copy source directory to destDir",
    "auto-enables the plugin after install",
    "throws PluginError if source path does not exist",
    "throws PluginError if plugin with same name already installed",
    "throws PluginError if manifest is missing required fields",
    "throws PluginError if manifest.json is missing",
    "throws PluginError if manifest JSON is invalid",
    "resolves source to absolute path",
    "clones git URL and returns Plugin",
    "calls spawnSync with git clone for https:// URL",
    "treats git@ URLs as git URLs",
    "throws PluginError if git clone fails",
    "cleans up tmpDir if git clone fails",
    "cleans up tmpDir if manifest validation fails after clone",
    "throws PluginError if destination already exists (duplicate)",
    "calls fsp.rename to move tmpDir to destDir on success",
    "does not call npm for git URLs",
    "installs npm package and returns Plugin",
    "calls spawnSync with npm install including --ignore-scripts",
    "passes --ignore-scripts flag to prevent postinstall script execution",
    "does not pass --ignore-scripts to git clone (only npm)",
    "handles scoped npm packages",
    "throws PluginError if npm install fails",
    "throws PluginError if package not found in node_modules after install",
    "cleans up tmp dir after successful npm install",
    "cleans up tmp dir on npm install failure",
    "throws if npm-installed plugin already exists",
    "copies from node_modules to plugins dir using fsp.cp",
    "creates tmp dir with fsp.mkdir",
    "auto-enables the npm-installed plugin",
    "treats http:// as a git URL",
    "treats local path starting with / as local install",
    "treats plain name as npm install"
   ],
   "core/plugin-manifest.test.ts": [
    "accepts a valid minimal manifest",
    "throws if raw is not an object",
    "throws if name is missing",
    "throws if name is empty string",
    "throws if version is missing",
    "throws if description is missing",
    "throws if entrypoint is missing",
    "defaults enabled to true when not specified",
    "preserves enabled=false",
    "accepts valid triggers array",
    "accepts empty triggers array",
    "throws if triggers is not an array",
    "throws if triggers contains non-string values",
    "omits triggers if not specified",
    "accepts valid permissions array",
    "throws if permissions is not an array",
    "throws if permissions contains non-string values",
    "accepts valid hooks with beforeSprint and afterSprint",
    "accepts hooks with only beforeSprint",
    "accepts hooks with only afterSprint",
    "accepts empty hooks object",
    "throws if hooks is not an object",
    "throws if hooks is an array",
    "throws if hooks.beforeSprint is not a string",
    "throws if hooks.afterSprint is not a string",
    "omits hooks if not specified",
    "accepts opus",
    "accepts sonnet",
    "accepts haiku",
    "throws for invalid model",
    "omits model if not specified",
    "accepts valid dependencies array",
    "accepts empty dependencies array",
    "throws if dependencies is not an array",
    "throws if dependencies contains non-string values",
    "omits dependencies if not specified",
    "accepts a complete v2 manifest with all fields",
    "accepts manifest with only some v2 fields"
   ],
   "core/plugin-remove.test.ts": [
    "returns false when plugin does not exist",
    "returns true when plugin is successfully removed",
    "removes the plugin directory from disk",
    "throws PluginError when attempting to remove a system plugin",
    "does not remove system plugin directory when error is thrown",
    "removes plugin that has no manifest.json (directory only)",
    "returns false when pluginsDir itself does not exist",
    "removes plugin even if it has nested files",
    "handles malformed manifest.json gracefully (removes plugin)",
    "does not affect other plugins when removing one"
   ],
   "core/plugin-sandbox-wire.test.ts": [
    "runs the pipeline with advisory defaults + a typed warning when no config is passed",
    "runs the pipeline when the effective config carries a plugins block",
    "warns but still loads a plugin the sandbox scan rejects",
    "warns but still loads a plugin outside the allowed paths",
    "warns but still loads an unsigned plugin when require_signature is set",
    "does not throw for a rejected plugin — the sprint keeps its previous behaviour",
    "blocks the load with a typed PluginSecurityError when the sandbox scan rejects",
    "blocks a hook outside the allowed paths",
    "blocks an unsigned hook when require_signature is enabled",
    "honours the legacy top-level plugin_require_signature field",
    "still loads a plugin that passes every step",
    "defaults to advisory for absent or empty config",
    "reads an explicit stance off the plugins block",
    "falls back to advisory with a typed warning on an unrecognized value",
    "always returns a config, defaulting containment to the canonical plugins dir",
    "passes the operator trust root and allowed paths straight through",
    "passes the real security config instead of the bare projectRoot",
    "rejects a non-whitelisted binary (rm) without spawning it",
    "rejects a whitelisted binary with an injected arg",
    "rejects sh -c injection attempts (sh is not whitelisted)",
    "honors a custom binWhitelist/argRegex override via the 4th param",
    "rejects a non-whitelisted binary (rm) without spawning it",
    "rejects a whitelisted binary with a backtick-injected arg",
    "throws synchronously — no ChildProcess is ever created for unsafe input",
    "runs a real whitelisted process and returns real stdout",
    "propagates a non-zero exit code like plain spawnSync would",
    "passes options through unchanged (cwd/env honored)",
    "runs a real whitelisted process and streams real stdout",
    "safeSpawnSync accepts (bin, args) with no options, mirroring assertSpawnSafe(bin, args)",
    "is a plain function value that can be imported and invoked like child_process.spawnSync"
   ],
   "core/plugin-security-config-authority.test.ts": [
    "reads security_enforcement from the typed plugins config and keeps advisory as the default",
    "advisory mode warns and loads an unsigned hook",
    "enforce mode blocks an unsigned hook with PluginSecurityError",
    "advisory mode warns and loads an out-of-scope hook",
    "enforce mode blocks an out-of-scope hook with PluginSecurityError"
   ],
   "core/plugin-security.test.ts": [
    "computes correct SHA-256 hash",
    "returns false when manifest has no signature",
    "returns true when signature matches file hash",
    "returns false when signature does not match",
    "throws PluginSecurityError when entrypoint file not found",
    "reports safe for clean plugin code",
    "detects dangerous code patterns (eval)",
    "detects child_process import",
    "rejects plugin that fails sandbox scan (dangerous code)",
    "rejects plugin with mismatched signature",
    "allows unsigned plugin with warning when require_signature=false",
    "rejects unsigned plugin when require_signature=true",
    "allows valid signed plugin (happy path)",
    "rejects plugin outside allowed_paths"
   ],
   "core/plugin-system.test.ts": [
    "create — scaffolds manifest.json, SKILL.md, README.md",
    "install — copies local plugin into pluginsDir",
    "enable — sets enabled=true and makes plugin appear in listPlugins",
    "list — returns all enabled plugins",
    "disable — sets enabled=false and removes plugin from listPlugins",
    "remove — deletes plugin directory and returns true",
    "full end-to-end: create → install → disable → re-enable → remove",
    "valid manifest — loads all fields correctly",
    "invalid manifest — missing name throws PluginError",
    "invalid manifest — empty version throws PluginError",
    "partial manifest — only name + version throws PluginError (missing description + entrypoint)",
    "manifest with null value throws PluginError",
    "enabled defaults to true when not present",
    "scans multiple plugins and returns all enabled ones",
    "ignores non-directory entries (files at top level)",
    "silently skips directories without manifest.json",
    "returns empty array when .deckent/plugins/ does not exist",
    "beforeSprint hook is called with correct context",
    "afterSprint hook is called with sprint data",
    "multiple hooks for same event run in registration order",
    "failing hook does not abort subsequent hooks",
    "getHookCount returns correct count",
    "clearHook removes only specified hook",
    "runHooks resolves immediately when no callbacks registered",
    "async hooks are awaited before next hook runs",
    "loadPlugin throws PluginError for missing manifest",
    "loadPlugin throws PluginError for corrupt JSON",
    "loadPlugin throws PluginError for JSON null (not object)",
    "installPlugin throws PluginError for non-existent source path",
    "installPlugin throws PluginError when plugin name already exists",
    "removePlugin throws PluginError for system plugins",
    "createPlugin throws PluginError if plugin already exists",
    "removePlugin returns false for non-existent plugin",
    "listPlugins returns correct Plugin shape for CLI display",
    "loadPlugin returns correct Plugin shape for info display",
    "createPlugin returns correct Plugin shape for create output",
    "scanPlugins returns Plugin[] suitable for CLI listing"
   ],
   "core/plugin-toggle.test.ts": [
    "returns true when plugin exists",
    "sets enabled=true in manifest.json",
    "sets enabled=true even when previously not set",
    "returns false when plugin does not exist",
    "preserves other manifest fields",
    "returns true when plugin exists",
    "sets enabled=false in manifest.json",
    "sets enabled=false even when previously not set",
    "returns false when plugin does not exist",
    "preserves other manifest fields",
    "can disable then re-enable a plugin",
    "can enable then disable a plugin",
    "returns only enabled plugins",
    "returns plugin without enabled field (defaults to enabled)",
    "returns empty array when all plugins are disabled",
    "returns multiple enabled plugins",
    "reflects enabled state after disablePlugin call",
    "reflects enabled state after enablePlugin call",
    "skips disabled plugins",
    "returns empty array when all plugins disabled",
    "defaults enabled to true when field is absent",
    "preserves enabled=false when set",
    "preserves enabled=true when set"
   ],
   "core/plugin.test.ts": [
    "throws PluginError if manifest.json does not exist",
    "throws PluginError if manifest.json is invalid JSON",
    "throws PluginError if manifest is not an object",
    "throws PluginError if required field is missing",
    "throws PluginError if field is empty string",
    "returns Plugin with manifest and dir on success",
    "reads manifest.json from plugin directory",
    "returns empty array if pluginsDir does not exist",
    "returns empty array if readdirSync throws",
    "skips non-directory entries",
    "skips directories with invalid manifests silently",
    "returns plugins for valid subdirectories",
    "returns multiple plugins",
    "scans .deckent/plugins/ in project root",
    "returns plugins found in .deckent/plugins/"
   ],
   "core/policy-engine.test.ts": [
    "permits when every evaluated layer passes",
    "permits an empty policy (no layers ⇒ nothing blocks)",
    "denies on RBAC authorization failure (hard)",
    "denies when an activation exclusion rule matches (hard)",
    "parks when the condition gate fails (precondition unmet, not forbidden)",
    "suggests when activation is below minScore (permitted-but-weak)",
    "RBAC deny outranks a failing condition gate (deny beats park)",
    "activation exclusion outranks weak activation and a failing gate",
    "failing gate (park) outranks weak activation (suggest)",
    "rbac layer reflects can(): admin+admin permitted, viewer+admin denied",
    "rbac layer denies on an invalid tenantId (delegates to isValidTenantId via can())",
    "activation layer reflects evaluateActivation() score + matched rules",
    "condition layer reflects evaluateCondition() operator semantics ($gt)",
    "does not mutate the input"
   ],
   "core/pool-activation-validation.test.ts": [
    "starts with zero invalid count before any load",
    "control: a real-shaped valid manifest (with activation) loads unchanged, invalidCount stays 0",
    "control: a manifest with no activation field at all still loads unchanged (behavior not narrowed)",
    "bozuk-JSON: corrupt manifest.json is excluded from pool AND visibly counted",
    "şema-dışı-activation: activation.rules is a string (not an array) is excluded AND counted",
    "şema-dışı-activation: a rule missing \"score\" is excluded AND counted",
    "eksik-alan: a manifest missing the required \"id\" field is excluded AND counted",
    "eksik-alan: activation missing \"minScore\" is excluded AND counted",
    "invalidManifests resets on each loadSkills() call (reflects most recent load only)",
    "accepts a valid manifest with the real activation shape",
    "accepts a manifest with no activation field (unvalidated, not narrowed)",
    "rejects activation as a non-object (e.g. a string)",
    "rejects activation as null",
    "rejects activation.exclude that is not an array",
    "rejects activation.minScore that is not a number",
    "rejects a rule.score that is not a number",
    "rejects a rule.when that is not an object",
    "accepts an exclusion rule with a \"reason\" string",
    "starts with zero invalid count before any load",
    "control: a real-shaped valid agent.json (with activation) loads unchanged, invalidCount stays 0",
    "control: an agent.json with no activation field still loads unchanged",
    "bozuk-JSON: corrupt agent.json is excluded from pool AND visibly counted",
    "directories with no agent.json at all are NOT counted as invalid (no false positives)",
    "şema-dışı-activation: activation.rules is a string (not an array) is excluded AND counted",
    "eksik-alan: an agent.json missing the required \"name\" field is excluded AND counted",
    "accepts a valid agent with the real activation shape",
    "accepts an agent with no activation field (unvalidated, not narrowed)",
    "rejects activation as a non-object",
    "rejects activation.rules that is not an array",
    "rejects a rule missing \"score\""
   ],
   "core/post-settlement-verification.test.ts": [
    "completes only after containment, fenced receipt, permission, command evidence and promotion proof",
    "keeps the %s ingress provider-neutral without creating logical work",
    "returns typed HOLD when build is attempted before coordinator containment or terminal receipt",
    "fences permission to the exact receipt generation, attempt, source and build identity",
    "rejects an unbounded argv command without exposing a shell command surface",
    "rejects NUL-delimited command adapter fields",
    "does not promote from exit status alone and rejects mismatched promotion evidence",
    "cannot transition by timer-shaped or out-of-order evidence"
   ],
   "core/preflight-typecheck-preference.test.ts": [
    "prefers the dedicated typecheck script over a dist-mutating build script",
    "rewrites the legacy bare `npx tsc` fallback to --noEmit when no typecheck exists",
    "falls back to the build command only when no typecheck command resolves",
    "skips honestly when the stack has neither typecheck nor build"
   ],
   "core/pricing-updater.test.ts": [
    "fetches and parses LiteLLM JSON",
    "throws on HTTP error",
    "fetches and parses OpenRouter models",
    "updates from mock LiteLLM data, dry-run preserves file",
    "writes file when not dry-run",
    "preserves user notes and cost_limits across update",
    "rejects malformed LiteLLM entries (unit safety)",
    "handles all-sources failure gracefully",
    "formats successful result",
    "formats delta report",
    "formats dry-run marker"
   ],
   "core/principal-can.test.ts": [
    "grants everything with \"*\"",
    "grants exact match",
    "grants resource wildcard \"<res>:*\"",
    "grants action wildcard \"*:<act>\"",
    "denies when not granted",
    "denies on empty permission set (fail-closed)"
   ],
   "core/principal.test.ts": [
    "projects the exact OS username or honestly returns unavailable",
    "resolves the actual os user + host with os-user assurance",
    "never produces the retired synthetic literals",
    "carries identityClass/assurance/provenance onto the actor",
    "flags a pre-PRINCIPAL-001 bare actor (no assurance fields)",
    "flags explicit unverified assurance (api-static class)",
    "accepts os-user and token-verified assurance",
    "recordActorAssurance never throws and returns the finding (P1a contract)",
    "flag OFF: an untrusted actor still passes (v1 behaviour byte-identical)",
    "flag ON: the same actor is refused with a typed error before admission",
    "flag ON: an explicitly unverified identity is refused too (api-static class)",
    "flag ON: a real OS identity passes (enforcement blocks only the untrusted)",
    "an explicit true reaches the resolved runtime config",
    "an explicit false reaches the resolved runtime config",
    "an absent key defaults to false — v1 behaviour, never blind-on"
   ],
   "core/process-group-anchor.test.ts": [
    "completes the ready handshake, proves group non-emptiness while alive, then group-terminates cleanly",
    "keeps stream EOF, IPC target-exit, and the exit record as three independent, correctly-ordered records",
    "signalGroup carries authority over a live target while the anchor survives the SIGTERM",
    "killTarget reaches a SIGTERM-ignoring target via the anchor-held handle without touching the anchor",
    "retains authority over a same-group grandchild that outlives the target that spawned it",
    "does not reach a detached grandchild that escaped into its own process group",
    "reports no authority once the group has been terminated: both signal paths return false and send nothing",
    "SIGKILLs its own group on owner IPC disconnect, with no supervisor involved",
    "surfaces a spawn-error through the anchor without losing the anchor itself",
    "reports unsupported-platform on win32 without spawning anything",
    "kills its own direct handle (not the group) on handshake timeout"
   ],
   "core/process-tree-termination.test.ts": [
    "signals the negative detached process-group id on POSIX",
    "falls back to the direct child when POSIX group signalling fails",
    "uses taskkill tree semantics on Windows and falls back on non-zero settlement",
    "signals the direct child when no platform tree identity exists",
    "escalates SIGTERM to SIGKILL after the bounded grace window",
    "cancels escalation when the child exits during the grace window"
   ],
   "core/production-wiring-adapters.test.ts": [
    "resolves a declared language and capability through the registry",
    "returns typed unsupported HOLD for an unknown declaration",
    "uses priority deterministically and HOLDs an unavailable selected adapter",
    "rejects malformed adapter evidence instead of failing open",
    "keeps static reachability as presence-only supporting evidence",
    "does not promote runtime observation state %s to complete"
   ],
   "core/production-wiring-contract.test.ts": [
    "registers the exact memory compact read/export identity without changing Terminal authority",
    "registers the metrics rotation/retention identity and host-completes its proof program",
    "completes only the exact memory identity into its two-asset bounded platform profile",
    "admits only the registered Terminal %s identity with capability-scoped platforms",
    "rejects a near-match Terminal health identity rather than borrowing its profile",
    "rejects near-match and foreign memory identities instead of borrowing the registered profile",
    "keeps both memory verifier assets outside the later feature task write scope",
    "canonicalizes a V2 read-only proof program and resolves only exact declared coverage",
    "holds missing, duplicate, and unexpected host proof target coverage",
    "rejects a missing producer target and an unbound producer-consumer relationship",
    "rejects ambiguous adapter authority and divergent execution within one observation group",
    "rejects a mutated host program digest rather than repairing it",
    "resolves a fully evidenced production path without a generic wired boolean",
    "holds incomplete topology and evidence with stable typed reasons",
    "resolves unsupported capability separately from incomplete evidence",
    "fails closed on contradictory evidence with precedence over unsupported evidence",
    "never promotes %s evidence to completion",
    "requires an executed proof target even when an authority record declares it complete",
    "does not grant public-library changes an implicit wiring exemption",
    "accepts Task 25 only as a same-DAG staged foundation with an outer barrier",
    "rejects a staged foundation with $name",
    "rejects a foundation that claims ordinary production completion"
   ],
   "core/production-wiring-evidence-schema.test.ts": [
    "host-derives both V2 contract and proof program digests deterministically",
    "retains V1 as a distinct historical evidence shape",
    "creates deterministic versioned plan evidence with exact closure tasks and outer barrier",
    "accepts digest-bound presence-only worker evidence without promoting it to completion",
    "rejects $name"
   ],
   "core/production-wiring-result-settlement.test.ts": [
    "settles only when the exact plan digest and canonical host consumer execution agree",
    "keeps a worker presence claim on HOLD until host consumer execution evidence is attached",
    "holds contradictory worker observations even when a host execution reference exists",
    "settles V2 only with the exact host proof program and COMMITTED effect bindings",
    "rejects V1, wrong-program and malformed effect authority at a V2 settlement boundary"
   ],
   "core/project-slug.test.ts": [
    "matches Claude Code parity for %s"
   ],
   "core/prompt-canary-bench.test.ts": [
    "every case transforms as declared",
    "case ids are sequential from 1 with no gaps"
   ],
   "core/prompt-compile-plan.test.ts": [
    "canonicalizes deterministically, freezes deeply, and emits evidence for every ID",
    "rejects evidence for an unknown criterion identity",
    "projects actual outcome without treating applicability as success authority",
    "allows tech debt only with exact structured criterion IDs"
   ],
   "core/prompt-cost-canary-archive.test.ts": [
    "ignores landing and skill sidecars while reading only canonical task roles",
    "folds root NO_GO and FIX DONE by terminal lineage identity, not filename order",
    "resolves legacy attemptNum-only evaluations through the result attempt identity",
    "accepts a bounded depth-2 fix chain (root → fix → fix-fix) in one lineage",
    "rejects duplicate canonical results, mismatched exact evaluation identity, and conflicted manifests",
    "does not consult unmanifested legacy docs, evidence, or transcripts"
   ],
   "core/prompt-cost-canary-receipt-store.test.ts": [
    "creates canonical private bytes, verifies replay, and replays in a fresh facade",
    "uses opaque tenant/environment scope and isolates reads",
    "rejects invalid decisions and non-canonical or conflicting durable bytes"
   ],
   "core/prompt-cost-canary-task-authority.test.ts": [
    "keeps workload identity stable across sprint ids and path separators",
    "keeps workload identity while binding a changed effective feature snapshot",
    "round-trips canonical authority and rejects any digest or snapshot tamper"
   ],
   "core/prompt-cost-canary.test.ts": [
    "promotes comparable measured cohorts and exposes exact aggregates, ratios, and deltas",
    "settles fully-unpriced subscription cohorts on digest-bound token-total authority without fabricating USD",
    "waives the cache-regression guard only under a measured cost reduction, visibly and never the floor",
    "rejects every failed promotion threshold with bounded reason codes",
    "holds incomparable cohorts before applying thresholds",
    "holds empty evidence and validates malformed or duplicate samples",
    "canonicalizes object keys and sample order for stable plan and decision digests",
    "never substitutes a reference price and treats measured cost from zero as an unbounded increase",
    "accepts unavailable duration unless a duration threshold explicitly requires it"
   ],
   "core/prompt-delivery-receipt.test.ts": [
    "publishes full-digest immutable core bytes and binds exact runtime argv",
    "fails closed when existing digest-path bytes or runtime argv do not match",
    "atomically round-trips deterministic bytes bound to final rendered segments",
    "returns typed HOLD for malformed, mismatched, and invalid-digest artifacts",
    "rejects non-canonical or internally contradictory current receipts",
    "uses current delivered identities and never lets legacy claims override them",
    "fails closed for fresh missing/malformed receipts but preserves explicit legacy paths"
   ],
   "core/provider-auth-probe.test.ts": [
    "keeps ANTHROPIC_API_KEY presence unknown without a paid provider request",
    "accepts login only from exit 0 + structured loggedIn=true",
    "accepts the installed CLI contract: structured loggedIn=false exits 1",
    "scrubs every canonical provider credential from the auth-status child env",
    "keeps failed status evidence unknown",
    "does not accept truthy or missing loggedIn values",
    "preserves an exact API-key method reported by the CLI status contract",
    "uses one canonical command and rejects positive text on non-zero exit",
    "logged-out from \"Not logged in\" stdout EVEN with exit 0 (substring-trap guard)",
    "logged-in from \"Logged in using ChatGPT\" stdout",
    "scrubs every canonical provider credential from the Codex status child env",
    "unknown on probe timeout",
    "unknown when the codex CLI is missing (spawnError)",
    "unknown on indeterminate stdout (no login keywords)",
    "keeps OPENAI_API_KEY presence unknown WITHOUT spawning the CLI",
    "spawns `cursor-agent status` — never the bare `cursor` editor binary",
    "uses one canonical command and rejects positive text on non-zero exit",
    "logged-in: typed result carries authenticated=true and a subscription method",
    "logged-out from \"Not logged in\" stdout EVEN with exit 0 (substring-trap guard)",
    "unknown on indeterminate stdout (no login keywords)",
    "unknown on probe timeout",
    "unknown when the cursor-agent CLI is missing (spawnError)",
    "scrubs every canonical provider credential from the cursor status child env",
    "keeps GEMINI_API_KEY presence unknown without a paid provider request",
    "keeps oauth_creds.json token presence unknown without a supported status command",
    "logged-out when no oauth file and no env key",
    "unknown when oauth file is malformed",
    "returns unknown for an unsupported provider",
    "never leaks any provided secret value into detail across all states"
   ],
   "core/provider-authority-composition.test.ts": [
    "defaults only solo tenancy to main and composes one host authority",
    "holds enterprise mode without a verified tenant before opening storage",
    "holds when %s is unavailable",
    "holds on missing or project-scoped custody without constructing a runtime"
   ],
   "core/provider-authority-keyring.test.ts": [
    "resolves only below platform dataDir and keeps snapshots secret-free",
    "domain-separates MACs and keeps account pseudonyms stable across signing rotation",
    "allows exactly one rotation from an expected revision",
    "rejects a re-hashed revision that rewrites carried key derivation metadata",
    "imports legacy material as domain-scoped retired verification authority",
    "fails closed on unknown keys, revision tamper, unsafe modes and project scope"
   ],
   "core/provider-authority-runtime-service.test.ts": [
    "opens one provider-free lifecycle root and preflights exact unattended scope",
    "holds exact missing source and unsupported termination adapters without source calls",
    "holds widening project policy instead of consuming merged config",
    "holds missing custody and native Windows without provisioning a key",
    "uses the existing %s global-scope adapter without provider work",
    "closes once, fails closed after close, and preserves project identity on restart"
   ],
   "core/provider-billing-evidence.test.ts": [
    "captures the provider-final total and normalized per-model usage",
    "keeps the last billed envelope and ignores usage-only events",
    "unwraps the canonical host-written usage LogEvent used by Docker capture",
    "does not trust arbitrary nested billing-looking content",
    "marks the Sprint-455 local/provider gap as variance",
    "rejects malformed or negative provider totals",
    "aggregates exact-attempt envelopes and preserves lineage provenance",
    "rejects duplicate attempts and cross-provider aggregation"
   ],
   "core/provider-bootstrap-openai-compat.test.ts": [
    "registers deepseek adapter when DEEPSEEK_API_KEY is set",
    "skips deepseek gracefully when DEEPSEEK_API_KEY is absent",
    "registers multiple OpenAI-compat providers when multiple keys are set",
    "applies .deck DECKENT_DEEPSEEK_API_KEY to env and triggers registration",
    "registers zhipu adapter when ZHIPU_API_KEY is set",
    "skips OpenAI-compat providers when no keys are set",
    "maps DECKENT_DEEPSEEK_API_KEY to DEEPSEEK_API_KEY in process.env",
    "maps DECKENT_DASHSCOPE_API_KEY to DASHSCOPE_API_KEY in process.env",
    "maps DECKENT_ZHIPU_API_KEY to ZHIPU_API_KEY in process.env",
    "does not set env vars for absent deck keys"
   ],
   "core/provider-bootstrap-register.test.ts": [
    "registers deepseek when DEEPSEEK_API_KEY is present",
    "skips OpenAI-compat providers gracefully when no keys are present",
    "registers deepseek + qwen + zhipu simultaneously when all keys are present",
    "exposes a registered adapter while preserving live endpoint availability truth"
   ],
   "core/provider-bootstrap.test.ts": [
    "should return registered, skipped, and defaultProvider fields",
    "should have ProviderName types in registered array",
    "should have name and reason in skipped entries",
    "should set default to brain_provider when available and registered",
    "should fall back to first registered when brain_provider unavailable",
    "should default to claude when no brain_provider configured",
    "should return null defaultProvider when no providers registered",
    "should skip already-registered providers without error",
    "should count pre-registered providers in registered array",
    "should report unavailable providers in skipped array",
    "should include reason string for each skipped provider",
    "should warn when configured brain_provider is unavailable",
    "should use explicit projectRoot when provided",
    "should fall back to config.projectRoot when projectRoot omitted",
    "should use provided registry instead of global singleton",
    "should register providers into the given registry",
    "should return a connector field in BootstrapResult",
    "should create Connector with available providers",
    "should have same providers in Connector as in registry",
    "should run health check during bootstrap",
    "should keep unhealthy providers registered in Connector",
    "should return empty Connector when no providers available",
    "backward compat: providerRegistry.getDefault() still works after bootstrap",
    "should not throw when health check fails internally",
    "Connector size matches registered provider count",
    "Connector getProvider returns same adapter as registry",
    "existing BootstrapResult fields still present alongside connector",
    "should load .deck API key for codex (OPENAI_API_KEY)",
    "should load .deck API key for gemini (GOOGLE_API_KEY)",
    "should load .deck API key for claude (ANTHROPIC_API_KEY)",
    "should OVERWRITE existing OPENAI_API_KEY — .deck takes precedence over system env",
    "should OVERWRITE existing GOOGLE_API_KEY — .deck takes precedence over system env",
    "should skip .deck loading when auth_mode is subscription",
    "should not crash when .deck file is missing (empty secrets)",
    "should ignore empty string values from .deck",
    "should load .deck when auth_mode is api",
    "should load .deck when auth_mode is hybrid"
   ],
   "core/provider-capabilities.test.ts": [
    "returns correct Claude capabilities",
    "returns correct Codex capabilities",
    "returns correct Gemini capabilities",
    "throws DeckentError for unknown provider",
    "returns a copy (not the original object)",
    "returns all seven providers for streaming",
    "returns claude/codex/gemini/openrouter/local-llm for toolUse (ollama: false)",
    "returns all 7 providers for maxContextTokens (non-zero)",
    "returns all 7 providers for costPerMillionTokens",
    "returns true when provider meets all boolean requirements",
    "returns true when provider meets context token requirement",
    "returns false when provider context is too small",
    "returns true for cost within budget",
    "returns false when provider cost exceeds budget",
    "returns true for empty requirements",
    "throws for unknown provider",
    "returns all seven providers, including cursor, openrouter and local-llm"
   ],
   "core/provider-command-spec.test.ts": [
    "codex declares the measured system-core and project-context suppression argv",
    "leaves prefix fields undefined for every other provider spec",
    "declares a stdin, plain-text, read-only isolated Codex planner profile",
    "does not invent planner profiles for providers without measured contracts",
    "does not consume prefix fields: every provider command remains byte-identical",
    "codex: exec with current flags (validated vs codex-cli 0.137.0 --help), apiId, stdin prompt",
    "gemini: -p $(cat …), yolo + skip-trust, -m apiId, no --dangerously-skip-permissions",
    "cursor: read-only ask mode, JSON output, apiId, then positional prompt",
    "cursor: execution permission is explicit and never default-on",
    "claude: stdin prompt (-p -), apiId, allowedTools + dangerously-skip-permissions on autoApprove",
    "claude without autoApprove omits dangerously-skip-permissions",
    "claude finite verifier narrows the visible tool schema and isolates project context",
    "ordinary claude worker does not receive finite-verifier isolation flags",
    "promptFeed: claude+codex=stdin (caller pipes < file); gemini=inline (cat embedded in -p)",
    "oauthHomeDir per provider (for container auth mount)",
    "claude: appends --effort <level> when reasoningEffort is set",
    "codex: appends -c model_reasoning_effort=<level> when reasoningEffort is set",
    "gemini: reasoningEffortArgs=null → no effort flag even if a level is passed",
    "no reasoningEffort → no effort flag (opt-in)",
    "claude: appends --exclude-dynamic-system-prompt-sections when excludeDynamicPromptSections is set",
    "claude: no flag when excludeDynamicPromptSections is false/unset (opt-in, byte-safe)",
    "codex/gemini: excludeDynamicPromptSectionsFlag=null → flag never emitted even when opted in",
    "returns the spec for a known provider",
    "declares live-usage capability separately from final usage output",
    "returns null for an unknown/host-only provider (e.g. ollama) → caller honest-fails",
    "claude (allowedToolsFlag present) + write scope → flag-enforced",
    "codex (allowedToolsFlag null) + write scope → UNENFORCED (the silent full surface)",
    "gemini (allowedToolsFlag null) + write scope → UNENFORCED",
    "cursor (allowedToolsFlag null) + write scope → UNENFORCED",
    "no write scope → nothing to enforce (NO_WRITE_SCOPE), even for codex",
    "unknown provider + write scope → UNKNOWN_PROVIDER (fail-visible, not silent)",
    "production grant (scoped Write + unscoped Bash) → DEFEATED_BY_SHELL",
    "paren-aware: commas INSIDE Write(a,b,c) do not split the token",
    "path-scoped Write with NO shell grant → TOOL_BOUND (scope holds)",
    "bare unscoped Write/Edit → WRITE_GRANT_UNSCOPED (nothing narrower to defeat)",
    "no write authority at all → NO_WRITE_GRANT (even with Bash present)",
    "declaredScope reflects the task filesWrite, independent of the verdict",
    "empty/undefined allowedTools → NO_WRITE_GRANT (no grant, nothing to escape)"
   ],
   "core/provider-concurrency-admission-wire.test.ts": [
    "uses the resolver intersection before creating the existing provider-limit reservation",
    "isOpenAIModel accepts a non-builtin codex model registered at runtime",
    "isGeminiModel accepts a non-builtin gemini model registered at runtime",
    "isClaudeModel accepts a non-builtin claude model registered at runtime",
    "builtin models still resolve to the correct provider after the change",
    "routes a non-builtin codex model to the codex provider",
    "routes a non-builtin gemini model to the gemini provider",
    "isValidModel returns true for a registered non-builtin model and false before",
    "exposes a freshly registered codex model on PROVIDER_MODEL_MAP.codex",
    "exposes a freshly registered gemini model on PROVIDER_MODEL_MAP.gemini",
    "removes an unregistered model from PROVIDER_MODEL_MAP on next access (live read)",
    "preserves builtin model coverage when no extras are registered",
    "ClaudeAdapter.supportedModels reflects a runtime-registered claude model",
    "CodexAdapter.supportedModels reflects a runtime-registered codex model",
    "GeminiAdapter.supportedModels reflects a runtime-registered gemini model"
   ],
   "core/provider-concurrency-capability.test.ts": [
    "admits the configured ceiling when all supplied authorities are fresh and sufficient",
    "degrades to the intersection and identifies every equally restrictive authority",
    "holds unknown provider capacity without fabricating an admitted ceiling",
    "holds evidence that is expired or not yet valid at the caller-supplied evaluation time",
    "holds rather than intersecting authorities from different tenant, principal, or auth scopes"
   ],
   "core/provider-concurrency-runtime-projection.test.ts": [
    "joins admitted capacity with direct execution intervals and exposes it through CLI JSON",
    "keeps missing capability as HOLD and unknown rather than rendering capacity as zero",
    "rejects intervals attributed to another principal instead of blending attained concurrency",
    "reads bounded attained truth without creating capacity authority or a missing database",
    "requires exact task and attempt attribution when the caller supplies a run scope",
    "keeps retired open rows as forensic history without projecting them as active concurrency"
   ],
   "core/provider-config-canonicalizer.test.ts": [
    "promotes flat-only %s to providers.%s",
    "deduplicates equal dual-form %s/providers.%s",
    "compares provider_overrides independent of object key order",
    "fails loudly for differing %s/providers.%s",
    "does not partially canonicalize earlier slots when a later slot conflicts",
    "leaves an invalid providers shape for the config validator"
   ],
   "core/provider-credential-key-threading.test.ts": [
    "passes the same base+registry key set to Codex, Gemini, Ollama, OpenAI-compatible and OpenRouter adapters"
   ],
   "core/provider-detection.test.ts": [
    "returns version string when CLI exits with 0",
    "returns undefined when CLI exits with non-zero",
    "returns undefined when spawnSync throws",
    "returns undefined when stdout is empty",
    "trims whitespace from version output",
    "passes custom args to spawnSync",
    "returns all auto-detected providers including cursor",
    "detects Cursor Agent from cursor-agent on PATH",
    "detects Claude as available when CLI returns success",
    "detects Claude as unavailable when CLI fails",
    "detects Codex as available when CLI + API key present",
    "detects Codex as available via OAuth session when CLI present but no API key",
    "detects Codex as unavailable when API key present but no CLI",
    "detects Gemini as available when GOOGLE_API_KEY set",
    "detects Gemini as unavailable when no API key",
    "Gemini has no version (no CLI)",
    "each provider includes its models from PROVIDER_MODEL_MAP",
    "empty OPENAI_API_KEY is not treated as api_key auth (falls back to session)",
    "empty GOOGLE_API_KEY is treated as missing",
    "includes \"Providers:\" header",
    "shows checkmark for available provider",
    "shows cross for unavailable provider",
    "shows model list for each provider",
    "omits version when undefined"
   ],
   "core/provider-discovery.test.ts": [
    "is exactly claude, codex, gemini in that order",
    "excludes cursor until the onboarding/MCP-host consumers can accept it",
    "reports present:false, version:undefined for every provider when the version probe finds nothing",
    "reports present:true with the parsed version for every provider when the version probe finds a match",
    "probes each provider independently — mixed present/absent",
    "authState is \"unknown\" for every provider when no auth probe is injected",
    "authState is \"unknown\" even for an absent provider when no auth probe is injected",
    "uses the injected auth probe, per provider, when supplied",
    "never calls the auth probe for a provider that is not present (auth probe still receives the call — presence is independent of auth)",
    "extracts the semver substring from a multi-word CLI banner",
    "extracts a two-part version (no patch)",
    "falls back to the raw probe output when no semver substring is present",
    "trims leading/trailing whitespace already handled by the probe (raw passthrough contract)",
    "does not throw when called with no arguments and does not spawn a real process (spawnSync mocked to fail closed)",
    "picks up a version via the default probe when spawnSync (mocked) reports success",
    "maps 1:1 onto ConnectProviderDetection\\'s field shape (name/cliAvailable/version/authState)"
   ],
   "core/provider-evidence-probe-contract.test.ts": [
    "accepts the %s arm and rejects a USD field",
    "requires owner-authored USD only for the metered-api arm",
    "keeps maxUsd absent from the subscription type",
    "guards the freshness identity and complete approval subject",
    "accepts only the bounded scalar transport request surface",
    "guards and exhaustively consumes every frozen union arm",
    "rejects promotion claims and malformed discriminator payloads"
   ],
   "core/provider-evidence-producer-freshness.test.ts": [
    "reuses fresh exact-scope evidence without another model probe",
    "lets a same-epoch follower reuse the winner instead of reporting replay blockage",
    "holds negative evidence through cooldown and opens a new immutable epoch after expiry"
   ],
   "core/provider-evidence-producer.test.ts": [
    "holds an unregistered exact source scope before any evidence source runs",
    "persists limit before one receipt-bound exact probe and exposes no raw principal",
    "persists unavailable limit evidence and never invokes a model when the limit source fails",
    "holds credential-only identity evidence before limit, probe, or receipt declaration",
    "keeps local execution accountless and never invokes the account authority",
    "admits the bounded probe on an advisory limit under the block ratio, keeping the durable snapshot advisory (§12.2 Öneri-A)",
    "holds a blocked advisory limit WITHOUT running the probe (fails closed at the block ratio)",
    "uses real Claude model-scoped evidence to admit Opus but hold Fable at its exact limit",
    "runs the bounded probe above the ratio threshold only under explicit observe-only policy",
    "reports a failed observe-only probe as probe_unreachable, never durable advisory limit_hold",
    "does not let a fresh cooldown from an older reachability source suppress the current source revision",
    "threads exact model and verified account provenance into the limit source",
    "records successful transport but rejects an exact-model mismatch",
    "allows exactly one model invocation for concurrent identical refreshes",
    "terminally rejects the receipt when truth persistence fails after the provider call",
    "holds a refresh whose budget projection violates the billing-mode contract, before any probe"
   ],
   "core/provider-evidence-source-registry.test.ts": [
    "is registration-order independent and resolves one exact scope",
    "does not fall back for a near-match scope %#",
    "rejects duplicate exact scope instead of overwriting by registration order",
    "resolves api/api exactly and never falls back to a local HTTP near-match",
    "snapshots nested source authorities against post-construction mutation",
    "rejects non-executable authority registration %#",
    "returns no source for an unknown query scope"
   ],
   "core/provider-execution-ingress-authority.test.ts": [
    "classifies both native and cross-realm-shaped authority HOLD errors",
    "preserves the separately gated default when no authored authority is injected",
    "projects an authority-open HOLD with an exact opaque ingress evidence ref",
    "passes a healthy composition as ready WITHOUT running the role admission",
    "grants no role an execution permit — Brain preflight is composition health only"
   ],
   "core/provider-execution-observation-adoption-adversarial.test.ts": [
    "rejects tampered digests, swapped references, and unknown fields without changing evidence",
    "rejects foreign-project replay and leaves the replay and both projects unchanged",
    "never follows a replacement symlink and preserves its target bytes",
    "rejects partial, non-canonical, and oversized JSON with typed failures",
    "rejects unsafe scope values and persists neither raw scope secrets nor database secrets"
   ],
   "core/provider-execution-observation-adoption-receipt-store.test.ts": [
    "publishes create-only canonical bytes, replays identically, and survives a fresh store instance",
    "isolates environment and tenant scopes and never stores their raw identifiers",
    "verifies one exact receipt after process restart without foreign-tenant discovery or private residue",
    "rejects non-canonical, duplicate, missing-version, and unsupported-version envelopes",
    "fails closed on traversal and on stale source or target bytes",
    "preserves a conflicting final artifact and reports a typed collision",
    "discovers only independently verified final receipts and enforces its explicit bound",
    "publishes identical bytes concurrently and retains one restart-safe final artifact",
    "reads the exact final entry in a 10k-entry scope without discovery and bounds inventory first",
    "rejects a malformed receipt stored under an exact final name"
   ],
   "core/provider-execution-observation-adoption.test.ts": [
    "proves all 43 legacy rows byte-exactly, separates extra owned rows, and never writes either database",
    "rejects a duplicate-capable identity schema before byte-identical rows can reach Map construction",
    "rejects malformed required column properties: %s",
    "accepts the canonical fresh-v2 column order as well as migrated-v2 fixtures",
    "rejects unowned extra rows and unsupported or structurally wrong schemas",
    "rejects a tampered plan digest and any database change after planning",
    "returns a replay-stable, verifier-produced receipt without leaking legacy identities or mutating files",
    "scans a multi-page large store within its bound and fails closed immediately above it",
    "leaves pre-existing empty WAL and non-empty SHM sidecars byte- and metadata-stable",
    "rejects a non-empty WAL for %s without cleaning source-side evidence",
    "keeps the complete source tree stable and removes every private snapshot on success and failure",
    "deterministically rejects a descriptor-pinned main-file change for %s",
    "rejects a database symlink for %s without replacing it",
    "rejects a WAL symlink for %s without replacing it",
    "enforces the byte bound before SQLite opens"
   ],
   "core/provider-execution-observation-migration-approval.test.ts": [
    "requires interactive authenticated allow before fresh apply and avoids direct decide",
    "never auto-allows deny, expiry, or parked expiry",
    "rejects self-approval and wrong tenant, project, digest, or generation",
    "rejects stale MAC/session and expired authorization reuse",
    "preserves FWW across authenticated replay, conflict, and restart"
   ],
   "core/provider-execution-observation-migration.test.ts": [
    "durably backs up and applies v1 while preserving the exact 43-row lineage and target index",
    "rejects absent, denied, expired, mismatched, and tampered authority before filesystem mutation",
    "detects source tamper and first-writer-wins without overwriting either migrator evidence",
    "retains verified backup and rolls back the transaction when apply fails after backup",
    "models a receipt-window crash, then safely replays as already current without assigning legacy ownership",
    "rejects path escape, database symlinks, row/byte overruns, and invalid platform adapters"
   ],
   "core/provider-execution-observation-reconciliation-approval.test.ts": [
    "returns a verified lineage claim only after an authenticated live allow, and is replay-safe",
    "holds a direct allow with missing authorization and rejects self, stale, denied, and mismatched-batch applies",
    "rejects exact request and decision tampering before reconciliation can apply"
   ],
   "core/provider-execution-observation-reconciliation-receipt-store.test.ts": [
    "binds the verified approval claim into a private content-addressed receipt",
    "rejects approval and candidate receipt tampering",
    "uses monotonic candidate-specific freshness after legitimate unrelated appends and retirements",
    "fails fresh verification when an exact candidate is no longer retired"
   ],
   "core/provider-execution-observation-reconciliation.test.ts": [
    "discovers settled candidates across live-shaped runs with a deterministic exact preimage and no synthetic end",
    "optionally narrows to explicit runs without retiring other settled runs",
    "enforces bounded discovery and rejects stale lineage rather than broadening retirement",
    "binds all-runs versus explicit selection and detects newly eligible runs outside the original candidates"
   ],
   "core/provider-execution-observation-store.test.ts": [
    "keeps the ownership delimiter as an escaped NUL in ordinary UTF-8 source text",
    "exports and uses one canonical project-relative database path by default",
    "persists exact-principal start/end intervals across restart",
    "opens an existing store read-only without mutating its schema or accepting writes",
    "rejects container and worker lifecycle proxies",
    "retains fence and conflicting replay evidence without altering the interval",
    "enforces bounded open, closed, and contradiction retention",
    "treats exact duplicate observations as idempotent",
    "selects and retires only exact run-owned open intervals without deleting foreign history",
    "keeps v1 reads byte-stable, requires explicit migration for writes, and preserves legacy ownership",
    "rejects an unsupported future schema without weakening schema mismatch admission"
   ],
   "core/provider-execution-observation.test.ts": [
    "parses a valid start/end input and rejects a malformed shape",
    "computes a deterministic content-addressable event id",
    "tracks attained concurrency, peak overlap and closes intervals cleanly",
    "foldProviderExecutionObservations applies an ordered batch identically to repeated folds",
    "accepts an exact duplicate start as a no-op returning the same state reference",
    "accepts an exact duplicate end as a no-op returning the same state reference",
    "holds on a blank fence",
    "holds on an end with no prior start",
    "holds when the end sequence does not follow the recorded start sequence",
    "holds when an end claims a different attempt than the recorded start (foreign attempt)",
    "holds when an end claims a different exact run than the recorded start",
    "holds when a second start for the same execution claims a different attempt (foreign attempt)",
    "holds on a conflicting (non-identical) replay of the same start",
    "holds on a conflicting (non-identical) replay of the same end",
    "prunes closed executions not explicitly retained, and always keeps open ones"
   ],
   "core/provider-execution-profile.test.ts": [
    "resolves built-in local and unknown remote providers from one catalog",
    "accepts a parametric custom local adapter declaration",
    "fails loudly when catalog and adapter authorities disagree"
   ],
   "core/provider-failure-classifier.test.ts": [
    "classifies the Claude CLI \"Usage limit reached\" message",
    "classifies an HTTP 429 / too many requests as usage-limit",
    "classifies a generic rate limit / rate_limit_error as usage-limit",
    "classifies authentication_error / invalid api key as auth",
    "classifies a 401 / \"Please run /login\" as auth",
    "classifies exitCode 137 as oom (hard rule, beats any text)",
    "classifies OOM/SIGKILL text (oomkilled, exit 137, sigkill) as oom",
    "returns unknown for empty / whitespace input",
    "returns unknown for ordinary code failures (tsc/test) — not a provider failure",
    "prefers usage-limit over auth when both signatures co-occur",
    "does NOT flag usage-limit when the worker produced work, despite \"rate-limit\" in notes",
    "still flags a genuine usage-limit when the worker produced NO work (exit-without-result)",
    "producedWork=true never masks a real OOM (exitCode 137 hard-rule wins)",
    "rejects usage-limit text without provider-reported evidence",
    "keeps planner, scope, and code failures in task/dependency repair",
    "does not quarantine sprint-480 partial execution with high usage and a diff",
    "rejects non-limit provider evidence instead of guessing from result notes",
    "two NO_GOs that produced work (one mentions rate-limit) → 0 usage-limit, skipFix false",
    "flags skipFix when ALL failures are usage-limit",
    "flags skipFix at exactly the 50% boundary (>=)",
    "does NOT skip when a single/sparse limit is below threshold (normal FIX preserved)",
    "does NOT skip for an empty failure set (total=0 never blocks FIX)",
    "counts each category accurately in a mixed set",
    "returns the Turkish notice for lang=tr",
    "returns the English notice for lang=en and for unknown langs (default en)"
   ],
   "core/provider-fallback.test.ts": [
    "returns primary provider when it is available",
    "returns original model unchanged when primary is available",
    "falls back when primary is unavailable",
    "remaps model via getEquivalentModel on fallback",
    "remaps standard-tier model correctly on fallback",
    "remaps economy-tier model correctly on fallback",
    "throws when primary unavailable and no fallback configured",
    "error message mentions no fallback_provider configured",
    "falls back when primary is not registered at all",
    "throws when both primary and fallback are unavailable",
    "error mentions both providers when both unavailable",
    "throws when fallback provider is not registered",
    "error mentions fallback not registered",
    "returns correct FallbackResult shape for primary",
    "returns correct FallbackResult shape for fallback",
    "does not check fallback availability when primary is available",
    "resolves the brain primary from brain_provider",
    "resolves the worker primary from worker_provider",
    "auditor primary defaults to brain_provider (brain-family) when no auditor_provider set",
    "auditor primary honors provider_fallback.auditor_provider over brain_provider",
    "defaults the primary to claude when nothing is configured",
    "grouped providers.brain wins over legacy brain_provider for the primary",
    "uses the per-role fallback chain in its configured order",
    "falls back to the global chain when no per-role chain is present",
    "preserves OpenRouter as a first-class fallback and auditor primary",
    "falls back to the legacy single fallback_provider when no chain is configured",
    "strips the primary out of the fallback chain and de-dups, preserving order",
    "CONFIGURED order is authoritative — reversing the config reverses the chain",
    "defaults unattended to true; honors an explicit false",
    "returns an empty fallback chain when nothing is configured",
    "fails loudly instead of iterating a string fallback chain character by character",
    "fails loudly when an untyped caller bypasses config validation"
   ],
   "core/provider-limit-admission.test.ts": [
    "returns executable %s admission only with an allowed primary reservation",
    "turns a primary reservation race into durable fallback admission",
    "returns terminal HOLD when every configured provider loses reservation admission",
    "never reserves when reachability is unknown",
    "replays the same durable admission idempotently",
    "grants provider execution only to the first atomic dispatch claimant",
    "holds a dispatched replay without trying a fallback provider",
    "holds a %s replay without opening fallback spend",
    "holds an expired admitted replay without trying fallback",
    "treats changed idempotent input as terminal store failure, never fallback",
    "fails closed at store-open on integrity-key mismatch before fallback is possible",
    "enforces one allowed provider winner for a logical invocation",
    "rejects a builder that changes the exact selected model"
   ],
   "core/provider-limit-authoring.test.ts": [
    "authors a validator-green provider_limits block from live provider truth",
    "closes the xverify composition hold the missing block caused",
    "derives selector hashes with the consumers own code, not a second derivation",
    "refuses typed when live account identity is unresolvable",
    "refuses typed when the account identity is expired rather than pseudonymizing it",
    "refuses typed when no live source bundle is registered for the scope",
    "refuses typed when the live source reports no windows",
    "refuses typed when the owner values do not form a complete parent layer",
    "refuses to write without an explicit owner confirmation",
    "writes the authored block into the global layer once confirmed",
    "writes into the exact global file the config loader reads back",
    "adds one exact provider policy while preserving the existing authority entries",
    "updates only the matching selector and makes observe-only explicit",
    "refuses a stale CAS plan after another authority revision lands"
   ],
   "core/provider-limit-policy.test.ts": [
    "projects exactly one authored runtime/backend selector and rejects ambiguity",
    "accepts an exact remote API selector without collapsing it into HTTP/in-process",
    "resolves a parent policy with digest-derived provenance and no numeric defaults",
    "allows project tightening while inheriting every omitted parent value",
    "resolves owner-authored observe-only and lets a project tighten it back to enforce",
    "holds a widening project layer before it becomes policy: %s",
    "holds when parent authority or its exact selector is missing",
    "derives the same effective ref regardless of authored set ordering",
    "rejects duplicate selectors and project selectors without parent authority",
    "enforces project-tighten-only at generic config merge without trusting the merged field",
    "normalizes, isolates and deep-freezes separately authored authority layers",
    "pre-resolves exact selectors for the reduced runtime store query",
    "holds when distinct full selectors collapse onto one runtime query"
   ],
   "core/provider-limit-store.test.ts": [
    "resolves the account-wide store under each platform global state root",
    "persists a snapshot across restart and shares account capacity across projects",
    "holds missing, unknown and stale snapshots without fabricating capacity",
    "release frees capacity and consumed usage remains reserved until a newer snapshot",
    "never releases capacity from unresolved termination evidence",
    "fails reads, capacity projection and duplicate release replay after authority revocation",
    "expires leases and enforces idempotency for local null-account scope",
    "rejects mismatched settlement scope and detects immutable payload tampering",
    "fails capacity projection closed on an unsigned released lifecycle row",
    "fails loudly on an incompatible pre-existing schema",
    "fails loudly when an immutable schema index is weakened",
    "isolates quota buckets and applies the requesting policy at reservation time",
    "holds incomplete estimate scope and a lease that outlives its evidence",
    "requires the same host integrity key across restart",
    "serializes projected policy across eight independent store connections",
    "keeps mixed active/retired limit evidence readable across key rotation",
    "migrates v1 evidence transactionally without rewriting payload identity",
    "rejects a stale writer after a newer limit authority revision is durable"
   ],
   "core/provider-limit-truth.test.ts": [
    "accepts canonical remote API transport and backend without rewriting local HTTP identity",
    "derives a fresh allow decision from every complete required window",
    "warns without blocking and blocks at the policy threshold",
    "keeps crossed ratios visible but non-blocking in explicit observe-only mode",
    "keeps absolute floors fail-closed while ratio enforcement is observe-only",
    "uses remaining floors for absolute request/token windows",
    "keeps incomplete or absent capacity unknown instead of converting null to zero",
    "never promotes complete advisory history to authoritative remaining capacity",
    "maps source %s to explicit HOLD",
    "projects expired evidence to stale HOLD without mutating the stored result",
    "projects future-dated evidence to unknown HOLD and rejects non-canonical timestamps",
    "rejects a forged allow decision and legacy model aliases",
    "requires canonical quota identity and exact backend evidence for known capacity",
    "does not let evidence freshness outlive a required quota reset",
    "accepts measured zero usage but rejects negative usage and unproven release",
    "requires owner provenance before operator evidence can become authoritative"
   ],
   "core/provider-map-extend.test.ts": [
    "deepseek map — resolves to deepseek",
    "qwen map — resolves to qwen (dashscope alias too)",
    "glm/zhipu map — glm resolves to zhipu",
    "unknown provider — returns null gracefully",
    "existing canonical providers still resolve",
    "registers a new provider that normalizeProvider can resolve",
    "supports canonical name alias via second argument",
    "unregistered dynamic providers return null",
    "is case-insensitive for registered providers"
   ],
   "core/provider-observation-retirement.test.ts": [
    "retires only the settling generation and leaves foreign history untouched",
    "is idempotent: a second reconciliation of the same generation is a no-op",
    "never deletes evidence and never moves the schema version",
    "opens legacy v1 evidence read-only without migration or inferred ownership",
    "honors the provider fence and rejects an untyped or unowned generation",
    "derives the run identity from the same authority the spawn site binds",
    "never creates observation authority when a project has none",
    "uses the canonical observation database for default settlement",
    "settles a run with open intervals and stays a no-op on re-finalize",
    "owns nothing when the settling generation has no exact attempt identity"
   ],
   "core/provider-ollama-bootstrap.test.ts": [
    "returns ollama as available when /api/tags responds 200",
    "returns ollama as unavailable when fetch throws (server unreachable)",
    "returns ollama as unavailable when /api/tags responds non-2xx",
    "lists ollama alongside claude/codex/cursor/gemini (5 providers total)",
    "respects DECKENT_OLLAMA_HOST env override",
    "registers ollama adapter when local server is reachable",
    "skips ollama gracefully when local server is unreachable",
    "ollama adapter exposes ProviderAdapter surface (isAvailable + buildCommand)"
   ],
   "core/provider-overflow-gate.test.ts": [
    "(a) flag-off (undefined config) → null, reason=disabled, no advisory",
    "(a) flag explicitly false → null, reason=disabled",
    "(a) flag-off even with over-quota + configured target → no overflow",
    "(b) flag-on + over-quota + codex target → overflow opus→gpt-5 on codex",
    "(b) provider-agnostic — gemini target → overflow opus→gemini-2.5-pro",
    "(c) flag-on + over-quota + NO target → null, reason=no_target, advisory set",
    "(d) flag-on + under-quota signal → null, reason=no_limit, no advisory",
    "(d) flag-on + null signal (no snapshot yet) → null, reason=no_limit",
    "already-api task → null, reason=already_api (nothing to overflow)",
    "over-quota + target with no tier-equivalent → null, reason=no_equivalent + advisory",
    "uses the default singleton registry when none is injected"
   ],
   "core/provider-overflow.test.ts": [
    "limit-altı subs — no throttle signal → keeps task unchanged",
    "limit-altı with null state → returns no_signal",
    "limit-üstü → falls back to equivalent API model (opus → gpt-5 on codex)",
    "eşdeğer-tier seçim — haiku → gpt-5-mini (economy), sonnet → gpt-4.1 (standard)",
    "API yoksa graceful — gemini-only registry → no_equivalent",
    "already-api task — skips overflow logic (returns already_api)",
    "honors apiProvider option — overflows to gemini when configured"
   ],
   "core/provider-packages.test.ts": [
    "covers exactly claude, codex, gemini",
    "is deeply frozen (SSOT cannot be mutated at runtime)",
    "claude maps to the published @anthropic-ai/claude-code package + claude binary",
    "codex maps to the published @openai/codex package + codex binary",
    "gemini maps to the published @google/gemini-cli package + gemini binary",
    "derives installHint as `npm install -g <npmPkg>` for every provider (no separate literal)",
    "returns true for %s",
    "returns false for non-CLI-provider value %j",
    "returns the exact frozen PROVIDER_PACKAGES entry for each known id",
    "no file outside provider-packages.ts + the known-offender list hardcodes a provider-package literal"
   ],
   "core/provider-registry-config.test.ts": [
    "registers an openai-compatible provider declared in config.providers.registry",
    "accepts the `adapter` key as an alias for `type`",
    "registers a CLI-kind alias under a custom name (zero code change)",
    "preserves built-in default behavior when no registry is configured (backward-compat)",
    "treats an empty registry array as a no-op (backward-compat)",
    "skips an entry missing a name without throwing",
    "skips an entry missing type/adapter without throwing",
    "skips an openai-compatible entry missing baseUrl even with API-key auth configured",
    "skips an entry with an unknown adapter type without throwing",
    "registers valid entries and skips invalid ones in the same registry",
    "is idempotent — a name already registered is not duplicated",
    "registers an arbitrary openai-compat provider through ALL 3 sites with NO union edit",
    "config-absent path leaves the openai-compat candidate set byte-for-byte (backward-compat)",
    "returns exactly the three built-in presets when no registry is supplied",
    "merges a config-declared openai-compat provider as a candidate",
    "config precedence: a config entry replaces the built-in of the same name",
    "omits incomplete config entries and ignores non-openai-compat kinds",
    "applies a config openai-compat provider deck secret via DECKENT_<apiKeyEnv>",
    "config + built-in providers coexist with zero cross-leak",
    "no-op when the config provider has no matching deck secret",
    "CLI-kind config aliases carry no apiKeyEnv → no deck mapping",
    "omitting providerDefs leaves built-in behavior byte-for-byte"
   ],
   "core/provider-terminal-usage-evidence.test.ts": [
    "binds adapter-normalized Codex usage without repricing or double-counting reasoning",
    "rejects estimates, unsafe counters and inconsistent totals"
   ],
   "core/provider-truth-store.test.ts": [
    "uses the platform global-state path rather than a project workspace path",
    "requires a host-private key before opening or writing the store",
    "fails loudly instead of opening an unversioned incompatible truth schema",
    "persists and verifies a catalog without promoting catalog presence to live proof",
    "persists exact scoped evidence across restart and projects stale at read time",
    "does not reuse evidence across account, backend, runtime, model or tenant scope",
    "uses insertion sequence, not random evidence id, for same-timestamp latest truth",
    "rejects conflicting idempotency and enforces append-only storage",
    "detects payload tampering even if the database trigger is bypassed",
    "detects normalized query-envelope tampering even when payload and its hash are untouched",
    "detects capability catalog envelope tampering",
    "stores no prompt, response, argv, endpoint URL or credential material",
    "rejects a forged payload even when an attacker recomputes plain SHA-256",
    "fails at open for the wrong key or a tampered immutable schema",
    "rejects same-named weak indexes and inert immutable triggers at open",
    "isolates equal opaque ids and idempotency keys across projects in one host-global database",
    "stamps exact key ids and keeps retired-key evidence readable after rotation",
    "migrates exact v2 evidence only through an explicit retired-key binding",
    "blocks a stale open writer from reactivating a retired authority revision"
   ],
   "core/provider-truth.test.ts": [
    "keeps catalog, binary and enabled evidence separate from live proof",
    "admits only a successful exact provider and exact API model invocation",
    "retains MCP as its own invocation transport instead of relabeling it as API",
    "maps %s to fail-closed unavailable evidence",
    "sanitizes a thrown probe into transport-error evidence",
    "does not call a live probe without a scoped allow decision and complete budget evidence",
    "requires capability-specific proof beyond a normal inference success",
    "reachability lifetime is its ttl — admission evidence no longer clamps it (7094/7081)",
    "expires evidence to stale and never carries a live proof across scope",
    "rejects removed aliases and preserves backend/auth dimensions in the probe request",
    "accepts the generic single-segment opaque form",
    "accepts the canonical content-addressed sha256 digest form",
    "rejects a wrong algorithm, malformed digest, extra colon, slash, or URL",
    "honors the required/null contract unchanged"
   ],
   "core/provider.test.ts": [
    "includes cursor in the canonical provider-name spine",
    "should define required properties",
    "should support readonly name property",
    "should support readonly supportedModels array",
    "spawn should be callable with taskId, model, prompt, opts",
    "kill should be callable with taskId",
    "listWorkers should return an array",
    "isAvailable should return a promise resolving to boolean",
    "buildCommand should return a string",
    "starts empty with no providers",
    "registerProvider adds provider and sets first as default",
    "registerProvider throws on duplicate name",
    "registerProvider with setDefault=true overrides default",
    "getProvider returns the correct adapter",
    "getProvider throws ProviderNotFoundError for unknown name",
    "listProviders returns all registered names",
    "getDefault throws when no providers registered",
    "setDefault changes the default provider",
    "setDefault throws ProviderNotFoundError for unknown name",
    "hasProvider returns true for registered providers",
    "hasProvider returns false for unregistered providers",
    "unregisterProvider removes the provider",
    "unregisterProvider returns false for unknown provider",
    "unregisterProvider resets default when default is removed",
    "unregisterProvider sets default to null when last provider removed",
    "clear removes all providers",
    "size reflects the number of registered providers",
    "ProviderError has correct name and providerName",
    "ProviderNotFoundError has correct name and message",
    "ProviderUnavailableError with reason",
    "ProviderUnavailableError without reason",
    "is a ProviderRegistry instance",
    "returns empty object for empty secrets",
    "sets ANTHROPIC_API_KEY from DECKENT_CLAUDE_API_KEY",
    "returns claude override with only ANTHROPIC_API_KEY",
    "sets OPENAI_API_KEY from DECKENT_OPENAI_API_KEY",
    "returns codex override with only OPENAI_API_KEY",
    "sets GOOGLE_API_KEY from DECKENT_GOOGLE_API_KEY",
    "returns gemini override with only GOOGLE_API_KEY",
    ".deck key takes precedence over existing system env var",
    ".deck key takes precedence for claude provider",
    ".deck key takes precedence for gemini provider",
    "skips empty string values — does not overwrite env or add override",
    "sets all three providers when all keys present",
    "ignores unknown .deck keys — does not affect env or overrides",
    "worker receives only needed key — codex override has only OPENAI_API_KEY",
    "BootstrapResult connector field accepts Connector instance",
    "Connector registered via bootstrap mirrors registry providers",
    "Connector getAvailableProviders returns same names as registry.listProviders",
    "Connector health check does not throw for available providers",
    "Connector health check returns available=false for unavailable providers",
    "BootstrapResult providerEnvOverrides only contains provider-specific keys"
   ],
   "core/provisioner.test.ts": [
    "maps claude CLI to npm-global install",
    "maps codex CLI to npm-global install",
    "maps gemini CLI to npm-global install",
    "plans tmux via apt on Debian/Ubuntu linux (pkg manager hint)",
    "plans tmux via brew on macOS",
    "treats node as manual (never auto-installs a runtime)",
    "treats docker as manual (no safe silent auto-install)",
    "does NOT spawn when consent is false (returns skipped/no-consent)",
    "spawns npm with array args and shell:false when consent is true",
    "manual-method tools are never spawned, returned as skipped/manual",
    "reports failed (not throw) when spawn exits non-zero",
    "rejects a command outside the provisioner whitelist (defense-in-depth)",
    "no-install mode: never prompts, never installs, returns skipped",
    "yes mode: installs npm tools without prompting",
    "prompt mode: installs only consented tools, skips declined",
    "empty missing list returns empty result",
    "defaults to prompt",
    "--yes selects non-interactive defaults without installation consent",
    "--install is the explicit unattended installation authority",
    "--no-install → no-install",
    "--no-install wins over all positive flags (conservative)",
    "returns provider CLIs that are unavailable",
    "adds failed required doctor checks (tmux, node, docker) mapped to ToolId",
    "skips passing checks and dedupes claude across provider+doctor"
   ],
   "core/psl6-auth-states.test.ts": [
    "ANTHROPIC_API_KEY set → present=true, authenticated=unknown, method=api-key",
    "structured CLI status loggedIn=true → present=true, authenticated=true, method=subscription",
    "structured CLI status loggedIn=false → present=true, authenticated=false, method=none",
    "malformed CLI status → present=true, authenticated=unknown (never guessed), method=none",
    "CLI timeout → present=unknown, authenticated=unknown, method=none",
    "OPENAI_API_KEY set → present=true, authenticated=unknown, method=api-key (no spawn)",
    "\"Not logged in\" stdout (exit 0) → present=true, authenticated=false, method=none",
    "\"Logged in using ChatGPT\" stdout → present=true, authenticated=true, method=subscription",
    "spawnError (CLI missing) → present=false, authenticated=false, method=none",
    "timeout → present=unknown, authenticated=unknown, method=none, state=unknown (honest, never guessed)",
    "indeterminate stdout → present=true (CLI answered), authenticated=unknown, method=none",
    "GEMINI_API_KEY set → present=true, authenticated=unknown, method=api-key",
    "oauth_creds.json with access_token → present=true, authenticated=unknown, method=subscription",
    "no oauth file and no env key → present=false, authenticated=false, method=none",
    "malformed oauth file → present=true, authenticated=unknown (never guessed), method=none",
    "present=unknown, authenticated=unknown, method=none — honest, never guessed",
    "resolves (never rejects) even when readFileImpl throws an unexpected, non-ENOENT error",
    "resolves (never rejects) when the spawn seam itself times out mid-flight",
    "claude authenticated result narrows to the old { state, detail? } shape",
    "codex logged-out result narrows to the old { state, detail? } shape",
    "a bare pre-enrichment-style literal ({ state, detail? } only) still satisfies AuthProbeResult",
    "unsupported-provider result still matches the pre-existing detail wording"
   ],
   "core/rate-limiter.test.ts": [
    "allows requests below the limit",
    "denies requests at or above the limit",
    "allows requests again after resetLimit",
    "isolates tenants — tenant B limit is independent of tenant A",
    "checkLimitResult returns metadata on allow",
    "checkLimitResult returns metadata on deny",
    "uses default maxConcurrent (10) when no flow config provided",
    "resets bucket on window expiry"
   ],
   "core/rbac-audit.test.ts": [
    "deny → writeAuditEvent is called with access:denied",
    "allow → writeAuditEvent is NOT called",
    "tenant field is included in the audit event",
    "actor is recorded in the audit event",
    "no audit written when auditCtx is omitted (backward compat)"
   ],
   "core/rbac-hierarchy.test.ts": [
    "admin has higher level than operator",
    "operator has higher level than viewer",
    "viewer has the minimum level",
    "admin inherits flow:manage from operator",
    "admin inherits sprint:write from operator",
    "admin inherits audit:read from operator",
    "admin inherits sprint:read from viewer (transitive)",
    "operator inherits sprint:read from viewer",
    "operator can flow:manage (own permission)",
    "viewer can sprint:read",
    "viewer cannot sprint:write",
    "viewer cannot flow:manage",
    "viewer cannot audit:read",
    "viewer cannot tenant:admin",
    "superadmin role denied for sprint:read",
    "empty string role denied",
    "root role denied",
    "isValidRole rejects unknown",
    "admin matrix includes all permissions",
    "operator matrix excludes admin-only permissions",
    "viewer matrix contains only viewer-level permissions",
    "admin matrix is superset of operator matrix",
    "operator matrix is superset of viewer matrix",
    "tenant:admin is exclusive to admin role"
   ],
   "core/rbac-runtime-enforce.test.ts": [
    "admin SPRINT_WRITE allowed when rbac enabled",
    "operator EXECUTE allowed when rbac enabled",
    "viewer SPRINT_WRITE denied when rbac enabled",
    "viewer EXECUTE denied when rbac enabled",
    "returns true when rbac disabled regardless of role",
    "returns true when rbac disabled with invalid role",
    "returns true when rbacConfig not provided (NO_OP bypass)",
    "enabled=true with invalid tenantId returns false",
    "enabled=false with invalid tenantId still NO_OP returns true"
   ],
   "core/rbac.test.ts": [
    "admin can READ",
    "admin can WRITE",
    "admin can EXECUTE",
    "admin can ADMIN",
    "admin can AUDIT",
    "viewer can READ",
    "viewer cannot WRITE",
    "viewer cannot EXECUTE",
    "viewer cannot ADMIN",
    "viewer cannot AUDIT",
    "operator can READ",
    "operator can WRITE",
    "operator can EXECUTE",
    "operator cannot ADMIN",
    "operator cannot AUDIT",
    "rejects path-traversal tenantId",
    "rejects empty tenantId",
    "rejects uppercase tenantId",
    "accepts valid lowercase tenantId",
    "superadmin role is denied",
    "root role is denied",
    "empty string role is denied",
    "recognizes admin",
    "recognizes operator",
    "recognizes viewer",
    "rejects unknown role",
    "rejects empty string"
   ],
   "core/reachability-probe-budget.test.ts": [
    "keeps USD absent for %s billing from tmpdir-authored config",
    "returns typed unavailable outcomes for absent profile and missing metered USD",
    "uses owner USD only for metered API and admits the derived projection without putting USD on ExecutionBudget",
    "lets provider truth validate subscription projection without requiring USD"
   ],
   "core/readjson-migration.test.ts": [
    "returns parsed JSON for valid file",
    "returns null for missing file",
    "returns null for invalid JSON",
    "returns parsed JSON for valid file",
    "returns null for missing file",
    "returns null for invalid JSON",
    "readGlobalConfig returns null when file does not exist",
    "source file imports readJsonSafe",
    "source file imports readJsonSafe",
    "SkillRegistry reads data correctly",
    "SkillRegistry returns empty on invalid file",
    "source file imports readJsonSafe",
    "loadPlugin throws PluginError on invalid manifest",
    "loadPlugin works with valid manifest",
    "enablePlugin returns false on invalid manifest",
    "disablePlugin returns false on invalid manifest",
    "source file imports readJsonSafe",
    "detectProjectStack works without package.json",
    "detectProjectStack reads valid package.json",
    "detectProjectStack handles invalid cache gracefully",
    "source file imports readJsonSafe",
    "AgentPoolManager loads valid agents",
    "AgentPoolManager skips invalid agent files",
    "source file imports readJsonSafe",
    "CredentialManager.getCredential returns null for missing file",
    "CredentialManager.getCredential returns key for valid file",
    "CredentialManager.getCredential returns null for invalid JSON",
    "CredentialManager.getCredentialEntry returns entry for valid file",
    "CredentialManager.listCredentials reads providers",
    "source file imports readJsonSafe",
    "SkillPoolManager loads valid skills",
    "SkillPoolManager skips invalid manifest files",
    "source file imports readJsonSafeAsync",
    "saveSubscriptionToConfig creates config with subscription",
    "saveSubscriptionToConfig merges with existing config",
    "source file imports readJsonSafeAsync",
    "loadConfig returns defaults when no config files exist",
    "loadConfig reads project config",
    "loadConfig handles malformed config gracefully",
    "source file delegates to detectProjectStack (no inline JSON.parse)",
    "analyzeProject works without package.json",
    "analyzeProject detects TypeScript project",
    "source file imports readJsonSafe",
    "source file imports readJsonSafe",
    "getNextSprintId uses readJsonSafe internally",
    "getNextSprintId reads config correctly",
    "getNextSprintId handles malformed config",
    "updateLastSprintId uses readJsonSafe",
    "${file} has no inline JSON.parse(readFileSync(...))"
   ],
   "core/readonly-argv-realshell.test.ts": [
    "%s writes a file with real bash+sort and is NOT read-only",
    "controls: a genuine read writes nothing and stays read-only; argv -- ends options",
    "sensitive on linux, insensitive on win32/darwin and for PowerShell",
    "protected trees match case-insensitively where the filesystem does",
    "a symlink inside the project pointing outside, a symlinked parent dir, and a missing path all fail closed; real files pass",
    "stays lexical when the root is not on disk (pure unit mode) or when resolveSymlinks is off"
   ],
   "core/readonly-boundaries.test.ts": [
    "refuses PowerShell output",
    "quoted/escaped argv still contains mutating option: %s",
    "PowerShell protected path comparison is case-insensitive",
    "symlink inside project must not silently read outside project",
    "POSIX sort really writes through a quoted flag (only disposable fixture)"
   ],
   "core/readonly-canonical-fixtures.test.ts": [
    "B3a: an in-root symlink to a protected file is PATH_PROTECTED (direct and chained)",
    "intermediate directory symlink escaping the root fails closed",
    "B3b: a glob whose match is a symlink to an outside file fails closed; a clean glob passes",
    "sh dotfile rule: `*` never reaches .env, `.*` does; `**` is `*`",
    "zero matches, over-bound expansion and brace expansion all fail closed",
    "re-walks parent components introduced by a symlink target (direct and nested dirlinks)",
    "POSIX bracket expressions: negation, ranges, classes, leading ], unsupported forms"
   ],
   "core/readonly-canonical-path.test.ts": [
    "cannot silently read a protected file through an innocuous in-root symlink",
    "glob expansion cannot silently follow an outside-root symlink"
   ],
   "core/readonly-canonical-target-parent.test.ts": [
    "walks parent components introduced by a symlink target",
    "POSIX bracket negation cannot hide an outside-root match"
   ],
   "core/reasoning-control.test.ts": [
    "accepts the three authored shapes and stamps configured provenance",
    "rejects %s with a typed error naming the path",
    "predicates: known vs unknown, toggleable vs not",
    "a template that reads enable_thinking → template toggle, budget shared, server-reported; probes the model-scoped props at the SERVER ROOT",
    "router returns nothing model-scoped → falls back to the bare /props; a template without the switch is toggle none",
    "no template but a llama.cpp witness → toggle unknown, budget shared",
    "unreachable, non-JSON or shapeless server → null (caller keeps unknown, uncached)",
    "registers the honest unknown descriptor when no facts are supplied, and upgrades in place when evidence arrives",
    "configured wins over everything; a known registry entry wins over the probe",
    "probe is memoized on success and retried after a null/failed probe",
    "no probe and nothing known → unknown",
    "defaults: auto mode with explicit budgets, one transport retry, bounded backoff",
    "merges authored reasoning fields independently over the defaults",
    "rejects %s loudly"
   ],
   "core/reasoning-effort.test.ts": [
    "claude: accepts low/medium/high/xhigh/max (claude --effort values)",
    "codex: accepts minimal/low/medium/high (codex model_reasoning_effort)",
    "opt-in only: no requested level → undefined (no flag emitted, CLI default kept)",
    "unsupported provider (gemini/ollama) → always undefined",
    "unrecognized level → undefined (ignored, not a broken flag)",
    "lists levels per provider; empty for unsupported",
    "REASONING_EFFORT_BY_PROVIDER has no gemini/ollama key (unsupported = absent)"
   ],
   "core/recent-work-retention.test.ts": [
    "retires an exact canonical phase5 duplicate only after verified manifest proof",
    "HOLDs a phase5 file before publication and never confuses sprint-610 with sprint-611",
    "archives sprint-479 recovery-not-dispatched evidence before retiring the live bytes",
    "preserves conflicting recovery bytes as a manifested variant",
    "HOLDs nested and unknown content and performs no directory-wide removal",
    "keeps changed source bytes when applying a stale plan"
   ],
   "core/recovery-artifact-policy.test.ts": [
    "uses an exhaustive versioned semantic class vocabulary",
    "rejects the Sprint-595 checkpoint-drag class without explicit supersession",
    "moves a checkpoint only when a distinct same-task successor explicitly supersedes its digest",
    "archives %s with owner-bound restore semantics and no invented successor",
    "keeps archived %s as evidence rather than a resumable artifact",
    "fails closed when identity is incomplete or source equals destination"
   ],
   "core/recovery-resume-outcome.test.ts": [
    "maps %s authority to %s with exit %s",
    "fails closed when returned status has no matching durable authority",
    "atomically round-trips only an owned runtime artifact"
   ],
   "core/redact-prefix.test.ts": [
    "redacts a short sk-ant- fixture key that the {20,} rule misses",
    "redacts a very short single-char sk-ant- key",
    "redacts a long sk-ant- key (regression — already covered by general rule)",
    "redacts sk-ant- as an assignment value",
    "does NOT redact an embedded sk-ant substring inside another word",
    "discriminates: a hyphen before sk IS a boundary, so it redacts",
    "general sk- rule unchanged: long non-ant sk- key still redacts",
    "general sk- rule unchanged: short sk- non-ant string stays intact",
    "other patterns unchanged: Bearer token still redacts",
    "other patterns unchanged: URL password still redacts"
   ],
   "core/redact-sensitive-coverage.test.ts": [
    "redacts an AWS access key ID",
    "does not redact a string that merely starts with AKIA but is the wrong length",
    "redacts a classic ghp_ GitHub token",
    "redacts a classic gho_ GitHub OAuth token",
    "redacts a fine-grained github_pat_ token",
    "redacts a real-shaped standalone JWT",
    "redacts a generic password= assignment",
    "redacts a generic passwd= assignment",
    "redacts a generic token= assignment (lowercase, not an ALL-CAPS env var)",
    "redacts a generic secret= assignment",
    "redacts generic assignments case-insensitively",
    "leaves prose containing \"password\" without \"=\" unchanged",
    "leaves prose containing \"token\" without \"=\" unchanged",
    "leaves prose containing \"secret\" without \"=\" unchanged",
    "leaves ordinary AWS/GitHub-adjacent prose unchanged",
    "redacts multiple new-family secrets in one string while preserving surrounding text",
    "still redacts pre-existing sk- keys and ENV_VAR= assignments unaffected by new patterns"
   ],
   "core/redact-sensitive.test.ts": [
    "redacts OpenAI-style sk- API keys",
    "redacts Anthropic-style sk-ant- API keys",
    "redacts key- prefixed API keys",
    "does not redact short sk- strings that are not API keys",
    "redacts Bearer tokens",
    "redacts bearer tokens case-insensitively",
    "redacts passwords in URLs",
    "redacts passwords in URLs with special characters",
    "redacts OPENAI_API_KEY=value",
    "redacts ANTHROPIC_API_KEY=value",
    "redacts SECRET_KEY=value",
    "redacts ACCESS_TOKEN=value",
    "returns empty string for empty input",
    "leaves non-sensitive text unchanged",
    "redacts multiple sensitive values in one string",
    "preserves surrounding text structure",
    "does not redact normal words that happen to contain \"key\"",
    "regression: function is a pure utility with no side effects",
    "regression: output.ts re-export produces same result as core import"
   ],
   "core/result-cost.test.ts": [
    "prices Opus input tokens with correct units (1M in → $5)",
    "prices Opus output tokens (1M out → $25)",
    "sums input + output + cache_read + cache_creation precisely",
    "treats a null cache-creation cost as $0 (no throw — gpt-5)",
    "omitted cache fields contribute $0",
    "prices an OpenAI model regardless of the deckent provider arg",
    "prices a Gemini model via alias",
    "resolves pricing by alias even when provider name differs from config key",
    "returns $0 + isLocal for an ollama-provider run, even with non-zero tokens",
    "detects local via config default_billing_mode even for an unlisted provider arg",
    "detects local via the provider-name fallback when the model is not catalogued",
    "reports an unknown model honestly (never silently priced)",
    "clamps negative / NaN token counts to $0 (no negative cost)",
    "emits a contract-shaped ResultCost (usd / currency / pricingSource / isLocal)",
    "sums per-task actual cost to a sprint total (Σ matches manual)"
   ],
   "core/role-invocation-resolver.test.ts": [
    "selects the configured primary when it is known-reachable and unlimited",
    "never throws when a reachable candidate exists (no loud failure)",
    "falls back to the next provider when the primary is known-unreachable",
    "falls back with reason fallback_limit_hold when the primary is at its limit",
    "remaps economy-tier model on fallback (haiku → gpt-5.6-luna)",
    "rejects a candidate with reachability state=%s even when reachable:true (degenerate trap)",
    "rejects a candidate with limit state=%s even when limited:false (unknown-limit != available)",
    "treats a provider absent from the evidence map as fully-unknown (rejected)",
    "the raw gate helpers reject non-known states with a positive signal",
    "returns selected:null + fallback_exhausted when no candidate is reachable",
    "rejects a fallback with no equivalent model as validation_failed (model check precedes evidence)",
    "the configured primary keeps its model verbatim (no equivalence remap)",
    "picks the FIRST reachable candidate in the given order, not any other",
    "resolves role=%s with the canonical purpose and its own policy",
    "gives the auditor a first-class, strict-by-default policy surface",
    "attended: tolerates an unknown LIMIT but still rejects an unknown REACHABILITY",
    "exposes configured/resolved selections, fallbackChain, reachability + limits",
    "type-checks as a request literal without any casts"
   ],
   "core/routing/agent-pool-capabilities.test.ts": [
    "parses and attaches a valid capabilities block onto the loaded AgentDefinition",
    "drops invalid capabilities but still loads the agent, recorded as a WARNING (visible-skip, not a full reject)",
    "drops a nested legacy model alias instead of routing it",
    "leaves an agent with no capabilities field entirely unaffected (backward-compat / additive guarantee)",
    "validates capabilities for temp agents (.tasks/agents/) too — same _loadFromDir path"
   ],
   "core/routing/capability-schema.test.ts": [
    "round-trips a full valid example",
    "accepts a valid example without the optional preferredModel",
    "accepts a positive integer maxParallel (not only null)",
    "requires capabilitiesVersion to be exactly 3",
    "rejects a stats key inside capabilities (outcome-stats NEVER in manifest)",
    "rejects a stats key nested under numerical too (strict at every level)",
    "rejects an unknown workType",
    "rejects a \"test\" workType entry (NO_GO gate — no test work-type exists)",
    "rejects a proficiency typo on content.workTypes",
    "rejects a proficiency typo on positional.domains",
    "accepts every valid proficiency value on both axes",
    "rejects an unknown deliverable type",
    "accepts every closed-core work-type and rejects \"test\" via isWorkType reuse",
    "rejects an unrecognized top-level key (strict on the outer object)",
    "rejects a negative or zero maxParallel",
    "rejects an empty role string",
    "returns ok:true with the parsed value on a valid input",
    "returns ok:false with a non-empty typed issues list on an invalid input, never throws",
    "never throws even on a wildly malformed input (null, array, primitive)",
    "is true when positional.writeAuthority is true",
    "is false when positional.writeAuthority is false"
   ],
   "core/routing/ci-guardian-eligibility.test.ts": [
    "survives stage 1 and appears in ranked results for test/quality fix and build work",
    "lets a cold eligible ci-guardian win through a nonzero exploration bonus",
    "keeps review primary while synchronizing both manifest copies byte-for-byte"
   ],
   "core/routing/completion-envelope-unwrap.test.ts": [
    "content-batch: raw envelope parses to ZERO entries (the shipped bug); unwrapped parses fully",
    "tie-judge: raw envelope yields null (the shipped bug — judge never fires); unwrapped yields the verdict",
    "unwrap is a no-op for a bare (non-enveloped) answer — codex/gemini direct output still works"
   ],
   "core/routing/config.test.ts": [
    "does NOT carry `enabled` in defaults — vestigial no-op post-S3 cut-over (V3 is unconditional)",
    "default weights sum to 1.0 (content 0.5, positional 0.3, numerical 0.2)",
    "passes its own schema + weights-sum validation",
    "defaults explorationBonus to 0 (OFF)",
    "accepts the default config",
    "rejects an unknown extra field (strict schema)",
    "rejects governanceMode outside the closed enum",
    "rejects a non-positive topK",
    "rejects confidenceFloor outside [0,1]",
    "accepts explorationBonus inside [0,1]",
    "rejects explorationBonus outside [0,1]: %s",
    "throws RoutingV3WeightsSumError when weights sum to less than 1.0",
    "throws RoutingV3WeightsSumError when weights sum to more than 1.0",
    "RoutingV3WeightsSumError is a DeckentError with the offending sum + weights",
    "accepts a valid non-default weight distribution that sums to exactly 1.0",
    "throws InvalidRoutingV3ConfigError (not the weights error) for a malformed shape",
    "returns the default config when neither global nor project override anything",
    "returns the default config when both args are omitted",
    "global config overrides defaults",
    "project config overrides global config, which overrides defaults",
    "project can override a global override back toward a default-adjacent value",
    "propagates RoutingV3WeightsSumError when the final merged weights are invalid"
   ],
   "core/routing/content-llm-slice2.test.ts": [
    "prompt carries the closed vocabulary, structural evidence and the anti-keyword rule",
    "parse: valid entries in, junk/unknown/invalid-workType visibly dropped",
    "completion failure → per-task structural fallback, provenance honest",
    "LLM claim contradicting structure is caught by the verifier cross-check",
    "refactor-worded → refactorer (V2's non-monotonic headline case)",
    "security-harden → write-capable FIX lane with security-domain evidence (auditor reviews, never builds)",
    "test-authoring task → a WRITER via build work-type (never devops)",
    "445-016 manifest-authoring → build lane despite audit-family words"
   ],
   "core/routing/corpus-harness.test.ts": [
    "real catalog loads with 19+ capability-carrying agents",
    "natural-experiment class: identical work routes IDENTICALLY regardless of agent-name-in-title",
    "${c.id} (${c.source})",
    "pending ai-stage cases are explicit and pinned (${EXPECTED_PENDING_COUNT})"
   ],
   "core/routing/engine-slice1.test.ts": [
    "V2 parity: construction work never reaches a Write-denied agent",
    "work-type never eliminates (refactorer out of build work)",
    "role contradiction: implementer persona out of review work unless explicitly granted",
    "deliverable mismatch is SOFT (positional), never a hard elimination (Slice-2 amendment)",
    "proficiency table drives the score; undeclared/never → 0",
    "subtype rolls up to parent",
    "explicit domain owner beats wildcard",
    "unknown capability domain surfaces a typed issue, never silent-zero",
    "cold-start: missing cells are NEUTRAL, never a penalty",
    "live signals absent → neutral, present → consumed",
    "legacy (no `active`) = 3-component neutral-filled mean; gated = pure tier signal",
    "a warm cell keeps the cells component in the mean — real failures now WEIGH (no neutral dilution)",
    "config weights decide the order (override flips it)",
    "tie and low-confidence produce typed indecision",
    "deterministic total order: identical runs → identical arrays",
    "calibrated confidence is monotonic in gap and in top score",
    "optional bonus absence preserves the legacy weighted result bit-for-bit",
    "bonus blend never raises a score above one",
    "bonus ranking is deterministic for identical inputs",
    "preserves the tie-break chain when blended scores tie",
    "content↔structural conflict caught both directions",
    "force bypasses ranking, never authority: violation → forceWarning",
    "defense-in-depth: an unfiltered candidate is still caught",
    "anti-temp: temp wins only when genuinely alone",
    "security-domain → only reviewer role (violation carries policy id)",
    "high-risk config → escalate + minConfidence honored",
    "absent file = clean empty pack; malformed layer skipped visibly",
    "build requirement → builder wins; decision frozen; story carries WHY",
    "explicit refactor requirement → refactorer (via requirement override)",
    "i18n-domain requirement → domain owner beats generalist",
    "word-inference ban holds through the FULL pipeline (agent-name + test token)",
    "catalog gap → typed CatalogGapError with actionable payload",
    "tie → escalation to Brain (decision-5)",
    "temp agent loses a near-tie to a builtin (anti-temp, vectorial)",
    "skills: honest-empty below fit floor; persona slice via alias table",
    "skills: generic build:primary with NO domain overlap is honest-empty (7094-F1c)",
    "skills: derived test authority is selected for a core plus tests task",
    "skills: an explicit `*` domain declaration still attaches (owner-authored wildcard, 7094-F1c)",
    "append/read round-trip; corrupted line skipped visibly; replay equality + drift",
    "keeps legacy v1 journals readable while making their missing skill replay proof explicit",
    "escalated trace summarizes the escalation; all lines ≤80 chars"
   ],
   "core/routing/exploration-bonus.test.ts": [
    "keeps routeTaskV3 output bit-identical for explicit zero and an absent config field",
    "lets a cold eligible agent pass a reachable incumbent while a warm agent gets no bonus",
    "bounds extreme blends at one and persists a journal entry that parses cleanly",
    "keeps agent-lint output bit-identical regardless of bonus configuration",
    "reports a tie when exploration narrows the winner-runner-up gap below epsilon"
   ],
   "core/routing/foundation-roundtrip.test.ts": [
    "merges builtin + org-overlay + project layers into one registry",
    "resolves routing_v3.structuralConfidence from a project-layer override (445-010 config plumbing)",
    "carries explorationBonus through default, global, and project layers",
    "the word \"Refactor\" in title/description does not change the vector; workType stays \"build\", never \"refactor\"",
    "migrates a v2 fixture manifest into a schema-valid, provisional v3 capability vector",
    "the token 'test' anywhere in title/description does not alter positional, content, or numerical axes",
    "an agent display-name (\"implementer\"/\"reviewer\") in title/description does not alter positional, content, or numerical axes"
   ],
   "core/routing/ghost-rejection.test.ts": [
    "ghost outcome rejected + counted; cells untouched",
    "non-finite quality rejected visibly; finite out-of-range clamped",
    "non-ghost outcomes record normally alongside a rejected sibling (store byte-consistent)"
   ],
   "core/routing/learning-cells.test.ts": [
    "returns an empty, schema-valid, deep-frozen snapshot when no ledger file exists yet",
    "degrades a corrupt/malformed existing file to an empty valid snapshot rather than throwing",
    "degrades a well-formed-JSON-but-wrong-shape file (cells as an array) to an empty valid snapshot",
    "persistently normalizes and merges legacy core-runtime cell keys on load",
    "creates the ledger file on disk at the exact relative path and readCellsSnapshot reflects it",
    "accumulates uses/successes/qualitySum across TWO DIFFERENT tasks that share the same cell identity",
    "a NO_GO verdict increments uses and qualitySum but NOT successes",
    "skips infra NO_GO learning, increments visibility, and keeps unknown on the penalty path",
    "two DISTINCT work-type/domain/agent shapes each get their OWN independent cell (K4 pin — no cross-task DNA sharing)",
    "throws InvalidWorkTypeError for a work-type outside the 8 closed-core types, without writing anything",
    "a second call with the SAME (taskId, sprintId) is a no-op: recorded=false, no double count",
    "the SAME taskId in a DIFFERENT sprint is treated as a distinct (taskId, sprintId) pair and records again",
    "the bounded recentKeys ring never grows past RECENT_KEYS_RING_CAP",
    "leaves no leftover *.tmp file behind in .deckent/stats/ after a successful write"
   ],
   "core/routing/requirement-content-structural.test.ts": [
    "%s",
    "code-src plurality (0.6) over config (0.4) → build — plurality drives, not \"any config present\"",
    "zero filesWrite + read-heavy scope (≥1 filesRead) → analyze",
    "zero filesWrite + zero filesRead → build (no structural signal, even with directories)",
    "provenance is always \"structural\"; summary/semanticTags/subtype are null (valid modeled state)",
    "calibratedConfidence defaults to DEFAULT_ROUTING_V3_CONFIG.structuralConfidence (from config, not a magic number)",
    "an injected structuralConfidence flows through — proving the value is config-sourced, not hardcoded",
    "adding 'test' anywhere in title/description does NOT alter the content vector",
    "adding an agent display-name (\"implementer\"/\"reviewer\") to title/description does NOT alter the content vector",
    "output round-trips through requirementContentSchema (null summary/semanticTags accepted)",
    "is a pure function of its arguments (same input → same output, called twice)",
    "the live 450-006 shape (proof .md + 4/7 test directories) → review",
    "doc writer over docs-only reads stays document (share 0)",
    "below the 0.5 share threshold stays document; at exactly 0.5 flips to review",
    "test FILES in filesRead count as test locations (no directories needed)",
    "e2e/ and __tests__/ segments are test locations",
    "code-test dominant deliverable is untouched by the rule (stays build)",
    "prose ban still holds: the token 'test'/'verify' in title cannot flip a docs-only task"
   ],
   "core/routing/requirement-numerical.test.ts": [
    "fileCount=%i moduleCount=%i → estimatedSize=%s",
    "moduleCount dedupes the top-level module across src/tests/lib prefixes",
    "fileCount/moduleCount are 0 for a fully empty scope",
    "defaults to \"normal\" when task.effort is absent at runtime",
    "a config-deliverable write (e.g. a .yaml file) yields riskClass \"high\"",
    "a migration-deliverable write (e.g. a .sql file) yields riskClass \"high\"",
    "a plain code-src write at trivial size yields riskClass \"low\" (no risky deliverable)",
    "matching security pathPattern → riskClass \"high\"",
    "renaming the security pathPattern so it no longer matches → riskClass drops out of \"high\"",
    "trivial/small size with no risky write → riskClass \"low\"",
    "medium+ size with no risky write → riskClass \"medium\"",
    "can be called with just `task` (vocabulary defaults to BUILTIN_DOMAINS)",
    "is a pure function of its arguments (same input → same output, called twice)",
    "output round-trips through requirementNumericalSchema"
   ],
   "core/routing/requirement-positional.test.ts": [
    "a task writing tests/ only → deliverables 100% code-test",
    "a mixed src+tests task → deliverables and domain weights split 50/50",
    "a docs task → deliverables 100% doc, single \"docs\" domain match",
    "a .github/workflows task → deliverables 100% workflow, \"devops/ci\" domain match",
    "needsWrite is false and deliverables empty when filesWrite is empty",
    "a directory-only match (no filesWrite yet) still surfaces the domain, at weight 0",
    "fully empty scope yields empty domains/deliverables/surfaces and needsWrite=false",
    "an English task: adding \"implementer\" to title+description does not change positional output",
    "a Turkish task: adding \"implementer\" to title+description does not change positional output",
    "output round-trips through requirementPositionalSchema",
    "is a pure function of its arguments (same input → same output, called twice)"
   ],
   "core/routing/skill-applicability-p0.test.ts": [
    "derives language ownership only from structural stack metadata, never prose/triggers",
    "holds an unknown language category instead of minting wildcard authority",
    "rejects Python for a TypeScript task even when the repository root contains Python markers",
    "uses explicit task files as language authority inside a mixed-language monorepo directory",
    "admits Python from a new task-local .py write path without requiring an existing file",
    "normalizes Windows/WSL separators byte-identically for task-local evidence",
    "rejects POSIX, Windows-drive, and UNC absolute scope paths on every host",
    "enforces Linux, macOS, Windows-native, and WSL platform predicates exactly",
    "enforces authored required, forbidden, platform, tenant, and policy evidence together"
   ],
   "core/routing/skill-attribution-p0.test.ts": [
    "records selection/delivery as exposure but grants zero efficacy credit without causal evidence",
    "canonicalizes the historical bare settlement digest at the producer boundary",
    "credits only host-validated applied skills that are a subset of delivered skills",
    "holds contradictory applied evidence instead of widening delivery authority",
    "publishes one content-addressed batch, replays identical bytes, and rejects conflict",
    "rejects a receipt whose inner digest was not recomputed even when the batch is present",
    "classifies duplicate logical receipts in one authored batch as input integrity, not replay conflict",
    "accepts an integrity-valid historical bare batch and migrates only an equivalent replay",
    "preserves a compatible historical base and publishes one receipt-keyed terminal successor",
    "rejects unrelated predecessor semantics and a sibling terminal receipt without changing the base",
    "never replaces the first receipt-keyed writer with different attribution bytes",
    "does not claim an EEXIST replay when directory durability cannot be confirmed",
    "fails before publication when the platform cannot enforce no-follow opens",
    "rejects a final-path symlink instead of following bytes through it"
   ],
   "core/routing/skill-profile-schema.test.ts": [
    "round-trips a full valid example",
    "accepts a valid example without the optional tokenCost",
    "rejects a negative tokenCost",
    "requires profileVersion to be exactly 3",
    "rejects writeAuthority on a skill profile (skills are knowledge, not actors)",
    "rejects role on a skill profile (skills are knowledge, not actors)",
    "rejects a stats key (outcome-stats NEVER in manifest, same rule as agents)",
    "rejects an unrecognized top-level key (strict on the outer object)",
    "rejects an unknown workType (reuses the same closed-core vocabulary as agents)",
    "rejects a \"test\" workType entry (same NO_GO gate as agents — no test work-type exists)",
    "rejects a proficiency typo on workTypes",
    "rejects a proficiency typo on domains",
    "accepts every valid proficiency value on both axes",
    "rejects an unknown deliverable type (reuses the same closed set as agents)",
    "returns ok:true with the parsed value on a valid input",
    "returns ok:false with a non-empty typed issues list on an invalid input, never throws",
    "never throws even on a wildly malformed input (null, array, primitive)",
    "returns the identical key shape for a CapabilityVector and a SkillProfile input",
    "projects an agent CapabilityVector content/positional axes into the flat shape",
    "projects a SkillProfile flat fields straight through unchanged",
    "parses a skill through capabilityVectorSchema-adjacent entry shapes (same vocabulary reuse)"
   ],
   "core/routing/skill-profiles-builtin.test.ts": [
    "enumerates every real builtin skill directory (sanity: sweep is not scanning an empty/wrong tree)",
    "every authored profile key corresponds to a real, on-disk skill directory (no fabricated entries)",
    "GHOST_SKILLS is empty over the current real tree (every builtin skill ships real content today)",
    "no authored profile is empty-competence (non-fabrication requires real signal, not a stub)",
    "is either profiled (non-ghost) or listed as a ghost — never both, never neither",
    "its authored profile (if any) validates via validateSkillProfile",
    "a skill with a MISSING SKILL.md is classified a ghost",
    "a skill with an EMPTY (whitespace-only) SKILL.md is classified a ghost",
    "a skill with real SKILL.md content is NOT classified a ghost (control case)",
    "the born-592 api-design ghost shape (manifest present with fake stats, SKILL.md missing) is still a ghost",
    "a skill with NO manifest.json at all still resolves the default SKILL.md entrypoint and classifies correctly"
   ],
   "core/routing/skill-selection-control-plane-p0.test.ts": [
    "keeps a 1,000-skill catalog deterministic under input reordering and concurrent callers",
    "selects a variable 0..N composition by marginal coverage, not a fixed top-three slice",
    "records every inapplicable candidate as a typed rejection and returns honest-empty",
    "never lets forceSkills bypass hard applicability",
    "stops composition at the prompt-token budget with a typed candidate reason"
   ],
   "core/routing/slice0-smoke.test.ts": [
    "stage 1: sync-migrates the fixture V2 manifest into a schema-valid, provisional V3 capabilities block",
    "stage 2: agent-pool loads the migrated capabilities onto the AgentDefinition, identical to what sync wrote to disk",
    "stage 3: produces schema-valid RequirementVectors for representative task shapes against the fixture project vocabulary",
    "stage 4: doctor vocabulary section reports zero project-introduced issues for this vocabulary-layer-free fixture",
    "stage 5: end-to-end coherence — every domain id the migrated capabilities reference resolves in the SAME vocabulary the doctor validated"
   ],
   "core/routing/tie-judge.test.ts": [
    "judge picks the runner-up: winner flips, provenance=ai, tie escalation preserved",
    "judge confirms deterministic top-1: winner stays, provenance=ai",
    "fail-open: judge throws → deterministic winner + provenance=deterministic",
    "fail-open: out-of-set / null verdicts keep the deterministic winner",
    "no tie → judge is NEVER called (real catalog, clear winner)",
    "governanceMode 'deterministic' silences the judge even on a real tie",
    "judge is NOT called; deterministic top-1 stays; tie escalation stays journaled",
    "the same tie WITH a domain signal still consults the judge (informed arm intact)",
    "a single code-src deliverable alone is NOT a discriminating signal (tautology rule)",
    "prompt names every tied candidate and demands JSON-only",
    "parse: valid pick passes; garbage / out-of-set / non-JSON → null",
    "makeCompleteTieJudge: complete-fn failure → null (never throws)"
   ],
   "core/routing/vocabulary-bootstrap.test.ts": [
    "returns empty candidates and skipped when there is no src/ directory at all",
    "proposes a candidate for a substantial, uncovered top-level src/ subdirectory (src/nervous -> nervous)",
    "skips an empty (non-substantial) subdirectory and records why",
    "skips a subdirectory already represented by a builtin domain id",
    "skips a subdirectory already represented by a builtin domain ALIAS (not just id)",
    "attaches matching stack dependencies as stackMarkers evidence on the candidate",
    "does not attach unrelated dependencies as stackMarkers",
    "proposes multiple independent candidates for multiple uncovered substantial subdirectories",
    "(c) creates the file when it does not exist yet",
    "(a) safely regenerates when the existing file is unedited since the last bootstrap write",
    "(b) refuses to overwrite a file that was locally edited by a user, and reports why",
    "(b) refuses to overwrite a pre-existing file that bootstrap never generated (no baseline recorded)",
    "is idempotent (a repeat write with identical defs is a no-op \"updated\" status)"
   ],
   "core/routing/vocabulary-builtin.test.ts": [
    "defines exactly the 8 closed-core work-types in spec order",
    "every work-type carries a non-empty contract, dodSignature and examples",
    "has unique work-type ids (no duplicate entries)",
    "never exposes a \"test\" work-type (Alperen decision — identifier check, not prose)",
    "getWorkTypeDef resolves a known type and rejects an unknown one",
    "defines exactly the 9 closed deliverable types",
    "isDeliverableType accepts members and rejects non-members",
    "accepts every closed-core work-type",
    "rejects unknown / empty tokens",
    "parses parent:subtype into rollup parent + free-text subtype",
    "treats a bare parent as the rollup form (subtype null)",
    "trims surrounding whitespace on both segments",
    "keeps additional colons inside the free-text subtype (splits on first)",
    "throws InvalidWorkTypeError when the parent is unknown",
    "throws InvalidWorkTypeError for a \"test\" parent (no test work-type)",
    "throws InvalidWorkTypeError on empty input",
    "throws InvalidSubtypeError when a colon is present but the subtype is empty",
    "throws InvalidWorkTypeError when the parent segment is empty (\":subtype\")",
    "InvalidWorkTypeError extends DeckentError and carries the offending value",
    "InvalidSubtypeError extends DeckentError and carries the offending value"
   ],
   "core/routing/vocabulary-doctor.test.ts": [
    "passes through loadVocabulary mergeReport.shadowed when a project domain overrides a builtin id",
    "reports zero shadowed domains when no project id collides with a builtin id",
    "flags a pattern matching zero real files, and does not flag one that matches a real file",
    "prunes node_modules from the walk — a pattern matching only a file under node_modules is still dead",
    "flags an alias shared by two domains, and does not flag a domain-unique alias",
    "flags an empty/whitespace-only description, and does not flag a domain with a real description",
    "renders a clean report with a PASS header and a PASS clean line (en)",
    "renders a clean report in Turkish",
    "renders every issue category as a WARN block, in both languages, with no leftover key placeholders"
   ],
   "core/routing/vocabulary-domains.test.ts": [
    "defines the builtin-base domains in spec order",
    "has unique domain ids (no duplicate entries)",
    "every domain carries a fully-populated DomainDef shape",
    "every domain has at least one pathPattern that matches a real path in this repo",
    "domain table lives only in vocabulary-builtin.ts (single export surface)",
    "resolves a known domain id and rejects an unknown one",
    "isDomainId accepts every registered domain and rejects unknown/empty tokens",
    "classifies $path as $expected",
    "covers all 9 closed DeliverableType members across the table above",
    "is a pure function of its path argument (same input → same output)",
    "normalizes backslashes and a leading \"./\" before classifying",
    "declares NO surfaces of its own",
    "cli/terminal still carries the cli surface (the location domain supplies it)",
    "no a11y pathPattern matches the accessibility-auditor agent directory"
   ],
   "core/routing/vocabulary-loader.test.ts": [
    "returns exactly the builtin domains when no project file and no org-overlay opt exist",
    "is absent-tolerant when opts.orgOverlayPath points at a nonexistent file",
    "project layer wins over builtin on duplicate id and reports the shadow",
    "resolves the full builtin -> org-overlay -> project shadow chain for the same id",
    "reports an intra-layer duplicate id as shadowed too (last entry in the file wins)",
    "skips a project layer with invalid JSON, reports it, and keeps builtin-only domains",
    "rejects an unknown field via zod strict mode and reports the layer as invalid",
    "rejects a domain entry missing the required id field",
    "a malformed org-overlay does not prevent a valid project layer from applying",
    "deep-freezes the registry, its domains array/entries, and the merge report",
    "does not mutate the shared BUILTIN_DOMAINS module singleton as a side effect",
    "reads the project layer from the documented relative path"
   ],
   "core/routing-engine.regression.test.ts": [
    "substring-collision immunity: Turkish \"icindeki\" (contains \"ci\") / \"hex\" (contains \"cd\") never ",
    "literal word \"test\" in the title never triggers an unowned/devops misroute — lands on a ",
    "score-tie is escalated to Brain, never silently arbitrated (misroute-corpus #15, 437-001 EXPIRE-SWEEP tie)",
    "agent-name-in-description delta invariance over the real catalog (443 natural-experiment, before/after framing)",
    "modelPreference mirrors the WINNING agent's own declared preferredModel — never invented",
    "modelPreference is null (honest-null) when the winning agent declares none",
    "every builtin agent declares a preferredModel that resolves via the LIVE model registry (ADR-G-036 — no frozen list)",
    "effortClass passthrough: task.effort round-trips through the numerical axis, defaulting to normal",
    "effortForWorkType: closed work-type table maps to the documented tiers; subtype rolls up to parent",
    "persistent completion failure: exactly 2 attempts, then structural fallback — never a 3rd call",
    "retry recovery: first attempt fails, second succeeds — exactly 2 calls, LLM content wins",
    "first-attempt success: exactly ONE call, no wasted retry",
    "both attempts return unparseable output (no exception): loop still bounds at 2 calls, then falls back"
   ],
   "core/routing-types.test.ts": [
    "returns unknown intent with zero confidence",
    "returns empty domains and operations",
    "returns small complexity by default",
    "returns empty rules with default minScore 5",
    "accepts custom minScore",
    "returns correct defaults",
    "returns true for valid intent types",
    "returns false for invalid types",
    "contains 12 intent types (testing removed Sprint 148, architecture added V3)",
    "includes core intent types (testing excluded)",
    "trivial gets 0 skills",
    "large gets 3 skills",
    "epic is capped at 3",
    "is 3"
   ],
   "core/rule-generator.test.ts": [
    "loads brain template",
    "loads auditor template",
    "loads worker-default template",
    "throws on missing template",
    "returns empty string for no ADRs",
    "formats accepted ADRs",
    "skips non-accepted ADRs",
    "is id-only — does not embed ADR titles or summaries (2026-06-19 index contract)",
    "emits a deckent recall pointer to the memory.db SSOT",
    "fails typed when an accepted ADR has a non-canonical identity",
    "defaults to src/** + tests/** for typescript/unknown/null",
    "uses layout-free extension globs for go/python/csharp",
    "uses convention dirs for ruby/dart/swift",
    "replaces ADR placeholder with ADR section",
    "removes placeholder when no ADRs",
    "extracts custom content between markers",
    "returns null when no markers",
    "returns null when only start marker",
    "preserves custom section from existing file",
    "creates empty custom section for new files",
    "claude adapter adds frontmatter with paths",
    "codex adapter generates plain markdown",
    "gemini adapter generates plain markdown",
    "generates 12 files for all providers × all roles",
    "includes ADR entries in generated rules",
    "preserves custom sections on re-generation",
    "is idempotent — same output on repeated runs",
    "handles subset of providers",
    "handles subset of roles",
    "preserves existing file content as custom when no markers present",
    "creates directories that do not exist",
    "brain gets directives + task/brain paths (product-safe, no dogfood leak)",
    "auditor gets dashboard + locks paths (product-safe, no dogfood leak)",
    "worker gets src/tests paths (default, no stack override)",
    "worker uses stack-aware override paths when provided (no TS assumption)",
    "emits .mdc files with MDC frontmatter on line 1",
    "does not emit a plain .md file for cursor",
    "is idempotent across repeated runs",
    "reports error for invalid template dir"
   ],
   "core/rule-regen-db-query.test.ts": [
    "renders brain.md from store.getByType(adr) — all 50 ADRs present",
    "filters out non-accepted ADRs (only accepted reach the output)",
    "returns auditor.md and worker.md alongside brain.md",
    "empty store produces rules without ADR section",
    "renderRulesFromStore reads via getByType(\"adr\") (uses store API)"
   ],
   "core/rule-regen-sentinel-idempotent.test.ts": [
    "replaces between AUTO-START and AUTO-END markers",
    "does not append — only one AUTO-START / AUTO-END pair after replace",
    "idempotent on repeated apply with same inner content",
    "preserves surrounding content unchanged",
    "returns content unchanged when no sentinel markers exist",
    "handles multi-line inner content"
   ],
   "core/run-flow-event-log.test.ts": [
    "assigns 1..N unbroken sequence numbers to ordered appends on a single flow",
    "keeps each flowId on its own monotonic sequence regardless of interleaving",
    "returns only records strictly past the cursor, including boundary values",
    "skips a half-written trailing line on read, and keeps computing the correct next sequence for a subsequent append",
    "commits a multi-event command once and returns a durable idempotent no-op on replay",
    "rejects stale-head and same-command/different-payload conflicts without appending",
    "returns the deduped, sorted flowId set across snapshot/handle/events files and skips unknown-suffix files",
    "returns an empty array when the store dir does not exist yet",
    "preserves an appended event’s commandId byte-identically on read",
    "round-trips an event that omits commandId as undefined, not null/empty-string"
   ],
   "core/run-flow-plan-digest-store.test.ts": [
    "selects v2 records by exact revision+digest+version instead of latest",
    "keeps version-absent records on the explicit legacy-v1 revision path",
    "enforces idempotency/CAS and publishes a handle only with ADMITTED",
    "requires explicit terminal predecessor CAS and a fresh idempotency key for a new generation",
    "paginates latest attempts with a deterministic bounded cursor"
   ],
   "core/run-flow-retention.test.ts": [
    "archives a terminal journal with revision/digest lineage while canonical history survives",
    "preserves proposed, approved, running, resumable, and ambiguous flows",
    "archives only stale running flows with exact fresh dead-liveness lineage",
    "fails closed on malformed canonical event payloads"
   ],
   "core/run-flow-store-concurrency.test.ts": [
    "preserves every concurrent append with unique ordinals and a lossless projection"
   ],
   "core/run-flow-store-migration.test.ts": [
    "imports valid JSONL once, records malformed evidence, and ignores later projection tampering",
    "rebuilds a projection from canonical rows when a crash intent is present",
    "upgrades v2 atomically and keeps generation recovery manifests immutable"
   ],
   "core/run-flow-store.test.ts": [
    "round-trips a saved snapshot",
    "returns undefined for a flowId that was never saved",
    "is append-only: a second save preserves the first line and load() returns the latest",
    "is atomic: no stray .tmp file survives a save",
    "is project-scoped: two different roots do not share state",
    "round-trips a saved run handle",
    "returns undefined for a flowId that was never started",
    "is append-only and load() returns the latest start attempt",
    "keeps snapshot and handle logs in separate files for the same flowId"
   ],
   "core/run-inspector-read-model.test.ts": [
    "projects persisted critical debt holds through the shared snapshot and run listing",
    "never attaches stale sibling debt holds to the canonical current run",
    "fails the read model for malformed holds bound to the current run",
    "returns an honest idle snapshot for an empty project without writing files",
    "sources the full lifecycle from authority and never echoes an ignored raw PAUSED claim",
    "tolerates torn task and heartbeat JSON while retaining valid task detail",
    "advances revision when a read source changes and is stable for equal inputs",
    "lists valid locks and reads capped task detail with task-id validation",
    "lists the authority run first and archived settlement records newest-first",
    "honestly returns only the authority entry when archive sources are absent",
    "reports absent lineage artifacts without fabricating evidence",
    "keeps stale task projection status distinct from the matching current run cause",
    "does not attribute current run truth to a historical task",
    "rejects a forged task prefix, explicit other sprint, and task-id mismatch",
    "returns the last default or overridden number of log lines",
    "hard-caps the requested line count and joined tail bytes without an ellipsis",
    "keeps a torn final log line as-is",
    "degrades invalid UTF-8 log content to null while retaining availability truth",
    "advances run-list revision when an archive settlement record appears",
    "delivers the current snapshot immediately when no revision cursor is supplied",
    "primes a reconnect cursor without delivering a duplicate snapshot",
    "coalesces rapid source changes and delivers only the latest revision on the next tick",
    "closes idempotently and stops future polling",
    "contains callback errors and reports a persistent read failure once"
   ],
   "core/run-policy-authority.test.ts": [
    "authors a version-1 snapshot with a deterministic canonical digest",
    "different constraint bytes or sourceRef produce different digests",
    "fail-closed: empty set, blank constraint, blank sourceRef and cap breaches throw typed bounds errors",
    "settles POLICY_PARITY when the worker echoes the exact plan digest",
    "missing worker evidence is a typed HOLD, never a silent pass",
    "a different observed digest is a typed HOLD",
    "malformed worker evidence is a typed HOLD",
    "a tampered plan snapshot (constraint bytes changed after digest) can never settle"
   ],
   "core/run-status-authority.test.ts": [
    "projects one PAUSED truth over stale ACTIVE/COMPLETE surfaces and exposes recovery",
    "reports ACTIVE only when the coordinator PID is alive",
    "reports dead-coordinator evidence as resumable ORPHANED instead of ACTIVE",
    "vetoes resumable ORPHANED when the identity-proven flow failed",
    "projects COMPLETE from an identity-proven terminal flow event",
    "keeps ORPHANED recovery when terminal flow identity cannot be proven",
    "uses a fresh coordinator snapshot when the host PID is namespace-invisible",
    "uses a fresh cross-platform lease when kernel start tokens are unavailable",
    "rejects a fresh snapshot from a different cross-platform lease",
    "expires stale namespace-fallback evidence to ORPHANED",
    "rejects a fresh namespace snapshot from a different process lifetime",
    "rejects matching process tokens when coordinator lifetime timestamps differ",
    "does not let a stale pause from another run hide a live current run",
    "reports a dashboard-only ACTIVE projection as canonical IDLE conflict",
    "surfaces a sprint.lock identity that differs from canonical execution",
    "retains identity-proven failed history without inventing foreign live conflicts",
    "preserves %s authority as a conflict"
   ],
   "core/run-status-read-model-artifacts.test.ts": [
    "ignores a known landing proposal instead of publishing a malformed-task HOLD",
    "retains an exact HOLD for a malformed canonical task record"
   ],
   "core/run-status-read-model.test.ts": [
    "folds FIX attempts into one logical denominator and advances revision only on semantic change",
    "keeps IDLE historical observations forensic without carrying a run HOLD",
    "counts only exact current-run task observations and rejects digest tampering",
    "publishes all eight planned tasks and committed progress despite stale materialized files",
    "rejects foreign snapshot %s without replacing persisted progress",
    "rejects an old lease with identical lifecycle and sprint identity; missing live identity also fails closed",
    "only the current NO_GO lineage projects retry pending, never a queued or held FIX",
    "resolves its live lease without injected authority (historical dashboard=%s)",
    "does not let a snapshot hint override a persisted different active run"
   ],
   "core/run-status-readiness.test.ts": [
    "treats resumable PAUSED authority as self-sufficient before resumability",
    "holds an unproven ACTIVE authority without a matching model",
    "treats proven ACTIVE liveness as self-sufficient",
    "returns READY for a model matching the exact authority",
    "projects retry-pending and budget-exhausted blocked FIX lineages"
   ],
   "core/runtime-adoption-receipt-store.test.ts": [
    "publishes canonical create-only bytes, replays, and reads in a fresh facade",
    "derives opaque tenant/environment scopes and prevents cross-tenant reads",
    "fresh replay fails closed on artifact and ownership changes without mutating existing authority",
    "rejects symlink paths, unsafe permissions, collisions, and bounds discovery"
   ],
   "core/runtime-adoption.test.ts": [
    "canonically binds every adoption authority and is deterministic and deeply frozen",
    "round-trips only exact canonical bytes",
    "returns typed HOLD codes for tampering, unknown fields, traversal, and unsupported versions"
   ],
   "core/runtime-artifact-classifier.test.ts": [
    "preserves databases, sidecars, tokens, and current status without content inspection",
    "requires archive-before-retire for inactive owned %s",
    "retires a duplicate only when its validated digest identifies the duplicate",
    "retires verified inactive ephemeral family %s",
    "holds unknown families rather than providing a catch-all retirement path",
    "holds retirement when evidence fails: %s",
    "does not expose a content or filename field in its input contract"
   ],
   "core/runtime-evaluation-retention.test.ts": [
    "archives nested opaque attempts, verifies family digests, then retires them",
    "preserves conflicting attempts at both locations and manifests both variants",
    "keeps the current window and prevents malformed or foreign ownership leakage",
    "retires a pre-existing canonical duplicate but never foreign siblings",
    "holds pending evaluation publication without mutating a terminal application archive",
    "keeps the terminal application receipt outside canonical manifest artifacts",
    "keeps changed source bytes when a plan becomes stale"
   ],
   "core/runtime-hygiene-adversarial.test.ts": [
    "does not follow symlink or hardlink escape candidates",
    "HOLDs a path swap after the fresh authority check without touching either target",
    "HOLDs a concurrent writer and preserves the writer\\'s complete new bytes",
    "HOLDs malformed JSON, recycled PID evidence, and a foreign tenant record",
    "rejects plan tampering before any source mutation",
    "treats malformed/conflicting first-writer receipt JSON as a pre-apply HOLD",
    "HOLDs an archive destination fault and neither overwrites it nor retires source bytes",
    "publishes an interrupted-apply receipt and a retry cannot overwrite it or retire held bytes"
   ],
   "core/runtime-hygiene.test.ts": [
    "bounds a 10k synthetic inventory and produces deterministic counters and digest",
    "composes archive/retire families and publishes a byte-stable FWW receipt",
    "reports an injected family fault, preserves its bytes, and continues other families",
    "fails the fresh authority check without archiving or retiring changed bytes",
    "rejects any sprint listed in the current authority window"
   ],
   "core/runtime-job-retention.test.ts": [
    "archives then retires old terminal records while retaining the latest identity view",
    "never retires RUNNING records with %s ownership",
    "does not let a terminal status string authorize deletion without inactive ownership",
    "supports current, legacy detached, and real sprint namespaces in archive lineage",
    "uses count pressure only against independently inactive terminal records"
   ],
   "core/runtime-log-retention.test.ts": [
    "retires the four named zero-byte start logs only in apply mode and writes receipts",
    "archives non-empty bot and JSONL logs before retiring them",
    "archives expired detached CLI logs from their dedicated runtime namespace",
    "preserves current writers and detects an append between inspect and apply",
    "retires expired temp residue but preserves fresh logs, databases, and tokens"
   ],
   "core/scheduled-flow.test.ts": [
    "parses a valid every-minute expression",
    "parses a specific scheduled expression",
    "parses step expressions",
    "rejects expression with wrong number of fields",
    "rejects out-of-range minute",
    "rejects out-of-range hour",
    "rejects empty expression",
    "returns a future date after the from date",
    "advances to the correct target minute",
    "uses current time when no from date is given",
    "returns seconds and milliseconds zeroed",
    "honors the hour field",
    "advances to the next day when hour has already passed",
    "honors the day-of-month field",
    "advances month when day-of-month has already passed",
    "honors the month field",
    "honors the day-of-week field (Monday-Friday)",
    "treats dow=7 as Sunday alias",
    "honors step expression */5 for minutes",
    "honors step expression */2 for hours",
    "honors range in minute field",
    "honors comma list in minute field",
    "throws for impossible date expression",
    "constructs a ScheduledFlow object with tenantId",
    "allows optional createdAt field",
    "allows an optional IANA timezone"
   ],
   "core/scheduler-shadow-retention.test.ts": [
    "(a) archives a journal older than retention_days (15 days, default 14)",
    "(b) keeps a journal newer than retention_days (5 days, default 14)",
    "(c) a file exactly retention_days old is KEPT — archival requires strictly greater age",
    "one millisecond past the boundary is archived",
    "(d) a 10-day-old file is kept under default (14d) but archived under override (7d)",
    "(e) returns an empty result and never throws when the directory does not exist",
    "(f) archived file content is byte-identical and remains JSON.parse-able",
    "ignores files without a .jsonl extension regardless of age"
   ],
   "core/scope-check.test.ts": [
    "converts backslashes to forward slashes",
    "strips a trailing slash",
    "resolves an existing path to its real path",
    "walks up to the nearest existing parent for a non-existent nested path",
    "follows a symlink to its real (out-of-scope) target",
    "returns null on a symlink cycle",
    "allows an ordinary in-scope existing path matched via directory",
    "allows a new (non-existent) in-scope path — parent-realpath handling",
    "allows a new (non-existent) file matched via a filesWrite exact list",
    "allows a path matched via a filesRead exact list",
    "checks exact lists in order and falls through to directories",
    "denies a path genuinely outside the declared scope",
    "denies every target when no scope is declared (fail-closed)",
    "rejects a symlink inside a directory scope that resolves outside scope",
    "rejects a symlink matched via an exact list that resolves outside scope (planted-symlink/TOCTOU)",
    "rejects a symlink cycle (ELOOP) instead of throwing",
    "rejects a symlink inside scope.directories that resolves outside scope",
    "rejects a symlink inside scope.filesWrite that resolves outside scope",
    "allows an ordinary in-scope path with no symlink involved",
    "allows a new (non-existent) file matched via scope.filesWrite",
    "denies a path genuinely outside assigned scope"
   ],
   "core/scope-gate.test.ts": [
    "rejects a tracked directory in filesWrite even with acknowledgeScopePaths",
    "blocks the exact born-573/518 wrong-directory write with a did-you-mean suggestion",
    "passes when acknowledgeScopePaths overrides the block",
    "confirms an existing tracked write path",
    "treats a brand-new file in an existing directory as new-plausible (does NOT block)",
    "does NOT flag a new file with a ubiquitous basename (index.ts) as wrong-dir",
    "does NOT flag a mirror-named NEW test whose task also owns the matching source (449-006 live case)",
    "KEEPS the wrong-dir block for a mirror-named test with NO matching source in the task (397-007 shape)",
    "KEEPS the wrong-dir block for a mirror-named test whose parent directory is untracked",
    "KEEPS the 573/518 wrong-dir block for source-module basenames even with a tracked parent",
    "treats a glob scope pattern matching tracked files as confirmed (flow-63aedcaf live case)",
    "still BLOCKS a glob pattern that matches no tracked file (wrong-directory pattern)",
    "flags a write into a fully invented directory as suspect (no suggestion)",
    "does not block on a suspect READ path (advisory only)",
    "confirms a READ of a file created by another task in the same plan",
    "does NOT block nested write paths in an empty repo",
    "flags the pass as greenfield with a visible notice and advisory verdicts",
    "root-only repo (tracked files, ZERO tracked dirs — e.g. README+LICENSE) is also advisory",
    "all-confirmed writes in a root-only repo carry NO greenfield notice (nothing unvalidated)",
    "non-greenfield behavior is untouched (no greenfield flag, block still fires)",
    "REPRODUCE: 397-011 dupe-typo still blocks with resolveSuggestions unset (RED-before-fix baseline)",
    "REPRODUCE: 397-007 wrong-dir rename still blocks with resolveSuggestions unset (RED-before-fix baseline)",
    "rule (a) drop-duplicate: resolveSuggestions=true does not block the 397-011 typo-dupe",
    "rule (0): a suspect inside a declared scope directory is intent, not a duplicate",
    "rule (b) auto-replace: resolveSuggestions=true does not block the 397-007 unambiguous rename",
    "rule (c) ambiguous: same basename in 2+ tracked dirs stays unresolved and still blocks in true-mode",
    "rule (c) no suggestion (invented-dir): no resolution is produced",
    "a suspect READ path never gets a resolution",
    "false-mode (default/omitted) is bit-identical to pre-399/003 behavior — resolved suspects still block"
   ],
   "core/self-audit-adapter-matrix.test.ts": [
    "returns typed unsupported without ever invoking the executor ($label)",
    "holds as invalid-request (never unsupported) for a whitespace-only project root, before adapter resolution",
    "holds as adapter-unavailable (distinct from unsupported) when a registered ecosystem adapter cannot run on this host",
    "holds as invocation-rejected when an adapter rewrites Windows backslashes away from the exact request path",
    "holds as invocation-rejected when an adapter strips a POSIX trailing slash away from the exact request path"
   ],
   "core/self-audit-adapter.test.ts": [
    "executes a scoped adapter with argv and shell disabled, then returns executed evidence",
    "returns typed unsupported without invoking a process when no ecosystem adapter exists",
    "fails closed as HOLD on timeout even when adapter evidence would claim execution",
    "rejects fake parity when an adapter reports no positive executed unit",
    "requires explicit separate authority before preparing or executing a full suite",
    "returns failed executed evidence for a non-zero exit instead of treating it as unsupported",
    "resolves the highest-priority matching capability adapter deterministically",
    "describes a timed-out run instead of dropping it",
    "describes the run behind an adapter-reported hold",
    "never carries the output itself",
    "distinguishes two runs whose streams differ only by boundary"
   ],
   "core/self-audit-vitest-adapter.test.ts": [
    "executes only explicitly scoped files through shell-free Vitest argv",
    "requires non-zero executed files and assertions even when Vitest exits zero",
    "preserves failed and skipped Vitest evidence without baseline comparisons",
    "preserves timeout as a fail-closed hold without accepting output evidence",
    "rejects a full-suite request even when separate authority was granted",
    "reports unavailable when capability detection fails"
   ],
   "core/self-dispatch-queue.test.ts": [
    "enqueues when dispatch=true AND requiresApproval=true",
    "does not enqueue when dispatch=false (no due dispatches)",
    "does not enqueue when requiresApproval=false",
    "returns only pending entries (approved are filtered out)",
    "transitions pending → approved and stamps approvedAt",
    "returns null for unknown id",
    "returns null when approving an already-approved entry (idempotent guard)",
    "does not invoke any callback or side-effect on enqueue or approve"
   ],
   "core/self-dispatch-runtime.test.ts": [
    "evaluates the policy and pushes pending-approval when dispatches present",
    "does not push when no due dispatches arrive on tick",
    "queued item preserves requiresApproval=true (default guard) — auto-start forbidden",
    "appends multiple pending-approval items across successive ticks",
    "never invokes any external start side-effect (queue is the only mutation)",
    "FlowRuntime.start is not implicitly called by the callback factory",
    "returns immediately when policy.disabled === true (no queue mutation)",
    "clock is not consulted when policy.disabled === true",
    "FlowRuntime.tick routes due dispatches into the pending-approval queue"
   ],
   "core/self-dispatch.test.ts": [
    "returns dispatch=true when due dispatches exist",
    "returns dispatch=false when no due dispatches",
    "defaults requiresApproval to TRUE when guard not specified",
    "honors explicit requiresApproval=true",
    "allows explicit opt-out via requiresApproval=false",
    "exports DEFAULT_GUARD with requiresApproval=true",
    "returns dispatch=true when metric crosses threshold (>)",
    "returns dispatch=false when threshold not crossed",
    "returns dispatch=false when threshold metric mismatches",
    "returns dispatch=false when policy.threshold config is missing",
    "supports >= operator",
    "returns dispatch=true when event matches policy eventType",
    "returns dispatch=false when event does not match",
    "returns dispatch=true for wildcard (eventType undefined matches any)",
    "returns dispatch=false when policy trigger does not match context kind"
   ],
   "core/session-interface.test.ts": [
    "should be importable from core/session-interface",
    "should register and retrieve a provider",
    "should return null for unregistered provider",
    "should overwrite a previously registered provider",
    "should return true for registered provider",
    "should return false for unregistered provider",
    "should return empty array when no providers registered",
    "should return all registered provider names",
    "should detect available provider in health check",
    "should detect unavailable provider in health check",
    "should report error when isAvailable throws",
    "should health check all providers when no name given",
    "should return error result for unregistered provider health check",
    "should report missing auth for codex without OPENAI_API_KEY",
    "should report ok auth for gemini with GOOGLE_API_KEY set",
    "should unregister a provider and return true",
    "should return false when unregistering non-existent provider",
    "should clear all providers",
    "should track provider count via size",
    "should produce HealthCheckResult with all required fields",
    "should be importable from orchestra/connector (re-export)"
   ],
   "core/shell-readonly-classifier.test.ts": [
    "reports examined paths root-relative and dedupes them",
    "accepts an absolute path INSIDE the project root",
    "honours a caller-supplied protected set (replaces the default)",
    "falls back to lexical containment without a project root",
    "ships the documented default protected set",
    "sed: accepts print/address scripts, rejects write/exec/read commands",
    "awk: distinguishes comparison `>` from redirection, rejects pipes/system/getline",
    "maps native Windows to PowerShell and every other host to POSIX sh"
   ],
   "core/siem-forwarder-warn.test.ts": [
    "emits a single advisory when no transport is configured and batch is discarded",
    "does not re-warn on subsequent flushes (once-per-instance guard)",
    "does not warn when a transport is configured — events forwarded normally"
   ],
   "core/siem-forwarder.test.ts": [
    "forward() does not throw when no transport is configured",
    "flush() does not throw when no transport is configured",
    "flush() with empty buffer completes silently",
    "flush() sends all forwarded events as one batch",
    "buffer is empty after flush — second flush is a no-op",
    "triggers an auto-flush when maxBatch is reached",
    "events after an auto-flush are buffered again",
    "flush() does not throw when transport rejects",
    "retries transport up to maxRetries then drops",
    "succeeds if transport recovers before maxRetries",
    "maps actor + action from the audit event",
    "outcome defaults to \"unknown\" when not in metadata",
    "outcome is extracted from metadata.outcome",
    "correlationId and causationId are extracted from metadata",
    "ts is an ISO-8601 string",
    "dispose() can be called multiple times without error"
   ],
   "core/siem-transport-http.test.ts": [
    "POSTs the batch as a JSON array with content-type application/json (2xx ok)",
    "throws on a non-2xx response with the status in the message (no internal retry)",
    "throws at creation time on a malformed URL",
    "throws at creation time on a non-http(s) scheme",
    "passes custom headers through alongside the JSON content-type",
    "throws an explanatory error when no fetchImpl is given and globalThis.fetch is missing",
    "skips the network call entirely for an empty batch",
    "propagates fetch rejections (network errors) to the caller"
   ],
   "core/siem-transport-syslog.test.ts": [
    "uses PRI <110> by default (facility 13 \"log audit\" * 8 + severity 6 info) and VERSION 1",
    "emits RFC 5424 field order: <PRI>1 TIMESTAMP HOSTNAME APP-NAME PROCID MSGID SD MSG",
    "MSG part is the SiemRecord JSON (round-trips via JSON.parse)",
    "honors custom facility and appName (facility 4 → PRI 38)",
    "formats one message per record in the batch",
    "empty batch is a no-op — sendImpl is never invoked",
    "NEVER opens a socket when sendImpl is injected (udp and tcp opts alike)",
    "defaults to udp (node:dgram) on port 514 when sendImpl is omitted",
    "uses tcp (node:net) with newline framing when protocol is tcp",
    "rejects when the injected sendImpl rejects (forwarder owns retries)",
    "binds the udp send-callback error to the promise and still closes the socket",
    "rejects when the tcp connection emits an error",
    "throws on an empty host",
    "throws on an invalid protocol",
    "throws on an out-of-range port or facility, and on whitespace in appName"
   ],
   "core/signature.test.ts": [
    "generateKeypair returns 32-byte private and public keys",
    "sign and verify round-trip succeeds",
    "verification fails with wrong public key",
    "verification fails with tampered message",
    "creates key files on first call",
    "sets correct file permissions",
    "sets correct directory permissions",
    "loads existing keys on second call",
    "deterministic: same message + same key produces same signature"
   ],
   "core/skill-agent-signal.test.ts": [
    "maps the canonical frontend/security/api/doc cluster skills to their agents",
    "does NOT map refactorer/architect/bug-fixer as values for any generalist skill",
    "boosts frontend-designer for a task with frontend-design skill",
    "boosts frontend-designer for a task with react-specialist skill (multi-skill→same-agent)",
    "boosts security-auditor for a task with security-specialist skill",
    "boosts doc-writer for a task with documentation-writer skill",
    "boosts api-builder agent for a task with api-builder skill",
    "refactorer gets 0 bonus even when frontend skills are assigned (still a candidate via base rules)",
    "returns 0 for empty / undefined assigned skills",
    "returns 0 for an unknown skill that has no agent affinity",
    "caps the bonus at a single application even with multiple matching skills",
    "multi-skill task surfaces the correct agent for each cluster"
   ],
   "core/skill-body-resolution.test.ts": [
    "accepts a relative path and normalises both separators",
    "flags every escaping shape as an escape, on every platform separator",
    "flags an absolute or drive-qualified path as an escape",
    "rejects a non-string or empty declaration without calling it an escape",
    "injects the DECLARED entrypoint, not the hardcoded SKILL.md",
    "honours a nested entrypoint under the skill root",
    "reads the schema-v1 entrypoint object beside the legacy string (design §3.3)",
    "defaults to SKILL.md when no entrypoint is declared (back-compat)",
    "resolves a package-only builtin from the builtin root, same code path",
    "HOLDs when the declared body does not exist",
    "HOLDs when the declared entrypoint is a directory, not a file",
    "resolves entrypoint and referenced files as one package",
    "HOLDs the WHOLE package when one referenced file is missing — never a partial prompt",
    "HOLDs an empty declared referenced path",
    "HOLDs a non-object referencedFiles entry at resolution (defence in depth)",
    "excludes a manifest whose referencedFiles shape is malformed, and reports it",
    "validates the referencedFiles shape at manifest level",
    "HOLDs a `../` escape in the entrypoint",
    "HOLDs a `../` escape in a referenced file",
    "HOLDs an absolute declared path",
    "HOLDs a symlink whose target leaves the skill root",
    "HOLDs a referenced file that symlinks out of the skill root",
    "still resolves a link that stays inside the skill root",
    "ships HIGH, non-blocking defaults (D8) that today\\'s manifests never hit",
    "HOLDs an over-budget single file",
    "HOLDs an over-budget package total",
    "HOLDs an over-count package before reading anything",
    "HOLDs an unknown id and a non-flat id distinctly from a body",
    "HOLDs a quarantined or retired id — withdrawn is unresolvable by any surface",
    "resolves a disabled skill — `enabled` is consumer-enforced, not a containment failure",
    "resolves a record handed in directly, without a second catalog pass",
    "keeps today\\'s shape — string entrypoint, no referencedFiles — valid and loadable",
    "still rejects an entrypoint that is neither a string nor a {path} object"
   ],
   "core/skill-cache.test.ts": [
    "loads and caches a skill file",
    "returns null when file does not exist",
    "returns null when read fails",
    "updates cache when reloading same skill",
    "returns null for non-cached skill",
    "returns cached skill after load",
    "preloads all skills from directory",
    "returns 0 when skills dir does not exist",
    "returns 0 when readdir fails",
    "returns true for non-cached skill",
    "returns false when file mtime matches",
    "returns true when file has been modified",
    "returns true when stat fails",
    "clears all cached skills",
    "evicts a specific skill",
    "returns false when evicting non-cached skill",
    "evicts oldest when exceeding size budget",
    "does not cache if single file exceeds budget",
    "tracks total bytes"
   ],
   "core/skill-catalog-readmodel.test.ts": [
    "accepts the flat kebab grammar every shipped id already uses",
    "rejects a publisher-qualified id — D9 chose flat-id + registry mapping",
    "rejects any path-bearing or platform-unsafe id",
    "$candidates resolves to $winner",
    "ranks project above generated above builtin",
    "never lets a generated skill outrank a hand-authored override (D1 NO-GO guard)",
    "returns undefined for an empty candidate list",
    "lets a hand-authored project manifest win over the shipped builtin",
    "classifies a generated/learned manifest as the generated layer, still above builtin",
    "reads the typed provenance block in preference to the legacy source string",
    "keeps the builtin fallback for a package-only skill (371-001 behaviour preserved)",
    "keeps the .deckent/config.json gate on the builtin layer",
    "still skips a builtin directory that ships its own manifest.json",
    "sorts entries byte-wise by id regardless of directory scan order (§5 rule 1)",
    "excludes a schema-invalid manifest and reports it under its directory name",
    "reports an unreadable/invalid-JSON manifest",
    "refuses a publisher-qualified id inside a manifest (D9 NO-GO guard)",
    "masks a retired id everywhere but still returns its tombstone",
    "masks a quarantined id with its own reason code",
    "distinguishes a withdrawn id from an unknown id",
    "keeps a disabled skill loadable — enabled stays a consumer-enforced flag",
    "ignores a malformed ledger, an unknown state and an unknown id",
    "keeps loadSkills/listSkills/listByCategory/listEnabled answering as before",
    "keeps the sidecar stats overlay winning over the manifest, and reports its source",
    "reports statsSource=manifest / defaults when no sidecar row exists",
    "normalizes optional array fields exactly as the previous loader did (born-641)",
    "scans nothing outside the two catalog roots",
    "resolves a single-id lookup in one pass — no private rescan",
    "exposes the resolution directly, invalid entries included",
    "refuses to register a publisher-qualified id",
    "registers a flat id"
   ],
   "core/skill-config.test.ts": [
    "accepts config without skills (undefined)",
    "accepts valid skills config with all fields",
    "accepts skills.enabled = false",
    "rejects non-boolean skills.enabled",
    "rejects skills.maxPerTask below 1",
    "rejects skills.maxPerTask above 10",
    "rejects non-number skills.maxPerTask",
    "accepts skills.maxPerTask at boundaries (1 and 10)",
    "rejects non-boolean skills.autoDetectStack",
    "rejects non-array skills.preferredSkills",
    "rejects non-string items in skills.preferredSkills",
    "accepts empty preferredSkills array",
    "accepts partial skills config (only enabled)",
    "accepts partial skills config (only maxPerTask)"
   ],
   "core/skill-manifest-live.test.ts": [
    "has SKILL.md + manifest.json on disk under .deckent/skills/<id>/",
    "SKILL.md is non-trivial real content, not a stub",
    "manifest.json passes SkillPoolManager.validateSkillDefinition",
    "has correct id/source/enabled/entrypoint and a NON-EMPTY description (the api-design ghost symptom)",
    "live SKILL.md is byte-identical to the builtin (materialize = copy, builtin untouched)",
    "EXACTLY-ONCE: exactly one on-disk directory in .deckent/skills/ matches this id",
    "pool-load smoke: SkillPoolManager.loadSkills() lists all 3 skills, enabled, with readable non-empty content",
    "EXACTLY-ONCE gate holds at the pool layer: a live-materialized skill is never also re-synthesized from the builtin fallback",
    "no unrelated skill was touched by this repair (nogo: \"başka skill\\'e dokunma\")"
   ],
   "core/skill-pool-stats.test.ts": [
    "increments totalUses by 1 on each call",
    "calculates successRate correctly for DONE evaluation",
    "calculates successRate correctly for GO_WITH_TECH_DEBT (counts as success)",
    "calculates successRate correctly for NO_GO (not a success)",
    "calculates successRate correctly across mixed evaluations",
    "calculates avgCoverage as rolling average",
    "updates lastUsedInSprint on each call",
    "persists stats to manifest.json on disk",
    "does nothing when skill id does not exist",
    "handles skill with undefined initial stats",
    "avgCoverage handles 0 coverage correctly",
    "stats survive across multiple manager instances"
   ],
   "core/skill-pool.test.ts": [
    "returns empty pool when skills directory does not exist",
    "loads skills from .deckent/skills/",
    "skips non-directory entries",
    "skips directories without manifest.json",
    "skips directories with invalid JSON",
    "skips skills failing validation",
    "loads multiple skills",
    "returns empty pool when readdirSync throws",
    "returns skill by id",
    "returns undefined for non-existent skill",
    "returns an array of all skills",
    "filters skills by category",
    "returns empty array when no skills match category",
    "returns only enabled skills",
    "returns false for non-existent skill",
    "enables a disabled skill and saves",
    "returns false for non-existent skill",
    "disables an enabled skill and saves",
    "writes skill to .deckent/skills/{id}/manifest.json",
    "writes valid JSON content",
    "preserves a demoted manifest without re-enabling it",
    "keeps fresh generated manifests enabled",
    "preserves unknown fields from an existing manifest",
    "returns false when skill directory does not exist",
    "removes skill directory and returns true",
    "does nothing when skill not found",
    "increments totalUses on DONE evaluation",
    "sets successCount to 1 on first DONE evaluation",
    "does not increment successCount on NO_GO evaluation",
    "increments successCount for GO_WITH_TECH_DEBT",
    "accumulates successCount across multiple uses",
    "counts GO_WITH_TECH_DEBT as success",
    "counts NO_GO as failure",
    "returns valid for a well-formed skill",
    "rejects null",
    "rejects arrays",
    "rejects missing id",
    "rejects empty id",
    "rejects missing name",
    "rejects invalid category",
    "rejects invalid model",
    "rejects non-number priority",
    "rejects non-boolean enabled",
    "rejects non-array triggers",
    "rejects non-string items in triggers",
    "rejects invalid promptInjection position",
    "rejects non-number promptInjection maxTokens",
    "rejects invalid stats",
    "rejects array as stackDetection",
    "rejects non-array stackDetection.files"
   ],
   "core/skill-profile-derivation.test.ts": [
    "derives a deterministic, valid V3 profile from source metadata",
    "maps semantic roots and never promotes generic categories to domains",
    "drops v1 garbage domains from %s",
    "derives domains from singular roots in name, description, and tags",
    "keeps a valid human-authored profile byte-authoritative",
    "deterministically upgrades a stale persisted generated profile",
    "keeps a valid persisted profile produced by the current derivation version",
    "returns a typed unroutable HOLD when source metadata cannot produce a profile",
    "is byte-idempotent across repeated derivation",
    "attaches derived eligibility or a typed HOLD to the canonical effective record",
    "derives valid canonical profiles for every TRACKED builtin package manifest"
   ],
   "core/skill-profile-state.test.ts": [
    "reports present-valid for the one real shipped profile block",
    "reports present-invalid for $label",
    "reports absent only when the manifest carries no profile key",
    "never carries a profile or issues that contradict its own state",
    "delegates validity to validateSkillProfile for every sample",
    "agrees with the V3 candidacy rule routing already applies",
    "carries a distinct state for each of the three manifests on disk",
    "reads a legacy V2-activation skill as absent, never as a reconciled profile (D6)",
    "keeps every unroutable skill in the catalog and labels it honestly",
    "labels a withdrawn skill as withdrawn even when its profile is valid"
   ],
   "core/skill-prompt-parity.test.ts": [
    "produces byte-identical prompts, in request order, for an unchanged catalog",
    "carries the declared entrypoint and its digest, so the prompt is auditable",
    "preserves credit-removal: a manifest whose body is missing is held, not injected",
    "skips a legacy project-conventions assignment tolerantly — generation is retired",
    "resolves the DECLARED entrypoint — the S3 fix the old reader could not see",
    "refuses an id with no catalog record instead of reading a stray file",
    "returns nothing for an empty assignment, without touching the catalog"
   ],
   "core/skill-registry.test.ts": [
    "registers a new skill",
    "replaces existing skill with same id",
    "registers multiple different skills",
    "persists to disk as skill-registry.json",
    "finds skill by id",
    "finds skill by description",
    "finds skill by trigger",
    "returns empty for no match",
    "returns empty for empty query",
    "multi-word search requires all terms",
    "returns skills sorted by totalUses",
    "getPopular respects limit",
    "getPopular handles limit of 0",
    "getPopular handles negative limit",
    "getAll returns all skills",
    "getAll returns empty array when no skills",
    "getAll returns copies (not references)",
    "removes existing skill and returns true",
    "returns false for non-existent skill",
    "only removes the specified skill",
    "count returns correct number",
    "data survives across registry instances",
    "handles empty/corrupt registry file gracefully",
    "finds skills by category"
   ],
   "core/skill-sandbox-honest-fail.test.ts": [
    "returns the sentinel (NOT empty array) when TypeScript is absent",
    "sentinel is distinguishable from a clean-scan result",
    "sentinel does not look like a real AST violation",
    "validateSkillSafety marks safe:false when TypeScript is unavailable (sentinel propagates)",
    "validateSkillSafety issues list contains the sentinel string for every .ts file scanned",
    "happy path: real AST violation is detected when TypeScript is present",
    "happy path: clean code returns empty array when TypeScript is present",
    "happy path: validateSkillSafety marks clean .ts file as safe when TypeScript is present"
   ],
   "core/skill-signing-sandbox.test.ts": [
    "trusts the real builtin skill ids",
    "no longer trusts the stale/non-existent ids",
    "getBuiltinTrustedSkills() reflects the corrected set exactly",
    "throws SkillSandboxError for an unsafe, untrusted skill",
    "returns the safety report (does not throw) for a clean, untrusted skill",
    "bypasses the scan entirely for a trusted skill id — never touches the filesystem",
    "bypasses the scan for an explicitly-trusted (non-builtin) skill id",
    "a formerly-trusted stale id (e.g. \"react-expert\") is now scanned like any third party",
    "buildSkillSignPayload is deterministic and order-sensitive",
    "a real signature verifies against the signer public key",
    "fails verification when the manifest is tampered after signing",
    "fails verification when the SKILL.md content is tampered after signing",
    "fails verification against the wrong public key",
    "a skill that passes requireSafe also produces a verifiable signature",
    "a skill that fails requireSafe never reaches the signature step"
   ],
   "core/skill-type-extensions.test.ts": [
    "is optional and defaults to undefined",
    "accepts an array of skill ID strings",
    "accepts an empty array",
    "is optional and defaults to undefined",
    "accepts an array of skill ID strings",
    "accepts an empty array"
   ],
   "core/skill-types.test.ts": [
    "returns a SkillStats with all fields zeroed",
    "includes successCount field",
    "returns a new object on every call (no shared reference)",
    "stats fields are independently mutable",
    "creates a skill with only id and name, rest defaults",
    "allows overriding version",
    "allows overriding description",
    "allows overriding entrypoint",
    "allows overriding category to language",
    "allows overriding category to framework",
    "allows overriding category to domain",
    "allows overriding category to workflow",
    "allows overriding triggers",
    "allows overriding stackDetection",
    "allows overriding composableWith",
    "allows overriding priority",
    "allows overriding promptInjection",
    "allows overriding model to opus",
    "allows overriding enabled to false",
    "allows overriding stats",
    "returns a new object each time (no shared reference)",
    "accepts all valid categories",
    "holds files, dependencies, and commands arrays",
    "accepts empty arrays",
    "accepts prepend position",
    "accepts append position",
    "accepts section position",
    "holds all expected fields",
    "accepts empty dependencies",
    "holds skills, scores map, and truncated flag",
    "truncated is true when capped"
   ],
   "core/spawn-backend.test.ts": [
    "should define required methods on TmuxBackend",
    "should define required methods on SubprocessBackend",
    "authors hold and warns when finite worker budgets fall back to subprocess",
    "does not author unmetered_backend when Docker is selected",
    "should have name \"tmux\"",
    "spawn() fails closed before tmux work when no execution budget exists",
    "spawn() rejects a turn ceiling because tmux has no live usage metering",
    "spawn() never forwards tool grants when admission is unmetered",
    "kill() delegates to killWorker",
    "list() returns listWorkers() result",
    "list() returns empty array when no workers",
    "isAvailable() returns true when tmux -V exits 0",
    "isAvailable() returns false when tmux -V fails",
    "should have name \"subprocess\"",
    "isAvailable() always returns true",
    "isAvailable() does not call spawnSync (no external check)",
    "same SpawnBackend interface as TmuxBackend",
    "should have correct name and message",
    "should be instance of Error",
    "returns SubprocessBackend when backend=\"subprocess\"",
    "returns TmuxBackend when backend=\"tmux\"",
    "subprocess backend does not call spawnSync (no tmux check)",
    "selects DockerBackend when auto and tmux is available",
    "selects DockerBackend when auto and tmux is not available",
    "auto mode does not probe tmux availability (deprecation moved decision to Docker)",
    "composes the trusted production-wiring host observer into the normal Docker backend",
    "returns true when tmux exits 0",
    "returns false when tmux exits non-zero",
    "returns false when spawnSync throws",
    "returns backend when isAvailable() is true",
    "throws SpawnBackendError when tmux backend is not available",
    "error message includes backend name",
    "can store different backends behind the same interface",
    "factory returns consistent interface regardless of backend",
    "auto mode resolves to docker regardless of tmux availability (Sprint 178 modernization)",
    "the default preserves the historical 100m (no silent behaviour change)",
    "worker_home_tmpfs_size CARRIES from config.json to the resolved runtime config",
    "absent from config → undefined resolved (backend falls back to the default)"
   ],
   "core/spawn-dep0190.test.ts": [
    "win32 → cmd.exe /c wrapper, discrete args, shell:false (no concat, PATHEXT resolves .cmd)",
    "posix → direct invocation, shell:false, byte-for-byte unchanged",
    "never returns shell:true on any platform",
    "win32 → spawnSync gets cmd.exe + discrete /c array, shell:false (NOT shell:true+array)",
    "posix → spawnSync gets the bare binary + args, shell:false (byte-for-byte)",
    "swallows spawn errors → undefined (no throw to the doctor caller)",
    "win32 spawn → cmd.exe /c <cli> wrapper, discrete args, shell:false",
    "posix spawn → bare \"claude\" command, shell:false, args unchanged (byte-for-byte)",
    "win32 isAvailable → cmd.exe /c <cli> --version, shell:false",
    "posix isAvailable → bare binary probe, shell:false"
   ],
   "core/spawn-lock-orphan-cleanup.test.ts": [
    "cleans orphan spawn locks for tasks not in activeTaskIds",
    "preserves all locks if all tasks active",
    "clears all if activeTaskIds empty",
    "returns 0 when no spawnlocks exist",
    "checkSpawnLock returns lock info when held, null otherwise",
    "checkSpawnLocks lists every held spawn lock",
    "releaseStaleSpawnLocksForTask releases every lock owned by the task",
    "releaseStaleSpawnLocksForTask is no-op for unknown taskId"
   ],
   "core/spawn-lock-stale-ttl.test.ts": [
    "removes spawn locks older than maxAgeMs",
    "preserves spawn locks newer than maxAgeMs",
    "default TTL of 5 minutes triggers removal for old locks",
    "returns 0 when no spawnlocks exist",
    "handles missing .locks directory gracefully"
   ],
   "core/spawn-safety.test.ts": [
    "contains the expected default binaries",
    "is frozen — mutation attempts throw in strict mode and have no effect",
    "matches alphanumerics, underscore, hyphen, dot, slash, whitespace, equals",
    "rejects shell metacharacters",
    "classifies newline/CR as whitespace per spec — \\\\s in JS regex includes \\\\n/\\\\r/\\\\t",
    "accepts a bare whitelisted binary",
    "accepts an absolute-path whitelisted binary via basename",
    "rejects a non-whitelisted binary such as rm",
    "rejects sh / bash because they are not whitelisted",
    "accepts every entry in ADAPTER_BIN_WHITELIST with a minimal arg",
    "rejects a semicolon-injected arg",
    "rejects standalone shell-separator args",
    "rejects backtick / dollar / pipe / redirect injection attempts",
    "accepts complex but safe vitest invocation",
    "accepts an empty args array",
    "accepts empty-string args (no injection vector)",
    "rejects non-string bin",
    "rejects non-array args",
    "rejects non-string args entries",
    "honors a custom binWhitelist",
    "honors a custom argRegex",
    "returns true for safe invocations",
    "returns false for whitelist violations",
    "returns false for arg-injection attempts",
    "returns false for invalid inputs",
    "exposes code / bin / badArg fields"
   ],
   "core/spawnlock-toctou.test.ts": [
    "never exposes the lock path in a partial/empty state at publish time",
    "adversarial interleave at the exact publish instant: exactly one task wins, the loser gets a clean SpawnLockError (mid-write read no longer triggers a phantom unlink+steal)",
    "still self-heals from a genuinely corrupted (0-byte) spawnlock file",
    "leaves no staging (.tmp-*) artifacts behind after a normal acquire"
   ],
   "core/spend-gate-prespawn.test.ts": [
    "returns a COST_LIMIT_WARN when flag-on AND already-logged daily spend + estimate exceeds daily_max_usd",
    "returns null when the flag is OFF — even with spend far past the limit (default-off path)",
    "returns null when the flag is UNSET (undefined) — same default-off no-read behavior",
    "returns null when flag-on but projected spend is UNDER the daily limit",
    "returns a monthly COST_LIMIT_WARN when daily is within limit but monthly is exceeded",
    "reads BOTH windows through the injected reader when the flag is on (no real ledger)",
    "never blocks — only ever returns a warn payload or null (warn-only contract)"
   ],
   "core/sprint-archive-seal.test.ts": [
    "accepts the canonical persisted terminal-receipt wrapper and verifies exact identity",
    "seals exact journal/sequence parity and replays idempotently",
    "replays an exact applied seal after hot counter retirement and rejects request drift without writes",
    "rejects a tampered prior application without writing through the retired-counter fallback",
    "requires a hot counter for initial seal and never masks a mismatched live counter on resume",
    "CAS-repairs only strict journal/sequence prefixes and preserves both preimages",
    "fails closed before staging for receipt, digest, reason, and non-prefix mismatches",
    "rejects non-canonical hot paths and regressed sequence counters",
    "does not declare green for a stale final-event identity",
    "binds manifest, compact Brain index, and guarded summary digests in applied receipt",
    "forwards caller-owned labels through Brain adoption without persisting render options",
    "binds the writer-owned adopted row while another live Brain WAL connection remains open",
    "replays an earlier adoption after a later canonical Brain refresh and rejects its row tampering",
    "verifies the Brain projection through a byte-stable read-only SQLite path",
    "verifies from a detached DB+WAL snapshot while a live Brain writer remains open",
    "fails closed before following a symlinked Brain database into external authority",
    "holds metadata digest mismatch through detached read-only verification without writes",
    "fails closed on a malformed live WAL without mutating Brain authority",
    "fails closed when the source WAL changes during the detached snapshot CAS",
    "rejects an applied-receipt attempt to downgrade required Brain adoption",
    "leaves a typed staged receipt instead of false success when guarded Brain adoption fails",
    "detects receipt/history traversal tampering and handles Windows containment semantics",
    "rejects a canonical hot namespace redirected through a symlink or junction parent",
    "rejects an archive namespace redirected through a symlink or junction",
    "publishes ordinary artifacts before a terminal sidecar exists",
    "does not write through a redirected canonical parent",
    "rejects new and conflicting publication after a seal or application sidecar without mutation",
    "retains a pre-seal conflict as a hash-addressed artifact",
    "proves an exact sealed replay and retires its matching source only on request"
   ],
   "core/sprint-archive.test.ts": [
    "archives only receipt-referenced full-digest core bytes and replay-verifies them",
    "rejects receipts whose exact invocation provenance contradicts their filename",
    "collects split evidence, retires only exact-sprint legacy files, indexes Brain, and is idempotent",
    "preserves conflicting bytes as hash-addressed variants and manifests the conflict",
    "predicts intra-batch conflicts in dry-run exactly as apply publishes them",
    "detects post-publication byte tampering",
    "keeps inspect/dry-run read-only while returning deterministic hashes",
    "includes already-canonical evidence in inspect/dry-run truth",
    "publishes direct writers through the same conflict-preserving authority",
    "archives only settled handoffs with two parsed current-sprint endpoints",
    "never adopts sprint-610 residue found inside sprint-611 legacy staging"
   ],
   "core/sprint-file-retention.test.ts": [
    "extracts sprint ID from sprint-NNN-events.jsonl",
    "extracts sprint ID from multi-hyphen family",
    "returns null for non-sprint files",
    "groups sprint files by sprint ID",
    "does not let legacy epoch job ids occupy the ordinal sprint window",
    "returns empty when .deckent/ absent",
    "archives oldest sprints when count exceeds keep_last_n",
    "no-op when sprint count ≤ keep_last_n",
    "preserves the canonical sprint-prefixed run filename",
    "refreshes a pre-existing historical manifest after retention publishes new evidence",
    "archives oldest sprints when total size exceeds cap",
    "deletes -seq and -checkpoint-seq files for given sprint",
    "ignores other sprints",
    "moves forensic .md files to the canonical sprint audit namespace",
    "no-op when no forensic files",
    "runs counter cleanup + forensic migration + retention in one pass",
    "fail-safe: null sprint ID skips counter cleanup",
    "can defer counter cleanup until after the terminal event is emitted"
   ],
   "core/sprint-finalizer-gate-authority.test.ts": [
    "publishes a first-writer gate bound to the exact input and replays only that input",
    "cannot use a Sprint-619 pre-fix TSC pass for fresh post-fix results",
    "digest binds run, generation, task set, winners, code, config, and observation",
    "fails closed on stale CAS, dishonest digest, and invalid invalidation"
   ],
   "core/sprint-historical-context.test.ts": [
    "reads the complete exact docs/brain-sprint.md only after both archive verifiers pass",
    "holds instead of truncating an oversized document",
    "does not apply the smaller note budget to the verification manifest",
    "holds before full verification when a canonical JSON artifact exceeds its resource budget",
    "bounds the actual hot journal before the canonical verifier reads it",
    "bounds a manifested delivery receipt by actual metadata before replay parsing",
    "holds on artifact tamper and never returns partial text",
    "rejects a symlinked artifact before reading its external target",
    "rejects invalid UTF-8 without replacement or partial text",
    "leaves every project-private file byte-identical after a successful read",
    "rejects invalid input and an already-aborted request without spawning verification"
   ],
   "core/sprint-status-recovery-reconciliation.test.ts": [
    "does not present Sprint-595 residue as resumable when its terminal successor exists",
    "types a PAUSED projection with no checkpoint separately",
    "types a receipt-only PAUSED projection as stale rather than generic ORPHANED",
    "keeps a valid PAUSED checkpoint resumable with the canonical resume command",
    "leaves every consulted file byte-identical on the read path",
    "is wired into the canonical status entrypoint without mutating recovery bytes"
   ],
   "core/sprint-terminal-publication.test.ts": [
    "publishes one immutable receipt bound to every authority fence",
    "returns the exact terminal winner for an idempotent replay",
    "HOLDs a conflicting terminal payload without replacing the winner",
    "HOLDs an ABORTED replay against a COMPLETE terminal winner",
    "HOLDs stale and future coordinator generations",
    "HOLDs publication after a foreign sprint or run owns the authority",
    "HOLDs a stale prior authority version before first publication",
    "rejects malformed authority and digest input at the pure boundary",
    "requires no timer, process, dashboard, or mutable adapter input"
   ],
   "core/sprint-work-attribution-baseline.test.ts": [
    "accepts settlement only when exact attempt and immutable baseline digest agree",
    "does not charge inherited predecessor files or ambient lines to the later attempt"
   ],
   "core/sprint-work-attribution.test.ts": [
    "excludes ambient legacy and HOLD claims from terminal work totals",
    "aggregates only exact VERIFIED attempts and preserves per-file attempt identities"
   ],
   "core/stack-detector.test.ts": [
    "detects TypeScript when tsconfig.json exists",
    "detects TypeScript when typescript dep exists",
    "detects JavaScript when only package.json exists with deps",
    "detects Rust when Cargo.toml exists",
    "detects Go when go.mod exists",
    "detects Python when setup.py exists",
    "detects Python when pyproject.toml exists",
    "returns unknown when no project files found",
    "detects React framework",
    "detects Next.js framework",
    "detects Vue framework",
    "detects Express framework",
    "detects Angular framework",
    "detects Svelte framework",
    "detects NestJS framework",
    "detects Fastify framework",
    "returns unknown framework when none detected",
    "detects Vitest by dependency",
    "detects Vitest by config file",
    "detects Jest by dependency",
    "detects Jest by config file",
    "detects Mocha",
    "detects pytest by pytest.ini",
    "detects Vite build tool",
    "detects Webpack build tool",
    "detects tsc for TypeScript without bundler",
    "detects cargo for Rust",
    "detects go build tool for Go",
    "collects dependencies from package.json",
    "caps dependencies at 200",
    "returns cached stack when not stale",
    "includes detectedAt timestamp",
    "returns true when cache does not exist",
    "returns true when cache stat fails",
    "returns false when no files are newer than cache",
    "returns true when package.json is newer than cache",
    "returns true when tsconfig.json is newer than cache",
    "ignores files that fail to stat",
    "invalidates a versioned cache when workspace authority content changes",
    "does not log statSync failures for absent monitored build files",
    "forces re-detection even when cache exists",
    "tries to remove cache file before re-detection",
    "handles cache removal failure gracefully",
    "returns fresh detection result",
    "writes cache file after detection",
    "cache file contains valid JSON with all stack fields",
    "uses cached data when stat shows cache is newer than all files",
    "re-detects when Cargo.toml is newer than cache",
    "re-detects when go.mod is newer than cache",
    "re-detects when pyproject.toml is newer than cache",
    "cache handles corrupt JSON gracefully",
    "cache handles missing language field gracefully",
    "refreshStack creates new cache timestamp",
    "cache write failure does not break detection",
    "detects Python when requirements.txt exists",
    "detects Python when Pipfile exists",
    "detects Django framework via manage.py",
    "detects Flask framework from requirements.txt",
    "detects FastAPI framework from pyproject.toml",
    "detects pytest via conftest.py for Python",
    "falls back to unittest when no pytest indicators for Python",
    "detects Java with Maven (pom.xml)",
    "detects Java with Gradle (build.gradle)",
    "detects Spring framework from pom.xml",
    "detects JUnit test framework from pom.xml",
    "detects C project with CMakeLists.txt (no CXX hints)",
    "detects C++ project with CMakeLists.txt with CXX",
    "detects C project with Makefile and .c files",
    "detects C++ project with meson.build",
    "detects Go test framework when _test.go files exist",
    "detects Rust with cargo_test framework",
    "has entries for all expected stacks",
    "each entry has build, test, lint, typecheck fields",
    "rust commands are correct",
    "go commands are correct",
    "typescript typecheck is the no-emit tsc invocation",
    "go typecheck is go vet",
    "rust typecheck is cargo check",
    "honest-empty typecheck for stacks with no known dedicated command",
    "returns commands for TypeScript project",
    "prefers real package scripts and common aliases over language defaults",
    "falls back per command without inventing absent package scripts",
    "ignores empty and non-string script entries",
    "returns commands for Rust project",
    "returns commands for Go project",
    "returns commands for Java Maven project",
    "returns commands for Java Gradle project",
    "returns commands for C CMake project",
    "returns commands for Python project",
    "returns empty commands for unknown language",
    "includes framework and testFramework in result",
    "detects React from sub-project package.json at arbitrary sub-directory",
    "adds rust to detectedLanguages when a sub-project has Cargo.toml",
    "adds python to detectedLanguages when a sub-project has pyproject.toml",
    "detects multiple sub-project languages in a monorepo layout",
    "detects Java sub-project (pom.xml) in monorepo",
    "does not add duplicate languages when sub-project matches root language",
    "uses Language: from IDENTITY.md when file is present",
    "normalizes TypeScript (ESM) to typescript",
    "normalizes Python 3 to python",
    "normalizes Go to go",
    "normalizes Rust to rust",
    "IDENTITY.md takes precedence over heuristic (Cargo.toml present but IDENTITY says typescript)",
    "detected identity defers to current repository evidence",
    "explicit config override wins over user identity and repository evidence",
    "falls back to heuristic when IDENTITY.md is absent",
    "falls back to heuristic when IDENTITY.md has no Language: line",
    "falls back to heuristic when IDENTITY.md has unrecognized language",
    "handles readFileSync error on IDENTITY.md gracefully"
   ],
   "core/state-paths.test.ts": [
    "falls back to os.homedir()/.deckent when no env and no projectRoot (global-install fallback)",
    "resolves to <projectRoot>/.deckent when projectRoot is given and no env override (current convention)",
    "DECKENT_HOME env override beats projectRoot",
    "DECKENT_HOME env override beats the global-install fallback when no projectRoot given",
    "treats an empty-string DECKENT_HOME as unset (falls through to the next precedence tier)",
    "falls back to os.homedir()/.brain when no env and no projectRoot (global-install fallback)",
    "resolves to <projectRoot>/.brain when projectRoot is given and no env override (current convention)",
    "BRAIN_HOME env override beats projectRoot",
    "BRAIN_HOME env override beats the global-install fallback when no projectRoot given",
    "DECKENT_HOME and BRAIN_HOME are independent overrides",
    "joins segments onto the resolved deckent home (project-local branch)",
    "joins nested segments onto the resolved deckent home (env-override branch)",
    "joins segments onto the global-install fallback when no projectRoot given",
    "returns the bare resolved home when no segments are given",
    "joins segments onto the resolved brain home (project-local branch)",
    "joins segments onto the resolved brain home (env-override branch)",
    "joins segments onto the global-install fallback when no projectRoot given",
    "picks up a DECKENT_HOME set AFTER the module was already imported",
    "picks up a BRAIN_HOME set AFTER the module was already imported",
    "delegates to path.win32.join when that is what node:path resolves to"
   ],
   "core/stats-sidecar.test.ts": [
    "RED: saveAgent(agent) (the OLD finalizer call) DOES rewrite agent.json — proves the bug was real",
    "GREEN: saveAgentStats(id, stats) (the NEW finalizer call) leaves agent.json byte-identical",
    "GREEN (skills): saveSkillStats(id, stats) leaves manifest.json byte-identical",
    "getAgent() returns sidecar stats when present, overriding the stale manifest value",
    "getAgent() falls back to manifest stats when the sidecar has no entry for this id (migration-friendly, consumer regression-pin)",
    "getSkill() unified read mirrors the agent behavior",
    "a finalizer-style read-then-write carries the manifest's prior stats forward on the FIRST sidecar write",
    "writing skill stats does not clobber a co-resident agents key, and vice versa",
    "sidecar write is atomic — no leftover .tmp file after a successful write",
    "loadAgents()/loadSkills() work fine with no .deckent/stats/ directory at all (fresh checkout)"
   ],
   "core/subscription.test.ts": [
    "returns detected: max when opus probe succeeds",
    "returns detected: pro when opus probe fails with non-zero exit",
    "returns detected: unknown when claude CLI is not found",
    "returns detected: unknown when claude CLI throws on --version",
    "returns detected: unknown with method: timeout when probe times out",
    "returns detected: unknown with method: timeout when signal is SIGTERM",
    "returns detected: unknown with method: error on non-timeout spawn error",
    "includes a valid ISO testedAt timestamp",
    "returns null when subscription is unknown",
    "returns null when max subscription uses max_plan (compatible)",
    "returns null when pro subscription uses pro_plan (compatible)",
    "warns when pro subscription uses max_plan (incompatible)",
    "warns when pro subscription uses max5x_plan (incompatible)",
    "gives an upgrade note when max subscription uses pro_plan",
    "returns null for api mode regardless of subscription",
    "creates config file with subscription when no existing file",
    "merges subscription into existing config",
    "overwrites existing subscription field",
    "handles corrupt existing config gracefully"
   ],
   "core/summary-debt-filter.test.ts": [
    "excludes resolved debts from Active Technical Debt section",
    "includes non-resolved debts regardless of exact status value",
    "does not contain the broken .brain/DECISIONS.md reference"
   ],
   "core/system-capacity.test.ts": [
    "returns 1 for systems with less than 4GB RAM",
    "returns 2 for systems with 4-8GB RAM",
    "returns 3 for systems with 8-16GB RAM and <8 cores",
    "returns 4 for systems with 8-16GB RAM and >=8 cores",
    "scales with cores for >16GB RAM, capped at 8",
    "returns at least 2 for >16GB even with few cores",
    "handles edge case: exactly 4GB",
    "handles edge case: exactly 8GB",
    "is the same function reference as suggestMaxWorkersFromCapacity",
    "produces identical results to suggestMaxWorkersFromCapacity",
    "returns subprocess for Windows",
    "returns docker when Docker is available on Linux",
    "returns docker when Docker is available on macOS",
    "returns subprocess when Docker is not available on Linux",
    "returns a valid SystemCapacity object"
   ],
   "core/system-profile.test.ts": [
    "returns correct value for normal conditions (8192MB free, 8 cores)",
    "caps at 30 for high free RAM and many CPU cores",
    "returns 1 minimum for very low free RAM (50MB)",
    "handles low free RAM (2GB = 2048 MB)",
    "handles high CPU (32 cores) with moderate RAM (8GB)",
    "returns 1 for single-core machine",
    "applies 30 cap regardless of RAM amount",
    "handles exactly 400MB free (boundary)",
    "handles 16GB free with 4 cores",
    "handles 2-core machine with enough RAM",
    "returns correct profile for 8-core, 16GB total, 8GB free",
    "returns correct profile for low RAM (2GB free)",
    "caps recommendedMaxWorkers at 30 for high-end machine",
    "returns minimum 1 worker for very low free RAM (50MB)",
    "profile has all required fields",
    "returns minimum 1 worker for single-core machine",
    "uses os API to gather system data"
   ],
   "core/task-166-005-docs-identity.test.ts": [
    "live .deckent/settings/docs.json lists no host adapter file",
    "seed docs.json.template propagates no host adapter file to new projects",
    "PostFinalizeHookOptions.skipIdentityRegen is annotated as deprecated in source"
   ],
   "core/task-artifact-classifier.test.ts": [
    "creates identity only from an exact active task filename and matching validated record",
    "rejects path-like input without relying on host path semantics: %s",
    "accepts exact canonical task-record filenames",
    "rejects every sidecar artifact filename"
   ],
   "core/task-attempt-custody-posix-adapter.test.ts": [
    "reads files, directories and scans without privacy writes; writer traversal still applies privacy",
    "measures current repeated-read traversal amplification by directory depth without weakening fences",
    "does not create or chmod a missing directory on a read",
    "shares one domain-separated semantic Docker label digest across custody boundaries",
    "creates a missing private root hierarchy from a native-pinned existing ancestor",
    "reopens a recursively created hierarchy through the native root-handle contract",
    "fails honestly when the canonical loader or effective probe cannot prove custody",
    "uses the pinned native directory handle for bounded sorted dispatch discovery",
    "keeps a private directory identity stable while its child namespace changes",
    "wires Store admission, durable markers, first-writer publication and verified reads",
    "runs the real Store provider-stream CREATE/APPEND/PUBLISH lifecycle and reads it back",
    "maps native symlink, special-file, hard-link, oversize and inode-swap evidence fail closed",
    "keeps publication authority opaque, exact-scope and terminally single-use",
    "pins Set replay intrinsics against hostile post-import prototype mutation",
    "preserves post-effect seal uncertainty through native reconciliation",
    "returns typed HOLD when no bounded mount consumer exists and never fabricates CONSUMED",
    "bounds path exposure to an injected callback and revalidates pinned native handles",
    "turns callback-time directory substitution into monotonic cleanup uncertainty",
    "fails closed when the pinned project identity changes after the Docker callback",
    "accepts canonical uint64 maxima for mutable output mount identity fields",
    "keeps valid mount custody independent of hostile post-callback intrinsic mutation",
    "rejects a stateful frozen cleanup-to-consumed forgery and leaves no mount OUTCOME",
    "rejects callback object replay and preserves native close uncertainty"
   ],
   "core/task-attempt-custody-store.test.ts": [
    "canonicalizes JSON independently of object key order and rejects non-JSON/bounded overflow",
    "matches a hard-coded canonical byte and domain-separated digest golden vector",
    "derives Docker mount transfer evidence deterministically from every exact evidence field",
    "rejects caller-authored mount summaries and incomplete or malformed Docker evidence",
    "rejects sparse, accessor, symbol and extra-property array/object collisions",
    "derives a stable, domain-bound policy digest regardless of map insertion order",
    "keeps frozen artifact authority stable under exported and Array prototype mutation",
    "fails closed on impossible metadata, artifact and JSON policy bounds",
    "rejects unknown, symbol and accessor fields in nested policy records",
    "rejects unsafe cross-platform relative path %s",
    "binds one Store to the exact canonical project root and project id",
    "rejects Proxy/accessor authority before reflection and captures adapter methods once",
    "classifies malformed post-create directory evidence as CREATE_UNCONFIRMED",
    "rejects uppercase UUID aliases at the identity boundary",
    "requires durable admission before any attempt artifact",
    "creates admission first-writer state idempotently and binds root capability proof",
    "rejects publication/readback physical proof substitution",
    "rejects accessor adapter envelopes and exposes only one opaque mount lease",
    "returns only finite frozen HOLD truth and never exposes a raw adapter cause",
    "maps every adapter read primitive to cause-less finite HOLD truth",
    "uses conservative typed HOLDs for post-effect readback failures",
    "rejects foreign and stale-generation mount authority before adapter consumption",
    "moves a mount lease to CONSUMING before await and never reopens on rejection",
    "keeps cleanup-uncertain mount consumption terminal",
    "persists mount intent before issue and never mints a replacement lease",
    "blocks restart reissuance when an issued opaque mount lease is abandoned",
    "binds mount receipt evidence durably and rejects same-store and restart reissuance",
    "terminalizes malformed mount transfer receipts and never replays them after restart",
    "rejects forged, extra, missing and null-on-success mount receipt snapshots",
    "rejects a self-consistent mount receipt bound to another generation",
    "keeps exact mount transfer success in reconciliation when OUTCOME durability fails",
    "rejects a self-consistent OUTCOME carrying substituted transfer evidence",
    "accepts an identical artifact replay and rejects conflicting first-writer bytes",
    "persists a canonical zero-byte staged chunk only when policy explicitly permits it",
    "keeps execution-effect evidence host-only and rejects arbitrary artifact classes",
    "publishes and reads only an exact committed effect-landing receipt",
    "publishes a host-authoritative IPC answer separately from exact worker delivery bytes",
    "probes an exact worker-output question without minting authority",
    "reconstructs IPC answer delivery after restart and keeps same-sequence replay idempotent",
    "does not replay an earlier durable effect when a later sequence carries identical bytes",
    "owns a durable contiguous IPC cursor across answer, restart, and the next sealed question",
    "rejects IPC gaps, duplicate open questions, reused source epochs, and stale answer publishers",
    "rejects deleted cursor prefixes, later-record gaps, and answered records without their open record",
    "settles the bounded final IPC sequence without exposing or publishing sequence max plus one",
    "does not recover cursor authority from first-writer bytes without a durable publication outcome",
    "binds fixed worker destination and immutable artifact key to the exact IPC sequence",
    "does not turn partial IPC answer publication into delivered truth after restart",
    "keeps a delivered destination in reconciliation when its durable receipt is unconfirmed",
    "keeps caller bytes authoritative against Proxy, SAB, subclass and adapter mutation",
    "rejects exact-prototype byte authority overrides without executing accessors or effects",
    "requires separate reconciliation authority for an exact intent-only restart",
    "never replays a mismatched durable effect marker",
    "does not revive an opaque publication session from durable markers after restart",
    "exposes verified bytes while mount authority stays adapter-owned and opaque",
    "captures and verifies exact worker landing and provider-observation attempt outputs",
    "binds each output source to one exact class and key with receipt-equal replay only",
    "rejects replay of a process-local output source after Store restart",
    "preserves exact %s adapter truth when publication custody cannot begin",
    "rejects adapter publication-token reuse and terminalizes the original wrapper",
    "seals a bounded Store-owned pristine stream into an exact artifact receipt",
    "binds append and abort outcomes to exact adapter receipt evidence",
    "rejects stale append and abort receipts without reopening publication custody",
    "fences reentrant append, seal and abort calls before every adapter effect",
    "preserves exact %s terminal seal disposition without abort guessing",
    "rejects a stream adapter that seals different bytes with the same length",
    "keeps a stream PUBLISHED_UNCONFIRMED when its durable receipt cannot be published",
    "never accepts a receipt file whose durable OUTCOME persistence failed",
    "terminally aborts adapter custody for %s before returning replay HOLD",
    "snapshots the stream seal descriptor once and aborts accessor substitution",
    "makes explicit stream abort monotonic and idempotent",
    "makes append uncertainty abort-only and never retries the append effect",
    "passes an isolated stream chunk copy and terminalizes adapter mutation",
    "persists stream publication outcome before any receipt publication",
    "does not abort or reseal after a namespace-effect seal failure",
    "keeps the outer stream terminal when adapter abort cleanup cannot be confirmed",
    "binds artifact class to its only valid capture mode and rejects laundering",
    "aborts an overflowing stream so a caller cannot seal a truncated prefix",
    "rejects an unissued capture capability and an artifact timestamp before admission",
    "revokes an opaque capability object if an adapter reuses it for another output path",
    "revokes the original access when its capability object is reused across access classes",
    "rejects sibling generation admission replay",
    "requires the persisted exact N-1 admission instead of an arbitrary digest",
    "rejects cross-task, cross-attempt and skipped-generation predecessor identities",
    "requires monotonic admission time across generation lineage",
    "builds an exact accepted-result to archive predecessor chain",
    "reads each predecessor once at the late validation boundary without an exponential traversal",
    "holds when current artifact IO changes the predecessor %s by %s before late validation",
    "does not reuse predecessor authority across top-level reads after tampering",
    "rejects accepted-result bindings with a foreign attempt, landing transaction, or chain",
    "rejects a chain stage with the wrong predecessor or artifact class",
    "rejects accepted-result before the committed effect-landing chain exists",
    "rejects a stage artifact captured before its predecessor chain event",
    "fails closed when durable admission bytes change behind their proof",
    "distinguishes true absence from orphaned admission and artifact publication",
    "detects a missing earlier chain stage when a later durable stage exists",
    "parses only an explicit frozen historical V1 sentinel",
    "atomically reserves canonical dispatch identity and reopens same request after restart",
    "discovers the bounded canonical dispatch directory and semantically rereads every admission",
    "preserves reserved-pending-admission during bounded restart discovery",
    "reconciles a current reservation crash from the host-staged task snapshot",
    "retires one legacy reservation before admission with append-only exact recovery authority",
    "isolates an identity-bound unreadable admission during recovery without trusting or suppressing its valid sibling",
    "quarantines only an old-epoch admitted no-effect terminal with append-only recovery authority",
    "distinguishes malformed and hash-tampered dispatch discovery candidates",
    "fails dispatch discovery closed on missing native scan and mutated scan receipts",
    "rejects extra-key, accessor and proxy authority at every dispatch ingress",
    "publishes and restart-rereads path-free immutable dispatch observations by class",
    "keeps observation bytes-without-receipt as HOLD and exact retry completes it",
    "keeps observation claim-without-bytes as HOLD and only exact retry may complete",
    "resolves dispatch observation first-writer races and rejects forged ingress",
    "leaves a typed pending reservation on admission interruption and completes exact retry",
    "recovers exact material-first crash but exposes the orphan as HOLD to ordinary readers",
    "enforces predecessor monotonicity and one first-writer child generation slot",
    "rejects PROVIDER_START while mount outcome is confirmed but RELEASED is pending",
    "settles RELEASED only from exact mount outcome and durable gated release evidence",
    "retains committed-journal release-pending truth with complete immutable inventory",
    "rejects partial artifact publication and binds recovery time after committed evidence",
    "refuses committed release-pending disposition when forbidden %s directory exists empty",
    "requires an exactly empty chain and exact structural evidence refs",
    "rejects stale/sibling authority, provider-observation substitution and disposition conflicts",
    "keeps release diagnostics in a distinct immutable Store slot",
    "roundtrips real Store domain-separated EFFECT_DIAGNOSTIC through the Docker publisher and reader",
    "keeps sibling and byte-tampered diagnostics on HOLD through the real Store boundary",
    "rejects a cryptographically intact diagnostic carrying a sibling identity",
    "preserves READY lifecycle artifact bytes and a real diagnostic in a failed-unlanded candidate",
    "partial retention custody preserves usage and blocks resurrection and competing dispositions",
    "retains started failure without erasing usage or reclassifying RELEASED as no-effect",
    "refuses started-failed disposition even for an empty %s directory",
    "rejects stale authority replay, foreign identity, corrupt evidence and an old custody epoch",
    "does not invent started-failed authority from a reservation without provider execution",
    "rejects semantically invalid provider exit despite a valid observation receipt (%s)",
    "settles proven NOT_DISPATCHED at zero public attempts and blocks later mount",
    "lets Docker reserve generation+1 only from the exact durable NOT_DISPATCHED receipt",
    "arbitrates NOT observation versus MOUNT claim with one immutable winner",
    "arbitrates MOUNT terminal check versus NOT claim without publishing mount intent",
    "reports MOUNT claim crash as transition-pending and exact retry writes intent",
    "reports NOT claim crash as transition-pending and exact retry terminalizes it",
    "keeps AMBIGUOUS nonterminal and permits one exact later RELEASED transition",
    "retains committed-journal release-pending truth with complete immutable inventory",
    "retains started failure without erasing usage or reclassifying RELEASED as no-effect",
    "propagates a typed platform capability HOLD instead of fabricating support",
    "reuses immutable admission facts only within an operation and rechecks native evidence",
    "rejects changed evidence even when a cached semantic result would match",
    "rechecks absence when another Store publishes before the operation ends",
    "cannot hide a mutation attempt by catching its error",
    "bounds retained memory and rejects asynchronous or nested operations",
    "does not reuse a policy or sibling identity and detects a removed durable marker",
    "keeps the same Store mutation fence active during final native rereads"
   ],
   "core/task-attempt-custody-win32-adapter.test.ts": [
    "implements the complete Store adapter contract as a Windows authority",
    "fails closed without a canonical Win32 native capability and never calls the mount consumer",
    "contains no POSIX proof fallback or serializable path/descriptor mount authority"
   ],
   "core/task-execution-admission.test.ts": [
    "__hard_crash_after_dispatch_receipt__",
    "orders durable boundary preparation before the sole sync dispatch",
    "adopts an already-dispatched execution without calling process hooks",
    "replays from caller-verified durable evidence without a second process",
    "leaves a recovery-locatable in-flight boundary after a real process crash",
    "creates zero processes and retains quarantine when preparation fails",
    "never turns a throwing process callback into an ordinary retry",
    "quarantines a returned dispatch when its durable projection fails",
    "refuses completion without durable dispatch evidence and blocks retry",
    "requires the caller to re-read and verify durable dispatch evidence",
    "reserves one bounded evidence slot for a post-boundary failure",
    "admits the maximum reserved evidence shape and completes with 16 refs",
    "rejects request evidence over the reserved count before any process hook",
    "rejects empty adoption evidence without calling recovery or process hooks",
    "rejects a thenable from the sync process hook and quarantines it",
    "consumes a rejected Promise returned by a sync hook before quarantining",
    "uses an immutable request snapshot when caller-owned input mutates",
    "surfaces terminal projection cleanup uncertainty as dispatched",
    "reports project maintenance as a pre-process hold",
    "supports async preparation and dispatch under one heartbeat authority",
    "shares one heap-scheduled timer across concurrent async admissions",
    "removes a stopped heap root and rebinds the sole timer to the next admission",
    "verifies async adoption from durable evidence without starting heartbeat work",
    "settles async adoption against an immutable revalidation evidence snapshot",
    "does not adopt after heartbeat authority fails during verification",
    "refreshes a post-commit renewal handle before quarantining heartbeat fault",
    "records heartbeat failure even when the renewal seam throws undefined"
   ],
   "core/task-execution-fence.test.ts": [
    "publishes an exact schema-v3 document with a durable compound fence",
    "initializes exact DB v4 quarantine tables, indexes, and guards",
    "transactionally migrates v3 boundary authority and audit bytes to v4",
    "keeps migration, projection, and quarantine reconciliation bounded by keyset pages",
    "keeps normal task renewal and release task-scoped with more than a thousand unrelated canonical rows",
    "transactionally migrates real v2 payloads into deterministic audited quarantine",
    "rolls back the entire v2 migration when any legacy row is malformed",
    "uses a unique owner generation and rejects concurrent task re-entry",
    "renews only the owning generation and preserves immutable metadata",
    "releases only the owning generation",
    "commits an in-flight boundary before side effects and never lease-retires it",
    "requires the exact live owner and exact fence to renew and complete in-flight work",
    "refuses exact active-generation adoption when expired owner liveness is %s",
    "durably adopts and exactly replays a dead active generation before atomic no-change settlement",
    "resolves active adoption from canonical current authority after projection publication crashes",
    "exposes a bounded oldest-to-current active adoption lineage across repeated restarts",
    "rejects an active owner/fence transfer without its exact durable adoption audit",
    "refuses active-generation adoption after an irreversible boundary exists",
    "refuses expired in-flight boundary adoption when owner liveness is %s",
    "fence-transfers only proven-dead in-flight work and replays its resumed terminal chain",
    "resolves the final boundary resume hop after canonical commit and projection crash",
    "atomically settles and exactly replays a durable no-change boundary",
    "keeps a committed completion authoritative when projection cleanup is uncertain",
    "monotonically quarantines failures and recovers only verified exact attestations",
    "resolves exact recovered authority with resume=%s, without accepting sibling generations",
    "rejects a structurally valid but unproven recovered audit: %s",
    "does not reinterpret successful completion as recovery",
    "keeps a committed recovery authoritative when projection cleanup is uncertain",
    "never unlinks a successor projection after the released DB commit",
    "surfaces a committed release when lock-directory projection cleanup loses its generation",
    "never recovers a non-expired lock even when a probe reports dead",
    "recovers an expired generation only after its owner is proven dead",
    "preserves an expired generation when owner liveness is %s",
    "fails closed for malformed lock metadata and exposes malformed inspection state",
    "fails closed for a malformed per-task mutation guard",
    "fails closed on crashed temp/sidecar artifacts and leaves no unbounded artifacts",
    "reconciles canonical DB authority after projection loss without reusing a fence",
    "holds on canonical DB loss and never silently resets the authority epoch",
    "keeps restored-snapshot fences byte-unique and rejects counter replay",
    "holds on DB/sentinel epoch disagreement",
    "keeps cross-namespace mount ids observational and can audit an explicit metadata adoption",
    "refuses mount adoption while a canonical execution is active",
    "rejects a second authority bootstrap after the bound lock directory is replaced",
    "rejects a second authority bootstrap after the bound project directory is replaced",
    "surfaces a committed renewal when projection repair loses the lock-directory generation",
    "surfaces a committed renewal after exact projection reconciliation",
    "rejects hardlinked DB, sentinel, and projection authority files",
    "burns the fence and compensates the exact DB owner on projection publish failure",
    "never unlinks a successor projection after the compensated DB commit",
    "reclaims an exact-owner crashed staging file only after owner death proof",
    "serializes mutations across processes with BEGIN IMMEDIATE",
    "provides project-wide maintenance admission and ownerPid authority",
    "rejects a maintenance actor on a non-maintenance projection",
    "rejects a non-maintenance actor on the maintenance projection",
    "returns typed foreign-host HOLD unless a distributed probe proves dead",
    "fails closed when .locks is a symlink/junction",
    "fails closed on symlink and oversized projection entries",
    "preserves a completed value as typed uncertainty when quarantine persistence is unreadable",
    "aborts cooperatively and reports uncertainty when heartbeat quarantine cannot persist",
    "labels a completed outcome quarantined only after exact durable re-read",
    "heartbeats a long operation and releases the lock after natural completion",
    "exposes the renewed live lock handle to the protected operation",
    "releases its generation when the protected operation throws",
    "validates task, lease, and heartbeat inputs before publishing authority"
   ],
   "core/task-lineage.test.ts": [
    "folds original → fix → fix-fix into one logical task resolved by the leaf",
    "projects a pending fix as the root task current state, not a second task",
    "selects each admitted fix depth exactly once and waits for its parent terminal state",
    "does not select a parallel repair that is outside the causal head",
    "derives attempt depth and bounded ancestors from explicit parent authority",
    "uses one logical-tip settlement projection for repair and dispatch outcomes",
    "decides only queued or active descendants of the accepted settled repair",
    "does not let a stale leaf replace accepted settlement or reopen the root",
    "projects NOT_DISPATCHED into resumable, exhausted, and dependency-starved states",
    "does not pause $unresolvedTasks/$totalTasks before the count threshold",
    "evaluates five unresolved roots against the independent ratio gate for $totalTasks tasks",
    "pauses below the ratio gate when an exhausted lineage still blocks unfinished dependants",
    "reports transitive blockage as an edge to the failed ROOT, not the intermediate",
    "never pauses through the configured thresholds when the policy is disabled",
    "does not pause a large run merely because five roots failed when the ratio is low",
    "uses effective-config thresholds rather than a fixed five",
    "excludes repaired roots while retaining an unresolved FIX lineage and its blocked dependant"
   ],
   "core/task-output-projection.test.ts": [
    "strips pristine content and capability from public metadata without changing source digests",
    "sanitizes the complete sealed stream before tailing a private key block",
    "evicts whole lines by byte budget and reports omissions, never partial words",
    "preserves surrogate pairs across bounded re-encoding and the natural final line",
    "does not infer a selected attempt or spawn from absent/denied/ambiguous/unreleased states",
    "keeps split UTF8/secret state independent across stdout and stderr; observer EOF is not DONE",
    "does not turn an aborted/source-changed partial word into a completed line",
    "flushes natural terminal units by observed source order, without a stdout-first bias",
    "rejects excessive view requests before custody access and supports a zero-line sealed view",
    "holds invalid/oversized live text after observer cleanup rather than leaking fragments",
    "serializes the two backpressured surface writers and stops on a sink failure"
   ],
   "core/task-output-text-framer.test.ts": [
    "requires a positive byte cap and a caller-owned redaction label",
    "preserves UTF-8 semantic lines across every byte boundary and flushes EOF once",
    "does not emit partial tokens and redacts only after a complete line arrives",
    "snapshots only the bounded pending fragment when the caller reuses its backing buffer",
    "treats only CR immediately before LF as a terminator and visibly escapes bare controls",
    "strips ANSI and OSC controls after shared redaction without changing nonsecret text",
    "uses one caller label for a private-key PEM block across chunks and requires its matching end",
    "recognizes private PEM markers after terminal sanitization but preserves certificates",
    "enforces the cap before emitting an oversized line, including newline and fragmented UTF-8",
    "holds large chunks without buffering the arbitrary input and preserves the hold through end",
    "holds invalid UTF-8 only at a completed line or EOF and never replacement-decodes it"
   ],
   "core/task-output-view-policy.test.ts": [
    "returns bounded view constants and canonical lifecycle defaults without creating files",
    "resolves project-over-global tenant and lifecycle values field by field",
    "refuses inconsistent authored lifecycle timings"
   ],
   "core/task-result-field-order.test.ts": [
    "puts the verdict cluster before assignment plumbing and cost",
    "preserves unknown fields at the end in original relative order, losslessly",
    "serializes to 2-space JSON whose parse round-trips data-equal",
    "order list has no duplicates"
   ],
   "core/task-result-notes-normalize.test.ts": [
    "passes a plain string through unchanged",
    "joins a string array with newlines (live codex shape)",
    "JSON-stringifies non-string array elements",
    "collapses null/undefined to empty string",
    "JSON-stringifies a plain object",
    "returns null unchanged (readJsonSafe miss passthrough)",
    "coerces array notes to a string in place",
    "leaves an already-string notes untouched",
    "preserves legacy command arrays without inventing a successful boolean claim",
    "maps a legacy command array to false when the worker reported NO_GO",
    "isVerificationTask tolerates array notes",
    "evaluateWithRubric returns a decision instead of throwing (live 366-001 shape)"
   ],
   "core/task-result-schema.test.ts": [
    "derives the documented top-level required fields from the executable schema",
    "rejects an empty object and lists every required field as missing",
    "accepts a full valid result and stamps schemaVersion + downstream defaults",
    "preserves explicit NOT_APPLICABLE and actual non-execution independently",
    "defaults schemaVersion to 1.0 when omitted",
    "preserves host-authored claim-time work attribution evidence",
    "preserves HOLD attribution details and rejects malformed attribution digests",
    "preserves provider-native cross-verify evidence through canonical validation",
    "preserves unavailable cross-verify evidence without fabricating a verifier",
    "rejects an unknown selfAssessment verdict (enum-guarded)",
    "treats a malformed nested value as an error, never as a missing field",
    "flags a missing required sub-field with its dotted path",
    "is reachable as the single-source re-export from ./types.js",
    "creates a strict host-bound V2 result without changing V1 behavior",
    "allows legacy attempt projection to differ from custody generation",
    "produces a key-order-stable domain-separated digest",
    "rejects missing V2 identity, binding, or explicit attempt",
    "rejects task/sibling identity mismatch and malformed digests",
    "uses the custody parser byte, UUID and lineage bounds without a weaker schema copy",
    "recomputes the host-assembled V1 promotion digest instead of trusting a supplied marker",
    "rejects nested unknown fields instead of silently stripping them",
    "refuses a worker-supplied custody binding or non-canonical V1 source"
   ],
   "core/task-result-settlement.test.ts": [
    "provides a deterministic read-only backend authority inspection",
    "publishes one private content-addressed prompt outside the worker project",
    "publishes an immutable private work-attribution baseline under the exact attempt",
    "fails prompt overwrite and detects byte or permission drift on read",
    "publishes one private content-addressed execution contract and rejects drift",
    "persists immutable host-owned execution budget authority for restart recovery",
    "derives immutable actual-call and terminal-usage receipts from exact host evidence",
    "derives a subscription actual-call proof from provider-reported usage, forging no usd",
    "refuses a subscription actual-call proof when provider usage is absent",
    "holds the subscription actual-call proof when terminal usage exceeds the owner ceiling",
    "settles docker terminal usage only when the contract carries the runtime-canonical budgetFingerprint (Gate B)",
    "resolves the durable claim fence unchanged across settlement closure (verdict-receipt path)",
    "refuses actual-call evidence when the exact contract model is absent from the envelope",
    "persists an exact pending attempt before publishing an immutable embedded result",
    "rejects wrong-task, forged-path and cross-project authorities",
    "fails closed when host state resolves inside the worker-mounted project",
    "detects embedded-result tampering instead of trusting outer metadata",
    "exposes a product result only after a matching lifecycle closure",
    "fails loudly on existing corrupt or digest-mismatched closure evidence",
    "derives daemon-global names and labels from project, task and attempt authority",
    "serializes same-task attempts through an append-only settlement/closure claim chain",
    "resolves the active or latest closed project/task authority without in-memory state",
    "keeps latest-authority lookup project-scoped and fails loud on corrupt chain evidence",
    "rejects a claim whose durable attempt evidence is missing or corrupt",
    "requires a durable attempt before claim and binds dispatch to immutable prepared metadata",
    "persists exact-attempt terminal provider billing outside the project with first-writer authority",
    "enumerates only project-scoped lifecycle-pending attempts and fails loud on corrupt records",
    "retires LANDED without a terminal result and opens one same-task continuation slot",
    "fails the claim chain closed when external LANDED retirement evidence is tampered",
    "creates, parses, digests and independently verifies the full archive chain",
    "keeps V1 historical parsing separate from V2 normal parsing",
    "rejects result tampering, sibling identity replay and unknown fields",
    "rejects a broken evaluation predecessor instead of selecting another attempt",
    "rejects a fully self-consistent archive tail that was persisted only in another store",
    "rejects settlement time before the persisted finalizer tail",
    "refuses to persist an archive artifact captured before the settlement chain event",
    "requires exactly one archive authority reference to the exact settlement digest",
    "keeps settlement digest stable across object key order"
   ],
   "core/task-result-write-authority.test.ts": [
    "publishes canonical bytes atomically and is restart-readable/idempotent",
    "preserves first-writer authority and reports conflicting content",
    "rejects wrong identities and non-canonical, invalid UTF-8/JSON, and oversize bytes",
    "fails closed for unsupported values rather than JSON.stringify normalization",
    "rejects schema defaults and unknown properties instead of silently normalizing them",
    "does not publish when schema validation or Windows adapter publication fails"
   ],
   "core/task-settlement-authority.test.ts": [
    "declares a deterministic pre-dispatch receipt across restart/idFactory drift",
    "keeps production ledger and snapshot verification behind a frozen factory facade",
    "dry-runs by default, then atomically settles a typed pre-dispatch rejection",
    "settles an abandoned dispatch as manual_review_required and replays idempotently",
    "refuses while the real backend adapter still reports runtime liveness",
    "holds instead of settling when the backend probe is %s",
    "refuses an eventless receipt — that shape belongs to settleNotDispatched",
    "refuses when the operator attestation does not bind this evidence snapshot",
    "captures the default settlement timestamp once for both atomic events",
    "preserves a stored rejection timestamp when appending its consumer settlement",
    "rejects an invalid settlement timestamp before probing or mutation",
    "never converts an explicit missing receipt reference into a legacy settlement",
    "requires exact freshly-probed legacy attestation and persists only hashes",
    "fails closed before mutation when no current task snapshot verifier is available",
    "allows only an explicit receipt-backed ephemeral one-shot before Task JSON publication",
    "durably retains exact backend zero-work evidence in the invocation settlement",
    "production authority rejects task bytes changed while the absence probe is pending",
    "revalidates the task snapshot inside the receipt transaction precondition",
    "atomically loses legacy reconciliation when a dispatch receipt wins the race",
    "rejects a NOT_DISPATCHED consumer that rewrites rejection cause or evidence",
    "fails closed for %s worker evidence",
    "projects one dispatched terminal, rejects contradictions, and replays idempotently",
    "opens an absent receipt store read-only without creating state",
    "bulk-projects mixed tenants in deterministic input order",
    "validates bulk projection requests identically when the store is absent",
    "production probe uses injected async process/backend adapters and canonical evidence",
    "detects canonical task-%s artifacts as present",
    "keeps complete Linux absence evidence stable across unrelated process and race churn",
    "reports any task-matching Linux worker as present with identity-bound evidence",
    "fails closed when any enumerated Linux process is unreadable",
    "fails closed when Linux process enumeration exceeds its configured bound",
    "fails closed when Linux process enumeration errors with %s",
    "accepts only the exact persisted attempt, custody ref and settlement digest",
    "never reads or enriches normal Docker authority from a public result",
    "returns HOLD when the bound evaluation artifact is malformed",
    "returns HOLD when the bound finalizer artifact bytes mismatch their receipt",
    "returns typed HOLD for stale, sibling and digest-spoofed authorities",
    "does not fall back to a public result when normal Docker custody is missing",
    "preserves no-dispatch as strict zero-attempt truth",
    "reads public compatibility only behind the explicit legacy non-Docker mode",
    "accepts the source-bound TaskResultV2 before evaluation and finalization",
    "does not read evaluation or finalizer authority at the collector boundary",
    "returns typed HOLD for a sibling accepted-result ref"
   ],
   "core/task-terminal-outcome.test.ts": [
    "maps %s to %s",
    "does not mutate for a non-terminal assessment"
   ],
   "core/task-terminal-projection.test.ts": [
    "projects winning attempt result %s",
    "preserves gate distinction %s",
    "accepts only the selected attempt winner",
    "fails closed for generation %s",
    "is replay-stable and never lets a stale result or gate retract terminal truth",
    "rejects foreign logical-task evidence without mutation"
   ],
   "core/task-types.test.ts": [
    "is explicit and frozen",
    "renders criterion identities and evidence slots without splitting punctuation",
    "renders forbidden criteria with an explicit polarity-safe outcome",
    "reads Cursor models live from the registry",
    "exposes TaskEvaluation.DEFERRED alongside legacy members",
    "renders \"Deferred Tasks\" section with correct count in retro markdown",
    "renders singular form when exactly one task is deferred",
    "always renders the section even when zero tasks are deferred",
    "counts only DEFERRED evaluations (cascade-exclusion semantic)",
    "returns deferred=0 when evaluation map is empty or has no DEFERRED"
   ],
   "core/taxonomy-readpath.test.ts": [
    "getById returns all 5 taxonomy columns after insert",
    "getByType returns taxonomy columns for every row",
    "non-ADR / taxonomy-omitted rows return null for all 5 fields (no false data)",
    "filters by adr_class",
    "filters by scope",
    "combines adr_class + scope filters (AND)",
    "omitted filters = unchanged full-type result (regression guard, default behavior)",
    "upsert WITHOUT taxonomy fields preserves existing classification",
    "upsert WITH new taxonomy fields overwrites them explicitly",
    "upsert taxonomy overwrite records field-level history diffs",
    "upsert does not record taxonomy history when fields are simply omitted (protected, not \"changed\")",
    "upsert on a non-existent id (insert path) still writes taxonomy columns"
   ],
   "core/telemetry.test.ts": [
    "is disabled by default",
    "can be created with enabled=true",
    "enable() sets enabled to true",
    "disable() sets enabled to false and clears events",
    "record() does nothing when disabled",
    "record() adds event when enabled",
    "record() adds ISO timestamp",
    "flush() returns events and clears internal list",
    "sanitize strips email-like values",
    "sanitize strips /home/ paths",
    "sanitize strips /Users/ paths",
    "preserves number and boolean properties",
    "getEvents() returns readonly array",
    "record() with empty properties works"
   ],
   "core/tenant-context.test.ts": [
    "accepts simple alphanumeric id",
    "accepts id with hyphens",
    "rejects id with path traversal",
    "rejects id with uppercase letters",
    "rejects id with slashes",
    "builds correct isolation path for default local tenant",
    "builds correct isolation path for custom tenant",
    "throws on invalid tenantId",
    "returns local tenant by default",
    "reads tenantId from opts",
    "reads tenantId from DECKENT_TENANT_ID env var",
    "opts tenantId takes priority over env var",
    "throws on invalid tenantId from opts",
    "createdAt is a valid ISO 8601 string"
   ],
   "core/tenant-runtime.test.ts": [
    "inner fn sees the correct tenantId",
    "after scope exits, tenantId is no longer set in ALS",
    "nested withTenant overrides the outer tenant",
    "throws on invalid tenantId",
    "returns local tenant by default (outside withTenant)",
    "returns active tenant inside withTenant scope",
    "resolves relative path under current tenant isolation root",
    "defaults to local tenant when called outside withTenant scope"
   ],
   "core/term-rpc.test.ts": [
    "a valid request round-trips through serialize -> parse losslessly",
    "a valid response round-trips through serialize -> parse losslessly",
    "rpcResponseSchema rejects a response carrying both result and error",
    "rpcResponseSchema rejects a response carrying neither result nor error",
    "parseRpcRequest never throws on malformed JSON and reports an error",
    "parseRpcRequest reports validation errors for a structurally invalid request",
    "TERM_RPC_METHODS is exactly the 7 v1 catalog entries",
    "TERM_RPC_METHOD_SCHEMAS has exactly one entry per catalog method (compile-time Record<> sync)",
    "isTermRpcMethod is true for a catalog method and false for an arbitrary string",
    "rejects a version mismatch with VERSION_MISMATCH, without invoking any handler",
    "rejects an unknown method with UNKNOWN_METHOD",
    "rejects invalid params for a known method with INVALID_PARAMS",
    "reports METHOD_NOT_IMPLEMENTED when the injected handler map has no handler for a known method",
    "wraps a thrown handler exception as INTERNAL_ERROR without escaping the dispatcher",
    "dispatches session.list to its fake handler and returns a schema-valid result",
    "dispatches run.start-detached to its fake (async) handler and returns the runId",
    "dispatches approval.decide to its fake handler with validated params"
   ],
   "core/terminal-workline-contract.test.ts": [
    "defaults composer thresholds when workline absent",
    "merges project overrides into paste policy"
   ],
   "core/test-discovery-contract.test.ts": [
    "extracts explicit test targets from scope and proof text once",
    "suppresses a bare-filename anaphor when the qualified path is also planned",
    "keeps a bare filename with no qualified counterpart (genuine root-level plan)",
    "blocks the Run 475 failure class before dispatch",
    "accepts a target matched by the configured runner include",
    "normalizes canonical and scavenged absolute workspace paths before matching",
    "does not mistake coverage.include for the test discovery contract"
   ],
   "core/token-counter.test.ts": [
    "estimates tokens from text using words/0.75",
    "returns 0 for empty string",
    "returns 0 for null/undefined input",
    "returns 0 for whitespace-only string",
    "handles multi-word text correctly",
    "handles text with extra whitespace",
    "estimates full prompt size",
    "handles empty skills array",
    "combines multiple skills",
    "detects when exceeding budget",
    "returns true when within budget",
    "returns true at exact budget",
    "returns false when exceeding budget",
    "returns null when within budget",
    "returns warning when exceeding budget",
    "formats warning message",
    "gets default budget",
    "sets custom budget",
    "custom budgets affect isWithinBudget",
    "accepts custom budgets in constructor",
    "returns context budget estimate with all components",
    "calculates scope tokens using avgTokensPerLine",
    "detects when scope exceeds model budget",
    "handles empty agent prompt and no skills",
    "includes system prompt overhead of 2000 tokens",
    "uses model contextWindow from registry as budget"
   ],
   "core/token-quota.test.ts": [
    "returns false when state is null (no API call observed yet)",
    "returns true when a 429 retry-after is present",
    "returns true when RPM is exhausted (< 2 remaining)",
    "returns false when there is plenty of quota left",
    "returns true when input tokens remaining < estimatedTokens * 1.2",
    "returns 0 when state is null and throttleFloorMs is 0",
    "returns throttleFloorMs when state is null and floor is positive",
    "honours retry-after (seconds) over the floor when bigger",
    "returns the floor when state suggests no backoff",
    "coerces negative throttleFloorMs to 0",
    "returns 0 when nothing is exhausted",
    "returns retryAfter on 429"
   ],
   "core/token-usage-rich.test.ts": [
    "includes cacheWriteTokens when provided (Anthropic cache_creation parity)",
    "clamps negative cacheWriteTokens to 0",
    "clamps NaN cacheWriteTokens to 0",
    "floors fractional cacheWriteTokens to integer",
    "omits cacheWriteTokens from output when not provided (backward-compat sparse output)",
    "includes reasoningTokens when provided (OpenAI o1/o3 reasoning)",
    "includes reasoningTokens when provided (Gemini thoughtsTokenCount)",
    "clamps negative reasoningTokens to 0",
    "clamps Infinity reasoningTokens to 0",
    "floors fractional reasoningTokens to integer",
    "omits reasoningTokens from output when not provided (backward-compat sparse output)",
    "handles both new fields together with all legacy fields",
    "honors provider-reported totalTokens that includes reasoning overhead",
    "satisfies TypeScript TokenUsage type with new optional fields present",
    "new fields default to absent (consumers use ?? 0) — explicit zero passes through"
   ],
   "core/token-usage.test.ts": [
    "fills totalTokens as input+output and defaults source to provider-adapter (goNogo)",
    "defaults every numeric field to 0 for an empty input",
    "defaults every numeric field to 0 when called with no argument",
    "preserves cache read/creation tokens",
    "honors an explicitly provided totalTokens (provider total may exceed input+output)",
    "allows the source to be overridden to tokenizer-fallback",
    "clamps negative and non-finite values to 0",
    "floors fractional token counts to integers"
   ],
   "core/tokenizer-fallback.test.ts": [
    "counts a known-family model externally, marked tokenizer-fallback (spec goNogo)",
    "UNKNOWN model still yields >0 (heuristic, never silent-zero)",
    "empty text → 0 (correct; not forced to ≥1)",
    "scales with text length (longer prompt → more tokens)",
    "is total — never throws on empty/garbage input"
   ],
   "core/tool-allowlist.test.ts": [
    "every kind always grants read/search/planning and NEVER a surface-risk group by default",
    "edit group ⟺ scope declares writable paths (the two-layer interaction) for every kind",
    "execute group depends on taskType, not on scope",
    "documentation and audit never get execute (read/write, no shell)",
    "code-development with writes → exactly the native read/search/edit/execute/plan tools",
    "audit with NO writable path → read-only (4 tools, no edit/execute)",
    "audit WITH a report path → edit surfaces (8 tools, still no execute)",
    "excludes connector / web / mcp / orchestration tools by default",
    "allowed/escalatable are sorted and stable regardless of universe input order",
    "deduplicates repeated tool names in the universe",
    "allowed and escalatable partition the (unique) universe — no overlap, full cover",
    "subtracts agent.deniedTools and never adds tools",
    "unknown agent id with no deniedTools is a no-op",
    "selects from a caller-supplied universe (Task 14 wiring path)",
    "admits a well-formed request for a real, currently-denied tool",
    "rejects a tool that is already allowed",
    "rejects a tool that is not in the universe",
    "rejects an empty or whitespace-only justification before anything else",
    "honors a caller-supplied universe when checking unknown-tool"
   ],
   "core/tool-availability.test.ts": [
    "probes once and memoizes within the TTL window",
    "re-probes after the TTL expires (fake clock)",
    "supports async probes",
    "applies defaultTtlMs when checkAvailability omits ttlMs",
    "caches independently per id",
    "invalidate() forces the next call to re-probe",
    "clear() drops all entries",
    "a fresh project has every tool enabled (empty config)",
    "setToolEnabled(false) disables a tool: isDisabled=true",
    "disable persists across a simulated restart (tmpdir round-trip, no shared instance)",
    "re-enabling removes the id from the disabled set (no unbounded growth)",
    "writes toolsets.json atomically (tmp file never left behind)",
    "a corrupt toolsets.json fails soft to all-enabled",
    "a schema-invalid (but parseable) toolsets.json fails soft to all-enabled",
    "creates the settings directory when missing"
   ],
   "core/tool-catalog.test.ts": [
    "accepts all 5 documented tiers",
    "rejects a value outside the 5-tier set",
    "accepts the base 3 tool-registry levels plus critical",
    "rejects an unknown risk level",
    "accepts all 4 documented sources",
    "rejects an unknown source",
    "classifies a builtin core-7 tool as Core",
    "classifies a project-defined tool as Project",
    "classifies an MCP-seeded tool as MCP",
    "classifies an enterprise-seeded tool as Enterprise",
    "clamps to Danger whenever riskLevel is critical, regardless of source",
    "clamp overrides what would otherwise be the most-trusted tier (builtin)",
    "is a pure function: identical input always yields identical output",
    "derives id from def.name and trustTier via classifyToolTrust (never passed in directly)",
    "lets riskLevel be overridden to critical, which clamps trustTier to Danger",
    "parses its own serialized output back to an equal value",
    "rejects a malformed record (wrong trustTier)",
    "register/get/has/list/size behave as an upserting map",
    "byTrustTier and bySource filter correctly",
    "returns undefined/false for an unknown id without throwing",
    "exposes no call/dispatch/execute/invoke method — pure catalog, no execution"
   ],
   "core/tool-core.test.ts": [
    "derives name/type/optional for each top-level field",
    "returns [] for an empty object schema",
    "returns [] for a non-object top-level schema",
    "labels array/enum/union fields with a simplified type name",
    "never fabricates a field absent from the real schema",
    "produces one {tool, eagerSchema, fullRef} entry per core tool, in coreTools() order",
    "derives eagerSchema from the real paramsSchema, not a hardcoded copy",
    "is a pure derivation over ToolSearchIndex.coreTools() — silently skips absent core names",
    "the eager-7 names are all real, disk-verified tools",
    "is deterministic: repeated calls return identical output",
    "lists every non-core tool, alphabetically sorted, in a single line",
    "reports the correct deferred count in the prefix",
    "returns \"\" when every tool is in the core set",
    "returns \"\" for an empty tool list",
    "is deterministic regardless of input (registration) order",
    "never fabricates a tool name absent from the input list"
   ],
   "core/tool-dispatch.test.ts": [
    "orders safe < moderate < destructive",
    "meets the threshold at and above it, not below",
    "DEFAULT_RISK_THRESHOLD is moderate",
    "unknown_tool: returns status unknown_tool without calling confirm or execImpl",
    "invalid: returns status invalid without calling confirm or execImpl",
    "safe risk skips confirm entirely, even when no confirm is supplied",
    "moderate/destructive risk with confirm=allow runs execImpl and reports executed",
    "confirm=deny denies the call and never runs execImpl",
    "fail-closed: risk meets threshold but no confirm supplied -> denied, execImpl never runs",
    "custom riskThreshold: raising it to destructive lets moderate calls skip confirm",
    "a missing risk on a valid plan defensively falls back to destructive (requires confirm)",
    "a throw from execImpl becomes a structured error, never rethrown",
    "a rejected promise from execImpl becomes a structured error",
    "a non-Error throw is wrapped without fabricating a stack",
    "a throw from confirm becomes a structured error and never runs execImpl",
    "dispatchToolCall itself never rejects even when both seams throw",
    "reports toolName/risk/category/status and a non-negative duration using the injected clock",
    "telemetry.status always matches the top-level result.status",
    "a safe/readOnly tool planned via the real ToolSearchIndex executes without confirm",
    "a destructive tool planned via the real ToolSearchIndex is denied without a confirm seam",
    "an invalid call planned via the real ToolSearchIndex short-circuits before any exec",
    "never touches the registry: dispatching a call has no observable side effect on it"
   ],
   "core/tool-hooks.test.ts": [
    "two separate registries do not share hooks",
    "vetoes dispatch with a reason",
    "short-circuits — a later pre-hook does not run after veto",
    "a non-matching hook is not vetoed against",
    "replaces args when a hook returns args",
    "passes args through unchanged when a hook returns void (observe-only)",
    "chains transforms — each hook sees the previous hook\\'s transformed args",
    "supports async run() implementations",
    "replaces result when a hook returns result",
    "passes result through unchanged when a hook observes only",
    "chains transforms across multiple post-hooks in order",
    "post-hook context receives the original dispatch args",
    "a throwing pre-hook does not kill the pipeline — later hooks still run",
    "records the thrown error with hook name, phase, and toolId",
    "a throwing async pre-hook (rejected promise) is isolated the same way",
    "a throwing post-hook does not kill the pipeline and is recorded in errors",
    "a throwing match() is isolated and treated as non-matching",
    "pre-hooks run in registration order",
    "post-hooks run in registration order",
    "pre and post hooks registered interleaved only run within their own phase, preserving relative order",
    "only invokes hooks whose match(toolId) returns true"
   ],
   "core/tool-registry.test.ts": [
    "contains exactly the tools present in TOOL_CATALOG",
    "contains the known real tool names used as TOOL-2 core-set candidates",
    "never fabricates a tool name absent from the real catalog",
    "validates well-formed params against the registered schema",
    "rejects params with a wrong type",
    "rejects params missing a required field",
    "returns an error for an unknown tool name without throwing",
    "derives destructive from destructiveHint",
    "derives safe from readOnlyHint",
    "defaults to moderate when annotations are absent or empty",
    "prefers destructiveHint over readOnlyHint when both are set",
    "is exposed on the ToolDefinition produced by toolDefinitionFromShape",
    "never invokes anything: handlerRef is a plain string, never a callable",
    "exposes no call/dispatch/execute/invoke method",
    "register upserts rather than throwing on a duplicate name"
   ],
   "core/tool-schema-override.test.ts": [
    "a fresh project has no overrides (empty config)",
    "loads a valid tmpdir tool-overrides.json",
    "a corrupt tool-overrides.json fails soft to empty config",
    "a schema-invalid (but parseable) tool-overrides.json fails soft to empty config",
    "an unknown top-level key fails soft to empty config (strict schema)",
    "is a no-op when the tool has no matching config entry (same reference)",
    "round-trips a tmpdir-loaded config into a visibly-overridden schema",
    "skips an enum override whose values are not a subset of the original enum",
    "skips a default override whose value does not validate against the field schema",
    "ignores an override for a field name that does not exist on the schema",
    "is a no-op on field overrides when paramsSchema is not a ZodObject (fail-soft)",
    "generates once per id and memoizes while the source content is unchanged",
    "regenerates when the source content changes (fake-source hash invalidation)",
    "invalidate(id) forces the next describe() call to regenerate even with the same content",
    "memoizes independently per id",
    "clear() drops all entries (next describe() regenerates) without resetting the generations counter",
    "accepts an injectable hash function (deterministic, no real crypto needed in the test)"
   ],
   "core/tool-scope-gate.test.ts": [
    "flags a symlink inside scope.directories that resolves outside scope as a violation",
    "flags a symlink inside scope.filesWrite that resolves outside scope as a violation",
    "rejects a symlink cycle (ELOOP) as a violation instead of throwing",
    "allows an ordinary in-scope existing path matched via directory",
    "allows a new (non-existent) in-scope path — parent-realpath handling",
    "allows a new (non-existent) file matched via scope.filesWrite exact entry",
    "flags a path genuinely outside assigned scope as a violation",
    "flags every write as a violation when no scope is declared (fail-closed)",
    "defaults to advisory mode when options.mode is omitted",
    "advisory mode never blocks: violation stays true, allowed stays true",
    "enforce mode blocks on violation: allowed mirrors violation",
    "enforce mode still allows an in-scope write",
    "allows a path matched via scope.filesRead",
    "allows a path matched via scope.filesWrite (write implies read)",
    "allows a path matched via scope.directories",
    "flags a path outside declared read scope as a violation (fail-closed)",
    "falls back to process.cwd() when projectRoot is omitted"
   ],
   "core/tool-search.test.ts": [
    "ranks an exact name match above a partial name match and a token-only match",
    "orders same-tier partial matches by overlap ratio, tightest match first",
    "ranks a name-partial match above a description-token-only match",
    "finds a tool via description tokens when the query does not match the name",
    "is deterministic: repeated identical calls return identical order and scores",
    "returns [] for an empty or whitespace-only query",
    "returns [] when nothing matches",
    "respects the limit option and defaults to a bounded result set",
    "never fabricates a tool name absent from the registry",
    "returns the full tool definition, including the real paramsSchema instance",
    "returns undefined for an unknown tool name",
    "labels a valid call with status \"valid\" and the tool risk",
    "labels an invalid call with status \"invalid\", still carrying the risk tag",
    "labels an unknown tool with status \"unknown_tool\" and no risk",
    "never executes anything: planning a destructive call has no observable side effect",
    "returns exactly the eager-7 core set in fixed order",
    "the eager-7 names are all real, disk-verified tools",
    "silently skips a core name absent from a partially-seeded registry",
    "exposes no call/dispatch/execute/invoke method"
   ],
   "core/tool-shadow-policy.test.ts": [
    "accepts all 3 documented sources",
    "rejects an unknown source",
    "defaults to builtin > project > mcp when no config is given",
    "defaults when config.priority is omitted",
    "applies a valid override, flipping the effective order",
    "falls back to default + records an error for a duplicate-source override",
    "falls back to default + records an error for a short (missing-source) override",
    "never throws on a malformed override — fail-honest fallback",
    "parses its own serialized output back to an equal value",
    "round-trips an empty config (priority omitted) identically",
    "resolveShadowPriority is unaffected by round-tripping the config",
    "rejects a malformed record (unknown source in priority)",
    "picks builtin as the deterministic default winner, shadows the other two",
    "is deterministic regardless of input candidate order",
    "honors a config override, flipping the winner to mcp",
    "falls back to default priority + audits the invalid override in priorityResolution.errors",
    "produces no audit entry and one selected candidate for a non-conflicting name",
    "handles multiple independent conflicting names in one call",
    "breaks a same-source tie with first-seen order (stable sort)",
    "returns an empty resolution for no candidates"
   ],
   "core/trace-config-types.test.ts": [
    "is optional and undefined when not set",
    "accepts { enabled: true }",
    "accepts an empty object (default-off)",
    "ResolvedConfig accepts training_trace",
    "is optional and undefined when not set",
    "accepts { enabled: true }",
    "ResolvedConfig accepts live_trace",
    "is optional and undefined when not set",
    "accepts { enabled: true, riskThreshold }",
    "accepts every ToolRiskLevel value",
    "ResolvedConfig accepts tool_surface",
    "is optional and undefined when not set",
    "accepts enabled independently of approvals and bg_turns",
    "accepts approvals: true while enabled is unset (independent sub-flag, 355-011)",
    "accepts bg_turns: true (reserved sub-flag)",
    "accepts all three flags set together",
    "ResolvedConfig accepts repl_surface",
    "is optional and undefined when not set",
    "accepts { enabled: true }",
    "ResolvedConfig accepts deck_broker",
    "is optional and undefined when not set",
    "accepts { enabled: true } independently of rollback_policy",
    "ResolvedConfig accepts rollback",
    "is undefined when the approval block is absent",
    "accepts api_decide alongside the existing rules/gate_enabled/relay_enabled fields",
    "accepts api_decide with no other approval fields set",
    "accepts a config with every new block populated, without throwing",
    "accepts a config with an unrecognized sub-field inside a new block, without throwing",
    "accepts the default config (none of the new blocks set) without throwing"
   ],
   "core/trace-labels.test.ts": [
    "parses all 5 canonical labels",
    "RUN_OUTCOME_LABELS exposes exactly the 5 values, in schema order",
    "rejects an unknown string",
    "rejects non-string input",
    "maps %s -> %s",
    "covers every TaskEvaluationLike value exactly once (exhaustive-mapping proof)",
    "produces a value that is always a valid RunOutcomeLabel",
    "empty sprint (no tasks at all) -> not_dispatched",
    "every task not_dispatched -> not_dispatched",
    "every task success -> success",
    "every task cancelled -> cancelled",
    "every task failed -> failed",
    "cancelled-rate at default threshold (0.5) -> cancelled, checked before failed",
    "failed-rate at default threshold (0.5), no cancellations -> failed",
    "mixed outcome below both thresholds -> partial",
    "tech-debt-only sprint (all partial) -> partial",
    "mostly success with a little tech debt -> partial (not success, since not all succeeded)",
    "respects custom thresholds",
    "parses a valid nonnegative-integer count record",
    "rejects a negative count",
    "rejects a non-integer count",
    "rejects an unknown label key"
   ],
   "core/training-trace-envelope.test.ts": [
    "classifies real legacy record evidence and uses an explicit hint only for ambiguous extracted files",
    "creates deterministic identities from caller-measured digests without needing full file content",
    "preserves real meta lineage and never substitutes sprintId for runId",
    "retains unknown roles and unsupported blocks as typed evidence instead of inventing assistant text",
    "models both OpenAI and Anthropic tool causality and quarantines unmatched results",
    "retains incomplete and duplicate legacy tool causality only as quarantined evidence",
    "carries source quarantine through the canonical gate",
    "requires explicit admission, positive weight, and observed consent when policy asks for it",
    "strictly rejects empty nested objects, digest mismatches, and impossible train-ready combinations"
   ],
   "core/type-cast-safety.test.ts": [
    "returns typed object when JSON matches expected shape",
    "returns null for malformed JSON (no unsafe cast crash)",
    "returns null for non-existent file (no throw)",
    "returns typed array when JSON is an array",
    "TaskStatus enum values are used directly (not string casts)",
    "AlertLevel enum values are used directly in createAlert",
    "DebtPriority enum values match expected strings",
    "handles AgentStatus enum value (no cast needed)",
    "handles plain string status",
    "returns 0 for unknown status",
    "handles EXECUTING status",
    "parses valid priority values correctly",
    "defaults to NORMAL for invalid priority values",
    "handles empty priority column gracefully",
    "accepts all valid DebtPriority values",
    "throws DECKENT_E060 for malformed JSON in task file",
    "throws DECKENT_E061 for missing task file",
    "acquireLock writes valid LockInfo shape",
    "checkLock reads back correct LockInfo shape",
    "checkLock returns null for corrupted lock file",
    "rejects null input",
    "rejects non-object input",
    "rejects missing required fields",
    "validates and returns correct types after cast",
    "rejects invalid model value",
    "rejects non-array triggers",
    "parseCoverageFromVitest returns null for empty input",
    "parseCoverageFromVitest returns null for non-object JSON",
    "parseCoverageFromVitest returns null for invalid JSON",
    "parseCoverageFromVitest correctly parses totals format",
    "parseCoverageFromVitest returns null for object without coverage data",
    "skips malformed heartbeat files without crashing",
    "skips heartbeats with malformed timestamps (no stale marking)"
   ],
   "core/types-edge.test.ts": [
    "should contain all expected values",
    "should have 10 members",
    "should accept valid status in a conditional check",
    "should distinguish terminal states from in-progress states",
    "should use string values (not numeric)",
    "should contain DONE, GO_WITH_TECH_DEBT, NO_GO",
    "should have exactly 5 members (MOAT-3 added NOT_DISPATCHED)",
    "should be usable as switch cases",
    "should allow opus, sonnet, haiku literal values",
    "should correctly assign each model value",
    "should be usable in task model assignment",
    "should narrow correctly in a conditional",
    "should build a minimal valid Task with only required fields",
    "should allow all optional fields to be set",
    "should accept all TaskEffort values",
    "should accept all TaskPriority values",
    "should allow empty scope arrays",
    "should accept DONE selfAssessment",
    "should accept GO_WITH_TECH_DEBT selfAssessment",
    "should accept NO_GO selfAssessment",
    "should allow optional fields to be omitted",
    "should allow optional completedAt and durationMs",
    "should include all three values",
    "should align with TaskEvaluation string values",
    "should validate a minimal DashboardState",
    "should allow optional auditorLastScan and violations",
    "should support nested agents with AgentInfo",
    "should support alerts with different levels",
    "should require all four fields: name, version, description, entrypoint",
    "should hold string values for all fields",
    "should be usable as a value in a Plugin wrapper",
    "should contain all expected values",
    "should have INFO, WARNING, CRITICAL",
    "should have NORMAL, HIGH, CRITICAL",
    "should contain all lifecycle phases",
    "should contain all sprint statuses",
    "should build a valid DebtItem",
    "should allow optional resolvedInSprintId",
    "should hold all numeric fields"
   ],
   "core/types-split.test.ts": [
    "re-exports TaskStatus with all 10 members",
    "re-exports TaskEvaluation with all 5 members",
    "re-exports AgentStatus with 12 members",
    "re-exports AlertLevel with 3 members",
    "re-exports SprintPhase with 10 members",
    "re-exports SprintStatus with 8 members",
    "re-exports DebtPriority with 3 members",
    "ModelType accepts opus/sonnet/haiku",
    "SelfAssessment matches TaskEvaluation string values",
    "PlanMode accepts valid plan names",
    "BrainPlanningMode accepts ai/structured/auto",
    "Task interface can be constructed with required fields",
    "DashboardState can be constructed",
    "SprintResult can be constructed",
    "TaskStatus from task-types.ts === TaskStatus from types.ts",
    "TaskEvaluation from task-types.ts === TaskEvaluation from types.ts",
    "SprintPhase from sprint-types.ts === SprintPhase from types.ts",
    "DebtPriority from sprint-types.ts === DebtPriority from types.ts",
    "AgentStatus from monitoring-types.ts === AgentStatus from types.ts",
    "AlertLevel from monitoring-types.ts === AlertLevel from types.ts",
    "task-types.ts exports Task without depending on sprint or monitoring",
    "sprint-types.ts exports DebtItem independently",
    "monitoring-types.ts exports AgentInfo independently",
    "AutoDocsConfig has tier1/tier2/tier3",
    "SetupRecommendation has all fields",
    "ProjectAnalysis can be constructed",
    "PatternEntry can be constructed",
    "DecayResult can be constructed",
    "BrainContext can be constructed",
    "SprintSizeRecommendation can be constructed",
    "Heartbeat can be constructed",
    "BoundaryViolation can be constructed",
    "LockInfo can be constructed",
    "SkillMeta can be constructed",
    "SubscriptionProfile can be constructed",
    "DoctorResult can be constructed",
    "StartOptions can be empty or fully specified",
    "all enum values are runtime-accessible from types.ts barrel",
    "type-only exports compile correctly (TypeScript-level check)",
    "project analysis type aliases are available"
   ],
   "core/types.test.ts": [
    "PENDING === \"PENDING\" (string serialize)",
    "DOCUMENTING === \"DOCUMENTING\" (Blueprint 5.3)",
    "has 10 members",
    "DRAFT === \"DRAFT\"",
    "has 5 members (incl. NOT_DISPATCHED)",
    "has 3 members",
    "has 10 members",
    "has 8 members",
    "has 12 members (VERIFYING added)",
    "includes DOCUMENTING",
    "has 3 members",
    "accepts autoApprove and sandboxMode as optional booleans",
    "allows empty StartOptions",
    "autoApprove is for permissions — sandboxMode is for Docker (separate concerns)",
    "does not have legacy sandbox field"
   ],
   "core/utils-date.test.ts": [
    "formats a Date object in English",
    "formats an ISO string in English",
    "formats a Date object in Turkish",
    "falls back to English for unknown lang",
    "formats seconds in English",
    "formats 1 second in English (singular)",
    "formats minutes in English",
    "formats hours in English",
    "formats days in English",
    "formats seconds in Turkish",
    "formats minutes in Turkish",
    "formats hours in Turkish",
    "formats days in Turkish",
    "returns zero for 0ms in English",
    "returns zero for 0ms in Turkish",
    "handles negative ms as 0",
    "prioritises larger units — 1 minute over 60 seconds",
    "formats past time in English",
    "formats past time in Turkish",
    "formats future time in English",
    "formats future time in Turkish",
    "formats 0 diff as 0 seconds ago",
    "formats hours ago in English",
    "formats hours in Turkish past",
    "formats days ago in English"
   ],
   "core/utils-debug-logging.test.ts": [
    "does nothing when DECKENT_DEBUG is not set",
    "writes to stderr when DECKENT_DEBUG is set",
    "handles string errors",
    "handles non-Error objects",
    "handles null/undefined errors",
    "format includes newline",
    "returns empty string on missing file without stderr when debug off",
    "logs to stderr on missing file when DECKENT_DEBUG is set",
    "returns empty string (fallback) regardless of DECKENT_DEBUG state",
    "reads successfully when file exists",
    "returns empty string when reading a directory path (throws EISDIR)",
    "returns null on missing file without stderr when debug off",
    "stays SILENT on missing file even when DECKENT_DEBUG is set (W7: expected soft-miss)",
    "still logs UNEXPECTED failures (malformed JSON) when DECKENT_DEBUG is set",
    "returns null on malformed JSON without stderr when debug off",
    "logs to stderr on malformed JSON when DECKENT_DEBUG is set",
    "returns null (fallback) on error regardless of DECKENT_DEBUG state",
    "parses successfully when JSON is valid",
    "returns null on missing file without stderr when debug off",
    "stays SILENT on missing file even when DECKENT_DEBUG is set (W7: expected soft-miss)",
    "returns null on malformed JSON",
    "returns null (fallback) on error regardless of DECKENT_DEBUG state",
    "parses successfully when JSON is valid",
    "does not throw or write to stderr when write fails (debug off)",
    "logs to stderr when write fails and DECKENT_DEBUG is set",
    "writes config successfully when path is valid"
   ],
   "core/utils-debug.test.ts": [
    "returns empty string when file does not exist (no stderr output)",
    "returns empty string for directory path (no stderr output)",
    "reads file successfully without error",
    "returns null for missing file (no stderr output)",
    "returns null for malformed JSON (no stderr output)",
    "parses valid JSON successfully",
    "returns null for missing file (no stderr output)",
    "returns null for malformed JSON",
    "parses valid JSON successfully",
    "does not throw when path is invalid",
    "writes config successfully when path and deckent dir are valid"
   ],
   "core/utils-decay.test.ts": [
    "parses sprint-021 → 21",
    "parses sprint-003 → 3",
    "parses sprint-001 → 1",
    "parses sprint-100 → 100",
    "returns 0 for unrecognised format",
    "returns 0 for empty string",
    "returns 0 for arbitrary string",
    "open entry → keep (returns false)",
    "open entry with resolvedInSprintId set → keep (resolved=false takes priority)",
    "resolved + resolvedInSprintId undefined (legacy) → remove",
    "resolved 1 sprint ago (diff=1 < 3) → keep",
    "resolved 2 sprints ago (diff=2 < 3) → keep",
    "resolved 3 sprints ago (diff=3 = retentionSprints) → remove",
    "resolved 5 sprints ago (diff=5 > 3) → remove",
    "resolved in same sprint (diff=0) → keep",
    "custom retentionSprints=5: diff=4 → keep",
    "custom retentionSprints=5: diff=5 → remove",
    "custom retentionSprints=1: diff=1 → remove",
    "recently-resolved entry (diff < 3) survives decay filter",
    "old-resolved entry (diff >= 3) is removed by decay filter",
    "mixed batch: only old entries are filtered out"
   ],
   "core/utils-deckent.test.ts": [
    "creates file with @DECKENT.md when file does not exist",
    "prepends @DECKENT.md when file exists without reference",
    "does nothing when file already contains @DECKENT.md (idempotent)",
    "is idempotent — calling twice does not duplicate reference",
    "preserves existing content when prepending",
    "handles empty existing file",
    "detects partial match — @DECKENT.md inside other text",
    "respects an on-demand \"see DECKENT.md\" reference — does NOT force @-auto-load back",
    "works with AGENTS.md path"
   ],
   "core/utils-io.test.ts": [
    "returns file content for an existing file",
    "returns empty string for a non-existent file",
    "returns empty string for a directory path",
    "returns empty string for an invalid path",
    "returns full multi-line content correctly",
    "returns empty string for empty file",
    "returns parsed object for a valid JSON file",
    "returns null for a non-existent file",
    "returns null for malformed JSON",
    "returns null for an empty file",
    "returns parsed array for JSON array file",
    "preserves nested objects",
    "returns null for plain text (non-JSON) file",
    "returns null for a directory path"
   ],
   "core/utils-shared.test.ts": [
    "returns content when file exists",
    "returns empty string when file does not exist",
    "returns empty string for an existing empty file",
    "returns empty string for a directory path",
    "returns full multi-line content",
    "returns empty string for invalid path",
    "returns parsed object when file has valid JSON",
    "returns null when file does not exist",
    "returns null for malformed JSON",
    "returns null for a directory path",
    "returns parsed object when file has valid JSON",
    "returns null when file does not exist",
    "returns null for malformed JSON",
    "parses sprint-021 to 21",
    "parses sprint-001 to 1",
    "returns 0 for unrecognized format",
    "returns 0 for empty string",
    "parses sprint-100 to 100"
   ],
   "core/utils-sprint-id.test.ts": [
    "returns sprint-001 when sprints directory does not exist and no config",
    "returns sprint-001 when sprints directory is empty and no config",
    "returns sprint-002 when sprint-001.md exists (no config)",
    "returns correct increment with multiple sprints (no config)",
    "handles non-sequential sprint files (finds max)",
    "ignores non-sprint files in directory",
    "ignores files that do not match sprint-NNN.md pattern",
    "pads sprint number to 3 digits",
    "handles large sprint numbers",
    "does not reuse a cleaned test sprint identity retained by the event ledger",
    "uses archived task evidence as a monotonic identity floor",
    "ignores legacy epoch job identities while retaining the ordinal archive floor",
    "does not advance past an approved-but-not-started task projection",
    "returns sprint-001 when directory has only non-matching files",
    "reads from config when sprint files are missing",
    "takes max of config and files (config higher)",
    "takes max of config and files (files higher)",
    "never goes backward when files are deleted but config persists",
    "handles numeric last_sprint_id in config",
    "handles malformed config JSON gracefully",
    "handles config without last_sprint_id field",
    "handles config with invalid last_sprint_id string",
    "classifies epoch-millisecond legacy IDs separately from ordinals",
    "does not impose a digit-count cap on large ordinal installations",
    "requires a complete ordinal identity in config",
    "writes sprint ID to existing config",
    "refuses to mint a config when the file is missing (2026-08-25 incident contract)",
    "preserves existing config fields when updating",
    "does not let a legacy detached-job identity poison the ordinal config floor",
    "does not overwrite the config floor with a lower ordinal",
    "rejects malformed ordinal writes instead of silently persisting them",
    "handles write errors gracefully (no throw)",
    "handles malformed existing config gracefully",
    "generates and parses debt table round-trip",
    "returns empty array for content with no table",
    "skips rows with fewer than 9 columns"
   ],
   "core/validators.test.ts": [
    "accepts a valid relative path within base",
    "accepts nested subdirectory",
    "rejects path traversal with ../",
    "rejects absolute path outside base",
    "rejects double-encoded traversal",
    "rejects null byte injection attempt",
    "accepts path that resolves to base itself",
    "has code PATH_TRAVERSAL",
    "accepts sprint-001",
    "accepts sprint-1234",
    "rejects sprint without number",
    "rejects path traversal in sprint ID",
    "rejects shell metacharacters",
    "rejects empty string",
    "rejects sprint with 5+ digits",
    "rejects sprint with 1-2 digits",
    "has code INVALID_SPRINT_ID",
    "accepts PLAN (uppercase)",
    "accepts plan (lowercase)",
    "accepts evaluate",
    "accepts all valid phases",
    "rejects unknown phase",
    "rejects path traversal in phase",
    "rejects shell injection in phase",
    "has code INVALID_PHASE",
    "accepts valid task ID",
    "accepts alphanumeric with hyphens and underscores",
    "rejects empty string",
    "rejects path traversal",
    "rejects shell metacharacters",
    "rejects null bytes",
    "rejects very long IDs (>100 chars)",
    "rejects URL-encoded traversal",
    "has code INVALID_TASK_ID",
    "is an instance of Error",
    "has correct name",
    "has default code"
   ],
   "core/verdict-types.test.ts": [
    "is exactly the five owner-approved single words, frozen",
    "maps every TaskEvaluation member (total function)",
    "maps rubric decisions, criterion statuses and cross-verify verdicts",
    "never fabricates: unknown selfAssessment and lossy projections return null",
    "writeEvaluationAudit stamps the normative projection on the persisted record"
   ],
   "core/verification-isolation-authority.test.ts": [
    "grants an immutable snapshot bound to task, attempt and generation",
    "grants a scoped project graph only when every impacted unit holds an attempt lease",
    "rejects a lease issued to a different attempt or generation",
    "attributes a path to its most specific owning unit",
    "is deterministic across repeated calls and changed-path ordering",
    "produces byte-identical decisions for every ecosystem label",
    "holds identically for a non-TypeScript graph that fails a rule",
    "holds when the identity binding is incomplete",
    "holds when the generation does not bind the requested generation id",
    "holds when the consumer is not on the allowed list",
    "rejects a repository-global lock outright",
    "never treats a mutable HEAD as an isolation surface",
    "holds when a snapshot claims immutability it does not have",
    "holds instead of falling open on an unknown materialization",
    "holds on missing, unnormalized or unattributable changed paths",
    "holds when no project graph authority exists",
    "attributes only observations bound to this attempt, generation and surface",
    "never classifies a concurrent attempt error as this task failure",
    "partitions a mixed batch and reports sorted ambient reason codes",
    "classifies native and cross-realm-shaped hold errors"
   ],
   "core/verification-typescript-adapter.test.ts": [
    "runs a granted, pre-materialized scoped config through shell-free tsc argv",
    "keeps non-zero compiler exit evidence as a failed result",
    "rejects config files outside the grant rather than widening to an ambient project check",
    "reports concurrent foreign errors separately from this attempt result",
    "fails closed when the authority is held or execution has no exit evidence"
   ],
   "core/verifier-entitlement-memory.test.ts": [
    "remembers a live refusal with the provider wording intact",
    "keeps the account under the global state dir, not per project",
    "does not match a different auth mode, provider or model",
    "returns the newest record when a pair was refused more than once",
    "refuses to persist transient failures",
    "refuses to persist a refusal with no provider wording",
    "forgets a refusal older than the TTL",
    "reports nothing learned when no log exists",
    "skips corrupt and unknown-schema lines instead of throwing",
    "degrades to nothing learned when the log cannot be read",
    "reports a failed write instead of throwing"
   ],
   "core/wm2-canonical.test.ts": [
    "single-sources isValidTaskType over the 7 decision task types (dup-delete regression-guard)",
    "every decision TaskType reconciles to a valid canonical TaskKind via decisionTypeToKind",
    "preserves the documented decision → canonical mapping",
    "single-sources the IntentType union from ALL_INTENT_TYPES (dup-delete)",
    "every IntentType reconciles to a valid canonical TaskKind via intentToKind",
    "taskKindToIntent round-trips back into the IntentType union for every canonical kind",
    "decision-types & routing-types expose canonical anchors typed as TaskKind",
    "legacy union members are assignable to their derived types (single-source proof)"
   ],
   "core/wm2-dedup.test.ts": [
    "rubric-registry TaskType is bidirectionally assignable to/from RubricTaskType",
    "task-router TaskType is bidirectionally assignable to/from RouterTaskType",
    "adr-selector TaskType is bidirectionally assignable to/from AdrTaskType",
    "rubric-registry detectTaskType output is a RubricTaskType member",
    "task-router detectTaskType output is a RouterTaskType member",
    "every preset key is an AdrTaskType and the full union is covered",
    "getRubric still selects the audit rubric for an audit task",
    "getRubric still selects the code rubric for a source task",
    "getEffectClass maps audit→pure and code→reversible (unchanged)"
   ],
   "core/work-model.test.ts": [
    "maps %s → %s",
    "maps %s → %s",
    "maps %s → %s",
    "maps %s → %s",
    "maps %s → %s",
    "every adapter maps an unrecognized value to generic",
    "rubric: kind → reverse → forward returns same kind",
    "adr-domain: kind → reverse → forward returns same kind",
    "intent: kind → reverse → forward returns same kind",
    "taskKindToRubric only emits the 3 rubric values",
    "is total and deeply frozen for every canonical task kind",
    "derives each row through taskKindToRubric",
    "EnvironmentType / RequirementProfile / ExecutionRequest are constructible",
    "agrees with the legacy core-system-prompt classifier — ${label}",
    "agrees with the legacy scope-block inspection predicate (write=%j read=%j)",
    "precedence: no write targets + an authored read list wins over a doc kind",
    "a declared kind wins over the injected fallback (fallback is never called)",
    "the directory heuristic only runs when there is no kind AND no fallback",
    "is total — an empty signal set degrades to the conservative code class",
    "DEFAULT_TASK_PROFILES carries exactly the literals the legacy predicates used",
    "config override is honored per-field; unspecified fields keep the default",
    "an empty (undefined-field) override object still resolves as the default"
   ],
   "core/worker-activity-heartbeat.test.ts": [
    "serializes one versioned identity-bound activity projection",
    "uses the same field contract in generated worker instructions",
    "returns a typed identity HOLD for ambiguous legacy data",
    "types malformed and authority-contaminated payloads instead of coercing"
   ],
   "core/worker-heartbeat-authority-store.test.ts": [
    "is the sole sequence and timestamp authority for an exact attempt",
    "returns typed HOLD for stale and foreign writers without replacing accepted authority",
    "rejects sprint-480 rollback and forged wall-time while preserving live process visibility",
    "records a same-attempt worker identity fence violation without replacing authority",
    "retains host process outcome separately from the worker verdict",
    "persists immutable revisions across instances without a project-global singleton",
    "holds an uninitialized attempt and a concurrent attempt-scoped writer",
    "rejects contradictory process/verdict facts without allocating a sequence",
    "rejects a persisted revision whose payload sequence differs from its authority filename"
   ],
   "core/worker-heartbeat-authority.test.ts": [
    "accepts host-observed timing fields and rejects malformed clock input",
    "advances only host-observed sequence and time while retaining separate process, verdict, and liveness facts",
    "is idempotent for an exact duplicate and folds ordered batches equivalently",
    "holds stale dual-writer updates and preserves the last accepted snapshot",
    "holds an observation from a foreign exact attempt",
    "classifies a competing writer on the same attempt as a writer-fence conflict",
    "holds process/liveness and exit-code/task-verdict contradictions without conflating their fields"
   ],
   "core/worker-heartbeat-file.test.ts": [
    "atomically writes without leaving a temporary artifact",
    "skips a timestamp regression for the same canonical attempt",
    "writes a legacy heartbeat without dropping fields"
   ],
   "core/worker-image-check.test.ts": [
    "reports 'missing' when docker image inspect fails (image not built)",
    "reports 'ready' when image exists with all CLIs and ca-certificates",
    "reports 'stale' when the exact native runtime authority probe fails",
    "reports 'stale' when the exact image-owned dependency source is absent",
    "reports 'stale' when a required CLI is missing from the image",
    "reports 'stale' when ca-certificates are absent (codex TLS would fail)",
    "reports 'stale' conservatively when the in-image probe fails entirely",
    "suggestedBuildCmd carries INSTALL_CODEX + INSTALL_GEMINI build-args when both required",
    "suggestedBuildCmd omits build-args for a claude-only fleet",
    "honors a custom image tag in inspect args and suggestedBuildCmd",
    "skips host-only providers (ollama) — no container CLI is probed for them",
    "uses the default image tag when none is provided",
    "treats a docker spawn error (docker unavailable) as 'missing'",
    "handles an empty/host-only requiredProviders list (ca-certs only gates readiness)",
    "uses async spawn with shell:false for the run probe (no shell injection surface)",
    "emits build-args only for opt-in providers, claude needs none"
   ],
   "core/workspace-artifact-contract.test.ts": [
    "registers the complete five-artifact workspace inventory exactly once",
    "round-trips provenance headers without touching user content",
    "verifies digest-bound managed content and fails closed after tampering",
    "delivers only the verified managed body from the inspected bytes when requested"
   ],
   "core/xverify-task-settlement.test.ts": [
    "cryptographically binds exact owner tier authority provenance",
    "projects host-adjudicated %s as terminal %s",
    "projects unavailable as terminal NO_GO rather than a pending stub",
    "projects HOLD only with typed fresh-generation resume authority",
    "rejects cross-generation transport receipt replay",
    "does not accept a provider verdict alone as a receipt",
    "forbids same-provider self-settlement",
    "binds no-replay keys to generation and settlement outcome",
    "round-trips only a byte-semantic valid persisted receipt"
   ],
   "core/xverify-verifier-tier-authority.test.ts": [
    "admits only the exact different-provider pair and projects its decision ref without changing tiers",
    "fails closed for absent, foreign-version, wildcard, and non-matching authority",
    "rejects unknown authority and decision fields instead of ignoring owner constraints",
    "returns an auditable owner refusal",
    "rejects unknown identities, same providers, deprecated verifiers and bad refs",
    "rejects deprecated authors and registered legacy aliases as non-canonical identities",
    "rejects duplicate/conflicting pairs and an over-bounded list"
   ]
  },
  "dashboard": {
   "dashboard/AgentDetail.test.tsx": [
    "renders with the task ID in the header",
    "displays model badge when task data loads",
    "displays status badge when task data loads",
    "displays task title",
    "displays task description",
    "displays scope directories",
    "displays log output",
    "shows 'No log output yet.' when log is null",
    "calls onClose when close button is clicked",
    "fetches from correct API endpoint with taskId",
    "supports custom apiBase prop",
    "sends the bootstrap Bearer token (auth header) when a token is configured",
    "polls for updates every 3 seconds",
    "stops polling on unmount",
    "does not show model badge when task has no model",
    "does not show scope section when directories are empty",
    "handles fetch error gracefully",
    "renders Log Output label",
    "has close button with correct aria-label"
   ],
   "dashboard/AppShell.test.tsx": [
    "exports AppShell component function",
    "renders structural zones with data-testid attributes",
    "renders children inside main zone and supports headerContent slot",
    "theme toggle söküldü — useTheme/testid/data-theme yok",
    "applies responsive CSS grid for sidebar breakpoint layout",
    "hides sidebar on mobile via hidden/md:flex responsive classes",
    "applies dark/light theme tokens consistently across zones",
    "imports navItems from Sidebar for navigation hierarchy",
    "renders NavLink for each nav item with active class logic",
    "uses useTranslation for i18n nav labels"
   ],
   "dashboard/AutonomousPage.test.tsx": [
    "renders the page wrapper with heading",
    "renders pending approvals with their action + ids",
    "renders NO approve/reject buttons; readonly notice present; no postJson",
    "shows the empty state when no pending approvals",
    "renders the backlog summary counts",
    "renders backlog entries"
   ],
   "dashboard/DashboardPage-control.test.tsx": [
    "DashboardPage.tsx file exists",
    "exports default DashboardPage function component",
    "renders SprintPhaseTimeline for PLAN→CLEANUP phase display",
    "uses PHASE_COLORS map with PLAN, EXECUTE, COMPLETE labels",
    "shows phase badge next to sprint status card header",
    "renders WorkerCardGrid with agents array from live state",
    "passes onSelect callback to WorkerCardGrid for AgentDetail sheet",
    "passes NO onKill callback — worker termination left the dashboard",
    "has NO New Sprint modal wiring",
    "has NO cleanup / kill-all buttons in any phase",
    "renders the ReadOnlyNotice with the sprint hint instead",
    "imports useLiveData for stale-while-revalidate polling",
    "polls /api/status via useLiveData when SSE is unavailable",
    "prefers SSE state over polled state over fallback state",
    "also retains useSSE for real-time push events",
    "imports DirectivesEditor component",
    "renders DirectivesEditor when no sprint is active",
    "status label references both sprint id and phase for top-level context"
   ],
   "dashboard/DirectivesEditor.test.tsx": [
    "file exists",
    "exports DirectivesEditor function component",
    "renders the DIRECTIVES content in a read-only pre (no Textarea)",
    "loads content from GET /api/directives via fetchJson",
    "renders the readonly notice with the directives hint",
    "has NO save button and never POSTs /api/directives",
    "has NO onChange edit path — content state only comes from the fetch",
    "tracks isEmpty derived state from content",
    "shows the empty warning banner when content is empty",
    "exposes onContentChange callback for parent integration"
   ],
   "dashboard/DocsHealthPage.test.tsx": [
    "renders the doc table from /api/docs/health",
    "filters the table when a heatmap cell is clicked"
   ],
   "dashboard/EnterprisePage.test.tsx": [
    "renders tenant list when tenant data is available",
    "renders RBAC role matrix with admin > operator > viewer order",
    "renders audit log table with action, actor, result columns",
    "shows empty state when no tenant data",
    "renders rate limit status with progress bars",
    "shows empty state in RBAC tab when no role data",
    "renders enterprise page wrapper with enterprise heading"
   ],
   "dashboard/EvolutionPage-data.test.tsx": [
    "shows loading skeleton when useApi returns loading=true",
    "shows error state when API returns a non-401 error",
    "shows 401 unauthorized message in error state",
    "shows empty states when all endpoints return null data (no error)",
    "renders genealogy data successfully when API returns results",
    "shows error in retirement tab when retirement endpoint fails"
   ],
   "dashboard/EvolutionPage.test.tsx": [
    "renders genealogy tree with agent nodes when data is available",
    "renders retirement timeline when retirement data is available",
    "renders prompt-diff table with agent metrics when data is available",
    "shows empty states when all endpoints return no data",
    "renders evolution page wrapper with heading and tabs",
    "shows genealogy child node indented under parent"
   ],
   "dashboard/Layout-godlevel.test.tsx": [
    "kanıt: grep grid|responsive|loading|header|sidebar|theme returns ≥3 matching lines",
    "renders god-level shell wrapper with sidebar + content grid testids",
    "content area uses an explicit CSS grid (not a vertically frozen skeleton stack)",
    "desktop header banner is md+ only with hidden md:flex",
    "mobile hamburger header remains visible on small screens via flex md:hidden",
    "sidebar width expands at lg: breakpoint for wider desktop",
    "exposes a dedicated layout-loading-state with role=status",
    "loading-state is gated on SSE connecting + no data (not a permanent skeleton)",
    "loading-state respects prefers-reduced-motion via motion-safe:",
    "loading-state surfaces brand + i18n connecting copy (not just a spinner)",
    "preserves existing SidebarContent + NavLinks + LanguageSwitcher composition (dark tek-kimlik: ThemeToggle yok)",
    "adds semantic landmarks (role=banner header, role=main content, aria-labels)",
    "desktop header surfaces sprint id + phase badge for top-level context",
    "dark/light tokens remain consistent (dark:bg-zinc-950, dark:bg-zinc-900, dark:border-zinc-800)",
    "dark tek-kimlik: theme-toggle yüzeyden kalktı (Alperen 2026-07-31)"
   ],
   "dashboard/Layout-ux.test.tsx": [
    "render: Layout exports with Outlet, ScrollArea, and flex h-screen structure",
    "dark tek-kimlik: toggle/Sun/Moon söküldü (Alperen 2026-07-31)",
    "responsive: uses md: and lg: breakpoints for sidebar and mobile layout",
    "layout: SidebarContent with NavLinks and footer LanguageSwitcher (dark tek-kimlik)"
   ],
   "dashboard/Layout.test.tsx": [
    "renders: exports Layout component and core structure",
    "dark tek-kimlik: theme toggle söküldü (Alperen 2026-07-31)",
    "responsive: uses md: breakpoints for desktop/mobile layout",
    "dark mode: has dark: variant classes for light/dark theme consistency"
   ],
   "dashboard/MemoryExplorer.test.tsx": [
    "renders whole units with compact identity, source and scoped revision evidence",
    "changes the actual API selector when ADR or debt tabs are selected",
    "uses a non-overlapping responsive search form",
    "uses tab-specific empty headings without claiming a sprint will populate the reader",
    "retries the same request only for retryable request failures and returns focus to search",
    "keeps one polite live status and restores search focus after navigation and detail close",
    "projects canonical HOLD scope and required IDs, then restarts an invalid cursor from the current first page",
    "shows required-entry and oversize guidance without a cursor reset action",
    "retries QUERY_FAILED HOLDs explicitly",
    "keeps a failed detail dismissible and invalidates it on query and page changes"
   ],
   "dashboard/MemoryExplorerPage.test.tsx": [
    "is a deep-linkable bounded-reader view, not a Markdown-derived ADR timeline"
   ],
   "dashboard/NervousPage-data.test.tsx": [
    "renders pending approval list with real data (veri render)",
    "renders NO accept/reject buttons; readonly notice present; no postJson",
    "shows empty state when no pending approvals (boş state)",
    "shows error message when pending fetch fails (error state)",
    "shows loading skeleton while pending approvals are loading",
    "shows panic guard badge with correct ACTIVE state",
    "renders the recommendation list with action id + payload summary",
    "renders NO dismiss buttons on recommendations; no postJson fires",
    "shows empty state when no open recommendations"
   ],
   "dashboard/NervousPage.test.tsx": [
    "renders pending approval list when approvals are available",
    "renders NO accept/reject buttons and the readonly notice instead",
    "shows empty state when no pending approvals",
    "shows panic-guard badge with correct state",
    "renders detector list from status endpoint",
    "renders the nervous page wrapper with heading"
   ],
   "dashboard/Onboarding.test.tsx": [
    "renders first step on initial load",
    "advances to next step when Next is clicked",
    "shows step progress indicators for all steps",
    "calls onSkip when Skip button is clicked",
    "calls onComplete on last step when Get started is clicked",
    "shows guide text for each step"
   ],
   "dashboard/RoutingDistribution.test.tsx": [
    "maps agentId field to id and label",
    "handles empty agents and skills",
    "returns false when all agents ≤80%",
    "returns true when any agent >80%",
    "renders distribution wrapper when data is provided",
    "passes agent entries to bar chart data prop",
    "shows imbalance warning when an agent exceeds 80%",
    "shows empty state when no agents or skills"
   ],
   "dashboard/SprintControlPanel.test.tsx": [
    "renders empty state when no sprint data available",
    "renders sprint status with phase badge when sprint is active",
    "renders phase timeline visualization with all phases",
    "renders worker grid with active workers",
    "renders empty worker state when no workers assigned",
    "renders NO kill-all/cleanup buttons in ANY phase",
    "shows the readonly notice when a sprint is active",
    "shows progress card with done/total counts when total > 0",
    "hides progress card when total is 0",
    "uses api fallback state when SSE data is null",
    "never calls postJson — the panel has no mutation wiring"
   ],
   "dashboard/SprintSummary.test.tsx": [
    "returns green for DONE",
    "returns teal for EXECUTING",
    "returns teal for TESTING",
    "returns teal for VERIFYING",
    "returns teal for CODING",
    "returns yellow for NO_GO",
    "returns yellow for ERROR",
    "returns orange for PAUSED",
    "returns zinc (gray) for unknown status",
    "returns green bg for DONE",
    "returns teal bg for EXECUTING",
    "returns yellow bg for NO_GO",
    "returns orange bg for PAUSED",
    "returns zinc bg for unknown status",
    "returns CheckCircle for DONE",
    "returns different icons for different statuses",
    "maps DONE to Done",
    "maps EXECUTING to Active",
    "maps CODING to Writing code",
    "maps TESTING to Running tests",
    "maps VERIFYING to Type checking",
    "maps NO_GO to Needs attention",
    "maps PENDING to Queued",
    "maps PAUSED to Paused",
    "maps unknown to Waiting",
    "returns 0 for empty array",
    "returns 0 when no tasks have feedbackLoop",
    "counts tasks with tscAttempts > 1",
    "counts tasks with testAttempts > 1",
    "counts tasks with both tsc and test retries",
    "returns empty for no agents and no tasks",
    "groups by task provider field",
    "infers Claude from agent model when no task providers",
    "infers Codex from gpt/codex model names",
    "infers Gemini from gemini model names",
    "prefers task providers over agent inference",
    "returns empty when no startedAt",
    "returns empty when done is 0",
    "returns empty when done equals total",
    "calculates remaining time based on average pace",
    "returns ~1 min for very short remaining",
    "returns empty when no startedAt",
    "returns 'just started' for less than a minute",
    "returns minutes for elapsed time",
    "renders without crashing",
    "renders progress bar",
    "shows correct percentage",
    "shows fraction (done/total)",
    "shows sprint ID in header",
    "shows phase in header",
    "shows done count",
    "shows active count",
    "shows queued count",
    "shows color-coded task list",
    "shows task status labels",
    "shows self-healing count when tasks auto-fixed",
    "hides self-healing count when zero",
    "shows provider breakdown",
    "shows active workers in 'What's happening now' section",
    "hides 'What's happening now' when no active agents",
    "shows ETA when tasks are in progress",
    "shows elapsed time",
    "shows warnings for tasks with many retries",
    "handles zero total tasks gracefully",
    "handles 100% completion",
    "does not show task list when no tasks provided",
    "does not show provider breakdown when no providers"
   ],
   "dashboard/StatusPage.route.test.tsx": [
    "App.tsx exists",
    "imports StatusPage",
    "registers /status route",
    "StatusPage route is inside Layout wrapper",
    "routes.tsx exists",
    "ROUTES array contains /status entry",
    "ROUTES /status entry has a label",
    "exports RoutePath type covering /status",
    "StatusPage.tsx file exists",
    "uses SSE hook for live updates",
    "fetches /api/status as fallback",
    "fetches /api/tasks for task info",
    "uses SprintSummary component",
    "renders a page title via i18n",
    "Sidebar.tsx exists",
    "exports navItems array including /status",
    "uses Activity icon for Status nav item",
    "exports SidebarNavLinks component",
    "renders NavLink with active state styling"
   ],
   "dashboard/TaskCard.test.tsx": [
    "returns green for DONE",
    "returns teal for EXECUTING",
    "returns teal for CODING",
    "returns teal for TESTING",
    "returns teal for VERIFYING",
    "returns red for NO_GO",
    "returns red for ERROR",
    "returns yellow for PAUSED",
    "returns zinc (gray) for unknown/PENDING",
    "returns different icons for different statuses",
    "returns same icon for active sub-statuses",
    "returns green for DONE",
    "returns teal for active statuses",
    "returns red for NO_GO",
    "returns yellow for PAUSED",
    "returns zinc for unknown",
    "returns custom currentAction when provided",
    "returns 'Completed' for DONE",
    "returns 'Working...' for EXECUTING",
    "returns 'Writing code' for CODING",
    "returns 'Running tests' for TESTING with single attempt",
    "returns 'Running tests (attempt N/3)' for TESTING with retries",
    "returns 'Type checking' for VERIFYING",
    "returns 'Failed — needs attention' for NO_GO",
    "returns 'Queued' for PENDING without dependencies",
    "returns 'Waiting for Task X' for PENDING with dependencies",
    "returns 'Waiting' for unknown status",
    "returns success for DONE",
    "returns info for active statuses",
    "returns destructive for NO_GO/ERROR",
    "returns warning for PAUSED",
    "returns secondary for unknown",
    "maps DONE to Done",
    "maps EXECUTING to Active",
    "maps CODING to Writing code",
    "maps NO_GO to No-Go",
    "maps PENDING to Queued",
    "maps PAUSED to Paused",
    "maps unknown to Waiting",
    "renders without crashing",
    "shows task ID and title",
    "shows correct data-status attribute",
    "shows current action text",
    "shows badge with correct label",
    "shows correct color for DONE status (green)",
    "shows correct color for EXECUTING status (teal)",
    "shows correct color for NO_GO status (red)",
    "shows correct color for PAUSED status (yellow)",
    "shows correct color for PENDING status (gray/zinc)",
    "does not show details when collapsed",
    "shows details when clicked (expandable)",
    "shows files changed count in expandable details",
    "shows test results in expandable details",
    "hides failed count when zero",
    "shows retry history in expandable details",
    "collapses details when clicked again",
    "does not expand when task has no details",
    "shows 'Waiting for Task X' when PENDING with dependencies",
    "shows test retry attempt text for TESTING status"
   ],
   "dashboard/api-client-token.test.ts": [
    "GET with token attaches Authorization: Bearer header",
    "GET without token sends no Authorization header",
    "401 GET dispatches deckent:unauthorized event and throws ApiError",
    "Authorization header format is exactly \"Bearer <token>\"",
    "POST with token attaches Authorization: Bearer header",
    "401 on POST also dispatches deckent:unauthorized",
    "getBootstrapApiToken returns token when window has it",
    "getBootstrapApiToken returns undefined when token absent",
    "GET falls back to the session token when no bootstrap token (A7)",
    "bootstrap token takes precedence over the session token"
   ],
   "dashboard/api.test.ts": [
    "returns parsed JSON on success",
    "throws ApiError on non-ok response",
    "ApiError has correct status",
    "sends POST with JSON body",
    "sends POST without body when undefined",
    "throws ApiError on failure",
    "has correct name and properties"
   ],
   "dashboard/approval-history-panel.test.tsx": [
    "renders the default 'all' view with rows from every settled category",
    "renders category badge, risk badge, policy, and channel for a row",
    "shows the reason line only when reason is non-empty",
    "shows an awaiting-decision placeholder (not null/blank) for an unswept-expired entry",
    "switches to the approved-only view on filter click",
    "switches to the denied-only view on filter click",
    "switches to the expired-only view on filter click",
    "returns to the all view on filter click after narrowing",
    "shows an empty state when the approved filter has zero entries",
    "shows an empty state when the expired filter has zero entries",
    "disables Previous on the first page and enables Next when hasMore",
    "advances to the next page and back on click",
    "resets to offset 0 when switching filters",
    "has zero decide/accept/deny controls — only the 4 filter + 2 pagination buttons exist",
    "has no emoji characters in rendered output"
   ],
   "dashboard/approval-history.test.tsx": [
    "renders the default 'all' view with rows from every settled category",
    "filters to a single category (karar-tipi) on filter click",
    "shows an honest empty state for a filter with zero entries",
    "has no emoji characters in rendered output"
   ],
   "dashboard/approvals-panel.test.tsx": [
    "polls /api/approvals",
    "renders all three sections",
    "renders the pending entry with its summary",
    "renders the approved and denied entries",
    "shows the maskedArgs one-line summary, never a raw/nested dump",
    "maps each risk level to a distinct badge variant class",
    "shows the decision channel for approved/denied entries",
    "shows an awaiting-decision placeholder (not null/blank) for pending entries",
    "renders a non-empty age string for every entry",
    "has zero decide/accept/deny controls anywhere in the DOM (DASH-1: dashboard observes only)",
    "has no emoji characters in rendered output",
    "shows an empty state for a category with zero entries"
   ],
   "dashboard/auth-status.test.tsx": [
    "file exists at expected path",
    "exports AuthStatus function component",
    "has data-testid=\"auth-status\" attribute",
    "has data-testid=\"logout-button\" attribute",
    "uses useAuth hook for identity and logout",
    "uses useTranslation for i18n — no hardcoded user-facing strings",
    "uses Lucide icons — no emoji-presentation characters",
    "renders nothing when identity is null",
    "renders auth-status root with logged-in name",
    "renders role badge when identity has a role",
    "does not render role badge when role is absent",
    "shows \"Local session\" for static mode",
    "renders logout button",
    "calls useAuth().logout() when logout button is clicked",
    "shows session-cleared hint after logout when bootstrap token persists"
   ],
   "dashboard/cards-mounted.test.tsx": [
    "App.tsx exists",
    "imports LimitsCard and EvaluateHealthCard",
    "renders both cards from the element mounted on the \\\"/\\\" route",
    "both cards are wired inside the Layout-wrapped route tree (nav-reachable)",
    "does not remove the existing \\\"/\\\" route (no regression)",
    "has zero emoji-presentation characters in the new App.tsx content",
    "renders LimitsCard and EvaluateHealthCard together (no longer orphans)",
    "renders both cards' real data (not stubbed placeholders)",
    "has zero emoji-presentation characters in the combined render output"
   ],
   "dashboard/chat-page-readonly.test.tsx": [
    "renders the chat-page signpost with the readonly notice",
    "has NO textarea, NO input, and NO send button",
    "performs no fetch/postJson to /api/chat or /api/chat/stream on mount"
   ],
   "dashboard/components.test.ts": [
    "file exists",
    "uses recharts components",
    "has left Y axis (tests) and right Y axis (coverage)",
    "uses blue for tests and green for coverage",
    "has custom tooltip formatter",
    "exports parseChartData function",
    "renders empty state when no data",
    "parses history records into chart entries",
    "handles missing fields with defaults",
    "handles empty array",
    "handles non-numeric tasks",
    "uses id over sprint for sprintId",
    "falls back to sprint when id missing",
    "strips % from coverage",
    "file exists",
    "renders table with correct columns",
    "has priority badge styling",
    "renders empty state",
    "exports parseDebtMarkdown function",
    "parses valid markdown table",
    "parses all rows correctly",
    "returns empty array for empty string",
    "returns empty array for content without table",
    "returns empty array when only header and separator",
    "handles rows with fewer columns gracefully",
    "handles missing status column",
    "trims whitespace from cells",
    "file exists",
    "exports Tabs, TabsList, TabsTrigger, TabsContent",
    "uses context for state management",
    "supports defaultValue prop",
    "TabsTrigger has role=tab and aria-selected",
    "TabsContent has role=tabpanel",
    "TabsList has role=tablist",
    "has active/inactive state styling",
    "uses cn utility for className merging"
   ],
   "dashboard/config-integration.test.ts": [
    "returns config object with expected shape",
    "sends GET request to correct URL",
    "throws ApiError(404) when config not found",
    "sends correct JSON payload",
    "returns merged config from server response",
    "throws ApiError(422) on validation failure",
    "throws ApiError(400) on non-object body",
    "mode round-trip: POST economic → GET returns economic",
    "language round-trip: POST tr → GET returns tr",
    "5-field round-trip: all fields match after POST → GET",
    "nested key round-trip: skill_routing.testing preserved after partial update",
    "nested key round-trip: modes.performance.max_workers updated, brain_model preserved",
    "returns default config object",
    "GET /api/config propagates network error",
    "POST /api/config propagates network error",
    "POST /api/config 500 throws ApiError"
   ],
   "dashboard/config-page.test.tsx": [
    "file exists",
    "fetches /api/config and /api/config/defaults",
    "has all config categories",
    "defines CONFIG_FIELDS with key, label, description, type, category, defaultValue",
    "has NO Reset to Default button per field",
    "has NO save feedback message surface",
    "has tooltip/description for each field using Info icon",
    "renders input types: select, boolean, number, text",
    "shows default value indicator when value differs from default",
    "never POSTs /api/config — reads only, with the readonly notice",
    "renders every input disabled with no dirty tracking and no onChange",
    "includes all expected config keys"
   ],
   "dashboard/dash-perf-494.test.tsx": [
    "%s is NOT a static top-level import (must stay lazy-loaded)",
    "lazy+Suspense are live: App.tsx imports both and wires a Suspense fallback",
    "contains no emoji characters (NO_GO condition)",
    "three simultaneous callers to the same URL share exactly one underlying fetch",
    "polling-storm shape: 3 useLiveData consumers polling the same URL still issue 1 fetch",
    "aborts the underlying request and issues no further polls after unmount"
   ],
   "dashboard/dashboard-build-smoke.test.ts": [
    "declares build:dashboard and build:all scripts",
    "build:dashboard invokes vite build for src/dashboard",
    "build:all chains tsc and build:dashboard",
    "src/dashboard/vite.config.ts exists",
    "vite config imports React and Tailwind plugins"
   ],
   "dashboard/dashboard-page.test.ts": [
    "file exists",
    "imports useSSE hook for real-time updates",
    "has fallback fetch to /api/status",
    "displays sprint status card with sprint ID, phase, status, updated",
    "renders worker cards and agent detail components",
    "has NO kill wiring — no /api/kill POST, no postJson, no onKill prop",
    "has progress bar with done, active, pending segments",
    "has alert section with level badges",
    "has NO new-sprint modal — the readonly notice replaces all control",
    "uses dark theme classes",
    "has phase color mapping for badges",
    "has elapsed time helper function",
    "component file no longer exists — sprints start from the terminal/Desktop",
    "file exists",
    "has Memory category fields in CONFIG_FIELDS",
    "has Auditor category fields in CONFIG_FIELDS",
    "has Rollback category fields in CONFIG_FIELDS",
    "has Project category fields in CONFIG_FIELDS",
    "has Advanced category fields in CONFIG_FIELDS",
    "has Sprint fix_phase_enabled and max_fix_retries fields",
    "CATEGORIES array has 14 categories",
    "CATEGORIES includes all required category names",
    "badge.tsx exists with variants",
    "dialog.tsx exists with all sub-components",
    "table.tsx exists with all sub-components",
    "textarea.tsx exists with dark theme",
    "progress.tsx exists with segments support"
   ],
   "dashboard/debt-filter.test.tsx": [
    "renders debt page with all entries by default",
    "filters by severity — critical only shows critical entries",
    "filters by sprint — sprint-217 only shows sprint-217 entries",
    "filters by status — active shows only active entries",
    "filters by status — resolved shows only resolved entries",
    "filters by search query — matches title and id",
    "shows empty state when no entries match combined filters",
    "shows empty state when API returns no debt entries",
    "shows filter dropdowns for severity, sprint, and status"
   ],
   "dashboard/dock-panel-gate.test.tsx": [
    "renders nothing when no terminal token is injected (no dead bar)",
    "renders the dock bar when a terminal token is present"
   ],
   "dashboard/enterprise-auth-alerts.test.tsx": [
    "EnterprisePage.tsx exists",
    "enterprise auth-data: uses __DECKENT_API_TOKEN__ and exposes Authorization bearer indicator",
    "tenant render: tenant-list section is present and renders tenant rows",
    "alert-dedup tek: dedupDocSyncAlerts reduces multiple doc-sync alerts to one entry",
    "provider-neutral: alert message includes CLAUDE/GEMINI/AGENTS, not just CLAUDE"
   ],
   "dashboard/enterprise-crud-ui.test.tsx": [
    "admin identity → NO create/edit/delete controls; readonly notice + matrix render",
    "viewer identity → create/edit/delete actions hidden in RBAC tab",
    "admin identity → no enterprise mutation fetch can fire from the RBAC tab",
    "admin identity → NO create/edit/delete controls; rate status still renders",
    "viewer identity → create/edit/delete actions hidden in Rate tab",
    "admin identity → no enterprise mutation fetch can fire from the Rate tab"
   ],
   "dashboard/enterprise-missions-audit.test.tsx": [
    "renders Missions Audit tab trigger with i18n key resolved (not raw key)",
    "renders missions audit table rows when data is available",
    "renders column headers with resolved i18n keys (not raw keys)",
    "renders title with resolved i18n (not raw key)",
    "renders empty state with resolved i18n when no data",
    "renders empty state when missions-audit data is null (no endpoint yet)",
    "renders error state with resolved i18n when error occurs",
    "renders loading skeleton when loading",
    "result badges show success and denied variants"
   ],
   "dashboard/enterprise-my-role.test.tsx": [
    "EnterprisePage.tsx exists",
    "imports useAuth hook",
    "oidc-role: renders 'You are: <role>' badge when identity.role is present",
    "static-mode: renders 'local (full access)' badge when mode is static and no role",
    "rbac-matrix: highlights user's own role row with my-role-row data-testid",
    "rbac-matrix: shows 'You' indicator badge inside the matched role row"
   ],
   "dashboard/evaluate-health-card.test.tsx": [
    "renders a skeleton while loading",
    "renders the honest clean state when no faults are present",
    "renders the clean state (not zero counters) even with no data at all",
    "renders all 4 counters with correct values when faults are present",
    "renders the last-event timestamp row when faults are present",
    "has no emoji characters in rendered output (clean state)",
    "has no emoji characters in rendered output (faulty state)"
   ],
   "dashboard/exact-task-output.test.tsx": [
    "consumes only the exact typed read model and bounds whole retained lines",
    "rejects foreign task/attempt bytes before rendering and preserves previous partial evidence",
    "rejects lines without verified identity or redacted display metadata",
    "pins provider/model custody identity and rejects missing or substituted fields",
    "distinguishes pending, sealed and observer-only EOF from task completion",
    "keeps denied and ambiguous outcomes distinct and never silently retries them",
    "disconnects without hiding lines; an explicit retry starts a new snapshot without duplicate append",
    "renders actual panel output and a HOLD together rather than hiding evidence",
    "defaults WorkerLogViewer to real provider lines, without synthesizing structured tool events",
    "shows Turkish view closure without claiming worker success",
    "lets the operator resolve ambiguity with an explicit attempt instead of selecting latest"
   ],
   "dashboard/i18n-coverage.test.ts": [
    "en.ts and tr.ts have the same number of keys",
    "every en.ts key exists in tr.ts",
    "every tr.ts key exists in en.ts",
    "no tr.ts value is an empty string",
    "${relPath} uses useTranslation",
    "${relPath} has no hardcoded English UI strings"
   ],
   "dashboard/i18n-no-literal-labels.test.tsx": [
    "${pagePath} should not contain hardcoded Turkish strings",
    "should have 0 total hardcoded Turkish strings across all 3 pages",
    "nav-items.ts — no NavItem has a literal label: override",
    "Layout.tsx — renders nav items via labelKey, not literal label override",
    "Sidebar.tsx — no literal label: NavItem definition",
    "regression guard — would FAIL if a literal label is re-introduced in nav-items.ts"
   ],
   "dashboard/kpi-dashboard.test.tsx": [
    "renders KPI rows from seeded /api/kpi payload",
    "shows KPI titles from payload",
    "shows status badges for KPI entries",
    "shows empty state when kpis array is empty",
    "shows empty state when no data at all",
    "uses i18n — scorecard title rendered via t()",
    "has no emoji characters in rendered output",
    "renders the trend page wrapper",
    "renders a KPI selector dropdown with the KPI list",
    "shows empty series state by default (no KPI selected)",
    "shows empty series state when series is empty",
    "renders trend series when series data is returned by useApi",
    "shows trend values in each series row",
    "has no emoji characters in rendered output",
    "App.tsx imports KpiTrendPage",
    "App.tsx registers /kpi route",
    "Sidebar.tsx contains /kpi nav link",
    "Sidebar.tsx uses i18n key for KPI nav label",
    "Sidebar.tsx uses BarChart2 lucide icon for KPI link"
   ],
   "dashboard/language-provider-readonly.test.tsx": [
    "setLang writes localStorage and performs NO POST /api/config",
    "initial lang comes from localStorage without any config fetch",
    "falls back to GET /api/config language only when nothing is stored"
   ],
   "dashboard/layout-chat-first.test.tsx": [
    "chat nav item appears before dashboard in navGroups definition",
    "chat is the first item in the first nav group (talk)",
    "all 3 nav groups are defined: talk, watch, manage",
    "active-link styling is preserved (border-l-2 border-gold on active)",
    "all 10 route paths are present in navGroups",
    "navGroups flat count equals 10 items (via navItems derived array)",
    "grouped nav uses data-nav-group attribute for each section",
    "Nav renders layout-nav testid for integration hooks"
   ],
   "dashboard/layout.test.ts": [
    "${file} exists",
    "exports Layout component",
    "has sidebar with 240px width",
    "uses NavLink from react-router-dom",
    "has all four nav items",
    "has nav labels via i18n keys",
    "has active link styling with bg-zinc-800 and blue border",
    "sidebar uses bg-zinc-900 border-r border-zinc-800",
    "main content area has correct styling",
    "has mobile hamburger menu icon",
    "uses Sheet for mobile sidebar",
    "hides desktop sidebar on mobile with md: breakpoint",
    "uses Outlet for child routes",
    "has deckent branding",
    "uses lucide-react icons for nav items",
    "uses ScrollArea component",
    "mobile nav closes on link click",
    "ThemeProvider söküldü",
    "dark: utilities html.dark ile always-on",
    "exports Sheet, SheetTrigger, SheetContent",
    "uses context for open state management",
    "supports controlled open/onOpenChange props",
    "has overlay backdrop",
    "handles escape key",
    "supports left and right side positioning",
    "has close button with X icon",
    "prevents body scroll when open",
    "uses cn for className merging",
    "has correct z-index layering",
    "exports ScrollArea component",
    "uses forwardRef",
    "has overflow-auto class",
    "uses cn for className merging",
    "has displayName set",
    "dark tek-kimlik: App ThemeProvider sarmalamaz (2026-07-31)",
    "uses Layout as route element",
    "uses route-based code-splitting: critical routes eager, the rest lazy (PUBLISH-P2, 2026-07-06)",
    "still has BrowserRouter",
    "has all four routes",
    "still imports tailwindcss",
    "still has @theme block",
    "has dark color scheme",
    "has scrollbar styling",
    "has body base styles",
    "preserves all theme variables"
   ],
   "dashboard/lazy-routes.test.tsx": [
    "imports lazy and Suspense from react",
    "%s is lazy-loaded via dynamic import",
    "%s's route element is wrapped in Suspense (via the Lazy helper)",
    "\"/\" (DashboardWithObservability) and ChatPage remain eager (not lazy)",
    "%s remains a static top-level import (protected by other route-wiring suites)",
    "route tree paths are unchanged (same 20 routes, same order)",
    "defines a Lazy wrapper using the SkeletonCard fallback (no new spinner invented)",
    "renders the SkeletonCard fallback before the dynamic import resolves",
    "mounts the real page content once the lazy import resolves"
   ],
   "dashboard/limits-card.test.tsx": [
    "renders a skeleton while loading",
    "renders 3 window bars with pct + reset time for a full probe",
    "renders only 2 window bars when the probe has no Fable window",
    "shows an honest empty-state (no fabricated bars) when the probe is unavailable",
    "falls back to a generic error message when unavailable with no reason",
    "treats a null data response (fetch not yet resolved to unavailable) as honestly unavailable",
    "renders zero emoji-presentation characters"
   ],
   "dashboard/live-data.test.ts": [
    "file exists",
    "exports useSSE function",
    "exports useSSEWithStatus function",
    "defines SSEStatus type with all 3 states",
    "creates EventSource with the bootstrap-token-aware SSE URL",
    "sets connected on onopen",
    "parses JSON from event.data on onmessage",
    "sets disconnected on onerror with 3s reconnect",
    "cleans up EventSource and timeout on unmount",
    "exports SSEResult interface",
    "defaults url to /api/events",
    "file exists",
    "defines STATUS_BAR mapping",
    "EXECUTING status has animate-pulse class",
    "DONE status has green status bar",
    "NO_GO status has red status bar",
    "renders agent id",
    "renders taskId",
    "has NO kill button — worker termination left the dashboard",
    "has detail button",
    "has WorkerCardGrid component",
    "maps agents array to cards",
    "file exists",
    "defines MAX_ENTRIES = 50",
    "slices entries to MAX_ENTRIES limit",
    "tracks phase changes via prevPhaseRef",
    "tracks agent status changes via prevAgentsRef",
    "handles spawned event type",
    "handles done event type",
    "handles nogo event type",
    "auto-scrolls via bottomRef",
    "displays no-activity message when no sprint",
    "displays waiting message when sprint active but no entries",
    "file exists",
    "defines all 8 phases",
    "active phase has bg-brand-500 and animate-pulse",
    "completed phases have bg-green-500",
    "future phases have border-zinc-600",
    "has connector lines between phases",
    "determines phase state based on currentIndex",
    "shows checkmark SVG for completed phases"
   ],
   "dashboard/log-viewer.test.tsx": [
    "WorkerLogViewer.tsx exists",
    "exports WorkerLogViewer function component",
    "subscribes to log-backfill SSE event",
    "subscribes to log SSE event for live appends",
    "subscribes to done SSE event for stream termination",
    "uses EventSource for SSE connection",
    "uses buildSseUrl for auth token injection",
    "has no emoji characters",
    "renders events ordered by seq (sort by seq ascending)",
    "ResultPanel.tsx exists",
    "exports ResultPanel function component",
    "renders token usage fields",
    "renders cost fields",
    "renders files changed section",
    "renders tests section",
    "has no emoji characters",
    "uses lucide-react icons, not emoji",
    "renders the viewer container",
    "shows empty state before any events",
    "renders events from log-backfill in seq order",
    "appends new events from live log pushes without duplicating",
    "does not duplicate events with the same seq already in view",
    "renders turn events with the turn data-testid",
    "renders tool_use events with the tool_use data-testid",
    "renders stderr events with stderr data-testid",
    "renders usage events with usage data-testid",
    "marks stream as done when done event received",
    "accepts label overrides for i18n",
    "shows close button when onClose prop is provided",
    "does not show close button when onClose is not provided",
    "renders the panel container",
    "renders loading skeleton when result is null",
    "renders loading skeleton when result is undefined",
    "renders assessment section for DONE verdict",
    "renders assessment section for NO_GO verdict",
    "renders assessment section for GO_WITH_TECH_DEBT verdict",
    "renders tokens section with input/output/total",
    "shows provider badge in tokens section",
    "shows estimated badge for tokenizer-fallback source",
    "renders cost section with usd value",
    "shows Local label for isLocal cost",
    "renders files section with file rows",
    "renders tests section with passed/failed/total",
    "renders TSC section for clean build",
    "renders TSC section with error count for failing build",
    "renders go-criteria section with met/unmet items",
    "renders notes section when notes are present",
    "does not render notes section when notes are empty",
    "renders auditor section with OK status",
    "renders full result with all sections",
    "accepts label overrides for i18n"
   ],
   "dashboard/manual-token-input.test.tsx": [
    "renders password input with data-testid when open=true",
    "does not render dialog content when open=false",
    "calls login() with the entered token when /api/auth/me returns 200",
    "closes the dialog (onOpenChange false) after successful login",
    "shows error and does NOT call login() when /api/auth/me returns 401",
    "shows error when fetch throws (network error)",
    "calls onOpenChange(false) when cancel button is clicked",
    "resets token input and error state on cancel",
    "does not call login() when token input is empty"
   ],
   "dashboard/memory-read-hook.test.tsx": [
    "never lets a slower prior query overwrite the newer scoped response",
    "ignores a rejected request after its component has unmounted",
    "projects forbidden API responses and network failures without exposing raw request details",
    "retries a same-query request only when explicitly invoked"
   ],
   "dashboard/nav-render.test.tsx": [
    "renders all 17 nav links into the DOM (real React render, not source grep)",
    "renders Evolution / Nervous / Enterprise / Memory Explorer entries (Sprint 215 god-level pages)",
    "preserves the 6 existing nav entries (no regression on /, /history, /memory, /config, /chat, /status)",
    "Sidebar.tsx is the SINGLE canonical source: navItems is exported, has exactly 17 unique routes",
    "each nav item has an icon component and a displayable label or labelKey"
   ],
   "dashboard/no-emoji-guard.test.tsx": [
    "WorkerCard.tsx has zero emoji-presentation characters in the target ranges",
    "ActivityFeed.tsx has zero emoji-presentation characters in the target ranges",
    "SprintControlPanel.tsx and DashboardPage.tsx have zero emoji-presentation characters",
    "total emoji count across all target files is zero"
   ],
   "dashboard/oidc-flow.test.ts": [
    "produces a 43-char base64url verifier and a base64url challenge (real crypto)",
    "is non-deterministic across calls (real entropy)",
    "is deterministic under an injected mock crypto",
    "throws crypto_unavailable when Web Crypto subtle is missing",
    "returns a base64url token and a distinct value each call",
    "throws crypto_unavailable when getRandomValues is missing",
    "includes every required authorization-code + PKCE parameter",
    "honours a custom scope and preserves pre-existing endpoint query params",
    "throws invalid_authorization_endpoint for a non-URL endpoint",
    "extracts code + state from a successful callback (with leading '?')",
    "extracts code + state without a leading '?'",
    "passes through an IdP error with its description",
    "reports invalid_callback when code or state is missing",
    "returns true only for an exact match",
    "returns false on a value mismatch",
    "returns false on a length mismatch",
    "returns false for empty, null, or undefined inputs",
    "persists and loads the verifier / state / nonce",
    "clears persisted secrets (load returns undefined fields)",
    "round-trips a real PKCE pair + state for the callback validation path"
   ],
   "dashboard/pages.test.ts": [
    "file exists",
    "imports useApi hook",
    "fetches /api/history",
    "imports SprintChart component",
    "renders history table columns via i18n keys",
    "renders loading state",
    "renders error state",
    "renders empty state",
    "uses Card components",
    "has dark theme classes",
    "maps over data records",
    "file exists",
    "delegates to the shared bounded reader explorer"
   ],
   "dashboard/refresh-cooldown.test.tsx": [
    "RefreshButton.tsx file exists",
    "exports RefreshButton function component",
    "calls onRefetch prop on click via handleRefresh",
    "onClick handler wired to button element",
    "renders Refresh label when not in cooldown",
    "tracks cooldown boolean state",
    "button is disabled during cooldown",
    "starts cooldown on refresh click",
    "tracks countdown numeric state for remaining seconds",
    "displays countdown seconds in button label during cooldown",
    "uses setInterval to decrement countdown each second",
    "clears cooldown when countdown reaches zero",
    "supports configurable cooldownMs prop (default 10000ms)",
    "cleans up interval on unmount via useEffect return"
   ],
   "dashboard/request-dedup.test.ts": [
    "concurrent calls to the same URL share a single underlying fetch",
    "issues a fresh fetch after the previous in-flight request has settled (no TTL)",
    "does not fetch twice for two DIFFERENT URLs (dedup keys by URL)",
    "reference-counts release: an underlying request is only aborted once every subscriber has released it"
   ],
   "dashboard/route-sidebar-wire.test.tsx": [
    "imports EvolutionPage, NervousPage, EnterprisePage, MemoryExplorerPage",
    "registers /evolution, /nervous, /enterprise, /memory-explorer route paths",
    "each new route renders its page element",
    "preserves existing 7 routes (no regression)",
    "new routes live inside Layout wrapper (after the Layout open tag)",
    "exports navItems with the 4 new entries",
    "preserves the 6 existing nav entries",
    "source file mentions all 4 new route paths (kanit grep target)",
    "each new nav item has a displayable label and an icon",
    "SidebarNavLinks render block uses the label-or-labelKey pattern",
    "renders an anchor for each navItem in jsdom",
    "renders the 4 new pages with the correct href targets",
    "renders the 4 new pages with displayable label text",
    "preserves the existing 6 nav targets in the rendered DOM (no regression)"
   ],
   "dashboard/scaffold.test.ts": [
    "${file} exists",
    "package.json has correct name and type",
    "package.json has required dependencies",
    "package.json has required devDependencies",
    "package.json has correct scripts",
    "tsconfig.json has correct compiler options",
    "vite.config.ts contains proxy and port configuration",
    "index.html has root div and module script",
    "App.tsx uses Layout wrapper and routes",
    "index.css imports tailwindcss",
    "node_modules exists (npm install ran)",
    "useSSE hook has auto-reconnect logic",
    "useApi hook has loading/error/data states",
    "button component has all variants",
    "card component exports all sub-components"
   ],
   "dashboard/sidebar-bell-badge.test.tsx": [
    "returns pendingCount from /api/nervous/status data",
    "returns 0 when data is null (endpoint not yet loaded)",
    "returns 0 when pendingCount is 0 in data",
    "shows badge with count when pendingCount > 0",
    "hides badge when pendingCount is 0",
    "hides badge when data is null (not yet loaded)",
    "caps badge at 99+ when pendingCount exceeds 99",
    "imports useNervousStatus hook",
    "has nervous-bell-badge data-testid attribute",
    "badge is conditional on pendingCount > 0",
    "uses useNervousStatus to get pendingCount",
    "contains no emoji characters in sidebar source"
   ],
   "dashboard/sso-wire.test.tsx": [
    "imports AuthProvider from hooks/useAuth",
    "wraps the tree with <AuthProvider>",
    "has /login route",
    "has /auth/callback route",
    "imports LoginPage and CallbackPage",
    "imports AuthStatus",
    "imports AuthStatus from ./AuthStatus.js",
    "renders <AuthStatus /> inside the header block",
    "AuthStatus is placed inside the header element (not sidebar or main)",
    "file exists at expected path",
    "uses useAuth hook for isAuthenticated",
    "uses useNavigate for redirect",
    "includes ManualTokenInput component",
    "uses generatePkce from oidc-flow",
    "uses buildAuthorizeUrl and persistFlowSession",
    "has sso-login-button testid (conditionally rendered)",
    "has manual-token-open-button testid (always rendered)",
    "fetches /api/config to check dashboard_oidc.enabled",
    "default-off: SSO block is conditional on oidcConfig being set",
    "file exists at expected path",
    "uses parseCallbackParams from oidc-flow",
    "uses validateState for CSRF protection",
    "uses loadFlowSession to retrieve stored verifier + state",
    "calls clearFlowSession after handling",
    "calls login() on successful exchange",
    "navigates to /login?error= on error paths",
    "calls POST /api/auth/oidc/exchange with code + code_verifier",
    "renders callback-page testid"
   ],
   "dashboard/status-realtime.test.tsx": [
    "StatusPage.tsx exists",
    "uses useLiveData for real-time task polling",
    "done-render: liveTasks.data fed to SprintSummary for DONE task display",
    "polls with pollIntervalMs for live status updates (done/working/no_go)",
    "phase shown from SSE — useSSE drives sprint.phase in SprintSummary"
   ],
   "dashboard/terminal/DockPanel.test.tsx": [
    "starts collapsed and toggles open via the terminal toggle button",
    "toggles back to collapsed on a second click",
    "exposes a resize separator only when expanded",
    "maximizes and restores the dock, hiding the resize separator while maximized"
   ],
   "dashboard/terminal/TerminalPanel.test.tsx": [
    "opens a new shell tab on quick-launch",
    "invokes killSession when the close button is clicked"
   ],
   "dashboard/terminal/TerminalView.test.tsx": [
    "renders a container for the given session",
    "cleans up xterm + ResizeObserver on unmount"
   ],
   "dashboard/terminal/useTerminalSocket.test.tsx": [
    "opens WS with deckent.<token> subprotocol and sends attach on open",
    "forwards output frames to onOutput callback",
    "returns an api ref that sends input + resize JSON frames",
    "skips connect when sessionId is null (no WS instance)",
    "does NOT recreate the WS when onOutput changes across re-renders and routes output to the latest callback",
    "reconnects and re-sends attach after onclose (tmux-like reattach)"
   ],
   "dashboard/terminal-api.test.ts": [
    "tracks multiple sessions and lists them all",
    "removes a session by id",
    "returns undefined for unknown session id",
    "navigates up through history entries",
    "navigates down back toward current input (returns undefined at bottom)",
    "resets cursor after a new push",
    "does not add duplicate consecutive entries",
    "accumulates appended chunks and returns joined string",
    "returns empty string for unknown session",
    "clears buffer for a session",
    "keeps sessions isolated from each other",
    "createSession result can be registered in SessionRegistry",
    "reads the injected bootstrap token",
    "POSTs a session create"
   ],
   "dashboard/terminal-panel-gate.test.tsx": [
    "renders nothing when no terminal token is injected (terminal disabled)",
    "renders the terminal chrome when a terminal token is present (enabled)"
   ],
   "dashboard/terminal-sessions.test.ts": [
    "openSession registers the session and sets it as active",
    "opening multiple sessions tracks all and last becomes active",
    "switchSession changes the active session",
    "switchSession returns false for unknown id and does not change active",
    "closeSession removes the session and falls back to remaining",
    "closeSession sets active to null when no sessions remain",
    "pushCommand + navigateHistory up returns most recent first",
    "navigateHistory down returns towards current (undefined at bottom)",
    "history is isolated per session",
    "navigateHistory uses active session when no id given",
    "pushCommand with no active session is a no-op",
    "appendToBuffer accumulates chunks and getBufferContent returns joined output",
    "getBufferContent returns empty string for unknown session",
    "clearBuffer empties the accumulated output",
    "buffers are isolated between sessions",
    "copyToClipboard writes text and returns true on success",
    "copyToClipboard returns false when clipboard API throws",
    "getClipboardText returns clipboard content on success",
    "getClipboardText returns undefined when clipboard API throws"
   ],
   "dashboard/theme-consistency.test.ts": [
    "ölü token-ikizi lib/theme.ts yok",
    "ThemeProvider ve light yolu söküldü",
    "dark: utilities deterministik always-on (OS-tercihinden bağımsız)",
    "koyu zemin/mürekkep tek kaynaktan: @theme background/foreground",
    "tema i18n anahtarları kaldırıldı (en + tr)"
   ],
   "dashboard/types.test.ts": [
    "DashboardState satisfies type shape",
    "AgentInfo satisfies type shape",
    "Alert satisfies type shape",
    "SprintMetrics satisfies type shape",
    "DeckentConfig satisfies type shape with optional fields",
    "DashboardState with full metrics"
   ],
   "dashboard/unauthorized-banner.test.tsx": [
    "renders nothing until a deckent:unauthorized event fires",
    "surfaces the 401 banner when deckent:unauthorized is dispatched (the listener that was missing)",
    "dismisses the banner when the close button is clicked",
    "removes its listener on unmount (post-unmount event is a no-op, no leak)"
   ],
   "dashboard/use-auth.test.tsx": [
    "uses bootstrap token when both bootstrap and sessionStorage have tokens",
    "uses session token when bootstrap token is absent",
    "token is undefined and identity is null when neither source has a token",
    "populates OIDC identity on mount when bearer returns JWT claims",
    "populates static identity when bearer is an opaque token",
    "sets identity null and isAuthenticated false on 401 from /api/auth/me",
    "stores token in sessionStorage and updates token state after login",
    "calls /api/auth/me with the newly-set token after login",
    "clears sessionStorage and resets identity on logout",
    "preserves bootstrap token reference after logout but resets identity"
   ],
   "dashboard/use-live-data.test.ts": [
    "keeps previous data on next-poll failure (does NOT reset to null) and flips isStale=true",
    "retries fetch after disconnect and recovers",
    "aborts in-flight request and stops polling when component unmounts",
    "populates error and flips status to reconnecting when fetch returns non-ok response"
   ],
   "dashboard/useApi-token.test.ts": [
    "GET with token attaches Authorization: Bearer header",
    "GET without token sends no Authorization header",
    "POST with token attaches Authorization: Bearer header",
    "POST without token omits Authorization header"
   ],
   "dashboard/utils.test.ts": [
    "merges class names",
    "handles conditional classes via clsx",
    "deduplicates tailwind classes via twMerge",
    "handles undefined and null inputs",
    "returns empty string for no inputs",
    "handles array inputs",
    "handles object inputs",
    "merges conflicting tailwind utilities"
   ],
   "dashboard/worker-grid-live.test.tsx": [
    "WorkerGrid.tsx file exists",
    "exports WorkerGrid function component",
    "imports useLiveData for stale-while-revalidate polling",
    "polls /api/status for real-time worker updates",
    "derives worker list from live data with no fixed cap",
    "shows reconnecting indicator when connection is stale",
    "delegates rendering to WorkerCardGrid with live agents",
    "accepts and passes onSelect callback for agent detail view",
    "accepts NO onKill callback — worker termination left the dashboard",
    "uses empty array fallback when no data yet (no crash)"
   ],
   "dashboard/workers-comms-panel.test.tsx": [
    "renders the comms panel with data-testid when agents are present",
    "shows the DONE agent as a handoff entry",
    "shows completed-worker count for DONE workers",
    "renders the handoff list container when DONE agents exist",
    "shows the taskId of the DONE worker inside the handoff row",
    "shows empty state container when no agents are present",
    "still renders the worker-comms-panel wrapper in empty state",
    "renders the resource summary with backend info when agents are present",
    "does not render resource summary when no agents",
    "contains 'comms' keyword in source (kanit: grep requirement)",
    "contains 'shared' keyword in source",
    "contains 'handoff' keyword in source",
    "contains zero emoji-presentation characters in the comms additions"
   ],
   "dashboard/workers-directives-pages.test.tsx": [
    "contains exactly 2 matches for path=\"/workers\" | path=\"/directives\" (kanıt grep)",
    "imports both pages and renders them as route elements inside Layout",
    "Sidebar navItems include /workers and /directives with i18n labelKeys and icons",
    "renders a card per worker with id, task, model and status from SSE state",
    "shows the empty state when there are no workers",
    "renders NO kill button for executing workers and never POSTs /api/kill",
    "falls back to polling /api/status via useLiveData when SSE has no data",
    "loads DIRECTIVES.md from GET /api/directives into the read-only pre view",
    "has NO textarea, NO save button, and never POSTs /api/directives",
    "still shows the load warning when the read endpoint is unavailable",
    "warns while the content is empty (observable fact, no save affordance)",
    "dashboard.subtitle interpolates the worker count instead of a raw {n}",
    "no en/tr dictionary value carries a single-brace placeholder (root-cause sweep)",
    "routes status + pending through useLiveData with an enabled poll interval",
    "renders NO accept button and never POSTs /api/nervous/accept",
    "fetchJson attaches Authorization: Bearer when the bootstrap token is injected",
    "fetchJson sends no Authorization header when no token is injected",
    "api-client.ts re-exports the canonical client (no parallel token path)",
    "buildSseUrl appends the token as a query parameter for EventSource"
   ]
  },
  "docker": {
   "docker/dockerfile.test.ts": [
    "Dockerfile exists",
    "uses a Node 24 slim base matching the package runtime floor",
    "is currently a single-stage build",
    "installs tmux and git",
    "copies the exact root manifest and canonical npm shrinkwrap",
    "runs npm ci for dependency installation",
    "runs npm run build for TypeScript compilation",
    "sets workspace directory",
    "enters through the same compiled CLI declared by the npm package",
    "docker-compose.yml exists",
    "defines a deckent service",
    "mounts .deckent volume",
    "mounts .brain volume",
    "exposes port 3100",
    "has healthcheck configuration"
   ],
   "docker/dockerignore-secrets.test.ts": [
    "keeps .deck and .deck.* ignored without later negation",
    "keeps the .env secret family ignored without later negation",
    "keeps Dockerfile.worker COPY sources independent of .deck",
    "admits only the exact built runtime file needed by Dockerfile.worker",
    "keeps host adapter and analysis state out of the expanded package-root context"
   ],
   "docker/worker-image-multicli.test.ts": [
    "has ARG INSTALL_CODEX build-arg declaration",
    "has ARG INSTALL_GEMINI build-arg declaration",
    "defaults both build-args to false (Claude-only lean image)",
    "has conditional Codex install RUN block (opt-in)",
    "has conditional Gemini install RUN block (opt-in)",
    "always installs Claude CLI regardless of build args",
    "Dockerfile.worker file exists"
   ],
   "docker/worker-image-providers.test.ts": [
    "ships the same worker recipe for repository and packaged installations",
    "Dockerfile.worker exists",
    "Dockerfile.worker has commented Codex install line (opt-in pattern)",
    "Dockerfile.worker has commented Gemini install line (opt-in pattern)",
    "Dockerfile.worker has Claude CLI install (always present)",
    "source and packaged worker images pin the Fable 5.1-proven Claude CLI release",
    "source and packaged worker images contain the exact native runtime authority",
    "Dockerfile.worker HEALTHCHECK references claude --version"
   ]
  },
  "docs": {
   "docs/CHANGELOG.test.ts": [
    "CHANGELOG.md file exists",
    "file starts with # Changelog header",
    "references Keep a Changelog spec",
    "references Semantic Versioning spec",
    "all version headers use bracket format",
    "has multiple version entries",
    "extractVersionHeaders correctly parses all headers"
   ],
   "docs/api-md-no-stale-refs.test.ts": [
    "contains no MEMORY_FILE constant reference",
    "contains no DECISIONS_FILE constant reference",
    "contains no DEBT_FILE constant reference",
    "contains no .brain/MEMORY.md path reference",
    "contains no .brain/DEBT.md path reference",
    "has zero stale Memory V1 references (combined regex)",
    "documents Memory V2 DB-first architecture",
    "documents searchMemory() FTS5 API",
    "documents MemoryStore type-specific queries",
    "documents exports directory as auto-generated views",
    "documents deckent memory export CLI command",
    "MCP resource deckent://memory references exports path",
    "MCP resource deckent://debt references exports path",
    "HTTP GET /api/memory description references exports path",
    "HTTP GET /api/debt description references exports path"
   ],
   "docs/api.test.ts": [
    "exists and is non-empty",
    "contains authentication note",
    "is written in English"
   ],
   "docs/ci-hermeticity-rule.test.ts": [
    "karpathy-discipline.md exists and contains a CUSTOM Test Hermeticity section",
    "hermeticity keywords present: hermetic, tmpdir, gitignored (≥3 matches)",
    "ci-sim reference is present in the rule",
    "rule mentions ci-guardian agent and ci-testing skill for routing"
   ],
   "docs/claude-rules-no-legacy.test.ts": [
    "(a) PATTERNS.md reference count = 0 in all .claude/rules/*.md files",
    "(b) auditor.md contains memory.db pattern upsert instruction",
    "(c) auditor.md does not instruct appending to flat .md files for pattern storage"
   ],
   "docs/cli-reference.test.ts": [
    "is present in both languages and is generated by one canonical producer",
    "documents every public contract path and omits the hidden runtime child",
    "publishes the hidden path in the internal bilingual manifest",
    "keeps internal options out of both public references",
    "contains option, argument, and execution-contract tables",
    "contains none of the stale source-parser or known-gap projections",
    "rejects the retired project-identity path and preserves the current identity authority",
    "derives CLI_COMMANDS from the complete visible contract",
    "builds complete EN/TR rows from the live Commander tree",
    "renders deterministically and rejects an empty command projection",
    "reports every generated target in sync"
   ],
   "docs/config-reference.test.ts": [
    "exists and is non-empty",
    "documents PlanModeConfig fields with types and defaults",
    "is written in English"
   ],
   "docs/contributing.test.ts": [
    "exists and is non-empty",
    "contains Getting Started section",
    "contains Development Setup section",
    "contains Running Tests section",
    "contains Architecture Overview (project structure)",
    "contains How to Add CLI Command section",
    "contains How to Add MCP Tool section",
    "contains How to Create Plugin section",
    "contains PR Guidelines section",
    "contains Code Style section",
    "is written in English"
   ],
   "docs/doc-honesty.test.ts": [
    "keeps current EN/TR vision positioning free of retired anti-X framing",
    "Gate #8 is marked PARTIAL in beta-tracker.md",
    "Sprint 185-200 section is marked historical in ROADMAP-GOD-LEVEL.md"
   ],
   "docs/doc-pillars-links.test.ts": [
    "the Pillar Module Contracts section exists",
    "extracted a substantial number of file:line references from the section",
    "${ref.raw} → ${resolveRepoPath(ref.path) ?? '(unresolvable)'}",
    "${ref.raw} — line ${ref.line} <= total lines of ${resolved}",
    "\"${key}\" is mentioned and paired with a flag-gated label",
    "no internal markdown links are broken (0 found is valid — section is prose-only)"
   ],
   "docs/docs-structure.test.ts": [
    "docs/${lang}/ exists and is non-trivial",
    "EN and TR are structurally paired (same relative file set)",
    "every language tree carries the load-bearing sections",
    "generated reference docs live under docs/generated/<lang>/reference/",
    "docs/generated/ carries its hand-edit ban notice",
    "generated output never lands inside the hand-written docs/<lang>/ tree",
    "docs/generated/master-plan-active.{md,json} projections exist",
    "docs/adr/ holds the ADR corpus and its generated index",
    "docs/MASTER-PLAN.md (work SSOT) is present at the docs root",
    "docs/index.md exists",
    "both root READMEs exist with canonical names",
    "brain.md exists",
    "worker-default.md exists",
    "auditor.md exists"
   ],
   "docs/guide-getting-started.test.ts": [
    "exists and is non-empty",
    "is written in English",
    "exists and is non-empty",
    "contains plan preview step",
    "explains sprint lifecycle phases",
    "is written in English",
    "exists and is non-empty",
    "is written in English"
   ],
   "docs/issue-templates.test.ts": [
    "file exists",
    "is written in English",
    "has proper YAML frontmatter",
    "has environment section",
    "file exists",
    "is written in English",
    "has proper YAML frontmatter",
    "has Alternatives Considered section"
   ],
   "docs/jsdoc.test.ts": [
    "${file} — all exported functions have JSDoc",
    "at least 50 exported functions are documented across priority files",
    "JSDoc comments include @param tags for functions with parameters",
    "JSDoc comments include @returns tags",
    "JSDoc comments include @throws tags where applicable",
    "brain.ts (re-export layer) does not need JSDoc on re-exports"
   ],
   "docs/karpathy-rule-presence.test.ts": [
    "(a) .claude/rules/karpathy-discipline.md exists",
    "(b) file has at least 80 lines",
    "(c) file contains frontmatter with paths field",
    "(a) contains Discipline 1 — Think Before Coding",
    "(b) contains Discipline 2 — Simplicity First",
    "(c) contains Discipline 3 — Surgical Changes",
    "(d) contains Discipline 4 — Goal-Driven Execution",
    "(e) contains Quick Reference Checklist section",
    "(f) contains Source attribution section",
    "(a) worker-default.md exists",
    "(b) worker-default.md references karpathy-discipline.md",
    "(c) reference is in the CUSTOM section (not AUTO-generated)"
   ],
   "docs/landing-page-content.test.ts": [
    "exists and is non-empty",
    "contains Hero section with tagline",
    "contains CTA with npm install",
    "contains Problem Statement section",
    "contains three problem points",
    "contains Solution section",
    "contains How It Works section with 3 steps",
    "contains Features section",
    "contains Comparison section with competitors",
    "contains Pricing section (free, open source)",
    "contains Getting Started section",
    "contains Footer links",
    "is written in English"
   ],
   "docs/layer-shims.test.ts": [
    "fixture auto-discovers a newly created crossing",
    "fixture rejects count-preserving replacement and symbol broadening",
    "fixture requires explicit shrink after atom removal",
    "fixture accepts exactly one exact exception owner",
    "fixture denies stale, zero-owner, and multiple-owner exception atoms",
    "fixture rejects zero and multiple source ownership",
    "portable fixture resolves a relative non-source asset without graph ownership",
    "portable fixture rejects %s with exit 2",
    "portable fixture rejects wrong-case resolution and case collisions",
    "portable fixture discovers a new file inside a nested workspace",
    "portable fixture explicitly ignores %s directories",
    "portable fixture explicitly ignores %s",
    "portable fixture capability-probes and rejects %s containment",
    "fixture produces stable JSON for exit statuses 0, 1, and 2",
    "fixture initializer is no-clobber and atomic under replay",
    "fixture shrink-only writer refuses replacement without changing bytes",
    "registry has schema-v2 exact exceptions with lifecycle metadata",
    "registry projects production layers to unique owners and fail-closed policy",
    "registry assertion exposes checked-in ADR exception-projection drift",
    "registry topology projection happens only in a tmpdir"
   ],
   "docs/license.test.ts": [
    "LICENSE file exists",
    "contains MIT license text",
    "contains correct copyright holder",
    "package.json license field is MIT"
   ],
   "docs/pr-template.test.ts": [
    "file exists",
    "has Summary, Changes, Test Plan, and Checklist sections",
    "checklist includes key items"
   ],
   "docs/proof-of-function-rule.test.ts": [
    "proof-of-function section exists in worker-default.md",
    "Smoke directive requirement is documented",
    "mock-only → GO_WITH_TECH_DEBT downgrade is documented",
    "section references karpathy-discipline.md for full spec"
   ],
   "docs/quickstart.test.ts": [
    "exists and is non-empty",
    "is written in English"
   ],
   "docs/readme-number-truth.test.ts": [
    "48 MCP tools, 8 MCP resources",
    "21 built-in agents, 31 built-in skills (src/core/builtins)",
    "20 dashboard pages",
    "is written in English without Turkish section headings",
    "contains no legacy stale tool/agent/skill/page counts",
    "reflects the live MCP tool count (${MCP_TOOLS})",
    "reflects the generator-owned agent/skill counts (${README_AGENTS}/${README_SKILLS})",
    "reflects the live dashboard page count (${DASHBOARD_PAGES})",
    "carries the run/sprint terminology bridge",
    "contains no legacy stale tool/page counts",
    "reflects the live MCP tool count (${MCP_TOOLS})",
    "reflects the generator-owned agent/skill counts (${README_AGENTS}/${README_SKILLS})",
    "reflects the live dashboard page count (${DASHBOARD_PAGES})",
    "carries the run/sprint terminology bridge",
    "contains no legacy stale tool/agent/skill counts",
    "reflects the live tool count (${MCP_TOOLS})",
    "uses the parametric catalog contract instead of a fixed model count",
    "reflects the live built-in agent/skill counts (${BUILTIN_AGENTS}/${BUILTIN_SKILLS})",
    "carries the run/sprint terminology bridge",
    "points product direction to the tracked canonical vision instead of an ignored analysis note"
   ],
   "docs/refdocs-adr-regen.test.ts": [
    "recognizes adr-g-NNN-*.md and adr-d-NNN-*.md filenames + class-prefixed headings",
    "sorts ADR-D-* before ADR-G-*, numeric ascending within each class",
    "still recognizes legacy NNN-slug.md filenames + non-class headings (backward compat)",
    "skips README.md and other non-ADR markdown regardless of taxonomy",
    "does not descend into archive/ (old-numeric archived ADRs stay excluded)",
    "round-trips a hand-written trailer note (e.g. docs/adr/README.md \"Archived\" note)",
    "collectGenerations reports the live ADR count with zero drift against the committed README",
    "`node scripts/gen-reference-docs.mjs --check` reports docs/adr/README.md in sync with the live entry count"
   ],
   "docs/reference-drift.test.ts": [
    "mcp-tools.md tool count matches src/mcp/tools/ registrations (code-derived)",
    "mcp-tools.md contains deckent_models tool",
    "mcp-tools.md contains all 5 nervous system tools",
    "mcp.md parity table contains the models tool",
    "cli.md is the contract-generated full reference (surface-truth lane), not the reset-era stub",
    "mcp.md documents CLI parity for the models tool",
    "configuration-schema.md documents dependency_pipeline_enabled field",
    "configuration-schema.md documents worker_memory_limit field",
    "configuration-schema.md documents worker_memory_swap field",
    "documents BRAIN_TOTAL_LINE_BUDGET, MEMORY_MAX_LINES, MEMORY_DECAY_SPRINTS"
   ],
   "docs/release-checklist.test.ts": [
    "exists and is non-empty",
    "contains step 1: tsc --noEmit",
    "contains step 2: vitest run",
    "contains step 3: npm pack --dry-run",
    "contains step 4: CHANGELOG updated",
    "contains step 5: README updated",
    "contains step 6: version number",
    "contains step 7: git tag",
    "contains step 8: npm publish --dry-run",
    "contains step 9: npm publish",
    "contains step 10: GitHub release",
    "contains step 11: announcement",
    "is written in English"
   ],
   "docs/release-docs.test.ts": [
    "release.yml and publish.yml exist and are non-empty",
    "carries zero npm-auth-secret references anywhere in either workflow (SEC-06)",
    "documents that publishing is owner-manual — no automatic publish or GitHub Release (0.100.0 rebaseline)",
    "requests least-privilege permissions and gives a no-provenance owner-manual command",
    "every `uses:` action reference in both workflows is pinned to an immutable commit SHA",
    "every pinned action carries a human-readable version comment (audit trail)",
    "release.yml reads release notes from the ROOT CHANGELOG.md, not docs/CHANGELOG.md",
    "release.yml never falls back to a silent placeholder release-notes body",
    "runs the version-integrity + CI-attestation validation gates before the final artifact upload",
    "the CI-attestation gate covers BOTH required workflows (CI + Cross-Platform E2E), not CI alone (415-002 RC5B)",
    "publish.yml documents why its dry-run step needs no registry credential"
   ],
   "docs/release-notes-beta.test.ts": [
    "file exists",
    "is the v1.0.0-beta.1 public beta release notes",
    "states the Node.js >= 24 requirement",
    "reflects the current development-sprint scale",
    "documents the four-provider fleet",
    "documents the 15 built-in agents and 21 built-in skills",
    "describes the 8-phase sprint lifecycle",
    "describes Memory V2 DB-first architecture",
    "describes the Nervous System and Autonomous engine",
    "lists Ollama sprint-worker support as a partial limitation",
    "discloses ADR-037 RBAC runtime enforcement as advisory/soft",
    "does NOT carry the retired Sprint-042/047 release-notes format"
   ],
   "docs/release-prep.test.ts": [
    "package.json exists",
    "has name field",
    "has valid version field",
    "has description field",
    "has required keywords",
    "has original keywords preserved",
    "files field includes dist/",
    "exports field is defined",
    "exports field has import and types",
    "has bin field for CLI entry points",
    "has license field",
    "has engines field with node requirement",
    "type field is module (ESM)"
   ],
   "docs/rules-parity.test.ts": [
    "file exists",
    "contains \"${section.key}\"",
    "file exists",
    "contains \"${section.key}\""
   ],
   "docs/runtime-gitignore.test.ts": [
    "declares \"${entry}\"",
    "ignores .deckent/runtime/jobs/<file> (runtime job queue)",
    "ignores SQLite runtime sidecar %s",
    "ignores .deckent/prompts/<file>.jsonl (injection-audit trail)",
    "ignores .deckent/traces/<file> (execution traces)",
    "does NOT ignore a root user-scratch file (deneme.md-class)",
    "does NOT ignore unrelated tracked .deckent/ content (no blanket .deckent/ sweep)"
   ],
   "docs/security-md-current.test.ts": [
    "(a) Supported Versions shows 1.0.0-beta.x as current",
    "(a) legacy < 1.0 row is present and marked unsupported",
    "(a) obsolete 0.1.x version row is removed",
    "(c) README no longer says \"strict role boundaries\"",
    "(c) README says \"advisory\" role boundaries",
    "(c) README mentions audit trail and V2 post-GA"
   ],
   "docs/security.test.ts": [
    "file exists",
    "is written in English and contains key sections",
    "contains security contact email"
   ],
   "docs/series-metrics.test.ts": [
    "returns null for a sprint with no archive directory",
    "loads task JSON and .result files keyed by id",
    "folds a NO_GO root to DONE via a same-sprint fix",
    "does not descend into a pending fix-of-fix grandchild",
    "treats a cross-sprint-carryover fix task as its own root slot",
    "leaves a fix-less NO_GO root as NO_GO",
    "computes sprint-901 counts, duration, agreement, and fix-heal rate",
    "computes sprint-902 counts, mismatch agreement, and null fix-heal (no fix attempts)",
    "returns null for a sprint with no archive",
    "aggregates the 901-902 range into a cumulative rollup",
    "lists an unarchived sprint in `missing` and excludes it from the cumulative",
    "renders a table row per sprint plus a bold cumulative row",
    "surfaces a missing-sprint warning line",
    "writes MD and JSON reports to the given --out paths and returns 0",
    "returns exit code 2 when the sprint range is missing",
    "returns exit code 2 for an invalid (reversed) sprint range"
   ],
   "docs/validate-publish.test.ts": [
    "extracts files, package size, and file count from tarball output",
    "does not capture the Tarball Details metadata lines as files",
    "passes when no internal-state directories are in the tarball",
    "flags a leaked .brain/ path",
    "flags leaked .deckent/, .tasks/, and .locks/ internal state",
    "passes when both main and types are declared",
    "fails and names the missing field when types is absent",
    "fails when both entry-point fields are absent",
    "passes for engines.node >=24",
    "fails for engines.node that allows <24",
    "fails when engines.node is missing entirely",
    "passes for a sane package size under the 7 MB ceiling",
    "fails when the package size exceeds the 7 MB ceiling",
    "errors when the package size cannot be parsed",
    "GATES lists exactly the 8 readiness gates",
    "aggregates all 8 gate checks and reports a numeric summary",
    "reports not-ok when engines.node is below 24",
    "checkBinExecBits reports missing bin files (never throws)",
    "checkDashboardBundle reports a missing bundle (never throws)"
   ]
  },
  "e2e": {
   "e2e/chain-safety.e2e.test.ts": [
    "all checks PASS → gate passes and returns next sprint trigger",
    "3+ NO_GO tasks → gate fails with noGoCount reason",
    "cost >$100 threshold → gate fails with cost reason",
    "doctor check fails → gate fails",
    "tsc errors → gate fails with error count",
    "vitest pass rate below 99% → gate fails",
    "exactly 2 NO_GO tasks → gate passes (boundary)",
    "cost exactly at threshold ($100) → gate fails (strict less-than)",
    "multiple failures (cost + NO_GO + tsc) → all captured in failReasons",
    "empty sprint (0 tasks) → gate passes",
    "all tasks NO_GO → gate fails with abort signal",
    "vitest pass rate exactly 99% → gate passes (boundary)",
    "mixed DONE and TECH_DEBT with 0 NO_GO → gate passes",
    "cost $99.99 just under $100 threshold → gate passes",
    "3-task sprint → finalize → gate → auto-trigger decision",
    "sprint with failures → gate blocks chain"
   ],
   "e2e/cli-smoke.e2e.test.ts": [
    "SKIP: dist/cli/helpers/messages.js predates src/cli/helpers/messages.ts ",
    "T2: `deckent doctor --json` prints real, well-formed DoctorResult JSON",
    "SKIP: ${ENTRY} not found — run \\`npm run build\\` first"
   ],
   "e2e/cross-platform/linux-subprocess.test.ts": [
    "detects linux platform correctly",
    "completes 3 simulated tasks via subprocess workers",
    "captures stdout lines correctly in log file",
    "exit code 0 produces DONE selfAssessment",
    "exit code 1 produces NO_GO selfAssessment",
    "writes result file on SIGTERM before exiting"
   ],
   "e2e/cross-platform/macos-tmux.test.ts": [
    "detects darwin platform correctly",
    "tmux version is >= 3.3",
    "completes 3 simulated tasks via tmux sessions",
    "heartbeat contains valid ISO 8601 timestamp and workerId",
    "result file written atomically (write-to-temp + rename pattern)",
    "all test tmux sessions are cleaned up after kill"
   ],
   "e2e/cross-platform/wsl2-docker.test.ts": [
    "detects WSL2 environment correctly via uname -r",
    "docker info returns successfully",
    "docker daemon server OS matches WSL2 expectations",
    "completes 3 simulated tasks via Docker containers",
    "fs.watch detects file creation within Linux filesystem",
    "fs.watch detects file modification",
    "converts /mnt/c/ paths to Windows drive format",
    "returns null for non-mount paths",
    "handles nested paths with special characters",
    "detects /mnt/ drive mounts on actual WSL2 filesystem",
    "normalizes CRLF to LF in config content",
    "LF content remains unchanged after normalization",
    "mixed line endings are fully normalized",
    "handles binary-like content without corruption"
   ],
   "e2e/dashboard-sso-smoke.test.ts": [
    "T1: valid HS256 JWT → 200 + mode:oidc + sub/email/name claims",
    "T2: JWT signed with wrong secret → 403 forbidden",
    "T3: no Authorization header → 401 authentication required",
    "T4: static-token Bearer → 200 mode:static (localhost fallback unbroken)"
   ],
   "e2e/docker-backend.test.ts": [
    "fails closed when live child mode has no parent authority receipt",
    "rejects sandbox authority outside the physical OS temp root",
    "creates and removes only a child nonce under caller-owned temp authority",
    "sweeps only receipt-bound managed containers and proves zero survivors",
    "waits through a late owned-container arrival before declaring cleanup quiescent",
    "isAvailable() matches isDockerAvailable() sync result",
    "T11a: detectOrphans returns orphan task when HB exists but no task JSON",
    "T11b: detectOrphans ignores tasks in the active set",
    "T11c: detectOrphans handles empty .tasks/ directory gracefully",
    "T11d: detectOrphans auto-discovers active tasks when no set provided",
    "T12a: cleanupOrphanHBs archives orphan HB files to the canonical sprint archive",
    "T12b: cleanupOrphanHBs returns zero counts when no orphans exist",
    "T12c: cleanupOrphanHBs preserves active task HB files",
    "T13a: acquireLock creates .lock file in .locks/ directory",
    "T13b: acquireLock throws when another worker holds the lock",
    "T13c: releaseLock removes the .lock file",
    "T14: clearStaleLocks removes locks older than maxAgeMs",
    "T15: Docker backend source contains verifyResultAfterStop with fsyncSync",
    "T16: Docker backend monitorContainer performs host-side fsync on result file",
    "T17a: heartbeat sequencing is host-authority-owned; wrapper writes fixed spawn/exit markers",
    "T17b: heartbeat sequence field increases monotonically across writes",
    "T17c: stale heartbeat suppressed when .result has DONE assessment",
    "T18: EXECUTING → DONE transition: HB status updates when .result written",
    "T19: EXECUTING → FAILED with timeout marker when .result missing + non-zero exit",
    "T19b: state machine reconciliation: FAILED exitCode 137 overridden by DONE .result",
    "T20: Docker backend writes container_start_failed .timeout marker on spawn error",
    "T21: spawn-backend-docker source does NOT delete .prompt-* files in monitorContainer",
    "T22: spawn() creates .prompt-{taskId}-{hash}.txt filename (hash-based naming)",
    "T23: spawn() adds -fix suffix when isPriorityFix option is true",
    "T24: archivePromptFiles() moves prompt files to the canonical sprint archive",
    "T25: archivePromptFiles() does not delete prior immutable sprint archives",
    "T26: archivePromptFiles() returns zero counts when tasksDir has no prompt files"
   ],
   "e2e/docker-hb-shutdown.test.ts": [
    "writes file atomically — file content is correct after write",
    "does not leave .tmp file after successful write",
    "overwrites existing file atomically",
    "handles large files (>64KB to exceed typical OS buffer)",
    "returns true when .result file exists and is fsync-able",
    "returns false when .result file does not exist",
    "does not corrupt file contents after fsync",
    "writeResult creates .result file with correct content",
    "writeResult does not leave .tmp artifact",
    "writeResult with GO_WITH_TECH_DEBT assessment",
    "finalizes HB as DONE when .result has DONE assessment",
    "finalizes HB as DONE when .result has GO_WITH_TECH_DEBT assessment",
    "leaves HB untouched when .result has NO_GO assessment",
    "leaves HB untouched when .result does not exist",
    "leaves HB untouched when .result has invalid JSON",
    "HB .tmp file does not remain after atomic write",
    "worker script contains fsync_file function definition",
    "worker script has both EXIT and TERM signal traps",
    "kill() uses configurable grace period (default 15s)",
    "kill() calls verifyResultAfterStop for post-stop verification",
    "monitorContainer fsync result file from host after container exit"
   ],
   "e2e/docker-oom-reproducer.test.ts": [
    "worker script writes .partial-result BEFORE Claude CLI starts",
    "worker script defines PRFILE variable for .partial-result path",
    ".partial-result contains partialMarker:true for host-side detection",
    ".partial-result is fsynced to disk after write",
    ".partial-result is cleaned up on normal exit (after Claude CLI)",
    "on_exit function cleans up .partial-result when .result already exists",
    "on_exit EXIT trap cleans up .partial-result after writing fallback .result",
    "DockerSpawnBackend constructor accepts gracefulTimeoutSeconds option",
    "kill() uses configurable grace period instead of hardcoded 15",
    "default graceful timeout is 15 seconds",
    "SpawnBackendFactoryOptions includes dockerGracefulTimeoutSeconds",
    "SpawnBackendFactory forwards gracefulTimeoutSeconds to DockerSpawnBackend",
    "monitorContainer checks for .partial-result when .result is missing",
    "monitorContainer distinguishes OOM kill (exit 137) in promoted result notes",
    "promoted .partial-result is fsynced from host side",
    ".partial-result is cleaned up after promotion",
    "monitorContainer detects and overwrites corrupt .result JSON",
    "corrupt .result is unlinked before writing fallback",
    "simulated .partial-result can be promoted to .result",
    "corrupt .result JSON is detected and replaced",
    ".partial-result with valid JSON survives container kill and is promotable",
    "timeout exit (124) is distinguished from OOM kill (137) in notes",
    "container logs are extracted BEFORE container removal",
    "docker logs output is saved to .log file",
    "docker logs has a timeout to prevent hanging on large output"
   ],
   "e2e/event-stream-runtime.test.ts": [
    "full pipeline: writeEvent → readEvents → reconstructState correctly aggregates sprint state",
    "sprint simulation uses ≥13 distinct channels (ADR-035 must-have coverage)",
    "full sprint simulation produces ≥50 events (real sprint scale)",
    "sequence numbers are strictly monotonic across all events in full sprint",
    "afterSequence filter enables incremental streaming reads",
    "can filter by source/channel on a large event stream without performance issues",
    "handles a corrupted sprint event file without crashing (partial write safety)",
    "reconstructState captures phase change ordering from PLAN→CLEANUP",
    "events from different sprint IDs are isolated from each other",
    "getCurrentSprintId → writeEvent integration writes to the active sprint stream"
   ],
   "e2e/first-sprint.test.ts": [
    "planSprint creates task files from single-task directives",
    "planSprint creates task files from multi-task directives",
    "task files have valid schema",
    "task files have correct sprint ID",
    "sprint has correct phase and status after planning",
    "loadTaskFiles reads task files from .tasks/",
    "loadTaskFiles returns empty array when no tasks",
    "loadTaskFiles ignores non-task files",
    "resetDashboard creates .dashboard file after plan",
    "doctor passes on project with active plan",
    "getMemoryHealthLabel returns correct labels",
    "getProviderSummary formats correctly",
    "getReadinessLabel returns READY when all required pass",
    "getReadinessLabel returns NOT READY when required fails",
    "getReadinessLabel returns READY with warnings for optional failures",
    "countDebtItems returns zero for fresh project",
    "getProviderTips suggests correct setup for missing providers",
    "full cleanup removes all project artifacts"
   ],
   "e2e/init-builtin-seed.test.ts": [
    "seedBuiltins() creates 15 agent directories",
    "seedBuiltins() creates 21 skill directories",
    "each agent has agent.json + PROMPT.md",
    "each skill has manifest.json + SKILL.md",
    "agent.json files are valid JSON with required fields",
    "skill manifest.json files are valid JSON with required fields",
    "idempotent: user overrides are preserved on re-seed",
    "does not seed temp agents",
    "PROMPT.md files have non-trivial content",
    "SKILL.md files have non-trivial content",
    "src/core/builtins/agents/ contains exactly 22 agents",
    "src/core/builtins/skills/ contains exactly 35 skills",
    "no temp agents in builtins source",
    "bundled stats are zeroed (clean distribution)"
   ],
   "e2e/install-flow.test.ts": [
    "init creates .deckent/ directory",
    "init creates .brain/ directory with sprints/",
    "init creates .tasks/ directory",
    "init creates .locks/ directory",
    "init creates DECKENT.md with project name",
    "init creates AGENTS.md referencing DECKENT.md",
    "init creates CLAUDE.md referencing DECKENT.md",
    "init creates all brain files",
    "init creates DIRECTIVES.md template",
    "init creates config.json with correct mode",
    "init creates config with Turkish language when specified",
    "init creates .claude/rules/ with agent rule files",
    "init creates .gitignore with correct entries",
    "init creates i18n files",
    "init creates workspace BOOT.md and TOOLS.md",
    "init creates PROJECT-IDENTITY.md with project name",
    "formatWelcomeBanner returns welcome text",
    "formatDetectedSetup shows node version and providers",
    "formatSetupProgress shows completed steps",
    "formatNextSteps returns English instructions",
    "formatNextSteps returns Turkish instructions",
    "doctor passes required checks on fresh init (node + git)",
    "doctor detects workspace on initialized project",
    "doctor detects brain dir on initialized project",
    "doctor detects directives on initialized project",
    "cleanup removes project directory completely",
    "cleanup handles non-existent directory gracefully"
   ],
   "e2e/install-matrix/fresh-install.test.ts": [
    "Node ${nodeVersion} fresh install + mini sprint → PASS",
    "npm ci exit 0 (no peer dependency warnings)",
    "deckent --version returns correct version"
   ],
   "e2e/kpi-surface-smoke.test.ts": [
    "T1: GET / → 200 (dashboard shell served by real binary)",
    "T2: GET /api/kpi → 200 + non-empty kpis[] + cost_per_sprint has numeric value",
    "T3: GET /api/kpi/trend?kpiId=cost_per_sprint → 200 + non-empty series[]",
    "SKIP: ${ENTRY} not found — run \\`npm run build\\` first"
   ],
   "e2e/nervous-bridge-delivery.test.ts": [
    "bridges critical+actions notification as human-checkpoint-required",
    "bridges critical without actions as task-no-go",
    "bridges warning severity as task-no-go",
    "bridges info severity as task-done",
    "bridges emergency+actions as human-checkpoint-required",
    "deduplicates same notification ID (bridge fires only once)",
    "nervous dispatch does not crash when global NotifyDispatcher is null",
    "prepends [Nervous] prefix to title in bridge notification",
    "passes sprintId from NervousNotification to bridge notification",
    "uses \"unknown\" sprintId when NervousNotification has no sprintId"
   ],
   "e2e/nervous-flow.test.ts": [
    "detects scope collision when 2 tasks write same file in PLAN phase",
    "triggers emergency alert when DIRECTIVES.md reverts to template mid-EXECUTE",
    "detects a worker whose exact attempt the host authority reports dead",
    "detects corrupt agent ID and routing anomaly in EVALUATE phase",
    "detects debt trend when 3-sprint average exceeds 15% threshold"
   ],
   "e2e/notify-sprint-lifecycle.test.ts": [
    "emits sprint-started event via both eventBus and NotifyDispatcher",
    "emits task-done event with correct payload",
    "emits sprint-finalized event after lifecycle completion",
    "maintains correct event ordering for full lifecycle sequence",
    "emits task-no-go with warning priority",
    "handles full NO_GO lifecycle: sprint-started → task-no-go → sprint-finalized",
    "emits human-checkpoint-required with critical priority (immediate dispatch)",
    "works when no global dispatcher is registered (eventBus only)",
    "continues when adapter throws error (fail-safe)",
    "includes details field when provided",
    "createNotification maps event names to correct priorities",
    "toEventPayload produces correct structure for event stream"
   ],
   "e2e/npm-pack-smoke.test.ts": [
    "npm pack creates tarball with correct package name, version, and file list",
    "tarball ships the exact canonical shrinkwrap and no competing root lock",
    "tarball preserves exec-bit (0o755) on dist/cli/entry.js and dist/mcp/server.js",
    "installed binary runs --version and installed entry.js has exec-bit",
    "tarball includes dist/mcp/server.js (MCP server binary)"
   ],
   "e2e/provider-matrix/claude-codex-mixed.test.ts": [
    "routes each task to the correct provider based on forceModel",
    "model tiers are correctly identified for each task model",
    "model providers are correctly identified",
    "falls back to Claude when Codex is unavailable",
    "model equivalence maps gpt-4.1 to Claude canonical standard on fallback",
    "model equivalence maps gpt-5.5 to Claude canonical premium on fallback",
    "throws ProviderUnavailableError when both providers unavailable",
    "throws when no fallback_provider configured",
    "tracks latency, tokens, and cost per provider per task",
    "estimateCost uses correct model pricing from ModelRegistry",
    "aggregates metrics by provider for retrospective report",
    "generates retro-compatible summary from aggregated stats"
   ],
   "e2e/provider-smoke.test.ts": [
    "should have correct name and supported models",
    "buildCommand produces valid claude CLI string with model",
    "buildCommand includes allowedTools when specified",
    "buildCommand includes --dangerously-skip-permissions when autoApprove",
    "buildCommand without opts produces clean command",
    "should have correct name and supported models",
    "buildCommand produces valid codex exec string",
    "buildCommand includes --full-auto in base command",
    "buildCommand always includes --full-auto regardless of autoApprove option",
    "listWorkers returns empty array initially",
    "should have correct name and supported models",
    "buildCommand produces valid gemini CLI string",
    "validateApiKey returns invalid when key not set",
    "buildApiScript produces valid JS with fetch call",
    "buildStreamingApiScript produces SSE streaming script",
    "getEndpoint and getStreamingEndpoint produce correct URLs",
    "registers providers and sets first as default",
    "setDefault changes the default provider",
    "unregisterProvider resets default when removing current default",
    "clear removes all providers",
    "getProvider throws for unregistered provider",
    "returns original provider when available",
    "falls back to alternative provider when primary unavailable",
    "applies model equivalence during fallback: sonnet → gpt-4.1",
    "applies model equivalence during fallback: haiku → gpt-5-mini",
    "throws ProviderUnavailableError when no fallback configured",
    "throws when both primary and fallback are unavailable",
    "fallback to gemini maps opus → gemini-2.5-pro",
    "getModelTier returns correct tiers",
    "getEquivalentModel maps across providers correctly",
    "getEquivalentModel returns same model for same provider",
    "getModelProvider returns correct provider",
    "MODEL_TIERS covers all three tiers",
    "detectAvailableProviders returns array of 5 providers (claude, codex, cursor, gemini, ollama)",
    "each detected provider has required fields"
   ],
   "e2e/serve-endpoints-smoke.test.ts": [
    "T1: GET /api/status → 200 idle (fresh project, no .dashboard yet)",
    "T1b: GET /api/status without Bearer token → 401 (auth active)",
    "T2: GET /api/limits → 200 real fail-honest unavailable (claude unresolvable by design)",
    "T2b: GET /api/limits without Bearer token → 401 (auth active)",
    "T3: GET /api/evaluate-health → 200 clean (no recent-works dir)",
    "T3b: GET /api/evaluate-health without Bearer token → 401 (auth active)",
    "T4: GET /api/approvals/history → 200 empty page (fresh approvals store)",
    "T4b: GET /api/approvals/history without Bearer token → 401 (auth active)",
    "SKIP: ${ENTRY} not found — run \\`npm run build\\` first"
   ],
   "e2e/single-provider.test.ts": [
    "registry contains ONLY codex — no claude, no gemini",
    "default provider is codex",
    "resolveProviderWithFallback selects codex for gpt-4.1 tasks",
    "resolveProviderWithFallback selects codex for gpt-5 tasks",
    "spawn is called on codexAdapter for codex tasks",
    "spawn called for both tasks in a 2-task sprint",
    "requesting claude provider throws ProviderNotFoundError",
    "requesting gemini provider throws ProviderNotFoundError",
    "buildCommand produces codex-flavored string",
    "resolveProviderWithFallback throws when requesting claude with no fallback",
    "registry contains ONLY gemini — no claude, no codex",
    "default provider is gemini",
    "resolveProviderWithFallback selects gemini for gemini-2.5-pro",
    "resolveProviderWithFallback selects gemini for flash models",
    "spawn is routed to geminiAdapter",
    "spawn called for 2-task gemini sprint",
    "requesting claude throws ProviderNotFoundError",
    "requesting codex throws ProviderNotFoundError",
    "buildCommand produces gemini-flavored string",
    "resolveProviderWithFallback throws when requesting claude with no fallback",
    "registry is empty",
    "getDefault throws ProviderError (not silently returning claude)",
    "getProvider(\"claude\") throws",
    "getProvider(\"codex\") throws",
    "getProvider(\"gemini\") throws",
    "resolveProviderWithFallback throws when no providers exist",
    "resolveProviderWithFallback throws even with fallback_provider configured but unregistered",
    "resolveProviderWithFallback error message does not default to claude",
    "hasProvider returns false for all providers",
    "no claude registered",
    "codex isAvailable returns false",
    "gemini isAvailable returns true",
    "fallback: gpt-4.1 (standard tier) maps to gemini-2.5-flash",
    "fallback: gpt-5 (premium tier) maps to gemini-2.5-pro",
    "fallback: gpt-5-mini (economy tier) maps to gemini-2.0-flash",
    "fallback result reason mentions both providers",
    "throws when both codex and gemini are unavailable",
    "throws when no fallback_provider configured and codex is down",
    "gemini adapter spawn is callable after fallback resolution",
    "codex spawn is NOT called during fallback"
   ],
   "e2e/skill-package-sync-proof.test.ts": [
    "delivers authored deckent profiles and bodies without overwriting local bodies"
   ],
   "e2e/sprint-160-smoke.test.ts": [
    "E2E-1: mini-sprint phase transitions are visible on state.json (PLAN → SPAWN → EVALUATE)",
    "E2E-2: checkpoint.eventStreamOffset > 0 after events.jsonl writes",
    "E2E-3: events.jsonl sequence numbers are monotonically increasing"
   ],
   "e2e/sprint-lifecycle.test.ts": [
    "single task DONE → correct evaluation and metrics",
    "mixed results → correct counts (no double counting)",
    "timeout task → detected via .timeout marker",
    "multiple workers with queue → all results collected",
    "empty sprint → zero metrics",
    "writes .result file for DONE scenario",
    "writes .timeout file for TIMEOUT scenario",
    "writes .hb heartbeat file",
    "list() returns active workers",
    "isAvailable() returns true",
    "finalized sprint produces metrics compatible with chain safety gate"
   ],
   "e2e/subprocess-backend.test.ts": [
    "uses CLAUDE_SUBPROCESS_CONFIG by default",
    "accepts custom timeout and provider config",
    "writes .hb file immediately on spawn with EXECUTING status",
    "delivers prompt text to worker stdin",
    "listWorkers() includes spawned taskId",
    "captures worker stdout to .log file",
    "writes fallback .result with selfAssessment when worker exits code 0 without result",
    "writes fallback .result with NO_GO when worker exits non-zero without result",
    "does NOT overwrite existing .result file",
    "writes heartbeat with DONE status after exit code 0",
    "writes heartbeat with FAILED status after non-zero exit",
    "terminates a running worker and removes it from list",
    "throws ProviderError when killing unknown taskId",
    "throws ProviderError when spawning same taskId twice",
    "automatically kills worker after configured timeout",
    "tracks multiple concurrent workers independently",
    "returns formatted command string from CLAUDE_SUBPROCESS_CONFIG",
    "includes allowedTools and autoApprove flags",
    "resolves true when CLI is accessible (node)",
    "resolves false when CLI is not found",
    "getLogPath() returns correct path",
    "getProjectDir() returns constructor dir",
    "getWorkerEntry() returns undefined for unknown taskId",
    "getWorkerEntry() returns entry for active worker",
    "uses custom CLI command and args",
    "returns a SubprocessSpawnBackend instance",
    "passes options through to constructor",
    "SubprocessBackend wrapper delegates to SubprocessSpawnBackend",
    "buildArgs includes model flag",
    "buildArgs includes allowedTools when provided",
    "buildArgs includes --dangerously-skip-permissions when autoApprove",
    "buildArgs omits optional flags when not set",
    "buildCommandString formats correctly"
   ],
   "e2e/test-containment-canary.test.ts": [
    "never treats a skip or raw host PID as containment proof"
   ],
   "e2e/tmux-backend.test.ts": [
    "stores message and optional command",
    "command is undefined when not provided",
    "produces default Claude CLI command without adapter",
    "includes --allowedTools when opts.allowedTools is set",
    "includes --dangerously-skip-permissions when autoApprove is true",
    "wraps with timeout when taskId is provided",
    "uses custom timeout seconds when provided",
    "delegates to adapter.buildCommand when adapter is provided",
    "WORKER_TIMEOUT_SECONDS defaults to 1200",
    "does not wrap timeout when timeoutSeconds is 0",
    "buildClaudeCommand is same reference as buildWorkerCommand (backward compat)",
    "creates TmuxBackend when backend=\"tmux\"",
    "TmuxBackend implements SpawnBackend interface",
    "TmuxBackend.isAvailable() checks tmux -V",
    "isTmuxAvailable() returns boolean matching tmux presence",
    "createAsync rejects when tmux is not available",
    "auto mode selects tmux or subprocess based on availability",
    "stores backendName and message",
    "T1: tmux -V returns a valid version string",
    "T2: can create and destroy a tmux session",
    "T3: new-window creates a named worker window",
    "T4: send-keys sends command and capture-pane reads output",
    "T5: kill-window removes the target window without affecting others",
    "T6: list-windows returns worker windows filtered by w- prefix",
    "T7: pipe-pane redirects window output to a log file",
    "T8: has-session returns 0 for existing session, non-zero for missing",
    "T9: kill-window on non-existent window returns non-zero status",
    "T10: multiple worker windows can be created and individually killed",
    "T11: prompt file can be written and read from .tasks/ directory",
    "T12: send-keys can execute a command that reads from a prompt file",
    "T13: TmuxBackend.isAvailable() returns true on system with tmux",
    "T14: SpawnBackendFactory auto mode includes tmux as candidate",
    "T15: heartbeat file pattern works with tmux window lifecycle",
    "T16: auditor window can be created idempotently"
   ],
   "e2e/worker-comms-flow.test.ts": [
    "T2 bridge: collectResults writes worker sharedNotes into SharedMemory",
    "T5 bridge: wireHandoffsForCompletedTasks persists handoffNotes into HandoffProtocol",
    "full roundtrip: Worker A shared notes and handoff message both appear in Worker B prompt",
    "control: disabled worker_comms — no shared-context or handoff blocks in Worker B prompt"
   ]
  },
  "extensions": {
   "extensions/vscode-panel-data.test.ts": [
    "connected: returns run.status data with connection \"connected\"",
    "empty-data: skips the call and stays connected/null when runId is omitted",
    "disconnected: a transport-kind RpcBridgeError maps to connection \"disconnected\"",
    "connected-with-error: an rpc-kind RpcBridgeError stays connection \"connected\"",
    "connected: returns the sessions array with connection \"connected\"",
    "empty-data: an empty sessions array is a valid connected state, not an error",
    "disconnected: a transport-kind RpcBridgeError maps to connection \"disconnected\"",
    "binds both sections and stamps fetchedAt from the injected clock",
    "one section disconnected never blocks or discards the other section",
    "empty-data: runId omitted and an empty session list both surface as connected/empty"
   ],
   "extensions/vscode-panel-refresh.test.ts": [
    "calls unref() on the interval timer so it never keeps the process alive",
    "start: fetches immediately, then again on every interval tick",
    "stop: dispose() halts further polling",
    "dispose is idempotent — a second call does not throw or double-clear",
    "refreshNow: triggers an immediate fetch outside the poll cadence and returns the snapshot",
    "honest connection-loss: a poll tick that fails reports disconnected, never reuses stale connected data",
    "never fires onData after dispose, even for a tick already in flight",
    "found: returns the matching session status; agent/model/resultSummary are always null",
    "not-found: an unmatched taskId reports found:false + status null, connection still forwarded",
    "disconnected: a transport error on the task-list section is forwarded honestly, not masked as not-found",
    "connected-with-rpc-error: an rpc-kind error on the task-list section is forwarded, connection stays \"connected\""
   ],
   "extensions/vscode-panel.test.ts": [
    "posts a well-formed envelope and returns the typed result",
    "omits Authorization header when no token is configured",
    "sends empty params and returns the sessions array",
    "returns the limits record",
    "surfaces a structured RPC-level error without throwing",
    "forwards an explicit scopeId",
    "omits scopeId from params when not provided",
    "returns a transport error on network failure (fetch rejects)",
    "returns a transport error on non-2xx HTTP status",
    "returns a transport error on a malformed (schema-invalid) 200 response body",
    "binds all 4 sections and stamps fetchedAt from the injected clock",
    "skips the run.status call and leaves runStatus empty when runId is omitted",
    "folds a failing section into PanelSection.error without discarding the others",
    "posts a deckent.panelData message to the webview and returns the data",
    "renders populated sections",
    "renders an error message for a failed section",
    "renders an RPC-level error message via error.error.message",
    "HTML-escapes hostile data instead of injecting raw markup"
   ],
   "extensions/vscode-rpc-bridge.test.ts": [
    "uses the established RPC envelope and omits an absent reason",
    "forwards a rejection reason",
    "returns api_decide_disabled as a typed RPC failure without throwing",
    "keeps an existing read call shape unchanged",
    "renders actions for non-critical rows and only a CLI hint for critical rows"
   ]
  },
  "github": {
   "github/.github-files.test.ts": [
    "should have valid YAML frontmatter",
    "should have security label",
    "should contain Vulnerability Type section",
    "should list common vulnerability types",
    "should contain Severity Assessment section",
    "should define severity levels with CVSS ranges",
    "should have CVSS score field",
    "should contain Steps to Reproduce section",
    "should request environment details",
    "should contain Impact Assessment section",
    "should define impact scope details",
    "should request affected component information",
    "should contain Responsible Disclosure section",
    "should define CVSS threshold for email disclosure",
    "should specify response and patch timelines",
    "should mention security advisory (GHSA)",
    "should request affected components examples",
    "should warn against public disclosure of exploits",
    "should include Proposed Fix section",
    "should include Timeline & Patches section",
    "should mention CHANGELOG credit",
    "should be valid YAML",
    "should have github sponsors section",
    "should have custom funding links section",
    "should reference GitHub documentation",
    "should include helpful comments",
    "should document link format in comments",
    "should include example funding platforms",
    "should list alternative sponsorship methods",
    "should have at least one custom funding link",
    "should have custom links starting with https",
    "should mark alternative methods as commented out",
    "should include npm tidelift example",
    "both files should exist",
    "security template should mention responsible disclosure",
    "funding file should reference GitHub docs"
   ],
   "github/ci-workflow.test.ts": [
    "should exist",
    "should have name CI",
    "should trigger on push to main",
    "should use checkout@v4",
    "should use setup-node@v4",
    "keys every root npm cache explicitly from npm-shrinkwrap.json",
    "uses the independent dashboard lock in every CI job that caches dashboard dependencies",
    "regenerates the npm lock graph but diffs only the canonical root shrinkwrap",
    "keys the coverage cache from root shrinkwrap plus the independent dashboard lock",
    "should have a coverage job",
    "should run test:coverage command",
    "should upload coverage artifact",
    "should set retention days for coverage artifact",
    "should upload coverage/ directory",
    "coverage workflow is scheduled + dispatchable (535: no per-merge run)",
    "should have a security job",
    "should run the fail-closed dependency-audit gate (SEC-05, 419-003)",
    "should name the security audit step",
    "should have a test-docs-scripts job",
    "should run docs tests in the isolated job",
    "should run scripts tests in the isolated job",
    "test-docs-scripts should not include docs or scripts in other jobs",
    "test-docs-scripts should depend on typecheck",
    "should run the docs-scripts flake canary env on the existing step (523-004)",
    "test-docs-scripts should keep continue-on-error until the RCA acceptance series is met",
    "keys both dashboard workflow caches from root shrinkwrap plus the dashboard lock",
    "should have a test-dashboard job",
    "should run npm run test:dashboard",
    "test-dashboard should depend on typecheck",
    "build job should depend on test-dashboard",
    "uses the durable runner-temp packed-networkless receipt as proof authority",
    "should still have typecheck job",
    "should still have test-core job",
    "should still have test-orchestra job",
    "should still have test-cli job",
    "should still have test-remaining job",
    "should still have build job with dist verification",
    "should still have windows allow-failure job",
    "should still use matrix strategy for node versions",
    "coverage job should have timeout",
    "test-docs-scripts job should have timeout"
   ],
   "github/dependabot.test.ts": [
    "should exist",
    "should be readable",
    "should have version 2",
    "should have updates section",
    "should exist in file",
    "should have weekly schedule",
    "should have open-pull-requests-limit",
    "should have commit-message prefix",
    "should have labels",
    "should have ignore configuration",
    "should exist in file",
    "should have weekly schedule",
    "should have open-pull-requests-limit",
    "should have commit-message prefix",
    "should have labels",
    "should have proper indentation",
    "should not have duplicate ecosystems"
   ],
   "github/workflows/ci.test.ts": [
    "should have correct name",
    "should trigger on push to main",
    "should trigger on pull_request to master",
    "should define all required jobs",
    "should run npm run lint",
    "should use Node.js 24.x (Active LTS)",
    "should test across multiple Node.js versions",
    "should depend on typecheck",
    "should run core tests",
    "should run orchestra tests",
    "should run CLI tests",
    "should run remaining tests",
    "should set timeout-minutes for test steps",
    "should depend on test jobs",
    "should run npm run build",
    "should verify dist files exist",
    "should verify shebang in CLI entry point"
   ],
   "github/workflows/release.test.ts": [
    "should have correct name",
    "should trigger on tag push matching v*",
    "should request least-privilege permissions — no publish/OIDC/write (0.100.0 rebaseline)",
    "should define actions:read permission (REL-02 — gh run list needs it)",
    "should define release job",
    "should run on ubuntu-latest",
    "should have required steps",
    "should use actions/checkout pinned to an immutable commit SHA (SEC-06)",
    "should fetch full history (fetch-depth: 0)",
    "should use actions/setup-node pinned to an immutable commit SHA (SEC-06)",
    "should specify node-version 24.x (Active LTS)",
    "should enable npm cache",
    "should key npm cache from root shrinkwrap plus the independent dashboard lock",
    "should configure registry-url for npm",
    "should exist, before Install dependencies",
    "should derive the tag version from GITHUB_REF_NAME",
    "should read package.json and npm-shrinkwrap.json versions",
    "should compare tag/package/shrinkwrap top-level/root-package versions and fail with exact evidence",
    "should run a registry-occupancy preflight via npm view",
    "should honestly warn (not silently pass) on a network/registry error, distinct from a clean 404",
    "should exist, before Install dependencies",
    "should query gh run list for this exact commit SHA against the CI workflow",
    "should require at least one successful CI run and fail with a named REL-02 error",
    "should ALSO query gh run list for the Cross-Platform E2E workflow, same commit SHA (T20 active Linux proof)",
    "should require at least one successful Cross-Platform E2E run and fail with a distinctly-named REL-02B error",
    "CI check and Cross-Platform E2E check are two separate named errors on two separate log lines — not merged",
    "should authenticate gh via GITHUB_TOKEN (no extra PAT)",
    "should use npm ci",
    "should exist, after CI attestation + install, before build",
    "should invoke the fail-closed gate script (not a bare `npm audit`)",
    "should carry zero continue-on-error anywhere in this workflow — audit is a hard gate, not advisory",
    "keeps the SHA-pin set minimal and drops the OIDC write permission (least privilege)",
    "should run npm run lint",
    "should run the release smoke test-gate (staged vitest)",
    "should run the canonical build:all chain including native package production",
    "should validate then prove the packed Linux package networklessly from a fresh private cache",
    "should exist with id changelog",
    "should extract version from GITHUB_REF_NAME",
    "should output notes_file to GITHUB_OUTPUT",
    "should read the ROOT CHANGELOG.md, not docs/CHANGELOG.md",
    "should exact-anchor the version heading (kills the old prefix-trap regex)",
    "should escape regex metacharacters in the version string",
    "should FAIL (not silently fall back) on zero matches, duplicate headings, or an empty section",
    "should NOT contain the old silent-empty placeholder fallback",
    "does NOT run an automatic npm publish",
    "does NOT create a GitHub Release",
    "documents that publishing is owner-manual",
    "carries zero npm-auth-secret references",
    "should use actions/upload-artifact pinned to an immutable commit SHA (SEC-06)",
    "should upload dist directory",
    "should set retention to 30 days",
    "should name artifact with ref_name",
    "should have at least 11 distinct steps (auto-publish + auto-release steps removed in the 0.100.0 rebaseline)",
    "should execute steps in logical order: checkout → setup → verify-integrity → verify-ci → install → lint → build → test-gate → changelog (publish/release are owner-manual, not steps)",
    "should NOT request id-token/contents:write — least privilege, no OIDC publish (0.100.0 rebaseline)"
   ]
  },
  "governance": {
   "governance/agent-discovery-census.test.ts": [
    "walks every source file and classifies definitions and raw agent-layer reads",
    "fails when a deliberately added twelfth raw scan is absent from the pin",
    "fails when a known discovery site disappears from the pin",
    "exactly reproduces the design §1 known set in both drift directions"
   ],
   "governance/approval-identity-parity.test.ts": [
    "accepts/rejects identically across the corpus",
    "rejects the exact axes the mandate named",
    "accepts a canonical id on both"
   ],
   "governance/closure-genesis-anchor.test.ts": [
    "--self-check: the adversarial conformance/forgery suite passes",
    "--generate: writes the private key OUTSIDE the repo and emits a conformant public anchor + fingerprint",
    "GUARD: existing --private-out is refused byte-identical (fail-closed, no overwrite)",
    "GUARD: existing --anchors-out is refused and NO private key is created",
    "GUARD: existing --fingerprint-out is refused and NO private key is created",
    "GUARD: a repo-local --private-out is refused (no key written)",
    "--adopt-public-key: emits anchor + fingerprint with ZERO private artifact",
    "--adopt-public-key: rejects a non-ed25519 (P-256) key with a typed reasonCode",
    "--adopt-public-key: rejects a malformed public key with a typed reasonCode",
    "BLOCKER A: --adopt-public-key rejects an ed25519 PRIVATE PKCS8 PEM (no ingestion, no output)",
    "BLOCKER B: --verify an ed25519 PRIVATE-PEM anchor → TRUST_ANCHOR_PRIVATE_KEY_FORBIDDEN, no manifest",
    "BLOCKER B: --verify a P-256 anchor → TRUST_ANCHOR_BAD_KEY_TYPE, no manifest",
    "BLOCKER B: --verify an RSA anchor → TRUST_ANCHOR_BAD_KEY_TYPE, no manifest",
    "--verify: a canonical ed25519 SPKI PUBLIC anchor passes and emits a fingerprint",
    "--verify: strict-validates a generated anchor and recomputes the identical fingerprint",
    "--verify: rejects a non-conformant anchor with a typed TRUST_ANCHOR_* code"
   ],
   "governance/closure-ledger.test.ts": [
    "passes every in-process assertion (clean exit, no stderr)",
    "passes every projection assertion (clean exit, no stderr)",
    "--dry-run emits a deterministic unsigned-manifest digest (writes nothing)",
    "--check reports the landed live projection drift and writes nothing",
    "an empty/absent ledger validates OK (exit 0, no stderr)",
    "a malformed ledger line is a typed HOLD (exit 1, LEDGER_PARSE) — fixture in tmpdir",
    "scan --check reports the current committed owner-proposal.md drift (writes nothing)",
    "LEVELS/LANES/PRIORITIES/ADMISSION_DISPOSITIONS/DECISION_KINDS deep-equal the schema arrays",
    "ROWREF_FIELDS is the FOUR-part rowRef SSOT and exactly equals schema.rowRef.requiredFields",
    "HOLD_LANE equals schema.lanes.holdState"
   ],
   "governance/orphan-deliverables.test.ts": [
    "flags // and * and /** lines",
    "does not flag real code lines",
    "catches static from-imports and export-from",
    "catches dynamic import() with literal specifiers",
    "ignores bare (non-relative) package specifiers",
    "skips specifiers that appear only in comment lines",
    "catches import type specifiers (type-only imports still count as usage)",
    "maps .js specifier to a sibling .ts file",
    "falls back to .tsx when .ts does not exist",
    "maps .jsx specifier to .tsx",
    "returns null for non-TS specifiers (.json, .css)",
    "returns null when no candidate file exists",
    "resolves an extensionless directory import to index.ts",
    "flags files with zero src/scripts importers, excluding dashboard candidates",
    "is deterministic — repeated runs on the same tree produce the same result",
    "a scripts/**-only importer is sufficient to NOT be flagged as orphan",
    "a tests/**-only importer is NOT sufficient — still flagged as orphan",
    "excludes src/dashboard/** from candidates entirely",
    "has the expected count and only well-formed src/**/*.ts(x) entries",
    "has no duplicate entries",
    "the live scan matches KNOWN_ORPHANS exactly (new orphan or a closed gap both fail loudly)"
   ],
   "governance/phase5-dry-run.test.ts": [
    "is digest-deterministic for the same unsigned fixture",
    "writes exactly the four staging artifacts and leaves inputs untouched",
    "refuses an outDir under docs/governance",
    "fails with a typed SCHEMA error when a required unsigned event field is missing",
    "emits the complete §3.4 subject and unsigned signed-binding preview",
    "accepts key rotation within one scope and rejects cross-scope anchors"
   ],
   "governance/phase5-sign.test.ts": [
    "emits a validator-verifiable receipt with the strict ordered decision window",
    "refuses an in-repository key path before reading key bytes",
    "refuses PUBLIC-key input and non-ed25519 private keys with typed codes"
   ],
   "governance/phase5-writer.test.ts": [
    "files one canonical broker-readable pending request and reuses claim.json",
    "fails closed on unknown flags",
    "refuses missing and malformed receipt before a ledger exists",
    "refuses filename/requestId mismatch",
    "archives exact bytes, appends a valid chain, writes projections, and finishes gate-green"
   ],
   "governance/release-workflow-unify.test.ts": [
    "parses a flat mapping",
    "parses nested mappings",
    "parses a flow array",
    "parses a block sequence of scalars",
    "parses a block sequence of mapping items (steps shape)",
    "parses a block scalar (|) preserving internal blank lines and comments",
    "does not treat a colon inside a URL as a mapping separator",
    "preserves an unquoted scalar containing embedded single quotes (an `if:` condition)",
    "skips full-line and blank lines between mapping keys",
    "OLD publish.yml + OLD release.yml together count 2 (the bug)",
    "FIXED publish.yml (dry-run only) + release.yml together count 1 (the fix)",
    "a workflow with only a --dry-run publish step is not counted as a publish authority",
    "NO release-event-triggered workflow performs a real npm publish (manual-publish contract)",
    "release.yml performs no real publish (validation-only)",
    "publish.yml no longer performs a real npm publish (dry-run only)",
    "has a build step that runs build:all (not plain build)",
    "does not run plain `npm run build` (only build:all)",
    "has a validate:publish step",
    "orders build:all → validate:publish, and carries NO publish step after them",
    "carries no registry auth material (validation needs no token — SEC-06 posture keeps)",
    "smoke test-gate is not a duplicate of the full staged multi-directory matrix",
    "permissions are read-only (0.100.0 rebaseline: no GH Release, no provenance token)",
    "structural parse-lint fallback: all 3 touched workflows parse without throwing and have name/on/jobs"
   ],
   "governance/workflow-yaml-pins.test.ts": [
    "parses a job env block with underscored keys and quoted numeric strings",
    "parses steps with a run block scalar containing a retry loop",
    "parses a with: block containing a nested path block scalar and if-no-files-found",
    "recognizes a full-SHA-pinned action",
    "rejects a tag-pinned action as not full-SHA-pinned",
    "rejects a short/partial-hex ref as not full-SHA (must be exactly 40 hex chars)",
    "GREEN: only known base names → no third-party action beyond allowlist",
    "RED: a new third-party action base name is flagged",
    "GREEN: bounded 2-attempt loop with sleep only reachable between attempts",
    "RED: unbounded loop (while true, no max_attempts) is flagged",
    "RED: sleep executes unconditionally BEFORE the exhausted-attempts exit guard",
    "RED: more than 2 bounded attempts is out of contract (max_attempts=5)",
    "RED: a duplicate/extra sleep beyond (max_attempts - 1) is flagged by count",
    "no retry loop present → found: false (distinguishes absence from violation)",
    "GREEN: failure()-only, SHA-pinned, covers npm logs, tolerates missing optional logs",
    "RED: an always()-conditioned sibling upload makes it not failure-only",
    "RED: an unconditional (no if:) sibling upload makes it not failure-only",
    "RED: tag-pinned upload-artifact is not full-SHA-pinned",
    "RED: a path that never mentions npm logs fails npm-log coverage",
    "no failure()-gated upload-artifact step present → found: false",
    "GREEN: a workflow with no continue-on-error yields zero locations",
    "RED: job-level continue-on-error is captured as a location",
    "RED: step-level continue-on-error is captured with job + step name",
    "GREEN: sane bounded retry/timeout config",
    "RED: missing retries entirely is not a bounded config",
    "RED: retries above the sane ceiling (unbounded-in-spirit) is rejected",
    "RED: mintimeout >= maxtimeout is an incoherent backoff window",
    "packed-install job exists with the active Linux proof name",
    "setup-node cache authority binds root shrinkwrap plus the independent dashboard lock",
    "delegates pack/cache/install/native proof to one exact networkless wrapper invocation",
    "uploads the wrapper receipt fail-closed and reuses the existing action SHA pin",
    "keeps diagnostics failure-only even though the proof receipt uploads on success",
    "required-status semantics are not relaxed: zero continue-on-error in this workflow",
    "no third-party action beyond the current known allowlist exists anywhere in the workflow set",
    "continue-on-error usage repo-wide matches exactly the known pre-existing allowlist (no new relaxation)",
    "every currently full-SHA-pinned action reference stays full-SHA-pinned (regression pin)",
    "every workflow file parses without throwing and has name/on/jobs"
   ]
  },
  "helpers": {
   "helpers/paths.test.ts": [
    "converts backslashes to forward slashes",
    "passes through forward slashes unchanged",
    "handles mixed slashes",
    "handles empty string",
    "handles absolute Windows-style paths",
    "handles paths with no separators",
    "handles trailing backslash",
    "converts backslashes to forward slashes",
    "passes through forward slashes unchanged",
    "handles empty string",
    "produces same result as normalizePath for backslash paths",
    "passes when paths are identical forward-slash paths",
    "passes when actual has backslashes and expected has forward slashes",
    "passes when both have backslashes",
    "throws when paths differ after normalization",
    "throws when filenames differ",
    "joins segments with forward slashes",
    "normalizes backslashes in segments",
    "collapses repeated slashes",
    "works with a single segment"
   ],
   "helpers/platform.test.ts": [
    "isWindows reflects process.platform",
    "isUnixOnly is the opposite of isWindows",
    "isWSL is a boolean",
    "isWSL is false on Windows native",
    "isWSL is false on macOS",
    "exactly one of isWindows/isUnixOnly is true",
    "skipOnWindows does not throw",
    "skipOnWindows calls fn on non-Windows platforms",
    "skipOnWindows does NOT call fn on Windows",
    "itUnix returns a callable test function",
    "creates a directory that exists",
    "returns an absolute path",
    "prefix appears in the directory name",
    "each call creates a unique directory",
    "handles special characters in prefix safely"
   ],
   "helpers/sandbox-home.test.ts": [
    "sets HOME to a directory inside tmpdir",
    "restores original HOME after fn completes",
    "cleans up the temp dir after fn completes",
    "restores HOME even when fn throws",
    "nested calls each get their own sandbox and are independent",
    "does not write anything to the project root",
    "sets HOME to a tmpdir-based path in beforeEach",
    "getSandboxDir returns the current sandbox directory",
    "sandbox dir is writable (can create a file inside)"
   ]
  },
  "hermeticity": {
   "hermeticity/global-setup.test.ts": [
    "is deterministic when path, bytes and metadata are unchanged",
    "detects create, delete, content and mode changes",
    "distinguishes an absent tree from an empty tree",
    "restores an existing caller value exactly",
    "deletes the marker when it did not exist before activation",
    "restores the caller marker when baseline acquisition fails"
   ],
   "hermeticity/runtime-write-guard.test.ts": [
    "intercepts guarded path capabilities inside a nonce fake repository",
    "allows writes owned by a nonce temp root",
    "resolves symlink ancestors before policy decisions",
    "treats a descendant named ..escape as inside the protected root",
    "blocks an ancestor mutation that would recursively remove protected roots",
    "fails loudly when Node adds an unclassified fs function",
    "case-folds Windows drive paths and accepts descendants",
    "normalizes Windows namespaced UNC paths without widening siblings",
    "normalizes lowercase Windows UNC namespaces and alternate data streams",
    "normalizes Win32 trailing-dot and trailing-space aliases by segment",
    "preserves literal trailing-dot semantics for Windows extended namespaces",
    "conservatively case-folds Darwin paths across volume semantics",
    "conservatively case-folds POSIX paths for WSL and remote-volume semantics",
    "does not confuse a leading-dot descendant name with parent traversal",
    "classifies canonical read-only string flags as pass-through",
    "classifies write-capable string flags as writes",
    "classifies numeric read-only and directory-pin flags as pass-through",
    "classifies every write-capable numeric bit as a write",
    "fails closed on unknown flag shapes"
   ]
  },
  "integration": {
   "integration/agent-selection.test.ts": [
    "selects security-auditor for JWT vulnerability task",
    "selects test-writer for unit test task",
    "selects doc-writer for README update task",
    "returns generic when no triggers match",
    "returns generic when agent pool is empty",
    "matches case-insensitively",
    "selects agent with highest trigger score",
    "handles single-trigger match",
    "skips disabled agents",
    "falls back to next best when preferred agent is disabled",
    "builds agent prompt with task context",
    "preserves original prompt template content",
    "selects correct agents for a batch of tasks",
    "handles task with multiple matching agents - picks highest score",
    "handles agent with empty triggers array",
    "handles task with empty title and description",
    "all disabled agents returns generic"
   ],
   "integration/approval-chain.test.ts": [
    "resolves one request via channel-decide, one via store.transition, and one via TTL sweep",
    "the .strict() contract schema rejects an unknown \"rawArgs\" key outright",
    "only maskedArgs + an opaque rawArgsRef pointer ever reach disk or a channel — never a raw value",
    "decidePolicy clamps to deny even when a misconfigured rule explicitly says auto-approve",
    "decidePolicy clamps to deny on the no-rule-matched fallback too, even with a permissive defaultAction",
    "end-to-end: a critical request never lands in the approved bucket, even after a TTL sweep with a safe defaultAction",
    "recovers pending/approved/denied/expired categorization with zero shared in-memory state"
   ],
   "integration/cascade-block-live.test.ts": [
    "dummy NO_GO task triggers cascade block — DEPENDENCY_BLOCKED written to event stream",
    "after dummy task resolves — DEPENDENCY_UNBLOCKED written to event stream",
    "full cascade lifecycle: dummy inject → BLOCKED → resolve → UNBLOCKED (both events in stream)",
    "transitive cascade: A fails → B and C (both depending on A) are blocked"
   ],
   "integration/collaboration-adaptive.test.ts": [
    "creates 3 waves from 5 tasks with deps (A->B, C->D, E independent)",
    "creates 3 waves from chained tasks A->B->C",
    "wave order is deterministic (sorted)",
    "throws on circular dependencies",
    "handles empty task list",
    "handles single task with no deps",
    "ignores unknown dependencies",
    "getExecutionPlan returns readable string",
    "detects conflict when two tasks modify same file",
    "returns no conflicts when tasks touch different files",
    "detects multiple conflicts",
    "resolves with last_writer_wins strategy",
    "resolves with first_writer_wins strategy",
    "resolves with manual strategy",
    "full adaptive flow: analyze -> suggest -> version -> canRollback",
    "adaptive flow with experiment",
    "metrics collection after versioning",
    "rollback triggers when version underperforms",
    "full pipeline: analyze -> version -> experiment -> metrics -> rollback",
    "adaptive agent detects multiple weakness types",
    "version pruning works when creating many versions"
   ],
   "integration/config-layers.test.ts": [
    "loads and merges global and project configs in correct order",
    "applies global config over defaults",
    "applies project config when global is missing",
    "project mode overrides global mode",
    "project language overrides global language",
    "project config overrides nested mode settings",
    "project config preserves global settings not specified in project",
    "falls back to defaults when global config does not exist",
    "falls back to defaults when project config does not exist",
    "uses all defaults when both global and project are null",
    "provides complete ResolvedConfig with activeModeConfig",
    "throws ConfigValidationError for invalid mode",
    "throws ConfigValidationError for invalid language",
    "throws ConfigValidationError for invalid brain_model in mode config",
    "throws ConfigValidationError for invalid max_workers",
    "catches validation errors with detailed error messages",
    "deep merges nested mode configs correctly",
    "preserves non-overridden fields in nested merge",
    "round-trips global config: save and load",
    "writes valid JSON with proper formatting",
    "creates parent directories for new config path",
    "handles empty global and project configs",
    "validates after merge (catches invalid merge result)",
    "handles config with undefined fields (skipped in merge)"
   ],
   "integration/cursor-production-wiring.integration.test.ts": [
    "carries Cursor from the image handler through canonical catalog and isolated Docker auth"
   ],
   "integration/e2e-init.test.ts": [
    "loads config from initialized project",
    "creates task files from DIRECTIVES.md via planSprint",
    "initializes dashboard after planSprint",
    "handles multiple projects independently",
    "preserves config across init and plan steps",
    "creates proper brain directory structure during init"
   ],
   "integration/e2e-sprint.test.ts": [
    "creates tasks with correct properties",
    "spawns workers with correct task ids",
    "initializes sprint with correct phase and status",
    "writes worker result file correctly",
    "handles multiple worker results in sequence",
    "evaluates result with correct assessment",
    "handles DONE evaluation correctly",
    "handles GO_WITH_TECH_DEBT evaluation",
    "handles NO_GO evaluation and triggers cross-dependencies",
    "writes retrospective file with correct format",
    "updates MEMORY.md with sprint learnings",
    "writes sprint log file",
    "calculates metrics correctly",
    "cleans up task files after sprint completion",
    "handles decay on memory accumulation",
    "completes full cycle: tasks → spawn → execute → evaluate → retro",
    "validates memory and retro files after sprint",
    "preserves task dependencies through lifecycle",
    "maintains sprint metrics across full cycle"
   ],
   "integration/event-truth-wave-evidence.test.ts": [
    "never lets competing heartbeat writes regress the durable file",
    "strictly advances competing read-model publications without losing either revision",
    "projects a proven failed terminal flow as non-resumable ABORTED with surface parity",
    "renders object-form dependency files in the prompt without a Pending marker"
   ],
   "integration/full-sprint-cycle.test.ts": [
    "completes full cycle: plan → evaluate → retro → decay → cleanup",
    "NO_GO creates fix task",
    "GO_WITH_TECH_DEBT inserts debt entry in DB",
    "debt escalation calls upsert with incremented sprintsOpen",
    "resolveDebt marks debt as resolved in DB",
    "calculateMetrics with debt counts resolved and open",
    "force decay calls store.decay via DB",
    "force decay returns result with linesBefore and linesAfter",
    "force decay always runs even when under budget",
    "no-op when force=false and under budget",
    "handleCrossDependencies creates fix for dependency of NO_GO task"
   ],
   "integration/lifecycle.test.ts": [
    "loads default config when no project config exists",
    "loads and merges project config",
    "deep merges mode overrides",
    "throws ConfigValidationError for invalid mode",
    "validatePartialConfig accepts valid partial",
    "readTask reads real JSON file",
    "readTask throws for missing task",
    "claimTask transitions PENDING → CLAIMED with real file",
    "writeTaskPlan creates .plan file on disk",
    "writeHeartbeat creates .hb file on disk",
    "writeResult creates .result and updates task status",
    "updateTaskStatus persists change through file",
    "acquireLock creates .lock file with correct content",
    "checkLock returns LockInfo for locked file",
    "checkLock returns null for unlocked file",
    "releaseLock deletes lock file from disk",
    "acquireLock throws LockError for different worker",
    "acquireLock is idempotent for same worker",
    "releaseAllLocks removes only owned locks",
    "scanHeartbeats reads real .hb files",
    "scanHeartbeats detects stale heartbeat",
    "checkStaleLocks detects stale lock",
    "buildWorkerScopeMap reads tasks with assignedWorker",
    "updateDashboard writes .dashboard file",
    "evaluateResultSync returns DONE for passing result",
    "evaluateResultSync overrides DONE→NO_GO when testsPassed=false",
    "handleEvaluation DONE updates task and releases locks",
    "handleEvaluation GO_WITH_TECH_DEBT inserts debt entry in DB",
    "handleEvaluation NO_GO creates fix task file",
    "escalateDebt calls upsert with incremented priority via DB",
    "readContext reads directives from FS and brain context from DB",
    "planSprint creates task files on disk",
    "planSprint auto-increments sprint number",
    "writeSprintLog creates sprint-NNN.md",
    "cleanup removes task artifacts",
    "doctor passes when node+git return valid versions",
    "doctor fails for old Node version",
    "doctor handles missing tools",
    "init creates full directory scaffold on real filesystem",
    "init writes valid config with selected mode",
    "init does not overwrite existing files",
    "init appends to .gitignore without duplicates",
    "detects a fix worker that left a container_start_failed marker with no .result",
    "returns null when the fix worker later wrote a .result (spawn ultimately recovered)",
    "ignores a FIRST-attempt (non-fix) task marker — those are handled by EVALUATE/FIX",
    "ignores a fix marker that is a plain WORKER_TIMEOUT (not a spawn/preflight failure)",
    "returns null when there are no timeout markers at all"
   ],
   "integration/mcp-flow.test.ts": [
    "init tool creates .deckent/config.json with project metadata",
    "init tool creates all required directories",
    "set_directives tool writes DIRECTIVES.md and returns task breakdown",
    "doctor tool returns response with _enriched metadata",
    "doctor tool with includeProfile includes system profile",
    "complete MCP flow chain: init → set_directives → doctor",
    "set_directives correctly computes estimated model distribution"
   ],
   "integration/mcp-sprint-isolation.test.ts": [
    "should generate correct IPC directory path",
    "should write and read runner config via IPC files",
    "should write and read IPC status",
    "should write and read IPC result on success",
    "should write and read IPC error on failure",
    "should return null when IPC files do not exist",
    "should overwrite previous status on update",
    "should isolate IPC for different job IDs",
    "should export SprintRunnerConfig type and IPC helpers",
    "should spawn runner as detached process with stdio ignore",
    "should preserve all config fields through JSON serialization"
   ],
   "integration/memory-nervous.test.ts": [
    "Test 1: ExecutionRecord → memory entry insert",
    "Test 2: FTS5 search directives-protection → returns Sprint 148 record",
    "Test 3: Sprint context tagged (sprint_id=sprint-148)",
    "Test 4: Retention — decay_exempt=false (respects decay)",
    "Test 5: Multiple records indexed and searchable"
   ],
   "integration/memory-v2-prod-readiness.test.ts": [
    "initializes DB and inserts all 7 entry types",
    "manages tag many-to-many associations correctly",
    "handles relations between entries",
    "performs FTS5 dual-layer search across TR, EN, and DE text",
    "decays old non-exempt entries, preserves exempt and recent",
    "roundtrips through export and reimport without data loss",
    "tracks field-level history across multiple upserts"
   ],
   "integration/memory-v2-stress.test.ts": [
    "inserts ${ENTRY_COUNT} entries with mixed types",
    "validates ${TAG_COUNT} tag associations across entries",
    "inserts ${RELATION_COUNT} relations and verifies count",
    "completes ${QUERY_COUNT} FTS5 queries each under 100ms",
    "handles ${CONCURRENT_READERS} concurrent reads without errors",
    "keeps DB file under 5MB with 1000 entries",
    "exports full DB to markdown under 500ms",
    "decays ~500 entries with measured performance"
   ],
   "integration/memory-v2.test.ts": [
    "roundtrips entries through all modules",
    "case-insensitive Turkish search with ASCII fallback",
    "deletes old non-exempt, preserves exempt and recent",
    "tracks field-level changes with changedBy"
   ],
   "integration/multi-agent-pipeline.test.ts": [
    "executes all steps sequentially",
    "calls executor with correct step and context",
    "passes accumulated results to subsequent steps",
    "aborts pipeline on step failure",
    "aborts pipeline on executor throw",
    "continues pipeline when continueOnError is true",
    "continues pipeline on throw when continueOnError is true",
    "handles empty pipeline",
    "handles single step pipeline",
    "preserves taskId and title throughout pipeline",
    "records all step outputs in correct order",
    "aborts at second step, preserving first step result",
    "abortReason includes agent name and action"
   ],
   "integration/multi-env.test.ts": [
    "detects codex env when CODEX_SESSION is set",
    "generates AGENTS.md content for codex environment",
    "detects gemini env when GEMINI_CLI is set",
    "generates GEMINI.md content for gemini environment",
    "detects cursor env when CURSOR_SESSION is set",
    "detects cursor env when TERM_PROGRAM is cursor",
    "creates .codex/config.toml with deckent MCP section",
    "creates settings.json with mcpServers.deckent entry",
    "creates mcp.json and rules file in .cursor directory",
    "detects Python stack from requirements.txt",
    "detects Flask framework from requirements.txt",
    "detects Go stack from go.mod",
    "detects go_test framework when _test.go files exist",
    "acquires, checks, and releases a sprint lock",
    "prevents double acquisition by same live process via re-acquire",
    "clears lock with dead PID automatically on isSprintLocked",
    "allows acquiring lock after stale lock is cleared",
    "returns shell when no IDE env vars are set",
    "cursor takes priority over codex when both env vars set",
    "detects Rust stack from Cargo.toml",
    "generateAgentsMd includes all required sections",
    "generateGeminiMd includes context reference"
   ],
   "integration/npm-install-sim.test.ts": [
    "dist/cli/index.js path is correctly configured in package.json",
    "package.json has correct bin entry for deckent",
    "shebang line would be present in dist output",
    "deckent init creates required directories",
    "deckent init creates config.json",
    "deckent init creates DECKENT.md",
    "deckent init creates brain memory files",
    "deckent doctor checks are available as exported function",
    "package.json engines requires Node >= 18",
    "package.json type is module (ESM)",
    "package.json exports field is configured",
    "file structure matches expected layout after init"
   ],
   "integration/plan-sprint.test.ts": [
    "creates task files from structured DIRECTIVES.md",
    "produces PENDING status tasks by default",
    "produces DRAFT status tasks when asDraft=true",
    "handles empty directives gracefully",
    "uses modelConstraint from recommendation when provided",
    "assigns sequential task IDs within sprint",
    "includes workers array matching task IDs",
    "sets planningMode to structured when mode=structured",
    "writes live PLAN progress to the canonical allocated sprint only",
    "dry-run leaves no raw journal/sequence and does not advance the allocator"
   ],
   "integration/plugin-lifecycle.test.ts": [
    "create — scaffolds plugin with valid manifest.json",
    "list — created plugin appears in enabled plugins list",
    "disable — plugin disappears from listPlugins after being disabled",
    "enable — disabled plugin reappears in list after re-enabled",
    "remove — plugin directory is deleted and plugin no longer appears",
    "full lifecycle — create, disable, enable, then remove",
    "sequence — multiple creates, disables, enables preserve state correctly",
    "create — generates SKILL.md and README.md with correct content",
    "loadPlugin — successfully loads plugin after disable/enable operations",
    "enable/disable — returns false when plugin does not exist"
   ],
   "integration/project-types/monorepo.test.ts": [
    "detects TypeScript from root config",
    "detects turbo as build tool",
    "detects React framework",
    "includes turbo in dependencies",
    "includes vitest in dependencies"
   ],
   "integration/project-types/python-fastapi.test.ts": [
    "detects Python language",
    "detects FastAPI framework",
    "detects pytest as test framework",
    "includes fastapi in dependencies",
    "includes pydantic in dependencies"
   ],
   "integration/project-types/typescript-react.test.ts": [
    "detects TypeScript language from stack",
    "detects React framework from stack",
    "detects vitest as test framework",
    "detects vite as build tool",
    "includes react and typescript in dependencies"
   ],
   "integration/prompt-cost-canary.integration.test.ts": [
    "fans in five real lineages, separates provider USD, and applies/replays one immutable receipt",
    "HOLDs rather than fabricating missing provider billing or an unmeasured cache denominator",
    "HOLDs an incomparable cohort and rejects a quality regression through the production kernel",
    "returns bounded archive rejection for tamper and symlink evidence, and rejects a wrong digest",
    "does not overwrite a colliding immutable receipt"
   ],
   "integration/prompt-quality-regression.test.ts": [
    "Sprint 181-001 (devops-engineer CI workflow): PQ quality assertions pass",
    "Sprint 181-002 (refactorer package.json): PQ quality assertions pass"
   ],
   "integration/provider-execution-observation-reconciliation.integration.test.ts": [
    "fans in fifteen settled executions across runs without deleting or synthesizing end observations"
   ],
   "integration/provider-flow.test.ts": [
    "registers a provider and retrieves it by name",
    "first registered provider becomes default automatically",
    "listProviders returns all registered names",
    "throws ProviderNotFoundError for non-existent provider",
    "throws ProviderError when registering duplicate name",
    "throws when getDefault called with no providers",
    "supports multiple providers registered simultaneously",
    "setDefault changes the default provider",
    "buildCommand generates correct command structure",
    "buildCommand includes allowedTools when specified",
    "isAvailable returns true for available provider",
    "isAvailable returns false when CLI missing",
    "factory creates SubprocessBackend when backend=subprocess",
    "factory creates TmuxBackend when backend=tmux",
    "SubprocessBackend.isAvailable always returns true",
    "createAsync throws SpawnBackendError when backend not available",
    "full flow: register provider → get backend → spawn worker",
    "auto mode resolves to docker when tmux unavailable (modernized)",
    "auto mode resolves to docker even when tmux is available (modernized)",
    "unregisterProvider removes a provider and resets default",
    "clear removes all providers",
    "hasProvider returns correct boolean"
   ],
   "integration/provider-observation-runtime-adoption.integration.test.ts": [
    "dry-runs, applies, and replays one canonical provider-linked composite receipt without mutating either database",
    "returns typed HOLDs for a wrong digest, symlink preimage, and concurrent target evidence",
    "fails closed for legacy, dead, and reused ownership records"
   ],
   "integration/security-flow.test.ts": [
    "should generate a valid API token",
    "should validate correct bearer token",
    "should reject missing authorization header",
    "should reject invalid bearer token",
    "should allow requests when no token is configured (backward compatibility)",
    "should build scope map correctly from tasks",
    "should detect files modified outside assigned scope",
    "should acquire lock exclusively",
    "should prevent lock acquisition by different worker",
    "should allow same worker to re-acquire own lock (idempotent)",
    "should release lock successfully",
    "should prevent lock release by wrong worker",
    "should handle concurrent lock acquisition attempts",
    "should detect stale locks correctly",
    "should prevent unauthorized task claim without auth token",
    "should enforce lock and scope together"
   ],
   "integration/skill-selection.test.ts": [
    "detects TypeScript + React stack from project files",
    "detects JavaScript when no tsconfig.json",
    "selects typescript-expert and react-specialist for React component task",
    "selects security-specialist for JWT auth task",
    "selects testing-expert for test coverage task",
    "returns empty selection when no skills match and all disabled",
    "returns empty when all skills disabled",
    "respects maxSkills limit",
    "truncated is false when within limit",
    "scores higher for stack dependency matches",
    "scores higher for proven skills with good stats",
    "builds prompt with both SKILL.md contents",
    "skips skills without content in prompt",
    "separates skill sections with dividers",
    "updates stats after successful evaluation",
    "updates stats after failed evaluation",
    "calculates rolling average coverage",
    "full E2E: detect stack -> select skills -> build prompt -> update stats",
    "handles empty skill pool gracefully"
   ],
   "integration/sprint-044-modules.test.ts": [
    "detectEnvironment() returns a valid DetectedEnv value",
    "createDeckTemplate() creates .deck file with all 9 known keys",
    "showSplash() renders the Unicode Kraken in an explicit UTF-8 terminal",
    "showSplash() renders the printable Kraken when ASCII is explicitly forced",
    "creates Connector, registers mock Claude adapter, and retrieves it",
    "routeTask() with code task returns provider=claude when claude is available",
    "routeTask() with doc task + skill_routing.docs=gemini returns provider=gemini",
    "formatSyncOutput produces readable output for mock changes",
    "buildExplainOutput produces sprint summary with task counts",
    "formatRichSprintSummary() with mock data has all 7 sections",
    "NO_COLOR suppresses ANSI codes in output",
    "createDefaultConfig() has ALL new fields",
    "config with custom values survives JSON roundtrip",
    "autoResolveDebt with no fix tasks returns 0",
    "detects vscode when VSCODE_PID is set",
    "detects tmux when TMUX is set",
    "falls back to shell when all env vars cleared"
   ],
   "integration/stack-detection.test.ts": [
    "detects TypeScript + React + Vitest stack",
    "detects JavaScript + Express + Jest stack",
    "detects Next.js framework (prefers next over react)",
    "detects Vue framework",
    "detects Python project",
    "detects Rust project",
    "returns unknown for empty project",
    "handles malformed package.json gracefully",
    "writes cache after detection",
    "reads from cache on second call",
    "refreshStack with force=true re-detects even with valid cache",
    "isStackStale returns true when no cache exists",
    "isStackStale returns true when package.json is newer than cache",
    "isStackStale returns false when cache is newer than package.json",
    "isStackStale returns true when tsconfig.json is newer than cache",
    "isStackStale returns true when package.json stat is missing",
    "extracts all dependencies including devDependencies",
    "handles package.json with only dependencies",
    "handles package.json with empty dependencies"
   ],
   "integration/task-attempt-custody-cutover.integration.test.ts": [
    "consumes the same accepted-attempt identity without reading or rewriting public result bytes"
   ],
   "integration/xverify-claude-model-limit-window.integration.test.ts": [
    "dispatches exact Opus 5 through the real factory when only Fable is exhausted",
    "holds exact Opus before reachability and dispatch when the shared week-all window is exhausted",
    "holds exact Fable before reachability and dispatch when its model-only window is exhausted"
   ],
   "integration/xverify-owner-tier-authority.integration.test.ts": [
    "fresh-loads Sol→Opus authority and closes mandatory usage, settlement and verdict receipt",
    "revokes the grant on called-model drift after mandatory execution",
    "keeps %s fail-closed without fallback",
    "rejects %s durable verdict receipt authority",
    "rejects corrupt, unknown-field and same-provider authority during fresh config loading"
   ],
   "integration/zero-config-flow.test.ts": [
    "creates valid DIRECTIVES format with header",
    "includes the description as Task 1 title",
    "includes description in the Description section",
    "includes Tests section",
    "handles complex description with special characters",
    "writes temporary DIRECTIVES.md when none exists",
    "created file has correct content",
    "warns (returns alreadyExisted=true) if DIRECTIVES.md already exists",
    "directivesPath points to correct file",
    "removes temporary DIRECTIVES.md after sprint",
    "does nothing if createdTemp is false",
    "does not throw if file already deleted",
    "returns null when DIRECTIVES.md does not exist",
    "returns content when DIRECTIVES.md exists",
    "returns \"auto\" when all evaluations are NO_GO",
    "returns \"ask\" when some evaluations are NO_GO",
    "returns \"never\" when all evaluations are DONE",
    "returns \"never\" for empty evaluations",
    "returns \"never\" when all are GO_WITH_TECH_DEBT (no NO_GO)",
    "returns \"ask\" for mixed DONE and NO_GO",
    "returns \"auto\" for single NO_GO evaluation",
    "returns false for non-git directory",
    "returns empty string for non-git directory",
    "returns \"HEAD\" for non-git directory",
    "returns empty array for non-git directory",
    "returns false for non-git directory",
    "prepare → safety check → rollback policy → cleanup",
    "zero-config with simple description works end-to-end",
    "zero-config with complex description works end-to-end",
    "error handling: cleanup works even after partial failure"
   ]
  },
  "intelligence": {
   "intelligence/alert-formatter.test.ts": [
    "includes every required section, typed classes, and exact baseline evidence refs",
    "returns identical text for identical input, including the injected time",
    "returns a typed error when the relevant baseline entry is missing",
    "names an honest typed gap value when no dimension is affected"
   ],
   "intelligence/baseline-catalog.test.ts": [
    "keeps the status vocabulary closed to exactly seven values",
    "requires evidence at the type boundary",
    "contains every canonical domain once with unique capability ids",
    "declares non-empty exact repo evidence paths that exist",
    "never authors a content digest, so an entry cannot claim stale content",
    "classifies every entry with an honest, evidence-bound status",
    "does not collapse every field into one uniform status"
   ],
   "intelligence/baseline.test.ts": [
    "derives entry and baseline digests from evidence contents deterministically",
    "invalidates both digests when referenced evidence changes",
    "leaves both digests untouched when unreferenced content changes",
    "turns an entry into a typed HOLD and records unreadable evidence"
   ],
   "intelligence/comparison.test.ts": [
    "keeps the five relative classes closed and reports all eight dimensions separately",
    "classifies maturity and non-rankable cases deterministically",
    "separates distribution, enterprise economics, and protocol implications",
    "suppresses DAG catch-up and unchanged positions but admits real movement",
    "preserves unknown entrants and unmapped terms as typed seams",
    "builds an English evidence-bound prompt without score-like numbers"
   ],
   "intelligence/event-history.test.ts": [
    "collapses mirror and rewrite copies to one custom MemoryStore entry",
    "uses a new fingerprint for material classification or capability evolution",
    "returns typed errors for missing fields, invalid dates, and confidence"
   ],
   "intelligence/source-retrieval.test.ts": [
    "defines the closed source priority vocabulary and orders retrieval as data",
    "parses GitHub releases and carries conditional state without retaining raw bodies",
    "returns unchanged on 304 and preserves validators",
    "parses JSON Feed and Atom metadata",
    "extracts only safe HTML metadata and never retains HTML body content",
    "turns a malformed feed into typed hold without failing successful siblings",
    "does not exceed the named retry limit for transient responses",
    "applies the named timeout to every bounded attempt"
   ],
   "intelligence/watch-capability.test.ts": [
    "rejects name-only admission with a typed outcome",
    "routes dry and real runs through one network-declaring audited handler",
    "returns typed rejected and hold outcomes with bounded receipts"
   ],
   "intelligence/watch-closure-integration.test.ts": [
    "uses the CLI, capability, watch service, and dry-run flow path with injected dependencies"
   ],
   "intelligence/watch-flow.test.ts": [
    "registers the canonical Istanbul schedule and routes through the capability",
    "replays a crash after mission ingest to identical ids without duplicate effects",
    "catches up exactly once per missed slot across a simulated restart",
    "dry-run writes no mission, watch event, notification, or flow cursor"
   ],
   "intelligence/watch-service.test.ts": [
    "runs the complete chain and emits one alarm across repeated runs",
    "returns a preview without mutating history, outbox, or source cursor",
    "replays a crash after enqueue with the same stable id and no duplicate notification",
    "keeps successful siblings when one source fails"
   ]
  },
  "kpi": {
   "kpi/collection.test.ts": [
    "produces exactly 11 measurements",
    "sprint_count is always 1",
    "tasks_total/done/no_go match metrics",
    "boundary_violations defaults to 0 when not in metrics",
    "retries = Σ(tscAttempts + testAttempts) = 2",
    "lines_added = Σ linesAdded = 2000",
    "cost_usd from usage = 7",
    "tokens_input, tokens_output, cache_read from usage",
    "each measurement carries sprintId, tenantId, and ts",
    "kind and unit are populated from the catalog",
    "does NOT crash when usage is null → cost/token measures default to 0",
    "does NOT crash when usage is undefined → cost/token measures default to 0",
    "still produces all 11 measurements with null usage",
    "handles tasks with no tscAttempts/testAttempts/linesAdded gracefully",
    "empty results array → retries=0, lines_added=0",
    "goCriteria: cost_per_sprint = 7 after pipeline (cost_usd=7, sprint_count=1)",
    "stores all 11 measurements in the DB (sprint measurements readable)",
    "preserves the legacy raw append contract when called twice",
    "does NOT crash when usage is null (nogo guard)",
    "defaults ts to now when omitted",
    "inserts the complete vector once and replays different capture metadata without appending",
    "accepts a matching legacy vector with caller ids and timestamps",
    "holds divergent, partial, and duplicate existing history without changing any rows",
    "preserves the original typed KPI HOLD reason from the transactional store",
    "wraps DB-open failure as a typed storage HOLD without creating a snapshot",
    "wraps invalid database storage and preserves its bytes without appending",
    "isolates identical sprint ids by explicit tenant and rejects non-finite derived values",
    "holds missing authoritative terminal usage without minting a zero-valued snapshot",
    "rejects incomplete, non-finite, negative, and unknown-billing usage before opening the DB"
   ],
   "kpi/finalizer-hook.test.ts": [
    "sums input/output/cacheRead and computes Opus-tier cost (> 0)",
    "treats missing cacheReadTokens as 0 (optional field)",
    "results WITHOUT tokenUsage contribute 0 (mixed fleet)",
    "tokenUsage-yok → zeros (no usage telemetry → all-zero totals, cost 0)",
    "empty results → zeros, never throws",
    "records 11 measurements; cost_per_sprint derives from buildUsageTotals cost",
    "lands rows even when no result carries tokenUsage (cost = 0)",
    "recordKpiMeasurements DOES throw on an unopenable db path (failure mode is real)",
    "the finalize guard pattern swallows the throw and execution continues"
   ],
   "kpi/formula-evaluator.test.ts": [
    "evaluates the four operators",
    "parses decimal literals",
    "ignores arbitrary whitespace",
    "multiplies before adding",
    "honours parentheses over default precedence",
    "left-associates subtraction and division",
    "resolves a simple a/b ratio",
    "resolves a parenthesised ratio",
    "resolves the nested cost/(lines/1000) KLoC formula",
    "treats 0-valued measures as legitimate zeros",
    "negates literals and identifiers",
    "applies inside terms and parentheses",
    "supports doubled unary minus",
    "returns null on direct division by zero",
    "returns null when a denominator expression evaluates to zero",
    "propagates null through every surrounding operator",
    "does NOT treat a zero numerator as no-data",
    "throws FormulaError for an identifier absent from the map",
    "throws when a whitelisted measure carries a non-finite value",
    "rejects inherited member '${name}' as unknown identifier",
    "does not leak a function value for constructor",
    "rejects function-call syntax",
    "rejects member access",
    "rejects process.exit(1) and other host-escape attempts",
    "rejects indexing",
    "rejects unsupported operators and characters",
    "rejects an empty or whitespace-only formula",
    "rejects trailing tokens",
    "rejects dangling operators",
    "rejects unbalanced parentheses",
    "is an Error with a stable code and name",
    "thrown errors are catchable as FormulaError",
    "contains no eval / Function / new Function call sites"
   ],
   "kpi/kpi-backfill.test.ts": [
    "extracts task totals from a canonical sprint summary",
    "returns null when a mandatory total is missing",
    "reconstructs measurements for a sprint that had none",
    "is IDEMPOTENT — a second pass records no duplicate rows",
    "skips a sprint record that is not parseable",
    "preserves tenant isolation — tenant-A backfill never yields tenant-B rows",
    "no-ops gracefully on a DB with no sprint history",
    "goCriteria: listSprintViews on a forward-collection-gap DB returns non-empty results with a numeric cost_per_sprint",
    "repeated reads do not double-record (idempotent self-heal)",
    "getTrend self-heals so a fresh DB yields a populated trend"
   ],
   "kpi/kpi-cli.smoke.test.ts": [
    "seed helper produces a real KPI row (recordKpiMeasurements is wired)"
   ],
   "kpi/kpi-definitions.test.ts": [
    "parses a valid definition",
    "rejects a missing required field (formula)",
    "rejects an invalid id (uppercase)",
    "rejects extra unknown fields (strict mode)",
    "parses optional threshold correctly",
    "accepts a valid definition with catalog-only formula",
    "rejects a definition with a catalog-external identifier in formula",
    "rejects a definition with invalid schema (missing title.tr)",
    "accepts div-by-zero formula (null result is valid catalog usage)",
    "accepts a formula with a complex arithmetic expression",
    "contains exactly 8 KPIs",
    "contains 5 universal and 3 dogfood KPIs",
    "includes all expected universal KPI ids",
    "includes all expected dogfood KPI ids",
    "cost_per_sprint has correct threshold {warn:3.0, critical:3.5}",
    "no_go_rate has correct threshold {warn:0.15, critical:0.3}",
    "cache_hit_rate and completion_rate have direction \"up\"",
    "all other builtins have direction \"down\"",
    "all titles are i18n objects with en and tr fields",
    "all formulas reference only catalog measure IDs",
    "all builtins have grain \"sprint\"",
    "all builtins are enabled",
    "returns 8 KPIs with no custom defs",
    "returns 8 KPIs with an empty custom defs array",
    "adds a new custom KPI (total becomes 9)",
    "custom def overrides builtin with same id",
    "throws for an invalid custom definition (schema error)",
    "throws for a custom definition with a catalog-external formula identifier",
    "applies multiple custom defs in order"
   ],
   "kpi/kpi-format.test.ts": [
    "renders $x.xx with two decimals",
    "rounds to the cent",
    "scales a 0..1 ratio to x.x%",
    "locale-groups with thousands separators (en-US, deterministic)",
    "keeps small integers ungrouped",
    "preserves fractional values",
    "renders seconds with one decimal",
    "renders an em-dash for missing data regardless of format"
   ],
   "kpi/kpi-service.test.ts": [
    "goCriteria: definition+değer+status returned; title.tr === Sprint Başına Maliyet",
    "returns a KpiView for every enabled sprint-grain definition",
    "cost_per_sprint status healthy when cost < warn threshold (2.5 < 3.0)",
    "goCriteria: no rollup results + measurements exist → canlı-hesap (live fallback)",
    "live path yields same result as rollup path (no formula-engine drift)",
    "sprint with no measurements at all → all views have result: null",
    "goCriteria: 2-sprint series [7, 5] returned in old→new order",
    "getTrend n=1 returns only the most recent sprint",
    "getTrend for an unknown kpiId returns empty array",
    "getTrend ResultRow shape: kpiId, grain, periodKey, value, status, computedAt",
    "custom KPI definition is included in listSprintViews output",
    "close() can be called without throwing"
   ],
   "kpi/kpi-store.test.ts": [
    "initSchema is idempotent (re-opening the same DB does not throw)",
    "recordMeasurements / upsertResults are no-ops on empty input",
    "persists and reads back rows ordered by ts, with tags round-tripped",
    "inserts once and accepts a semantic replay while preserving original ids and timestamps",
    "rejects mixed scope, duplicate input, and non-finite values before publication",
    "keeps tenant scopes independent for the same sprint identity",
    "serializes concurrent first writers so a matching loser replays without overwrite",
    "record+fold produces correct count/sum/min/max/last per measure",
    "is idempotent — folding twice yields identical rollup values",
    "fold on a sprint with no measurements is a no-op",
    "upsertResults then getResults returns the persisted snapshot",
    "upsert is idempotent on PK conflict — same key replaces, no duplicate row",
    "getSprintMeasurements never returns another tenant rows",
    "foldSprintRollups folds only the queried tenant; rollups are tenant-isolated",
    "getResults never returns another tenant snapshot",
    "throws on empty/blank tenant id across read and write paths"
   ],
   "kpi/kpi-trend-cli.test.ts": [
    "goCriteria: emits valid JSON { kpiId, series } for a seeded kpi",
    "series items have periodKey + value + status fields",
    "goCriteria: old→new ordering — sprint-330 before sprint-331",
    "--n limits the number of results returned (n=1 → most recent)",
    "JSON mode: empty series[] for unknown kpiId (no crash)",
    "JSON mode: empty series[] when DB has no results for this KPI",
    "JSON mode: empty series[] when DB file does not exist (no crash)",
    "table mode: renders kpi.no_data message on empty series (no crash)",
    "renders i18n headers: Value, Target, Status columns",
    "renders the sprint period key (sprint id) in the table rows",
    "renders direction arrow (↓) for lower-is-better cost_per_sprint",
    "renders currency-formatted value ($7.00) for cost_per_sprint",
    "Turkish lang uses kpi.title TR form in the trend header"
   ],
   "kpi/measure-catalog.test.ts": [
    "contains exactly 11 Faz-1 measures",
    "every measure id matches its catalog key",
    "all expected measure ids are present",
    "cost_usd has kind=gauge and unit=USD",
    "all counter measures have valid aggMethod",
    "all measures have non-empty description",
    "returns undefined for unknown id",
    "returns the correct measure for known id",
    "returns sprint_count with counter kind and sum aggMethod",
    "returns same reference as BASE_MEASURES"
   ],
   "kpi/rollup-engine.test.ts": [
    "null value → unknown (na), in both directions",
    "no threshold configured → healthy (ok), even for an extreme value",
    "value ≥ critical → critical",
    "warn ≤ value < critical → warning",
    "value < warn → healthy",
    "value ≤ critical → critical",
    "critical < value ≤ warn → warning",
    "value > warn → healthy",
    "computes cost_per_sprint = 7 with status critical (≥3.5, direction down)",
    "computes cache_hit_rate ≈ 0.75 with status healthy (no threshold)",
    "token_per_task → status unknown (na) on division-by-zero, never crashes",
    "persists results idempotently — re-running yields identical, non-duplicated rows",
    "does NOT crash on a KPI referencing a not-yet-emitted (Phase-2) measure → na",
    "skips disabled and non-sprint-grain definitions",
    "does not compute formulas outside the SSOT evaluator (value matches evaluateFormula)"
   ],
   "kpi/scorecard.test.ts": [
    "currency → $x.xx (2 decimals)",
    "percent → x.x% (ratio ×100, 1 decimal)",
    "number → locale-grouped number",
    "null / undefined / non-finite → —",
    "empty views array → \"\"",
    "all views with null result (no measurement data) → \"\"",
    "goCriteria: heading \"### KPI Scorecard\" + value + status present",
    "renders one markdown row per KPI that has a computed result",
    "lang=\"tr\" localizes column headers and KPI titles"
   ]
  },
  "load": {
   "load/load-harness.test.ts": [
    "should measure loadConfig() × 100 calls with P50/P95/P99",
    "should measure loadConfig() force reload × 20 calls",
    "should measure task claim/release × 50 with P50/P95/P99",
    "should benchmark Map.get() vs Array.find() with 1000 lookups on 200 results",
    "should benchmark buildResultsMap() construction with 500 results",
    "should measure SkillSandbox AST scan × 20 with P50/P95/P99",
    "should measure SkillSandbox scan with suspicious patterns",
    "should output all benchmark results as JSON"
   ]
  },
  "mcp": {
   "mcp/annotation-parity.test.ts": [
    "every registered tool has a catalog entry and every catalog entry is registered",
    "registering a tool that is not in the catalog fails closed",
    "the annotations clients receive are exactly the catalog annotations",
    "catalog hints are internally consistent with the declared side-effect class",
    "every tool module declares a complete boolean annotation triple",
    "no tool module claims a WIDER effect than its catalog entry (catalog may not understate)",
    "a module that calls a mutating primitive cannot expose only read-only tools",
    "a single-tool module that calls a mutating primitive declares that tool non-read-only",
    "deckent_audit is destructive: it writes the gate file and can permanently prune audit events",
    "deckent_models is not read-only: action=\"refresh\" fetches remotely and rewrites the cache",
    "deckent_checkpoint is read-only: approve/reject are typed CLI-only refusals",
    "the catalog overrides a stale module literal without touching behaviour"
   ],
   "mcp/approvals-lifecycle-view.test.ts": [
    "projects effective lifecycle, lineage, quarantine and timeout audit without any write ingress",
    "reads a real overdue confirmation without settling, quarantining or changing any byte"
   ],
   "mcp/audit-tool-actions.test.ts": [
    "defaults to action=\"gate\" when action is omitted (back-compat)",
    "explicit action=\"gate\" runs the Self-Audit Gate",
    "action=\"gate\" without sprintId returns an error (required)",
    "action=\"query\" calls queryAudit with channel/tenant filters (CLI parity)",
    "action=\"query\" defaults sprint to \"sprint-001\" and applies the limit slice",
    "action=\"compliance\" calls runComplianceReport with config-derived flags (SSOT)",
    "action=\"compliance\" defaults flags to false when config has none",
    "action=\"retention\" is a dry-run by default (apply=false) with CLI policy mapping",
    "action=\"retention\" with apply=true forwards the destructive apply flag",
    "action=\"retention\" rejects invalid keepDays/keepCount without calling the runner (CLI validation parity)",
    "rejects an unknown action with an error (zod bypass guard)",
    "declares apply as DESTRUCTIVE in the tool description and exposes the action schema"
   ],
   "mcp/autonomous-approval-lifecycle.test.ts": [
    "returns a typed localized late-decision error and writes no human allow"
   ],
   "mcp/autonomous-approval.test.ts": [
    "registers both tools",
    "both tools are mutating (non-read-only, non-destructive, non-idempotent)",
    "approves a fixture-parked trigger and writes an approved decision to disk",
    "propagates a custom reason into the recorded decision",
    "prefers triggerId over id when both are supplied",
    "refuses to approve an id absent from pending.json and persists no decision",
    "returns an error when neither id nor triggerId is supplied",
    "rejects a fixture-parked trigger and writes a rejected decision to disk",
    "propagates a custom reason into the recorded decision",
    "refuses to reject an id absent from pending.json and persists no decision",
    "returns an error when neither id nor triggerId is supplied"
   ],
   "mcp/autonomous-broad-approval-lifecycle.test.ts": [
    "rejects an expired legacy row with structured closure evidence"
   ],
   "mcp/autonomous-start-honest.test.ts": [
    "disabled config (default) → does NOT spawn, reports spawned=false honestly",
    "enabled config, no existing loop → spawns for real (spawned=true + pid recorded)",
    "already-running loop (alive pid) → refuses to spawn a duplicate",
    "stale pid record (dead process) → clears it and proceeds to a real spawn",
    "spawn() throws synchronously → honest failure, no false success",
    "still clears a stale stop marker before evaluating spawn eligibility"
   ],
   "mcp/autonomous-surface.test.ts": [
    "registers both tools",
    "deckent_autonomous_backlog is mutating, deckent_autonomous_status is read-only",
    "returns an empty list for a fresh project (no backlog file yet)",
    "lists entries from an existing backlog.json fixture",
    "adds a one-off entry and persists it to disk (round-trip)",
    "adds a recurring entry when a valid cron is given",
    "rejects a malformed cron at intake without writing the file",
    "returns error when id is missing",
    "returns error when title is missing",
    "returns error on duplicate id",
    "removes an existing entry (add then remove round-trip)",
    "returns error when id is missing",
    "returns error when entry is not found",
    "returns zeroed status for a fresh project (no state files)",
    "summarizes backlog counts by status from a fixture",
    "reports stop marker present when the file exists",
    "counts pending approvals from pending.json",
    "tolerates a corrupt backlog.json without throwing"
   ],
   "mcp/branch-coverage.test.ts": [
    "returns error when dashboard file contains invalid JSON",
    "skips rows with fewer than 9 columns",
    "handles undefined resolvedInSprintId from DB entry",
    "handles readFileSync error (catch block line 44)",
    "returns all DB entries including those with empty fields",
    "reads existing .gitignore and appends new entries",
    "handles corrupted settings.json (catch block line 109-112)",
    "skips MCP registration when deckent already in settings",
    "handles non-Error thrown values in loadConfig",
    "returns taskCount 0 when content has no task headers",
    "returns taskCount 0 for content without matching headers",
    "returns null when retro file exists but is empty"
   ],
   "mcp/catalog-parity.test.ts": [
    "registers all 3 tools",
    "registers each tool with a title and non-read-only annotations",
    "forwards deps.registryClientFactory to the skill-manage tool",
    "add creates a new custom agent on disk",
    "add defaults name to id and preferredModel to sonnet when omitted",
    "add rejects a duplicate id",
    "remove deletes an existing agent",
    "remove reports removed=false for a missing agent (not an error)",
    "promote persists a temp-pool agent into the persistent pool",
    "promote is a no-op-safe re-save when the agent is already in the persistent pool",
    "promote fails for an unknown id",
    "add creates a new skill on disk with defaults",
    "add honors explicit name/category/triggers",
    "add rejects a duplicate id",
    "add fails without an id",
    "remove deletes an existing skill",
    "marketplace-list returns registry results when the registry succeeds",
    "marketplace-list falls back to local skills when the registry is unreachable",
    "marketplace-list defaults query to empty string when omitted",
    "insert fails when .brain/memory.db does not exist",
    "insert creates a new entry",
    "refuses to accept an ADR through this surface",
    "still admits a proposed ADR",
    "refuses to promote an existing ADR to accepted through update",
    "leaves non-decision entries untouched by the gate",
    "insert fails when required fields are missing",
    "insert rejects a duplicate id",
    "update patches an existing entry",
    "update fails for an unknown id",
    "update fails without an id",
    "decay-trigger soft-deletes entries past the retention window",
    "decay-trigger fails without current_sprint_num",
    "decay-trigger defaults decay_after_sprints to 8 when unspecified and no config exists",
    "decay-trigger reads decay_after_sprints from .deckent/config.json when not overridden"
   ],
   "mcp/cost-tool.test.ts": [
    "(1) registerCostTool registers deckent_cost on the server",
    "(2) tool schema accepts optional sprint and tenantId fields",
    "(3) tool annotation is readOnly",
    "(4) returns numeric budget fields matching config",
    "(5) returns optional budget fields when set in config",
    "(6) returns enabled providers with per-MTok pricing",
    "(7) spendTodayUsd comes from injected spendFn",
    "(8) returns configVersion and null configLastUpdated when absent",
    "(9) configLastUpdated is returned when present in config",
    "(10) returns empty providers map when no enabled models — honest empty, no throw",
    "(11) provider defaultBillingMode falls back to first supported mode when default_billing_mode absent",
    "(12) handler returns valid JSON cost view with numeric fields",
    "(13) handler accepts sprint and tenantId args without error",
    "(14) handler returns isError response on configFn throw, does not propagate"
   ],
   "mcp/docs-track-action.test.ts": [
    "track-scan then track-status returns rows from memory.db"
   ],
   "mcp/doctor-provider-diagnostics-parity.test.ts": [
    "uses the same reconciled producer as the CLI surface"
   ],
   "mcp/doctor-provider-health-score.test.ts": [
    "includes reconciled auth warnings without flipping base ok",
    "preserves the provider-free response when providers is false"
   ],
   "mcp/enrich.test.ts": [
    "preserves all existing fields (spread test)",
    "adds _enriched field without modifying response",
    "_enriched.summary is a string",
    "_enriched.hints is an array",
    "_enriched.timestamp is ISO 8601 format",
    "tr localization works",
    "en localization works",
    "unknown tool returns generic fallback summary (en)",
    "unknown tool returns generic fallback summary (tr)",
    "unknown tool returns empty hints array",
    "defaults to en when no context provided",
    "returns known tool summary in en",
    "returns known tool summary in tr",
    "returns generic for unknown tool",
    "defaults to en",
    "returns hints array for known tool",
    "returns empty array for unknown tool",
    "start tool hints include watch command"
   ],
   "mcp/execution-authority.test.ts": [
    "registers as an idempotent mutating, non-destructive tool",
    "uses the shared core authority in dry-run mode by default",
    "refuses apply before core mutation without explicit attestation",
    "forwards apply attestation and returns the canonical evidence DTO",
    "returns only a typed reason and never leaks internal authority paths"
   ],
   "mcp/format.test.ts": [
    "returns healthy message when all checks pass",
    "shows issue count when checks fail",
    "uses singular \"issue\" for single failure",
    "shows recommendations count",
    "uses singular \"recommendation\" for one item",
    "returns \"No health checks available.\" when checks empty",
    "returns \"No health checks available.\" when checks undefined",
    "limits failed check names to 3",
    "uses label when name is missing",
    "returns \"No retrospective available.\" when content is null",
    "returns \"No retrospective available.\" when content is undefined",
    "shows sprint ID and success rate",
    "shows self-healing rate",
    "shows self-healed count",
    "uses singular task for 1 self-healed",
    "does not show self-healed count when 0",
    "shows highlight count when no metrics",
    "returns generic message when content present but no metadata",
    "combines all metrics",
    "returns \"No sprint history available.\" when empty",
    "returns \"No sprint history available.\" when undefined",
    "shows sprint count and trend",
    "shows declining trend",
    "shows stable trend",
    "omits trend when insufficient_data",
    "uses singular sprint for 1 entry",
    "shows avg success rate",
    "wraps doctor data with summary",
    "wraps retro data with summary",
    "wraps history data with summary",
    "formatStatusResponse handles empty data gracefully",
    "formatPlanResponse handles empty data gracefully",
    "formatStartResponse handles empty data gracefully",
    "formatDoctorResponse handles empty data gracefully",
    "formatRetroResponse handles empty data gracefully",
    "formatHistoryResponse handles empty data gracefully",
    "formatErrorResponse handles empty data gracefully",
    "returns \"No active sprint.\" when active is false",
    "returns custom message when active is false with message",
    "returns summary with progress, workers, and ETA",
    "shows singular worker when only 1",
    "omits ETA when unknown",
    "shows critical alert count",
    "shows non-critical alert count when no criticals",
    "handles missing progress gracefully",
    "shows 0% when no tasks done",
    "uses active count from progress when no agents array",
    "returns \"No tasks planned.\" when empty",
    "returns \"No tasks planned.\" when tasks undefined",
    "shows task count and model distribution",
    "shows wave info when multiple waves",
    "shows risk assessment",
    "uses singular \"task\" for single task",
    "includes the registry-derived economy tier",
    "handles unknown model names gracefully",
    "returns success message with watch command",
    "returns failure message with suggestion",
    "handles missing error message",
    "omits duration when not provided",
    "shows what happened with code",
    "uses whatHappened over message",
    "suggests checking phase logs when no howToFix",
    "suggests deckent doctor as fallback",
    "handles completely empty error data",
    "omits code bracket when no code",
    "wraps data with summary",
    "preserves original data structure",
    "has both data and summary keys"
   ],
   "mcp/inspect.test.ts": [
    "no-arg call returns the run listing deep-equal to the core projection",
    "taskId call returns the drill-down deep-equal to the core projection",
    "invalid task id is a typed error result, never a throw",
    "unknown valid task id is a typed not-found error result"
   ],
   "mcp/kill-force.test.ts": [
    "inputSchema exposes force as an optional boolean (CLI parity)",
    "inputSchema exposes userExplicit as an optional boolean (CLI parity)",
    "inputSchema still exposes taskId and all (no regression)",
    "emits mcp:kill:panic-bypass debug breadcrumb when force+userExplicit are both true",
    "includes all=true in the breadcrumb when kill-all is requested with bypass",
    "surfaces bypassRequested=true in the response payload (single taskId)",
    "surfaces bypassRequested=true in the response payload (all=true)",
    "emits mcp:kill:panic-bypass-partial when only force is set",
    "emits mcp:kill:panic-bypass-partial when only userExplicit is set",
    "does not emit panic-bypass (full) breadcrumb when only one flag is set",
    "partial-flag responses still carry bypassRequested=false (kill proceeds, override not granted)",
    "does not emit any panic-bypass breadcrumb when neither flag is set",
    "response payload carries bypassRequested=false by default",
    "still returns an error when neither taskId nor all is provided (no regression)",
    "releases only legacy namespaces and never scans execution authority projections",
    "panic-bypass breadcrumb warning text references feedback_sprint_kill_always_ask_user explicitly"
   ],
   "mcp/kpi-tool.test.ts": [
    "(1a) registers deckent_kpi on the server",
    "(1b) schema accepts empty args (both fields optional)",
    "(1c) tool is readOnly (readOnlyHint: true)",
    "(2) pre-computed result for cost_per_sprint → numeric value in response",
    "(3) all 8 built-in KPI slots appear (result-null slots included)",
    "(4) missing DB → returns { sprintId, kpis: [] } (no error)",
    "(5) no sprint arg + sprintFn returns null → sprintId null, kpis []",
    "(6) tenantId arg is respected — other-tenant data not returned",
    "(7) each kpi item has required fields with correct types"
   ],
   "mcp/kpi-trend-tool.test.ts": [
    "(1) returns { kpiId, series:[...] } with numeric points",
    "(2) empty history → { kpiId, series:[] }, no throw",
    "(3) missing DB → { kpiId, series:[] }, no throw",
    "(4) no trend arg → returns unchanged { sprintId, kpis } scorecard shape",
    "(5) n=2 limits the returned series to 2 most-recent points"
   ],
   "mcp/lint-mcp-instructions.test.ts": [
    "(a) exits 0 with OK message when no drift",
    "(b) exit 0 output contains the canonical tool count",
    "(c) server.ts DECKENT_MCP_INSTRUCTIONS lists all 4 previously-missing tools",
    "(d) server.ts DECKENT_MCP_INSTRUCTIONS header shows the canonical count",
    "(e) lint script detects drift when a tool is removed from instructions (temp file test)"
   ],
   "mcp/mcp-description-catalog.test.ts": [
    "binds every registered tool — count matches the canonical catalog",
    "leaves no hardcoded tool description in source — every one resolves through the catalog",
    "resolves every registered description through mcpToolDescription with no unresolved placeholder",
    "every bound key is a real en+tr pair, never an English-only row",
    "parity: every CLI-counterpart tool reads the SAME key the CLI command reads",
    "keeps the two namespaces honest: MCP-only owns mcp.*.desc, CLI-shared never does",
    "gives each tool its own key — no two tools resolve the same description",
    "language sample 1 — deckent_start renders differently in en and tr",
    "language sample 2 — MCP-only deckent_agent_manage renders differently in en and tr",
    "the server-seeded language reaches registration — tools register in tr when seeded tr",
    "falls back to the canonical resolver, never a hardcoded literal, when nothing is seeded",
    "fails closed on an unbound tool instead of shipping an empty description",
    "keeps deckent_audit\\'s DESTRUCTIVE warning after the migration",
    "a tr-seeded server renders tool AND field descriptions in tr from the same seed",
    "registering under a tr seed renders every deckent_kill field in tr, and the seed does not leak past reset"
   ],
   "mcp/mcp-field-description-catalog.test.ts": [
    "registers every tool in both languages",
    "source: every `.describe(` on a tool module is one of the three catalog reads — no literal, template or free expression",
    "inventory: catalog-read sites equal the registered inputSchema fields, and every field carries a description",
    "every scanned catalog key is a complete en+tr row with identical placeholders",
    "runtime: every rendered field description is exactly one catalog row — en under en, tr under tr, tr differs from en",
    "tools/list shape is language-invariant: JSON schema minus descriptions, and annotations, are identical in en and tr",
    "derives the key from tool + field (snake_case, _desc suffix) and rejects malformed names",
    "fails closed on an unbound field instead of shipping the raw key or an empty description",
    "requires exactly the en+tr pair the rest of the catalog surface requires",
    "language precedence: the seeded language reaches field resolution, an explicit override wins, nothing seeded falls back to the resolver",
    "language sample — deckent_plan.mode describes the config-resolved Brain planning modes, not a fixed provider or API requirement (7085 v2)",
    "language sample — deckent_init.mode is a plan-tier description in both languages, with no fixed model-family name"
   ],
   "mcp/memory-query.test.ts": [
    "returns empty results for query with no matches",
    "finds entries by text query",
    "filters by type array",
    "filters by status array",
    "filters by sprint_range min",
    "uses AND mode requiring all tokens",
    "uses OR mode (default) for broader recall",
    "respects limit parameter",
    "returns relevance score for FTS results",
    "returns snippet with highlight markers for FTS results",
    "performs structured search when no text is provided",
    "handles Turkish text through normalized columns",
    "excludes soft-deleted entries by default",
    "filters by tags_contain"
   ],
   "mcp/memory-read-consumers.test.ts": [
    "runs the registered memory query tool against a readonly bounded view without the legacy 200-character content slice",
    "runs the registered memory resource through the same bounded readonly view instead of returning an unbounded getByType corpus"
   ],
   "mcp/models.test.ts": [
    "registers a tool named deckent_models",
    "returns all models when no provider filter is given",
    "filters models by provider when provider param is given",
    "returns empty models array when provider filter matches nothing",
    "passes offline flag to loadCatalog",
    "returns error response when loadCatalog throws",
    "calls loadCatalog with forceRefresh: true",
    "returns error on refresh failure",
    "returns tier info for a known model id",
    "finds a model by apiId",
    "returns error when model is not found",
    "returns error when model param is missing"
   ],
   "mcp/nervous-edit.test.ts": [
    "missing pending file → not found (fail-safe empty list)",
    "corrupt pending file → not found (fail-safe, never throws)",
    "real pending file on disk → builds accept-with-edit plan (proof-of-function)",
    "matches by shortCode against the real disk-backed store",
    "throws when id is empty",
    "throws when id is whitespace-only",
    "throws when modifiedPayload is missing keys (empty object)",
    "delegates to handleEdit against the injected store — found + plan",
    "returns found:false when the injected store has nothing matching",
    "no records at all → supported:false with a reason",
    "most recent reversible+accepted+success record → supported plan with mark-undone step",
    "skips non-accepted records even if reversible+success (only \"accepted\" is undoable)",
    "specific id not found → supported:false naming the id",
    "specific id found but not reversible → honest unsupported with reason",
    "specific id found but decision is not accepted → honest unsupported",
    "specific id found, reversible+accepted+success → supported plan",
    "no history file on disk → supported:false (never throws)",
    "real appended record on disk → default NervousHistory produces an undo plan (proof-of-function)",
    "never calls markUndone itself — history file still has only the original record",
    "registers with readOnly annotations",
    "happy path returns the plan as JSON",
    "missing id → isError with message",
    "empty modifiedPayload → isError with message",
    "registers with readOnly annotations",
    "happy path — real disk-backed history returns a plan",
    "nothing undoable → supported:false, not an error",
    "registers exactly the 2 tools"
   ],
   "mcp/nervous-tools-e2e.test.ts": [
    "status returns valid JSON structure with config, pending, recent, totalRecords, subscribers",
    "status returns defaults when config does not exist (invalid root fallback)",
    "subscribe registers client and returns subscribed=true",
    "subscribe duplicate sprintId is idempotent",
    "accept with invalid ID format returns MCP error",
    "accept with valid ns-prefixed ID returns accepted response",
    "reject with reason returns rejected response with reason recorded",
    "reject without ID (empty string) returns MCP error",
    "config set_preset with valid preset persists and returns confirmation",
    "config set_preset without preset parameter returns error"
   ],
   "mcp/nervous-tools.test.ts": [
    "deckent_nervous_subscribe registers and returns subscription confirmation",
    "deckent_nervous_accept is registered with correct schema",
    "deckent_nervous_reject is registered and captures rejection reason",
    "deckent_nervous_status is registered and responds",
    "deckent_nervous_config is registered and responds",
    "deckent_nervous_status returns pending + recent + config snapshot",
    "deckent_nervous_accept returns error for invalid ID format",
    "deckent_nervous_config list_actions returns 30 actions",
    "deckent_nervous_config set_preset autopilot → persisted",
    "all 5 nervous tools are registered (total MCP tools count contribution)"
   ],
   "mcp/nervous-undo-compensating.test.ts": [
    "accept → real forward archive → runNervousCompensatingAction restores the exact files to .tasks/ (proof-of-function)",
    "chains with the existing plan-builder (handleNervousUndo, nervous-edit.ts): plan.record → real reversal",
    "destination conflict — a live file with the same name is never clobbered",
    "all destinations conflict → honest applied:false, nothing clobbered",
    "no archive directory on disk → honest applied:false (never archived, or already restored)",
    "empty archive directory → honest applied:false",
    "missing payload.sprintId → honest applied:false, does not throw",
    "does NOT append a markUndone compensation record when nothing was applied",
    "injected historySink is used instead of a real NervousHistory",
    "%s (reversible:true in the registry, but never self-executed by Nervous) → honest applied:false",
    "never silently fakes success — result always carries recordId + actionId + a specific detail"
   ],
   "mcp/planner-evidence-start.test.ts": [
    "${language} preserves %s as a safe MCP error",
    "leaves an unknown error on the pre-existing MCP path"
   ],
   "mcp/recover.test.ts": [
    "projects the same recovery report without MCP-owned lifecycle logic"
   ],
   "mcp/resources.test.ts": [
    "returns dashboard state",
    "returns inactive when no dashboard file",
    "returns directives content",
    "returns empty string when no DIRECTIVES.md",
    "returns memory content",
    "returns a typed unavailable hold when memory.db is absent",
    "parses debt from MemoryStore",
    "returns empty array when no DEBT.md",
    "returns config JSON when file exists",
    "returns error when config file is missing",
    "returns error when config is invalid JSON",
    "registers config resource with correct name and mimeType",
    "returns valid JSON when config file exists",
    "returns error object when config file does not exist",
    "returns error object when config file has invalid JSON",
    "includes uri in content",
    "registers dashboard resource with correct name and mimeType",
    "returns dashboard state when file exists",
    "returns { active: false } when dashboard file does not exist",
    "returns { active: false, error } when dashboard JSON is malformed",
    "returns correct mimeType in contents",
    "registers debt resource with correct name and mimeType",
    "returns debt entries from DB as JSON array",
    "returns empty array when DEBT.md does not exist",
    "returns empty array when DEBT.md has no table rows",
    "handles CRITICAL priority debt item",
    "registers directives resource with correct name and mimeType",
    "returns DIRECTIVES.md content as markdown",
    "returns empty string when DIRECTIVES.md does not exist",
    "preserves full markdown content including headers and code blocks",
    "registers memory resource with correct name and mimeType",
    "returns memory entries from DB",
    "returns a typed unavailable hold when memory.db does not exist",
    "returns correct uri in content",
    "registers retro resource with correct name and mimeType",
    "returns retro content from DB when DB exists",
    "returns empty string when RETRO.md does not exist",
    "returns correct uri in content",
    "registers tasks resource with correct name and mimeType",
    "returns task list when .tasks dir exists",
    "returns empty task list when .tasks dir does not exist",
    "registers agents resource with correct name and mimeType",
    "returns agent list when .deckent/agents dir exists",
    "returns empty agents list when .deckent/agents dir does not exist",
    "registers all 8 resources on the server",
    "config: readFileSync throwing non-JSON error returns error object",
    "dashboard: readFileSync throwing returns error fallback",
    "debt: readFileSync throwing returns empty array"
   ],
   "mcp/run-attempt-custody.test.ts": [
    "projects accepted-awaiting-evaluation without writing its own task/result authority",
    "returns exact zero-work identity instead of collapsing it into a generic error",
    "returns reconciliation-required with its exact receipt instead of calling it zero work",
    "serializes receipt authority when admission throws before dispatch"
   ],
   "mcp/run-budget-authority.test.ts": [
    "persists and dispatches the same owner-authored remote ceiling",
    "forwards the runtime-wide attended authority to the shared final dispatch seam",
    "holds remote execution before disk/routing/prompt/spawn: %s",
    "consumes the injected process authority HOLD before every execution side effect",
    "keeps adapter-declared local execution exempt without inventing a numeric budget"
   ],
   "mcp/run-provider-free.test.ts": [
    "derives Codex ownership from the exact model ID",
    "derives Gemini ownership from the exact model ID",
    "infers Claude ownership when config has no provider configured",
    "rejects a provider/model mismatch before writing a task"
   ],
   "mcp/run-tool-parity.test.ts": [
    "exposes modelEffort, timeoutMs and keep in the inputSchema",
    "forwards modelEffort to the spawnWorkerMultiProvider opts (spawn wire)",
    "sets task.modelEffort in the written task JSON (ExecutionRequest path)",
    "omitted modelEffort → undefined at spawn and absent from the task JSON (no behavior change)",
    "forwards an invalid modelEffort raw to spawn without erroring (validation lives in spawn, CLI parity)",
    "resolveReasoningEffort silently drops invalid/unsupported levels (spawn validation contract)",
    "echoes timeoutMs in the response and defaults to 300000 (CLI --timeout parity)",
    "keep=false → background watcher waits with timeoutMs and cleans up once the result arrives",
    "keep omitted (MCP default: preserve) → no watcher, no cleanup",
    "keep=false + timeout without a result → files preserved (no cleanup under a live worker)",
    "accepts a known exact ID (gpt-5.6-sol); Task JSON + spawn carry the ID + inferred provider",
    "accepts an unseen versioned ID with provider=codex, byte-for-byte through Task JSON + spawn",
    "omitted model resolves from the canonical config default (never a literal alias)"
   ],
   "mcp/server-boot-no-singleton.test.ts": [
    "the singleton-lock module is gone",
    "two boot-equivalent lease acquisitions never throw (no SingletonLockError)"
   ],
   "mcp/server-entrypoint.test.ts": [
    "accepts the direct module path and rejects an imported-module path",
    "fails closed for missing argv and malformed module URLs"
   ],
   "mcp/server.test.ts": [
    "creates a server instance",
    "server has correct name",
    "recognizes a direct executable path and fails closed for absent paths",
    "instructions is a non-empty string",
    "instructions contains all 15 tool names",
    "instructions contains sprint lifecycle phases",
    "instructions contains workflow steps in order",
    "instructions contains DIRECTIVES format example",
    "instructions contains parameter reference with exact provider model IDs, not legacy aliases",
    "instructions contains error recovery guidance",
    "instructions contains all 8 resources"
   ],
   "mcp/start-autoapprove.test.ts": [
    "inputSchema exposes autoApprove (CLI/MCP parity — ADR-022-V2)",
    "autoApprove schema validates without explicit input (default applies) and accepts explicit true",
    "default call (no autoApprove) writes runnerConfig.autoApprove = false",
    "explicit autoApprove=false writes runnerConfig.autoApprove = false",
    "explicit autoApprove=true writes runnerConfig.autoApprove = true (opt-in old behavior)",
    "autoApprove=true does not bypass cost gate (acknowledgeCost still required for over-budget)",
    "autoApprove=true still passes through when cost gate accepts (composability)"
   ],
   "mcp/start-cost-gate.test.ts": [
    "inputSchema accepts acknowledgeCost flag (CLI/MCP parity)",
    "inputSchema includes autoApprove (debugging surface, default false for CLI parity)",
    "returns isError with COST_GATE_EXCEEDED payload when estimate exceeds budget",
    "does not call writeJobState when cost gate blocks (no sprint spawned)",
    "passes acknowledgeCost flag through to evaluateCostGate",
    "proceeds to spawn when acknowledgeCost=true bypasses an over-budget estimate",
    "calls evaluateCostGate with acknowledgement when force=true",
    "blocks unknown pricing even when force=true",
    "passes the cost gate and starts the sprint when estimate is within budget",
    "fails closed with COST_GATE_UNAVAILABLE when planSprint throws"
   ],
   "mcp/start-estimate.test.ts": [
    "surfaces the REAL sprint-estimator output, not the hardcoded ~10-30 minutes",
    "passes the planned tasks through to the estimator (estimate scales with the plan)",
    "force acknowledges numeric overrun without bypassing the cost pre-plan or duration evidence"
   ],
   "mcp/start-lifecycle.test.ts": [
    "returns within 500ms even with a slow detached child (handler must NOT await runSprint)",
    "response advertises deckent_status in monitoring guidance (deckent_watch: Sprint 191 T-006 source gap)",
    "response includes the jobId so callers can correlate detached child output",
    "writes IPC config.json BEFORE forking the detached child (ordering contract)",
    "IPC config written before fork carries jobId and projectRoot (sprint identity for runner)",
    "IPC config written before fork has no childPid field (fork has not happened at write time)",
    "registers an exit handler that cleans up the IPC directory on child success (code=0)",
    "registers an exit handler that cleans up config-only IPC dir on child failure (code!=0)",
    "stale-child exit does NOT clobber a newer sprint anchor (jobId guard) — Sprint 191 T-006 source gap: _internals not exported"
   ],
   "mcp/start-provider-authority.test.ts": [
    "HOLDs %s before every mutation/planning/spawn branch",
    "preserves provider-free dry-run behavior when no authority is configured"
   ],
   "mcp/start-snapshot-branch.test.ts": [
    "starts from the approved snapshot: no re-plan, spawns via spawnDetachedDeckent, persists the handle",
    "forwards autoApprove/force-scope/force-prompt-gate/sandbox/timeout onto the spawned CLI args (CLI parity)",
    "blocks unknown snapshot pricing before detached spawn, even with force=true",
    "noop-duplicate: an identical CAS-matching second start does not re-spawn or re-save",
    "RUN_JOB_DIGEST_MISMATCH: approved snapshot exists but CAS key does not match",
    "RUN_JOB_FLOW_NOT_APPROVED: no snapshot was ever approved for this flowId",
    "EXACT_START_REFERENCE_MISMATCH: the durable attempt journal owns stale-start conflicts",
    "RUN_FLOW_INCOMPLETE_PARAMS: flowId given without revision/planDigest",
    "RUN_FLOW_V2_DISABLED: full flow args given but config.terminal.run_flow_v2 is not true",
    "no flow args at all takes the pre-existing spawn path — flow modules never touched"
   ],
   "mcp/status-failed-tasks.test.ts": [
    "reports failedTasks=2 when sprint has 2 NO_GO tasks",
    "reports failedTasks=0 when all tasks are DONE",
    "reports failedTasks=2 when sprint has mixed DONE and NO_GO tasks",
    "status shape is otherwise unchanged (has sprint, progress, agents)"
   ],
   "mcp/status-readiness-parity.test.ts": [
    "keeps a resumable PAUSED run self-sufficient on all three surfaces",
    "holds an ACTIVE claim without coordinator liveness evidence on all three surfaces",
    "keeps a proven terminal failed flow ABORTED and self-sufficient on all three surfaces"
   ],
   "mcp/status-terminal-receipt.test.ts": [
    "lifecycle=%s -> state=%s, receipt stays null, version matches",
    "surfaces the exact persisted receipt through the shared CLI/MCP projection",
    "surfaces terminal-authority-observed with a null receipt when the run is COMPLETE (no live dashboard)",
    "surfaces terminal-authority-observed for an ABORTED run without fabricating a receipt",
    "surfaces state=open for an ACTIVE run with a live dashboard projection",
    "surfaces state=open for an IDLE run (no active or terminal sprint)"
   ],
   "mcp/tool-annotations.test.ts": [
    "deckent_plan is a write tool (readOnlyHint:false — it writes .tasks/)",
    "deckent_process is a write tool (readOnlyHint:false)",
    "nervous sub-tools carry correct read/write hints"
   ],
   "mcp/tools/annotations.test.ts": [
    "all 15 tools are registered",
    "every tool has an annotations object",
    "every tool has readOnlyHint defined (boolean)",
    "every tool has destructiveHint defined (boolean)",
    "read-only tools do not have destructiveHint=true",
    "destructive tools are NOT read-only",
    "every tool has a non-empty description",
    "every description is at least 80 characters (enriched)",
    "deckent_set_directives description includes DIRECTIVES format example",
    "deckent_plan description mentions plan/task concepts",
    "deckent_status description mentions agents, alerts, progress, job",
    "deckent_review description explains GO/NO_GO/GO_WITH_TECH_DEBT",
    "deckent_analyze_project description mentions what is detected",
    "deckent_retro description explains what is returned",
    "deckent_init: not destructive, idempotent",
    "deckent_status: read-only, not destructive",
    "deckent_kill: destructive, not read-only",
    "deckent_cleanup: destructive, not read-only",
    "deckent_plan: write tool (writes .tasks/task-*.json), not destructive",
    "deckent_retro: read-only",
    "deckent_history: read-only",
    "deckent_doctor: read-only",
    "deckent_analyze_project: read-only, idempotent",
    "deckent_review: read-only (only reads task results)",
    "deckent_sync: not destructive, idempotent (additive-only)"
   ],
   "mcp/tools/audit.test.ts": [
    "should register deckent_audit tool",
    "declares its widest side effects — gate writes gate.json, retention apply prunes (row 490)",
    "should return PASS gate result",
    "should return GATE_FAILURE result",
    "should handle errors gracefully"
   ],
   "mcp/tools/autonomous.test.ts": [
    "registers deckent_autonomous tool",
    "has correct annotations",
    "returns empty status when no state files exist",
    "counts pending approvals from pending.json",
    "reports stop marker present when file exists",
    "summarizes backlog counts",
    "clears stop marker when present and returns guidance",
    "does not call rmSync when no stop marker present",
    "writes stop marker and returns stopped=true",
    "calls backlogAdd with correct params and returns added=true",
    "returns error when id is missing",
    "returns error when title is missing",
    "returns error when backlogAdd throws (e.g. duplicate)",
    "returns empty list when no entries",
    "returns entries from backlogList",
    "calls backlogRemove and returns removed=true",
    "returns error when id is missing",
    "returns error when backlogRemove throws (not found)",
    "returns empty list when no pending approvals",
    "returns pending items from approval gate",
    "calls gate.accept with triggerId and returns approved=true",
    "falls back to id field when triggerId is absent",
    "returns error when neither triggerId nor id is provided",
    "calls gate.reject with triggerId and returns rejected=true",
    "returns error when neither triggerId nor id is provided"
   ],
   "mcp/tools/checkpoint.test.ts": [
    "registers read-only annotations",
    "lists pending checkpoints without mutating files",
    "refuses approve with NOT_A_DECISION_SURFACE and preserves fixture bytes",
    "refuses reject with NOT_A_DECISION_SURFACE and preserves fixture bytes"
   ],
   "mcp/tools/docs.test.ts": [
    "accepts valid relative file path",
    "accepts nested file path",
    "rejects path traversal with ../",
    "rejects double traversal ../../",
    "rejects absolute path outside root",
    "rejects traversal hidden in nested path",
    "accepts file at root level"
   ],
   "mcp/tools/doctor.test.ts": [
    "registers with name deckent_doctor",
    "has correct title and description",
    "schema accepts includeProfile boolean",
    "returns checks array in response",
    "reports node check passing when the version meets the engines floor",
    "reports git check passing when git is found",
    "reports tmux check failing when tmux is not found",
    "reports claude check failing when claude CLI is not found",
    "returns valid JSON in content text",
    "includes healthScore as number",
    "includes recommendations array",
    "includes _enriched metadata",
    "includes overall ok field",
    "adds failed check names to recommendations",
    "healthScore reflects actual check pass rate",
    "ok is false when required checks fail",
    "includes systemProfile when includeProfile is true",
    "omits systemProfile when includeProfile is false",
    "includes subscription info in systemProfile"
   ],
   "mcp/tools/explain.test.ts": [
    "registers tool with name deckent_explain",
    "returns sprint summary with learnings for latest sprint",
    "returns specific sprint when sprintId is provided",
    "returns not found for missing sprint",
    "includes all learnings and task details when verbose is true",
    "returns raw JSON without wrapper when json is true",
    "returns helpful message when no sprints exist"
   ],
   "mcp/tools/help.test.ts": [
    "registers deckent_help tool on the server",
    "tool config has readOnlyHint annotation",
    "returns initialized=false when config.json does not exist",
    "returns initialized=true when config.json exists",
    "returns hasDirectives=false when DIRECTIVES.md is absent",
    "returns hasDirectives=true when DIRECTIVES.md has real content",
    "returns sprintActive=true when .dashboard active=true",
    "returns sprintActive=false when .dashboard is absent",
    "returns lastSprint from job state when jobs dir exists",
    "returns lastSprint=null when no jobs exist",
    "returns routingEngine from config when config exists",
    "returns agentCount from .deckent/agents/ directory count",
    "recommends init when project is not initialized",
    "recommends set_directives when initialized but no directives",
    "recommends status when sprint is active",
    "recommends retro when sprint completed and lastSprint is set",
    "includes version field",
    "includes all three workflow categories",
    "includes tools array with readOnly field",
    "includes resources array with uri field",
    "marks destructive tools correctly",
    "declares the canonical tool count in the instructions header",
    "should list deckent_memory_query in instructions",
    "should use V2 export paths instead of V1 .md paths in resources",
    "lists every canonical tool name in instructions",
    "should list all 8 resources in instructions"
   ],
   "mcp/tools/history.test.ts": [
    "registers tool with name deckent_history",
    "registers tool with schema accepting last and json parameters",
    "returns insufficient_data trend and empty runs list when no run logs exist",
    "calls enrichResponse with history tool name on the summary path",
    "reads run logs and returns one entry per file",
    "limits results to the last N entries",
    "detects an improving trend across increasing completion rates",
    "calls formatHistoryResponse on the non-json summary path",
    "returns an error response when reading run logs throws"
   ],
   "mcp/tools/index.test.ts": [
    "TOOL_CATALOG names match exactly the registered tool names — no missing, no extra",
    "MCP_TOOL_COUNT, TOOL_CATALOG.length and registered count are all equal (50)",
    "the deckent_approvals tool identity appears exactly once in the catalog",
    "catalog has no duplicate tool names",
    "each catalog read-only flag matches the registered readOnlyHint annotation",
    "every catalog entry carries a non-empty description",
    "help output tool list equals TOOL_CATALOG (faithful — single-source change flows to the consumer)",
    "help output includes all 12 tools the old 23-entry list was missing (drift regression)",
    "## Tools (N) header count equals MCP_TOOL_COUNT (single source of the count)",
    "instruction tool names match TOOL_CATALOG exactly — no missing, no extra (drift regression)"
   ],
   "mcp/tools/init.test.ts": [
    "registers tool with name deckent_init",
    "registers tool with title and description",
    "registers tool with inputSchema containing projectName, mode, language",
    "creates required directories",
    "creates config.json with correct fields",
    "returns success: true in response",
    "writes DECKENT.md with project name",
    "calls ensureDeckentImport for AGENTS.md and CLAUDE.md",
    "registers MCP server in .claude/settings.json",
    "uses provided projectName in response",
    "uses provided mode in response",
    "uses provided language in response",
    "supports balanced mode",
    "passes language to enrichResponse for localization",
    "merges into existing config.json instead of overwriting",
    "handles malformed existing config.json gracefully",
    "does not re-register MCP server if already present in settings.json",
    "returns content array with type: text",
    "response includes _enriched meta with summary and hints",
    "response includes nextSteps array",
    "response includes created array of created paths",
    "calls enrichResponse with tool name \"init\""
   ],
   "mcp/tools/job-runner.test.ts": [
    "does not trust a terminal decision stored in the worker-writable result projection",
    "never turns a missing, malformed, or worker-only result into DONE",
    "bounds observational notes without changing evaluation authority",
    "creates collision-resistant job identities outside the sprint namespace",
    "writes, advances, and reads one durable job projection",
    "refuses a terminal rewrite",
    "fails closed while another writer owns the job projection lock"
   ],
   "mcp/tools/memory-query.test.ts": [
    "accepts mode=or",
    "accepts mode=and",
    "defaults mode to or when omitted",
    "rejects invalid mode",
    "accepts an opaque continuation or detail reference without accepting a tenant selector"
   ],
   "mcp/tools/misc-tools.test.ts": [
    "registers tool with name deckent_retro",
    "registers tool with title and description",
    "returns content when a retro entry exists and has data",
    "extracts highlights from bullet points",
    "limits highlights to at most 5 items",
    "returns null content when RETRO.md does not exist",
    "still calls enrichResponse when file missing",
    "returns empty content and empty highlights for an empty retro entry",
    "includes _enriched metadata in response",
    "registers tool with name deckent_sync",
    "registers tool with title and description",
    "calls ensureDeckentImport for CLAUDE.md when DECKENT.md exists",
    "calls ensureDeckentImport for AGENTS.md when DECKENT.md exists",
    "calls ensureDeckentImport twice (CLAUDE.md and AGENTS.md)",
    "returns success: true when sync completes",
    "returns synced file list in response",
    "returns changeCount in response",
    "returns error response when DECKENT.md not found",
    "sets isError flag when DECKENT.md not found",
    "does not call ensureDeckentImport when DECKENT.md missing",
    "includes _enriched metadata in sync response",
    "registers tool with name deckent_analyze_project",
    "registers tool with title Analyze Project",
    "calls analyzeProject with cwd",
    "includes language from analysis result",
    "includes framework from analysis result",
    "includes configSuggestion array in response",
    "suggests test framework setup when testFramework is unknown",
    "suggests pro_plan mode for small projects",
    "suggests max_plan mode for large projects",
    "includes _enriched metadata",
    "calls enrichResponse with \"analyze\" toolName",
    "registers tool with name deckent_set_directives",
    "registers tool with title Set Directives",
    "writes content to DIRECTIVES.md",
    "returns success: true after writing",
    "counts task blocks correctly for Görev format",
    "counts task blocks correctly for Task format",
    "overwrites existing DIRECTIVES.md without error",
    "handles empty content string",
    "writes empty string to file",
    "includes breakdown in response",
    "includes estimatedModels in response",
    "includes _enriched metadata in response",
    "calls enrichResponse with \"set_directives\" toolName"
   ],
   "mcp/tools/plan.test.ts": [
    "returns critical debt holds as structured truth and localized recovery summary",
    "registers the tool with name deckent_plan",
    "schema includes dryRun and mode parameters",
    "schema exposes exact-plan approval and scope acknowledgement",
    "mode parameter accepts ai, structured, auto values",
    "dryRun defaults to true when not provided",
    "passes ai mode to planSprint",
    "passes structured mode to planSprint",
    "passes auto mode to planSprint",
    "passes undefined mode when not specified",
    "does not create task files (planSprint is the only action)",
    "returns response without spawning workers",
    "passes dryRun:true to planSprint so it never writes task files (R7)",
    "forces dryRun even when the caller asks for dryRun:false (preview-only contract)",
    "returns and optionally approves the durable exact flow tuple",
    "returns tasks with id, title, model, priority fields",
    "returns correct task count matching planSprint output",
    "returns model distribution in response",
    "returns wave breakdown based on maxWorkers",
    "returns the same digest-bound execution topology used by preview and scheduler",
    "surfaces the top-level runtime worker override instead of the mode preset",
    "returns risk assessment based on task count",
    "response includes sprintId, sprintNumber and planningMode",
    "returns isError when planSprint throws (e.g. missing directives)",
    "preserves planner proof from a pre-Sprint planning failure",
    "returns isError when loadConfig fails",
    "calls enrichResponse with plan tool name",
    "response content is JSON-serialized enriched object"
   ],
   "mcp/tools/process.test.ts": [
    "registers the deckent_process tool",
    "action=submit builds a controller and returns the submit result",
    "returns a structured held result unchanged and closes the controller",
    "action=submit without description → error",
    "action=submit parses capabilityArgs JSON into the target",
    "action=status reads the durable backlog entry by id",
    "action=status for an unknown id → found:false",
    "action=status without executionId → error"
   ],
   "mcp/tools/recover.test.ts": [
    "registers a destructive, non-idempotent mutation surface",
    "keeps dry-run read-only and delegates exactly once",
    "passes an exact approval through without MCP-owned lifecycle logic",
    "returns the same typed HOLD code and details to MCP readers",
    "does not translate unknown implementation failures into success"
   ],
   "mcp/tools/run.test.ts": [
    "sets assignedAgent and assignedSkills from routeSingleTaskV3 decision",
    "calls routeSingleTaskV3 with the task and projectRoot (V3 signature)",
    "fails closed before task publication when routeSingleTaskV3 holds",
    "always routes via routeSingleTaskV3 (V1 purged — ROUTE-V1-PURGE / ADR-G-006)"
   ],
   "mcp/tools/start-detached-fork.integration.test.ts": [
    "creates IPC directory under .deckent/<jobId>-ipc/",
    "writes config.json with valid SprintRunnerConfig schema",
    "preserves the exact parent admission time in the detached child config",
    "config.json autoApprove defaults to false (Sprint 189 T-009 — CLI parity)",
    "config.json honors explicit autoApprove=true (opt-in old behavior)",
    "config.json forwards sandbox and timeout parameters",
    "config.json projectRoot matches process.cwd()",
    "fork() is called with detached:true and stdio:ignore",
    "child.unref() is called to detach from parent event loop",
    "fork() receives IPC directory as first argument",
    "fork() runner path points to sprint-runner-entry.js",
    "concurrent starts produce unique IPC directories",
    "IPC directory path matches getIpcDir() contract",
    "handler returns immediately with success and jobId"
   ],
   "mcp/tools/start.test.ts": [
    "registers tool with name deckent_start",
    "registers tool with schema accepting autoApprove parameter",
    "returns immediately with a job-scoped timestamp and UUID identity",
    "returns status RUNNING immediately without waiting for sprint",
    "writes RUNNING job state via writeJobState immediately",
    "includes message about background execution",
    "does not set isError on successful start",
    "returns error response when loadConfig throws",
    "formats BrainError with phase info in error response",
    "calls enrichResponse with start tool name",
    "response includes _enriched metadata",
    "detects a fake live-PID lock inside its own isolated sandbox and blocks the sprint",
    "force=true bypasses the fake live lock — sandbox-scoped, not real-repo-dependent"
   ],
   "mcp/tools/status-agents.test.ts": [
    "includes agentAssignments in response",
    "includes skillAssignments in response",
    "groups multiple tasks under same agent",
    "returns empty assignments when no tasks dir",
    "backward compatible: no agentAssignments in no-dashboard response",
    "handles tasks without assignedAgent",
    "handles tasks without assignedSkills",
    "skips malformed task files gracefully",
    "preserves all original fields in response"
   ],
   "mcp/tools/status-history.test.ts": [
    "registers tool with name deckent_status",
    "registers tool with title Sprint Status",
    "registers tool with description mentioning dashboard",
    "returns active: false when dashboard file does not exist",
    "reads and parses dashboard file when it exists",
    "includes progressBar field in response",
    "includes eta field in response",
    "includes workerSummary with agent count",
    "includes alertSummary with alert count",
    "handles zero total tasks with empty progress bar",
    "shows plural alerts for multiple alerts",
    "includes latest job state in no-dashboard response",
    "includes latest job state in active dashboard response",
    "includes job: null when no job state exists",
    "shows RUNNING job state in response",
    "shows COMPLETE job state in response",
    "returns active: false and isError when dashboard is malformed JSON",
    "returns error message mentioning parse failure",
    "includes job state in parse error response",
    "calls enrichResponse with tool name status",
    "response includes _enriched meta with summary and hints",
    "_enriched summary contains status information",
    "response content array has exactly one text item",
    "eta returns unknown when no startedAt or no progress",
    "registers tool with name deckent_history",
    "registers tool with title Sprint History",
    "registers tool with inputSchema containing last parameter",
    "registers tool with description mentioning sprint logs",
    "returns empty sprints array when sprints dir does not exist",
    "reads sprint files from sprints directory",
    "returns sprint id and content for each sprint",
    "filters only sprint-*.md files",
    "returns only the last N sprints",
    "returns sprints in sorted order (most recent last)",
    "handles last=1 returning only most recent sprint",
    "returns all sprints when last exceeds available count",
    "returns insufficient_data when only one sprint",
    "returns insufficient_data when sprints dir is missing",
    "detects improving trend when success rate increases",
    "detects declining trend when success rate decreases",
    "detects stable trend when success rate stays same",
    "returns empty sprints when sprints directory is missing",
    "does not set isError when sprints dir is missing (graceful)",
    "handles empty sprints directory gracefully",
    "calls enrichResponse with tool name history",
    "response includes _enriched meta with summary and hints",
    "_enriched summary mentions sprint history",
    "response content array has exactly one text item",
    "_enriched hints are non-empty for history tool"
   ],
   "mcp/tools/status-rich.test.ts": [
    "registers outputMode parameter in inputSchema",
    "includes eventStreamTail field in response",
    "returns empty eventStreamTail when event file does not exist",
    "limits eventStreamTail to last 20 events",
    "includes metricSnapshot field in response",
    "returns empty metricSnapshot when metrics file does not exist",
    "includes backendBreakdown field in response",
    "returns empty backendBreakdown when no tasks dir",
    "includes phaseCountdown when dashboard has phase and phaseStartedAt",
    "returns null phaseCountdown when no phaseStartedAt",
    "uses explainatory mode when outputMode=explainatory",
    "uses verbose mode when outputMode=verbose",
    "does NOT include _outputMode for standart mode (default path)",
    "includes formatted output body for explainatory mode",
    "includes lastOutputs field in response",
    "returns empty lastOutputs when output dir does not exist",
    "reads output lines from .out files when dir exists"
   ],
   "mcp/tools/status.test.ts": [
    "projects the persisted read-model revision and provider concurrency",
    "does NOT include dependencyGraph when verbose=false",
    "does NOT include dependencyGraph when verbose is omitted",
    "returns undefined dependencyGraph when no depgraph files exist",
    "includes dependencyGraph field when verbose=true and depgraph exists",
    "dependencyGraph has format=mermaid",
    "dependencyGraph.content contains Mermaid graph TD syntax",
    "dependencyGraph.content contains task node definitions",
    "dependencyGraph.json contains wave and dependency data",
    "dependencyGraph.json contains sprintId",
    "returns dependencyGraph with content when only mmd exists",
    "does NOT include dependencyGraph when both canonical and dashboard sprintId are null",
    "tool has verbose parameter in inputSchema",
    "reads metricSnapshot from per-sprint file when it exists",
    "falls back to flat metrics.jsonl when per-sprint file does not exist, filtering by sprintId",
    "returns empty metricSnapshot when neither per-sprint nor flat metrics file exists",
    "includes metricSnapshot in standard (non-json) response",
    "chain: per-sprint metrics file written by observability, read by status tool",
    "failedTasks reflects real NO_GO count (≥1 NO_GO → failedTasks > 0)",
    "failedTasks in json response reflects NO_GO count from task files",
    "failedTasks is 0 when no NO_GO tasks exist (DONE-only)"
   ],
   "mcp/tools/watch.test.ts": [
    "should register deckent_watch tool",
    "should backfill events via sendLoggingMessage",
    "should subscribe to eventBus and forward new events",
    "should filter events by channel keywords",
    "should unsubscribe when sendLoggingMessage throws",
    "should use ALERT level for ALERT channel events",
    "should use provided sprintId instead of current",
    "should respect custom tail count",
    "should also filter live subscription events by channel"
   ],
   "mcp/tools-debt-061-006.test.ts": [
    "deckent_analyze_project uses error:true format on failure",
    "deckent_history uses error:true format on failure",
    "deckent_retro uses error:true format on failure",
    "deckent_set_directives uses error:true format on failure",
    "deckent_config catch uses error:true format",
    "deckent_config validation errors use error:true format",
    "deckent_kill validation error uses error:true format",
    "deckent_sync validation error uses error:true format and keeps success:false",
    "deckent_sync catch error uses error:true format",
    "deckent_review catch error uses error:true format",
    "deckent_run catch error uses error:true format",
    "deckent_cleanup catch error uses error:true format",
    "deckent_config read returns enriched response without error field",
    "deckent_kill success returns enriched response",
    "deckent_config has Zod schema with action enum",
    "deckent_review has Zod schema with auto boolean",
    "deckent_run has Zod schema with model enum",
    "deckent_kill has Zod schema with taskId and all",
    "deckent_cleanup has Zod schema with decay and dryRun"
   ],
   "mcp/tools-enrichment-004.test.ts": [
    "enriched response has breakdown field",
    "breakdown categorizes verification tasks as docs",
    "enriched response has estimatedModels field",
    "existing fields (success, taskCount) preserved after enrichment",
    "response includes _enriched meta with summary/hints/timestamp",
    "Görev headers counted in breakdown",
    "enriched response has waveBreakdown field",
    "12 tasks with maxWorkers=8 → wave1=8, wave2=4",
    "enriched response has modelDistribution field",
    "riskAssessment is low for 0-3 tasks",
    "riskAssessment is high for 9+ tasks",
    "existing fields (sprintId, tasks, recommendation) preserved",
    "enriched response has activeWorkers field",
    "enriched response has queuedTasks field",
    "enriched response has estimatedDuration hint string",
    "existing fields (success, jobId, status, message) preserved",
    "enriched response has progressBar field (mixed)",
    "progressBar is all-empty when done=0",
    "progressBar is all-filled when done=total",
    "enriched response has eta field",
    "workerSummary shows correct active count",
    "alertSummary shows correct alert count",
    "singular alert count shows \"1 alert\"",
    "existing sprint and job fields preserved after enrichment"
   ],
   "mcp/tools-enrichment-batch2.test.ts": [
    "doctor.ts should import enrichResponse",
    "init.ts should import enrichResponse",
    "retro.ts should import enrichResponse",
    "history.ts should import enrichResponse",
    "sync.ts should import enrichResponse",
    "analyze.ts should import enrichResponse",
    "doctor should have recommendations field",
    "init should have nextSteps field",
    "retro should have highlights field",
    "history should have trend field",
    "sync should have changeCount field",
    "analyze should have configSuggestion field",
    "enrich.ts should define Enriched type with _enriched field",
    "enrich.ts should have EnrichedMeta interface",
    "enrich.ts should support localization",
    "enrich.ts should have SUMMARIES for all tools",
    "enrich.ts should have HINTS for all tools",
    "enrichResponse should preserve existing response fields",
    "doctor.ts should not break existing response structure",
    "init.ts should support language parameter for localization"
   ],
   "mcp/tools-enrichment.test.ts": [
    "includes recommendations array and healthScore",
    "preserves original checks field",
    "includes nextSteps array",
    "preserves original fields (success, projectName)",
    "includes highlights from retro content",
    "preserves content field",
    "returns empty highlights when no retro file",
    "includes trend field",
    "returns insufficient_data trend when no sprints",
    "preserves sprints array",
    "includes changeCount",
    "preserves success and synced fields",
    "includes configSuggestion",
    "preserves original analysis fields",
    "_enriched has correct structure across all tools"
   ],
   "mcp/tools-quality-059010.test.ts": [
    "config tool has summary in enrich.ts",
    "review tool has summary in enrich.ts",
    "kill tool has summary in enrich.ts",
    "run tool has summary in enrich.ts",
    "cleanup tool has summary in enrich.ts",
    "deckent_analyze_project returns isError on analyzeProject failure",
    "deckent_history returns isError on readdirSync failure",
    "deckent_retro returns isError on DB read failure",
    "deckent_set_directives returns isError on writeFileSync failure",
    "deckent_plan returns isError on planSprint failure",
    "deckent_sync returns isError on ensureDeckentImport failure",
    "deckent_config read action returns enriched response",
    "deckent_config get action returns error when key missing",
    "deckent_config set action returns error when value missing",
    "deckent_kill requires taskId or all=true",
    "deckent_review returns enriched response with reviews array",
    "deckent_cleanup dryRun returns preview without deleting",
    "deckent_run creates task with enriched response"
   ],
   "mcp/tools.test.ts": [
    "creates project structure",
    "registers MCP in .claude/settings.json",
    "writes DIRECTIVES.md and counts tasks",
    "counts Task N: headers too",
    "returns planned sprint with tasks",
    "passes mode input to planSprint",
    "returns error when loadConfig fails",
    "returns dashboard state when active",
    "returns inactive when no dashboard",
    "includes latest job state in response",
    "returns doctor checks",
    "returns retro content when file exists",
    "returns null when no retro file",
    "returns project analysis JSON",
    "has readOnlyHint annotation",
    "returns sprint logs",
    "returns empty array when no sprints dir",
    "syncs CLAUDE.md and AGENTS.md when DECKENT.md exists",
    "returns error when DECKENT.md is missing"
   ],
   "mcp/usage-lineage-projection.test.ts": [
    "(1) accepts an optional lineage object of tasks + attempts",
    "(2) produces the exact same aggregate as calling the core authority directly",
    "(3) folds a dynamic FIX attempt into its logical root and retains all usage",
    "(4) never silently converts an unmetered-invoice gap to zero billed USD",
    "(5) returns the projected aggregate untouched when lineage input is supplied",
    "(6) existing window-mode getUsageData path is unaffected by the lineage addition"
   ],
   "mcp/usage-tool.test.ts": [
    "(1) registerUsageTool registers deckent_usage on the server",
    "(2) tool schema accepts all optional fields (sprint, since, until)",
    "(3) returns model summary array when records exist",
    "(4) returns empty message when no records found",
    "(5) model summary includes expected shape fields",
    "(6) sprint mode returns tasks + totals + cacheGate shape",
    "(7) sprint mode with no matching tasks returns empty tasks array",
    "(8) script exits 0 with the canonical tool count"
   ],
   "mcp/writer-lease-failclosed.test.ts": [
    "denies the write (handler never runs) when the lease check throws an fs-error",
    "normal lease acquire (no fs-error) still allows the write to proceed",
    "owner-held live+fresh lease (non-error deny path) is unaffected — still WRITER_LEASE_DENIED"
   ],
   "mcp/writer-lease-gate.test.ts": [
    "non-mixed write tools are always writes",
    "config read is not a write; config set is",
    "docs list is read; docs track-scan is write",
    "autonomous approve/reject are writes; status/backlog_list are reads",
    "returns a non-throwing tool result with code + ownerPid",
    "read tools (readOnlyHint:true) run ungated even when another window holds the lease",
    "write tool is denied when another live window owns the lease",
    "write tool runs after handover (owner dead → steal)",
    "fails CLOSED (denies the write, isError:true) when the lease cannot be written",
    "mixed tool read action runs even when lease is held"
   ],
   "mcp/writer-lease.test.ts": [
    "acquires when no lease exists and writes this pid",
    "refreshes heartbeat when already owned by self",
    "denies when owned by another live + fresh pid",
    "steals when owner pid is dead",
    "steals when owner is alive but heartbeat is stale (> ttl)",
    "treats a corrupt lease file as free and acquires",
    "release removes the lease only when owned by self",
    "release is a no-op when the lease is owned by another pid",
    "isProcessAlive returns true for the current process and false for an impossible pid",
    "adds exactly one exit-listener on double-call and second call is no-op"
   ]
  },
  "mcp-client": {
   "mcp-client/broker.test.ts": [
    "listTools returns the tools exposed by a connected server",
    "callTool returns the server result and fires the onCall audit hook",
    "disconnect removes the server from the pool",
    "connect to a non-existent stdio command rejects with a wrapped error",
    "callTool on an unknown server throws a graceful error and emits an audit record for errors only when the call reaches the SDK",
    "callTool wraps SDK transport errors and emits an error audit record"
   ],
   "mcp-client/mcp-config.test.ts": [
    "returns empty map when no config files exist",
    "parses stdio and http server definitions from project .mcp.json",
    "merges 3 scopes with local > project > user precedence",
    "includeProjectScope:false drops the git-tracked project scope, keeps user + local (REPL-575 K1-C)"
   ],
   "mcp-client/mcp-registry.test.ts": [
    "registers tools with namespaced keys (server__tool)",
    "resolve returns correct { server, tool } for a namespaced name",
    "resolve returns undefined for unknown namespaced name (no clash with deckent tools)",
    "register is idempotent on refresh — second call replaces old entries",
    "multi-server tools do not clash with each other",
    "clear removes only the specified server entries",
    "listForServer returns only tools for that server",
    "handles tool names containing underscores correctly"
   ]
  },
  "monitor": {
   "monitor/adr-noise.test.ts": [
    "returns empty violations array when changedFiles is []",
    "does NOT instantiate MemoryStore when changedFiles is [] (DB scan skipped)",
    "does NOT emit ADR_VIOLATION event when changedFiles is [] and sprintId is provided",
    "still loads DB and scans files when changedFiles is non-empty (regression guard)",
    "returns adr=\"ok\" when filesChanged is [] (injected checkAdr)",
    "returns adr=\"ok\" when filesChanged is [] (real checkADRCompliance with early-return)",
    "does NOT write ADR_VIOLATION to event-stream when filesChanged is []"
   ],
   "monitor/alert-dedup.test.ts": [
    "returns single entry when same source is added twice — count increments",
    "creates separate entries for different sources",
    "caps list at MAX_ALERTS when many distinct alerts accumulate",
    "merges repeated source entries into one with count",
    "keeps distinct sources as separate entries",
    "sorts by lastSeenAt descending — most recent first",
    "caps at DASHBOARD_MAX_ALERTS when given more than 50 distinct entries",
    "repeated emitAlert for same source produces one dashboard entry with count",
    "different subjects produce separate dashboard entries",
    "59 repeated calls result in exactly 1 dashboard entry with count=59"
   ],
   "monitor/alert-emitter.test.ts": [
    "appends alert to dashboard.json and emits to event stream",
    "handles missing dashboard.json gracefully — only emits to event stream",
    "provider parity — .codex .gemini .cursor rules all have paths frontmatter",
    "merges same source + DIFFERENT message into one entry, refreshing the message"
   ],
   "monitor/apdd-pilot-rules.test.ts": [
    "does not fire when the DB carries the real id adr-g-002 (spawnSync now lives in authority-enforcer.ts)",
    "fires with the real taxonomy id adr-d-004 and reports the canonical uppercase adrId",
    "does not fire for the same file when it has no brain import (clean)",
    "ignores files outside the rule targetFiles list even with a brain import",
    "regression guard: the rule does not self-match its own definition text"
   ],
   "monitor/auditor-agent.test.ts": [
    "includes agentId from heartbeat file",
    "handles heartbeat without agentId"
   ],
   "monitor/auditor-async-liveness.test.ts": [
    "returns a Promise (non-blocking async probe, not spawnSync)",
    "detects a running Docker container (non-empty stdout → true)",
    "returns false for a stopped Docker container (empty stdout)",
    "detects an active tmux session (exit 0 → true)",
    "returns false for a dead tmux session (exit 1)",
    "returns false for subprocess backend without spawning (conservative)",
    "returns false for an undefined backend (conservative)",
    "resolves false (fail-safe) when the probe emits an error",
    "resolves false and kills the child on timeout",
    "resolves false (fail-safe) when spawn throws synchronously",
    "probes N workers IN PARALLEL (all spawns start before any resolves)",
    "resolves a verdict for every worker",
    "isolates a single failing probe — other workers are unaffected",
    "populates the module-level liveness cache",
    "returns an empty map for no heartbeats (no spawns)",
    "suppresses stale when the cache reports the worker alive (no sync probe)",
    "reports stale when the cache reports the worker dead + HB stale + no result",
    "still returns false for a FRESH heartbeat regardless of cache",
    "returns active (non-DONE) heartbeats and skips DONE workers",
    "returns [] when the .tasks directory is absent",
    "refreshLivenessFromDisk reads HBs, probes them, and warms the cache",
    "probeWorkerAlive source uses async spawn — no spawnSync in the probe path"
   ],
   "monitor/auditor-baseline.test.ts": [
    "parses the JSON reporter shape (numPassedTests + numFailedTests + numTotalTests)",
    "falls back to the human Tests-line (\"3 failed | 11312 passed (11315)\")",
    "returns parseOk=false on completely garbled output",
    "rejects JSON with internally inconsistent totals",
    "returns SPAWN_FAIL with zero counts when subprocess never produces an exit code (after retry)",
    "returns SPAWN_FAIL with attempts=1 when retry is disabled",
    "returns SPAWN_FAIL when spawnSync surfaces an ENOENT-like error twice",
    "recovers on retry when the first attempt fails but the second succeeds",
    "returns OK when the JSON reporter output is parseable",
    "returns PARSE_FAIL when subprocess exited but output is unparseable",
    "truncates large stderr to last 2KB into stderrTail",
    "persists a record with vitest_invocation_status=OK and full counts",
    "zeros the counts when vitest_invocation_status=SPAWN_FAIL",
    "zeros the counts when vitest_invocation_status=PARSE_FAIL",
    "creates the .deckent directory when it does not exist yet",
    "uses the injected spawnFn instead of node:child_process spawnSync"
   ],
   "monitor/auditor-deadlock-e2e.test.ts": [
    "detects A → B → C → A cycle",
    "detects self-dependency (A → A)",
    "returns no violations for independent tasks",
    "linear chain A → B → C is NOT a deadlock",
    "diamond dependency A→B, A→C, B→D, C→D is NOT a deadlock",
    "detects two-node cycle A → B → A",
    "empty task list returns no deadlocks",
    "single task with no dependencies returns no deadlocks",
    "handles 10+ tasks without cycle correctly",
    "handles 10+ tasks with a cycle among a subset",
    "complex graph: mixed cycles and non-cycles",
    "violation type is always circular_dependency",
    "violation has a timestamp"
   ],
   "monitor/auditor-edge.test.ts": [
    "multiple stale agents produce separate violations and alerts each",
    "all heartbeats malformed JSON → empty heartbeats, no stale",
    "timestamp at exactly threshold boundary is NOT stale (elapsed == threshold = not >)",
    "future timestamp (clock skew) is NOT marked as stale",
    "stale detail string contains elapsed seconds and task ID",
    "numeric-string timestamp (not ISO) parses as NaN → agent is skipped (not marked stale)",
    "nested directory access is allowed (deep path within scope)",
    "prefix overlap protection: src/core-extra/ is NOT inside src/core/",
    "filesWrite exact match allows the file",
    "empty scopes map → no violations regardless of git changes",
    "git error (status !== 0) → returns empty violations",
    "multiple workers — file outside all scopes generates one violation per worker",
    "git stdout is null → returns empty",
    "malformed lock JSON is skipped gracefully",
    "multiple stale locks each generate separate violations and alerts",
    "lock at boundary (just under 5 min) is NOT stale",
    "stale lock detail includes file path and elapsed seconds",
    "stale lock alert level is WARNING (not CRITICAL)",
    "no .lock extension files are not processed",
    "3-way cycle A→B→C→A is detected",
    "diamond dependency (no cycle): A→B, A→C, B→D, C→D",
    "single task with self-dependency (A depends on A) is cycle",
    "empty task list returns no violations",
    "dependency on non-existent task is not treated as cycle",
    "partial cycle: A↔B in larger graph is still detected",
    "circular dependency violation includes timestamp",
    "corrupted existing dashboard JSON starts fresh without throwing",
    "violation count is sum of all violation types",
    "agent with matching heartbeat gets currentAction updated",
    "alert deduplication: same alert twice → count=2, length stays 1",
    "exactly 50 existing alerts + 1 new unique → oldest dropped, length stays 50",
    "clearInterval stops future ticks from executing",
    "multiple ticks call onScanComplete each time",
    "returns an interval handle (not null/undefined)",
    "custom interval overrides default AUDITOR_SCAN_INTERVAL_MS",
    "updates timestamp of existing alert when incremented",
    "multiple different sources each tracked independently",
    "capping at 50 preserves newest entries (slice from end)"
   ],
   "monitor/auditor-hb-reconciliation.test.ts": [
    "contains DONE and GO_WITH_TECH_DEBT",
    "does not contain NO_GO",
    "(Sprint 134 exact case) HB FAILED exitCode 137 + result DONE → returns false (suppress alert)",
    "HB stale + no .result file → returns true (normal stale, alert should fire)",
    "HB stale + result NO_GO → returns true (honest failure, alert should fire)",
    "HB stale + malformed JSON in .result → returns true (fail-safe)",
    "HB stale + result GO_WITH_TECH_DEBT → returns false (suppress alert)",
    "result file exists but selfAssessment field is missing → returns true",
    "result file with unknown selfAssessment value → returns true",
    "readFileSync throws (I/O error) → returns true (fail-safe)"
   ],
   "monitor/auditor-heartbeat-authority.test.ts": [
    "uses a fenced alive snapshot for liveness even when the legacy heartbeat is stale",
    "keeps task verdict separate from host exit outcome when deciding terminal state",
    "reads authority snapshots without caching, so a new fence is visible immediately",
    "turns conflicting fences into a HOLD alert instead of a stale decision"
   ],
   "monitor/auditor-patterns.test.ts": [
    "records a violation pattern entry in memory.db",
    "groups violations by type with an occurrence count in metadata",
    "creates a distinct entry per violation type",
    "upserts idempotently — re-detecting the same sprint+type keeps one row",
    "does nothing when there are no violations",
    "is a graceful no-op when memory.db is absent"
   ],
   "monitor/auditor-scope-export.test.ts": [
    "returns true for a file inside a scoped directory",
    "returns false for a file outside every scoped directory",
    "returns true for an exact filesWrite match"
   ],
   "monitor/auditor-spawn-lock-binding.test.ts": [
    "emits stale_spawn_lock alert for orphan spawn locks (no matching task)",
    "preserves spawn locks for active tasks (task.json exists in non-terminal status)",
    "emits stale_spawn_lock TTL alert for spawn locks older than 5 minutes",
    "does not emit spawn-lock alert when no spawn locks present"
   ],
   "monitor/auditor-validation.test.ts": [
    "open → persists an open finding to .deckent/findings/<sprint>.json",
    "open is idempotent — re-opening the same artifact preserves openedAt + does not duplicate",
    "recheck increments rechecks + keeps the finding open; close marks closed",
    "recheck on a non-existent finding is a no-op (undefined)",
    "findingId is stable + unique per (taskId, artifact)",
    "INCOMPLETE result → opens a finding + reports INCOMPLETE with missingFields",
    "after the field is filled, a recheck of THAT result closes the finding (tracked to resolution)",
    "a separate OK result is validated ONCE and never re-checked",
    "the ledger tracks the artifact open → closed across the full lifecycle",
    "result and log artifacts of the same task are tracked independently"
   ],
   "monitor/auditor.test.ts": [
    "returns proper Alert with all fields and ISO timestamp",
    "allows optional source (undefined)",
    "returns empty when no .hb files exist",
    "parses valid heartbeat files",
    "detects stale agent (>120s)",
    "skips invalid JSON (1 valid + 1 invalid → 1 heartbeat)",
    "does not mark agent as stale when timestamp is malformed placeholder text (resilient)",
    "does not mark agent as stale when timestamp is empty string",
    "fresh UTC ISO timestamp (new Date().toISOString()) is NOT marked as stale",
    "timestamp 121 seconds old is marked as stale (>120s threshold)",
    "malformed timestamp: agent still appears in heartbeats list (tracked but not stale)",
    "skips stale check for heartbeats with DONE status",
    "skips stale check when .result file exists with successful selfAssessment (cleanup_delay_ms scenario)",
    "other active workers are not affected by completed task heartbeat cleanup",
    "returns empty when git has no changes",
    "detects file outside scope",
    "passes file within scope",
    "returns empty when no .lock files",
    "detects stale lock (>5min)",
    "passes fresh lock without issues",
    "removes stale lock file when autoClean=true",
    "does NOT remove stale lock when autoClean=false (default)",
    "does NOT remove stale lock when autoClean omitted (default false)",
    "does NOT remove fresh (non-stale) lock even when autoClean=true",
    "logs INFO alert (not WARNING) when lock is auto-removed",
    "logs WARNING alert (not INFO) when lock is stale but autoClean=false",
    "still reports staleLocks violation even after auto-removal",
    "handles unlinkSync failure gracefully — falls back to WARNING alert",
    "removes multiple stale locks when autoClean=true",
    "mixes stale + fresh: only removes stale ones when autoClean=true",
    "runScanCycle passes autoCleanLocks=true to checkStaleLocks",
    "removedLocks is empty array when no locks dir exists",
    "returns empty for acyclic dependencies (A→B→C)",
    "detects circular dependency (A↔B)",
    "returns empty when no dependencies",
    "writes JSON to .dashboard file",
    "rejects a stale watcher overwrite after terminal publication",
    "builds scope map from task files",
    "combines all sub-scan results",
    "calls runScanCycle on each tick",
    "uses default AUDITOR_SCAN_INTERVAL_MS",
    "continues loop even if runScanCycle throws",
    "invokes onScanComplete callback after scan",
    "swallows callback errors — loop continues",
    "writes dashboard JSON with merged alerts",
    "updates agent statuses from heartbeats",
    "adds a dynamically born worker from heartbeat plus exact task routing metadata",
    "handles no existing dashboard (fresh start)",
    "limits alerts to 50",
    "includes auditorLastScan in output",
    "includes violations count",
    "dashboard JSON contains all DashboardState fields",
    "writes fresh state with correct sprint ID",
    "sets progress to 0/total with correct task count",
    "clears old alerts (empty alerts array)",
    "clears old agents (empty agents array)",
    "sets updatedAt to ISO timestamp",
    "adds new alerts with count=1",
    "does not add duplicate source+message — increments count instead",
    "allows same message with different source",
    "count increments correctly across multiple duplicates",
    "caps result at 50 (removes oldest when over limit)",
    "returns empty array when both inputs are empty",
    "handles undefined source correctly (deduplicates by message only)",
    "writeScanToDashboard uses deduplication — repeated alerts do not stack",
    "returns zero when tasks dir does not exist",
    "counts result files correctly",
    "extracts task IDs from result filenames",
    "ignores non-result files",
    "returns empty set when no files in dir",
    "takes progress.done from the canonical read-model, never the result-file count (T-671-002)",
    "takes progress.active from the canonical read-model (heartbeats are presentation-only)",
    "publishes the read-model quadruple verbatim when no result files exist",
    "marks agent status as DONE when task has .result file",
    "preserves the existing progress quadruple when the read-model is unavailable (fail-closed)",
    "should return NOT_TRIGGERED when result is already DONE",
    "should verify code on disk for Docker NO_GO pattern",
    "exports the sentinel value",
    "parses grep command from Kanıt line",
    "returns null for disallowed commands",
    "returns null when no Kanıt line exists",
    "maps src files to test paths",
    "skips non-src files",
    "skips test files",
    "returns PASS when no affected tests exist",
    "passes when notes explain tech debt",
    "downgrades to NO_GO when notes are empty",
    "preserves NO_GO without ambient shared-worktree promotion",
    "dispatches GO_WITH_TECH_DEBT to validateTechDebt pipeline",
    "dispatches DONE to verifyFunctional pipeline",
    "parses ADR entries from DECISIONS.md content",
    "detects ADR-006 violation (spawnSync with shell:true)",
    "returns no violations for clean files",
    "inventory-drift: no advisory when changedFiles excludes package.json",
    "inventory-drift: warns (severity warning, never NO_GO) for each undocumented package.json dep",
    "inventory-drift: no advisory when every dep is documented (any count is fine)",
    "checkDependencyInventoryDrift (direct): returns [] when package.json is not in the changeset",
    "reads from disk on first access and caches result",
    "returns cached value when mtime unchanged",
    "re-reads from disk when mtime changes",
    "returns null and clears cache when file does not exist",
    "returns null and clears cache when file is malformed JSON",
    "clearHeartbeatCache empties the cache",
    "detects running Docker container",
    "returns false for stopped Docker container",
    "detects active tmux session",
    "returns false for dead tmux session",
    "returns false for subprocess backend (conservative)",
    "returns false when backend is undefined",
    "returns false when spawnSync throws",
    "returns false (not stale) when HB timestamp is fresh",
    "returns false (not stale) when HB is fresh AND result exists",
    "returns false (not stale) when HB is stale but result DONE",
    "returns false (not stale) when HB is stale but container running",
    "returns true (stale) when all 3 signals indicate dead",
    "returns true for malformed timestamp",
    "Sprint 138 false positive regression: DONE result suppresses stale",
    "returns true (stale) when HB stale + result NO_GO + no process",
    "considers sequence progression via hbPath cache",
    "uses cached heartbeats and multi-signal detection",
    "does not produce false positive stale alert when result DONE",
    "returns empty when no .hb files exist",
    "returns empty when all HB task IDs are in active set",
    "detects orphan HB when task ID is not in active set",
    "detects multiple orphans",
    "auto-discovers active task IDs from disk when not provided",
    "returns empty result when .tasks dir does not exist",
    "returns zero counts when no orphans exist",
    "archives orphan HB files through the canonical sprint archive authority",
    "continues processing other orphans when one rename fails",
    "releases locks for orphan workers via clearOrphanLocks integration",
    "returns empty array when tasks directory does not exist",
    "returns empty array when no HB files exist",
    "detects violation when worker is executing before dep is DONE",
    "does NOT flag violation when dep is DONE (status DONE)",
    "does NOT flag violation when dep has a DONE .result file",
    "does NOT flag violation for workers with non-executing statuses (e.g., DONE)",
    "does NOT flag violation for tasks with no dependencies",
    "handles multiple violations across multiple workers",
    "includes timestamp in violation result",
    "runScanCycle includes dependencyViolations in result",
    "calls writeEvent with VERIFICATION_RESULT channel",
    "does not throw if writeEvent throws (fail-safe)",
    "passes status field when provided",
    "calls writeEvent with ADR_VIOLATION channel when violations exist",
    "does NOT call writeEvent when violations list is empty",
    "does not throw if writeEvent throws (fail-safe)",
    "emits VERIFICATION_RESULT event when sprintId is provided",
    "does NOT emit event when sprintId is omitted (backward compat)",
    "still returns correct verdict when event write fails",
    "emits ADR_VIOLATION event when sprintId provided and violation found",
    "does NOT emit event when no violations found",
    "does NOT emit event when sprintId is omitted"
   ],
   "monitor/dashboard-manager.test.ts": [
    "should return true for a valid dashboard state",
    "should return false for null",
    "should return false for a string",
    "should return false when sprint is missing",
    "should return false when sprint.id is missing",
    "should return false when agents is not an array",
    "should return false when progress is missing",
    "should return false when progress.done is not a number",
    "should return false when progress.total is not a number",
    "should return false when alerts is not an array",
    "should return false when updatedAt is missing",
    "should accept DASHBOARD_INITIAL_STATE constant as valid",
    "should return null for valid data",
    "should report missing sprint",
    "should report missing progress.done",
    "should report missing updatedAt",
    "should create .dashboard when file does not exist",
    "should not touch valid .dashboard file",
    "should repair corrupt JSON in .dashboard",
    "should repair valid JSON with wrong schema",
    "should return valid=false when file does not exist",
    "should return valid=true for a correctly shaped file",
    "should auto-repair and report corrupt JSON",
    "should merge defaults for partial but parseable JSON objects",
    "should handle empty file gracefully (corrupt JSON)",
    "should handle truncated JSON (partial write) gracefully",
    "should treat non-object JSON (array) as invalid",
    "should preserve extra fields from dashboard file",
    "pins COMPLETE even when the raw snapshot still says ACTIVE (no regression)",
    "projects ABORTED outcomes and leaves progress untouched",
    "fails soft on a malformed receipt — raw projection survives",
    "ignores a receipt belonging to a different sprint (mismatch is fail-soft)"
   ],
   "monitor/gate-w1-boundary-alert.test.ts": [
    "out-of-scope write → CRITICAL alert (not WARNING)",
    "out-of-scope write → alert message contains worker id and file path",
    "out-of-scope write → emits authority violation to event stream",
    "in-scope file (allowed) → no alert emitted",
    "non-file_outside_scope violation type → skipped (no alert)",
    "multiple out-of-scope violations → each gets a CRITICAL alert",
    "file inside directory → in scope",
    "file outside directory → not in scope",
    "file matching explicit filesWrite → in scope"
   ],
   "monitor/sprint-state.test.ts": [
    "(1) returns sprintId from sprint-state.json when it exists",
    "(2) returns null when neither sprint-state.json nor sprint-active.json exist",
    "(3) stale .dashboard is NOT consulted — sprint-active.json is the truth",
    "(4) returns null when sprint-state.json has malformed JSON (parse fail → null)",
    "(5) prefers sprint-active.json over sprint-state.json when both exist",
    "(6) falls back to sprint-state.json when sprint-active.json has malformed JSON"
   ]
  },
  "native": {
   "native/exec-authority-native.test.ts": [
    "requires a real canonical binding for the active Linux proof cell",
    "does not downgrade a built Linux prebuild to metadata-unavailable on read-only foreign-owned mount ancestors",
    "keeps malformed root-separation results and alias ambiguity fail-closed",
    "transfers POSIX created-directory rollback authority to the common result gate",
    "sizes the common effect dispatch argv for the full source-read contract",
    "derives the binary build type from the exact node-gyp configuration",
    "rejects forged, stale and cross-domain handles",
    "rejects cancellation, bounds, symbolic links and hard links",
    "binds the real native capture digest to the full canonical entry body",
    "streams a no-follow workspace source through a bounded opaque cursor",
    "keeps source authority proxy, accessor, cancellation and hardlink paths fail-closed",
    "keeps effect-v2 in a distinct trust domain, ABI and handle generation",
    "enables effects only on Linux and makes Darwin/Win32 an explicit unsupported boundary",
    "pins bounded no-follow capture before any content read",
    "reopens the pinned capture root without sharing directory offsets",
    "pins the bounded source-read cursor to workspace custody and before/after identity",
    "pins staged identity, CAS, durable landing, crash reconciliation and final fan-in",
    "pins proxy/accessor rejection, immutable byte snapshots and typed handle replay checks",
    "binds probe and installed-package receipts to real effect ABI and landing evidence",
    "admits one exact bounded scan operation and rejects legacy readdir as authority",
    "pins POSIX enumeration to a duplicated descriptor with strict bounds and mutation proof",
    "implements the Win32 HANDLE-bound source contract without claiming runtime support",
    "opens directories relative to a pinned parent and reports identity",
    "creates a directory only after the common guard accepts its rollback authority",
    "refuses to traverse a symlink component (O_NOFOLLOW fail-closed)",
    "lists, unlinks and removes entries strictly through the pinned handle",
    "renames within pinned handles and keeps inode identity",
    "resolves the kernel-verified CURRENT path of a handle (fdPath, W3-PR-B slice-2)",
    "reports platform-typed mount and host/boot identity availability",
    "confirms only a frozen, identity-bound disjoint result",
    "rejects same-target and ancestor overlap in both directions",
    "rejects a symlink component instead of following it",
    "rejects replay of a closed custody-root handle",
    "accepts only the nonce-bound child lifecycle receipt",
    "rejects a fake outer receipt followed by target process.exit(0)",
    "rejects target stdout pollution even when the real runtime lifecycle succeeds",
    "rejects malformed or multiple target output records",
    "kills and rejects a target that keeps the isolated import pending"
   ]
  },
  "nervous": {
   "nervous/action-handlers.test.ts": [
    "handles WORKER_RESPAWN by killing and re-spawning when canRespawn",
    "proposes WORKER_RESPAWN (no kill/spawn) when canRespawn is false",
    "writes a cooperative respawn-request when requestRespawn is injected (N3)",
    "handles ORPHAN_TASK_ARCHIVE by invoking archiveOrphanTasks",
    "handles STALE_LOCK_RELEASE by invoking releaseLock",
    "handles DEAD_EVENT_STREAM_CLEANUP by invoking cleanDeadEventStream",
    "handles every registry action (none returns `unimplemented`)",
    "returns `unimplemented` for unknown action IDs",
    "returns failure outcome when underlying helper throws",
    "returns failure when required payload is missing",
    "handles LOG_ROTATION by invoking rotateLogs",
    "handles CACHE_INVALIDATE with default cacheType=all",
    "handles CACHE_INVALIDATE with custom cacheType",
    "handles IPC_DIR_CLEANUP by invoking cleanIpcDir",
    "handles DEBT_TRENDING_REPORT by invoking generateDebtReport",
    "handles METRIC_EMIT by invoking emitMetric",
    "handles METRIC_EMIT with default value=1 when value omitted",
    "returns failure for METRIC_EMIT with missing metricName",
    "LOG_ROTATION is idempotent (calling twice yields success both times)",
    "%s lands a Brain proposal via recommend() (no repo mutation)",
    "recommendation handlers never touch destructive deps",
    "surfaces failure when recommend() throws",
    "COMPLETENESS: no registry action returns `unimplemented`",
    "createActionHandler bridges to Executor (autonomous policy chain)",
    "createActionHandler maps `unimplemented` → executor failure",
    "createActionHandler executes new low-risk LOG_ROTATION via Executor",
    "METRIC_EMIT default dep appends a real .deckent/metrics.jsonl line",
    "a recommendation action default dep appends a real proposal",
    "DEBT_TRENDING_REPORT default dep writes a real trend report (no mock)",
    "ActionHandlerResult outcome field has correct union shape"
   ],
   "nervous/action-registry.test.ts": [
    "should contain exactly 30 actions",
    "should have correct category distribution",
    "should look up ORPHAN_TASK_ARCHIVE by ID",
    "should have all 5 safety floor actions with requiredSafetyFloor",
    "should have defaultRisk consistent with category",
    "should return undefined for unknown action ID",
    "should return 11 medium-risk actions",
    "should identify safety floor actions correctly",
    "should return false for non-safety-floor actions",
    "should have no duplicate action IDs"
   ],
   "nervous/approval-bridge-risk-tier.test.ts": [
    "sets the safety floor from authoritative riskTier, not legacy risk",
    "fails malformed explicit tier to the safety floor"
   ],
   "nervous/approval-bridge.test.ts": [
    "maps accepted -> allow and clears the pending store",
    "maps rejected -> deny",
    "honors an explicit channel/decidedAt override instead of the nervous default",
    "works without an injected pendingCleanup (optional)",
    "a second accept/reject for an already-decided id is swallowed, not thrown, and cleanup still runs both times",
    "rethrows a broker error that is NOT already-decided",
    "isNervousSourced is true only for requester.role === \"nervous\"",
    "returns undefined for a non-nervous-sourced request",
    "projects a full NervousNotification from a nervous-sourced request",
    "maps policy=%s -> nervous policy=%s",
    "omits sprintId/taskId when details does not carry them as strings"
   ],
   "nervous/approval-edit-transport.test.ts": [
    "round-trips modifiedPayload through writeApproval → readPending",
    "preserves a nested/complex modifiedPayload verbatim",
    "omits the modifiedPayload key when absent (byte-compat with legacy writes)",
    "writes exactly one pending file carrying the edit",
    "merges modifiedPayload over the original on accepted (suggest policy)",
    "runs with the original payload when no edit is supplied (byte-identical path)",
    "treats an undefined modifiedPayload as no edit",
    "ignores modifiedPayload on a rejected decision (handler not called)",
    "preserves SAFETY_FLOOR gating and still merges the edit on accept",
    "applies an edit forwarded the way the poller hands a request to the executor"
   ],
   "nervous/approval-loop-death.test.ts": [
    "same finding re-proposed → same fingerprint and SAME shortCode, new id",
    "a materially different finding gets a different fingerprint",
    "the fingerprint is SPRINT-scoped — a new sprint is a new question (no stale-reject swallow)",
    "rejected → suppressed within the window, free after it",
    "executed → cool-down, then ESCALATE (repeatAfterAction) instead of re-ask",
    "config windows override the defaults",
    "corrupt memory file fails soft (no throw, no suppression)",
    "hasPendingFingerprint true while parked; reject writes decision-memory and unparks",
    "a successful execution stamps the fingerprint \"executed\"",
    "a rejected fingerprint is dropped before dispatch",
    "an already-parked fingerprint is not dispatched twice (pending-dedup)",
    "a post-cool-down re-fire is dispatched as an ESCALATION, not a verbatim re-ask",
    "the real 443-003→443-004 shape (sequenced same-file writers) is NOT a collision",
    "independent same-file writers ARE still a collision (guard not weakened)",
    "a partially-sequenced group (one independent member) stays a collision",
    "a transitive chain (a→b→c) counts as fully sequenced"
   ],
   "nervous/ask-brain-escalation.test.ts": [
    "is true for a rejected decision",
    "is true for a still-pending outcome regardless of decision",
    "is false for a settled accepted/success outcome",
    "is false for autonomous/success",
    "does not escalate below the default threshold (3)",
    "escalates exactly at the threshold — memory-store type:escalation + notify",
    "breaks the re-notify loop: a 4th call for the same id does not repeat the record/notify",
    "resets the counter once a suggestion settles (accepted) — needs the full threshold again",
    "is fail-safe when notifyFn throws — the memory-store record still lands and no error propagates",
    "tracks distinct notification ids independently",
    "supports a custom threshold override",
    "exposes the documented default threshold constant"
   ],
   "nervous/authority-matrix.test.ts": [
    "STRICT: low-risk action returns suggest-30m",
    "BALANCED: risk levels map correctly",
    "AUTOPILOT: high-risk action returns suggest-5m",
    "FULL_AUTO: normal high-risk action returns autonomous",
    "FULL_AUTO: KILL_LIVE_SPRINT forced to approve by safety floor",
    "user override takes precedence over matrix default",
    "safety floor cannot be overridden by user override",
    "resolvePolicy throws for unknown action ID",
    "reason strings reflect the resolution path used",
    "all 4 matrices have identical safetyFloor reference",
    "MATRIX_BY_MODE contains exactly 4 presets",
    "preset matrices are frozen and immutable",
    "returns true for safety floor actions",
    "returns false for non-safety-floor actions",
    "permissive default: no actor → permit, allow-all, role null",
    "permissive default: unknown role → permit (backward-safe)",
    "viewer with read-only capabilities → permit",
    "viewer requesting fs-write → SOFT warn (allowed, emits, not blocked) by default",
    "enforce_rbac flag ON: viewer requesting fs-write → HARD deny (blocked, no console.warn)",
    "engineer with dev capabilities → permit",
    "engineer requesting enterprise-admin capability (erp-write) → soft warn",
    "admin permits every capability",
    "known role with empty required capabilities → permit",
    "normalizeWorkerRole maps known roles (case-insensitive) and rejects others",
    "operator-permit: operator with execute-class capabilities → permit",
    "viewer-deny: viewer requesting execute-class capabilities → soft warn (blocked under enforceRbac)",
    "WORKER_ROLE_CAPABILITY_MAP is a nested hierarchy (admin ⊇ engineer ⊇ viewer)",
    "ENFORCE_RBAC_CONFIG_KEY is the enforce_rbac config flag name",
    "permissive default: no actor → allowed, no violations, not enforced",
    "denied under enforceRbac: viewer + fs-write + enforceRbac:true → blocked",
    "unknown role → permissive allow: allowed, no violations, not enforced"
   ],
   "nervous/bootstrap-activation.test.ts": [
    "enabled=true → observer kurulur (NervousObserver + start + pipeline ctors)",
    "disabled (enabled=false OR missing) → null (default-off respect, ADR-040 opt-in)",
    "dispose() temizler — observer.stop + executor.shutdown + idempotent",
    "pipeline wire — detection event triggers decide → propose → dispatch + handle",
    "custom actionHandler is forwarded to Executor (W2-1 injection point)"
   ],
   "nervous/bootstrap-w3-event-emit.test.ts": [
    "writes a NERVOUS_NOTIFICATION event carrying the exact accept command",
    "is fail-safe when no active sprint (no event, no throw)",
    "add() forwards to the base store AND emits a live event; remove() only forwards"
   ],
   "nervous/bootstrap.test.ts": [
    "returns null when nervous_system.enabled=false (default-off respect)",
    "returns null when nervous_system config is missing",
    "returns {observer, dispose} when enabled=true and starts observer",
    "dispose() stops observer and clears executor pending timers",
    "omitting actionHandler binds createActionHandler({ projectRoot }) — not a stub",
    "an explicitly injected handler wins over the default (test seam preserved)",
    "wires NervousIpcQueue.startPolling so MCP IPC approvals reach the executor",
    "dispose() stops the IPC polling",
    "add() persists a notification the CLI bridge reads; remove() drops it",
    "dispatched notification carries the detector title/message/detectorId, never \"unknown\""
   ],
   "nervous/dead-event-stream.test.ts": [
    "Test (a): event stream 10dk+ sessiz + aktif worker var → severity:critical alarm",
    "Test (b): event stream sessiz + aktif worker yok → alarm yok (normal idle)",
    "Test (c): yeni event yazıldı → sessizlik sayacı sıfırlanır, alarm yok",
    "Test (d): sprint kapandı (CLEANUP faz) → detector pasif, alarm yok"
   ],
   "nervous/decision-engine.test.ts": [
    "Balanced mode + low risk action -> policy=autonomous",
    "Strict mode + medium risk -> approve",
    "Full-auto + safety floor -> approve (VETO)",
    "User override application (config.actionOverrides)",
    "Multiple suggestedActions -> multiple DecisionOutputs",
    "Unknown action ID -> skipped (not thrown)",
    "Invalid authorityMode -> throws",
    "DecisionOutput.reason contains human-readable context",
    "risk-gate OFF (default) + HIGH-risk request -> no gating (backward-safe)",
    "risk-gate ON + HIGH-risk request (shell) -> decision parked (approve)",
    "risk-gate ON + HIGH-risk via capabilityTarget verb (mail.send) -> parked",
    "risk-gate ON + LOW-risk request (fs-read) -> unchanged (autonomous)",
    "risk-gate ON but no request passed -> no gating (backward-safe)",
    "risk-gate ON + HIGH-risk: parks normal decisions, preserves safety-floor",
    "Quiet hours 23:00 (config 22:00-08:00) -> shouldDelay true for info",
    "Quiet hours 23:00 + critical severity -> shouldDelay false (bypass)",
    "same-day range: 09:00-17:00, now=12:00 -> true",
    "same-day range: 09:00-17:00, now=18:00 -> false",
    "wrap-around: 22:00-08:00, now=03:00 -> true (after midnight)",
    "wrap-around: 22:00-08:00, now=10:00 -> false (after end)",
    "no quietHours config -> shouldDelay returns false",
    "emergency severity -> shouldDelay false even in quiet hours",
    "warning severity in quiet hours -> shouldDelay true"
   ],
   "nervous/detector-reach.test.ts": [
    "BUILD_FAILURE_INVESTIGATE reaches a real notification channel",
    "INVESTIGATE_STALL / FORCE_EVALUATE / KILL_WORKERS reach a real notification channel",
    "NOTIFICATION_BRIDGE_REPAIR reaches a real notification channel",
    "an id with no registry entry and no fallback entry is dropped, not synthesized"
   ],
   "nervous/detectors/agent-routing-anomaly.test.ts": [
    "positive: test-writer gets 9/10 tasks (90%) → ADR-041 warning (Sprint 147 replay)",
    "negative: balanced distribution (all ≤50%) → null",
    "edge: fewer than 5 tasks → null (anomaly detection skipped for small sprints)"
   ],
   "nervous/detectors/agent-routing-positive.test.ts": [
    "T1: 28 task Sprint 148 dağılımı → architect %43 borderline medium-severity alert",
    "T2: string; corrupt agent pattern → 0 detection (reform sonrası temiz data)",
    "T3: Anomaly threshold 0.40 → architect %43 sınırda, farklı threshold ile doğrulama",
    "T4: False positive test — architect %43 legitimate, warning ama blocker değil",
    "T5: Sprint 147 replay — aynı detector %95 → critical alert (regression test)"
   ],
   "nervous/detectors/agent-routing.test.ts": [
    "Test 1: .tasks/ dizininde hiç task yoksa null döndürür",
    "Test 2: tüm agent\\'lar geçerli ve anomaly yok → null döndürür",
    "Test 3: agent=\"string;\" olan 1 task → corrupt-agent + severity=critical (Sprint 146 canlı bug)",
    "Test 4: agent=\"a\" (çok kısa, min 2 karakter gerekli) → corrupt-agent tespit edilir",
    "Test 5: 14/17 task test-writer\\'a atanmış (%82) → anomaly warning (Sprint 145 replay)",
    "Test 6: Sprint 146 replay — 9/17 test-writer + 1 string; → 2 issue (critical)",
    "Test 7: EVALUATE fazı değil → null döndürür",
    "Test 8: corrupt-agent + anomaly mix → severity=\"critical\" (worst case)"
   ],
   "nervous/detectors/build-failure-recurrence.test.ts": [
    "positive: detects file failing in 2+ consecutive sprints",
    "negative: no failed tests in current sprint → null",
    "edge: wrong event type → null (only RETRO phase triggers)"
   ],
   "nervous/detectors/debt-trend-live.test.ts": [
    "Test 4: Sprint 145-147 gerçek debt verisi → avgDebtRate ~%40 hesaplanır, threshold aşıldı",
    "Test 5: Sprint 145-147 verisi → severity=warning, suggestedAction=DEBT_REPRIORITIZE içerir",
    "Test 6: Payload sprint ID\\'leri — sprint-145, sprint-146, sprint-147 listelenir"
   ],
   "nervous/detectors/debt-trend.test.ts": [
    "Test 1: sprint-lifecycle dışı kaynak → null döndürür",
    "Test 1b: SPRINT_RETRO_COMPLETE dışı event tipi → null döndürür",
    "Test 2: windowSize\\'dan az sprint kaydı → null döndürür (yetersiz veri)",
    "Test 3: ortalama debt rate < threshold → null döndürür",
    "Test 4: ortalama debt rate >= threshold → DEBT_REPRIORITIZE içeren DetectorResult döndürür",
    "Test 5: payload.sprints son sprint ID\\'lerini içerir"
   ],
   "nervous/detectors/directives-protection-stress.test.ts": [
    "1. should return null for PLAN phase (not protected)",
    "2. should return null for EXECUTE phase + normal full DIRECTIVES",
    "3. should emit emergency alert for EXECUTE + 463 byte content (Sprint 145 replay)",
    "4. should emit emergency alert for EXECUTE + template pattern match (>2KB content)",
    "5. should emit emergency alert for EXECUTE + DIRECTIVES deleted",
    "6. should emit emergency alert for FIX phase + template (FIX also protected)",
    "7. should include autoRestore:true + sprintId + phase in suggested action payload",
    "8. stress simulator overwrite → detector emergency alert → auto-restore → sprint continues"
   ],
   "nervous/detectors/directives-protection.test.ts": [
    "should return null for PLAN phase + DIRECTIVES change (not protected)",
    "should return null for EXECUTE phase + normal DIRECTIVES (full content)",
    "should emit emergency alert for EXECUTE phase + 463 byte template (Sprint 145 replay)",
    "should emit emergency alert for EXECUTE phase + template pattern match",
    "should emit emergency alert for EXECUTE phase + DIRECTIVES deleted",
    "should emit emergency alert for FIX phase + template (FIX also protected)",
    "should return null for RETRO phase + template (not protected, expected transition)",
    "should include autoRestore: true in suggested action payload"
   ],
   "nervous/detectors/notification-delivery-health.test.ts": [
    "positive: 8/10 notifications failed (80%) → critical alert",
    "negative: 1/10 notifications failed (10%) → null (below threshold)",
    "edge: NOTIFICATION_DELIVERY event with cumulative stats → detects bridge degradation",
    "positive: aged pending durable-outbox record → warning alert",
    "negative: same record acknowledged → no alert"
   ],
   "nervous/detectors/scope-collision-live.test.ts": [
    "Test 1: 28 Sprint-148 task temiz plan — scope collision tespit edilmez (0 collision)",
    "Test 2: Deliberate collision — 2 task detector-registry.ts yazar → 1 collision event üretilir",
    "Test 3: Collision payload — taskIds her iki task ID\\'sini ve çakışan dosya yolunu içerir"
   ],
   "nervous/detectors/scope-collision-rate.test.ts": [
    "positive: 15 collisions via SCOPE_COLLISION event → warning",
    "negative: 5 collisions (below threshold of 10) → null",
    "edge: EVALUATE phase with sprintCollisionCount → detects accumulated collisions"
   ],
   "nervous/detectors/scope-collision.test.ts": [
    "should return null when .tasks/ directory has no task files",
    "should return null when 1 task writes to 1 file (no collision possible)",
    "should detect collision when 2 tasks write to the same file",
    "should detect 2 collisions when 3 tasks have overlapping filesWrite (A+B share file1, A+C share file2)",
    "should return null for phases other than PLAN and EXECUTE (RETRO, CLEANUP, etc.)"
   ],
   "nervous/detectors/stale-worker-live.test.ts": [
    "should boot with 5 active detectors when all enabled in config",
    "should expose all 5 detector IDs when fully configured",
    "should have 4 active detectors when stale_worker disabled",
    "should continue running other detectors when one throws",
    "should have 0 active detectors with empty config",
    "should return results array from runAll without throwing"
   ],
   "nervous/detectors/stale-worker.test.ts": [
    "Test 1: aktif worker yoksa null döndürür",
    "Test 2: tüm worker\\'lar taze heartbeat gönderiyorsa null döndürür",
    "Test 3: 1 stale worker → 1 action içeren DetectorResult döndürür",
    "Test 4: 2 stale worker → 2 action, groupKey her iki ID\\'yi içerir",
    "Test 5: event-bus kaynağından gelen event → null döndürür (sadece cron/fs tetikler)",
    "Test 6: IDLE ve CLEANUP fazlarında null döndürür",
    "does not respawn frozen activity when host verdict is %s",
    "emits exactly once for one exact dead attempt",
    "does not treat a worker without host attempt authority as stale admission"
   ],
   "nervous/detectors/task-mode-idle.test.ts": [
    "Test 1: task mode + idle < 5min → null döndürür",
    "Test 2: task mode + idle > 5min → info notification döndürür",
    "Test 3: sprint mode + herhangi bir idle → null döndürür (detector skip)",
    "Test 4: cron dışı event (filesystem) → null döndürür",
    "Edge: lastUserActivity payload olmadığında null döndürür",
    "Edge: tam eşik değerinde (300_000ms) → null döndürür (eşik dahil değil)"
   ],
   "nervous/detectors/token-spike.test.ts": [
    "positive: cost exceeds threshold → warning alert",
    "negative: cost below threshold → null",
    "honest-unknown: an unknown model is NOT priced at a named default",
    "edge: non-RETRO event → null"
   ],
   "nervous/directives-protection-auto-restore.test.ts": [
    "multiple legitimate mid-sprint set_directives updates do NOT trigger rollback when auto_restore=true and baseline-update hook is invoked"
   ],
   "nervous/directives-protection-baseline.test.ts": [
    "set_directives success refreshes the baseline",
    "detects unauthorized change and restores when auto_restore=true",
    "CLI `deckent nervous baseline-refresh` updates baseline manually"
   ],
   "nervous/dispatcher.test.ts": [
    "should always dispatch to file channel",
    "should include mcp channel when DECKENT_MCP_ACTIVE is detected",
    "should include cli channel when TTY is available and MCP is off",
    "should broadcast to all enabled channels on critical severity",
    "should broadcast to all enabled channels on emergency severity",
    "should not re-dispatch a notification with the same ID",
    "should dispatch to file only when mcp and cli are disabled",
    "should fall back to CLI when MCP dispatch fails",
    "should not fallback to CLI if CLI is already in channels (critical)",
    "should pass full notification object to adapters",
    "should return only file when no context matches and channels are enabled",
    "should track dispatched count and support clearing dedup",
    "should not crash when an adapter throws"
   ],
   "nervous/executor-approve-timeout.test.ts": [
    "arms the timer for a non-safety-floor action with a positive timeout",
    "never arms for a safety-floor (locked) action — explicit approval required",
    "does not arm when auto-proceed is disabled (timeout <= 0 = never auto-proceed)"
   ],
   "nervous/executor-pending-store.test.ts": [
    "adds a parked approve-policy notification, removes it on resolve",
    "resolveApproval by shortCode purges the pending by FULL id — no re-notify (FIX-6)",
    "adds a parked suggest-timeout notification to the store",
    "is optional — no store wired still resolves cleanly"
   ],
   "nervous/executor.test.ts": [
    "should execute autonomous action immediately with success",
    "should capture error when autonomous handler throws",
    "should resolve suggest-5m with accepted when user accepts before timeout",
    "should auto-apply suggest-30m action when timeout expires",
    "should resolve suggest-5m with rejected when user rejects",
    "should await indefinitely for SAFETY_FLOOR approve policy until user resolves",
    "should not call handler when approve policy is rejected",
    "resolveApproval resolves by shortCode, not only the full id (FIX-2)",
    "resolveApproval returns true on consume, false when nothing matches (FIX-1)",
    "should handle multiple actions in a single notification sequentially",
    "should clear timers and reject pending approvals on shutdown",
    "should call history.append for every execution record",
    "should set reversible flag from ActionDefinition lookup",
    "should propagate action payload to handler correctly"
   ],
   "nervous/f10-002-risk-gate.test.ts": [
    "shell.exec → high (exec verb)",
    "erp.write → high (write verb)",
    "db.write → high (write verb)",
    "mail.send → high (send verb)",
    "fs.delete → high (delete verb)",
    "shell capability → high",
    "erp-write capability → high",
    "db-write capability → high",
    "fs-read capability → low (safe read)",
    "calendar.list → low (list is not a write/send/delete/exec verb)",
    "flag ON + shell.exec HIGH-risk verb → decision parked (policy=approve)",
    "flag ON + db.write HIGH-risk verb → parked",
    "flag ON + mail.send HIGH-risk verb → parked",
    "flag ON + shell capability HIGH-risk → parked",
    "flag OFF (default) + HIGH-risk shell.exec → mevcut davranış (no gating)",
    "flag ON + LOW-risk fs-read → no gating (unchanged)",
    "flag ON + no request passed → mevcut davranış (backward-safe)",
    "flag ON + HIGH-risk: preserves safety-floor untouched"
   ],
   "nervous/gate-w2-lethal.test.ts": [
    "DESTRUCTIVE_GIT → blocked=true, reason includes actionId, warning emitted",
    "all 5 SAFETY_FLOOR actions → blocked=true",
    "non-lethal action WORKER_RESPAWN → blocked=false, reason empty, no warning",
    "empty actionId → blocked=false (not in SAFETY_FLOOR)",
    "unknown actionId → blocked=false",
    "blocked result reason always contains required guard markers",
    "does not call warn for non-lethal action",
    "calls warn with informative message for lethal action",
    "DESTRUCTIVE_GIT actionId → throws SpawnBackendError before process spawn",
    "thrown SpawnBackendError contains SAFETY_FLOOR message for lethal action",
    "ADR_DEPRECATE_ACCEPTED actionId → blocked",
    "no actionId → guard does not block (may fail for other reasons, not guard)",
    "non-lethal actionId WORKER_RESPAWN → guard does not block",
    "DESTRUCTIVE_GIT actionId → throws SpawnBackendError before ensureSession",
    "MANUAL_FILE_DELETE → SpawnBackendError with backendName=tmux",
    "no actionId → guard does not block (may fail for other reasons, not guard)",
    "DESTRUCTIVE_GIT actionId → throws SpawnBackendError before any spawn",
    "KILL_LIVE_SPRINT → SpawnBackendError with backendName=docker + SAFETY_FLOOR",
    "guard outcome depends only on SAFETY_FLOOR membership — not on any nervous toggle",
    "non-lethal action proceeds freely regardless of nervous state"
   ],
   "nervous/history.test.ts": [
    "should append single record as one JSONL line",
    "should append 3 records and readAll returns all 3 in order",
    "should findById existing record",
    "should return null for findById with nonexistent id",
    "should findRecentReversible — only reversible+success, limit, newest first",
    "should markUndone by appending compensation record with originalId ref",
    "should prune records older than N days and return pruned count",
    "should handle concurrent append — both records present"
   ],
   "nervous/integration/detector-to-decision.test.ts": [
    "should resolve low-risk action to autonomous in balanced mode",
    "should resolve medium-risk action to suggest-30m in balanced mode",
    "should resolve high-risk action to approve in balanced mode",
    "should enforce approve for safety floor action even in full-auto mode",
    "should enforce approve for DESTRUCTIVE_GIT in autopilot mode",
    "should apply user override over default risk policy",
    "should not allow user override to bypass safety floor",
    "should produce multiple DecisionOutputs for multiple suggested actions",
    "should skip unknown action IDs without throwing",
    "should throw for invalid authority mode"
   ],
   "nervous/integration/dispatcher-end-to-end.test.ts": [
    "should dispatch critical notification to all enabled channels",
    "should dispatch info notification only to file and context-detected channel",
    "should deduplicate same notification ID dispatched twice",
    "should fallback to CLI when MCP adapter fails",
    "should dispatch to MCP when DECKENT_MCP_ACTIVE context detected",
    "should always include file channel even when all other adapters fail"
   ],
   "nervous/integration/observer-to-detector.test.ts": [
    "should detect stale worker on cron tick with outdated heartbeat",
    "should return null when all workers are fresh",
    "should not trigger on event-bus events (only cron/fs)",
    "should detect template reversion on filesystem event during EXECUTE",
    "should not trigger in PLAN phase",
    "should not trigger for non-DIRECTIVES filesystem events",
    "should detect corrupt agent ID on EVALUATE phase change",
    "should not trigger on non-EVALUATE phase changes",
    "should detect scope collisions during PLAN phase",
    "should not trigger in RETRO phase"
   ],
   "nervous/integration/proposer-to-executor.test.ts": [
    "should produce notification from detector result and execute autonomous action",
    "should handle suggest-30m timeout auto-apply when no user decision",
    "should handle user acceptance before timeout expires",
    "should handle user rejection of suggest-timeout action",
    "should handle handler failure gracefully",
    "should throttle duplicate notifications via proposer",
    "should not produce notification when shouldNotify is false",
    "should propagate payload through full pipeline",
    "should handle executor shutdown by rejecting pending approvals",
    "should include timeout in notification for suggest-5m policy"
   ],
   "nervous/integration/regression-sprint-146.test.ts": [
    "should detect Sprint 146 T-146-005 agent string; corruption pattern",
    "should replay Sprint 145 08:14 TRT DIRECTIVES.md template reversion",
    "should produce correct notification and decision for emergency directives scenario",
    "should correctly mock and restore fs modules without leaking between tests",
    "should handle multiple detectors running on same event without interference"
   ],
   "nervous/integration-runtime.test.ts": [
    "cron tick → stale-worker → dispatcher (file) + executor (history)"
   ],
   "nervous/ipc-queue.test.ts": [
    "writeApproval writes a JSON request file to pending dir",
    "readPending returns all pending approval requests",
    "markResolved relocates the pending file to resolved dir",
    "handles concurrent writeApproval calls without losing data",
    "MCP nervous_accept skips IPC write when nervous_system disabled",
    "startPolling drives handler and moves processed files; dispose stops the loop",
    "leaves the pending file when the handler returns false (not consumed)",
    "ages out an orphaned (older-than-TTL) unconsumed approval"
   ],
   "nervous/live-w1b-adaptive.test.ts": [
    "10-file scope → threshold ~%20 artar (0 dirs)",
    "0-scope → base threshold (no change)",
    "cap 2×base when files+dirs would exceed",
    "cap 2×base at exactly 50-file boundary (0 dirs)",
    "dirs contribute to threshold increase",
    "combined files and dirs scale correctly",
    "task with 10 files → adaptive threshold prevents false-stale-kill",
    "task with 10 files → still stale when elapsed exceeds adaptive threshold",
    "0-scope (task file missing) → falls back to base threshold",
    "0-scope (empty filesWrite) → base threshold is used"
   ],
   "nervous/maintenance-ops.test.ts": [
    "archives sprint logs beyond the keep-count, newest retained",
    "is a no-op (0) when at or under the keep-count, and when dir absent",
    "clears managed-docs cache entries for cacheType 'all' / 'docs' (preserves the file)",
    "leaves the cache untouched for a non-doc cacheType (no such persisted cache)",
    "removes IPC files older than the cutoff, keeps fresh ones",
    "returns 0 when no IPC dirs exist",
    "drops corrupt lines and rewrites, keeping valid JSON events",
    "returns 0 for an absent or already-clean stream",
    "appends a snapshot and writes a markdown trend report",
    "records openCount 0 when debt export says none / is absent",
    "accumulates multiple snapshots (a real time series)"
   ],
   "nervous/nerv-w1-predicate.test.ts": [
    "returns ok=true when payload has no collisions",
    "returns ok=true and auto-serialize-OK reason for a simple 2-task single-file collision",
    "returns ok=false when a task appears in multiple collision groups (circular heuristic)",
    "calls canAutoApply predicate and logs result when predicate returns ok=true, then auto-proceeds",
    "vetoes auto-apply and keeps action pending when predicate returns ok=false",
    "falls back to matrix-timeout auto-proceed when no canAutoApplyMap is provided (backward compat)"
   ],
   "nervous/nerv-w1b.test.ts": [
    "returns ok=true for single stale worker (tek-stale)",
    "returns ok=true for 2 stale workers (below cascade threshold)",
    "returns ok=false for 3+ stale workers (cascade veto)",
    "returns ok=false for 5 stale workers",
    "returns ok=true when staleCount is absent (defaults to 1)",
    "returns ok=false for DIRECTIVES_WRITE during EXECUTE phase",
    "returns ok=true for DIRECTIVES_WRITE during FIX phase",
    "returns ok=true for other phases (PLAN, RETRO, etc.)",
    "includes staleCount=1 in single stale worker action payload",
    "includes staleCount=3 in each action when 3 workers are stale",
    "auto-proceeds WORKER_RESPAWN for single stale worker and logs canAutoApply",
    "vetoes WORKER_RESPAWN auto-apply for cascade (staleCount>=3) and keeps pending",
    "vetoes DIRECTIVES_WRITE auto-apply during EXECUTE phase",
    "auto-proceeds DIRECTIVES_WRITE during FIX phase and logs canAutoApply"
   ],
   "nervous/nervous-apr-wire.test.ts": [
    "routes to legacyResolve unchanged and never touches the bridge",
    "is the default expectation when approvalBridgeEnabled is false for a reject too",
    "accept -> broker.decide(allow) + pending-store cleanup runs",
    "reject -> broker.decide(deny) with reason forwarded + pending-store cleanup runs",
    "an already-decided id is surfaced as applied:false, not thrown, and cleanup still runs",
    "defaults to false when nervous_system config is undefined",
    "defaults to false when approval_bridge is absent",
    "is true only when approval_bridge is explicitly true",
    "auto-proceeds (timeout-auto-applied) for non-SAFETY_FLOOR after 50ms",
    "action handler IS called when timeout auto-proceeds",
    "SAFETY_FLOOR action does NOT auto-proceed after timeout — still awaiting human",
    "early accept resolves immediately before timeout fires",
    "early reject resolves immediately, action handler NOT called",
    "result is appended to history after auto-proceed",
    "after early-accept, subsequent timeout event does not re-execute action"
   ],
   "nervous/observer-phase-guard.test.ts": [
    "Case 1 — PLAN phase: detector cycle does NOT run even with 17 FS events",
    "Case 2 — EXECUTE phase: detector cycle runs (single FS event → 1 detector call)",
    "N1 — IDLE phase + activeInAnyPhase=true: detector cycle RUNS (autonomous)",
    "N1 — IDLE phase + activeInAnyPhase=false (default): detector cycle does NOT run (sprint guard preserved)",
    "Case 3 — Debounce batch: 17 FS events within 500ms window → 1 detector cycle",
    "Case 4 — IDLE phase: cron tick does NOT trigger detector cycle"
   ],
   "nervous/observer-resource.test.ts": [
    "Case 1 — scan-interval honours configured cronIntervalMs",
    "Case 2 — idle-throttle: IDLE phase multiplies cron interval",
    "Case 3 — lazy-detector: no DetectorConfig → registry not constructed, runAll never called",
    "Case 4 — active detector runs in EXECUTE phase after debounce"
   ],
   "nervous/observer.test.ts": [
    "should set isStarted=true on start(), 2nd call is no-op",
    "should clear all watchers and timer on stop()",
    "should emit observe event with source=event-bus when eventBus publishes",
    "should emit observe event with source=filesystem on FS change",
    "should emit observe event with source=cron within interval",
    "should emit valid ObserverEvent with UUID and ISO timestamp from all sources",
    "should extract sprintId and taskId from payload",
    "should be idempotent — multiple start() calls do not duplicate subscriptions",
    "should continue when FS watch target does not exist",
    "should not receive events after stop()",
    "should tag sprint lifecycle events with source=sprint-lifecycle"
   ],
   "nervous/panic-gate-failclosed.test.ts": [
    "dashboard reject marker shape (rejectedVia, no decision field) resolves REJECTED — regression for the fail-OPEN bug",
    "ambiguous marker ({}) resolves REJECTED — fail-closed default, not silent approval",
    "unrecognized decision value resolves REJECTED — fail-closed default",
    "explicit decision: \"reject\" resolves REJECTED — legitimate reject path intact",
    "acceptPanicGuard-shaped marker (acceptedAt/acceptedBy, no decision field) resolves APPROVED — legitimate open path must not break",
    "explicit decision: \"approve\" resolves APPROVED — legitimate open path intact",
    "SAFETY_FLOOR locked action with an ambiguous marker still resolves REJECTED"
   ],
   "nervous/panic-gate-nonblocking.test.ts": [
    "advisory mode returns PROCEED immediately for non-locked action (spawn does not wait)",
    "safety_floor action rejected synchronously — advisory does NOT bypass locked actions",
    "TIMEOUT_AUTO_PROCEED when no marker arrives — never silent infinite wait",
    "safety_floor locked action does NOT auto-proceed on timeout — resolves REJECTED",
    "resolved marker present → APPROVED before deadline",
    "marker-yok (no marker) → polling proceeds without crashing, eventually auto-proceeds"
   ],
   "nervous/proposer.test.ts": [
    "should return null when shouldNotify is false",
    "should return null when severity is below severityMin filter",
    "should throttle same groupKey within 5 minutes, allow after",
    "should bypass severity filter and throttle for critical/emergency",
    "should include multiple actions from multiple decisions",
    "mints a short, deterministic approval code (phone-typeable, derived from the FINDING fingerprint)",
    "should compute timeoutMs as smallest suggest-* timeout",
    "should return null timeoutMs when only approve policies exist",
    "should propagate payload from detectorResult.suggestedActions to notification.actions",
    "should return null for empty decisions",
    "should return null for autonomous-only decisions",
    "should return 300000 for suggest-5m",
    "should return 1800000 for suggest-30m",
    "should return smallest when both suggest present"
   ],
   "nervous/recommendation-log.test.ts": [
    "appends a recommendation as one JSONL line and stamps id + createdAt",
    "creates the .deckent directory when absent",
    "appends multiple recommendations (append-only, newest last)",
    "readRecommendations returns [] when the feed is absent",
    "deduplicates an open operational recommendation by action and key",
    "dismissRecommendation flips an open entry to dismissed (by full id)",
    "dismissRecommendation matches a unique rec- prefix and leaves others open",
    "dismissRecommendation returns false when no open match exists",
    "readRecommendations skips malformed lines without throwing"
   ],
   "nervous/recovery-notification.test.ts": [
    "deduplicates the same decision binding and stores only evidence digests",
    "binds acceptance to exact identity and invokes the shared service once",
    "records rejection without invoking a recovery effect",
    "rejects stale approval replay against a different generation"
   ],
   "nervous/respawn-request.test.ts": [
    "requestWorkerRespawn appends + drain returns the taskId then clears the file",
    "dedupes repeated requests for the same task",
    "drain on a fresh project returns []",
    "skips corrupt lines without throwing"
   ],
   "nervous/runtime-scope.test.ts": [
    "should allow instantiation in Brain context (no DECKENT_WORKER_MODE)",
    "should throw NervousScopeViolationError in worker context",
    "should include ADR-037 and Brain-scoped in error message",
    "should emit violation event on the real eventBus deckent-event channel",
    "should enforce scope on NervousObserver constructor",
    "should not throw when DECKENT_WORKER_MODE is not \"1\""
   ],
   "nervous/scope-collision-reorder-executor.test.ts": [
    "accepted + fresh identity → handler called once with the fenced effect, not the raw payload",
    "accepted + autonomous policy also fences (funnel applies uniformly)",
    "rejected → handler never called, non-mutating, still an auditable record",
    "resolveApproval on an unmatched id (\"ignore\") is non-mutating and does not disturb the real pending approval",
    "accepted + malformed payload (no collisions) → handler never called, non-mutating, auditable failure record",
    "accepted + single-task collision (no longer an actual collision) → handler never called",
    "non-fenced action ids are unaffected — payload passes through unchanged"
   ],
   "nervous/stale-worker-activity-truth.test.ts": [
    "%s host verdict never proposes respawn despite an old activity timestamp",
    "a dead exact attempt proposes respawn",
    "one host observation notifies exactly once",
    "a later host sequence is a new episode and notifies again"
   ]
  },
  "orchestra": {
   "orchestra/acceptance-all-branches-wire.test.ts": [
    "%s is a typed service branch",
    "owns rubric, runtime-budget authority, enforcement and confirmation in one preparation boundary",
    "publishes the final repair-authorized verdict and audit receipt before returning",
    "FIX dependency ingress waits for the service result and cache misses HOLD instead of re-evaluating",
    "controller-only result discovery settles through the same authority before handoff publication",
    "every written cell key carries a real vocabulary domain id (D1 catcher)",
    "is idempotent per (taskId, sprintId) — a re-finalize cannot double-count",
    "skips infra-classified failures entirely — no penalty, no reward, visible counter"
   ],
   "orchestra/acceptance-all-surface-closure.integration.test.ts": [
    "links producer, federation, authenticated decision, debt CAS, receipts, audit, and restart replay",
    "does not leak or mutate across a colliding foreign-tenant route"
   ],
   "orchestra/acceptance-authority-restart.integration.test.ts": [
    "restarts after a bound %s decision and durably reaches PREPARED, debt CAS, APPLIED and audit",
    "returns typed HOLD for a corrupt authority binding and does not leak across project authority"
   ],
   "orchestra/acceptance-confirmation-composition.test.ts": [
    "settles only through the production decideAndSettle adapter with broker MAC authority",
    "refuses a raw store settlement whose receipt has no broker MAC authority",
    "owns real stores and closes deterministically",
    "fails closed for foreign tenant before debt mutation"
   ],
   "orchestra/acceptance-confirmation-reconciler.test.ts": [
    "drains and replay-skips a bounded keyset through the default production composition",
    "drains terminal candidates once and coalesces receipt projections across restart replay",
    "keeps non-terminal, corrupt, foreign, and applied observations typed and effect-free",
    "uses a bounded tenant keyset and rejects cursor regression before effects",
    "rejects unbounded and oversized pages before mutation",
    "propagates a typed HOLD without uncertain closure"
   ],
   "orchestra/acceptance-confirmation-service.test.ts": [
    "writes confirmation before provisional debt and remains typed HOLD",
    "recovers partial create from the durable confirmation",
    "fresh-reads and orders canonical PREPARED, debt CAS, canonical APPLIED",
    "returns typed HOLD/DENIED without CAS when terminal authority is absent or invalid",
    "replays a crash after PREPARED and never reports DONE before APPLIED",
    "does not trust a persisted APPLIED receipt after fresh terminal authority disappears",
    "rejects changed fresh terminal bytes against canonical PREPARED FWW"
   ],
   "orchestra/acceptance-controller-settlement.test.ts": [
    "consumes one immutable attempt receipt before dependency release",
    "replays the immutable exact-attempt verdict without recomputing it after restart",
    "holds a structurally corrupt receipt instead of projecting its result prose",
    "holds a raw DONE when its settlement receipt is missing",
    "holds a receipt whose immutable verdict conflicts with the phase projection",
    "scopes conflicts to the exact attempt so an older receipt cannot block recovery"
   ],
   "orchestra/acceptance-enforcement-canary.test.ts": [
    "emits the canonical full lineage without consumer-minted identity",
    "%s cannot mint lineage"
   ],
   "orchestra/acceptance-enforcement.test.ts": [
    "observe mode (default) never changes the evaluation",
    "enforce REJECT caps at NO_GO with a salvage-proof row and a B1 cause",
    "enforce ROUTE downgrades DONE, carries statements + author, never fires on NO_GO",
    "does not create or apply a ROUTE intent without complete explicit authority",
    "derives identical route claims from identical canonical inputs",
    "binds producer inputs to exact sprint/task/attempt/generation authority",
    "is idempotent by deterministic id and settles single-shot"
   ],
   "orchestra/acceptance-main-enforce-canary.integration.test.ts": [
    "runs evaluator intent through production confirmation→debt and audits the exact HOLD receipt",
    "keeps baseline when only confirmation is durable and returns the exact debt HOLD reference"
   ],
   "orchestra/acceptance-secondary-branches.integration.test.ts": [
    "pins the five secondary branches to the same typed service contract",
    "one preparation service owns rubric/cache, enforcement, and the durable receipt digest",
    "completion preserves exact attempt identity before publishing dependency-visible state",
    "has no secondary evaluator or cache-miss fallback"
   ],
   "orchestra/adaptive-agent-outcome-wire.test.ts": [
    "triggers adaptAgentRuntime: recordOutcome attaches skillAdaptation metadata to persisted outcome",
    "successful agent: no skill changes suggested (suggestAdd is empty)",
    "failing agent (low coverage GO_WITH_TECH_DEBT history): suggests testing-expert skill",
    "idempotent: same history produces the same skillAdaptation across separate trackers",
    "generic agent: no skillAdaptation attached (advisory short-circuit)",
    "drift tespit: drifted agent returns driftScore > 0 and non-keep recommendation",
    "drift yok: aligned agent returns low driftScore and keep recommendation",
    "çoklu agent: returns one DriftReport per input agent",
    "boş veri: empty input returns empty array (graceful)",
    "agent with no recentResults returns driftScore 0 and keep",
    "formats drift reports as markdown section with heading",
    "renders empty-state message when no drift data provided",
    "end-to-end: collect → build produces a retro-ready markdown section"
   ],
   "orchestra/adr-pointer-path.test.ts": [
    "resolves an id to its real docs/adr/*.md file by id-prefix",
    "is case-insensitive on the id-prefix match",
    "fails soft to null when no file matches the id-prefix",
    "fails soft to null when the directory does not exist",
    "does not prefix-collide across zero-padded ids (adr-g-006 vs adr-g-0060)",
    "resolves the constraint-tier \"background constraint\" pointer to the fixture docs/adr path when adrDocsDir is supplied",
    "falls back to the legacy .brain/memory.db pointer when the fixture docs/adr tree has no matching file",
    "keeps byte-identical legacy pointer text when adrDocsDir is omitted (default, zero fs access)",
    "resolves the scope-gated \"[full: …]\" condensed pointer too"
   ],
   "orchestra/adr-preset.test.ts": [
    "exports preset matrix with expected task types",
    "core-dev preset ADRs appear in top-4 results",
    "docs preset ADRs appear in top-3 results",
    "preset bonus adds +0.3 to combined score",
    "skill block absent when no skills provided",
    "skill block present when skills provided",
    "dependencies section absent when task has no dependencies"
   ],
   "orchestra/adr-render.test.ts": [
    "does not emit **Status:** accepted in the outer header",
    "still emits the ## adrId: title outer header",
    "preserves full content body after dedupe",
    "emits only the operative section when markers are present (adrRender=operative)",
    "appends [full text: .brain/memory.db adr-NNN] footnote in operative mode",
    "falls back to full content when no operative markers present (adrRender=operative)",
    "handles mixed ADRs: operative extraction where markers exist, full elsewhere",
    "does not apply operative extraction when adrRender=full (default)",
    "DEFAULT_PROMPT_CONFIG.adr_render === 'full'",
    "validateConfig accepts prompt.adr_render = 'operative'",
    "validateConfig rejects prompt.adr_render = 'invalid-value'",
    "governing ADR WITH markers renders the pinned slice + full-text pointer, not the full body",
    "governing ADR WITHOUT markers keeps the FULL body (zero-loss default preserved)",
    "adrRender='full' ignores markers even for governing ADRs (legacy path unchanged)"
   ],
   "orchestra/adr-selector-explicit-ref.test.ts": [
    "extracts standard ADR-NNN references",
    "is case-insensitive (adr-012 and ADR-012 both match)",
    "handles ADR without dash (ADR012)",
    "deduplicates repeated references",
    "returns empty array when no ADR refs present",
    "extracts multiple distinct references",
    "forces adr-012 into result when description explicitly mentions ADR-012 (271-004 regression)",
    "places explicitly referenced ADR at the front of results",
    "marks forced ADR with 'explicit-ref' in matchReasons",
    "silently ignores reference to non-existent ADR-999",
    "forces multiple explicitly referenced ADRs into results",
    "deduplicates: explicit ref appears only once even if scoring would include it",
    "returns all explicit refs even when their count exceeds topN",
    "behaves identically to original scoring when no explicit refs present",
    "forces ADR when referenced in lowercase (adr-012)"
   ],
   "orchestra/adr-selector-taxonomy.test.ts": [
    "extracts a new-format class-lettered ref (ADR-G-025)",
    "extracts a lowercase dashed ref (adr-d-002) and pads the number",
    "still extracts a legacy classless ref (ADR-025) for back-compat",
    "extracts multiple distinct refs, deduplicated",
    "returns [] when there is no ref",
    "returns the Enforcement `today=` clause, NOT the `**Class:**` metadata",
    "prefers an explicit summary when provided",
    "falls back to the ## Decision line when there is no Enforcement header",
    "returns the `## Decision (Today)` body (through the next ## header)",
    "still honours explicit operative-marker comments when present",
    "returns null when neither markers nor a Decision section exist"
   ],
   "orchestra/adr-selector.test.ts": [
    "selects relevant ADRs for core-dev task (ADR-008, ADR-015, ADR-023)",
    "selects relevant ADRs for docs task (ADR-029, ADR-030, ADR-032)",
    "selects relevant ADRs for CLI task (ADR-010, ADR-011, ADR-022-v2)",
    "assigns scope-path-match score correctly",
    "assigns keyword-match score correctly",
    "F1.3: broad ADR overlapping only on generic words does not keyword-match",
    "applies age penalty to older ADRs",
    "enforces topN cap of 3",
    "returns empty array for empty input",
    "WP-15: selects the test-hermeticity ADR-087 for a hermetic test task",
    "ADR-TAXONOMY: test preset includes the hermeticity ADR (adr-d-002, absorbed old adr-087)",
    "ADR-TAXONOMY: security preset prefers isolation adr-g-017 (old adr-034), never Dead-Code",
    "WP-20: prepends a one-line Active constraint above the full, contiguous ADR body",
    "WP-20: prefers an explicit entry.summary for the Active constraint when present",
    "buildAdrPromptSection full mode includes full ADR content",
    "buildAdrPromptSection summary mode returns concise summaries",
    "classifies CLI task correctly",
    "classifies docs task correctly",
    "classifies orchestra task correctly",
    "never slices a word in half when the summary exceeds the cap",
    "returns the summary unchanged when within the cap"
   ],
   "orchestra/agent-exclusion-dynamic.test.ts": [
    "documentation intent → exclude migration-specialist, devops-engineer, security-auditor",
    "src/orchestra/ scope → exclude frontend-designer, accessibility-auditor",
    "src/cli/ scope → exclude frontend-designer, accessibility-auditor, migration-specialist",
    "src/dashboard/ scope → exclude data-engineer, migration-specialist",
    "security intent → no exclusions",
    "documentation intent + src/cli/ scope → merged unique exclusions",
    "previously hard-coded agents are NOT globally excluded",
    "Sprint 146 scenario: different tasks get different exclusions"
   ],
   "orchestra/agent-genealogy-wire.test.ts": [
    "promote() records lineage in genealogy on successful agent promotion",
    "demote() removes agent from genealogy on successful demotion",
    "promotes two agents and calls registerAgent for each (parent chain)",
    "promote() for skill entity does NOT call genealogy registerAgent"
   ],
   "orchestra/agent-prompt-single-source.test.ts": [
    "1. PROMPT.md is canonical — full content returned verbatim with source=prompt-md",
    "2. No concatenation — agent.json::systemPrompt MUST NOT appear when PROMPT.md exists",
    "3. Fallback — PROMPT.md missing → returns systemPrompt with degraded=true",
    "4. 15-agent loop — every built-in agent yields a non-empty PROMPT.md result",
    "5. Generic / unknown agents return source=none",
    "agent.json::systemPrompt field is preserved on disk (used by routing scoring + UI)"
   ],
   "orchestra/agent-retirement-wire.test.ts": [
    "demote() calls retire() for low-success agent qualifying for retirement",
    "demote() does NOT call retire() when agent does not qualify for retirement",
    "retire() receives reason string with demotion context and original reasons",
    "demote() is idempotent — returns false without crash when manifest is missing"
   ],
   "orchestra/agent-stats-update.test.ts": [
    "updateAgentStats updates generic agent totalUses",
    "updateAgentStats records lastUsedInSprint for generic",
    "updateAgentStats calculates successRate for generic agent",
    "agent.json is updated on disk after updateAgentStats",
    "stats accumulate over multiple updates",
    "NO_GO decreases success rate",
    "does not throw for non-existent agent",
    "sprint log tasks section uses table format with Agent column",
    "defaults to generic when assignedAgent is undefined",
    "shows specific agent name in table",
    "shows assigned skills in Skills column",
    "shows dash when no skills assigned",
    "shows dash when assignedSkills is undefined",
    "shows single skill without comma"
   ],
   "orchestra/ai-planner-honest-fallback.test.ts": [
    "mode=ai + reason=spawn_failed → throws BrainError naming provider + reason + detailed message",
    "mode=auto + reason=parse_failed → structured fallback succeeds (honest fallback via notify, not console.error)",
    "mode=auto + reason=%s → fails loud instead of minting a structured planning authority",
    "mode=ai + reason=no_providers → throws BrainError with reason=no_providers (registry empty)",
    "mode=ai + ok=true (success path) → uses data.tasks, planningMode=ai, no warning emitted",
    "gives each dry-run a durable but execution-distinct receipt identity",
    "subscription-spawn: callBrainPlanner receives the brain_provider-resolved adapter (no hardcoded default)",
    "uses grouped providers.brain as adapter, proof, and receipt requested authority",
    "keeps an absent configured primary unresolved and never substitutes registry default",
    "mode=auto + reason=timeout → structured fallback succeeds (honest fallback via notify, not console.error)"
   ],
   "orchestra/allowlist-flag-wire.test.ts": [
    "renders byte-identical legacy output (no Tool Surface block) when no config file exists",
    "fails soft without tier guidance when the task model is absent from the registry",
    "keeps tier guidance enabled for a canonical economy model",
    "stays off (no Tool Surface block) when the config file has no tools block",
    "stays off (no Tool Surface block) when allowlist_enabled is explicitly false",
    "fails soft to off (no throw, no block) when the config file is malformed JSON",
    "renders a narrowed Tool Surface block when allowlist_enabled is true",
    "omits the edit group (no Write) when the task declares no writable paths, even flag-on",
    "defaults taskType to generic (never throws) when task.type is undefined, flag-on",
    "returns \"\" when the allowlist is absent (flag-off default)",
    "returns \"\" when the allowlist grants no tools (never a stranded empty header)",
    "renders the narrowed surface: header, granted tool names, of-N count, escape hatch",
    "a code task actually narrows the surface (edit+execute+base, no web/connector/mcp)",
    "the compiled prompt carries no tool-allowlist block when ctx.toolAllowlist is absent",
    "no segment is tagged with the tool-allowlist kind when the flag is off",
    "injects the tool-allowlist block listing the granted tools",
    "emits exactly one tool-allowlist segment, tagged T2 per the classifyTier SSOT",
    "removing the tool-allowlist segment from the flag-on prompt reproduces the flag-off prompt"
   ],
   "orchestra/approval-decision-federation-acceptance.test.ts": [
    "preserves mirror lineage and settles only through PREPARED -> debt CAS -> APPLIED",
    "returns canonical non-null HOLD references for lineage mismatch and expiry",
    "retries after PREPARED and gives a changed late decision no effect"
   ],
   "orchestra/approval-decision-federation-timeout.test.ts": [
    "mirrors from the original source clock, preserves shorter expiry, and rejects tenant/id collisions",
    "parks a confirmation as system UNDECIDABLE and publishes a restart-idempotent ACK",
    "settles autonomous timeout without replay and preserves a prior human FWW winner",
    "expires pairing authority without granting and retries unacked durable receipts after restart",
    "rejects a forged critical pairing receipt that asks for permissive timeout"
   ],
   "orchestra/approval-decision-federation.test.ts": [
    "gates origins: lifecycle decision federation includes pairing; panic/bot stay out",
    "settles a nervous decision through the SAME IPC queue the executor polls",
    "settles an autonomous trigger through the gate authority (forged id fail-closed)",
    "refuses raw confirmation settlement without canonical broker authority",
    "flips a pending checkpoint file in place; deny rejects; guards are typed"
   ],
   "orchestra/archive-directives-default-preserve.test.ts": [
    "preserves DIRECTIVES.md by default (auto_archive_directives=false)",
    "overwrites DIRECTIVES.md when autoArchive=true (opt-in legacy)",
    "skips silently when DIRECTIVES.md does not exist",
    "phase guard still rejects non-CLEANUP/COMPLETE phases (default preserve)"
   ],
   "orchestra/archive-directives-phase.test.ts": [
    "should REJECT archiving when called during PLAN phase",
    "should REJECT archiving when called during EXECUTE phase",
    "should REJECT archiving when called during FIX phase",
    "should REJECT archiving when called during RETRO phase",
    "should ALLOW archiving when called during CLEANUP phase",
    "should restore DIRECTIVES.md from task JSON files when current is a placeholder",
    "should simulate Sprint 145 08:14 TRT scenario: EXECUTE phase call is rejected"
   ],
   "orchestra/archive-directives.test.ts": [
    "should archive DIRECTIVES.md and write placeholder (happy path)",
    "should write placeholder with correct next sprint number and references",
    "should skip archiving when DIRECTIVES.md does not exist",
    "should delegate canonical sprint docs publication to the archive authority",
    "should handle sprint IDs with leading zeros correctly"
   ],
   "orchestra/archive-finalizer-normal-seal.integration.test.ts": [
    "seals the exact completed marker, refreshes Brain parity, and retires the journal counter",
    "replays an older applied seal after a later Brain refresh and rejects conflict or tampering"
   ],
   "orchestra/archive-finalizer-recovery-seal.integration.test.ts": [
    "seals recovery completion as the last durable event and refreshes the Brain archive projection",
    "holds a mismatched receipt before emitting any false recovery COMPLETE marker"
   ],
   "orchestra/archive-path-authority.test.ts": [
    "defaults to the canonical deckent sprint namespace",
    "resolves from the sprint_file_retention config family when archive_path is set",
    "ignores a blank/absent archive_path instead of resolving to the project root",
    "is the same destination the recover path reports — no second resolver",
    "archives terminal artifacts into the resolved destination and leaves zero residue",
    "preserves non-terminal artifacts INSIDE the canonical location with a typed marker",
    "moves rather than removes — every seeded artifact still exists afterwards",
    "is idempotent — a second settle finds nothing left and destroys nothing",
    "lands recovered artifacts in the same directory the settle path uses",
    "consolidates a legacy tasks-local archive into the canonical destination",
    "never clobbers existing archived evidence with a same-named artifact",
    "settle and recover resolve byte-identically under a configured archive_path"
   ],
   "orchestra/archived-sprint-terminal-disposition.test.ts": [
    "reads ABORTED from a well-formed seal",
    "reads COMPLETE from a well-formed seal",
    "returns null when no archive exists",
    "returns null for a seal belonging to a different sprint",
    "returns null when the seal contradicts its own receipt",
    "returns null when the operator reason digest does not match",
    "returns null for a foreign kind",
    "returns null when the receipt is missing or malformed",
    "returns null for a non-terminal outcome",
    "returns null for an unparseable file",
    "returns null for a malformed sprint id instead of touching the filesystem"
   ],
   "orchestra/as2-p2-mixed-fleet.test.ts": [
    "calls switchProvider with the provider name when /provider <name> is typed",
    "calls switchProvider with trimmed provider name",
    "emits a confirmation message after /provider <name>",
    "emits usage hint when /provider is bare (no arg)",
    "does not invoke the provider adapter for /provider switch (no LLM round-trip)",
    "works without switchProvider option — backward compat (no crash)",
    "switching provider in session-A does not affect session-B (no cross-talk)",
    "each session accumulates its own transcript independently",
    "ollama task routed to host adapter, claude task routed to docker backend — no cross-routing",
    "both workers produce a .result on disk — parallel independent success",
    "provider state does not leak between concurrent workers (each result carries its own via)"
   ],
   "orchestra/as2-p3-failover.test.ts": [
    "detects \"429\" in notes",
    "detects \"rate limit\" (lowercase, space-separated)",
    "detects \"rate_limit\" (underscore)",
    "detects \"too many requests\" case-insensitively",
    "detects \"ratelimit\" (no separator)",
    "returns false for unrelated errors",
    "returns false for empty notes",
    "returns null when no 429 signal and no rateLimitState",
    "returns null when task is already api authMode (no overflow needed)",
    "returns null when non-exhausted rateLimitState provided (no throttle signal)",
    "opus-sub → gpt-5-api: detects 429 in notes and overflows to codex",
    "uses provided exhausted rateLimitState to trigger failover without 429 in notes",
    "returned fallback model is in the model registry and same tier as original",
    "overflowed task preserves all non-provider fields from original",
    "returns new task when 429 detected in result notes",
    "returns null when no 429 detected in notes",
    "uses explicit exhausted rateLimitState when notes have no 429",
    "returns null when task is already on api authMode"
   ],
   "orchestra/attribution-limit-death.test.ts": [
    "mints PROVIDER_LIMIT_DEATH_ZERO_WRITE when limit-death evidence meets a measured zero-write",
    "keeps ATTRIBUTION_DIFF_UNMEASURABLE when the diff is genuinely unmeasurable, limit death or not",
    "leaves a measured zero-write with a live provider as the honest no-work outcome",
    "ignores limit-death evidence bound to a different attempt",
    "does not mint the class when the limit-killed attempt actually wrote lines",
    "never lets a limit death mask an out-of-scope claim",
    "names the new class among the known codes",
    "parses every code this host mints, including the new one",
    "still parses a code minted by a different host revision (additive, not breaking)",
    "rejects a structurally empty code rather than carrying a blank reason",
    "routes PROVIDER_LIMIT_DEATH_ZERO_WRITE away from the generic attribution-hold downgrade",
    "keeps every other attribution hold on WORK_ATTRIBUTION_HOLD",
    "does not honour the class from worker-authored fields",
    "routes the host-minted class to a clean same-task restart",
    "does not let repeated limit deaths be read as an unsatisfiable task definition",
    "still escalates a genuinely repeated zero-diff lineage that is not a limit death",
    "ignores the class when only worker prose claims it"
   ],
   "orchestra/attribution-unmeasurable-rca.test.ts": [
    "measures the real edit on its own — the work IS measurable and IS on disk",
    "discards that measured change wholesale when a later scope entry is unmeasurable",
    "overwrites a worker claim that named real in-scope files",
    "gitBlobHash: a scope entry that is a directory at reconcile time (E_BLOB_HASH_UNAVAILABLE)",
    "countTextLines: a file created with a NUL byte (E_BINARY_OR_UNMEASURABLE_NUMSTAT)",
    "blobNumstat: a baselined text file rewritten as binary (numstat prints \"-\\\\t-\")",
    "gitBlobLineCount: a deleted file whose baseline blob is not in the object store",
    "the 523 signature: identical work, measurable only when git-location env is absent",
    "refutes the stale-baseline theory: a foreign attempt id holds as AUTHORITY_MISMATCH"
   ],
   "orchestra/auditor-stale-race.test.ts": [
    "projects a dynamic FIX heartbeat by its activity task identity",
    "uses exact-attempt host authority and never converts unavailable into dead",
    "PENDING task with stale HB → no stale alert (false positive suppressed)",
    "CLAIMED task with stale HB → no stale alert",
    "EXECUTING task with fresh HB → no alert",
    "EXECUTING task with stale HB → CRITICAL alert (genuine stale)",
    "DONE task with stale HB → WARNING alert (downgrade, not CRITICAL)",
    "dashboard done equals logicalProgress.done exactly, not the result-file count",
    "absent read-model: existing progress left untouched + typed warning (no fabricated number)",
    "done>total invariant violation: no write at all + typed warning"
   ],
   "orchestra/authority-enforcer-symlink.test.ts": [
    "rejects a symlink inside scope.directories that resolves outside scope (regression: was accepted)",
    "rejects a symlink inside scope.filesWrite that resolves outside scope (regression: was accepted)",
    "allows an ordinary in-scope path with no symlink involved",
    "allows a new (non-existent) in-scope file path — parent-realpath handling",
    "allows a new (non-existent) file matched via scope.filesWrite",
    "denies a path genuinely outside assigned scope",
    "rejects a symlink cycle (ELOOP) instead of throwing",
    "resolves an existing path to its real path",
    "walks up to the nearest existing parent for a non-existent nested path",
    "follows a symlink to its real (out-of-scope) target",
    "returns null on a symlink cycle",
    "rejects a worker write to a symlink inside scope.directories resolving outside scope",
    "permits an ordinary in-scope write with an explicit projectRoot",
    "permits creating a brand-new in-scope file with an explicit projectRoot",
    "falls back to process.cwd() when projectRoot is omitted (backward-compatible default)"
   ],
   "orchestra/authority-enforcer.test.ts": [
    "matches exact paths",
    "matches directory wildcards (/**)",
    "matches directory prefixes",
    "normalizes paths consistently",
    "permits Brain to write .tasks files",
    "permits Brain to write .brain/MEMORY.md",
    "permits Brain to write .deckent/sprint-state.json",
    "denies Brain writing src/** (source code)",
    "denies Brain writing tests/**",
    "permits Brain to read src/**",
    "denies Brain writing .brain/DECISIONS.md",
    "denies Brain writing .dashboard",
    "permits Auditor to write .dashboard",
    "permits Auditor to read src/**",
    "permits Auditor to write docs/audits/**",
    "denies Auditor writing src/** (audit independence)",
    "denies Auditor writing tests/**",
    "denies Auditor writing .brain/MEMORY.md",
    "permits Auditor to append .brain/PATTERNS.md",
    "permits Auditor to read .tasks/*.hb",
    "permits Worker to write within assigned scope directories",
    "permits Worker to write assigned filesWrite",
    "denies Worker writing outside assigned scope",
    "denies Worker writing .brain/DECISIONS.md (privilege escalation)",
    "denies Worker writing .dashboard (auditor exclusive)",
    "permits Worker to write own .tasks files",
    "permits Worker to read .brain/DECISIONS.md (ADR-036 mandatory read)",
    "permits Worker to read DIRECTIVES.md",
    "permits Worker to write src/** when isSelfModifyingSprint is true",
    "permits Worker to write tests/** when isSelfModifyingSprint is true",
    "does NOT permit Worker to write .brain/DECISIONS.md even with self-modifying flag",
    "does NOT activate self-modifying for Brain role",
    "permits Brain to emit BRAIN→WORKER:TASK_ASSIGN",
    "denies Brain emitting AUDITOR→BRAIN:VERIFICATION_RESULT",
    "permits Brain to consume WORKER→BRAIN:RESULT",
    "permits Auditor to emit AUDITOR→BRAIN:GATE_COMPUTED",
    "permits Worker to emit WORKER→BRAIN:HEARTBEAT",
    "denies Worker emitting BRAIN→WORKER:TASK_ASSIGN (wrong source)",
    "permits Worker to consume BRAIN→WORKER:FIX_REQUEST",
    "permits Auditor to consume broadcast BRAIN→*:SPRINT_PHASE_CHANGE",
    "emits event to event stream on authority violation",
    "does not throw when writeEvent fails",
    "always returns mode: soft in Sprint 139",
    "returns fail-closed for unknown paths with write action",
    "returns allow for read on unknown paths (default read allow)"
   ],
   "orchestra/auto-archive.test.ts": [
    "creates a tar.gz snapshot with correct hash",
    "returns null when .tasks/ does not exist",
    "returns null when no task files match sprint",
    "returns null for invalid sprint ID format",
    "returns true for valid hash",
    "returns false for tampered file (hash mismatch)",
    "returns false for non-existent file",
    "preserves PENDING task files",
    "preserves EXECUTING task files",
    "archives DONE task files",
    "archives NO_GO task files",
    "handles mixed statuses correctly",
    "archives orphan files without JSON (unknown status)",
    "preserves all file types for active tasks",
    "treats .prompt-* files as always archivable",
    "reads status from valid task JSON",
    "returns null for invalid JSON",
    "returns null for missing file",
    "restores files from a valid snapshot",
    "fails when snapshot does not exist",
    "fails when snapshot hash is corrupted"
   ],
   "orchestra/autonomous/approval-adapter-resolution.test.ts": [
    "applies an accept written by a separate gate instance (the CLI process)",
    "applies a reject written by a separate gate instance",
    "persists the decision to a decisions.json sibling and consumes it once",
    "🔴 NO-AUTO-APPROVE: stays pending across repeated requests with no decision"
   ],
   "orchestra/autonomous/approval-lifecycle.test.ts": [
    "threads resolved lifecycle, project root and tenant authority through the engine composition root",
    "persists canonical EffectClass, risk, tier and immutable source-clock expiry",
    "migrates a legacy row from its original enqueuedAt and never resets age on restart/sweep",
    "uses per-request FWW: timeout cannot be replaced and never reaches takeResolved/replay",
    "does not authorize from a cached pending row after the durable queue becomes corrupt",
    "preserves a fresh human FWW winner when broker timeout settle-back races later",
    "reads restart-safe timeout terminal truth without exposing it for replay"
   ],
   "orchestra/autonomous/approval-no-replay.integration.test.ts": [
    "never executes, redrives or revives a trigger after timeout, including restart"
   ],
   "orchestra/autonomous/approval-redrive.test.ts": [
    "returns null for a parked-but-undecided trigger (no busy-loop)",
    "returns the full trigger (payload intact) once a decision is on disk",
    "does not consume — request() still applies the decision afterwards",
    "yields a resolved trigger ahead of scheduled flows",
    "drains a resolved trigger even when policy.disabled",
    "wires gate.takeResolved into the trigger source — approve makes next() re-emit",
    "an approved parked trigger is executed within the next cycles",
    "the runtime gate parks at autonomousPendingPath, and the read hub finds it there"
   ],
   "orchestra/autonomous/artifact-ref.test.ts": [
    "returns all open checklist items of a whole file",
    "scopes to a section anchor (until the next heading of same/higher level)",
    "returns [] for a missing file"
   ],
   "orchestra/autonomous/backlog-eval.test.ts": [
    "maps entry+result into a Task with JIT description, scope, and run-id",
    "clean DONE → reconciled false, quality = totalScore",
    "sets schemaRejected when the rubric is a schema_validation failure",
    "selfAssessment NO_GO but kernel decided GO_WITH_TECH_DEBT → reconciled true",
    "a clean passing result decides non-NO_GO",
    "an honest NO_GO with no disk work stays NO_GO",
    "in-scope clean result → boundary clean, adr ok, functional pass",
    "out-of-scope filesChanged → boundary violation list",
    "maps a FAIL/DOWNGRADE verifyWorkerResult verdict to functional fail",
    "skips functional when no files changed",
    "treats scopeDir \".\" as unrestricted (no boundary claim)",
    "honest-skips (ran:false) when cross_verify is disabled (no config)",
    "surfaces a refuted advisory but does NOT throw / does not block (advisory)",
    "reconciles a Brain NO_GO when Auditor confirms real, in-scope, functionally-passing work",
    "does NOT reconcile when the worker honestly self-reported NO_GO",
    "does NOT reconcile when the Auditor functional verdict is not pass",
    "does NOT reconcile when there is a boundary violation",
    "does NOT reconcile when there is no real work (empty filesChanged)",
    "does NOT reconcile a genuine quality NO_GO (not a schema rejection) even when functional passes",
    "passes a non-NO_GO evaluation through unchanged",
    "merges brainAssessment into the worker .result, preserving existing fields",
    "is fail-safe when the .result file is absent (never throws)"
   ],
   "orchestra/autonomous/backlog-lastresult-shape.test.ts": [
    "persists decision + reconciled + quality + audit + crossVerify and reloads them"
   ],
   "orchestra/autonomous/backlog-planned-fields.test.ts": [
    "accepts planned + summary + fanOut on a valid entry",
    "accepts kind=process",
    "rejects a malformed fanOut (non-numeric concurrency)",
    "rejects NaN / Infinity / float concurrency",
    "rejects a non-object fanOut and an empty over",
    "rejects non-boolean planned and non-string summary"
   ],
   "orchestra/autonomous/backlog-trigger.test.ts": [
    "yields a trigger carrying the due entry, then null when none due",
    "yields null when backlog has no due entries",
    "requestedBy reflects tenant when present",
    "hybrid returns first source that yields, falls through when earlier idle",
    "hybrid returns null when all sources idle",
    "hybrid prefers the earlier source when both would yield",
    "normalizes a scheduled-flow trigger to autonomous.execute with a persisted backlog entry",
    "detects 'sprint' actions as kind=sprint and requiresApproval=false as policy=auto",
    "passes non-flow triggers through untouched",
    "passes an approval-redrive replay (payload already carries an entry) through untouched — no re-enqueue",
    "same cadence re-fire does not duplicate the backlog entry (id dedupe)",
    "idle inner source stays idle"
   ],
   "orchestra/autonomous/backlog.test.ts": [
    "loads a valid backlog file",
    "returns empty backlog when file absent (fresh project)",
    "rejects an entry with an invalid policy",
    "accepts a fully valid entry",
    "queryDue returns pending one-off entries",
    "updateStatus persists atomically and re-loads",
    "throws when entries is not an array",
    "throws on loadBacklog when a stored entry has an invalid kind",
    "updateStatus writes lastResult and lastRun when result provided",
    "updateStatus throws on unknown id",
    "rejects an entry with an empty title",
    "rejects an entry with an array spec",
    "queryDue also surfaces pending reactive entries",
    "purgeCompletedBacklog removes done/failed entries beyond keepRuns",
    "purgeCompletedBacklog never removes active (pending/running/parked) entries",
    "purgeCompletedBacklog keeps all completed entries when count <= keepRuns",
    "purgeCompletedBacklog persists atomically (re-load confirms)",
    "cleanupAutonomousArtifacts removes task-run-* files and returns the count",
    "cleanupAutonomousArtifacts removes _*.pid files",
    "cleanupAutonomousArtifacts leaves unrelated task files intact (returns only artifacts removed)",
    "cleanupAutonomousArtifacts is a no-op (returns 0) when tasks directory is absent",
    "reenqueueRecurring resets a recurring done entry to pending when next-due has passed",
    "reenqueueRecurring leaves a recurring done entry done when next-due is still in the future",
    "reenqueueRecurring leaves a one-off done entry untouched",
    "reenqueueRecurring leaves a recurring done entry done when cron is malformed (fail-safe, no throw)",
    "queryDue surfaces pending recurring entries (pending recurring = due now)",
    "applyRecurringReenqueue persists the done→pending flip when the cadence is due",
    "applyRecurringReenqueue leaves the file untouched when nothing flips",
    "enqueueCandidates appends new candidates, persists, and returns them",
    "enqueueCandidates dedupes by id against entries of any status",
    "enqueueCandidates dedupes within a single candidate batch",
    "enqueueCandidates skips invalid candidates without throwing and does not write when none are valid",
    "accepts a capability entry with a capabilityTarget",
    "rejects a capability entry without a capabilityTarget",
    "rejects a capability entry whose capabilityTarget has no capability verb"
   ],
   "orchestra/autonomous/composition.test.ts": [
    "builds a runtime with the execute action wired + a policy gate + a trigger source",
    "returns an approvalGate adapter on the bundle",
    "policy gate returns auto for a non-backlog trigger (no entry payload)",
    "policy gate returns auto for a backlog entry with policy=auto",
    "policy gate returns park for a backlog entry with policy=approval-required",
    "accepts an optional reactiveSource and includes it in the hybrid source"
   ],
   "orchestra/autonomous/cycle-policy.test.ts": [
    "policyGate=park → cycle parks (pending), executor NOT called",
    "policyGate=park + approvalGate rejects → rejected, executor NOT called",
    "policyGate=park + approvalGate approves → executes",
    "policyGate=auto → executor runs",
    "policyGate=deny → cycle denied, executor and approval gate NOT called",
    "no policyGate → legacy behavior preserved (executes when authority allowed)",
    "authority denied still short-circuits before policy gate",
    "authority needs_approval (approved) + policyGate=park → approvalGate.request called ONCE",
    "system + AUTONOMOUS_EXECUTE_ACTION → allowed (reaches policy gate)",
    "system:engine + AUTONOMOUS_EXECUTE_ACTION → allowed (prefix match)",
    "non-system requestedBy + AUTONOMOUS_EXECUTE_ACTION → delegates to base authority (not blindly allowed)",
    "system + unrelated action → delegates to base authority (default-deny preserved)"
   ],
   "orchestra/autonomous/engine-config-clone.test.ts": [
    "runTaskMode receives config with deckent_style === \"task\""
   ],
   "orchestra/autonomous/engine-wiring.test.ts": [
    "dispatches a due recurring done entry and persists the pending flip",
    "stays idle while the recurring entry is not yet due",
    "enqueues + dispatches self-generated candidates when generateWork is provided",
    "does not duplicate or re-yield an already-known candidate id (any status)",
    "polls the work-generator only when backlog + scheduled sources are idle",
    "omits the work-generator source when generateWork is absent (backward-safe)",
    "forwards process-root provider admission and parks before task execution",
    "executes a capability backlog entry end-to-end through a full autonomous cycle",
    "parks a risk-tagged side-effecting capability entry instead of executing it",
    "denies machine-initiated dispatch when rbac_policy is enabled with a role lacking execute",
    "permits machine-initiated dispatch when the enforced role has execute (operator)",
    "rbac_policy absent → dispatch ungated (backward-safe)",
    "a due user-configured flow EXECUTES end-to-end (no-approval guard): runTask runs, backlog records done",
    "a due flow with the default approval guard PARKS for a human (ADR-040 preserved)"
   ],
   "orchestra/autonomous/execute-dispatcher-eval.test.ts": [
    "drives the Brain verdict into the rich lastResult and fires ordered flow steps",
    "a Brain NO_GO marks the entry failed",
    "reconciles a Brain NO_GO into done when the Auditor independently confirms real work (live false-NO_GO fix)",
    "an out-of-scope boundary violation stays advisory (still done on a GO decision)",
    "writes the Brain assessment back into the worker .result file"
   ],
   "orchestra/autonomous/execute-dispatcher-jit.test.ts": [
    "generates + persists the description before running a planned task",
    "reconciles a late result after the initial waitForResult timeout (false-FAILURE fix)",
    "fails a process entry with an honest reason (F3-008 pending)"
   ],
   "orchestra/autonomous/execute-dispatcher-process.test.ts": [
    "process with a capability step → real dispatch, backlog done, reason is NOT \"not available\"",
    "process with a task step → runTask invoked sequentially, backlog done",
    "process with NO definition → honest-fail (failed; reason NOT \"not available\")"
   ],
   "orchestra/autonomous/execute-dispatcher.test.ts": [
    "fails loudly when the live cross-verify path is reached",
    "parks kind=%s before JIT or dispatch with durable HOLD provenance",
    "keeps provider-free capability execution outside the provider admission gate",
    "fails loudly before JIT/dispatch when canonical admission rejects the identity",
    "kind=capability → registry invoked, backlog moves pending→running→done",
    "kind=capability: unknown verb → CAPABILITY_NOT_FOUND, entry failed",
    "kind=capability without a wired registry → failure with a clear reason",
    "kind=capability with a missing capabilityTarget in the PAYLOAD → failure (defensive)",
    "kind=capability passes projectRoot + tenant-derived actor into the invocation context",
    "kind=task → runTask invoked with entry provider/model, backlog moves pending→running→done",
    "kind=task: waitForResult→null (timeout) → entry becomes failed, outcome=failure",
    "threads the exact Docker settlement authority into result waiting",
    "uses exact accepted-result authority without polling the public result projection",
    "rejects a metadata-less exact accepted projection without polling public bytes",
    "fails closed when exact accepted-result authority is unavailable",
    "kind=task: selfAssessment=NO_GO → entry failed, outcome=failure",
    "kind=task: GO_WITH_TECH_DEBT → success (mirrors run.ts:320)",
    "kind=task: runTask returns no taskId → failure (cannot track completion)",
    "persists proven zero-work ingress evidence as failed without calling the result waiter",
    "parks process execution with durable reconciliation evidence instead of generic failure",
    "returns a typed durability HOLD and emits no parked claim when backlog publication fails",
    "kind=sprint success → entry done, outcome=success",
    "persists an authored exact ref and parks while digest-bound approval is pending",
    "missing entry payload → failure (no silent success)",
    "runTask throwing → outcome=failure, entry becomes failed",
    "runTask returning rejected promise → outcome=failure (async rejection caught)",
    "executeSprint rejecting → failure with error, entry becomes failed",
    "falls back to entry.title when spec.description is absent",
    "AUTONOMOUS_EXECUTE_ACTION constant is exported and stable",
    "resultTimeoutMs is forwarded to waitForResult",
    "pool.submit is called when pool is provided",
    "no pool → direct execution (serial fallback, backward-safe)",
    "maxConcurrency=1 runs jobs serially (one at a time)",
    "maxConcurrency=2 allows two jobs to run in parallel",
    "caps in-flight at maxConcurrency — third job waits for a slot",
    "error in one job propagates to its promise but does not block subsequent jobs"
   ],
   "orchestra/autonomous/execution-pool.test.ts": [
    "runs submitted jobs and returns results in order",
    "a throwing job rejects its own submit, pool keeps working",
    "serializes overlapping submissions (no interleave)"
   ],
   "orchestra/autonomous/flow-reporter.test.ts": [
    "emits an ordered record on the audit channel and a human line on print",
    "is a no-op-safe partial (missing channels never throw)",
    "localizes the step label (tr)"
   ],
   "orchestra/autonomous/goal-consumer-crash-reopen.test.ts": [
    "reopens a successful empty authoring checkpoint without provider or author replay",
    "keeps an effect checkpoint pending when the real receipt append proxy fails",
    "reopens an acknowledged real acceptance receipt into one stable canonical decision",
    "reopens an actual artifact-bound accepted receipt after effect commit without duplicate effect",
    "rejects a sibling/effect-digest checkpoint conflict before any effect or acknowledgement"
   ],
   "orchestra/autonomous/goal-invocation-transport.test.ts": [
    "rejects a missing backend as typed no-dispatch identity failure",
    "uses the admitted provider/model and requires provider-reported usage",
    "holds output that lacks provider-reported usage"
   ],
   "orchestra/autonomous/goal-planner-map.test.ts": [
    "maps a task item to a valid pending+planned backlog entry (no description)",
    "maps recurring trigger + fanOut + capabilityTarget"
   ],
   "orchestra/autonomous/goal-planner-types.test.ts": [
    "accepts a minimal valid item",
    "accepts recurring trigger + fanOut + capabilityTarget",
    "rejects an unknown kind",
    "rejects an absolute or traversing scopeDir"
   ],
   "orchestra/autonomous/goal-planner.test.ts": [
    "parses + validates items, dropping invalid ones",
    "strips code fences and dedups by id",
    "unwraps a Claude CLI envelope ({type,result:fenced-json}) into items (dogfood 2026-06-17)",
    "calls the LLM with the goal and returns validated items, capped to maxItems",
    "includes artifact seeds in the prompt when provided",
    "includes the default policy in the prompt when provided",
    "declares the runtime-admitted kind set when the execution surface supplies one"
   ],
   "orchestra/autonomous/idle-spin.test.ts": [
    "non-active outcome (no_trigger) sleeps intervalMs immediately — no threshold window",
    "all idle ticks sleep intervalMs regardless of count (no backoff window)",
    "active work sleeps 0, then idle ticks sleep intervalMs immediately",
    "returns maxIterations reason after running all ticks",
    "aborts immediately when signal fires"
   ],
   "orchestra/autonomous/jit-detail.test.ts": [
    "true for a planned task/sprint without a description",
    "false once a description exists",
    "false for capability/process (no code detail needed)",
    "false for a non-planned entry",
    "fills spec.description from the LLM for a task and includes the summary in the prompt"
   ],
   "orchestra/autonomous/mission-audit-bridge.test.ts": [
    "persists a mission lifecycle event readable by readMissionAudit",
    "writes under the stable autonomous-missions partition",
    "links two consecutive events via prevHmac/hmac",
    "does not throw when the projectRoot is unwritable (ENOTDIR ancestor)",
    "returns only missions:-prefixed actions on the shared partition"
   ],
   "orchestra/autonomous/mission-scheduler-fairness.test.ts": [
    "(a) caps each tenant at perTenantPoolSize and lets tenants progress in parallel",
    "(a2) a flooding tenant cannot starve another (more items than poolSize)",
    "(b) perTenantPoolSize undefined → global-only behaviour unchanged (regression-guard)",
    "(c) single tenant + cap → concurrency bounded to the cap"
   ],
   "orchestra/autonomous/mission-store/goal-invocation-admission-authority.test.ts": [
    "delegates byte-identically to admissionRuntime.admit when estimates are ready",
    "admits non-reservable subscription without calling buildReservation",
    "holds when purpose admission is missing",
    "holds percent windows under api authMode",
    "holds when projection authority provider mismatches caller provider",
    "holds when purpose admission policy is hold"
   ],
   "orchestra/autonomous/mission-store/goal-invocation-budget-authority.test.ts": [
    "maps canonical token and request ceilings without inventing provider truth",
    "holds an unsupported %s window"
   ],
   "orchestra/autonomous/mission-store/goal-invocation-runtime.test.ts": [
    "settles only a persisted checkpoint bound to the exact successful output artifact",
    "does not claim semantic consumption until the caller settles it",
    "does not admit or dispatch a deterministic replay",
    "resumes a succeeded unsettled consumer from bound output without another provider call",
    "holds a replay when the candidate map key masks a different provider identity",
    "holds replay when the artifact reservation backend differs from the candidate and receipt",
    "records terminal transport truth and releases only proven pre-dispatch failures (%s)",
    "holds an executor without explicit backend capability before claim or dispatch",
    "retries the same identity after a pre-reservation capability HOLD without a poisoned receipt"
   ],
   "orchestra/autonomous/mission-store/goal-mission.test.ts": [
    "creates a kind=goal mission (renderAs goal) with goal + acceptance persisted",
    "reconciles an authored effect committed before receipt ack after SQLite reopen without duplicate work",
    "preserves a reopened terminal %s mission and its result when consumer reconciliation is held",
    "does not reactivate a mission cancelled while consumer reconciliation is awaiting",
    "reuses an acknowledged empty author stage when acceptance is held",
    "persists acceptance evaluation before ack and finalizes the identical decision after reopen",
    "rolls back a checkpoint with its conflicting batch and rejects sibling payload reuse",
    "passes the exact immutable acceptance contract to the author prompt seam",
    "atomically persists criterion evidence and completes only with evaluator receipt provenance",
    "fails closed and persists unknown when explicit acceptance lacks receipt/evidence provenance",
    "authors the next work-items when there are no open items",
    "stamps missionId on enqueue even when author returns a foreign missionId",
    "completes the mission (accepted) when author is empty and accept returns true",
    "fails the mission (exhausted) when author is empty and accept returns false",
    "persists a typed author HOLD without exhausting or finalizing the mission",
    "persists a typed accepter HOLD but propagates an ordinary author failure",
    "rejects a malformed HOLD before it can change mission state",
    "is a no-op (waiting) while an open work-item is still pending/running",
    "is a no-op (waiting) while recovered work is parked for owner reconciliation",
    "trips the maxRounds guard → exhausted/failed without authoring",
    "throws when the mission does not exist",
    "rejects an unsupported authored batch before any item is enqueued",
    "stamps and atomically persists the production fence on an authored goal round",
    "adapts planner→author: planner is invoked and its items are enqueued",
    "adapts accepter→accept: when the planner is dry, the accepter decides completion",
    "forwards maxRounds to the loop guard (planner not consulted past the cap)"
   ],
   "orchestra/autonomous/mission-store/goal-provider-candidate-authority.test.ts": [
    "binds both stores to the same exact executable profile",
    "holds legacy selectors without executable authority"
   ],
   "orchestra/autonomous/mission-store/mission-acceptance-receipt.test.ts": [
    "settles only after a restarted ledger verifies exact scope, provenance, and terminal outcomes",
    "rejects wrong purpose, unknown authority evidence, non-terminal transport, failed calls, and corrupt ledgers"
   ],
   "orchestra/autonomous/mission-store/mission-acceptance.test.ts": [
    "binds the exact criterion text, author provenance, and timestamp into the digest",
    "fails loud when a persisted contract is present but digest-corrupt",
    "is exact-replay idempotent, rejects a changed same-round decision, and survives restart",
    "rolls back the decision insert when mission settlement fails in the same transaction"
   ],
   "orchestra/autonomous/mission-store/mission-approval-coordinator.test.ts": [
    "waits for dependencies, then atomically parks and publishes one request",
    "reconciles crash-after-submit/before-ack without a duplicate request",
    "hydrates allow after restart and preserves exactly-once claim",
    "keeps an unattested legacy allow parked instead of turning it into Goal-v2 claim authority",
    "rechecks live session authority immediately before claim and dispatches nothing after revocation",
    "maps %s to durable %s and item %s",
    "maps TTL expiry to blocked and scheduler propagates dependency failure before dispatch",
    "propagates a signed deny through transitive dependencies before dispatch and preserves it after restart",
    "sweeps an overdue undecided request before hydration and blocks it durably",
    "durably parks missing request identity without creating approval authority",
    "continues dispatching independent auto work when an invalid approval request is held",
    "fails loud when a deterministic request id is already bound to different Broker content",
    "does not apply a foreign allow decision whose request body conflicts with the outbox",
    "returns the persisted first binding when two admission attempts race"
   ],
   "orchestra/autonomous/mission-store/mission-approval-lifecycle.test.ts": [
    "authors canonical v2 from request creation clock with effective risk/EffectClass parity",
    "does not use item.createdAt as a fresh TTL and parks an invalid producer window fail-closed",
    "blocks new governed outbox records when lifecycle rollout is disabled"
   ],
   "orchestra/autonomous/mission-store/mission-deliver.test.ts": [
    "calls notify with correct payload when mission has deliverTo",
    "calls notify with to=null when deliverTo is null (default channel)",
    "swallows (does not propagate) a synchronous throw from notify",
    "swallows a rejected Promise from notify (async fail-safe)",
    "passes the mission status (failed) correctly into the notify payload"
   ],
   "orchestra/autonomous/mission-store/mission-dispatch.test.ts": [
    "routes kind=task to runTask with ctx built from spec, returns its ResultLike",
    "falls back to item.id as description when spec has none; omits absent optional fields",
    "reports failure (never throws) when runTask itself throws",
    "forwards an authored execution budget as a request-level narrowing input",
    "does not silently drop malformed budget values before the canonical validator",
    "forwards the exact item + dispatch claim and preserves the typed result",
    "returns { ok: false } with the error message when exact execution throws",
    "routes kind=capability to runCapability with spec.capabilityTarget",
    "fails with \"no capability broker\" when runCapability dep is absent",
    "fails clearly when a capability item carries no capabilityTarget",
    "delegates kind=process to the injected runProcess broker with the full spec (runTask untouched)",
    "runs inline spec.steps[] sequentially as task-dispatches and reports ok when all pass",
    "stops at the first failing step (fail-stop) and never runs later steps",
    "fails with an explicit reason when there is no runProcess broker and no steps (no silent task-fallback)",
    "reports an explicit reason for an unknown (runtime-malformed) kind"
   ],
   "orchestra/autonomous/mission-store/mission-engine-wire.test.ts": [
    "returns true ONLY when autonomous.engine === \"v2\"",
    "returns false when the flag is absent (default → v1 path)",
    "returns false for an explicit \"v1\" flag",
    "returns false when the autonomous block itself is absent",
    "passes an active mission claim only through the coordinator exact-executor seam",
    "parks before coordinator admission when the exact executor is absent",
    "fails closed when the Goal-v2 coordinator is absent even if legacy runTask is provider-capable",
    "threads the durable approval coordinator before claim and dispatches only after allow",
    "opens+migrates the store and drives a list-mission to completion via injected runTask",
    "persists a host HOLD without contradictory failed settlement detail or delivery",
    "boot recover() parks an orphaned running item instead of risking duplicate side effects",
    "does not author, accept, or dispatch a goal whose recovered attempt is parked",
    "marks a mission failed when an injected runTask reports ok:false",
    "parks an unfenced sprint item before the exact executor is reachable",
    "imports backlog.json into the store at boot (migrateBacklogJson called)",
    "boots past a quarantined legacy kind and still dispatches its admitted sibling",
    "fires the settle-delivery notify when a mission settles",
    "opens its OWN store when none is injected (production path) and closes it",
    "refuses a second engine before recovery while another process owns the active lease",
    "renews the engine lease while provider work remains in flight and releases it on completion",
    "reconciles an enqueue-committed consumer checkpoint before the open-item guard after reopen",
    "acknowledges a pending consumer on a reopened accepted mission without execution or delivery",
    "keeps a held terminal checkpoint eligible for another pass without reactivation",
    "threads the approval coordinator through the goal-driven scheduler drain",
    "drives a goal mission end-to-end: author→item→scheduler→accept→completed",
    "does not re-author a canonically accepted goal after a clean restart",
    "continues a completed goal without accepted terminal truth: $id",
    "drains without dispatch/finalize on HOLD, then resumes exactly once after restart",
    "fails a goal mission when the loop exhausts (author a round, then dry + reject → failed)",
    "does not re-deliver a dependency-failed goal on a clean restart",
    "still drives plain list missions when goalDeps is present (no regression)"
   ],
   "orchestra/autonomous/mission-store/mission-events.test.ts": [
    "append + readTail round-trips; reset unlinks the mission file"
   ],
   "orchestra/autonomous/mission-store/mission-ingest.test.ts": [
    "creates 1 list-mission + N pending work-items for a 20-item list",
    "derives item.id as `${missionId}-${index}` when not provided, preserves explicit ids",
    "propagates tenant and deliverTo to the mission",
    "creates mission with 0 items when items array is empty",
    "accepts forward dependencies and exact replay without duplicate rows",
    "rejects $name before persisting any row",
    "rolls back the mission when a global work-item identity conflicts",
    "rejects a conflicting replay without mutating the original batch",
    "validates the whole runtime batch before invoking MissionStore persistence",
    "persists one production admission fence for every executable list item",
    "rejects an arbitrary kind cast at the store boundary before a mission is written"
   ],
   "orchestra/autonomous/mission-store/mission-kind-admission.test.ts": [
    "contains exactly the four canonical work-item kinds",
    "binds exactly the handlers declared by the immutable registry",
    "validates the complete frozen attempt authority before the bound handler runs",
    "admits a described task in production and rejects an empty description",
    "requires a capability target and a live broker",
    "requires a real process definition and runner",
    "requires exactly one canonical exact-plan or unplanned source before runner admission"
   ],
   "orchestra/autonomous/mission-store/mission-migrate.test.ts": [
    "imports legacy backlog entries as a legacy mission\\'s work-items (idempotent)",
    "fails loud when a fingerprinted source backlog changes after import",
    "reconciles a matching pre-provenance mission and restores terminal evidence",
    "fails loud when a concurrent revision wins before terminal evidence backfill",
    "rejects pre-existing terminal evidence that differs from the legacy source",
    "quarantines an invalid legacy definition without blocking an admitted sibling",
    "preserves a raw unknown legacy kind as failed quarantine while fresh intake stays strict",
    "parks a valid legacy definition when its canonical runner is not wired",
    "parks an imported running task with durable recovery evidence",
    "refuses presence-only replay of import/recovery creation state",
    "rolls back the reserved legacy mission when an item identity conflicts"
   ],
   "orchestra/autonomous/mission-store/mission-scheduler.test.ts": [
    "runs at most poolSize items concurrently and settles all done",
    "dispatches each item exactly once (race-free claim)",
    "passes the exact claim authority to dispatch and settles through that authority",
    "threads owner bounds through restart-safe transitive propagation without freezing independent work",
    "marks mission completed when all items done; fires onMissionSettled once",
    "marks mission failed when any item fails",
    "fails loud when exact settlement CAS loses and never performs a blind second write",
    "parks a host HOLD without counting a provider dispatch or settling the mission successful",
    "counts a granted/ambiguous provider dispatch while parking it for reconciliation",
    "does not dispatch or increment dispatched when the query-to-claim row revision turns stale",
    "fails loud before provider dispatch when the engine lease is lost after claim",
    "refuses stale settlement when the engine lease is lost during provider dispatch",
    "a throwing dispatch marks the item failed; loop continues",
    "propagates a delayed settlement conflict while draining a %s boundary",
    "abort drains in-flight and leaves no item running",
    "empty store returns drained without hanging",
    "runs Type-1 (list) and Type-2 (goal) missions concurrently; both settle",
    "never dispatches a dependent before its prerequisite is done, even with poolSize > 1",
    "propagates upstream failure transitively without dispatching downstream items",
    "fails a cycle and settles the mission without invoking dispatch",
    "never dispatches an approval-required item when no coordinator is composed"
   ],
   "orchestra/autonomous/mission-store/mission-view.test.ts": [
    "projects mission + items + render_as + derived progress"
   ],
   "orchestra/autonomous/mission-store/mission-worker-invocation-coordinator.test.ts": [
    "parks null authority and inactive claims before preparation or provider execution",
    "preserves an exact upstream composition HOLD before the executor",
    "binds known primary claim→receipt→grant→zero-usage settlement exactly once",
    "uses the canonical fallback reservation and receipt when primary capacity is held",
    "persists a terminal rejection receipt when every exact fallback is limit-held",
    "settles a provider grant as released only with host-verifiable no-effect evidence",
    "never executes when receipt declaration fails",
    "parks a crash after provider grant but before receipt dispatch-start and never redrives",
    "parks an ambiguous executor failure and concurrent replay gets no second grant",
    "keeps consumed usage and parks when consumer-receipt settlement crashes",
    "parks for reconciliation when transport receipt persistence fails after execution",
    "parks for reconciliation when provider-limit settlement rejects measured scope",
    "rejects actual called-provider drift and executor-authored parked semantics",
    "binds tenant/project before reservation or receipt declaration",
    "derives immutable invocation/call/reservation identity so producer drift cannot redrive",
    "rejects a reservation/claim identity mutation before provider execution",
    "rejects a legacy model alias before creating a reservation or provider execution"
   ],
   "orchestra/autonomous/mission-store/mission-worker-invocation-recovery.test.ts": [
    "closes only the exact open worker receipt and durably acknowledges it",
    "keeps missing or identity-mismatched receipts pending without inventing terminal truth",
    "acknowledges an exact pre-existing terminal receipt without appending a duplicate",
    "resumes the capture-to-receipt saga after both durable stores restart"
   ],
   "orchestra/autonomous/mission-store/missions-crud.test.ts": [
    "createMission applies defaults and getMission round-trips",
    "listMissions filters by status + tenant",
    "updateMissionStatus sets completed_at + last_result on completion; setMissionProgress persists",
    "clears a stale terminal timestamp when a round-based goal becomes active again"
   ],
   "orchestra/autonomous/mission-store/sqlite-mission-store-schema.test.ts": [
    "migrate() creates mission, work-item, and approval-outbox tables (idempotent)",
    "keeps normalized dependency authority default-off and requires an explicit composition reference",
    "creates a normalized mission batch atomically without JSON dependency authority",
    "additively upgrades a pre-fence work_items table without deleting rows",
    "prepares but never activates a valid legacy graph without an exact digest-bound decision",
    "quarantines malformed legacy dependency evidence without partial activation",
    "validates reconciliation bounds before mutating legacy runtime admission state",
    "recover() parks orphaned running work_items with reconciliation evidence",
    "enforces mission foreign keys on direct work-item writes"
   ],
   "orchestra/autonomous/mission-store/work-items-claim.test.ts": [
    "enqueueItem + queryDue returns pending items (limit honored)",
    "parks trigger=%s before due-query and never publishes a template approval",
    "atomically refuses direct recurring claim and revokes stale running authority during recovery",
    "journals an exact running claim before trigger recovery clears its authority",
    "rejects unknown fresh trigger families and fails closed for persisted trigger drift",
    "imports an unknown legacy trigger without aborting boot, then quarantines it durably",
    "claimItem is atomic — exactly one of N concurrent claims wins",
    "issues one exact dispatch authority and persists only its token hash",
    "settles an exact claim once and rejects wrong, stale, or replayed authority",
    "atomically adopts a returned exact plan ref with claim, revision, lease, and admission-fence CAS",
    "recovery revokes an orphaned claim so its old authority can never settle",
    "claimItem skips an already-running item; exact settlement persists result",
    "backfills only claim-free terminal legacy evidence under exact row revision",
    "queryDue and atomic claim refuse stale pending work owned by a terminal mission",
    "queryDue and atomic claim both refuse unmet dependencies",
    "reconcilePendingDependencies fails missing and cyclic chains durably",
    "blocks a later item that depends on an already-blocked upstream",
    "queryDue and claimItem fail-close policy=%s until a durable allow exists",
    "keeps policy=auto behavior unchanged and fails closed for an unknown persisted policy",
    "durably fails an unsupported persisted kind before due-query or approval publication",
    "atomically refuses and classifies an unsupported kind when claim is the first read",
    "classifies a corrupt running row before orphan recovery and stays idempotent",
    "persists the production registry fence and records claim authority provenance",
    "parks a missing-fence legacy task durably and idempotently",
    "parks a v1 runner fence when production advances to the host-authority v2 contract",
    "parks a fence-bearing task tampered to canonical but unwired kind=%s",
    "fails a definition changed after admission even when row revision was not advanced",
    "loses a stale row-revision claim without dispatch authority",
    "holds a fence when the same registry revision is reused with a different digest",
    "grants one active engine lease and fences old claim authority after expiry takeover",
    "releases only exact engine authority and keeps epochs monotonic across clean handoff",
    "rolls back a whole admitted goal batch on one id conflict",
    "rejects a semantically mismatched decision-state transition",
    "uses normalized edges across approval, due and both claim seams and ignores legacy JSON tamper",
    "bounds and fairly rotates failure propagation jobs and resumes them after restart"
   ],
   "orchestra/autonomous/policy-gate-effectclass.test.ts": [
    "docs/audits/ scope → pure",
    "docs/ scope (non-audit) → reversible",
    "src/ scope → reversible",
    "kind=sprint (no scopeDir) → reversible",
    "\"npm publish\" in description → critical-irreversible",
    "\"webhook\" keyword in description → compensable",
    "\"db-migration\" keyword in description → idempotent",
    "no scope, no description → fail-safe critical-irreversible",
    "irreversible keyword takes priority over scope",
    "(a) pure scope + risk-tagged → auto",
    "(a2) reversible scope + risk-tagged → auto",
    "(b) critical-irreversible description + risk-tagged → park",
    "(b2) compensable description + risk-tagged → park",
    "(c) unknown nature (no spec) + risk-tagged → fail-safe park",
    "(d) decidePolicy called without effect → backward-compat (default reversible → auto)",
    "auto policy always → auto regardless of computed effect",
    "approval-required policy always → park regardless of computed effect",
    "result carries a non-empty reason string in all branches",
    "read-only capability verbs classify as pure",
    "side-effecting / unknown capability verbs fail safe to critical-irreversible",
    "risk-tagged + read-only capability → auto; risk-tagged + side-effecting → park"
   ],
   "orchestra/autonomous/policy-gate.test.ts": [
    "auto policy → auto",
    "approval-required policy → park",
    "risk-tagged + reversible effect (default) → auto",
    "risk-tagged + pure effect → auto",
    "risk-tagged + critical-irreversible effect → park",
    "risk-tagged + idempotent effect → park (only pure/reversible auto-safe)",
    "risk-tagged + compensable effect → park",
    "every policy branch carries a non-empty reason"
   ],
   "orchestra/autonomous/post-item-lifecycle.test.ts": [
    "removes task-run-* and _*.pid artifacts but keeps legitimate task files",
    "preserves the just-completed run via keepTaskId, sweeping only prior-run artifacts",
    "is idempotent — a second run against the cleaned state is a no-op (no throw)",
    "trims completed entries to the most recent 5, keeping active entries",
    "a failing step (corrupt backlog) does not throw and does not block decay",
    "a throwing decay hook is swallowed (item outcome never corrupted)",
    "two consecutive autonomous items leave no task-run-* / _*.pid leakage"
   ],
   "orchestra/autonomous/reactive/nervous-reactive-source.test.ts": [
    "normalizes a detection and forwards it to the ingester on start()",
    "stop() removes the listener — later detections are ignored"
   ],
   "orchestra/autonomous/reactive/reactive-ingester.test.ts": [
    "ingest on match writes a pending reactive entry",
    "ingest with no matching rule returns unmatched, writes nothing",
    "ingest dedups against an existing pending reactive entry with the same key",
    "ingest preserves existing unrelated entries (atomic append)",
    "ingest writes a new entry when a same-key reactive entry is already done (not pending/running)"
   ],
   "orchestra/autonomous/reactive/reactive-map.test.ts": [
    "loadReactiveMap returns empty map when file absent",
    "loadReactiveMap loads a valid file",
    "validateReactiveRule rejects a rule with no match criteria",
    "validateReactiveRule accepts a valid rule",
    "mapEventToEntry matches by groupKey + risk threshold → entry",
    "mapEventToEntry returns null when groupKey differs",
    "mapEventToEntry returns null when risk below threshold",
    "mapEventToEntry folds risk/severity into description"
   ],
   "orchestra/autonomous/recovery.test.ts": [
    "parks an authority-less running entry instead of blind-redriving it",
    "leaves non-running entries untouched",
    "is a no-op (no throw) when the backlog file is absent"
   ],
   "orchestra/autonomous/repo-watch-reactive-source.test.ts": [
    "ingests a repo ReactiveEvent on a working-tree change (immediate when debounceMs=0)",
    "ignores deckent-internal paths (.git / .deckent / .brain / .tasks / dist) — no self-trigger",
    "honours custom risk + groupKey",
    "debounces a burst into a single event carrying all paths",
    "stop() closes the watcher and clears a pending timer"
   ],
   "orchestra/autonomous/scheduled-flow.test.ts": [
    "returns a future date strictly after `from`",
    "zeros seconds and milliseconds",
    "advances exactly 1 minute for every-minute wildcard expression",
    "advances to the target minute within the same hour",
    "wraps to the next hour when target minute has already passed",
    "evaluates */5 step — lands on the next multiple of 5",
    "evaluates */5 step from minute 0 — next is minute 5",
    "evaluates */15 step",
    "waits until the target hour when already past it",
    "fires at the target hour on the same day when not yet reached",
    "evaluates */3 step on hour",
    "advances to the next weekday for 1-5 (Mon–Fri)",
    "stays within current weekday if hour not yet reached",
    "supports Sunday using cron alias 7",
    "supports Sunday using cron value 0",
    "supports a comma-separated day-of-week list (Mon, Wed, Fri)",
    "waits for the 15th of the month",
    "returns same day when dom matches and time not yet reached",
    "waits for the target month (March)",
    "fires in the same month when not yet past target month",
    "evaluates minute range with step: 0-30/10",
    "evaluates step on hour range: 8-18/2",
    "returns the closer of dom and dow when both are specified",
    "evaluates \"30 9 * * 1-5\" — weekdays at 09:30",
    "uses current time when no `from` is provided",
    "is available and works correctly from the autonomous module"
   ],
   "orchestra/autonomous/tok-aut.test.ts": [
    "(a) fills non-zero tokenUsage from CLI log when entry has model+provider",
    "(b) falls back to heuristic estimate when entry has model but no CLI log",
    "(c) enrichResultTokenUsage is called with stub carrying entry model+provider",
    "(d) no model+provider on entry → tokenUsage stays at worker-stub zeros (backward-compat)"
   ],
   "orchestra/autonomous/token-usage-enrichment.test.ts": [
    "(a) fills non-zero tokenUsage from CLI log when a measured file exists",
    "(b) leaves tokenUsage as worker-stub zeros when no CLI log exists"
   ],
   "orchestra/autonomous/webhook-reactive-source.test.ts": [
    "maps event → webhook.<event> groupKey, defaults risk to low",
    "keeps a valid risk + severity",
    "rejects a body with no event name",
    "drops an invalid risk back to low",
    "drains appended webhook events into the ingester",
    "persists the offset — a second drain ingests only NEW events, no replay",
    "resumes from the persisted offset across a fresh source (restart, no replay)",
    "skips corrupt lines without throwing",
    "start()/stop() poll cadence is timer-driven and unref-safe"
   ],
   "orchestra/autonomous/work-generator-source.test.ts": [
    "yields first candidate as a trigger when generate returns entries",
    "returns null when generate returns empty array",
    "swallows generator errors and returns null (fail-safe)",
    "includes tenant in requestedBy when entry has tenant",
    "uses system requestedBy when entry has no tenant",
    "yields only the first candidate per call",
    "re-calls generate on each next() invocation",
    "maps active debt items to work-generator candidates",
    "maps HIGH/CRITICAL debt priority to risk-tagged policy",
    "throttles scans: within intervalMs the loader is not re-called and [] is returned",
    "re-scans after intervalMs elapses",
    "returns [] when the debt loader throws (fail-safe)"
   ],
   "orchestra/autonomous/work-generator.test.ts": [
    "returns [] for empty input",
    "maps a debt record to a BacklogEntry candidate",
    "uses title in description when debt has no description",
    "maps high-severity debt to risk-tagged policy",
    "maps a TODO marker to a BacklogEntry candidate",
    "maps a FIXME marker to a risk-tagged candidate",
    "produces stable ids across repeated calls with same input",
    "handles both debt and todo markers together",
    "debt entries have source tag in spec.description",
    "TODO entries have source tag in spec.description",
    "all candidates are pending with one-off trigger"
   ],
   "orchestra/autonomous-action-adapter.test.ts": [
    "finds and runs registered handler → ok:true on success outcome",
    "returns ok:false when no handler registered for action",
    "returns ok:false with error message when handler throws",
    "passes trigger payload through to handler",
    "returns ok:false with error when handler returns failure outcome"
   ],
   "orchestra/autonomous-approval-adapter.test.ts": [
    "first request returns pending and queues the trigger",
    "repeated request for same trigger does not duplicate queue entry",
    "after accept(), next request returns approved and clears the queue",
    "after reject(), next request returns rejected and clears the queue",
    "repeated requests without accept/reject NEVER return approved",
    "time alone does not auto-approve — clock advances make no difference",
    "persists pending entries and a fresh adapter hydrates from disk",
    "accept and reject also call executor.resolveApproval",
    "accept() on an unparked trigger throws APR_UNKNOWN_REQUEST and persists no decision",
    "reject() on an unparked trigger is refused the same way",
    "writes a durable audit record for the refused attempt",
    "a genuinely parked trigger still accepts (guard does not block the real flow)",
    "a separate gate instance accepts an id parked on shared disk (cross-process, no false refusal)"
   ],
   "orchestra/autonomous-audit-adapter.test.ts": [
    "record writes an event to the stream file (record→event written)",
    "all AuditRecord fields are preserved in the event payload",
    "each record() call is tmpdir-isolated — second sink sees zero events"
   ],
   "orchestra/autonomous-authority-adapter.test.ts": [
    "allowed-map: brain reading returns allowed",
    "needs_approval-map: worker writing to unknown target returns needs_approval",
    "denied-map: unrecognized requestedBy subject returns denied",
    "bilinmeyen→default-deny: empty requestedBy returns denied without calling checkAuthority",
    "system requestedBy is treated as brain role and can read",
    "worker:tenant prefix resolves to worker role"
   ],
   "orchestra/autonomous-runtime-loop.test.ts": [
    "runs exactly N ticks when maxIterations is set, then returns \"maxIterations\"",
    "idle tick (no_trigger) awaits the configured intervalMs",
    "active tick yields to the event loop with sleep(0), not intervalMs",
    "pre-aborted AbortSignal stops cleanly with zero iterations",
    "signal aborted mid-loop stops cleanly after the in-flight cycle",
    "invokes onTick observer for every cycle result",
    "denied-cycle: unknown tenantId → authority denies, audit JSONL records \"denied\"",
    "needs_approval → cycle halts at \"pending\", executor not called, audit records \"pending\"",
    "idle real wire (no flows) → no audit events, all sleeps are intervalMs",
    "(a) pending outcome sleeps intervalMs — busy-spin prevented for approval-gate entries",
    "(b) executed outcome sleeps 0 — fast re-tick preserved for real dispatched work"
   ],
   "orchestra/autonomous-runtime.test.ts": [
    "executes action when authority allows, writes audit, no approval gate call",
    "returns no_trigger and skips downstream calls when source is idle",
    "approved → executes action + audit(\"executed\")",
    "rejected → does NOT execute, audit(\"rejected\")",
    "pending → does NOT execute, audit(\"pending\")",
    "does NOT call approval or executor, audit(\"denied\")",
    "records audit(\"failed\") with executor error when action.ok=false",
    "calls nervousObserver.tick() once per cycle (happy path)",
    "tick() error is swallowed — cycle completes normally (fail-safe)",
    "tick() is called even when trigger source returns null (no_trigger cycle)",
    "absent nervousObserver leaves all existing cycle behavior unchanged",
    "constructs a NervousObserverDep from nervousTick and passes it into deps",
    "constructed observer tick fires once per cycle when cycle runs",
    "absent nervousTick returns deps unchanged (backward-safe)",
    "throwing nervousTick does NOT break the cycle (fail-safe via runAutonomousCycle)"
   ],
   "orchestra/autonomous-tenant-authority.test.ts": [
    "keeps solo-mode pending and approval persistence byte-identical",
    "refuses a foreign tenant exactly like an unknown request and leaks no pending metadata",
    "does not consume another tenant decision when trigger ids collide",
    "refuses a tenant-less strict principal with the canonical typed error"
   ],
   "orchestra/autonomous-trigger-adapter.test.ts": [
    "converts a due scheduled flow into an AutonomousTrigger with mapped fields",
    "returns null when no flows are registered",
    "returns null when policy.disabled === true (even if flows are due)",
    "returns null on a re-tick within the same minute (scheduler state preserved)",
    "default policy → trigger payload carries requiresApproval=true (DEFAULT_GUARD)",
    "explicit guard requiresApproval=false propagates through to payload",
    "yields one trigger per next() call in scheduler order, then null"
   ],
   "orchestra/avgcoverage-repair.test.ts": [
    "excludes coverage-less results from the average (phantom-zero-dilution fix)",
    "blends new coverage-bearing samples with prior real history, excluding this sprint\\'s non-covered task from the weight",
    "treats a genuine 0% coverage result as a real sample, not a measurement gap",
    "writes skill avgCoverage — previously never computed at all (always stayed 0)",
    "still writes successCount alongside avgCoverage (no regression to the existing field)",
    "updateAgentStats(coverage=null) advances totalUses/successRate but leaves avgCoverage untouched",
    "updateAgentStats(coverage=<number>) still blends normally (no regression)",
    "updateSkillStats(coverage=null) advances totalUses/successRate but leaves avgCoverage untouched"
   ],
   "orchestra/barrel-exports.test.ts": [
    "exports runSprint",
    "exports readContext",
    "exports planSprint",
    "exports confirmDraftTasks",
    "exports buildWorkerPrompt",
    "exports cleanup",
    "exports runDecay",
    "exports BrainError class",
    "exports isSessionActive",
    "exports ensureSession",
    "exports spawnWorker",
    "exports killWorker",
    "exports attach",
    "exports destroy",
    "exports setupWatchWindow",
    "exports createWatchLayout",
    "exports attachToWorkerPane",
    "exports TmuxError class",
    "exports registerUpdater",
    "exports runAllUpdaters",
    "does NOT export startAuditor (internal tmux helper)",
    "does NOT export sendKeys (internal tmux helper)",
    "does NOT export listWorkers (internal tmux helper)",
    "does NOT export buildPlanPrompt (internal planner helper)",
    "does NOT export parsePlannerResponse (internal planner helper)",
    "does NOT export callBrainPlanner (internal planner helper)",
    "does NOT export pauseSprint (internal sprint lifecycle)",
    "does NOT export resumeSprint (internal sprint lifecycle)",
    "does NOT export checkAndAutoPause (internal sprint lifecycle)",
    "does NOT export checkAndAutoResume (internal sprint lifecycle)",
    "does NOT export isStaleTaskFile (internal task file utility)",
    "does NOT export getChannelRegistry (internal IPC registry)",
    "does NOT export registerWorkerChannel (internal IPC registry)",
    "does NOT export unregisterWorkerChannel (internal IPC registry)",
    "does NOT export evaluateResultDI (internal DI variant)",
    "does NOT export waitForResultsDI (internal DI variant)",
    "does NOT export TmuxBackend (internal spawn backend)",
    "does NOT export SubprocessBackend (internal spawn backend)",
    "does NOT export SpawnBackendFactory (internal factory)",
    "does NOT export SpawnBackendError (internal error class)",
    "does NOT export changelogUpdater (internal doc updater instance)",
    "does NOT export sprintLogUpdater (internal doc updater instance)",
    "does NOT export readmeMetricsUpdater (internal doc updater instance)",
    "does NOT export healthCheckUpdater (internal doc updater instance)",
    "does NOT export getRegisteredUpdaters (internal registry query)",
    "does NOT export clearUpdaters (internal registry mutation)",
    "exports exactly the expected public functions (no extra runtime symbols)",
    "public surface count is within expected range (no export explosion)"
   ],
   "orchestra/baseline-tracker.test.ts": [
    "writes baseline JSON to .deckent/sprint-NNN-baseline.json",
    "creates .deckent directory if it does not exist",
    "returns null for non-existent baseline",
    "detects new failures (delta > 0) → claimValid = false",
    "validates claim when no new failures (delta == 0)",
    "validates claim when failures decreased (delta < 0)",
    "parses standard vitest summary output",
    "returns null for unparseable output",
    "triggers on \"pre-existing failures unrelated to this task\"",
    "triggers case-insensitively",
    "triggers on \"already failing\" variant",
    "does NOT trigger on unrelated usage of \"pre-existing\"",
    "returns false for empty notes",
    "returns false for normal worker notes",
    "returns triggered=false when notes have no trigger phrases",
    "returns violation=true when trigger detected and delta > 0",
    "returns violation=false when trigger detected but delta == 0",
    "returns violation=false when no baseline exists (graceful degradation)",
    "returns violation=false when current capture fails",
    "returns a Promise (non-blocking — was spawnSync, froze the event loop)",
    "parses the injected async runner output into a baseline",
    "reads vitest output from stderr too (vitest writes the summary there)",
    "degrades to null when the runner yields no parseable output"
   ],
   "orchestra/brain-budget-decay.test.ts": [
    "returns 0 when DB is missing",
    "returns 0 when no resolved items exist",
    "returns count of all resolved items",
    "returns 0 when DB has only active debt",
    "counts resolved entries correctly with mixed statuses",
    "closes the store after querying",
    "handles all resolved items (returns full count)",
    "calls store.decay when force=true and returns counts",
    "skips decay when under budget and force=false"
   ],
   "orchestra/brain-context-model-label.test.ts": [
    "returns provider/apiId for known claude model (opus)",
    "returns provider/apiId for known claude model (sonnet)",
    "returns provider/apiId for known codex model (gpt-4.1)",
    "returns provider/apiId for known gemini model (gemini-2.5-pro)",
    "returns the original id when model is not in registry",
    "does not throw for empty string",
    "opus is premium tier in registry",
    "haiku is economy tier in registry",
    "sonnet is standard tier in registry",
    "model distribution shows provider/ prefix for known model",
    "model distribution shows codex/ prefix for codex model",
    "unknown model is displayed without crash and appears in output",
    "mixed known and unknown models all appear"
   ],
   "orchestra/brain-context.test.ts": [
    "formats full stack",
    "returns \"Unknown stack\" for empty stack",
    "omits missing fields",
    "handles language-only stack",
    "appends stack info to directives when cache exists",
    "returns context unchanged when no stack cache",
    "returns context unchanged on read error",
    "does not mutate original context",
    "returns \"No agents available.\" for empty list",
    "formats agent table with stats",
    "formats multiple agents",
    "includes table headers",
    "returns context unchanged for empty agents",
    "appends agent stats to directives",
    "does not mutate original context",
    "returns \"No skills available.\" for empty list",
    "formats skill table with stats",
    "formats multiple skills",
    "includes table headers",
    "returns context unchanged for empty skills",
    "appends skill stats to directives",
    "does not mutate original context",
    "formats basic history data",
    "includes NoGo patterns",
    "limits to 3 NoGo patterns",
    "limits to 5 task types",
    "enforces max 500 chars",
    "handles zero success rate",
    "handles 100% success rate",
    "returns context unchanged when no sprints dir",
    "appends history to directives when sprint files exist",
    "returns context unchanged when sprint dir is empty",
    "does not mutate original context",
    "uses default sprintRange of 5"
   ],
   "orchestra/brain-coverage.test.ts": [
    "returns NO_GO when self-assessment is NO_GO",
    "returns GO_WITH_TECH_DEBT when self-assessment is GO_WITH_TECH_DEBT",
    "returns NO_GO when testsPassed is false",
    "returns DONE for doc task regardless of coverage",
    "returns DONE when coverage >= 90 (no vitest output)",
    "returns GO_WITH_TECH_DEBT when coverage < 90 (no vitest output)",
    "returns DONE when reported coverage matches actual (within 5%)",
    "returns DONE when reported coverage exactly matches actual",
    "returns GO_WITH_TECH_DEBT when reported coverage differs from actual by > 5%",
    "returns GO_WITH_TECH_DEBT when worker over-reports coverage significantly",
    "returns GO_WITH_TECH_DEBT when vitest JSON cannot be parsed",
    "returns DONE for doc task even with mismatched vitest JSON (doc tasks skip validation)",
    "returns DONE when vitest output is empty string (no validation, falls through to coverage check)",
    "returns GO_WITH_TECH_DEBT when coverage < 90 even when vitest matches (< 90 fallback)",
    "returns NO_GO when self-assessment is NO_GO even with valid vitest JSON",
    "returns NO_GO when testsPassed false even with valid vitest JSON",
    "validates coverage within 5% boundary — exactly 5% diff is still valid",
    "validates coverage with diff just over 5% — should be GO_WITH_TECH_DEBT",
    "returns true for docs-only scope",
    "returns false for src scope"
   ],
   "orchestra/brain-crash-injection.test.ts": [
    "S1: SIGTERM mid-EXECUTE — restoreSprintFromCheckpoint returns action=resume-evaluate",
    "S2: unhandledRejection carrying API key — redactSensitive scrubs it (T-001)",
    "S4: state.json desync + checkpoint @EVALUATE — restoreSprintFromCheckpoint re-syncs state.json (T-003 + T-004)",
    "S5: No checkpoint file — restoreSprintFromCheckpoint returns action=fresh (no false-positive)",
    "S6: writeEvaluationAudit throws — runEvaluatePhase still completes (T-003 fail-soft)"
   ],
   "orchestra/brain-eval-integrity.test.ts": [
    "coverage:null + new test file + selfAssessment=DONE under refactorer → DONE (the Sprint 206 happy path)",
    "agent-independence: refactorer / bug-fixer / generic all evaluate the SAME result to the SAME decision (idempotency invariant)",
    "NaN guard: non-finite coverage (NaN / undefined) must NOT propagate to totalScore (Sprint 207 P0-3)",
    "source-only change + coverage:null under generic agent → still NO_GO (Sprint 153/154 anti-regression guard preserved per commit 854eb63f)",
    "idempotent: evaluating the same result twice yields byte-identical decision and totalScore (no FIX-retry mask)",
    "source-only refactorer (no test file) → still DONE via P0-2 allowlist bridge (proves P0-1 + P0-2 are additive)"
   ],
   "orchestra/brain-integration.test.ts": [
    "calculateModelScore is the same function",
    "inferModelFromDirective is the same function",
    "resolveTaskModel is the same function",
    "calculateModelScore returns consistent results via both paths",
    "inferModelFromDirective returns consistent results via both paths",
    "createTask is the same function",
    "extractScopeFromDirective is the same function",
    "parseStructuredDirectives is the same function",
    "buildWorkerPrompt is the same function",
    "plannerTaskToParams is the same function",
    "createTask produces identical output via both paths",
    "handleEvaluation is the same function",
    "handleCrossDependencies is the same function",
    "escalateDebt is the same function",
    "resolveDebt is the same function",
    "runDecay is the same function",
    "decay is the same function",
    "trimMemoryWithHeader is the same function",
    "writeRetrospective is the same function",
    "writeSprintLog is the same function",
    "calculateMetrics is the same function",
    "updateProjectDocs is the same function",
    "trimMemoryWithHeader produces identical results via both paths",
    "model-selector + task-builder: resolveTaskModel output used in createTask",
    "all exported functions are actual functions"
   ],
   "orchestra/brain-integrity-regression.test.ts": [
    "export-non-empty: decisions.md contains ADR IDs after writeGuardedExports",
    "decay-keeps-window: entries within the window and undated entries survive store.decay()",
    "rubric-varies-not-78.75: coverage:null tasks score differently based on quality signals",
    "combined: seed DB, export non-empty, decay safely, rubric varies — all three fixes"
   ],
   "orchestra/brain-ipc.test.ts": [
    "returns a ChannelRegistry instance",
    "returns the same registry instance on repeated calls",
    "registerWorkerChannel adds channel to the registry",
    "unregisterWorkerChannel removes channel from the registry",
    "unregisterWorkerChannel closes the channel when removing",
    "unregisterWorkerChannel is a no-op for unknown taskId",
    "registering multiple channels works independently",
    "sends PAUSE via IPC when channel is registered for a task",
    "does NOT call proc.send when no IPC channel is registered (tmux fallback)",
    "writes .paused file as fallback when no IPC channel exists",
    "writes .paused file even when IPC channel exists (dual mode)",
    "only sends PAUSE to channels for tasks that are pauseable",
    "updates sprint status to PAUSED after pauseSprint",
    "updates dashboard to show PAUSED status",
    "IPC send errors are swallowed (channel stays stable, file marker still written)",
    "returns a PauseState with correct sprintId and pausedTaskIds"
   ],
   "orchestra/brain-pause-resume.test.ts": [
    "calls killWorker for EXECUTING tasks without an IPC channel (tmux backend)",
    "calls killWorker for PENDING tasks without an IPC channel",
    "calls killWorker for CLAIMED tasks without an IPC channel",
    "calls killWorker for TESTING and DOCUMENTING tasks without an IPC channel",
    "does NOT call killWorker when an IPC channel is registered (subprocess backend)",
    "sends IPC PAUSE via channel for subprocess backend workers",
    "does NOT call killWorker for DONE tasks",
    "does NOT call killWorker for NO_GO tasks",
    "continues gracefully when killWorker throws",
    "kills multiple tmux workers in a single pause call",
    "writes .paused marker with taskId, previousStatus, and pausedAt",
    "records PENDING as previousStatus when task was PENDING",
    "records TESTING as previousStatus when task was TESTING",
    "writes task JSON with PAUSED status alongside .paused marker",
    "calls updateDashboard with SprintStatus.PAUSED",
    "dashboard preserves sprint phase during pause",
    "dashboard alert level is WARNING with the pause reason",
    "dashboard progress.active is 0 after pause",
    "dashboard progress.blocked equals number of paused tasks",
    "transitions all PAUSED tasks to PENDING",
    "leaves DONE tasks unchanged on resume",
    "leaves NO_GO tasks unchanged on resume",
    "sets sprint status to ACTIVE after resume",
    "writes updated task JSON with PENDING status",
    "removes .paused marker file for each resumed task",
    "removes pause-state.json on resume",
    "sends IPC RESUME to subprocess workers that have a registered channel",
    "sends IPC RESUME to all subprocess workers",
    "does NOT send RESUME for DONE tasks even if channel exists",
    "continues gracefully when IPC resume throws",
    "does not send RESUME when no IPC channel registered (tmux backend)",
    "calls updateDashboard with SprintStatus.ACTIVE",
    "dashboard progress.active equals number of resumed tasks",
    "dashboard progress.blocked is 0 after resume",
    "dashboard alerts are empty after successful resume",
    "EXECUTING → PAUSED → PENDING full cycle (tmux worker gets killed then must re-spawn)",
    "IPC subprocess: pause sends PAUSE, resume sends RESUME",
    "DONE tasks survive the full pause/resume cycle unchanged",
    "mixed tmux+IPC workers: each handled correctly"
   ],
   "orchestra/brain-planning-precedence.test.ts": [
    "top-level absent → preset \"%s\" own brain_planning is preserved (today\\'s behavior)",
    "explicit top-level \"structured\" overrides preset \"%s\" even when the preset itself is \\'auto\\'",
    "deckent-dev manual-mask regression: top-level=\"structured\" + modes.performance.brain_planning=\"structured\" (both explicit, agreeing) → still \"structured\"",
    "both top-level and preset brain_planning absent → falls back to \"auto\"",
    "preset=structured, top-level absent → structured path taken (today\\'s behavior preserved, AI planner NOT invoked)",
    "preset=structured, top-level=\"ai\" (explicit override) → AI path taken (the original bug: this used to be silently ignored)",
    "preset=ai, top-level absent → AI path still taken (preset-only behavior unchanged)"
   ],
   "orchestra/brain-provider.test.ts": [
    "returns null when no provider is registered",
    "returns the default registered provider",
    "handles any error from providerRegistry gracefully",
    "uses provided spawnBackend.spawn() for each task",
    "does NOT call ensureSession when spawnBackend is provided",
    "does NOT call spawnWorker (tmux) when spawnBackend is provided",
    "fails closed when the legacy tmux path cannot meter the task budget",
    "passes autoApprove to spawnBackend",
    "passes allowedTools derived from task scope",
    "handles multiple tasks up to maxWorkers",
    "uses spawnBackend.list() instead of listWorkers()",
    "calls spawnBackend.kill() for each active worker",
    "does NOT call killWorker (tmux) when spawnBackend is provided",
    "uses legacy tmux killWorker when no backend provided",
    "archives .tasks/.prompt-*.txt hidden tmpfiles into the canonical sprint namespace",
    "does NOT archive another run\\'s prompt files (sprint-ownership filter)",
    "does not archive non-prompt hidden files",
    "handles cleanup errors gracefully (does not throw)",
    "SpawnBackendFactory.create is callable and accessible from brain module",
    "spawnWorkers with spawnBackend does not call SpawnBackendFactory.create"
   ],
   "orchestra/brain-rebuild-gate.test.ts": [
    "emits BUILD_STALE_WARNING when dist is 1 hour newer than sprint-state",
    "does NOT emit when dist is older than sprint-state",
    "does NOT emit when dist and sprint-state mtimes are equal",
    "skips check and returns dist-missing when dist file is absent",
    "skips check and returns state-missing when sprint-state file is absent",
    "is fail-safe when statSync throws — no warning, no crash",
    "computes ageSeconds correctly for large gaps (24h)",
    "emits with target=user and source=sprint (channel routing contract)"
   ],
   "orchestra/brain-rollback.test.ts": [
    "returns \"never\" for empty evaluations",
    "returns \"auto\" when all tasks are NO_GO",
    "returns \"ask\" when some tasks are NO_GO",
    "returns \"never\" when all tasks are DONE",
    "returns \"never\" when all tasks are GO_WITH_TECH_DEBT",
    "returns \"auto\" for single NO_GO",
    "returns \"ask\" for mix of DONE and NO_GO",
    "createSafetyPoint is called when rollback is enabled (default)",
    "createSafetyPoint is NOT called when rollback: false",
    "safety point is saved to disk after creation",
    "rollback is triggered when all tasks are NO_GO",
    "recordRollbackInDebt is called after successful rollback",
    "no rollback on partial success (mix of DONE and NO_GO)",
    "no rollback when all tasks succeed",
    "deleteSafetyPoint called after successful sprint (no rollback)",
    "deleteSafetyPoint NOT called when sprint is rolled back",
    "sprint.rolledBack is false/undefined when rollback: false",
    "is exported from brain.ts",
    "is exported from brain.ts",
    "is exported from brain.ts",
    "is exported from brain.ts",
    "empty evaluations returns never",
    "is exported from brain.ts"
   ],
   "orchestra/brain-self-learning.test.ts": [
    "suggests planning mode change when NO_GO rate > 50%",
    "suggests testing skill when coverage < 40%",
    "suggests max_workers increase when duration > 1 hour",
    "returns empty array for healthy sprint",
    "detects files that appear in NO_GO tasks across 3 sprints",
    "returns empty for files in < 3 sprints",
    "returns empty when fewer than 3 sprints provided",
    "only considers last 3 sprints",
    "produces markdown with sprint score",
    "includes config suggestions when present",
    "includes recurring files when present"
   ],
   "orchestra/brain-skill.test.ts": [
    "planSprint returns Sprint object with tasks",
    "no skill pool → NO ghost skill is injected (project-conventions retired, CATALOG-STATS-AUTHORITY-001)",
    "tasks get assignedSkills when skills exist in pool",
    "skill selection is non-fatal when import fails",
    "assignedSkills contains correct skill ids",
    "disabled skills are not assigned",
    "multiple tasks can each get different skills",
    "planSprint returns correct sprint id format",
    "planSprint is an async function",
    "skill selection does not affect task count",
    "tasks still have correct status after skill selection",
    "task files written to disk include assignedSkills when present",
    "empty skill pool does not break planSprint"
   ],
   "orchestra/brain-task-assign-fresh-read.test.ts": [
    "always reads from disk (no cache)",
    "throws if task.json missing",
    "reads full task payload (id, scope, etc.)",
    "returns block decision and emits BRAIN→SPAWN:BLOCKED event",
    "event payload preserves reason and detectedAt context"
   ],
   "orchestra/brain.test.ts": [
    "has name BrainError",
    "stores phase property",
    "HOLDs an explicit ADR when the memory database is absent",
    "bounds full-corpus FTS discovery while preserving explicit ADR authority",
    "reads all brain files",
    "returns empty string for missing files",
    "skips corrupt task JSON files",
    "handles git status command error",
    "parses git ls-files into fileTree",
    "reads debt from MemoryStore as DebtItem array",
    "returns empty tasks when .tasks/ does not exist",
    "creates task with correct ID format",
    "sets status to PENDING",
    "pads sequence to 3 digits",
    "sets isPriorityFix when provided",
    "extracts src/ directory paths",
    "extracts tests/ directory paths",
    "extracts .ts file paths to filesWrite",
    "extracts .js file paths to filesWrite",
    "returns empty scope for directives without paths",
    "deduplicates paths",
    "always returns empty filesRead",
    "includes .tasks/task-{id}.result instruction",
    "includes task id and title",
    "includes selfAssessment options",
    "shows canonical read scope when available",
    "shows fallback message when no scope directories",
    "preserves single quotes in prompt (tmux handles escaping)",
    "includes explicit vitest run instruction",
    "includes explicit tsc verify instruction",
    "references WORKER-GUIDE.md for error handling instructions",
    "marks the result file as mandatory (never exit without it)",
    "reselects bounded memory when directives change after context capture",
    "auto-increments sprint number from sprints dir",
    "writes task JSON files to .tasks/",
    "creates .tasks/ directory",
    "creates priority fix tasks for CRITICAL debt",
    "keeps normal directive work while surfacing legacy and malformed CRITICAL debt holds",
    "plans all tasks regardless of maxWorkers (queue mechanism handles parallelism)",
    "throws BrainError on deadlock detection",
    "strips \"- \" prefix from directive lines",
    "extracts scope from directive paths",
    "mode=structured uses structured parse (no AI call)",
    "mode=auto tries AI, falls back to structured on null",
    "mode=auto uses AI result when available",
    "mode=ai throws BrainError when AI returns null",
    "asDraft=true creates tasks with DRAFT status",
    "Sprint includes reasoning and planningMode fields",
    "mode=auto falls back when AI returns fewer tasks than directives (8 vs 12)",
    "mode=auto accepts AI result when task count matches directives (12 vs 12)",
    "mode=auto falls back when AI returns fewer tasks (5 vs 10)",
    "mode=auto accepts AI result when directives have no structured tasks",
    "mode=ai does NOT fall back even when AI returns fewer tasks",
    "fallback sets planningMode to \"fallback\"",
    "CRITICAL debt tasks are preserved alongside fallback tasks",
    "falls back to structured when AI returns fewer tasks than directives (task-count-low)",
    "AI null + auto still falls back to structured (existing behavior)",
    "mode=structured ignores AI entirely and plans all structured tasks",
    "changes DRAFT tasks to PENDING",
    "skips non-DRAFT tasks",
    "uses initialStatus when provided",
    "defaults to PENDING when initialStatus not provided",
    "calls ensureSession first",
    "does NOT call startAuditor (scan loop runs in-process)",
    "spawns one worker per task",
    "includes task id in worker prompt",
    "updates dashboard after spawning",
    "returns immediately when all results exist",
    "returns empty array when no results and timeout=1",
    "handles corrupt result files gracefully (typed parse-failure, sprint-549)",
    "collects results for multiple tasks",
    "returns partial results on timeout",
    "does not include duplicates",
    "polls until result appears (async behavior)",
    "returns Promise (is async)",
    "returns true for docs/ scope",
    "returns true for docs/ subdirectory scope",
    "returns true for tmp-test/ scope",
    "returns true for scripts/ scope",
    "returns true for root-level (./)",
    "returns false for src/ scope",
    "returns false for tests/ scope",
    "returns false for lib/ scope",
    "returns false for mixed scope (docs/ + src/)",
    "returns false for mixed scope (scripts/ + tests/)",
    "returns false when directories is empty",
    "returns false when scope is undefined",
    "returns NO_GO when selfAssessment is NO_GO",
    "returns GO_WITH_TECH_DEBT when selfAssessment says so",
    "returns DONE for successful result",
    "overrides DONE to NO_GO when testsPassed=false",
    "overrides DONE to GO_WITH_TECH_DEBT when coverage < 90",
    "is pure — does not call any I/O functions",
    "doc task (docs/ scope) skips coverage check — low coverage returns DONE",
    "doc task (tmp-test/ scope) skips coverage check — returns DONE",
    "doc task (scripts/ scope) skips coverage check — returns DONE",
    "doc task still returns NO_GO when testsPassed=false",
    "mixed scope (docs/ + src/) uses normal coverage evaluation",
    "DONE: updates status and releases locks",
    "GO_WITH_TECH_DEBT: updates status, releases locks, inserts debt via MemoryStore",
    "NO_GO: updates status to NO_GO, does NOT release locks",
    "NO_GO: creates fix task JSON file",
    "uses task.id as workerId fallback when assignedWorker is undefined",
    "returns empty list when no NO_GO tasks",
    "creates fix task for suspect dependency",
    "handles multiple dependencies",
    "writes fix task JSON files",
    "increments sprintsOpen for unresolved items",
    "escalates to HIGH at 2 sprints",
    "escalates to CRITICAL at 3+ sprints",
    "does not touch resolved items",
    "writes to .brain/sprints/ directory",
    "creates sprints directory",
    "respects SPRINT_LOG_MAX_LINES limit",
    "counts DONE, DEBT, and NOGO correctly",
    "calculates coverage average",
    "calculates noGoRate correctly",
    "handles empty sprint",
    "does nothing when lines <= 300",
    "removes resolved patterns",
    "removes resolved debt items",
    "archives old sprint logs keeping last 2",
    "creates archive directory",
    "kills all workers via listWorkers",
    "releases locks for assigned workers",
    "deletes ALL task extensions (.json, .plan, .hb, .result, .paused, .log)",
    "deletes task files for sprints below 100 (padded-prefix authority)",
    "deletes .lock files",
    "does not throw when worker kill fails",
    "handles stale files older than 24h",
    "skips non-task files during cleanup",
    "returns true for files older than maxAgeMs",
    "returns false for recent files",
    "returns false when statSync throws",
    "uses custom maxAgeMs",
    "returns a sprint with COMPLETE status",
    "sets startedAt and completedAt",
    "throws BrainError on PLAN phase failure",
    "parks the sprint PAUSED in FIX when a task result is missing",
    "skips FIX phase when all tasks are DONE",
    "recovers from RETRO/DECAY errors",
    "passes autoApprove opts to spawnWorkers",
    "returns empty array when no structured sections",
    "parses Görev sections into tasks",
    "extracts scope from Kapsam lines",
    "extracts directory scope from matching lines",
    "falls back gracefully when sections have no valid title",
    "passes autoApprove: false by default (not haiku_allowed)",
    "passes autoApprove: true when spawnOpts.autoApprove is true",
    "passes autoApprove: false when spawnOpts.autoApprove is false",
    "RunSprintOptions accepts sandboxMode without error",
    "RunSprintOptions allows omitting sandboxMode",
    "haiku_allowed in config is not conflated with autoApprove in RunSprintOptions",
    "skips decay when force=false and under budget",
    "runs decay when force=true even under budget",
    "runs decay when over budget without force",
    "delegates to store.decay with correct sprint number",
    "returns correct DecayResult structure",
    "writes evaluation result instead of task.status when evaluations provided",
    "falls back to task.status when no evaluations provided",
    "decay() delegates to runDecay()",
    "preserves single quotes in prompt (tmux handles escaping)",
    "includes heartbeat file path (.hb)",
    "includes heartbeat workerId hint",
    "mentions CODING, TESTING, DOCUMENTING status values",
    "fails closed when heartbeat execution identity was not host-bound",
    "forbids inferring missing heartbeat identity",
    "mentions timestamp refresh instruction in heartbeat hint",
    "warns against placeholder text or locale strings for timestamp",
    "calls startScanLoop after spawn",
    "clears scanInterval during cleanup",
    "spawnWorkers does not import startAuditor",
    "calls resetDashboard after PLAN and before SPAWN",
    "continues sprint even if resetDashboard throws",
    "adds +3 score for cross-module scope (2+ directories)",
    "does not add score for single module",
    "adds +2 score for architectural keyword: refactor",
    "adds +2 score for architectural keyword: redesign",
    "adds +2 score for architectural keyword: migrate",
    "adds +2 score for architectural keyword: breaking",
    "adds +1 score for filesWrite.length > 5",
    "adds +2 score for filesWrite.length > 10",
    "adds +3 score for filesWrite.length > 15",
    "subtracts -2 score for docs/ scope (only docs)",
    "subtracts -2 score for config scope (only config)",
    "does not subtract for mixed docs and source scope",
    "subtracts -1 score for single directory scope",
    "subtracts -1 score for test-only task",
    "does not penalize integration tests in mixed scope",
    "returns opus for score >= 4: cross-module + architecture",
    "returns opus for score >= 4: many files",
    "returns haiku for score <= -1: doc task",
    "returns haiku for score <= -1: test-only",
    "returns haiku for score <= -1: simple config",
    "returns sonnet for score between -1 and 4: simple feature",
    "returns sonnet for score between -1 and 4: moderate refactoring",
    "returns sonnet for score between -1 and 4: medium file count",
    "correctly scores Task 019-004 (this task): score-based model selection",
    "correctly scores Doc Task: BRAIN-GUIDE.md",
    "correctly scores Task Queue Fix: cross-module",
    "updates CHANGELOG.md with new sprint entry",
    "writes new CHANGELOG if file does not exist",
    "updates SPRINT-LOG.md with new sprint section",
    "updates README.md sprint count",
    "does not throw when CHANGELOG write fails",
    "handles GO_WITH_TECH_DEBT in highlights",
    "uses fallback text when no tasks are DONE or DEBT",
    "skips README update when file does not exist"
   ],
   "orchestra/budget-landing-checkpoint-quote-safety.test.ts": [
    "atomically writes a valid task identity",
    "reproduces the legacy raw single-quoted JSON EOF failure"
   ],
   "orchestra/build-violation-guard.test.ts": [
    "returns null when dist/ does not exist",
    "returns fileCount 0 for an empty dist/",
    "counts nested files and tracks the max mtime",
    "reflects a new file added after the initial snapshot",
    "reflects a file removed after the initial snapshot",
    "reflects an existing file rewritten in place (same name, bumped mtime)",
    "false when both null (dist/ absent before and after)",
    "false when identical snapshots",
    "true when fileCount differs",
    "true when maxMtimeMs differs",
    "true when dist/ appeared (null -> non-null)",
    "true when dist/ disappeared (non-null -> null)",
    "patches distMutated:true into an existing .result when mutated",
    "adds no field when not mutated",
    "no-ops when .result does not exist (never blocks the worker)",
    "tolerates malformed JSON in .result without throwing",
    "flags .result.distMutated + warns when dist/ was mutated during the container run",
    "adds no distMutated field and does not warn when dist/ was untouched",
    "never throws / never blocks even when there is no .result to patch (advisory-only)"
   ],
   "orchestra/buildworkerprompt-projectroot-thread.test.ts": [
    "renders the fixture ADR sourced from the caller-supplied projectRoot, not process.cwd()",
    "renders the fixture ADR sourced from the caller-supplied projectRoot when dispatching a dependency-just-satisfied task"
   ],
   "orchestra/c0c-collision-live-fire.test.ts": [
    "strips leading \"./\" prefix",
    "collapses repeated forward slashes",
    "strips trailing slash",
    "composes leading-dot + double-slash + trailing-slash",
    "drops empty / whitespace-only entries",
    "is idempotent (already-canonical paths pass through)",
    "handles readonly input arrays (TypeScript variance)",
    "paralel 2 task identical filesWrite → collision detected",
    "scope normalize: ./src/foo vs src/foo recognized as same logical file",
    "double slash + trailing slash collapse to canonical",
    "emits BRAIN→SPAWN:BLOCKED when path variants alias the same file",
    "does not spawn a later writer while the first writer is already EXECUTING",
    "allows a FIX writer after its colliding original reached terminal NO_GO",
    "prioritizes an appended priority fix over a dependency-blocked colliding dependant",
    "fills a free slot instead of letting a blocked writer consume the worker ceiling"
   ],
   "orchestra/canonical-attribution-consumer.test.ts": [
    "credits an exact-file selector",
    "does not credit files merely because a directory is in scope",
    "compiles the write field to exact-file selectors only",
    "credits an exact-file selector even when the file is absent from the inventory",
    "fails closed on a portable path collision rather than crediting one arbitrarily",
    "credits nothing when the compiled scope selects nothing"
   ],
   "orchestra/canonical-wiring-closure.integration.test.ts": [
    "joins repaired lineage, collision serialization, provider observation and terminal CLI/MCP truth"
   ],
   "orchestra/cascade-circuit-breaker.test.ts": [
    "pauses a pure NOT_DISPATCHED lineage after its one re-dispatch authority is exhausted",
    "pauses when post-FIX unresolved roots meet both count and ratio gates",
    "does NOT scale the absolute gate for a three-task sprint (scale-honest, sprint-546)",
    "does not pause when the unresolved ratio is below policy",
    "pauses when one exhausted root below the ratio gate blocks an unfinished dependant",
    "does not count cascade-skipped tasks that never consumed a provider attempt",
    "parks a below-threshold FAILED lineage instead of allowing receipt HOLD",
    "does not park a fully completed lineage"
   ],
   "orchestra/cascade-unblock-wire.test.ts": [
    "runEvaluatePhase: NO_GO with CODE failure cascades to 2 dependents (PAUSED)",
    "runEvaluatePhase: RUNTIME failure does NOT cascade (dependents stay PENDING)",
    "runEvaluatePhase: DONE result emits no cascade event",
    "runFixPhase: result-only DONE cannot unblock without verification-isolation authority",
    "runFixPhase: fix NO_GO does NOT unblock dependents (stay PAUSED)"
   ],
   "orchestra/catalog-stats-outcome-truth.test.ts": [
    "collects V3 and FIX attempts once each and prefers carried result identities",
    "credits current delivered identities and fails closed on malformed current receipts",
    "counts every V3/FIX terminal attempt exactly once in the sidecar",
    "leaves unused entity recency unchanged and preserves legacy unknown fields",
    "publishes one complete state with one temp write and one rename",
    "parses real comms counters from a result carrying sharedNotes/handoffNotes/handoffsReceived",
    "defaults comms counters to zero/false for a legacy result missing comms fields",
    "never throws on malformed comms shapes and still defaults to zero/false",
    "collectCatalogStatsTerminalOutcomes attaches commsUsage per outcome",
    "records commsUsage once per taskId/attempt in the sidecar with single-write atomicity preserved",
    "preserves a legacy sidecar without a commsUsage section (byte-compatible upgrade)"
   ],
   "orchestra/changelog-update.test.ts": [
    "extracts Added/Changed/Fixed prefixed lines (case-insensitive)",
    "returns empty buckets for empty/null input",
    "ignores prefix-shaped lines with empty body",
    "appends a new sprint entry with Keep a Changelog format",
    "routes Added: / Changed: / Fixed: prefixes from result.notes into their sections",
    "skips duplicate entries when the sprint version header already exists",
    "falls back to title heuristic when notes carry no category prefix",
    "deduplicates repeated Added: lines within the same sprint",
    "keeps GO_WITH_TECH_DEBT annotation when notes have no Changed: hint"
   ],
   "orchestra/checkpoint-cascade-restore.test.ts": [
    "already-MRR task survives restore, direct+transitive PENDING descendants cascade-skip, zero spawn across the whole snapshot",
    "a PENDING task whose .result already exists on disk is committed to NO_GO without rewriting the result",
    "does not overwrite or commit a cascade target while its Docker settlement is pending",
    "projects a closed NO_GO outside activeWorkers before cascading descendants",
    "a second restore call is a true no-op: no re-decision, no result rewrite",
    "writeCheckpoint/readCheckpoint still round-trip schemaVersion=2 + taskStates",
    "recovers an already-MRR task missing from v1 buckets and cascade-skips its PENDING dependent"
   ],
   "orchestra/checkpoint-loop.test.ts": [
    "1. computeEventStreamOffset returns max sequence from events.jsonl",
    "2. writeCheckpoint partitions tasks into completed/pending/active disjoint sets",
    "3. writePhaseCheckpoint increments checkpointNumber on subsequent calls",
    "4. writePhaseCheckpoint reflects brainPhase parameter (not sprint.phase) on disk",
    "5. Atomic rename leaves no .tmp leftover after a successful write",
    "6. computeEventStreamOffset returns 0 when events.jsonl is missing",
    "7. computeEventStreamOffset returns 0 when events.jsonl is empty",
    "checkpoint.notify_pending_title interpolates {phase} in en and tr",
    "checkpoint.notify_escalation_title/summary interpolate {phase}/{summary}/{elapsedMinutes} in en and tr",
    "checkpoint.notify_timeout_title/summary interpolate {phase}/{summary}/{timeoutMinutes} in en and tr",
    "en lang never leaks the Turkish literal (guards against a re-introduced hardcode)",
    "waitForHumanApproval still resolves false on bounded timeout after the getMessage migration (regression smoke)"
   ],
   "orchestra/checkpoint-mrr-restore.test.ts": [
    "writes schemaVersion=2, full taskStates (incl. fixForTaskId), remainingQueue, lastDecisionSeq=0",
    "writer reverts to the exact pre-v2 shape when DECKENT_CHECKPOINT_V1=1",
    "MRR task present in restoredSprint.tasks; PENDING dependent cascade-skipped",
    "preserves interrupted work as PENDING even when its scoped tree has untracked files",
    "returns the interrupted task and dependent to the resumable PENDING queue",
    "recovers an already-MRR task the v1 buckets never captured, from persisted task-*.json records",
    "does not pull in a task-*.json belonging to a different sprintId"
   ],
   "orchestra/ci-baseline-honest.test.ts": [
    "preserves old baseline when 0-pass suspicious and old exists",
    "writes real baseline when testPassed > 0",
    "writes baseline on first run even when 0-pass (no old to preserve)",
    "is idempotent — calling twice with same valid data yields same result",
    "writes baseline when testFailed is 0 (all pass — not suspicious)",
    "treats 0-pass/0-fail as not suspicious (empty suite — write it)"
   ],
   "orchestra/ci-reporter.test.ts": [
    "upserts retro entry to DB when store is provided",
    "is idempotent — second call does not duplicate",
    "skips when no CI report file exists",
    "is a no-op when no store is provided",
    "upserts memory entry to DB when store is provided",
    "upsert replaces existing entry on second call",
    "is a no-op when no store is provided"
   ],
   "orchestra/cleanup-stale-hb.test.ts": [
    "sweeps a foreign task-*.hb whose sidecar json belongs to the previous (dead) sprint",
    "preserves a task-*.hb belonging to a different (live) sprint",
    "sweeps an orphan task-*.hb with no sidecar json at all",
    "sweeps .timeout and .partial-result markers attributable to the previous sprint",
    "preserves .timeout/.partial-result belonging to the live sprint",
    "does not touch the sidecar task-*.json of a swept orphan (out of this sweep\\'s scope)",
    "is idempotent and safe when .tasks/ is empty",
    "is idempotent and safe when .tasks/ does not exist at all",
    "removes a stale (>24h) .timeout marker regardless of cleanupPhase",
    "removes a stale (>24h) .partial-result marker regardless of cleanupPhase",
    "preserves a fresh (non-stale) .timeout/.partial-result marker"
   ],
   "orchestra/cleanup-state-truth.test.ts": [
    "cleanup('sprint-end') removes .dashboard and ci-baseline.json",
    "cleanup('sprint-end') removes only the settled sprint's task artifacts",
    "cleanup('sprint-end') retires only the settled sprint's process-suffixed temp residue",
    "cleanup('spawn-fail') preserves both for post-mortem",
    "cleanup('sprint-end') removes legacy locks but preserves execution authority",
    "cleanup('sprint-end') clears a dead coordinator sprint.lock",
    "cleanup('sprint-end') preserves another live process sprint.lock"
   ],
   "orchestra/codex-spawn-readiness.test.ts": [
    "scenario: cli-missing → not ready, authMode none",
    "scenario: cli-ok, auth-missing (no env key, no active session) → not ready",
    "scenario: cli-ok + env api key → ready without probing auth status",
    "scenario: cli-ok + subscription auth (no env key) → ready",
    "scrubs provider credentials from both local readiness subprocesses",
    "does not accept the logged-in substring inside a logged-out status",
    "treats a codex spawn error (binary not installed) as cli-missing",
    "image missing → backendRequired 'subprocess'",
    "image ready with codex CLI + ca-certs → no backendRequired",
    "image present but codex CLI missing → backendRequired 'subprocess'",
    "image present with codex CLI but missing ca-certificates → backendRequired 'subprocess'",
    "honors a custom image tag",
    "defaults to DEFAULT_WORKER_IMAGE when none is given",
    "never spawns a real codex process (pure, buildCommand only)",
    "proves the canonical API ID passes to --model unchanged",
    "proves the prompt-feed format: prompt is fed via `$(cat <promptPath>)`, not inline",
    "proves the output-format flag: CODEX_USAGE_EMIT_ARGS is --json (structured JSONL stream)",
    "a model with no registry apiId override falls back to the raw id unchanged",
    "composes host + docker + model-arg evidence into one report, all deps injected",
    "omits backendRequired/reason entirely when the docker image is fully ready",
    "supports a caller-specified model list for arg evidence"
   ],
   "orchestra/comms-instruction.test.ts": [
    "returns empty string when enabled is undefined (default-off)",
    "returns empty string when enabled is false",
    "returns non-empty string when enabled is true",
    "contains sharedNotes instruction when enabled",
    "contains handoffNotes instruction when enabled",
    "starts with the expected header when enabled",
    "does NOT include Worker Communications block when workerCommsEnabled is absent",
    "does NOT include Worker Communications block when workerCommsEnabled is false",
    "includes Worker Communications block when workerCommsEnabled is true",
    "includes sharedNotes and handoffNotes in the prompt when workerCommsEnabled is true",
    "places Worker Communications block AFTER the Karpathy section (END region)",
    "comms block is at the very end when no shared/handoff blocks are present"
   ],
   "orchestra/completed-checkpoint-terminalizer-events.test.ts": [
    "persists an ordered terminalization-only timeline without replaying evaluator or workers",
    "reuses dynamic fix-task evidence discovered by the canonical finalizer loader",
    "reuses only a Store-revalidated T11 receipt for an exact checkpoint task",
    "terminalizes the fresh canonical exact checkpoint instead of the stale restored checkpoint",
    "holds a completed checkpoint when exact custody exists without its Store reference",
    "rejects a forged public terminal verdict when Store marks the task exact but the checkpoint ref is missing",
    "fails closed when the Store discriminator cannot prove exact or legacy custody",
    "records the legacy safe-retention decision explicitly",
    "emits a typed resumable HOLD at the exact failed stage",
    "never terminalizes a checkpoint task whose result authority is absent",
    "does not append recovery HOLD evidence after an immutable staged seal exists"
   ],
   "orchestra/concurrent-verification-isolation.integration.test.ts": [
    "keeps two snapshot compiler verdicts attributed and foreign evidence retry-neutral"
   ],
   "orchestra/confirmation-durable-write.test.ts": [
    "applies the DONE downgrade only after a durable v2 create",
    "retains the rubric DONE and removes the route cause when durable create fails"
   ],
   "orchestra/conflict-resolver.test.ts": [
    "returns empty array for empty results",
    "returns empty array for single worker",
    "detects same_file_write conflict",
    "detects test_interference conflict",
    "detects scope_overlap when 2+ files overlap",
    "does not report scope_overlap for single file overlap",
    "no conflicts when workers touch different files",
    "handles three workers with shared file",
    "recognizes .spec.ts as test file",
    "recognizes __tests__ directory as test file",
    "handles workers with empty filesChanged",
    "last_writer_wins picks last worker",
    "first_writer_wins picks first worker",
    "manual returns unresolved",
    "returns unresolved for empty workers",
    "returns \"No conflicts detected\" for empty list",
    "generates report with conflict details",
    "pluralizes correctly for multiple conflicts"
   ],
   "orchestra/connector.test.ts": [
    "should register and retrieve a provider",
    "should return null for unregistered provider",
    "should overwrite a previously registered provider",
    "should return true for registered provider",
    "should return false for unregistered provider",
    "should return empty array when no providers registered",
    "should return all registered provider names",
    "should detect available provider in health check",
    "should detect unavailable provider in health check",
    "should report error when isAvailable throws",
    "should report missing auth for codex without OPENAI_API_KEY",
    "should report ok auth for codex with OPENAI_API_KEY set",
    "should report missing auth for gemini without GOOGLE_API_KEY",
    "should health check all registered providers when no name given",
    "should return error result for health check on unregistered provider",
    "should handle multiple providers registered simultaneously",
    "should unregister a provider",
    "should return false when unregistering non-existent provider",
    "should clear all providers",
    "should track provider count via size"
   ],
   "orchestra/container-path-sanitizer.test.ts": [
    "exports the portable project dir token",
    "exports the host project dir token alias",
    "exports the sanitized audit channel",
    "CONTAINER_WORKSPACE_RE is a global regex",
    "is idempotent — a second pass rewrites nothing",
    "positive: ${p}",
    "negative: ${n}",
    "normalizes windows separators",
    "handles a /workspace/-prefixed (container-absolute) host-facing path",
    "rewrites leaks across all host-facing file types",
    "is idempotent on re-run (totalRewrites=0)",
    "does NOT sweep non-host-facing files",
    "does NOT corrupt a .deckent/workspace path inside a host-facing file",
    "skips files that do not exist on disk"
   ],
   "orchestra/content-generators-counts.test.ts": [
    "returns code-derived count for src/core (must be > 90)",
    "returns code-derived count for src/orchestra (must be > 76)",
    "returns 0 for a non-existent directory",
    "count is consistent with readdirSync .ts file listing",
    "findGenerator matches \"architecture\" section title",
    "generates table with core/ and orchestra/ module counts from disk",
    "core/ module count in table is > 90 (not hardcoded old value)",
    "orchestra/ module count in table is > 76 (not hardcoded old value)"
   ],
   "orchestra/content-generators-numbers.test.ts": [
    "returns ≥32 — code-derived from server.ts deckent_ registrations",
    "returns 0 for a non-existent project root",
    "returns ≥49 — code-derived from cli/index.ts register imports",
    "returns 0 for a non-existent project root",
    "findGenerator matches \"deckent by the numbers\" and \"project status\"",
    "project-status generator output contains MCP count ≥32",
    "project-status generator CLI count matches cliCommandCount (no drift)"
   ],
   "orchestra/continuation-admission-modes.test.ts": [
    "admits a cumulative-semantics first observation with exact delta-from-zero arithmetic",
    "admits an incremental-semantics first observation with unchanged behaviour",
    "holds a cumulative first observation that proves no completed turn",
    "holds a missing first observation closed"
   ],
   "orchestra/continuous-dispatch-safety.test.ts": [
    "holds a re-dispatch candidate behind an active exact writer",
    "wires vanished-worker reaping before both initial and watcher collection"
   ],
   "orchestra/coordination-wire.test.ts": [
    "creates handoff with ready status when artifact files exist",
    "skips handoff for NO_GO results",
    "skips handoff when filesChanged is empty",
    "starts daemon when enabled=true (SPAWN-phase wire)",
    "stop() is called on daemon (CLEANUP-phase wire)",
    "opt-out: returns null and does not start when enabled=false",
    "flows a persisted per-sprint inventory into the rendered env-probe block",
    "renders byte-identical legacy output (no env-probe block) when no inventory was ever persisted",
    "stays undefined (no env-probe block, no throw) when the task carries no sprintId",
    "keeps wave-level stack commands out when a task-local Test command exists",
    "fails closed when no task-local verification command was compiled",
    "uses the same fail-closed proof hold when the stack resolves no commands"
   ],
   "orchestra/cost-gate-advisory.test.ts": [
    "EMITS a daily COST_LIMIT_WARN when cumulative spend EXCEEDS daily_max_usd (flag-on)",
    "spend UNDER the cap → no warn (emitter not called, returns null)",
    "enforce_spend_gate OFF (default) → no warn even when over cap (never blocked)",
    "EMITS a monthly COST_LIMIT_WARN when daily is within but monthly exceeds",
    "default emitter (no inject) on a breach: writes the event + console.warn, never throws",
    "missing ledger + missing config → no throw, returns null (fail-safe)",
    "reads spend + config and emits the checkSpendGate advisory (no disk touched)",
    "NON-BLOCKING: an injected emitter throw is swallowed — does NOT fail finalize",
    "NON-BLOCKING: a loadConfig throw is swallowed — null, emit never called",
    "execution continues after the hook (finalize is not aborted)"
   ],
   "orchestra/cost-guard-dispatch-gate.test.ts": [
    "DORMANT default (no cost_guard): returns undefined (gate stays open)",
    "DISABLED (cost_guard.enabled=false): returns undefined (inert)",
    "ENABLED but no threshold set: returns a monitor that never trips (max absent)",
    "ENABLED with threshold: returns a started monitor, dispatch NOT stopped before any tick",
    "guard NOT stopped → always false (normal loop untouched, break never fires)",
    "stopped BEFORE any dispatch (all still PENDING) → complete immediately",
    "stopped with in-flight tasks still running → wait (false)",
    "stopped and every in-flight task has reported → complete (true)",
    "stopped with no PENDING left (all dispatched) mirrors the normal all-collected break",
    "does not require PAUSED/PENDING tasks to manufacture results after a run-wide hold",
    "yields to repair when all remaining PENDING work is admission-held",
    "keeps EXECUTE open while an independently admitted task can still run",
    "never enters repair while a worker is still in flight"
   ],
   "orchestra/cost-guard-enabled-path.test.ts": [
    "trip stops new dispatch and completes gracefully before the bounded deadline",
    "trip completes the loop under timeoutMs=0 (unlimited) — live hang-fix proof",
    "control: enabled but under-threshold → identical topology DOES dispatch the dependent task",
    "nervous respawn drain under cost-stop: kills the stale worker but does NOT respawn",
    "task-level token exhaustion settles only that task and leaves the next slot dispatchable"
   ],
   "orchestra/coverage-validator.test.ts": [
    "returns null for empty string",
    "returns null for whitespace-only input",
    "returns null for invalid JSON",
    "returns null for non-object JSON",
    "parses direct coverage summary format",
    "calculates averageCoverage correctly",
    "parses total-wrapped format",
    "parses coverage wrapped in coverage field",
    "returns null for JSON missing coverage fields",
    "returns null for partial coverage (missing branches)",
    "handles 100% coverage correctly",
    "handles 0% coverage correctly",
    "exposes totals in result",
    "parses coverageMap.data format",
    "returns OK when reported matches actual exactly",
    "returns OK when diff is below threshold",
    "returns OK when diff equals threshold exactly",
    "returns WARNING when diff exceeds threshold",
    "returns WARNING when reported is lower than actual by more than threshold",
    "uses custom threshold when provided",
    "WARNING with custom threshold when exceeded",
    "includes reported and actual in result",
    "message includes coverage values",
    "handles edge case: both 0",
    "handles edge case: both 100",
    "returns false for src/ tasks",
    "returns false for tests/ tasks",
    "returns false for lib/ tasks",
    "returns true for docs/ only tasks",
    "returns true for .brain/ only tasks",
    "returns true for README.md tasks",
    "returns false when mixed src + docs",
    "returns false for empty directories",
    "returns true for src without trailing slash in non-src dir",
    "returns false for src without trailing slash",
    "returns false for tests without trailing slash",
    "returns null for doc-only tasks",
    "returns OK with note when no vitest output provided",
    "returns WARNING when vitest JSON cannot be parsed",
    "returns OK when coverage matches within threshold",
    "returns WARNING when coverage diverges by more than 5%",
    "uses custom threshold",
    "extracts line coverage from parsed vitest output for comparison"
   ],
   "orchestra/criterion-evaluation.test.ts": [
    "keeps semantic GO and NO_GO undecidable despite the referenced file existing",
    "decides a file-path go requirement from disk/result evidence",
    "a go requirement with provably absent evidence is decisive NO_GO input",
    "a SATISFIED no-go item is decisive (the failure condition provably holds)",
    "prose requirements stay honestly undecidable and never decide (legacy debt-fix shape)",
    "does not infer file authority from slashy legacy prose",
    "rejects unsafe explicit file locator %s as undecidable",
    "leaves explicit $kind requirements for a non-deterministic adapter",
    "returns null for tasks without typed items (no behaviour change)",
    "a decisive typed-contract failure caps DONE at NO_GO with criterion audit rows",
    "prose-only legacy items leave the verdict untouched",
    "salvage gates never soften a deterministic contract failure",
    "without projectRoot the kernel stays inert (pure-sync callers unchanged)",
    "marks an in-policy absent file unsatisfied without reading the host tree",
    "leaves an out-of-policy file undecidable instead of asserting absence",
    "accepts a regular file only when it is present in the committed final manifest"
   ],
   "orchestra/cross-backend-prompt-uniformity.test.ts": [
    "all 3 backends (docker, subprocess, tmux) document the persist contract",
    "all 3 backends reference the Sprint 168 C0e cross-backend contract marker"
   ],
   "orchestra/cross-sprint-analyzer.test.ts": [
    "returns stable for single value",
    "returns stable for equal values",
    "returns improving when second half is clearly higher",
    "returns deteriorating when second half is clearly lower",
    "returns stable when difference is within threshold",
    "returns empty report when learnings.json does not exist",
    "returns empty report when no sprints in learnings",
    "computes trend points from skill sprint history",
    "detects improving skill trend",
    "detects deteriorating skill trend",
    "computes agent success rates from outcomes files",
    "handles corrupt outcomes file gracefully",
    "respects n limit — takes last n sprints"
   ],
   "orchestra/cross-verify-admission-policy.test.ts": [
    "admits only bounded material lineages in risk then materiality order",
    "de-duplicates already verified and competing logical lineages",
    "fails closed for unavailable or same-provider verifier authority",
    "distinguishes materiality policy, bounded count, and finite budget exhaustion",
    "rejects malformed policy input without admitting a candidate"
   ],
   "orchestra/cross-verify-channel-truth.test.ts": [
    "binds approval request identity to the run attempt nonce",
    "persists the validation reason for a stale approval decision",
    "writes readable bootstrap and composition hold details",
    "reaches ready when the semantic response byte capacity equals the durable receipt cap",
    "persists bounded schema-rejected raw output and successful assertion breakdowns",
    "settles the twin projection for terminal %s"
   ],
   "orchestra/cross-verify-config-verifier-model.test.ts": [
    "dispatches the configured exact model instead of the tier-equivalent one",
    "reaches a premium judge for a standard-tier task — unreachable via tier equivalence",
    "lets an explicit caller flag outrank the configured identity",
    "only applies the entry for the provider actually selected as verifier",
    "keeps tier equivalence byte-for-byte when no entry is configured",
    "honestly skips on an unknown model ID rather than substituting one",
    "honestly skips a deprecated identity the tier path would have filtered out",
    "honestly skips when the configured ID belongs to a different provider"
   ],
   "orchestra/cross-verify-dispatch-rejection.test.ts": [
    "reports a refused dispatch as unavailable, carrying the provider wording",
    "leaves a real verdict alone even when the log also holds a refusal row",
    "keeps unclear when no verifier log was written at all",
    "keeps unclear when the log shows a failure but no refusal evidence",
    "carries verifier identity and the refusal structurally, not only in prose",
    "reports identity but no refusal when the verifier merely stayed unclear",
    "remembers the refused pair so the next sprint does not re-pay it",
    "skips a known-refused pair before spending the dispatch",
    "does not suppress a pair refused under a different auth mode",
    "does not remember a transient refusal",
    "keeps unclear when the verifier spoke before the run collapsed"
   ],
   "orchestra/cross-verify-docker-runtime-authority.test.ts": [
    "binds prepared execution, observes exact terminal evidence and replays after restart",
    "derives a missing actual-call receipt after restart from immutable contract and billing",
    "records LANDED as consumed D3 evidence and requires a fresh continuation authority",
    "holds unsupported percent windows before dispatch settlement",
    "frames a valid v2 two-line response (response then verdict)",
    "falls back to the lone verdict line for a valid v1 single-line protocol",
    "returns the verdict line (v1 shape) when the v2 JSON response is missing",
    "fails closed on a duplicated JSON response marker",
    "fails closed on a duplicated terminal verdict (v1 and v2 shapes)",
    "fails closed on a missing verdict and on a response after its verdict"
   ],
   "orchestra/cross-verify-docker-strict-launcher.test.ts": [
    "derives an immutable runtime identity from the local image and in-image CLI",
    "holds before a provider call when immutable image identity is unavailable",
    "leaves the ordinary worker prompt transport unchanged",
    "mounts only the host-authority prompt read-only and returns a two-field handle",
    "isolates typed v2 from the project and mounts only broker evidence read-only",
    "holds prompt byte drift before durable preparation or docker run",
    "re-verifies the immutable prompt after prepared evidence and before docker run",
    "re-verifies the immutable execution contract before docker run",
    "re-verifies the persisted task snapshot immediately before docker run",
    "holds a termination-binding failure before docker run"
   ],
   "orchestra/cross-verify-evidence-preparation.test.ts": [
    "holds an unreadable project model authority before provider or Docker access",
    "holds an inactive project model before provider authority or Docker inspection",
    "holds when the provider authority is absent or held",
    "holds with the exact backend reason when docker identity is unavailable",
    "fresh known∧reachable evidence skips APPROVAL but still runs the canonical refresh (7081 layer-2)",
    "holds on the owner budget profile before any approval is requested",
    "holds approval_authority_unavailable when no approval runtime is composed",
    "holds approval_undecided with the request id when no live decision arrives in the window",
    "D2b-2a micro-wiring: the poll tries the rules engine per tick and stays fail-soft",
    "drives claim → refresh with the owner projection and reports producer holds typed",
    "returns ready with evidence refs when the producer refresh succeeds"
   ],
   "orchestra/cross-verify-invocation-authority.test.ts": [
    "builds one deterministic exact candidate and immutable receipt projection",
    "changes deterministic identity across attempts without declaring either receipt",
    "changes deterministic identity across durable attempt and fence bindings",
    "keeps one attempt identity stable when evidence changes so declaration detects drift",
    "holds project, scope, canonical-model and evidence mismatches"
   ],
   "orchestra/cross-verify-invocation-coordinator.test.ts": [
    "holds before provider work when host authority is absent",
    "claims one exact route and settles receipt plus provider usage from host evidence",
    "dispatches and settles the non-reservable subscription arm from transport usage, forging no reservation",
    "parks route drift after dispatch and never accepts or settles the verdict",
    "holds prompt-contract drift before dispatch and invokes neither launcher nor observer",
    "holds substituted dispatched prompt bytes before dispatch",
    "rejects launcher-authored terminal fields before the host observer can consume them",
    "keeps the claimed reservation open when host observation cannot prove the call",
    "keeps a claimed reservation dispatched when terminal usage evidence is missing",
    "holds a missing provider-window mapper before dispatch",
    "rejects substituted quota-scope authority before receipt declaration or dispatch",
    "settles a no-call terminal only through termination-proven release evidence",
    "does not re-dispatch a prior exact invocation",
    "does not open fallback or retry when the exact executor throws after dispatch",
    "holds provider-limit estimate drift before dispatch and never invokes the launcher"
   ],
   "orchestra/cross-verify-outcome.test.ts": [
    "REFUTED → failCount incremented and successRate reduced for agent",
    "REFUTED leaves skill efficacy unchanged without skill-specific causal evidence",
    "REFUTED leaves agent+skill synergy unchanged without skill-specific causal evidence",
    "CONFIRMED → successCount incremented for agent performance",
    "CONFIRMED leaves skill efficacy unchanged without skill-specific causal evidence",
    "unclear verdict → learnings completely unchanged",
    "rejects a forged receipt instead of learning from provider-shaped prose",
    "recordOutcome without cross-verify call → behavior byte-for-byte as before",
    "null agentId → no efficacy projection changes"
   ],
   "orchestra/cross-verify-owner-tier-authority.test.ts": [
    "keeps requested-model floor #1 fail-closed before mandatory execution without authority",
    "admits the requested and actual called model and binds the decision ref into terminal settlement",
    "revokes the requested grant when the production composition calls a different model",
    "uses the same authority at resolved-model floor #3 without opening a CLI-only bypass",
    "never lets malformed same-provider authority weaken the floor"
   ],
   "orchestra/cross-verify-production-ingress-authority.test.ts": [
    "bootstraps a typed claim from structured criteria and an immutable broker snapshot",
    "touches no provider authority while cross-verification is disabled",
    "excludes the model-owned producer provider when task.provider is absent",
    "holds before provider authority when task.provider contradicts its model registry identity",
    "holds implementation verification before provider authority without a closed producer settlement",
    "accepts only the exact closed producer result before advancing to provider authority",
    "HOLDs before selector, claim, reservation or dispatch when profile authority is absent",
    "rejects an exact verifier model owned by another provider before profile resolution",
    "holds an inactive exact verifier from the project store before profile resolution",
    "holds an unreadable model authority before profile or provider resolution"
   ],
   "orchestra/cross-verify-progression.test.ts": [
    "absent stores → refresh → the exact candidate-gate reads flip from unavailable to known∧reachable",
    "expiry rollover: stale evidence stops satisfying the gate and a NEW epoch re-proves it",
    "negative-cooldown refusal: a failed probe blocks a premature new epoch with a typed cooldown",
    "every preparation hold reason renders a paired en+tr remedy naming the unblocking action"
   ],
   "orchestra/cross-verify-provider-observation-retirement.test.ts": [
    "retires only the exact closed verifier attempt, preserves a foreign row, and replays as a no-op",
    "holds rather than retiring from an open settlement claim",
    "fails closed when the exact settlement cannot be read",
    "fails closed when the existing observation database cannot be opened"
   ],
   "orchestra/cross-verify-task-settlement.test.ts": [
    "publishes host-adjudicated %s as a private immutable %s receipt",
    "writes terminal unavailable and resumable HOLD receipts instead of leaving PENDING"
   ],
   "orchestra/cross-verify-wire.test.ts": [
    "disabled config → skip \"disabled\", spawn never called",
    "enabled:false → skip \"disabled\"",
    "NO_GO evaluation → skip \"not-passing\", spawn never called",
    "default path writes audit budget provenance and forwards Docker execution options",
    "returns UNCLEAR without spawn when exact verifier artifacts cannot be prepared",
    "does not apply the Claude finite-verifier effort profile to another provider",
    "consumes a terminal Docker receipt and finalizes task projection from that receipt",
    "does not accept a verifier receipt without lifecycle closure as a verdict",
    "does not override a settled NO_GO with a later provider-log verdict",
    "follows a LANDED parent to its exact exhausted continuation without hiding either truth",
    "uses a provider-log verdict only as advisory evidence without synthesizing settlement",
    "waits for a provider log finalized shortly after the wrapper marker",
    "settles the verifier twin FAILED (never fabricated DONE) when no terminal verdict exists",
    "waits through finalization and rejects a verdict followed by a later assistant continuation",
    "enabled + high-stakes + 2 providers + CONFIRMED → runs, writes advisory, not refuted",
    "threads an explicit claim-adjudication operation class into the verifier prompt",
    "REFUTED verdict → refuted=true, advisory written, evaluation NOT downgraded",
    "unparseable verifier output → verdict \"unclear\" (honest non-result), still runs",
    "holds an injected same-provider composition before coordinator execution",
    "keeps %s diagnostic-only and refuses exact settlement authority",
    "allows only a typed host-derived confirmation with a durable verdict receipt",
    "fans a truthful 2,169-character typed confirmation into the production runner and binds its exact raw output to the durable receipt",
    "propagates an exact schema rejection through adjudication, result and report",
    "HOLDs when provider terminal CONFIRMED disagrees with typed host derivation",
    "projects a settled exact coordinator result without touching legacy spawn",
    "maps exact terminal %s without opening a second verifier",
    "does not promote provider CONFIRMED when host execution is %s",
    "persists mandatory confirmation without requiring a mutable public result",
    "maps exact coordinator $state to unavailable+blocked without fallback",
    "maps an exact coordinator fault to unavailable+blocked without legacy fallback",
    "keeps a completed exact call fail-closed when its private receipt path is occupied",
    "preserves post-dispatch truth when a reconciliation HOLD receipt conflicts",
    "keeps config default-off ahead of the exact coordinator",
    "does not let a legacy REFUTED spawn satisfy mandatory verification",
    "does not let a legacy CONFIRMED spawn fabricate mandatory success",
    "enforce_refuted=false (explicit) + REFUTED → blocked=false (advisory-only)",
    "holds enforced verification before dispatch when receipt authority is missing",
    "settles an explicit terminal UNCLEAR as an accepted verifier protocol result",
    "records malformed output as a consumer parse rejection without fabricating success",
    "settles a thrown spawn as failed transport and rejected consumer",
    "rejects an immutable receipt binding mismatch before declaration or dispatch",
    "accepts a contiguous fallback chain ending at the admitted exact candidate",
    "blocks an invalid fallback chain before declaration or provider work",
    "blocks a duplicate receipt declaration without a second provider call",
    "blocks before provider work when dispatch_started cannot be persisted",
    "does not expose a semantic verdict when terminal receipt settlement fails",
    "blocks enforced verification when only a provider-name list is supplied",
    "does not let exact evidence plus a legacy receipt bypass the mandatory coordinator",
    "blocks advisory exact verification for unknown reachability before dispatch",
    "blocks an advisory exact candidate whose model is not capability-equivalent",
    "does not promote registry/catalog presence into verifier eligibility",
    "uses catalog local economics for an Ollama verifier without owner remote budget",
    "final-only verifier without owner authorization → durable HOLD before any spend",
    "authorized final-only verifier carries the owner wall-clock containment into the spawn",
    "an incremental-usage verifier never receives a final-only containment grant",
    "cursor is in needsSpawnBackend → unmetered default backend HOLDs, codex-symmetric",
    "cursor eligibility evidence must name the exact spawn backend, codex-symmetric",
    "final-only cursor without owner authorization → durable HOLD before any spend",
    "authorized cursor draws its containment from the same grant codex does",
    "missing auditor budget policy → durable HOLD before verifier dispatch",
    "holds before verifier dispatch when the owner turn reserve cannot finish the finite protocol",
    "single provider (no different provider) → honest-skip, spawn never called",
    "low-stakes task with high_stakes_only=true (default) → skip, spawn never called",
    "low-stakes task with high_stakes_only=false → verifies anyway",
    "preserves exact API IDs and auditor budget for a Codex-authored Fable verification",
    "spawn throws → does NOT throw, records explicit unavailable evidence",
    "reports evidencePersisted=false when the canonical result is missing",
    "uses the production factory in both policy modes and preserves typed HOLD semantics",
    "records exact advisory UNCLEAR without allowing it to block settlement",
    "verifier_priority config is respected when several providers qualify",
    "rejects an explicit verifier model owned by a different provider before spawn"
   ],
   "orchestra/data-w1-tokenusage.test.ts": [
    "timeout (no disk evidence) → tokenUsage defined with outputTokens=0 (not null)",
    "timeout (no disk evidence) → provider carried from task",
    "estimateTokenUsage (disk-evidence heuristic) → outputTokens>0",
    "aggregateTokenUsage counts synthetic timeout result (failed-attempt cost visible)",
    "aggregateTokenUsage: mixed real+synthetic — synthetic failure is included"
   ],
   "orchestra/debt-chain-walk.test.ts": [
    "single-hop (direct fix, no ancestor task file): resolves only the immediate parent debt",
    "multi-hop (A-fix → B → C): resolves the WHOLE ancestor lineage to the root",
    "records the correct resolvedInSprintId on every hop",
    "cycle guard: a self-referential fixForTaskId resolves once and does not loop forever",
    "never resolves the fix task\\'s own debt (seed guard): firstAncestor === seed is a no-op",
    "does not double-resolve when a longer chain revisits an id (bounded by `seen`)"
   ],
   "orchestra/debt-db-accessor.test.ts": [
    "maps a DB debt entry to the DebtItem shape",
    "activeOnly excludes resolved debt",
    "returns an empty array when no memory.db is present",
    "writes the rollback record to the DB, not to .brain/DEBT.md",
    "is idempotent — a second call does not duplicate the record",
    "does not throw when no memory.db is present",
    "resolves the DB debt entry whose fix task completed",
    "leaves debt active when the fix task did not complete",
    "resolves multiple debt entries in one pass"
   ],
   "orchestra/debt-hold-production-wiring.test.ts": [
    "preserves an open hold through durable state, shared read model, and CLI projection"
   ],
   "orchestra/debt-injection-noop-echo.test.ts": [
    "(1) REPRODUCE: fix-wave origin + no-defect note → skipped as noop, not injected, not in `skipped`",
    "(2) REGRESSION: fix-wave origin + real-defect note → injected exactly as before",
    "(3) conjunctive guard A: no-defect note but NON-fix-wave origin → injected normally (content alone is insufficient)",
    "(4) conjunctive guard B: fix-wave origin but no no-defect marker → injected normally (origin alone is insufficient)",
    "(5) `-xfix` suffix (cross-fix tasks) also classifies as fix-wave origin",
    "(6) description carries the full debt note verbatim, not the old generic placeholder",
    "(7) `skippedNoop` and the existing `skipped` (verified-no-result) stay independent",
    "(8) long note is truncated in the task TITLE but kept full in the description"
   ],
   "orchestra/debt-injection-success-echo.test.ts": [
    "typed class success-echo → skippedNoop, no task, not in skipped",
    "legacy row (no class) with a pure-success note → skippedNoop via the text fallback",
    "mixed success+gap note (debt-561-002 live shape) is still injected — gap wins",
    "producer-typed standard class is authoritative — text fallback does not run",
    "injected task strips the evaluator preamble from the title and frames the description",
    "true for pure verification-evidence notes",
    "false when any gap language is present (residual/remaining)",
    "false for a note with no success signal at all (plain defect description)",
    "false for empty/undefined"
   ],
   "orchestra/debt-ledger-coverage.test.ts": [
    "self-DEBT: evaluation DONE + selfAssessment GO_WITH_TECH_DEBT still records a ledger entry",
    "evaluator-DEBT: evaluation GO_WITH_TECH_DEBT still records a ledger entry (regression guard)",
    "DONE + selfAssessment DONE: no ledger entry (common case unaffected)",
    "idempotent: pre-existing ledger row is not duplicated by the self-DEBT path",
    "idempotent: pre-existing ledger row is not duplicated by the evaluator-DEBT path"
   ],
   "orchestra/debt-manager-acceptance-route.test.ts": [
    "delegates create to transactional FWW with exact tenant/project lineage",
    "rejects a caller-supplied confirmation id that does not match the full lineage",
    "allows only canonical reducer APPLIED resolution to invoke the CAS",
    "performs no write for QUALIFIED or expired settlements",
    "does not let a rewritten source verdict imitate the exact resolve settlement",
    "leaves unrelated and generic debt byte-identical and fails closed on CAS mismatch"
   ],
   "orchestra/debt-manager-attempt-custody.test.ts": [
    "parks an exact result when immutable repair-birth context is absent",
    "births one no-clobber deterministic FIX only from exact terminal evidence",
    "creates no child for unchanged evidence or non-product failure",
    "treats the same no-clobber receipt as idempotent and a foreign child as HOLD",
    "holds exact XFIX without context and admits it only with failed and target authorities"
   ],
   "orchestra/debt-manager-fix-authority-wire.test.ts": [
    "accepted: persists inherited scope plus an authority fingerprint",
    "accepted: the fingerprint is deterministic across two identical calls (idempotent)",
    "worker prose cannot grant scope or create a birth-time PAUSED fix",
    "late paths in full notes remain diagnostic-only",
    "carries prior accepted repair lineage without recursively trusting prose",
    "no evidence paths in notes: repairAuthority stays accepted with an empty added delta"
   ],
   "orchestra/debt-manager.test.ts": [
    "DONE: updates task status to DONE and releases locks",
    "DONE: does not write debt file",
    "DONE: uses w-{id} as worker fallback when assignedWorker is missing",
    "GO_WITH_TECH_DEBT: updates task status to DONE",
    "GO_WITH_TECH_DEBT: writes debt entry via MemoryStore",
    "GO_WITH_TECH_DEBT: derives debt sprint_id from task id when task.sprintId is missing (B10)",
    "GO_WITH_TECH_DEBT: does not insert duplicate debt entry",
    "NO_GO: updates task status to NO_GO",
    "NO_GO: creates a fix task file",
    "NO_GO: fix task has isPriorityFix=true and CRITICAL priority",
    "NO_GO: fix task preserves model and skill constraints verbatim",
    "NO_GO: does not release locks",
    "identical typed acceptance failure after one FIX pauses instead of creating fix-fix",
    "returns empty array when no NO_GO tasks",
    "returns empty array when NO_GO task has no dependencies",
    "creates cross-fix task when NO_GO depends on DONE task",
    "cross-fix preserves model and skill constraints verbatim",
    "does not create a concurrent cross-fix while the NO_GO task direct fix is pending",
    "does not create a late cross-fix after the direct fix lineage settled",
    "creates cross-fix task when NO_GO depends on GO_WITH_TECH_DEBT task",
    "writes cross-fix task JSON file",
    "does not create cross-fix when NO_GO depends on NO_GO",
    "skips dependency not in sprint tasks",
    "does nothing when no debt entries in DB",
    "increments sprintsOpen for unresolved items",
    "escalates NORMAL to HIGH after 2 sprints open",
    "escalates to CRITICAL after 3+ sprints open",
    "does not escalate resolved items",
    "does not re-escalate CRITICAL items",
    "returns false when no debt entries in DB",
    "returns false when debt id not found",
    "returns false when debt is already resolved",
    "marks debt as resolved and returns true",
    "writes resolvedInSprintId correctly",
    "does not resolve acceptance-route debt through the generic unscoped path",
    "returns early (no decay) when below budget and no force",
    "runs decay when force=true even under budget",
    "runs decay when over budget",
    "calls store.decay with correct sprint number",
    "returns totalCount before and after decay",
    "closes the store even on forced decay",
    "is a function that can be called",
    "calls runDecay internally (no error thrown)",
    "does not return a value (void)",
    "handleEvaluation: GO_WITH_TECH_DEBT truncates long notes to 80 chars",
    "handleEvaluation: GO_WITH_TECH_DEBT with empty sprintId uses empty string",
    "handleCrossDependencies: empty sprint tasks returns empty array",
    "handleCrossDependencies: multiple cross-fix tasks from multiple NO_GO tasks",
    "escalateDebt: handles empty content gracefully (no parseDebtTable call expected)",
    "resolveDebt: with multiple items — only resolves the correct one",
    "runDecay: under budget — archivedSprints is empty",
    "parses sprint number from sprint-015 format",
    "parses sprint number from sprint-001 format",
    "returns zero counts from DB-first decay",
    "closes store after decay run",
    "closes store even when under budget (early return)"
   ],
   "orchestra/debt-origin-wiring-roundtrip.test.ts": [
    "round-trips canonical producer authority through private MemoryStore/readContext into an idempotent fix task",
    "marks malformed persisted origins invalid through readContext without aborting the plan context",
    "revalidates direct injection and keeps mixed valid, legacy, and tampered debts separately observable",
    "keeps a structurally malformed debt metadata record as an invalid held debt, not a global PLAN hold"
   ],
   "orchestra/debt-parse-fix.test.ts": [
    "parses a valid markdown table with 3 rows correctly",
    "returns empty array for empty string",
    "returns empty array for corrupt/invalid format without throwing",
    "returns empty array for header-only table (no data rows)",
    "writeRetrospective does not throw when DEBT.md contains markdown table"
   ],
   "orchestra/debt-preflight.test.ts": [
    "matches the live sprint-433/445/449 completion-report shapes",
    "does NOT match a note describing genuinely remaining work",
    "extracts tsc, npm run lint, and a vitest file command from prose",
    "strips the trailing prose punctuation of the live 449-008 shape (path followed by colon)",
    "accepts multiple consecutive test paths and stops at the first prose token",
    "never extracts prose, flags, traversal, or shell-metacharacter tokens",
    "dedups repeated mentions of the same command",
    "verifies + marks resolvable a claim-bearing debt whose evidence runs green",
    "keeps a debt whose evidence is red, stops at the first red, and annotates the failure",
    "reports timeout distinctly in the annotation",
    "claim without extractable evidence → no-evidence, kept, runner never called",
    "no completion claim → no-claim, never runs commands even if the note names some",
    "skips non-critical and already-resolved debts entirely",
    "vanished evidence path → stale-evidence: never auto-closed, annotation says re-point (live 445-013/017 shape)",
    "memoizes identical commands across debts — tsc named in 3 notes runs once",
    "exhausted time budget keeps the debt with an UNVERIFIED annotation (fail-open, never auto-close)",
    "stale-evidence annotation reaches the injected fix task in a dryRun plan",
    "reports ok:true for exit 0 and ok:false with the exit code otherwise",
    "kills and reports timedOut when the command exceeds its timeout",
    "reports ok:false (not a throw) for an unspawnable binary"
   ],
   "orchestra/debt-resolution.test.ts": [
    "resolves an existing unresolved debt and returns true",
    "sets resolved status and resolvedInSprintId via DB upsert",
    "returns false when debt ID is not found",
    "returns false when debt is already resolved (idempotent)",
    "returns false when DB does not exist",
    "does not modify other debt items (only target is resolved)",
    "closes the store after operation",
    "computes resolvedDebtCount correctly with debt parameter",
    "computes totalOpenDebt correctly with debt parameter",
    "returns 0 for debt fields when debt parameter is omitted (backward compat)",
    "DONE evaluation triggers debt resolution for task ID",
    "isPriorityFix + DONE resolves debt for fixForTaskId",
    "NO_GO evaluation does not resolve debt via DB",
    "resolveDebt handles multiple items and only resolves the target"
   ],
   "orchestra/debt364-followups.test.ts": [
    "TIMEOUT_WITH_WORK selfAssessment records class=timeout-partial with an honest ledger entry",
    "a normal evaluator GO_WITH_TECH_DEBT (not a timeout) still classes as standard (regression guard)",
    "class=timeout-partial CRITICAL debt is skipped, not injected as a fix task",
    "class=standard CRITICAL debt (contrast case) still gets a fix task injected"
   ],
   "orchestra/decay-config-wire.test.ts": [
    "honors caller-provided decaySprints (20) — does NOT fall to 8",
    "falls back to 8 only when decaySprints is undefined (legacy callers)",
    "wires decaySprints=20 from config when budget OVER (force-path)",
    "wires decaySprints=20 from config when budget OK (normal-path)"
   ],
   "orchestra/deck-worker-isolation.test.ts": [
    "shadows /workspace/.deck read-only when .deck exists",
    "emits NO mount when .deck is absent (avoids phantom host .deck)",
    "always mounts read-only (never rw — a worker must not write the shadow)",
    "is idempotent across workers — a second write must not throw EACCES",
    "a real container reads an empty .deck and a missing .deck is not phantom-created"
   ],
   "orchestra/dep-normalize.test.ts": [
    "normalizes a title-prefix ref into the written task-JSON as a slot-id (goCriteria)",
    "mutates the task objects in place (same array references)",
    "normalizes a \"Task N\" human-natural ref",
    "normalizes a pure-integer (0-based index) ref",
    "leaves an already-correct plan-slot id ref resolved to itself",
    "keeps timestamp-backed canonical ids across a second normalization pass",
    "preserves a timestamp-id high-fan-in barrier during idempotent normalization",
    "leaves tasks with no dependencies untouched",
    "WARNs to stderr and drops an unresolvable ref (non-strict default)",
    "aggregates warnings across multiple tasks",
    "throws on the first unresolved ref under { strict: true }",
    "does not throw under strict mode when every ref resolves"
   ],
   "orchestra/dep-parser-string-to-array.test.ts": [
    "parses a bare string into a single-element array",
    "parses a comma-separated list into a multi-element array",
    "parses a single-element JSON array literal",
    "parses a multi-element JSON array literal",
    "returns empty array for empty / whitespace / \"none\"",
    "trims whitespace around comma-separated values",
    "is idempotent — re-stringifying and re-parsing yields the same array",
    "falls back to comma-split when JSON parsing fails",
    "parses a JSON array dependencies line (Sprint 169 plan format)",
    "parses a multi-element JSON array dependencies line",
    "parses a bare-string dependencies line (back-compat)",
    "parses a comma-separated dependencies line (back-compat)",
    "returns undefined for missing line",
    "returns undefined for \"none\""
   ],
   "orchestra/dep-ref-loud.test.ts": [
    "resolves \"Task 1\" to the first task\\'s real id (1-based)",
    "resolves \"Task 2\" to the second task\\'s real id",
    "is case-insensitive (\"task 3\")",
    "tolerates extra internal whitespace (\"Task   2\")",
    "trims surrounding whitespace (\" Task 1 \")",
    "returns undefined for an out-of-range \"Task N\" (never throws)",
    "returns undefined for \"Task 0\" (1-based — there is no task 0)",
    "does not collide with the existing 0-based pure-integer form",
    "does not match \"Task\" without a trailing number",
    "does not match a ref that merely starts with the letters \"task\"",
    "\"Task 1\" binds to the first AUTHORED task, not the first debt prepend",
    "\"Task 2\" binds to the second authored task",
    "bare integer \"0\" (0-based) also skips debt prepends",
    "out-of-range against the directive sublist stays undefined",
    "explicit plan-slot id still reaches a debt task when intentionally named",
    "title-prefix labels keep binding by title, debt present or not",
    "resolveTaskDependenciesLoud carries the same directive-only indexing",
    "plan-slot id still resolves by exact id match",
    "title-prefix / token match still resolves",
    "reserved keywords still resolve to undefined",
    "resolves every ref with no warnings and no stderr output when all refs are valid",
    "de-duplicates resolved ids the same way resolveTaskDependencies does",
    "emits a \"[deckent] WARN: ...\" stderr line for an unresolvable ref",
    "drops the unresolved ref from `resolved` and records it in `warnings`",
    "reports one warning per unresolved ref while resolved refs still succeed",
    "throws on the first unresolved ref when options.strict is true",
    "does not throw when every ref resolves, even under strict mode",
    "defaults to non-strict (warn, not throw) when options is omitted",
    "defaults to non-strict when options.strict is explicitly false",
    "\"Task N\" now resolves via the shared resolveDependencyRef",
    "still silently drops an unresolvable ref (back-compat: no throw, no stderr contract)"
   ],
   "orchestra/dependencies-title-prefix-resolver.test.ts": [
    "resolves a title-prefix label to the task whose title contains it",
    "resolves a plan-slot ID by exact task.id lookup (back-compat)",
    "resolveTaskDependencies handles a mixed array (title-prefix + plan-slot)",
    "returns undefined for an unresolvable reference (and drops it in batch mode)",
    "title-prefix ref survives auto-debt prepend offset drift (Sprint 176/178 regression)",
    "does not match a longer prefix that merely contains the ref as a substring",
    "is case-insensitive",
    "matches a ref appearing mid-title when surrounded by non-word separators",
    "returns undefined when the task list is empty",
    "parses a JSON-array title-prefix directive line and resolves both refs",
    "parses a comma-separated mixed directive line and resolves all refs",
    "end-to-end: directive title-prefix ref is correct after auto-debt prepend"
   ],
   "orchestra/dependency-aggregate-fix-aware.test.ts": [
    "(a) getAggregateVerdict: main NO_GO + fix DONE returns DONE",
    "(b) emitDependencyResolvedByFix emits structured event",
    "(c) CHANNELS.DEPENDENCY_RESOLVED_BY_FIX channel defined",
    "(d) buildDependenciesBlock embeds both original + fix digest with aggregate marker",
    "(e) honest-gate intact: Brain re-evaluate UPDATE not blocked",
    "(f) production prompt consumes Brain-evaluated FIX lineage and grants dependency outputs read-only",
    "(g) FIX-of-FIX prompt inherits logical-root dependencies without changing dispatch topology",
    "(h) dependency projection consumes the newest redispatch audit verdict",
    "(i) normalizes object-form filesChanged into a dependency entry",
    "(j) renders object-form dependency paths instead of Pending in the worker prompt"
   ],
   "orchestra/dependency-blocked-debounce.test.ts": [
    "initial emit — first call for a (taskId, deps) tuple writes the event",
    "state-change emit — when unresolvedDeps changes for same taskId, a new event IS emitted",
    "spam suppress — repeated identical unresolvedDeps for same taskId does NOT emit duplicates",
    "order-independence — [A,B] and [B,A] are treated as the same blocked state",
    "clearDependencyBlockedState — explicit clear allows next emit for same taskId",
    "zero-deps auto-clear — emit with empty unresolvedDeps removes the entry",
    "cross-task isolation — T1 dedupe state does not block T2 emission",
    "emitDependencyBlockedIfChanged — explicit API mirrors writeEvent dedupe semantics"
   ],
   "orchestra/dependency-pipeline-integration.test.ts": [
    "spawns Wave 2 (T4) and Wave 3 (T5) tasks across ticks; no ghosts",
    "spawns B+C in the same tick after A DONE; D spawns only after both B+C DONE",
    "does not spawn dependent task when upstream is NO_GO; emits BRAIN→WORKER:DEPENDENCY_BLOCKED",
    "debt result mutates taskRef to TaskStatus.DONE; dependent task spawns",
    "respects slotsAvailable; 3rd eligible stays PENDING until slot opens",
    "throws DependencyCycleError when A→B and B→A; error.code matches",
    "error message and code carry forensic information",
    "drives a 3-tier (S1 reduction) fixture to fixpoint via driveToFixpoint helper"
   ],
   "orchestra/dependency-pipeline-wire.test.ts": [
    "respawnEligibleTasks returns [] when pipeline disabled (no wire activation)",
    "spawns Wave 2 task when all Wave 1 deps marked DONE; status → EXECUTING",
    "spawns Wave 3 task when Wave 1 + Wave 2 all DONE",
    "does not spawn Wave 2 task whose dep is NO_GO",
    "spawns only as many as slotsAvailable allows",
    "applyStatusMutation maps GO_WITH_TECH_DEBT → DONE so Wave 2 dep filter passes",
    "applyStatusMutation + respawnEligibleTasks in sequence yields eligible spawn",
    "applyStatusMutation maps NO_GO selfAssessment → TaskStatus.NO_GO",
    "respawnEligibleTasks is idempotent when slots are saturated"
   ],
   "orchestra/dependency-pipeline.test.ts": [
    "parses \"- Dependencies: 134-005\" into [\"134-005\"]",
    "parses comma-separated dependencies",
    "returns undefined for no line",
    "returns undefined for \"none\"",
    "returns undefined for empty value",
    "trims whitespace from IDs",
    "parses Dependencies line from DIRECTIVES block",
    "returns undefined dependencies when no Dependencies line",
    "does not spawn task with unresolved dependencies when pipeline enabled",
    "spawns task when all dependencies are DONE",
    "defers a dependent when a DONE dependency has NO settlement receipt yet (DEPENDENCY_RECEIPT_PENDING)",
    "spawns newly eligible tasks after dependency completes",
    "returns empty when pipeline disabled",
    "does not spawn tasks with still-pending deps",
    "throws DependencyCycleError for circular T1↔T2",
    "DependencyCycleError includes task IDs",
    "validates clean dependency graph without throwing",
    "spawns all PENDING tasks regardless of deps when pipeline disabled",
    "spawns all tasks when pipeline not configured (undefined)",
    "calls onWaveTransition with duration when tasks are respawned",
    "does not call onWaveTransition when no tasks spawned"
   ],
   "orchestra/dependency-scheduler.test.ts": [
    "should build graph with no dependencies — single wave",
    "should build linear chain — 3 waves",
    "should handle diamond dependency pattern",
    "should detect cycle — A→B→C→A",
    "should detect partial cycle — only some tasks in cycle",
    "should retain unresolved dependency refs so runtime enforcement blocks instead of silently spawning",
    "should add collision edges when includeCollisions=true",
    "should NOT add collision edges when includeCollisions=false",
    "should handle empty tasks list",
    "should handle complex multi-wave with mixed deps and collisions",
    "should build correct dependents (reverse edges)",
    "warns (debugLog) and retains an unresolvable dependency as a blocker",
    "does not warn when every dependency resolves to a real task id",
    "should mark tasks with no deps as eligible",
    "should block tasks with unresolved deps",
    "should unblock task when all deps are done",
    "should handle partial deps satisfied",
    "should handle empty candidate list",
    "should block direct dependents of failed task",
    "should cascade-block transitive dependents",
    "should NOT block tasks that do not depend on failed task",
    "should only block PENDING tasks — not EXECUTING ones",
    "should handle task with no dependents",
    "should unblock direct dependents when all deps are done",
    "should NOT unblock if other deps are still missing",
    "should only unblock PAUSED tasks — not PENDING or EXECUTING",
    "should handle task with no dependents",
    "should unblock multiple direct dependents at once",
    "should cascade block dependents on CODE failure",
    "should NOT cascade on RUNTIME failure — risk-taking retry policy",
    "should NOT cascade on AMBIGUOUS failure — risk-taking retry policy",
    "should emit event log for each BLOCKED transition",
    "should NOT emit events for RUNTIME failure (no cascade = no events)",
    "should be idempotent — calling cascade twice does not double-block",
    "should emit UNBLOCKED events via unblockDependents onTransition callback",
    "should handle task with no dependents — cascade returns empty",
    "should cascade transitively — grandchild tasks get blocked too",
    "orders appended 006-fix before blocked 007 for shared-write serialization",
    "keeps a blocked queue entry and fills the free slot from dispatchable work",
    "does not mark a task EXECUTING when backend dispatch did not spawn a worker",
    "should enforce correct wave ordering with deps + collisions",
    "should cascade block and then unblock on resolution",
    "round-trip: serialize then deserialize preserves graph structure",
    "serialized format contains sprintId and persistedAt",
    "round-trip: cycle graph preserves hasCycle and cycleTaskIds",
    "empty graph round-trips correctly",
    "starts with \"graph TD\"",
    "includes node definitions with wave labels",
    "includes dependency edges (parent --> child)",
    "includes classDef highlights for DONE and NO_GO status",
    "does not emit empty classDef when all tasks are PENDING",
    "handles hyphens in task IDs safely (replace with underscores)",
    "JSON persist: writes .deckent/sprint-NNN-depgraph.json",
    "Mermaid persist: writes .deckent/sprint-NNN-depgraph.mmd",
    "JSON content is valid JSON with correct structure",
    "loadDependencyGraph returns null when file does not exist",
    "loadDependencyGraph restores graph matching original",
    "persist is fail-safe — does not throw even on I/O error",
    "state consistency: loaded graph enforces same wave ordering as original"
   ],
   "orchestra/directive-parsing.test.ts": [
    "always returns an object with directories, filesRead, filesWrite",
    "filesRead is always empty array",
    "returns empty arrays for empty string",
    "extracts a single .ts file from a Dosya: line",
    "extracts a single .ts file from a Kapsam: line",
    "extracts multiple files from a comma-separated Kapsam: line",
    "extracts a src/ directory path",
    "extracts a tests/ directory path",
    "extracts a .js file path",
    "extracts test file paths (*.test.ts)",
    "returns empty scope for a pure prose line",
    "returns empty scope for a comment line (#)",
    "deduplicates repeated file paths",
    "deduplicates repeated directory paths",
    "handles the DEBT-002 scope line from real DIRECTIVES.md",
    "handles the DEBT-005 four-file scope line from real DIRECTIVES.md",
    "returns empty array for plain text (no headings)",
    "returns empty array for empty string",
    "returns empty array for content with only # headings (not ## Görev)",
    "returns empty array for content with ## but not Görev/Task pattern",
    "parses a single ## Görev N: block",
    "parses \"- Backend: docker|tmux|subprocess\" override (Sprint 252 PSL-1), ignores invalid",
    "parses \"- ModelEffort: <level>\" override (Sprint 252 F1-RE), distinct from Effort",
    "parses multiple ## Görev blocks",
    "extracts inline title from heading line",
    "strips leading \"- \" prefix from title when it is a list item",
    "includes description as the full block text",
    "parses ## Task N: (English heading) as well",
    "skips blocks whose title resolves to empty string",
    "ignores preamble before the first ## Görev heading",
    "extracts filesWrite from a Dosya: line",
    "extracts filesWrite from a Kapsam: line with single file",
    "extracts multiple files from a comma-separated Kapsam: line",
    "extracts directory from a Kapsam: line",
    "deduplicates files that appear in both Dosya: and Kapsam: lines",
    "filesRead is always empty for every parsed task",
    "returns empty scope when task block has no file references",
    "extracts testTarget from a \"- Test: ...\" line",
    "testTarget is undefined when no Test: line exists",
    "extracts testTarget from all tasks independently",
    "extracts testTarget from DIRECTIVES.md Görev 1 format",
    "extracts testTarget from DIRECTIVES.md Görev 2 format",
    "strips the \"- Test:\" prefix cleanly from testTarget",
    "handles \"Test:\" line without leading dash",
    "parses exactly 5 tasks from Sprint 2 DIRECTIVES.md",
    "task 1 title is \"waitForResults async polling (DEBT-004)\"",
    "task 2 title is \"haiku_allowed semantik düzeltme (DEBT-005)\"",
    "task 3 title is \"Config doğrulama entegrasyonu (DEBT-002)\"",
    "task 4 title is \"Directive parsing iyileştirme (DEBT-003)\"",
    "task 5 title is \"Worker prompt zenginleştirme\"",
    "task 1 scope includes src/orchestra/brain.ts",
    "task 2 scope includes all 4 files",
    "task 1 testTarget is \"async polling, timeout, partial result senaryoları\"",
    "task 2 testTarget is \"config validation, start komutu flag parsing\"",
    "task 3 testTarget is \"mock spawnSync çıktısı ile parsing testleri, hata senaryoları\"",
    "task 4 testTarget is \"yapılandırılmış directive parsing, fallback, edge case\"",
    "no task has undefined title",
    "no task has empty description",
    "handles content with only whitespace gracefully",
    "handles block with no lines after heading",
    "handles block with only whitespace after heading",
    "handles non-sequential numbering (## Görev 3: without 1 or 2)",
    "handles large block number (## Görev 99:)",
    "does not treat ## Hedef section as a task",
    "handles mixed Görev/Task headings in one document",
    "scope.filesRead is always [] regardless of content",
    "handles a Dosya: line with a parenthetical annotation",
    "returns an array (never throws) for any string input"
   ],
   "orchestra/directives-builder.test.ts": [
    "build → parseStructuredDirectives → reconstructBuildTask deep-equals the original intent",
    "produces a document header + goal that the parser ignores (no phantom tasks)",
    "is deterministic — identical input produces byte-identical output",
    "round-trips a task with no Model/Effort/Skills lines at all",
    "distinguishes Skills: none ([]) from an omitted Skills line (undefined)",
    "parses a hand-written ### goNogo block",
    "returns empty arrays when there is no ### goNogo heading",
    "round-trips planner-authored evidence requirements without changing display strings",
    "throws when intent.tasks is empty",
    "throws on an empty title",
    "throws on an empty files list (DISTINCT-FILE)",
    "throws on empty goCriteria or nogo",
    "throws when desc smuggles a \"## Task N:\" heading (would fracture the block split)",
    "throws when desc smuggles a reserved directive-label line (e.g. \"Model:\")",
    "throws when desc smuggles a \"### goNogo\" heading",
    "throws when a files/scope/deps/skills item contains the \",\" join delimiter",
    "a goCriteria/nogo item with the \";\" join delimiter round-trips via escaping (born-677 — eski hard-error öldü)",
    "throws when the top-level title/goal would fracture the first block split"
   ],
   "orchestra/directives-delimiter-safety.test.ts": [
    "does not throw when a goCriteria item contains the \";\" join delimiter (live case)",
    "does not throw when a nogo item contains the \";\" join delimiter",
    "round-trips the born-677 NL-target triple (\";\", \",\", newline+backtick) in goCriteria",
    "round-trips the born-677 NL-target triple in nogo",
    "keeps the emitted goCriteria/nogo line as a single physical line despite an embedded newline",
    "round-trips an item containing a literal backslash",
    "round-trips multiple delimiter-laden items in the same list without cross-contamination",
    "still round-trips plain items with no special characters (no regression)",
    "extractGoNogo recovers the NL-target triple directly from a hand-assembled description",
    "does not reject a goCriteria item that opens with a reserved directive label",
    "does not reject a nogo item that opens with a reserved directive label",
    "round-trips reserved-label goCriteria items losslessly (proof they cannot be mis-parsed)",
    "does not reject a criteriaItems evidence requirement that opens with a reserved label",
    "does not reject a criteriaItems statement that opens with a reserved label",
    "round-trips a reserved-label evidence requirement losslessly through the JSON channel",
    "round-trips Unicode line and paragraph separators through the criteriaItems JSON channel",
    "still rejects a reserved directive label in desc — that field is emitted raw",
    "still rejects a \"## Task N:\" heading smuggled into desc",
    "keeps a file path intact while a directory still gains its trailing slash"
   ],
   "orchestra/directives-files-to-scope.test.ts": [
    "parses an explicit `Files:` list into filesWrite",
    "leaves filesWrite empty when only `Scope:` directories are provided (no synthetic filenames)",
    "renders an open-formulation fallback when filesWrite is empty but directories are set"
   ],
   "orchestra/directives-restore-quirk.test.ts": [
    "root cause: a raw `git stash` on a dirty tree reverts an uncommitted DIRECTIVES.md to the PREVIOUS committed sprint content",
    "gap: DirectivesMidSprintProtection does not flag revert-to-a-different-real-sprint (only template/small-size corruption)",
    "silent propagation: archiveDirectives faithfully (and silently) archives the reverted stale content under the sprint-378 label",
    "control: archiveDirectives is NOT independently buggy — given the correctly-restored content it archives the correct content"
   ],
   "orchestra/directives-title-description-split.test.ts": [
    "extracts description from after `### Description` (skips title + metadata lines)",
    "renders title and description on separate lines (no \" — \" concatenation)",
    "preserves markdown structure (lists, bold) in description"
   ],
   "orchestra/dishonest-detector.test.ts": [
    "returns dishonest=false when claimed LoC matches git numstat",
    "returns dishonest=false when result has no filesChanged",
    "returns dishonest=false within tolerance band",
    "returns dishonest=false when claimed LoC < minLocThreshold",
    "flags claimed +200 vs actual +50 (75% deviation > 50%)",
    "respects custom tolerance",
    "sums git additions across multiple files",
    "flags when ALL claimed files have zero git delta",
    "does NOT flag when at least one file has real changes (partial-touch)",
    "flags when notes claim +N LoC disagrees with both result and git",
    "does NOT flag when notes claim agrees with one of result/git",
    "parseNotesClaims extracts +N LoC and \"added N lines\" forms",
    "detects the canonical \"+220 LoC outcome-tracker\" lie as NO_GO trigger",
    "invokes the sink with channel + payload when dishonest",
    "is a no-op when the finding is honest",
    "uses the channel constant BRAIN→AUDITOR:DISHONEST_RESULT_DETECTED",
    "returns honest=true when result matches git",
    "downgrades to NO_GO and emits audit event when dishonest",
    "does not emit when suppressEmit=true even on dishonest"
   ],
   "orchestra/disk-verify.test.ts": [
    "(a) returns hasDiskEvidence=false when git numstat sum is 0 and no untracked files",
    "(b) returns hasDiskEvidence=true with linesAdded=100 when numstat reports 100",
    "(c) returns hasDiskEvidence=true with untrackedFiles when ls-others reports new files",
    "(d) respects scope.filesWrite — production provider receives only those paths",
    "(l) returns hasDiskEvidence=false when git commands throw (fail-open)",
    "handles empty scope arrays without invoking the providers",
    "returns positive numstatSum after modifying a tracked file",
    "returns untracked file when a new file is created inside the directory",
    "fails open with linesAdded=0 when projectDir is not a git repo",
    "(g) flags filesChanged=[] when disk-verify finds evidence",
    "(h) returns honest=false for filesChanged=[] when disk is empty (legacy behavior preserved)",
    "preserves legacy short-circuit when diskVerify context is omitted",
    "(i) stale EXECUTING with on-disk changes → PENDING for non-terminal recovery",
    "(j) stale EXECUTING with empty disk → PENDING without synthetic NO_GO",
    "(k) does not emit a terminal mismatch event during non-terminal recovery",
    "(e) .timeout + .result missing + disk evidence → MANUAL_REVIEW_REQUIRED indicator",
    "(f) .timeout + .result missing + disk empty → synthetic NO_GO (legacy preserved)",
    "exposes DISK_VS_CLAIM_MISMATCH_CHANNEL with the canonical channel string",
    "(NEW-a) untracked NEW FILE only (no tracked diff) → hasDiskEvidence=true",
    "(NEW-b) untracked file outside scope.directories is not seen by the gate",
    "(NEW-c) mixed tracked diff + untracked file → both reflected",
    "(NEW-d) gitLsOthers throws (sandbox) → numstat alone still drives the gate",
    "(NEW-e) result-collector contract: linesAdded=0 + untrackedFiles=[X] → MANUAL_REVIEW + filesChanged=[X]",
    "untracked NEW file only (tracked unchanged) → MANUAL_REVIEW_REQUIRED + audit event",
    "reports a tracked modification with both added and removed lines from numstat",
    "counts a new untracked write file as all-added and lists it",
    "returns empty for a no-op task (already-fixed born-item shape) → caller keeps worker claim",
    "scopes to the WRITE authority — a sibling edit in a read-only directory is NOT attributed"
   ],
   "orchestra/dispatch-evaluate-race.test.ts": [
    "returns a PENDING task whose dependency is now DONE (the 271-013 victim)",
    "excludes a task whose dependency is not yet DONE",
    "excludes a ready task that is already assigned (idempotency / Bug F)",
    "excludes a PENDING task that has NO dependencies (overflow keeps its cadence)",
    "is aggregate-aware: a fix-task DONE satisfying the original dep id unblocks the dependent",
    "flags a deps-satisfied PENDING task with no heartbeat (about to take a false NO_GO)",
    "does NOT flag the task once it shows a dispatch signal (heartbeat on disk)",
    "dispatches a dependency-just-satisfied PENDING task before returning (no synthetic NO_GO)",
    "does not release a dependent from a raw worker DONE when host aggregate evaluation is NO_GO",
    "releases a dependent only after host aggregate evaluation accepts DONE",
    "closes honestly via timeout when a ready task cannot be spawned (spawn error) — no infinite wait",
    "keeps a remote dispatch fail-closed when the backend omits landing capability",
    "is an inert no-op for legacy callers without config (behavior preserved)"
   ],
   "orchestra/disposition-event.test.ts": [
    "writes a stable owner-outbox event and remains complete with Nervous off",
    "registers bilingual remediation messages",
    "bridges the same informational event only when Nervous is enabled"
   ],
   "orchestra/dna-filter-stat-credit.test.ts": [
    "excludes a DNA-filtered skill from assignedSkills credit and emits skill.dna_filtered",
    "leaves a DNA-relevant skill\\'s credit untouched and does not emit skill.dna_filtered for it",
    "does not filter when routing is not v2 (matches buildWorkerPrompt bypass)",
    "does not filter a single-skill task (matches buildWorkerPrompt\\'s length>1 gate)"
   ],
   "orchestra/doc-sync-ground-truth.test.ts": [
    "extracts every \"N agents\" / \"N agent\" occurrence",
    "returns no claims when text has no agent count",
    "does not partial-match digits embedded in larger numbers",
    "counts directory children under src/core/builtins/agents/",
    "returns -1 when the agents directory does not exist",
    "returns empty list when the overrides file is missing",
    "loads valid overrides from .deckent/ground-truth-overrides.json",
    "overrideApplies respects metric, value, and sprint window",
    "TEST 1 — flags mismatch when claim 16 != measured 15 and no override exists",
    "TEST 2 — active whitelist override suppresses mismatch",
    "TEST 3 — no claim in task description yields no false positive",
    "zero-tolerance: threshold is 1 (any single mismatch produces a violation)",
    "scanTasksForGroundTruthMismatches integrates with .tasks/ directory",
    "scan returns empty when ground truth not measurable (no agents dir)",
    "detects mismatches in directive descriptions before worker spawn",
    "respects whitelist overrides",
    "audits every task in a planner result and reports stale numeric claims",
    "returns overrides whose until_sprint has passed",
    "excludes overrides still within their until_sprint window",
    "treats current sprint == until_sprint as expired (overrideApplies uses strict <)",
    "returns empty for a malformed sprint id",
    "returns empty for an empty override list"
   ],
   "orchestra/doc-task-verification-scope.test.ts": [
    "task.type=audit (docs/audits/ report) gets doc-only VERIFY STEPS, never CRITICAL VERIFY STEPS/tsc/vitest",
    "an unset-type task whose scope shape is audit (docs/audits/, single .md) is still doc-only (fallback reuses rubric-registry)",
    "a document-write task with a declared **Test:** command names that exact command, not repo-wide tsc/vitest",
    "a document-write task with no declared command keeps the generic file-exists + optional-lint text",
    "a code-development task keeps CRITICAL VERIFY STEPS (DO NOT SKIP) with type-check + targeted-test guidance",
    "a code-development task with a declared **Test:** command keeps the TASK-DECLARED AUTHORITY block untouched",
    "a mixed scope (docs + src) task is NOT doc-only, even with no explicit type"
   ],
   "orchestra/doc-tracking-finalize-hook.test.ts": [
    "does nothing when sync_on_finalize is not set",
    "runs a sync when sync_on_finalize is true",
    "is fail-safe — never throws even if the root is unusable"
   ],
   "orchestra/doc-updaters/changelog-updater.test.ts": [
    "has correct metadata",
    "shouldRun returns true by default",
    "shouldRun returns false when tier1 disabled",
    "uses Keep a Changelog header when creating new file",
    "writes a sprint-only header, NOT a product-version header (0.100.0 rebaseline)",
    "writes the same sprint-only header even when there is no package.json",
    "DONE tasks go into Added section",
    "GO_WITH_TECH_DEBT tasks go into Changed section with note",
    "NO_GO tasks are not included",
    "fix tasks go into Fixed section",
    "bug tasks go into Fixed section",
    "includes task summary metrics",
    "inserts new entry before existing entries",
    "handles multiple task types in one sprint"
   ],
   "orchestra/doc-updaters/changelog.test.ts": [
    "has correct metadata",
    "targetFile is docs/CHANGELOG.md",
    "writes to docs/CHANGELOG.md path",
    "shouldRun returns true by default",
    "shouldRun returns false when tier1 disabled",
    "creates CHANGELOG with sprint entry when file does not exist",
    "inserts new entry before existing entries",
    "shows \"No completed tasks\" when all NO_GO",
    "includes GO_WITH_TECH_DEBT in highlights"
   ],
   "orchestra/doc-updaters/doc-updater-consistency.test.ts": [
    "sprintLogUpdater.targetFile is docs/SPRINT-LOG.md (not docs/archive/SPRINT-LOG.md)",
    "sprintLogUpdater writes to docs/SPRINT-LOG.md path (no archive)",
    "sprintLogUpdater result.file matches targetFile exactly",
    "changelogUpdater.targetFile is docs/CHANGELOG.md",
    "changelogUpdater writes to docs/CHANGELOG.md path",
    "changelogUpdater result.file matches targetFile",
    "changelogUpdater result.updated is always true",
    "changelogUpdater sprint entry includes correct sprint number format",
    "changelogUpdater sprint 058 entry formats with two digits",
    "sprintLogUpdater sprint 056 entry includes sprint header"
   ],
   "orchestra/doc-updaters/health-check.test.ts": [
    "has correct metadata",
    "shouldRun returns false for non-internal projects",
    "shouldRun returns false when tier2 disabled",
    "shouldRun returns true for internal project (file existence not required)",
    "shouldRun returns true for internal project with file present",
    "creates file when it does not exist",
    "uses consistent HEALTH_DOC_PATH in shouldRun and run",
    "updates metric table rows",
    "creates file with correct content when missing at run time",
    "returns skipped_no_changes when nothing matches"
   ],
   "orchestra/doc-updaters/metrics-updater.test.ts": [
    "has correct metadata",
    "shouldRun returns true when tier2 enabled and README exists",
    "shouldRun returns false when tier2 disabled",
    "shouldRun returns false when no README",
    "run updates sprint count in README",
    "run updates task count",
    "run updates success rate (prefix format)",
    "run updates success rate (suffix format)",
    "run returns updated:false when no changes needed",
    "run returns updated:false when README not found",
    "run handles README without metric patterns gracefully",
    "run preserves other README content",
    "run updates usage data when present in sprintResult",
    "run updates test count based on totalTasks"
   ],
   "orchestra/doc-updaters/readme-metrics.test.ts": [
    "has correct metadata",
    "shouldRun returns false when tier2 disabled",
    "shouldRun returns false when README does not exist",
    "shouldRun returns true when tier2 enabled and README exists",
    "updates sprint count in README",
    "updates coverage percentage",
    "does not fabricate or corrupt the test count from coverage (R5)",
    "returns skipped_not_found when file missing at run time",
    "returns skipped_no_changes when content unchanged"
   ],
   "orchestra/doc-updaters/registry.test.ts": [
    "starts empty",
    "registers an updater",
    "clearUpdaters removes all updaters",
    "runAllUpdaters runs all registered updaters",
    "skips updater when shouldRun returns false",
    "catches errors in updater run and returns error result",
    "returns results in registration order",
    "handles empty registry"
   ],
   "orchestra/doc-updaters/sprint-log.test.ts": [
    "has correct metadata",
    "targetFile matches actual write path (docs/SPRINT-LOG.md)",
    "writes to docs/SPRINT-LOG.md (not docs/archive/SPRINT-LOG.md)",
    "result.file matches targetFile",
    "shouldRun returns true by default",
    "shouldRun returns false when tier1 disabled",
    "creates new sprint log when file does not exist",
    "preserves an unrelated existing sprint section byte-for-byte",
    "replaces and coalesces every section with the exact sprint identity",
    "accepts ABORTED as explicit terminal receipt-backed projection input",
    "writes through a same-directory temporary file and atomic rename",
    "cleans up the temporary file and preserves rename failure",
    "includes metric table",
    "includes task list with evaluations"
   ],
   "orchestra/docker-auth-precedence.test.ts": [
    "subscription + ANTHROPIC_API_KEY set → does NOT forward ANTHROPIC_API_KEY",
    "api authMode + ANTHROPIC_API_KEY set → forwards ANTHROPIC_API_KEY",
    "codex provider (gpt-4.1) → metering HOLD before any credential forwarding",
    "gemini provider → metering HOLD before any credential forwarding",
    "claude provider + subscription → does NOT forward OPENAI_API_KEY even if set",
    "%s remains held in API mode until incremental Docker usage exists"
   ],
   "orchestra/docker-backend-fixpack.test.ts": [
    "converges a pre-existing read-only (0o400) shadow to writable 0o600 without throwing",
    "creates a fresh 0o600 shadow when none exists",
    "is idempotent across repeated calls sharing the same path (multi-worker sprint)",
    "resolves the configured \"documentation\" kind to the current live config value (1536m)",
    "resolves a differently-configured kind (\"code-development\") to its own limit",
    "returns undefined for an unconfigured kind (\"design\" is not in the live map)",
    "short-circuits to undefined when kindMemoryLimits is empty (today\\'s production reality — the wiring gap)",
    "returns undefined when the task JSON is missing (readTaskKind graceful degradation)",
    "constructor default is what an unconfigured/unresolved kind falls back to"
   ],
   "orchestra/docker-backend-owned-settlement.test.ts": [
    "prepares and dispatches a durable exact attempt when the caller supplies no ref",
    "reuses an exact caller ref idempotently and rejects a conflicting active attempt pre-dispatch",
    "leaves an ambiguous Docker ACK open for restart reconciliation instead of sealing not-dispatched"
   ],
   "orchestra/docker-budget-stream.test.ts": [
    "rejects a missing remote budget before any Docker work starts",
    "rejects a final-only provider before any Docker work starts",
    "rejects maxUsd before any Docker work until live pricing is available",
    "follows normalized events when UI live_trace is disabled",
    "reports a critical follower failure exactly once while activity-only stays fail-soft",
    "treats a missing stdout stream as a critical observer failure"
   ],
   "orchestra/docker-budget-termination.test.ts": [
    "prioritizes durable budget-stop evidence over ambiguous exit 137",
    "preserves terminal-usage fallback provenance",
    "does not attribute exit 137 to OOM without a measurement",
    "attributes exit 137 to OOM only on a measured OOMKilled=true",
    "vetoes exit-0 DONE with exact host counters and durable provenance",
    "surfaces terminal counter provenance separately from containment provenance",
    "is idempotent for the same exhausted attempt",
    "leaves a successful result untouched without terminal budget evidence",
    "patches terminal within-budget counters without changing verdict or billing truth",
    "is idempotent and supplies canonical identity when the worker omitted it",
    "does not patch nonterminal, unmeasurable, or exceeded evidence",
    "vetoes success when terminal usage is unmeasurable without claiming zero usage",
    "vetoes nonterminal observer-loss evidence idempotently",
    "does not overlap terminal within-budget or exceeded reconciliation",
    "freezes the whole container before exact checkpoint-stop termination",
    "fails loud when Docker cannot freeze the checkpoint-stop attempt",
    "unpauses for hard-containment adoption when frozen-container kill fails",
    "accepts docker stop only after inspect proves the container is stopped",
    "escalates through TERM and verifies the post-wait state",
    "uses SIGKILL when TERM cannot produce a verified stop",
    "fails loudly after SIGKILL when Docker cannot prove Running=false"
   ],
   "orchestra/docker-capture-truth.test.ts": [
    "the OLD capture pattern loses data and never sees the ENOBUFS error",
    "GREEN: streams a 2 MiB single chunk fully — truncated=false, captureIncomplete=false",
    "GREEN: reassembles many chunks past 1 MiB (no per-chunk cap)",
    "concatenates stdout THEN stderr (old `(stdout)+(stderr)` order preserved)",
    "spawn-injectable: invokes `docker logs <container>` via the injected spawn only",
    "sanity: the production default ceiling is 256 MiB (not 1 MiB)",
    "ceiling hit → truncated + captureIncomplete + on-disk marker + loud warn + child killed",
    "non-zero exit → captureIncomplete=true, partial content STILL returned (not hidden)",
    "terminating signal → captureIncomplete=true, signal surfaced",
    "spawn 'error' event → captureIncomplete=true, no throw, exitCode null",
    "hung docker logs → timeout kills the child + returns captureIncomplete",
    "clean exit-0 → captureIncomplete=false (normal path is not falsely flagged)",
    "RED: a 1 MiB-capped capture drops the terminal usage line → patch DIES (tokens stay 0)",
    "GREEN: the full 256 MiB-ceiling capture keeps the terminal usage line → patch writes REAL tokens"
   ],
   "orchestra/docker-container-start-failed.test.ts": [
    "maps image-not-found stderr to DECKENT_E081",
    "maps port-collision stderr to DECKENT_E082",
    "maps resource-limit stderr to DECKENT_E083",
    "falls back to DECKENT_E084 with exitCode + stderr in message",
    "parses true|0 as running",
    "parses false|137 as stopped",
    "returns null on malformed input",
    "preserves the full container ID and exact ownership labels",
    "rejects malformed identity/state projections",
    "clean start: docker run + Running=true → no retry, no .timeout marker",
    "stopped non-zero current container is finalized without duplicate provider dispatch",
    "docker-run failure with no created container retries boundedly without rm",
    "foreign name collision fails loud without rm, kill or second docker run",
    "authority inspect permission failure fails closed without rm, kill or second docker run",
    "exact-attempt collision adopts the full container ID without redispatch",
    "instant-exit ExitCode=0: container started and gracefully exited → success, no retry",
    "exposes MAX_SPAWN_ATTEMPTS=2 (task spec)",
    "exposes HEALTH_CHECK_DELAY_MS=3000 (task spec — 3 seconds)",
    "keeps the original start codes and exposes fail-closed ownership conflict"
   ],
   "orchestra/docker-continuation-lineage.test.ts": [
    "archives the parent log and persists cumulative host usage and complete billing",
    "marks terminal billing partial when the exact parent envelope is unavailable",
    "fails loudly when the content-addressed parent archive is corrupt",
    "refuses to rewrite a result for terminal evidence from another attempt"
   ],
   "orchestra/docker-dist-guard.test.ts": [
    "mounts /workspace/dist read-only when dist/ exists",
    "emits NO mount when dist/ is absent (avoids phantom host dist/)",
    "always mounts read-only (never rw — a worker must not write dist/)",
    "adds the read-only dist/ mount to `docker run` argv when dist/ exists",
    "omits the dist/ mount when dist/ does not exist (no phantom host dir)",
    "keeps every pre-existing mount byte-identical — only the new ro-mount is additive"
   ],
   "orchestra/docker-exit-marker.test.ts": [
    "emits the canonical EXIT_WITHOUT_RESULT fields (NO_GO + tokenUsage 4 fields)",
    "workPresent=true → flag set + verify-and-complete hint in notes",
    "workPresent=false → flag clear + \"nothing to recover\" note (work-absent case)",
    "trims diffStat and defaults last-hb fields to unknown/0",
    "exitCode>128 surfaces signal info; source defaults to host",
    "source=wrapper is reflected in notes when provided",
    "adds the last-chance flush window (5×1s re-check before synthesizing a marker)",
    "writes the enriched EXIT_WITHOUT_RESULT marker with discriminator fields",
    "preserves the TIMEOUT_WITH_WORK regression surface (timeout-with-work guard)",
    "work-present run → valid JSON marker with workPresent=true and passthrough fields",
    "work-absent run → valid JSON marker with workPresent=false and empty diffStat"
   ],
   "orchestra/docker-final-only-containment.test.ts": [
    "refuses a final-only provider with a live ceiling when the owner authorized no containment",
    "rejects a containment grant whose wall clock is not a positive integer",
    "narrows the container wall clock to the owner-authorized window",
    "never widens the configured timeout, even with a larger authorization",
    "leaves an incremental-usage provider on its configured timeout"
   ],
   "orchestra/docker-follow-activity.test.ts": [
    "flag ON: each tool_use line in the follow stream lands as a \"🔧 …\" ACTIVITY event",
    "flag OFF: never spawns, never emits (zero-cost no-op)",
    "flag OFF: still follows provider events when the host budget monitor is attached",
    "does NOT write the task .log (activity-only — post-exit writer is authoritative, no double-write)",
    "stop() kills the follow child; a spawn that throws degrades to a no-op stop"
   ],
   "orchestra/docker-git-async.test.ts": [
    "returns Promises and preserves the exact hash-object manifest evidence",
    "keeps the spawnSync ratchet reduced to the remaining non-git debt"
   ],
   "orchestra/docker-heartbeat-authority-wire.test.ts": [
    "routes host process and worker verdict facts through the exact-attempt store",
    "ingests nothing before the provider is invoked",
    "opens the interval at provider invocation and never closes it from container exit",
    "closes the interval as aborted when the provider settles on TERM",
    "rejects container-forged run, principal, and fence ownership instead of manufacturing overlap",
    "persists a principal pseudonym without exposing credential material"
   ],
   "orchestra/docker-multicli-buildarg.test.ts": [
    "codex → assembles `--build-arg INSTALL_CODEX=true` against Dockerfile.worker",
    "gemini → assembles `--build-arg INSTALL_GEMINI=true`",
    "claude → NONE (default lean image): no `--build-arg`, still targets the image",
    "host-only / unknown provider (e.g. ollama) → lean image, no `--build-arg`",
    "honors a custom image tag (multi-tenant / private registry)",
    "codex worker HOLDs on final-only usage before image inspection",
    "codex metering HOLD does NOT silently fall back to a claude container",
    "gemini worker HOLDs on final-only usage before image inspection",
    "claude worker, image absent → throws WITHOUT any `--build-arg` (NONE / default image)",
    "image present → claude worker runs the default image with NO `--build-arg` and NO docker build",
    "declares INSTALL_CODEX / INSTALL_GEMINI ARGs defaulting to false (lean single-CLI image)",
    "opt-in RUN guards install the provider CLIs only when the build-arg is true",
    "claude CLI stays unconditionally baked in (always-present default)",
    "keeps ca-certificates in the base (codex Rust CLI TLS uses the system root store)"
   ],
   "orchestra/docker-provider-auth.test.ts": [
    "mounts only the Claude credential for claude-sonnet-5 subscription",
    "does NOT mount ~/.claude for a codex model (gpt-4.1)",
    "does NOT mount ~/.claude for a gemini model (gemini-2.5-flash)",
    "resolves the %s Cursor host credential root from platform authority",
    "ignores unsafe or relative %s Cursor env input and uses the documented home fallback",
    "mounts only Cursor auth.json from the resolved host root into its private Linux destination",
    "fails closed when Cursor auth.json is absent and preserves API-only no-mount behavior",
    "mounts only the Claude credential for claude-haiku-4-5-20251001",
    "does NOT mount ~/.claude for gpt-5.6-sol (codex model)",
    "does NOT mount ~/.claude for gemini-2.5-pro (gemini model)",
    "mounts only the Claude credential for canonical claude-sonnet-5 in subscription mode",
    "holds canonical Codex before auth mounting because Docker usage is final-only",
    "holds canonical Gemini before auth mounting because Docker usage is final-only",
    "mounts only the Claude credential for canonical Haiku (subscription default)",
    "holds canonical gpt-5.6-sol before auth mounting because Docker usage is final-only",
    "holds canonical gemini-2.5-pro before auth mounting because Docker usage is final-only"
   ],
   "orchestra/docker-provider-binary.test.ts": [
    "returns \"claude\" for a Claude API model ID",
    "returns \"codex\" for a codex model (gpt-4.1)",
    "returns \"gemini\" for a gemini model (gemini-2.5-flash)",
    "rejects an unknown model before selecting any provider binary",
    "rejects a registered Ollama model at the Docker binary boundary",
    "rejects a registered OpenRouter model at the Docker binary boundary",
    "returns \"claude\" for the exact Opus API model ID",
    "returns \"codex\" for the exact GPT-5.5 API model ID",
    "returns \"gemini\" for gemini-2.5-pro model"
   ],
   "orchestra/docker-provider-cli.test.ts": [
    "claude worker script is built from PROVIDER_COMMAND_SPECS.claude (string-assert)",
    "forwards Docker PID1 SIGTERM to the tracked provider supervisor",
    "threads the finite verifier tool/context profile into the real Docker worker script",
    "codex command is built from its shared spec with a canonical API ID",
    "keeps the default-off codex argv byte-identical",
    "emits the codex core file and composes spec-defined core + suppression argv when enabled",
    "keeps claude argv identical when the codex prefix flags toggle",
    "gemini command is built from its shared spec with a canonical API ID",
    "returns true when `command -v <bin>` exits 0 inside the image",
    "returns false when `command -v <bin>` exits non-zero (CLI not on PATH)",
    "fails open (true) when the probe itself could not run (docker hiccup/timeout)",
    "codex final-only usage → HOLD before image probing or container work",
    "gemini final-only usage → HOLD before image probing or container work",
    "honest-fail never falls back to silently spawning the container (no real `docker run -d`)",
    "claude is never probed even with the flag on (always baked in, no build-arg)",
    "codex final-only usage still HOLDs before a positive image probe",
    "codex spawn with no probe opt still HOLDs on final-only usage",
    "gemini spawn with probe explicitly off still HOLDs on final-only usage"
   ],
   "orchestra/docker-provider-execution-observation.test.ts": [
    "emits schema-valid start/end only around the provider command and folds to a closed interval",
    "does not equate container lifecycle with provider execution and refuses a missing fence",
    "persists idempotently, classifies terminal outcomes, and retains incomplete intervals while bounding closed pairs",
    "wires the host-authored wrapper and active claim fence into the real Docker spawn ingress",
    "derives a stable principal pseudonym without persisting raw auth material"
   ],
   "orchestra/docker-restart-reconcile-archived-attempt.test.ts": [
    "skips an attempt whose custody chain is complete through archive",
    "inventories an archived attempt absent in containment mode once the daemon proves it",
    "holds an archived attempt whose container the daemon still reports present",
    "holds an archived attempt as state-unknown when the daemon probe %s",
    "never probes the daemon for an archived attempt in resume mode",
    "reports a containment hold with the daemon container proven absent",
    "lets the registry retire that hold as foreign, already-contained history",
    "still recovers the same attempt when the archive chain is absent"
   ],
   "orchestra/docker-restart-reconcile.test.ts": [
    "discriminates exact task authority read-only and never fail-opens Store errors",
    "publishes worker absence only after an async exact Store reconciliation",
    "keeps historical retired landings out of the current task workspace",
    "adopts and closes a valid pre-lifecycle settlement without rewriting its receipt",
    "resumes an interrupted legacy adoption from the exact existing claim",
    "does not adopt a legacy settlement while its deterministic container is present",
    "also refuses adoption when only the current project-scoped container name is present",
    "rejects a legacy raw-result mismatch before publishing claim or closure",
    "fails closed when Docker cannot prove a legacy container absent",
    "rejects a legacy task identity mismatch before publishing claim or closure",
    "closes an attempt that never crossed the prepared/provider boundary",
    "closes multiple unprepared attempts without overwriting the first raw recovery result",
    "contains a prepared attempt as NO_GO when the exact container is proven absent and its result is missing",
    "preserves malformed result bytes and closes the proven-absent attempt with a host NO_GO",
    "terminal-only ingress closes a dispatched absent attempt without project leadership",
    "terminal-only ingress never closes a prepare-without-dispatch race",
    "adopts a running exact attempt, contains it, and awaits normal monitor settlement",
    "recovers the exact container when crash happened between Docker ACK and dispatch persistence",
    "rejects short container identities without any recovery mutation",
    "adopts an exact stopped exit-0 attempt and manufactures no success when result is missing",
    "restores persisted budget and vetoes DONE when recovered usage is unmeasurable",
    "settles a durable result after exact container absence without redrive",
    "reconciles exact host terminal billing after container absence and ignores worker billing",
    "holds an absent attempt when immutable settlement conflicts with terminal billing receipt",
    "reconciles absent cumulative token and exact host billing without trusting project-mounted log",
    "holds an absent continuation when exact terminal usage is missing",
    "refuses to close an immutable attempt-only continuation settlement",
    "threads exact continuation authority into a running recovery monitor",
    "resumes a retired checkpoint when the host crashed before creating its continuation claim",
    "completes an already-checkpointed LANDED attempt after coordinator restart without terminal settlement",
    "settles a LANDING_REQUESTED restart without continuation when terminal usage is absent",
    "settles honestly without LANDED authority when the final restart proposal is corrupt",
    "fails loud on foreign labels without stop, removal, lock release or redrive",
    "fails closed when Docker authority cannot prove presence or absence"
   ],
   "orchestra/docker-result-settlement.test.ts": [
    "carries real route→persona/skill body→receipt identities into settlement and finalizer attribution",
    "embeds the final result under the exact project/task/attempt authority",
    "denies agent and skill credit when a fresh task receipt is malformed",
    "does not invent authority for direct legacy backend calls and rejects cross-project refs",
    "closes the durable claim only after dispatch, settlement and lifecycle cleanup evidence",
    "promotes only an exact wrapper marker from assistant protocol and preserves evidence",
    "does not promote prompt echoes, incomplete protocol, or a genuine worker result",
    "binds prepared, dispatch, prompt and claim custody to goal identity without a taskId",
    "rejects a changed %s behind the same invocation key",
    "rejects malformed UUIDs, mixed task/goal refs and project mismatch",
    "keeps task reference, labels and persisted prepared bytes unchanged",
    "lists pending tasks beside goal attempts without treating goals as corrupt task records",
    "still throws on a corrupt real task UUID attempt when goal records coexist",
    "keeps legacy raw task ID %j separate from the goal runtime identity"
   ],
   "orchestra/docker-settlement-monitor-wire.test.ts": [
    "uses attempt-local counters for a checkpoint-subtracted continuation budget",
    "writes receipt and closure only after exact-ID removal and lock release",
    "contains an exit-0 malformed worker result as forensic-backed NO_GO instead of hanging",
    "persists terminal provider billing from the live follower before removal and settlement",
    "does not write receipt or closure when exact-ID removal fails",
    "preserves registry and locks when monitor lacks settlement authority",
    "closes an unmeasurable budgeted DONE as explicit host NO_GO",
    "closes a measurable within-budget DONE with exact host counters",
    "closes partial usage plus observer loss as unmeasurable host NO_GO",
    "retains lifecycle authority when budget evidence cannot be projected",
    "contains an errored wait by exact ID and finalizes NO_GO exactly once",
    "holds registry, locks and receipts when wait-failure containment is unproven",
    "buffers split wait output and finalizes the strict complete integer once",
    "rejects malformed complete wait evidence %j and contains before settlement",
    "stamps, stops, and retires a reserve-threshold attempt as LANDED without terminal settlement",
    "closes a terminal landing-requested attempt when its proposal never advances",
    "vetoes LANDED and continuation when provider shutdown crosses the hard ceiling"
   ],
   "orchestra/docker-spawn-race.test.ts": [
    "prompt file is PRESERVED during the 3s race window after markPending",
    "spawn failure path clears pending so the Set does not leak across sprints"
   ],
   "orchestra/docker-worktree-session-isolation.test.ts": [
    "overlays a primary checkout Git directory read-only",
    "maps a linked worktree to container-native Git paths",
    "preserves non-Git project support without synthetic metadata",
    "fails loudly for a malformed linked-worktree pointer",
    "creates Claude session-env as a private directory, never a file",
    "does not create Claude state for another provider",
    "keeps an override worktree root for kill/result/lock teardown paths"
   ],
   "orchestra/eaa-atomic.test.ts": [
    "leaves no leftover .tmp sibling after a clean write",
    "refuses a conflicting rewrite before rename and leaves the prior record untouched",
    "recovers on retry after a simulated rename failure and produces the fully-new content (never partial)",
    "creates no final file at all when the very first write for a path fails at rename time"
   ],
   "orchestra/ecosystem-intelligence.test.ts": [
    "generates activation rules from trigger keywords",
    "generates exclusion rules for documentation skill",
    "returns a default rule when skill has no recognizable keywords",
    "returns a valid ActivationConfig structure",
    "handles missing manifest gracefully",
    "uses SKILL.md content for keyword extraction",
    "writes manifestVersion and activation to manifest.json",
    "does not overwrite an existing V2 manifest",
    "silently skips when manifest file is missing"
   ],
   "orchestra/effect-class.test.ts": [
    "returns \"pure\" for an audit task (detectTaskType=audit)",
    "returns \"reversible\" for a document-write task (detectTaskType=document-write)",
    "returns \"reversible\" for a code-development task (detectTaskType=code-development)",
    "returns \"reversible\" for an empty-scope task (default fallback to code-development)",
    "returns \"reversible\" for a mixed src+docs scope (code-development by precedence)",
    "returns \"pure\" for an audit task even when directories include docs/ (audit precedence)",
    "always returns a value in the EffectClass union",
    "is referentially stable across calls (pure function)",
    "maps audit → pure",
    "maps document-write → reversible",
    "maps code-development → reversible"
   ],
   "orchestra/ent1-deepen.test.ts": [
    "hard-deny (enforce_rbac:true) → writes authority.denied with enforced:true",
    "soft-warn (enforce_rbac:false) → still writes authority.denied with enforced:false",
    "permit (no violation) → no audit event written",
    "no audit ctx → no event written (backward-safe)",
    "checkBacklogEntryRbac deny + audit ctx → authority.denied written",
    "checkSprintSpawnRbac deny + audit ctx → authority.denied written",
    "capability verb db.write → db-write",
    "capability verb db.query → db-query",
    "capability verb erp.read → erp-read",
    "capability verb shell.exec → shell",
    "unknown verb → fs-read (least-privilege)",
    "task / sprint / process → fs-write",
    "viewer + db-write capability + enforce_rbac:true → deny + audit-event",
    "viewer + db-write + flag-off → allowed (warn — backward)",
    "admin + db-write + enforce_rbac:true → allowed",
    "no actor role → allowed (permissive default, backward-safe)",
    "sprint-kind viewer + enforce_rbac:true → deny (checkSprintSpawnRbac path)",
    "enforce_rbac:true + viewer entry requiring db-write → decision deny + audit-event",
    "enforce_rbac:false + viewer entry → NOT denied (backward — soft warn)",
    "enforce_rbac:true + admin entry → NOT denied (reaches policy decision)",
    "enforce_rbac:true + role-less entry → NOT denied (permissive default)"
   ],
   "orchestra/ent1-rbac-enforce.test.ts": [
    "hard-denies viewer + fs-write when enforce_rbac:true",
    "soft-warns viewer + fs-write when enforce_rbac:false (flag off — regression)",
    "allows (permit) when enforce_rbac is absent (undefined → false)",
    "allows (permit) when no actor — permissive default (ADR-037 V1.0)",
    "allows engineer + fs-write when enforce_rbac:true",
    "hard-denies viewer + shell when enforce_rbac:true",
    "hard-denies viewer + fs-write when enforce_rbac:true (spawn deny)",
    "allows viewer + fs-write when enforce_rbac:false (flag off — regression)",
    "allows when no actor — permissive default (ADR-037 V1.0)",
    "allows engineer + shell + fs-write when enforce_rbac:true",
    "hard-denies viewer + erp-write when enforce_rbac:true",
    "allows admin + all capabilities when enforce_rbac:true"
   ],
   "orchestra/ent2-tenant-thread.test.ts": [
    "rejects an unsigned tenant claim when strict_tenant_isolation:true",
    "returns all entries when strictTenantIsolation is false (backward-compat)",
    "returns all entries when no req is provided (server.ts backward-compat path)",
    "does not grant admin access from an unsigned role claim",
    "does not match non-autonomous routes (returns false)"
   ],
   "orchestra/ent3-lineage.test.ts": [
    "stamps fresh entries with the provided correlationId",
    "does not assign correlationId when omitted (backward-safe)",
    "deduplicates entries — already-present ids are skipped",
    "task-A(corr=X) → audit event carries correlationId=X",
    "A→B spawn: B.causationId=A.hmac forms a causal chain",
    "readAuditEventsByCorrelationId(X) → [A,B] in insertion order",
    "buildCausalChain orders events: spawn before result",
    "returns 200 with causal-tree JSON for a known correlationId",
    "returns 200 with empty events for an unknown correlationId",
    "returns 400 when correlationId segment is empty",
    "does not match non-GET requests to lineage path (falls through)"
   ],
   "orchestra/env-probe-wire.test.ts": [
    "persists and reads back the one-line inventory",
    "creates the .deckent directory when absent",
    "returns undefined when no file was ever written for this sprint",
    "scopes the file path per sprint id",
    "persists the formatted result of an injected probe",
    "is fail-soft: a rejecting probe writes no file and never throws",
    "is fail-soft: a synchronously-throwing probe never throws",
    "renders real, non-empty data via the unmocked buildEnvProbeBlock",
    "stays empty (byte-for-byte pre-born-670a behavior) when no probe ever ran",
    "probes exactly once and persists the result at sprint start",
    "never aborts sprint start when the probe rejects"
   ],
   "orchestra/eval-audit-revive.test.ts": [
    "rubric-having evaluation (extension-hit/alive-grace-hit shape) writes an attempt-1 record",
    "rubric-having NO_GO evaluation preserves failed criteria in the record",
    "rubric-less evaluation (timeout/synthetic-NO_GO shape) still writes decision + rationale",
    "rubric-less evaluation without an explicit rationale override still gets a non-empty rationale",
    "never throws even when projectRoot is unwritable (fail-soft, mirrors every other EVALUATE gate)"
   ],
   "orchestra/evaluate-cross-verify-wire.test.ts": [
    "carries the owner-authored verifier selection into the runner",
    "passes no selection when the owner authored none, preserving the fail-closed skip",
    "drops an unknown provider name instead of forwarding it as a selection",
    "stops billed dispatches at the owner canary ceiling",
    "verifies every eligible task when the owner set no ceiling",
    "fails closed when a mandatory verification cannot dispatch because the ceiling is reached",
    "fails closed when the mandatory runner faults unexpectedly"
   ],
   "orchestra/evaluate-enforcement-gates.test.ts": [
    "accepts valid boolean flags (no throw)",
    "rejects a non-boolean verify_delta_downgrade",
    "rejects a non-boolean enforce_adr_compliance",
    "absent flags leave the existing max_tech_debt_ratio validation intact",
    "flag-on + DONE + short verify-delta (ratio 0.7) → downgraded DONE→GO_WITH_TECH_DEBT",
    "flag-on + DONE + severe verify-delta (ratio 0.3) → downgraded DONE→NO_GO",
    "flag-OFF → DONE unchanged (byte-identical), even with a short baseline on disk",
    "flag-on but NO baseline on disk → DONE unchanged (computeVerifyDelta returns null)",
    "flag-on + real ADR-006 violation in a changed file → downgraded to NO_GO",
    "flag-OFF → DONE unchanged, enforceAdrCompliance never called (byte-identical)",
    "flag-on + clean files (no violation) → DONE unchanged",
    "flag-on + enforcer fail-open (returns pass:true with enforcerError) → no block, DONE preserved",
    "flag-on + enforcer THROWS → wiring fail-open keeps DONE (an enforcer bug must not block tasks)"
   ],
   "orchestra/evaluate-loop-armor.test.ts": [
    "a rubric TypeError on task 1 does not truncate the loop — task 2 still evaluated",
    "faulted task falls back honestly: worker DONE caps at GO_WITH_TECH_DEBT",
    "faulted task with worker NO_GO stays NO_GO (no silent promotion)",
    "surfaces the fault as BRAIN→AUDITOR:EVALUATION_FAULT event"
   ],
   "orchestra/evaluate-phase-idempotency.test.ts": [
    "two parallel runEvaluatePhase calls — second is NO_OP, single result set",
    "same-PID re-entry (sequential) — second call is NO_OP",
    "stale lock (dead PID) is reclaimed by new caller",
    "live other-PID lock — caller is NO_OP, foreign lock preserved",
    "successful run releases the lock and writes a valid payload while held",
    "sequential calls after release each acquire successfully"
   ],
   "orchestra/evaluate-premature-retry.test.ts": [
    "empty-then-filled: retries exactly once, populates evaluations, no abort surfaced",
    "empty-then-still-empty: retries once, then loudly aborts (event + notify + flag)",
    "normal path: evaluations already populated — zero-effect no-op (no retry, no event)",
    "no-results path: results empty — zero-effect no-op regardless of evaluations state"
   ],
   "orchestra/evaluate-result.test.ts": [
    "returns true for docs/ directory",
    "returns true for docs/subdirectory",
    "returns true when all directories are under docs/",
    "returns false for src/ directory",
    "returns false for mixed scope (docs/ + src/)",
    "returns false for empty directories",
    "doc task with testsPassed=true → DONE (skips coverage check)",
    "doc task with coverage=0 still → DONE",
    "doc task with testsPassed=false → NO_GO",
    "doc task selfAssessment=NO_GO → NO_GO",
    "doc task selfAssessment=GO_WITH_TECH_DEBT → GO_WITH_TECH_DEBT",
    "normal task with coverage >= 90 → DONE",
    "normal task with coverage < 90 → GO_WITH_TECH_DEBT",
    "normal task with testsPassed=false → NO_GO",
    "normal task selfAssessment=NO_GO → NO_GO regardless of coverage",
    "mixed scope with low coverage → GO_WITH_TECH_DEBT (not treated as doc task)",
    "mixed scope with high coverage → DONE"
   ],
   "orchestra/evaluate-trigger-gate.test.ts": [
    "full-dispatch (all tasks have .result) — guard passes, evaluation proceeds",
    "partial-dispatch (one task pre-dispatch) — guard fires, evaluations empty, premature event emitted",
    "undispatched task explicitly DEFERRED — guard passes, deferred task skipped (no synthetic NO_GO)",
    "task with assignedWorker (no .result yet) — guard passes (in-flight dispatch signal)",
    "task with .hb but no assignedWorker/.result — guard passes (worker-in-flight signal)",
    "gate disabled (legacy/test caller) — evaluation runs even with undispatched tasks",
    "isTaskDispatched — recognizes each dispatch signal independently",
    "findUndispatchedTaskIds — returns only tasks lacking every dispatch signal"
   ],
   "orchestra/evaluate-with-rubric-spurious-reconcile.test.ts": [
    "OOM-killed NO_GO + projectRoot triggers reconcileSpuriousNoGo with vitest-skip dep",
    "NO_GO with projectRoot but no OOM marker still calls reconcile WITHOUT vitest-skip",
    "NO_GO with projectRoot omitted skips spurious reconcile (backwards compat)",
    "typed attribution HOLD cannot be promoted from the final shared-worktree diff",
    "reconcileSpuriousNoGo returning NO_GO leaves evaluation NO_GO (no false positive)",
    "pure evaluateWithRubric no longer self-calls reconcileSpuriousNoGo (R8: sync grader)"
   ],
   "orchestra/evaluation-audit-trail.test.ts": [
    "writes an audit-rubric record under the canonical layout",
    "writes a doc-write record that reflects coverageRelaxed in schemaValidation",
    "writes a code-rubric record and preserves criterionScores round-trip",
    "formats decisionRationale for DONE / TECH_DEBT / NO_GO and schema-invalid variants",
    "writes distinct files per attempt; same-decision rewrite is idempotent, different-decision is conflict-fail-closed",
    "creates the sprint subdirectory on first write",
    "defaults timestamp to an ISO-8601 UTC string when omitted",
    "does not touch a pre-existing unrelated file in the sprint dir"
   ],
   "orchestra/evaluation-honesty-negative-replay.test.ts": [
    "keeps explicit failure repairable and refuses score-only debt or completed settlement",
    "allows spurious reconciliation only for exact-attempt material work with all criteria passing"
   ],
   "orchestra/evaluation-verification-isolation.test.ts": [
    "detects the enforceVerifyLoop no-admission marker",
    "detects the adapter-level isolation-on-hold marker",
    "detects foreign verification diagnostics",
    "detects config-not-admitted and invalid-request adapter holds",
    "detects execution-timeout adapter hold",
    "returns false for a genuine scoped compiler failure",
    "returns false for empty notes",
    "does not let worker notes promote a NO_GO self-assessment",
    "still returns NO_GO for a genuine scoped failure with no isolation evidence",
    "keeps a worker-note isolation claim inside the normal NO_GO rubric",
    "leaves a genuine scoped NO_GO untouched",
    "never spends FIX/retry budget on a non-NO_GO decision",
    "parks and does not spend budget when a host-observed isolation hold receipt is present",
    "does not treat worker notes as repair-budget authority",
    "spends the FIX/retry budget for a scoped NO_GO with no isolation evidence"
   ],
   "orchestra/evaluator-consistency.test.ts": [
    "returns DONE when testsPassed=true, coverage sufficient, selfAssessment=DONE",
    "returns NO_GO when testsPassed=false and selfAssessment=NO_GO",
    "returns GO_WITH_TECH_DEBT when bash unavailable pattern detected",
    "uses default rubric when no rubric parameter is provided",
    "sprint-phases.ts does NOT import evaluateResult (only evaluateWithRubric)"
   ],
   "orchestra/evaluator-fix-context.test.ts": [
    "includes task ID in reason",
    "includes \"tests failed\" when testsPassed is false",
    "includes \"no files changed\" when filesChanged is empty",
    "includes rubric scores when rubricResult provided",
    "includes truncated worker notes",
    "handles result with no notes gracefully"
   ],
   "orchestra/evaluator-schema.test.ts": [
    "returns valid for complete result",
    "detects missing coverage",
    "detects missing selfAssessment",
    "detects missing testsPassed",
    "detects missing taskId",
    "detects missing filesChanged array",
    "accepts exact digest, applicability, commands and criterion identities",
    "rejects a digest mismatch and testsPassed/applicability gaming",
    "requires exact evidence IDs and explicit open IDs for tech debt",
    "returns NO_GO when coverage is missing",
    "returns NO_GO when taskId is empty",
    "passes schema check for valid result and proceeds to rubric scoring",
    "tolerates missing coverage + testsPassed when the result explicitly changed nothing",
    "still requires coverage for a non-empty source-only change (153/154 guard preserved)",
    "does NOT exempt when filesChanged is missing entirely — the declaration must be an explicit empty array",
    "evaluateWithRubric no longer schema-NO_GOs the sprint-449-shaped honest result"
   ],
   "orchestra/evaluator-scope-heuristic.test.ts": [
    "gives 100 for files all within scope",
    "gives 0 for files completely outside scope with no auxiliary",
    "gives partial credit (80) for auxiliary docs/ files outside scope",
    "gives partial credit for .deckent/ files outside scope",
    "handles mixed in-scope + auxiliary files correctly",
    "handles mixed in-scope + out-of-scope + auxiliary files",
    "gives 100 for no files changed",
    "gives partial credit for .tasks/ files",
    "gives partial credit for CHANGELOG at root",
    "does NOT give NO_GO when only auxiliary files are out of scope"
   ],
   "orchestra/evaluator-verification-task.test.ts": [
    "detects \"verify\" keyword in task description",
    "detects \"already implemented\" keyword in task description",
    "detects \"Sprint N\\'de yapıldı\" Turkish pattern",
    "detects \"audit\" keyword in task title",
    "detects verification pattern in worker notes",
    "returns false when filesChanged is non-empty",
    "returns false when testsPassed is false",
    "returns false for normal coding tasks",
    "returns DONE for verification task with filesChanged=[] + testsPassed=true",
    "does NOT fast-path when description has no verification keywords"
   ],
   "orchestra/event-bus.test.ts": [
    "should deliver published event to subscriber",
    "should filter events by channel — NOTIFY only skips PHASE events",
    "should filter events by sprintId — sprint-144 subscriber ignores sprint-145 events",
    "should not deliver events after unsubscribe is called",
    "should swallow subscriber exceptions without breaking event flow",
    "should return last N events from JSONL file via tail()",
    "should return empty array for missing file via tail()",
    "should return empty array for empty file via tail()",
    "should publish events when writeEvent() is called (integration)",
    "should clean up all file watchers on unwatchAll()",
    "should deliver event to all matching subscribers",
    "should deliver all events to wildcard (*) sprintId subscriber",
    "should swallow async subscriber rejections",
    "should track subscriber count correctly",
    "should deliver event to all subscribers when sprintId is not provided",
    "should skip malformed JSON lines in tail()",
    "should emit raw event on EventEmitter for direct listeners",
    "should detect new JSONL lines via watchFile",
    "should auto-start watchFile on tail() so live events reach subscribers (deckent_watch wire)"
   ],
   "orchestra/event-stream-emit.test.ts": [
    "should define SPRINT_PHASE_CHANGE channel",
    "pins completed-checkpoint recovery observability channels",
    "should define TASK_ASSIGN channel",
    "should define HEARTBEAT channel",
    "should define RESULT channel",
    "should define SCOPE_COLLISION_DETECTED channel",
    "should define ADR_VIOLATION channel",
    "should define NOTIFY channel",
    "should emit SPRINT_PHASE_CHANGE for PLAN→SPAWN transition",
    "should emit SPRINT_PHASE_CHANGE for SPAWN→EXECUTE transition",
    "should emit TASK_ASSIGN with correct payload",
    "should emit FIX_REQUEST with NO_GO task info",
    "should emit HEARTBEAT with workerId",
    "should emit RESULT with task result",
    "should emit SCOPE_COLLISION_DETECTED with details",
    "should emit ADR_VIOLATION with violation details",
    "should return null on write failure without throwing",
    "should filter events by channel codes",
    "should sequence events correctly"
   ],
   "orchestra/event-stream-notify.test.ts": [
    "should write event with channel === CHANNELS.NOTIFY",
    "should write correct payload structure (level, title, body)",
    "should set source=deckent and target=user",
    "should work without meta argument",
    "should accumulate multiple NOTIFY events in JSONL",
    "should produce JSONL lines containing NOTIFY channel",
    "should accept info, warn, and error levels",
    "should export writeEvent and CHANNELS from event-stream module"
   ],
   "orchestra/event-stream-timeout.test.ts": [
    "defines TIMEOUT_ASSIGN channel",
    "defines TIMEOUT_WARNING channel",
    "defines TIMEOUT_CAP_EXCEEDED channel",
    "writes TIMEOUT_ASSIGN event after call",
    "includes breakdown fields (base, multipliers) in TIMEOUT_ASSIGN payload",
    "writes TIMEOUT_CAP_EXCEEDED when estimate exceeds max_timeout",
    "does NOT write TIMEOUT_CAP_EXCEEDED when estimate is within bounds",
    "emits correct taskId in both events for capped scenario"
   ],
   "orchestra/event-stream.test.ts": [
    "should write and read a single event",
    "should write multiple events with monotonic sequence",
    "should filter events by source",
    "should filter events by channel",
    "should filter events by afterSequence",
    "should reconstruct sprint state from events",
    "never throws on write failure (invalid projectRoot)",
    "should return empty array for non-existent events file",
    "should skip malformed lines in event stream",
    "should maintain monotonic sequence across writes",
    "should read sprint ID from sprint-state.json",
    "should return null when sprint-state.json missing",
    "should export all ADR-035 channel codes (15 base + extensions)",
    "should have HEARTBEAT channel for WORKER→BRAIN communication",
    "should have RESULT channel for WORKER→BRAIN communication",
    "should have QUESTION channel for WORKER→BRAIN communication",
    "should have CODE_VERIFY_REQUEST channel for WORKER→AUDITOR communication",
    "should write HEARTBEAT event with correct payload structure",
    "should write RESULT event with selfAssessment and filesChanged",
    "should write QUESTION event with question and context",
    "should write CODE_VERIFY_REQUEST event for auditor",
    "worker event sequence should be monotonically increasing across all 4 hook types",
    "writeEvent with lineage propagates both correlationId and causationId",
    "writeEvent without lineage leaves correlationId/causationId undefined (backward-safe)",
    "writeEvent with partial lineage (correlationId only) omits causationId",
    "extractLineage returns both fields from a source object",
    "extractLineage with undefined input returns empty lineage (no fields set)",
    "extractLineage with partial source propagates only the present field"
   ],
   "orchestra/evolution-pipeline.test.ts": [
    "returns positive bonus after 5+ successful outcomes",
    "returns negative bonus after many failures",
    "returns no bonus when below minimum samples",
    "all bonuses are capped at LEARNING_BONUS_CAP",
    "generates auto-applied activation rule with enough data",
    "does not generate rules with insufficient data",
    "generates synergy-based rules for skill pairs",
    "returns promote action for entity with 8+ tasks and 85%+ success",
    "returns wait action when below minimum tasks",
    "returns wait action when success rate is below threshold",
    "evaluates demotions for high failure rate",
    "uses custom promotion criteria",
    "returns non-empty rows when skillMap is provided",
    "returns empty rows when skillMap is undefined",
    "returns empty rows when skillMap is empty",
    "adds positive bonus for high quality scores",
    "adds negative penalty for low quality scores",
    "tracks incremental average quality score correctly",
    "defaults to avgQualityScore=0 when no quality data provided",
    "uses custom minSamplesForBonus",
    "uses custom recentSprintWindow",
    "uses custom sprintRecencySuccessBonus and sprintRecencyFailurePenalty",
    "default config matches expected values",
    "returns empty string when no sprints recorded",
    "returns empty string when combos have fewer than MIN_COMB_SAMPLES",
    "returns formatted worst combinations",
    "sorts by success rate ascending and respects limit",
    "skips generic agent outcomes",
    "treats GO_WITH_TECH_DEBT as success in performance tracking",
    "returns +1 for mostly successful (≥75%)",
    "returns -1 for mostly failed (<35%)",
    "returns no bonus for mixed rate (35-75%)",
    "returns empty map when fewer than 2 sprints",
    "detects skill-skill conflict in reasoning without emitting an unconditional rule",
    "does not generate synergy rules for agent+skill pairs",
    "promote returns false when temp entity not found",
    "demote returns false when manifest not found",
    "evaluateDemotions returns empty for entities below minTasks",
    "evaluateDemotions returns nothing for entities with acceptable fail rate",
    "uses task.assignedSkills when skillMap is not provided",
    "computes avgCoverage from results when provided",
    "returns positive intent-specific bonus when delta > 0.15",
    "returns negative intent-specific bonus when delta < -0.15",
    "record → bonus → evolve → promote forms a coherent chain",
    "saves and retrieves evolved rules via tracker",
    "synergy matrix builds correctly across outcomes"
   ],
   "orchestra/exact-acceptance-evidence.test.ts": [
    "validates the canonical accepted-V2 reference and complete lineage without declaring authority",
    "rejects tampered %s",
    "rejects sibling lineage even when the attacker recomputes the envelope digest",
    "rejects extra fields and caller hooks without executing them",
    "never issues source authority from caller getter or proxy input",
    "does not upgrade a genuine accepted Store fixture with missing lifecycle proof to ready",
    "hashes exact descriptors independently of ordering, excluding only the binding envelope",
    "requires distinct binding, manifest and final byte citations with authored GO/NO-GO polarity",
    "reconstructs ordered chunks and verifies aggregate digest, with detached output bytes",
    "permits a single canonical empty chunk",
    "rejects %s mismatch before handing out any bytes",
    "rejects %s immutable chunk content"
   ],
   "orchestra/exact-accepted-result-evaluation-settlement.test.ts": [
    "folds only the finalizer trusted work vector while preserving the legacy reporter path",
    "settles genuine semantic custody with a Date-only frozen wall clock without future fixture receipts",
    "rederives the producer route from real Store lifecycle and immutable landed bytes before any terminal write",
    "applies a bound durable verifier decision, settles T11, and rejects missing actual-call evidence on terminal reread",
    "reconciles the %s restart cut without a second verifier dispatch",
    "never promotes an %s semantic verifier outcome to accepted T11 authority",
    "re-reads the accepted result and admitted Task, then emits one durable terminal chain",
    "keeps terminal chain timestamps causal when the host clock steps backward",
    "adopts a durable evaluation artifact after a crash before chain publication",
    "adopts a durable archive artifact after a crash before chain publication",
    "rejects caller fields, accessors, and proxies before writing evaluation state",
    "holds before evaluation when durable provider-exit authority is absent"
   ],
   "orchestra/exact-accepted-result-terminal-authority.test.ts": [
    "accepts the single fully bound accepted/evaluation/finalizer authority",
    "accepts identical authority after canonical disk key ordering",
    "rejects a sibling attempt and a changed artifact digest",
    "rejects stale or sibling terminal rereads before dependency data can be consumed",
    "rejects a foreign terminal-decision schema or kind even when receipts match",
    "rejects getters, proxies and cyclic authority values without invoking them",
    "rejects oversized strings before accepting a structurally similar envelope"
   ],
   "orchestra/exact-committed-unsettled-recovery.test.ts": [
    "rejects a non-exact target before Store, Docker, or fence access",
    "revalidates adapter semantic evidence on every planning read and fails closed on drift",
    "shares startup archive proof with planning (archived=%s)",
    "publishes and idempotently rereads the real committed-anchor Store disposition"
   ],
   "orchestra/exact-controller-terminal-fanin.test.ts": [
    "writes fresh PLAN authority before coordinator publication with prior state %s",
    "still rejects a missing terminal authority for an exact task in the current sprint",
    "still rejects a held terminal authority for an exact task in the current sprint",
    "replays a production receipt and completes terminal skill publication after a post-receipt recovery crash",
    "propagates completed-recovery terminalization failure without entering fresh PLAN",
    "settles a cold recovered accepted result and fresh-reads T11 before restore",
    "keeps a durable exact NOT_DISPATCHED admission out of settlement",
    "preserves a safe recovered authority HOLD reason without exposing raw diagnostics",
    "replaces unsafe recovered authority diagnostics with the typed fallback",
    "preserves a safe exact settlement HOLD reason",
    "fails closed when recovered acceptance cannot become a current terminal receipt",
    "retires a foreign unsettleable recovered attempt on a fresh start",
    "still fails closed when the unsettleable attempt is the restore candidate",
    "never retires an attempt from a later sprint than the restore candidate",
    "never retires while the owning run is still ACTIVE",
    "never retires when the accepted authority is not identity-bound to the task",
    "never calls a bare archive directory terminal",
    "never calls an ACTIVE settlement record terminal",
    "never calls a bare pid snapshot terminal",
    "never calls an unbound job record terminal",
    "reads an identity-proven terminal flow closure as durably terminal",
    "never calls an identity-unproven flow terminal",
    "never calls an ACTIVE authority row terminal",
    "binds a recovered attempt identity only when every custody field is present",
    "re-derives terminal history for immutable missing worker evidence after each cold start",
    "keeps immutable missing evidence held at an unsafe owner boundary: %j",
    "requires a generation-bound disk closure for missing worker evidence (bound=%s)",
    "keeps missing worker evidence held for %s",
    "retires a foreign attempt whose evaluation replay can never match again",
    "never retires a hold whose origin is the post-settle terminal re-read",
    "never retires a transient registry-mismatch settlement hold",
    "never retires an actionable non-hold settlement outcome",
    "classifies only decided, permanent settlement holds as retirable",
    "treats a zero-padded restore candidate as the same sprint",
    "never retires when the restore candidate sprint id cannot be parsed",
    "never retires a task id whose owning sprint cannot be derived",
    "ignores a forged public cascade/pre-dispatch result for an exact task",
    "does not publish a ready handoff from worker DONE when T11 says NO_GO",
    "keeps recovered history out of the current lifecycle without discarding registry evidence",
    "preserves fail-closed handling for a current task authority hold"
   ],
   "orchestra/exact-docker-dispatch-task-authority.test.ts": [
    "accepts the complete canonical Task including arrays and preserves every field",
    "preserves and freezes the immutable V2 proof program in the exact dispatch snapshot",
    "rejects an unregistered planner-designated harness before exact dispatch admission",
    "rejects historical V1 wiring and any forged V2 proof program digest",
    "rejects exact production write scope when its host-derived classification has no V2 contract",
    "repeats the verifier-asset write-scope guard at immutable snapshot admission",
    "rejects a task missing required field %s",
    "rejects unknown, undefined, malformed nested, and forged digest fields",
    "rejects proxies/accessors before invoking caller code",
    "overrides assignedWorker through the producer and isolates caller mutation",
    "omits undefined optional fields and derives scope applicability only at the producer boundary",
    "binds canonical snapshot bytes, task identity, material digests, and size"
   ],
   "orchestra/exact-docker-effect-containment.test.ts": [
    "rereads an exact completed maintenance boundary after terminal cleanup and successor acquisition",
    "rejects an invalid caller-derived boundary identity before durable mutation",
    "accepts only an exact idempotent in-flight boundary replay"
   ],
   "orchestra/exact-docker-observation.test.ts": [
    "accepts bounded read syntax %j",
    "rejects mutation or ambiguous syntax %j",
    "does not return a thread message before the observation thread exits",
    "waits for child close after a real deadline decision",
    "preserves injected command runner identity",
    "routes full container inspection through observation worker, without coordinator spawn",
    "routes admitted mutation through execution transport, never observation or coordinator IO",
    "preserves measured completion across delayed parent delivery and waits for retirement",
    "does not accept %s",
    "rejects population stdin and unbound volume targets before creating a thread",
    "retains production runner provenance across the observation wrapper without admitting custom runners",
    "isolated mutation rejects %s without coordinator fallback"
   ],
   "orchestra/exact-docker-private-output-contract.test.ts": [
    "keeps a provider permissive override rejected by the production POSIX reader",
    "seals raw result and landing bytes as owner-private without changing project modes",
    "refuses a pre-existing sealed leaf without overwriting it",
    "preserves the sealed result and emits a safe marker when landing draft is absent",
    "seals a sequence-unique question and its source receipt as private files",
    "rejects a symlinked sealed namespace ancestor",
    "retains only typed safe capture cause fields",
    "rejects a permissive sealed landing postimage with a typed privacy cause",
    "durably captures and decodes only the runner seal failure allowlist"
   ],
   "orchestra/exact-docker-provider-exit-authority.test.ts": [
    "derives exit only through accepted identity, dispatch release, and Store observation",
    "holds when the provider-exit observation is missing",
    "holds on forged wait evidence instead of accepting the caller exit code",
    "holds when dispatch authority belongs to a sibling attempt"
   ],
   "orchestra/exact-docker-release-recovery.test.ts": [
    "never retries identity contradictions or diagnostic publication failures",
    "distinguishes a competing replay owner from a consumed replay receipt",
    "retains live custody lookup when completion is held without absence evidence",
    "preserves live lookup through the public acceptance API on HOLD",
    "preserves original release failure when %s throws"
   ],
   "orchestra/exact-evaluation-policy-authority.test.ts": [
    "freezes one complete rubric and acceptance row before provider birth",
    "rejects dormant custom rubric and malformed acceptance instead of dropping rules",
    "rejects proxy/accessor config and forged nested digests",
    "binds approved material to task, sprint, task kind, and dispatch digest"
   ],
   "orchestra/exact-pause-resume-custody.test.ts": [
    "delegates %s to the registry recovery/adoption owner exactly once",
    "propagates a typed reconciliation HOLD instead of continuing projection"
   ],
   "orchestra/exact-plan-spawn-authority.test.ts": [
    "accepts already-canonical dependencies without mutating the approved tasks",
    "rejects a title dependency that would mutate after approval",
    "requires the materialized task artifact and rejects semantic drift",
    "ignores prompt-build runtime fields when an exact spawn is retried",
    "allows legacy disk refresh but rejects exact runtime route mutation",
    "does not re-route or mutate a digest-bound exact task at the execution boundary",
    "reports nothing when the plan task and the disk artifact agree",
    "names the exact top-level field, with both sides",
    "recurses one level so a nested scope drift is named scope.filesWrite",
    "marks a field missing on one side as (absent) rather than silently skipping",
    "truncates a huge value so the operator message stays readable",
    "keeps the code as the first token so message.includes(code) consumers still match",
    "stays a bare code when there is nothing extra to say (back-compat)",
    "EXACT_PLAN drift gets the artifact-identity remedy, NOT \"check provider credentials\"",
    "applies to every exact-plan code, including the missing-artifact case",
    "a genuinely unknown spawn error still falls back to the generic hint",
    "names each attempt and its error in order",
    "is empty for a single attempt — nothing was hidden, so nothing is added",
    "is empty when there are no recorded attempts",
    "carries a non-Error rejection without throwing",
    "preserves the error code when one is present"
   ],
   "orchestra/exact-plan-start-service.test.ts": [
    "refuses a legacy intent snapshot before PREPARED or process birth",
    "refuses an authority downgrade with no source binding before PREPARED",
    "refuses v5 planning context downgraded to legacy directives before PREPARED or process birth",
    "requires the exact planned v5 authority before lower exact-ref start",
    "orders PREPARED → START_REQUESTED → process birth and publishes handle only at ADMITTED",
    "defers a fresh exact-Docker task projection until per-task release",
    "commits a canonical recovery manifest before a retry process can be born",
    "permits unavailable-token immediate capability admission but refuses adoption without it",
    "never overwrites a conflicting task artifact",
    "blocks closed-scope drift before creating any task artifact",
    "materializes a digest-bound planned-new output while rejecting pre-start creation drift",
    "migrates only an adoption-bound missing structured-criteria projection before admission",
    "facade returns settled only after admission and terminal settlement",
    "publishes a persisted runtime failure after admission=$admitted",
    "holds after a persisted settlement when terminal lifecycle publication fails",
    "publishes blocked no-admission truth and republishes it idempotently on duplicate replay",
    "facade preserves an unplanned planner-evidence refusal as held with no process birth"
   ],
   "orchestra/exact-production-wiring-host-settlement.test.ts": [
    "keeps production wiring on typed HOLD when no trusted host observer is composed",
    "persists, re-reads, and fans the same host settlement into T11 terminal authority",
    "leaves no T11 terminal artifacts when the trusted host observer is unavailable",
    "rejects an observer that does not machine-bind ingress, enablement and proof targets",
    "rejects a proof receipt whose group harness is not the digest-bound plan harness",
    "rejects a host observation timestamp older than the exact accepted-result chain",
    "does not promote host observation when immutable worker evidence is absent",
    "rejects tampered durable bytes, sibling attempts, and accepted-chain replay"
   ],
   "orchestra/exact-rejected-result-closure.test.ts": [
    "survives a new Store and replays without acceptance or duplicate publication",
    "rejects a wrong source digest or foreign recovery authority",
    "prevents publishing acceptance after the negative terminal marker",
    "fails closed when the first-writer terminal record is corrupted",
    "rejects a backdated decision and a conflicting first-writer replay"
   ],
   "orchestra/exact-released-unaccepted-recovery.test.ts": [
    "preserves all bytes, survives restart and replays without acceptance",
    "refuses wrong sprint authority and a timestamp older than release",
    "refuses replaced release evidence instead of accepting a digest assertion"
   ],
   "orchestra/exception-handler.test.ts": [
    "writes error.json with redacted payload on uncaughtException",
    "writes error.json on unhandledRejection",
    "handles SIGTERM with graceful exit code 143",
    "idempotent — installCrashHandlers iki kez çağrılırsa tek listener kalır",
    "writes error.json even when IPC dir does not pre-exist content"
   ],
   "orchestra/exec-throw-armor.test.ts": [
    "does not throw when scope.directories/filesWrite are undefined (malformed/legacy task)",
    "preserves existing behavior for a well-formed scope",
    "skips a dep-blocked head-of-queue task IN PLACE — it stays in remainingQueue",
    "lets the previously-blocked queued task enter the spawn plan once its dependency completes",
    "does not disturb an already-assigned queue entry\\'s drop semantics (no requeue needed)",
    "retries a dependency-ready task after buildWorkerPrompt throws once, instead of leaking it into assignedTaskIds forever",
    "survives transient identical tick errors and still completes once they stop",
    "escalates (rethrows) once the SAME error repeats on more than 5 consecutive ticks",
    "logs a tick-armor debug entry when a tick step throws"
   ],
   "orchestra/execute-fix-quiescence.integration.test.ts": [
    "drains admitted work before spawning and settling the NO_GO repair under an unlimited timeout"
   ],
   "orchestra/execution-continuation-runner.test.ts": [
    "uses only the async exact custody port and awaits the released start-bound identity",
    "never falls back to generic spawn when an exact continuation port is incomplete",
    "publishes zero continuation claim for contained or ambiguous exact dispatch",
    "rejects the legacy continuation surface for a production exact Docker backend",
    "dispatches once and adopts the same first-writer lineage on retry",
    "holds before claim when backend landing capability is unsupported",
    "holds an exact xverify parent before continuation claim or generic spawn",
    "holds before claim when requested backend differs from checkpoint identity",
    "retries the exact claimed attempt when host crashed before Docker prepare",
    "holds an under-reserved legacy checkpoint before first claim and spawn",
    "holds before claim when the observed parent startup floor cannot fit",
    "holds before claim when immutable parent startup evidence is missing",
    "holds an under-reserved pre-dispatch claim but adopts durable dispatch evidence"
   ],
   "orchestra/execution-effect-docker-lifecycle.test.ts": [
    "preserves raw Docker daemon CreatedAt nanos and offsets as volume identity",
    "requires durable ALLOCATING reread before any adapter effect and rehydrates a fresh token",
    "projects sensitive paths out without reading or returning their names",
    "admits public design token assets but keeps credential-shaped token JSON fail-closed",
    "round-trips host-screened exclusions through allocation without exposing their paths",
    "denies an explicit sensitive write scope before any lifecycle adapter effect",
    "binds image-owned read-only dependencies outside the workspace effect manifest",
    "performs exact absence, labeled create, inspect, population, seal and no-change release",
    "keeps empty filesWrite as a mandatory read-only policy and rejects null policy input",
    "returns typed unsupported before any adapter call on Darwin and Windows",
    "maps admitted WSL execution to the explicit wsl2-linux snapshot authority",
    "does not create over a pre-existing exact volume",
    "rejects mutable or foreign daemon image identity before volume inspection",
    "fails closed before workspace creation when dependency-volume authority is unavailable",
    "rejects post-create identity drift before trusted population",
    "requires an exact baseline revalidation before provider start",
    "accepts the same logical baseline reopened in a fresh Linux mount namespace",
    "accepts final captures of the same volume from fresh Linux mount namespaces",
    "requires zero foreign volume attachments before provider authorization",
    "requires stopped-provider detachment and two identical final captures",
    "rejects proxy capture results and post-call adapter uncertainty",
    "captures stopped-provider hidden effects and quarantines the whole attempt",
    "allows a declared changed effect and requires COMMITTED rather than no-change",
    "rejects foreign stop authority before final capture",
    "refuses deletion/absence evidence before a canonical committed landing receipt",
    "closes release from intent-bound reconciled absence after a delete receipt crash gap",
    "recovers provider-container compensation from durable Store authority",
    "replays the same projection after a crash before landing publication",
    "cannot release when dependency-volume deletion or absence evidence is missing",
    "rejects accessor adapter methods before any lifecycle effect",
    "redacts Bearer tokens without requiring sk-ant in the same message",
    "redacts sk-ant keys without requiring Bearer in the same message",
    "strips control characters from adapter failure messages",
    "truncates multibyte adapter failure messages without splitting code units",
    "truncates long single-byte adapter failure messages to 512 bytes",
    "preserves string error codes on adapter failures when present",
    "serializes debugLog adapter failures with stage before message",
    "keeps evidenceDigest stable when only adapter failure message differs",
    "surfaces prepare-call adapter failures with redacted bounded detail",
    "surfaces provider-start revalidation adapter failures with redacted bounded detail",
    "surfaces rehydrate-allocation adapter failures when allocation methods are missing",
    "does not accept forged or reused opaque lifecycle sessions",
    "preserves typed capture stage and bounded command details through prepare failure projection"
   ],
   "orchestra/execution-effect-landing-coordinator.test.ts": [
    "derives landing intent through one deterministic strict authority helper",
    "distinguishes staged-source %s failure from project preimage mismatch",
    "preserves post-lease %s %s leaf and context even if quarantine publication throws",
    "passes the canonical landing intent from prepare into staged source authority",
    "never exposes an apply session before the durable locator and completes an exact retry",
    "recovers a locator-published apply-not-started crash without process-local session state",
    "fails closed for stale, sibling and tampered durable locators",
    "never backfills a missing locator after an APPLYING or COMMITTED journal exists",
    "lands only through staged chunks, immutable journal steps, final fan-in and terminal lease evidence",
    "commits an empty diff as COMMITTED_NO_CHANGE without opening an irreversible boundary",
    "fails closed when production adapters are missing, proxied or accessor-authored",
    "preserves an immutable authority snapshot after caller-owned manifest mutation",
    "derives missing parent creation before child add and binds the child parent CAS to that step",
    "lands an explicitly allowed empty directory as one real ADD_DIRECTORY effect",
    "derives every missing nested ancestor from the exact real child effect",
    "validates partial journal without adopting or mutating: %s",
    "reuses only identity-pinned derived directories while keeping the child ADD",
    "rejects replacement of a reused directory after preparation",
    "rejects a shared derived directory with a conflicting mode",
    "rejects a changed durable native prefix before successor lease adoption",
    "reconciles the exact applied crash prefix without replaying the native effect",
    "keeps journal causal time after a wall-clock rollback during %s",
    "uses the shared host clock at the prepared boundary and rejects malformed clock authority",
    "adopts a PREPARED-only crash from the immutable acquired lease snapshot",
    "finishes a verified changed COMMITTED journal whose terminal publication crashed",
    "finishes a verified no-change COMMITTED journal whose terminal publication crashed",
    "returns an already terminal transaction read-only without lease adoption",
    "bounds quarantine evidence after a late failure in a large operation set",
    "quarantines instead of committing when the native final full-postimage fan-in fails",
    "rejects a native hardlink postimage even when its semantic content matches",
    "holds after COMMITTED publication when durable lease terminal evidence is missing",
    "rejects malformed journal bytes and missing durable lease terminal evidence on reread",
    "rereads and rejects a corrupted STEP instead of trusting COMMITTED alone",
    "rejects a nested proxy before manifest parsing can execute proxy traps",
    "uses deterministic code-point ordering for Unicode paths",
    "rejects a forged opaque session and stale project preimage under the acquired lease"
   ],
   "orchestra/execution-effect-lock-adapter.test.ts": [
    "binds a changed transaction to one maintenance fence and durable terminal audit",
    "settles a no-change transaction through a durable terminal contract",
    "rejects a forged or stale lease object",
    "adopts an exact dead active lease across repeated process restarts",
    "rebinds an immutable applying boundary to the fresh dead-owner fence",
    "keeps active lease restart fail-closed when owner liveness is %s"
   ],
   "orchestra/execution-effect-native-adapter.test.ts": [
    "captures a canonical %s manifest from the sealed Docker volume",
    "returns typed workspace-capture HOLD on %s",
    "rejects a workspace-capture receipt with foreign %s",
    "rejects Docker capture output above the hard 16 MiB ceiling",
    "rejects full-entry tampering even when the native path set is unchanged",
    "snapshots capture filesWrite before the async Docker boundary",
    "rejects a proxy capture input before Docker access",
    "returns typed HOLD on %s",
    "maps the canonical native missing-entry code to an absent project preimage",
    "retains Node ENOENT compatibility for an absent project preimage",
    "keeps non-not-found native inspection failures fail-closed",
    "writes every source and receipt byte after finite short writes",
    "retries interrupted, backpressured, and zero-progress writes without dropping bytes",
    "bounds persistent source-write backpressure by the existing deadline",
    "rejects invalid progress and preserves non-retryable write failures",
    "streams Docker bytes through native ingress and durable Store chunks",
    "supports a single durable zero-byte chunk",
    "rejects a validly sealed Docker receipt with foreign %s",
    "fails verification after a Store chunk semantic tamper",
    "rejects staged-source replay under a foreign attempt digest",
    "snapshots source authority before the async Docker boundary",
    "rejects accessor-backed source authority before Docker access",
    "rejects root-separation uncertainty before Docker or Store access",
    "keeps capability identity stable when only root directory cardinality metadata changes",
    "treats native handle-close uncertainty as HOLD",
    "rematerializes Store chunks into a fresh native staging object per adapter invocation",
    "rejects a proxy factory input before native or Docker access"
   ],
   "orchestra/execution-effect-store-adapter.test.ts": [
    "fails closed when a later cleanup artifact exists behind a missing predecessor",
    "publishes and rereads ALLOCATING before project-root landing authority exists",
    "publishes coordinator journals and exposes accepted authority only after full Store fan-in",
    "proves a committed coordinator journal without mutating Store bytes",
    "rejects %s evidence when immutable bytes are %s",
    "rejects committed-pending evidence after release progress is durable",
    "rejects journal replacement, forged references, missing landings and policy-null authority",
    "proves a production-shaped coordinator commit before release or accepted-result publication",
    "rejects %s evidence when immutable bytes are %s",
    "rejects committed-pending evidence after release progress is durable"
   ],
   "orchestra/execution-landing-coordinator.test.ts": [
    "accepts only a fully equal released and terminal exact-custody landing",
    "writes the pre-provider baseline then stamps the path-free %s billing custody chain",
    "selects the finite proposal cadence only for the closed xverify protocol",
    "does not mint a checkpoint when the attempt-bound proposal is absent",
    "rejects LANDED when exact terminal usage exceeded after the reserve trigger",
    "rejects semantic proposals that predate scoped disk work",
    "holds the terminal production path with resumable attribution for a regressed sequence",
    "stamps host truth around an untrusted attempt-bound semantic proposal",
    "adds no prompt or context when no landing policy is present"
   ],
   "orchestra/execution-landing-proposal-consumer.test.ts": [
    "binds an exact producer proposal to the typed durable checkpoint receipt",
    "rejects an existing orphan artifact instead of treating existence as consumption"
   ],
   "orchestra/execution-recovery-adapter.test.ts": [
    "exports exactly the canonical mode/platform/capability/primitive vocabularies",
    "maps every platform to exactly one honest process identity primitive",
    "declarationSupports checks the exact declared capability list",
    "maps every allowed core operation to exactly one capability, never inventing a decision",
    "maps RESUME_EXACT_ATTEMPT to the resume capability",
    "maps FINALIZE_EXACT_ATTEMPT to the settle capability",
    "derives abort/terminate only when the canonical core outcome explicitly allows them",
    "returns the adapter evidence unchanged when native evidence is not mutated",
    "rejects unsupported inspect capability without ever calling the adapter",
    "detects in-place mutation of native evidence during inspect and rejects it",
    "delegates to adapter.apply exactly once when identity and capability match",
    "rejects a foreign identity before ever calling adapter.apply (no clearing foreign authority)",
    "rejects an unsupported capability before ever calling adapter.apply (no silent fallback)",
    "never retries: a failing adapter.apply is still called exactly once"
   ],
   "orchestra/execution-recovery-assurance.test.ts": [
    "%s yields one typed provider-neutral decision",
    "PID reuse / ignored termination evidence fails before external effect",
    "healthy inspection performs no process probe, persistence, lock, or effect work",
    "refuses cleanup while live Sprint authority exists",
    "requires an exact generation/fence approval before any recovery mutation"
   ],
   "orchestra/execution-recovery-service.test.ts": [
    "decides from adapter evidence without process or persistence mutation",
    "classifies exact coordinator %s / process %s / lock %d→%d as %s",
    "does not report a stale PID/state projection as live non-resumable execution",
    "durably resolves orphaned NO_GO with a complete exact-fix repair decision",
    "durably parks every inconclusive NO_GO as a resumable HOLD",
    "never claims a NO_GO resolution when its decision is not durable",
    "applies an approval/fence-bound resume once and durably records an immutable receipt",
    "refuses a duplicate effect before invoking the adapter again",
    "refuses out-of-order and mismatched approval/fence commands without effects",
    "records a failed continuation once while leaving recovery evidence resumable",
    "supports explicitly approved, exact-identity %s only when the core outcome allows it",
    "never reports success when the receipt cannot be durably committed"
   ],
   "orchestra/execution-request-builder.test.ts": [
    "threads actor from ExecutionRequest onto Task when present",
    "leaves task.actor undefined when actor is absent (backward-safe)",
    "preserves actor with only required id field",
    "does not affect other Task fields when actor is set",
    "forwards actor from input to ExecutionRequest",
    "leaves req.actor undefined when not supplied",
    "preserves maxTokens/maxUsd through ExecutionRequest into Task JSON shape",
    "accepts a known ID exact and infers its owning provider from the registry",
    "rejects an unseen cloud ID without pricing evidence even with an explicit provider",
    "rejects a legacy alias (gpt-5 / sonnet) before it can reach a Task",
    "rejects an unknown ID without a provider (never guesses ownership)",
    "rejects a provider/model mismatch for a known model",
    "rejects an unknown explicit provider string (no garbage provider on unseen IDs)"
   ],
   "orchestra/export-wipe-guard.test.ts": [
    "refuses to overwrite decisions.md when the render collapses to the empty marker while DB has ADRs (preserves previous file)",
    "writes decisions.md normally when DB has ADRs and the renderer emits real content",
    "writes decisions.md when DB has no ADRs (empty render is correct, not catastrophic)",
    "emits a descriptive warning per guard trip and preserves memory.md alongside decisions.md",
    "writes debt.md unconditionally (not gated by the empty-marker heuristic)"
   ],
   "orchestra/f10-deepen.test.ts": [
    "activation(pass)+condition(pass)+rbac(admin,pass) → outcome=success, status=done",
    "condition gate false → entry parked, runTask not called",
    "viewer role (RBAC deny, EXECUTE) + shell capability → parked",
    "risk-gate: permit+shell+risk_gate_enabled → park (independent of RBAC)",
    "risk-gate disabled (risk_gate_enabled:false) + shell → NOT parked (backward-safe)",
    "admin role + EXECUTE permission → RBAC permit → execution proceeds",
    "operator role + EXECUTE permission → RBAC permit → execute",
    "policyEngine absent → no gate applied, executes normally",
    "policyEngine.enabled=false → gate skipped"
   ],
   "orchestra/f1014-auth-isolation.test.ts": [
    "does NOT inject OPENAI_API_KEY into claude (sonnet) container",
    "does NOT inject GOOGLE_API_KEY into claude (sonnet) container",
    "does NOT inject OPENAI_API_KEY into claude (opus) container",
    "holds codex (gpt-4.1) before any container can inherit ANTHROPIC_API_KEY",
    "holds canonical gpt-5.6-sol before any container can inherit ANTHROPIC_API_KEY",
    "holds Gemini before any container can inherit ANTHROPIC_API_KEY",
    "Claude container has no OPENAI/GOOGLE key; Codex is held before container work",
    "Claude container has no GOOGLE_API_KEY; Gemini is held before container work",
    "injects CLAUDE_AUTH_REQUIRED for claude (sonnet)",
    "holds Codex before any CLAUDE_AUTH_REQUIRED env can be assembled",
    "holds Gemini before any CLAUDE_AUTH_REQUIRED env can be assembled"
   ],
   "orchestra/failure-disposition-chain.test.ts": [
    "preserves policy-terminal exemptions while keeping worker NO_GO remediation live"
   ],
   "orchestra/failure-disposition-fix-gate.test.ts": [
    "records the suppression and lands exactly one scheduler NO_MINT instead of minting a repair"
   ],
   "orchestra/finalize-coordinator-retirement.test.ts": [
    "sprint-480: live unowned coordinator produces terminal-red typed HOLD",
    "sprint-481: dead coordinator permits terminal receipt with death proof",
    "rejects receipt publication when required retirement proof is HOLD"
   ],
   "orchestra/finalize-error-surface.test.ts": [
    "regression: finalizeSprint success still returns metrics, no notify/stderr noise",
    "a throwing finalizeSprint reaches stderr, fires notify, and returns the finalizeFailed marker (no re-throw)",
    "localizes the notice for config.language \"tr\" (i18n-first — not hardcoded to one language)",
    "a non-Error throw (e.g. a rejected string) is still surfaced without crashing"
   ],
   "orchestra/finalize-orphan-cleanup.test.ts": [
    "finalize → sprint-state.json status COMPLETE / phase COMPLETE",
    "pids — .pid and .snapshot.json are removed",
    "state-yok → no-op (does not materialize a fresh sprint-state.json)",
    "--force idempotent — second call produces the same terminal state",
    "state preserved without .pid — clearPid silently no-ops"
   ],
   "orchestra/finalize-sprint.test.ts": [
    "should return calculated metrics",
    "holds unknown terminal billing authority after receipt but before archive",
    "should set sprint.metrics",
    "should call writeSprintLog",
    "should call writeRetrospective (updates MEMORY.md and RETRO.md)",
    "should call updateLastSprintId",
    "should call runDecay",
    "should skip decay when skipDecay option is true",
    "should run afterSprint hooks",
    "should skip hooks when skipHooks option is true",
    "should call updateProjectDocs when config is provided",
    "should NOT call updateProjectDocs when config is not provided",
    "refuses to settle an unevaluated task (fail-closed, was: empty evaluations gracefully)",
    "refuses to archive a zero-task sprint (fail-closed, was: empty tasks gracefully)",
    "should survive writeSprintLog failure",
    "should survive writeRetrospective failure",
    "should survive runDecay failure",
    "should survive afterSprint hook failure",
    "should be idempotent-safe: calling twice does not throw",
    "refuses plain finalize while a logical task is NO_GO (fail-closed, was: mixed evaluations)",
    "should combine all options correctly",
    "should accept empty options object",
    "should accept undefined options",
    "records a prompt-version use per task agent (V1 routing path)",
    "records a prompt-version use on the V2 routing path too (dogfood path)",
    "does not record for a DEFERRED task (worker never executed)",
    "discovers disk-born FIX tasks for sprints below 100"
   ],
   "orchestra/fix-agent-selection.test.ts": [
    "test task (ci-guardian agent) → preserves ci-guardian, not bug-fixer",
    "doc task (doc-writer agent) → returns doc-writer",
    "bug task (bug-fixer agent) → returns bug-fixer",
    "test task via ci-testing skill → preserves original agent",
    "exit-no-result → original agent re-run (no rotation)",
    "generic task → refuses a read-only fresh-eyes persona and routes to an implementer",
    "test task → fix task assignedAgent and forceAgent are ci-guardian, not bug-fixer",
    "doc task → fix task agent is doc-writer",
    "bug task → fix task agent is bug-fixer",
    "exit-no-result → fix task reuses original agent for re-run"
   ],
   "orchestra/fix-budget-counting.test.ts": [
    "spends the slot on an admitted-but-deferred fix, which can never be re-admitted",
    "lets a deferred sibling strand while the executed lineage burns the rest of the budget",
    "caps an executing lineage at exactly max_fix_retries admitted rounds",
    "admits nothing at all when the budget is zero",
    "counts depth by explicit fixForTaskId authority",
    "gates admission on depth <= maxFixRetries",
    "admits at most one attempt per logical root, shallowest depth first",
    "refuses to admit a child while its parent attempt is still in flight",
    "does not pause a small run below the authored absolute count (scale-honest, PLANNER-TRUTH 2026-08-18)",
    "does not pause when the surviving repairs kept the ratio under policy",
    "keeps the only %-shaped knob on the pause gate, not on the budget",
    "derives the budget as an integer round count with no percentage term",
    "still calls the budget \"admitted FIX rounds\" and loops on maxFixRetries",
    "marks every selected fix attempted BEFORE the wave is spawned",
    "publishes the undispatched remainder of an admitted wave instead of hiding it"
   ],
   "orchestra/fix-dispatch-continuation.test.ts": [
    "an attempt with no lineage releases nothing",
    "a NO_GO attempt withholds the release (repair-rejected)",
    "GO_WITH_TECH_DEBT counts as an accepted aggregate settlement",
    "a fix-of-a-fix resolves to the ROOT, not the intermediate attempt",
    "an accepted attempt whose lineage still has an in-flight sibling withholds the release",
    "a self-referencing fixForTaskId is cycle-bounded, not an infinite walk",
    "accepted FIX folds into the logical lineage and is never counted as a new task",
    "accepted FIX releases the root dependants (PAUSED → PENDING) on aggregate DONE",
    "NO_GO FIX preserves PAUSE — dependants stay parked and the withholding is observable",
    "an accepted attempt withholds the release while a sibling repair attempt is in flight",
    "the FIX wave ingests results under Brain authority — a raw self-claim cannot release a dependency",
    "the NOT_DISPATCHED re-dispatch wave settles on the Brain verdict, not the raw claim",
    "the POSTFIX-PENDING-SCAN safety net settles on the Brain verdict, not the raw claim"
   ],
   "orchestra/fix-eval-audit-trail.test.ts": [
    "fix DONE + orijinal reconcile → fix attempt-1 VE orijinal attempt-2 yazılır",
    "fix NO_GO (reconcile YOK) → sadece fix attempt-1, orijinal attempt-2 YAZILMAZ",
    "fixForTaskId yoksa orijinal attempt-2 yazılmaz (defansif)"
   ],
   "orchestra/fix-failure-classification.test.ts": [
    "turns a fix-ineligible pre-dispatch settlement into a referenced cascade skip",
    "admits a pre-dispatch settlement only when canonical policy opts it in",
    "fingerprints normalized typed provenance stably",
    "stops an identical typed failure after one FIX",
    "allows a bounded FIX when typed failure evidence changed",
    "routes a SIGKILL/OOM exit to an unchanged re-run",
    "routes a coordinator crash before backend prepare to an unchanged re-run",
    "routes a runtime budget circuit-breaker kill to an unchanged re-run",
    "refuses a fix task for a boundary violation and demands a scope revision",
    "treats a host attribution HOLD with out-of-scope claims as a scope violation",
    "escalates an unsatisfiable task definition instead of re-running it",
    "re-issues a dishonest completion claim with a hardened instruction, not a plain retry",
    "corrects a verification gap narrowly when real work already landed",
    "keeps an ordinary acceptance shortfall as a fix attempt — that is what FIX is for",
    "escalates once the lineage has proven re-running changes nothing",
    "still allows a fix while the lineage is below the futility threshold",
    "does not call repetition futile when the latest attempt actually changed code",
    "escalates a missing result — absence is unmeasurable, not a retryable blip",
    "lets a scope violation outrank a substantially-complete diff",
    "routes typed upstream attribution repair with write authority"
   ],
   "orchestra/fix-mode.test.ts": [
    "(a) DONE + rubric all ≥90 → verify-only",
    "(b) DONE + boundary violation in notes → amend",
    "(c) NO_GO + testsPassed=false → re-implement",
    "(d) NO_GO + testsPassed=true → amend (safest default)",
    "GO_WITH_TECH_DEBT → amend",
    "DONE + rubric partially below 90 → amend (not high enough for verify-only)",
    "DONE + scope_violation in notes → amend (boundary detection via \"scope.?violation\")",
    "DONE + no rubricScores → amend (cannot verify scores)"
   ],
   "orchestra/fix-model-preserve.test.ts": [
    "inherits forceModel/provider/backend/modelEffort the producer dropped (born-476 fixture: gpt-5/codex/subprocess)",
    "old no-inheritance behavior no longer occurs (negative test): fields are never left undefined when the original pinned them",
    "only inherits fields the original actually pinned — leaves the rest untouched",
    "persists the reconciled fix-task back to disk when a field is inherited",
    "emits a METRIC_EMITTED event when a field is inherited — no silent change",
    "keeps an already-set fix-task provider that differs from the original (e.g. provider-fallback policy)",
    "reports a conscious override via the emitted event instead of staying silent",
    "is a no-op for a non-fix task (isPriorityFix falsy)",
    "is a no-op when isPriorityFix is true but fixForTaskId is missing",
    "is a no-op (never throws) when the original task file cannot be read",
    "does not write or emit when the fix-task already carries every field the original has"
   ],
   "orchestra/fix-phase-map.test.ts": [
    "re-authorizes a dynamic Codex FIX task from owner policy before dispatch",
    "returns a typed FIX failure instead of swallowing missing budget authority",
    "dispatches only pending fix tasks owned by the current sprint namespace",
    "does not dispatch a legacy unscoped orphan fix from another run",
    "fix task DONE → original task evaluation updated to DONE in Map",
    "fix task GO_WITH_TECH_DEBT → original task evaluation updated to GO_WITH_TECH_DEBT in Map",
    "fix task NO_GO → original task evaluation remains unchanged (still NO_GO)",
    "fixForTaskId undefined → orphan is ignored without crashing",
    "evaluations Map starts empty, fix task populates correct key-value pairs",
    "dispatches a fix-of-a-fix in the same run and settles one logical root"
   ],
   "orchestra/fix-phase-trace.test.ts": [
    "threads attempt/retryOf/purpose/verdict into the written meta when provided",
    "additive: unset fields are OMITTED (pre-TT551 shape byte-identical — consumer-safe)",
    "records the fix attempt as a separate labeled entry alongside the NO_GO original",
    "runFixPhase body wires the training_trace-gated recorder with additive fix labels"
   ],
   "orchestra/fix-repair-authority.test.ts": [
    "admits only exact tracked evidence paths within reviewed directories",
    "is deterministic across order and duplicate evidence",
    "pauses without granting a delta when an evidenced path is outside review",
    "pauses for invalid or untracked evidence instead of inferring a path",
    "turns the same impossible authority fingerprint into a repeated pause"
   ],
   "orchestra/fix-result-brain-eval.test.ts": [
    "enriches a fix-result with brainEvaluation + brainEvaluationReason",
    "produces the SAME brainEvaluation block shape as the main (EVALUATE) path",
    "never adds rubricScores to the fix-result (audit-only invariant)",
    "preserves every non-Brain result field unchanged",
    "is a silent no-op (no throw, no file) when the .result is absent",
    "does not throw on a malformed .result (readJsonSafe → null)",
    "derives concrete_test_failed for a NO_GO fix-result with testsPassed:false",
    "a thrown evaluateWithRubric no longer collapses a valid DONE+tests result to a fabricated rubric total 0",
    "thin durable evidence on a thrown evaluation → honest GO_WITH_TECH_DEBT, not a fabricated zero and not NO_GO",
    "a concrete test failure still forces NO_GO even when rubric evaluation throws",
    "worker self-NO_GO still forces NO_GO even when rubric evaluation throws",
    "appends an [evaluation-fault recovery] note documenting the reconstructed decision",
    "a healthy evaluateWithRubric call is returned unchanged — the durable-evidence path is fault-only"
   ],
   "orchestra/fix-retry-circuit-breaker.test.ts": [
    "parks an unchanged impossible fingerprint before another provider-dispatchable task exists",
    "permits one bounded retry only after an exact authority delta from the prior impossible fingerprint",
    "parks an exhausted failed attempt without creating an infinite FIX chain or completion"
   ],
   "orchestra/fix-spawn-observability.test.ts": [
    "S6: a queued FIX task held back by an exhausted worker pool publishes worker-slot-exhausted — never a silent empty pass",
    "S5: dependency_pipeline_enabled=false returns early — the queued FIX task is named with dependency-pipeline-disabled",
    "S8: an unsatisfied dependency is published as dependency-unsatisfied naming the blocking dependency",
    "a pass that DOES spawn the FIX task publishes a spawn, and adds no skip for it",
    "reducer tick: a SpawnTask effect the executor declines is journaled with its typed reason",
    "every non-spawned disposition maps to a typed reason; a real spawn maps to none",
    "a persisting stall costs ONE journal line, not one per pass, until the reason changes",
    "skip records share the existing journal file — no new file family"
   ],
   "orchestra/fix-task-enrichment.test.ts": [
    "description-inject: fix-task.description contains the ORIGINAL task description",
    "nogo-reason-inject: fix-task.description carries an explicit \"## NO_GO Reason\" with failure signals",
    "carries the host schema failure into the next FIX instead of replaying implementation blindly",
    "inherits the exact typed verification authority including command prefixes",
    "empty-description-fallback: when original description is empty the fix-task uses an explicit fallback marker (no crash)",
    "idempotent: two handleEvaluation calls with identical input write byte-identical fix-task descriptions",
    "worker prose never widens the FIX write scope — inherited authority is carried exactly",
    "out-of-scope prose cannot park the FIX — no birth-time PAUSED trap, authority stays accepted"
   ],
   "orchestra/fix-task-force-skills.test.ts": [
    "auto-assigned parent skills are NOT promoted to forceSkills on the fix task",
    "the parent's GENUINE operator forceSkills are inherited with full severity"
   ],
   "orchestra/force-abort-sprint.test.ts": [
    "publishes ABORTED truth and preserves unresolved task evidence for cleanup"
   ],
   "orchestra/forced-skill-lineage-wire.test.ts": [
    "re-adds a forceSkills id that filterSkillPromptsByDNA scored 0 for an unrelated task intent",
    "does not duplicate a forced skill that DNA filtering already kept",
    "a non-forced skill that scores 0 under DNA filtering is dropped as before (487-023 does not weaken filtering)",
    "adds a missing forceSkills id back into assignedSkills after routing",
    "preserves a forced skill id alongside an unrelated already-assigned skill (union, not replace)",
    "does not duplicate a forced skill id already present in assignedSkills",
    "leaves assignedSkills untouched when no forceSkills is declared",
    "writes a typed NO_GO and skips spawn when a forced skill failed to resolve (missing)",
    "writes a typed NO_GO and skips spawn when a forced skill is administratively disabled (inactive)",
    "spawns normally when the forced skill resolves and is active (positive control)",
    "does not honest-fail a task with no forceSkills even if assignedSkills fails to resolve",
    "plan site — a decision without the forced id keeps it (force first, then routed)",
    "single-task site — an EMPTY V3 result cannot erase the force",
    "both sites agree on the same effective assignment for the same inputs",
    "excludeSkills prunes a routed id but never the forced one (directive force wins)",
    "a routed id is never duplicated when the operator also forced it",
    "no directives — the V3 decision is assigned verbatim (no behaviour change)",
    "the merged assignment still survives routeSprintTasks (Part 2 lineage, end-to-end)"
   ],
   "orchestra/format-consistency.test.ts": [
    "parseSprintLog parses Total Tasks written by sprintLogUpdater",
    "parseSprintLog reads No-Go header (not NO_GO)",
    "sprintLogUpdater writes field names matching parseSprintLog regex patterns",
    "Files Changed header is consistent",
    "parses Agents and Skills sections consistently",
    "loadLearningData is not exported from history.ts",
    "history module exports only expected functions",
    "parseDebtTable parses debt table rows correctly",
    "parseDebtTable returns empty array for empty content",
    "archive-debt imports parseDebtTable from core/utils (integration)",
    "debt-manager uses parseDebtTable from core/utils (integration)"
   ],
   "orchestra/fresh-eyes-rotation.test.ts": [
    "opus is preserved (no downgrade)",
    "sonnet is preserved",
    "haiku is preserved",
    "gpt-5 is preserved",
    "gpt-4.1 is preserved",
    "o3 is preserved",
    "gemini-2.5-pro is preserved",
    "gemini-3.1-pro-preview is preserved",
    "returns same model for unknown models too",
    "architect → code-reviewer",
    "bug-fixer → code-reviewer",
    "code-reviewer → bug-fixer (reverse rotation)",
    "security-auditor → code-reviewer",
    "test-writer → bug-fixer",
    "undefined → code-reviewer (default)",
    "null → code-reviewer (default)",
    "generic → code-reviewer (default)",
    "unknown agent → code-reviewer (default)",
    "returns enabled strategy for opus+architect",
    "adds companion skill for architect rotation",
    "rationale string contains original and rotated identifiers",
    "handles task without assignedAgent (treats as generic)",
    "preserves model (haiku stays haiku)",
    "does not mutate the input task",
    "writes fix task JSON with rotated model and agent",
    "fix task carries rotationStrategy field",
    "fix task sets forceModel and forceAgent for routing override",
    "fix task preserves isPriorityFix, CRITICAL priority, and PENDING status",
    "fix task merges existing assignedSkills with rotation companion skills",
    "fix task on sonnet+bug-fixer preserves sonnet AND keeps bug-fixer (Sprint 210-007 selectFixAgent)",
    "re-exports rotateModelForFix from debt-manager",
    "re-exports rotateAgentForFix from debt-manager",
    "re-exports applyFreshEyesRotation from debt-manager",
    "exports emitRotationMetricIfApplicable",
    "emitRotationMetricIfApplicable is a no-op for non-fix tasks",
    "emitRotationMetricIfApplicable swallows I/O errors for fix tasks"
   ],
   "orchestra/gate-techdebt-downgrade.test.ts": [
    "returns DONE when no verifyDeltaCompletionRatio provided (no sprint-level signal)",
    "downgrades DONE → GO_WITH_TECH_DEBT when completionRatio < 0.8 (DONE threshold)",
    "downgrades DONE → NO_GO when completionRatio < 0.5 (NO_GO threshold — severe)",
    "does NOT downgrade when completionRatio >= 0.8 (gate triggered but severity is mild)",
    "escalates GO_WITH_TECH_DEBT → NO_GO when completionRatio < 0.5",
    "preserves NO_GO without downgrade (already terminal)",
    "returns null (no-op) when gate is absent",
    "returns null (no-op) when max_tech_debt_ratio is 0",
    "returns null when totalTasks is 0 (avoid division by zero)",
    "returns null when debtRatio equals maxDebtRatio (boundary — no downgrade at exact threshold)",
    "returns null when debtRatio is below maxDebtRatio",
    "returns GO_WITH_TECH_DEBT when debtRatio mildly exceeds threshold",
    "returns GO_WITH_TECH_DEBT with 4/10 tasks debt and max 0.3",
    "returns GO_WITH_GATE_FAILURE when debtRatio severely exceeds threshold",
    "returns GO_WITH_GATE_FAILURE with extreme 8/10 debt and any threshold",
    "handles single task sprint: 1 debt task, max 0.3 → GO_WITH_TECH_DEBT",
    "handles decimal thresholds correctly",
    "GO_WITH_GATE_FAILURE is exported from result-evaluator"
   ],
   "orchestra/generated-skill-durability.test.ts": [
    "persistPlanGeneratedProjectConventionsSkill is an honest no-op (returns false, writes nothing)",
    "a legacy assignedSkills reference to the retired id resolves to a tolerant skip, not a prompt",
    "fails closed with FORCED_SKILL_UNAVAILABLE when a forced skill is genuinely absent"
   ],
   "orchestra/ghost-finalize.test.ts": [
    "1. removes checkpoint.json, .json.tmp and -checkpoint-seq",
    "2. is idempotent / fail-safe when no checkpoint files exist",
    "3. only removes the targeted sprint, leaving other sprints intact",
    "4. false when only sprint-state.json is COMPLETE for this sprint",
    "5. false when only the sprint-log markdown exists",
    "5b. true only when the canonical terminal archive verifies",
    "6. false when neither signal is present",
    "7. false when sprint-state COMPLETE but for a DIFFERENT sprint",
    "8. false when sprint-state exists but status is not COMPLETE (active)",
    "9. purges checkpoint artifacts during normal finalize",
    "10. `finalize --force` on an older sprint purges that sprint checkpoint",
    "11. leftover + sprint-state COMPLETE alone preserves checkpoint and does not start fresh",
    "12. leftover + sprint-log alone preserves checkpoint and does not start fresh",
    "12b. verified terminal archive authorizes checkpoint cleanup and fresh planning",
    "13. leftover + NOT finalized + all terminal → resume-evaluate for authoritative finalization",
    "14. leftover + NOT finalized + pending tasks → \"resume-evaluate\" preserved",
    "15. no checkpoint → \"fresh\" (existing behavior unchanged)",
    "16. finalized sprint with NO leftover checkpoint → \"fresh\" without spurious work"
   ],
   "orchestra/git-self-mutation-guard.test.ts": [
    "self-project: createSafetyPoint is a no-op (no git stash, no backup branch)",
    "self-project: createSafetyPoint emits breadcrumb log",
    "user-project: createSafetyPoint runs normally (creates branch)",
    "self-project: rollback is a no-op (git reset --hard NOT called)",
    "self-project: rollback returns breadcrumb message",
    "user-project: rollback calls git reset --hard"
   ],
   "orchestra/git-worker-guard.test.ts": [
    "starts with a POSIX shebang and embeds the real git path",
    "lists every denylisted subcommand in the case pattern",
    "en and tr messages both mention the exit code path and are distinct",
    "escapes an embedded single quote in the real git path",
    "writes an executable dir/git file",
    "is idempotent across repeated installs (retry/fix-worker re-spawn)",
    "blocks git %s with exit %s and a stderr message",
    "blocks `git stash` (bare)",
    "blocks `git reset --hard`",
    "blocks `git checkout -- .`",
    "blocks `git clean -fd`",
    "passes `git status` through to real git (exit 0, matches real output)",
    "passes `git diff` through to real git (exit 0)",
    "passes `git show HEAD:` through to real git (disk-verify path stays intact)",
    "passes an unknown subcommand through unchanged (permissive default)",
    "finds a real, existing git binary on this host",
    "falls back to the POSIX default when PATH has no git",
    "falls back to the win32 default when PATH has no git.exe",
    "buildDockerGitGuardArgs mounts the shim dir read-only under /workspace",
    "uses the documented Debian/Dockerfile.worker git path as the container default",
    "prependGitGuardToPath prepends onto an existing PATH using the platform delimiter",
    "prependGitGuardToPath handles an empty/undefined current PATH",
    "isGitGuardSupportedPlatform is false only for win32",
    "buildGitGuardPathExport produces a POSIX export line for the send-keys prefix",
    "lives outside any project directory (OS tmpdir), never embedding a caller-supplied path",
    "returns a different directory on every call, even for the same taskId (cross-project collision-safety)"
   ],
   "orchestra/git-workflow-service.test.ts": [
    "a non-git directory answers note:not-a-git-repo (honest, never a throw)",
    "clean repo → branch + clean; dirty repo → XY-coded entries incl. untracked",
    "parses sha/subject/author/date (field-separator survives spaces in subjects)",
    "unborn HEAD (no commits) → empty array, non-git → note",
    "worktree change shows a real hunk; staged:true shows only the staged set",
    "oversized diffs are truncated at the explicit cap (never silently)",
    "default stages everything under root (untracked included), explicit paths stage only those",
    "PARENT-REPO SAFETY: a project inside a host repo never stages the host\\'s files (`-A -- .`)",
    "a bad explicit path carries git\\'s own stderr honestly",
    "commits the staged set and reports the short sha",
    "empty message and nothing-staged both fail with honest errors (never a silent no-op)",
    "clean tree → note:clean; untracked-only tree is NOT clean (counted via status)",
    "intent becomes the subject (72-char cap, first line only) + the deckent-run trailer",
    "baseSha pins the stats to the run\\'s OWN feet (a commit made mid-run still counts)",
    "no handle → HEAD-fallback proposal still works",
    "a stored handle\\'s gitBase anchors the stats (same feet as --diff)",
    "captures stderr on failure and never throws on a missing cwd-command context"
   ],
   "orchestra/gitignore-invariant.test.ts": [
    ".deckent/cache/ files are not git-tracked",
    ".deckent/sprint-*-events.jsonl files are not git-tracked",
    ".deckent/sprint-*-seq counter files are not git-tracked",
    ".deckent/sprint-*-checkpoint.json files are not git-tracked",
    ".deckent/metrics.jsonl is not git-tracked",
    ".deckent/jobs/ files are not git-tracked",
    ".deckent/sprint-*-ipc/ files are not git-tracked"
   ],
   "orchestra/goal-narrative-not-a-task.test.ts": [
    "plans exactly the two bullet tasks; the Goal narrative produces none",
    "a Goal section BULLET is still work — only prose is excluded"
   ],
   "orchestra/golden-flow.test.ts": [
    "runs the full fake-seam flow end-to-end and returns a completed result",
    "emits a deterministic step-by-step event sequence (TERM-LIVE-shaped)",
    "re-running with identical seam outputs re-produces the identical event sequence (determinism)",
    "cancels cleanly when approvePlan rejects, never starting or evaluating",
    "cancels before intent when the signal is already aborted",
    "cancels at plan when the signal fires during deriveIntent",
    "cancels at start when the signal fires during approvePlan",
    "cancels at evaluate when the signal fires during startSprint",
    "emits an error event and rethrows when a seam throws",
    "renders a preview whose markdown matches buildDirectives(intent) exactly",
    "never mutates the input intent (defensive copies of array fields)",
    "contains no file-write call anywhere in the module"
   ],
   "orchestra/gonogo-unescape.test.ts": [
    "unescapes writer-escaped semicolons in GO/NOGO lines",
    "plain lines without escapes are unchanged"
   ],
   "orchestra/gwtd-fix-trigger.test.ts": [
    "reproduces the durable 522-002 verdict: worker GO_WITH_TECH_DEBT, Brain NO_GO at 43/100",
    "holds a passing score at the worker debt ceiling instead of raising it to DONE",
    "maps every rubric verdict onto the settlement enum without renaming it",
    "admits a FIX for a lineage the Brain settled NO_GO",
    "routes a lineage the Brain settled GO_WITH_TECH_DEBT to debt, never to FIX",
    "does not admit a FIX when only the worker says NO_GO",
    "does not admit a FIX when only the worker says GO_WITH_TECH_DEBT",
    "still admits a FIX when the Brain says NO_GO and the worker claimed DONE",
    "lets only the Brain-NO_GO lineage into the FIX spawn set"
   ],
   "orchestra/handoff-notes.test.ts": [
    "stores notes when provided",
    "omits notes field when not provided (backward compat)",
    "persists notes to disk and listHandoffs returns them",
    "listHandoffs returns handoff without notes when none was set",
    "passes handoffNotes from result to handoff.notes",
    "creates handoff without notes when result has no handoffNotes",
    "propagates same handoffNotes to multiple downstream tasks"
   ],
   "orchestra/handoff-prompt-inject.test.ts": [
    "returns empty string for undefined or empty entries",
    "renders the header and one \"- from … : artifacts [...], note: …\" line per entry",
    "omits the note segment when the handoff has no notes",
    "renders multiple upstream handoffs in the given (caller-sorted) order",
    "appends the Upstream Handoffs block when ctx.upstreamHandoffs has entries",
    "places the Upstream Handoffs block at the END (after Skills/Agent/Scope/Task regions)",
    "emits no block (byte-for-byte legacy prompt) when upstreamHandoffs is absent",
    "emits no block when upstreamHandoffs is an empty array (== no-field baseline)",
    "coexists with the Shared Context block (both in the END region, no timestamp leak)",
    "injects an executed handoff when worker_comms.enabled && inject_handoffs",
    "injects handoffs by default when enabled (inject_handoffs omitted)",
    "injects nothing when worker_comms is disabled (even with a ready handoff)",
    "injects nothing when inject_handoffs is explicitly false",
    "injects nothing when there is no handoff targeting this task",
    "skips a pending (not executed) handoff — only ready handoffs are injected",
    "renders multiple upstream handoffs targeting this task (deterministic id order)",
    "injects caller-supplied exact terminal handoffs without a mutable handoff file",
    "treats an exact empty handoff snapshot as authoritative over stale host projections",
    "keeps worker_comms disablement authoritative over supplied exact handoffs"
   ],
   "orchestra/handoff-protocol.test.ts": [
    "creates a handoff with pending status",
    "throws on empty fromTaskId",
    "throws on empty toTaskId",
    "throws on empty artifacts",
    "persists to file",
    "returns success when all artifacts exist",
    "returns failure with missing artifacts",
    "returns failure for unknown handoff",
    "marks handoff as ready on success",
    "returns failure for failed handoff",
    "marks handoff as failed with reason",
    "throws for unknown handoff",
    "returns empty array when no handoffs dir",
    "lists created handoffs",
    "sorts handoffs by id"
   ],
   "orchestra/handoff-recovery-wire.test.ts": [
    "marks pending handoff as failed when source task is NO_GO",
    "does not touch pending handoffs when evaluations contain no NO_GO",
    "leaves ready handoff untouched; only fails pending handoff from NO_GO source",
    "returns without error when no handoffs exist",
    "listHandoffs state is reflected in event payload when handoffs exist",
    "B-HANDOFF-STALE: summary excludes stale cross-sprint handoffs",
    "deletes stale cross-sprint handoffs and keeps only the current sprint",
    "keeps a handoff when EITHER endpoint belongs to the current sprint",
    "is a no-op (returns 0) when no handoff registry exists",
    "prunes stale cross-sprint handoffs, retains the current sprint, returns count > 0",
    "leaves an in-flight handoff intact when EITHER endpoint is in the current sprint",
    "is a no-op (returns 0) when no handoff registry exists",
    "swallows a thrown error from pruneCompletedSprints and returns 0"
   ],
   "orchestra/heartbeat-contract.test.ts": [
    "bind the same version, identity, backend, and activity-only fields",
    "ships at least one pending check",
    "every command in the default template is accepted by validateCommand",
    "the default template contains no shell metacharacters at all",
    "row-110 regression: no pipe-to-tail and no 2>&1 in the default template",
    "resolves its binaries through npx so the template is not platform-specific",
    "still rejects a genuinely hostile template wholesale",
    "still rejects each individual metacharacter, including the two the old template used",
    "still rejects a non-whitelisted base command even when metachar-free",
    "treats a silent exit-0 command as a pass with explicit empty-success output",
    "treats a non-zero exit as a failure and captures stderr without 2>&1",
    "blocks a piped command instead of running it",
    "skips completed tasks and counts only pending ones"
   ],
   "orchestra/heartbeat-daemon.test.ts": [
    "allows whitelisted commands",
    "rejects semicolon injection",
    "rejects ampersand injection",
    "rejects pipe injection",
    "rejects backtick injection",
    "rejects dollar sign injection",
    "rejects parenthesis injection",
    "rejects commands not in whitelist",
    "rejects curl (not whitelisted)",
    "rejects wget (not whitelisted)",
    "rejects empty command",
    "rejects whitespace-only command",
    "rejects null byte injection",
    "allows commands with path prefix if base name is whitelisted",
    "rejects commands with path prefix if base name is not whitelisted"
   ],
   "orchestra/honest-gate-deletion-aware.test.ts": [
    "keeps the 679-002 24-deletion/0-addition result DONE",
    "still rejects a genuine 0/0 stub without evidence",
    "preserves rejection of deletion-only work without verification evidence"
   ],
   "orchestra/honest-gate-deletion.test.ts": [
    "honest when linesRemoved>0 even with linesAdded=0 (module+test deletion)",
    "honest when only linesRemoved=1 (minimal deletion still skips empty-write flag)",
    "still flags genuine stub — linesAdded=0 linesRemoved=0 no disk evidence",
    "disk evidence bypasses empty-write check regardless of linesRemoved",
    "*.md file outside scope.filesWrite is NOT a boundary violation",
    "*.md file inside scope is still clean (no regression on in-scope docs)",
    "out-of-scope .ts source file IS still flagged as a boundary violation",
    "mixed: out-of-scope .md skipped, out-of-scope .ts flagged",
    "deletion + out-of-scope *.md doc update → honest (both FPs absent)",
    "out-of-scope *.ts source file STILL downgrades to NO_GO (regression guard)"
   ],
   "orchestra/honest-gate-garbage.test.ts": [
    "detects throw new Error(\"unreachable\")",
    "detects throw new Error(\"placeholder\") with double quotes",
    "detects throw new Error(\"TODO\")",
    "returns clean for code with no garbage throws",
    "passes legitimate throws like Error(\"file not found\")",
    "detects multiple garbage throws across files",
    "handles empty file map"
   ],
   "orchestra/host-lifecycle-heartbeat.test.ts": [
    "derives the daemon-global Docker name from canonical project + task authority",
    "tmux window target is the workerId (w-<taskId>)",
    "buildLivenessTarget derives w-<taskId> workerId by default",
    "docker: container running → alive with container-state signal",
    "docker: container stopped → dead, deadSignal=container-state",
    "docker: probe throwing fails closed (dead, not throw)",
    "tmux: pane alive → alive with tmux-pane signal",
    "tmux: pane dead → dead, deadSignal=tmux-pane",
    "subprocess (POSIX pid): live pid → alive with process-pid signal",
    "subprocess (POSIX pid): dead pid + no log → dead, deadSignal=process-pid",
    "isProcessAlivePosix: own pid alive; invalid pids rejected",
    "subprocess (Windows tasklist): live pid CSV row → alive; empty → dead",
    "unknown backend fails closed (honest, never silently alive)",
    "no pid + fresh host .log → alive via log-activity (pidUnavailable, honest)",
    "no pid + no/stale log → dead, deadSignal=process-pid",
    "isLogActivityFresh: stale mtime → false, fresh → true",
    "RED: the OLD in-file-timestamp logic marks this stale (would kill)",
    "GREEN: the NEW host-primary decision keeps it alive (live container)",
    "decideWorkerLiveness NEVER reads the .hb timestamp — verdict is identical for any hb ts",
    "NO .hb file at all + live docker container → alive (old mtime logic would kill)",
    "stale-mtime .hb + live container → alive (mtime is not consulted)",
    "docker dead → log names container-state",
    "subprocess dead → log names process-pid",
    "tmux dead → log names tmux-pane",
    "alive verdict → non-kill log line",
    "readHeartbeatCurrentAction reads a legacy .hb shape (no pid/livenessSource)",
    "blank / missing currentAction → undefined",
    "createHeartbeat (worker.ts) stays back-compat: parses via readHeartbeat, carries pid additively"
   ],
   "orchestra/idempotency-key-inject.test.ts": [
    "injects IDEMPOTENCY_KEY=<16-hex> env var into docker run args",
    "emits the env var with the -e flag immediately preceding it",
    "generates a fresh IDEMPOTENCY_KEY for each spawn call",
    "emits the \"## Idempotency Key\" header in the rendered prompt",
    "embeds the computed deterministic key (no literal ${IDEMPOTENCY_KEY} placeholder)",
    "includes the Idempotency-Key header usage directive",
    "places the directive after \"## Your Task\" and before \"## What To Do\""
   ],
   "orchestra/identity-mutation-loop.test.ts": [
    "applies mutation when success rate is below threshold (requiresApproval=false)",
    "is a no-op when success rate is at or above threshold",
    "records the variant in genealogy with parent=sourceAgentId and a mutation reason",
    "is idempotent: a second call with identical inputs is a no-op",
    "returns proposal without writing when requiresApproval=true (default)"
   ],
   "orchestra/ipc-question-bridge-wire.test.ts": [
    "flag-off: fake bridge is never called, historical continue answer is written",
    "flag-on without a bridge/broker supplied: seam inert, historical continue answer",
    "flag-on with only a bridge (no broker): seam still inert",
    "calls the bridge with the question + broker, and writes the resolved BrainAnswer",
    "answers deterministically and never invokes the bridge, even fully wired",
    "a still-pending round-trip is not resubmitted by concurrent polls",
    "a later call surfaces the settled answer without invoking the bridge again",
    "bridges a pending question for an uncollected task when flag+seam are supplied",
    "flag-off: checkWorkerQuestions answers synchronously with the historical continue, bridge unused"
   ],
   "orchestra/ipc-registry.test.ts": [
    "should return a ChannelRegistry instance",
    "should return the same instance on multiple calls (singleton)",
    "should register and unregister channels",
    "returns correct question file path",
    "returns correct answer file path",
    "round-trips a question file",
    "returns undefined for missing file",
    "round-trips an answer file",
    "returns undefined for missing file",
    "removes both question and answer files",
    "does not throw for missing files",
    "file-based happy path — returns answer when Brain writes answer file",
    "timeout fallback returns default continue answer",
    "writes question file before polling",
    "returns undefined when no question file exists",
    "auto-answers with continue when question file exists",
    "answers fail-closed continue with the explicit not-approved message",
    "never honors suggestedAction even when honorWorkerQuestionAction is on (no self-approval)",
    "notifies the human exactly once — re-answer cycles with an existing answer file skip notify",
    "without a sprintId the advisory is still answered but never notified",
    "leading whitespace before the marker still classifies as NPM-ADVISORY",
    "answers pending questions for uncollected tasks",
    "skips already collected tasks",
    "askBrain re-exported from worker-ipc is the same function",
    "getQuestionPath re-exported from worker-ipc is the same function",
    "getAnswerPath re-exported from worker-ipc is the same function",
    "writeQuestionFile re-exported from worker-ipc is the same function",
    "readQuestionFile re-exported from worker-ipc is the same function",
    "writeAnswerFile re-exported from worker-ipc is the same function",
    "readAnswerFile re-exported from worker-ipc is the same function",
    "cleanupQuestionFiles re-exported from worker-ipc is the same function",
    "handleWorkerQuestion re-exported from result-collector is the same function",
    "checkWorkerQuestions re-exported from result-collector is the same function",
    "binds the untrusted payload to the exact private question receipt",
    "rejects sibling-attempt and task-spoof receipts instead of inferring identity from payload",
    "rejects swapped question bytes even when the caller reuses a valid private receipt",
    "rejects oversized identity, question, and context strings before linear normalization",
    "bounds canonical receipt hashing against cycles, depth, flat nodes, and bytes",
    "answers only through the private first-writer publisher, then emits compatibility projections",
    "keeps no-dispatch at zero attempts and never reads or writes public IPC files",
    "rejects forged resolver state records without invoking proxy/getter side effects",
    "rejects nested authority, identity, question, and publisher descriptor forgery",
    "revalidates through the same descriptor-safe resolver snapshot",
    "fails closed when private answer delivery is unavailable",
    "keeps hostile public projections as reconciliation debt without blocking private delivery",
    "fails public reads closed when O_NOFOLLOW is unavailable while private delivery continues",
    "rejects a private answer receipt whose recomputable digest was forged",
    "accepts durable answered state without replaying private publication",
    "rejects foreign durable answered proof across fence, admission, envelope, and receipt changes",
    "surfaces an asynchronous authority HOLD exactly once instead of silently restarting approval",
    "isolates registry capacity per run so a full project cannot starve another project",
    "cleans collected latches and supersedes old attempt/generation for the same logical task",
    "never reuses a settled async decision across fence, admission, or envelope authority changes",
    "does not transfer an in-flight async decision across fence, admission, or envelope changes",
    "keeps first-writer projection bytes unchanged for stale, newer, and sibling publications",
    "rejects a copied envelope digest when legacy-visible projection content was changed",
    "rejects a hard-linked compatibility projection through fd-bound link-count validation"
   ],
   "orchestra/ipc-worker-question-action.test.ts": [
    "flag ON + suggestedAction:\"abort\" → honors abort (pre-fix RED was always continue)",
    "flag ON + suggestedAction:\"retry\" → honors retry",
    "flag ON + NO suggestedAction → falls back to continue",
    "flag ON + suggestedAction:\"continue\" → continue (historical message preserved)",
    "flag OFF (default 2-arg call) + suggestedAction:\"retry\" → continue, byte-for-byte today",
    "flag explicitly false + suggestedAction:\"skip\" → continue",
    "returns undefined when no question file exists (unchanged)",
    "flag ON → honored action written for the pending question",
    "flag OFF (default 3-arg call) → continue, byte-for-byte today"
   ],
   "orchestra/kpi-forward-collection.test.ts": [
    "uses result.cost.usd verbatim (NOT the token estimate) when present",
    "is provider-agnostic: sums each result real cost (Claude + Codex + Gemini)",
    "mixes real cost + estimate fallback per result (only the no-cost result is estimated)",
    "honors cost.usd === 0 (local/ollama) as authoritative — never re-estimates",
    "separates subscription reference value from billed/API spend",
    "uses the settled billed field for API spend and the separate reference field for comparison",
    "marks API billing unknown when only an unverified catalog reference exists",
    "falls back to the Opus estimate when NO result carries cost (legacy behavior)",
    "null-safe: a result with neither tokenUsage nor cost contributes 0",
    "empty results → zeros, never throws",
    "cost present but tokenUsage absent → real cost counted, tokens 0",
    "records 11 measurements with NON-ZERO cost sourced from result.cost.usd",
    "a result with NO cost falls back to the estimate and still persists (no crash)",
    "maps SprintMetrics → measures (tasks_total / tasks_done / no_go / boundary_violations)",
    "returns false and does NOT throw when the DB cannot be opened",
    "execution continues after the hook (finalize is not aborted)"
   ],
   "orchestra/landing-continuation-deadlock.test.ts": [
    "preserves real terminal landing counters without fabricating billing or success",
    "admits a continuation on a small absolute turn minimum, not the hard-budget reserve",
    "settles a held continuation as a typed terminal result",
    "never overwrites a real worker result with a hold settlement",
    "does not write a result when the project has no tasks directory",
    "reports the budget breaker when durable evidence exists",
    "claims OOM only when docker measured it",
    "does not let a false OOMKilled flag rule memory out (cgroup-v2 child kill)",
    "never asserts OOM when it could not be measured"
   ],
   "orchestra/layer4-runtime.test.ts": [
    "should detect shell: true violation",
    "should pass when shell: false or no shell option",
    "should detect multiple shell: true violations in one file",
    "should not flag shell:true in comments or strings",
    "should detect core/ importing from orchestra/",
    "should pass for non-core files importing orchestra/",
    "should pass for core/ files importing from core/",
    "should detect dynamic require from orchestra/",
    "should return pass:true for clean files",
    "should return pass:false with violations for bad files",
    "should handle missing files gracefully (fail-safe)",
    "should check multiple rules on one file set",
    "does NOT NO_GO on package.json dependencies (DEP-POLICY-WIRE / ADR-D-005)",
    "should emit breadcrumb events for violations"
   ],
   "orchestra/learnings-decay-exempt.test.ts": [
    "memory entry has decay_exempt=true",
    "retro entry has decay_exempt=true",
    "sprint log entry has decay_exempt=true",
    "learning entries survive decay even with old sprint_num",
    "pattern entry written by detectPatterns has decay_exempt=true",
    "pattern entry survives decay even with old sprint_num",
    "backfilled sprint/retro/memory entries have decay_exempt=true",
    "entries without decay_exempt=true are deleted by decay"
   ],
   "orchestra/lifecycle-approval-adr-failclosed.test.ts": [
    "never hangs: a lost/forgotten approval times out and resolves false within the bounded window",
    "marks the checkpoint file \"timeout\" (distinct from an explicit rejection) when the bound is reached",
    "still resolves true when approved mid-poll (regression guard)",
    "still resolves false when rejected mid-poll, without overwriting the rejection as \"timeout\" (regression guard)",
    "fails CLOSED (pass:false) when the enforcer itself crashes — not fail-open"
   ],
   "orchestra/lifecycle-vocabulary.test.ts": [
    "SprintPhase enum is exactly the canonical ten-member vocabulary — no CLEANUP",
    "every emitPhaseChange call in sprint-controller.ts references a real SprintPhase member",
    "the emitted transition chain forms one contiguous PLAN→…→COMPLETE path with no CLEANUP hop",
    "sprint.phase is only ever assigned real, non-CLEANUP SprintPhase members",
    "no comment claims a CLEANUP phase-transition arrow (the CODE-DOC-DIFF this task closes)",
    "runCleanupPhase maintenance is documented as non-phase, not a numbered SprintPhase"
   ],
   "orchestra/lineage-descendant-cancellation.test.ts": [
    "replays sprint-490: accepted second FIX supersedes the queued third repair",
    "returns typed active cancellation without force-killing",
    "does not touch queued work from an unrelated lineage"
   ],
   "orchestra/liveness-adopt.test.ts": [
    "no backend → host-signal-unavailable (never silent)",
    "docker with a pre-probed live signal → alive",
    "docker with a pre-probed dead signal (no host .log) → not alive",
    "docker WITHOUT a pre-probed signal → host-signal-unavailable (sync scan never spawns)",
    "subprocess with a LIVE pid → alive (spawn-free kill(0))",
    "subprocess with no pid and no tasksDir → host-signal-unavailable",
    "stale unauthenticated .hb remains stale even when its claimed subprocess pid is live",
    "a genuinely dead worker (no backend, stale ts, no result) is still stale (fallback preserved)",
    "GREEN: stale-ts .hb for a LIVE subprocess worker is NOT flagged stale",
    "a stale-ts .hb with no host signal (no backend) is still flagged stale (fallback preserved)",
    "probeDockerRunningAsync: State.Running true → alive; false/non-zero → dead",
    "probeTmuxPaneAliveAsync: a non-dead pane (0) → alive; all dead (1) → dead",
    "probeProcessAliveWindowsAsync: CSV row for the pid → alive; empty / invalid pid → dead"
   ],
   "orchestra/local-backend-auth-parity.test.ts": [
    "tmux is held by the earlier unmetered-remote budget gate with zero auth/provider spawn",
    "does not apply Claude auth authority to a non-Claude provider"
   ],
   "orchestra/log-complete-capture.test.ts": [
    "writes ALL 6 stream events as JSONL (not just the final summary)",
    "captures the intermediate turn + tool_use events (faithful: not dropped)",
    "includes the final usage event (single source for token capture)",
    "stamps a monotonic seq + ISO ts on every JSONL line",
    "honors startSeq for multi-stream (stdout+stderr) continuation",
    "never drops non-JSON / plain stderr lines (degrade to text)",
    "is robust to arbitrary chunk boundaries (lines split across chunks)",
    "fires the optional onEvent hook once per event (Phase-5 live tap)",
    "keeps presentation taps fail-soft by default but lets authority taps fail closed",
    "handles an empty stream without creating spurious events"
   ],
   "orchestra/managed-docs/content-generators-metrics-wire.test.ts": [
    "renders \"N/A\" instead of \"NaN%\" when coveragePercent is NaN",
    "renders non-negative duration when metrics.durationMs is negative",
    "renders normal coverage and duration unchanged when inputs are valid",
    "renders \"N/A\" when coveragePercent is Infinity"
   ],
   "orchestra/managed-docs/content-generators.test.ts": [
    "finds sprint metrics generator",
    "finds active debt generator",
    "finds sprint history generator",
    "finds agent performance generator",
    "finds changelog generator",
    "finds test coverage generator",
    "finds module map generator",
    "finds dependencies generator",
    "returns null for unknown section",
    "generates content for matching sections",
    "skips unrecognized sections",
    "generates changelog with task titles",
    "generates test coverage table",
    "handles empty auto sections array",
    "reads debt entries from store when available",
    "returns noDebt when all debt entries are resolved",
    "reads sprint entries from store when available",
    "returns noHistory when no sprint entries exist"
   ],
   "orchestra/managed-docs/doc-cache.test.ts": [
    "auto-inserts _meta key with ADR-031 reference",
    "_meta appears as first key in file (self-documenting)",
    "doc entries are preserved alongside _meta",
    "creates directory if not exists",
    "returns empty object when cache does not exist",
    "reads back cache entries including _meta",
    "backward-compat: old caches without _meta parse fine",
    "returns empty object on malformed JSON",
    "removes doc entries but preserves _meta",
    "works even if cache file does not exist yet",
    "returns 40-char hex string",
    "same input produces same hash",
    "different inputs produce different hashes"
   ],
   "orchestra/managed-docs/docs-config.test.ts": [
    "converts file path to lowercase kebab id",
    "handles nested paths",
    "handles special characters",
    "returns null when config does not exist",
    "loads valid config",
    "returns null for invalid JSON",
    "returns null for missing docs array",
    "creates config file",
    "writes valid JSON",
    "creates config and adds doc",
    "updates existing entry with same path",
    "adds multiple docs",
    "accepts custom id",
    "removes doc by id",
    "removes doc by path",
    "returns false for non-existent doc",
    "returns false when no config exists",
    "returns doc by id",
    "returns doc by path",
    "returns null for missing doc"
   ],
   "orchestra/managed-docs/docs-path-safety.test.ts": [
    "accepts valid relative paths",
    "rejects absolute Unix paths",
    "rejects absolute Windows paths",
    "rejects path traversal with ..",
    "rejects path traversal and does not modify config",
    "rejects absolute path and does not modify config",
    "accepts valid nested paths",
    "accepts .deckent/ relative paths"
   ],
   "orchestra/managed-docs/managed-doc-runner.test.ts": [
    "returns empty when no config",
    "returns empty when config has no docs",
    "reports file_not_found for missing files",
    "reports no_auto_sections when none configured",
    "updates auto sections in existing file",
    "appends missing auto section",
    "skips disabled docs",
    "handles multiple docs",
    "regenerates a managed doc when the sprint changes (sprint-aware cache)",
    "reports a cache hit when re-run within the same sprint",
    "reads latest sprint ID from DB when memory.db exists",
    "returns standalone when no DB exists (V2: no file fallback)",
    "returns null when no docs config",
    "passes store through to context for downstream generators"
   ],
   "orchestra/managed-docs/section-updater.test.ts": [
    "parses headings correctly",
    "finds ## level headings",
    "captures section content",
    "handles empty content",
    "finds section case-insensitively",
    "returns null for non-existent section",
    "replaces section content",
    "preserves other sections",
    "returns unchanged when section not found",
    "preserves AUTOGEN marker blocks nested in the replaced section",
    "appends new section at end",
    "adds ## prefix if not present",
    "preserves ## if already present",
    "updates existing auto section",
    "appends missing auto section",
    "skips auto sections with no generated content",
    "preserves protected sections",
    "returns content unchanged if within limit",
    "truncates content exceeding limit",
    "collapses accumulated duplicate sub-sections to a single fresh copy",
    "is idempotent — a second pass yields identical output",
    "no-op when generated content has no sub-headings (never strips following sections)"
   ],
   "orchestra/managed-docs/sprint-aware-cache.test.ts": [
    "produces different keys for different sprint IDs (Bug S core fix)",
    "isCacheHit returns MISS when only sprintId differs",
    "same (entryHash, fileHash, sprintId) yields the same key on repeated calls",
    "isCacheHit is idempotent — repeated calls return the same result",
    "omits sprint segment when sprintId is undefined (legacy callers)",
    "isCacheHit falls back to hash-only check when caller omits sprintId",
    "legacy entry on disk parses without sprintId and survives round-trip",
    "legacy entry (no sprintId) + caller with sprintId → MISS (force refresh)",
    "end-to-end disk round-trip: sprint-165 entry → sprint-166 check is a MISS",
    "computeCacheKey changes when sprint advances even with identical hashes"
   ],
   "orchestra/managed-docs/universalization.test.ts": [
    "matches Turkish \"Modüller\" to module-map generator",
    "matches Turkish \"Bağımlılıklar\" to dependencies generator",
    "matches Turkish \"Sprint Metrikleri\" to sprint-metrics generator",
    "matches German \"Abhängigkeiten\" to dependencies generator",
    "still matches English \"Module Map\" after i18n changes",
    "returns null for unmatchable titles",
    "resolves nested paths from sprint result",
    "resolves dynamic project stats (providerCount, providerList)",
    "resolves language from config",
    "returns empty string for unresolved placeholders",
    "resolvePath walks nested objects and Maps",
    "buildTemplateScope includes date and datetime",
    "returns empty array when no generators dir",
    "loads a JSON generator with template",
    "skips JSON files missing a template",
    "user generators take precedence over built-ins via findGenerator",
    "contentHash is stable for equal inputs",
    "readDocCache returns empty object when no cache file",
    "writeDocCache + readDocCache round-trip",
    "clearDocCache empties the cache file doc entries",
    "second run with unchanged content skips via cache",
    "updates Turkish sections via built-in pattern matching"
   ],
   "orchestra/managed-docs-auditor-template.test.ts": [
    "(a) template has no PATTERNS.md reference",
    "(b) template has no \"Append new patterns\" instruction",
    "(c) template contains memory.db pattern upsert instruction",
    "(d) loadTemplate(\"auditor\") returns template without PATTERNS.md",
    "(e) .claude/rules/auditor.md paths frontmatter does not include .brain/PATTERNS.md"
   ],
   "orchestra/managed-docs-content-generators.test.ts": [
    "findGenerator matches \"MCP Tools\" section title",
    "generates MCP tool table including key tools",
    "findGenerator matches \"CLI Commands\" section title",
    "generates CLI command table with 50+ commands",
    "findGenerator matches \"Boot Sequence\" section title",
    "generates 7-step boot sequence with all key phases",
    "findGenerator matches \"Anti-Patterns\" section title",
    "generates content with verify-ran marker and honest-result gate"
   ],
   "orchestra/memory-decay.test.ts": [
    "returns OK with zeros when .brain/ does not exist",
    "correctly separates exempt vs decayable entries",
    "returns OVER when decayable entries exceed budget",
    "returns OK when only exempt entries exceed budget but decayable do not",
    "DECAY_EXEMPT set contains expected permanent files",
    "triggers decay when total entries exceed budget",
    "does NOT trigger decay when only exempt files cause total to exceed budget",
    "returns no-op result (linesBefore === linesAfter) when under budget",
    "force option bypasses budget check and always runs decay",
    "edge case: exempt files alone exceed budget — warning expected but no error thrown"
   ],
   "orchestra/memory-limit-by-kind.test.ts": [
    "derives swap at 1.5× the limit (matches 4g/6g default ratio)",
    "derives swap for 768m limit → 1152m",
    "derives swap for 1536m limit → 2304m",
    "applies documentation kind limit when task type matches",
    "applies code-development kind limit when task type matches",
    "falls back to default 4g when task kind is not in kindMemoryLimits",
    "falls back to default 4g when kindMemoryLimits is empty (zero-config behavior)",
    "fails closed before Docker dispatch when task JSON has no canonical kind",
    "throws at construction time for invalid memory limit strings",
    "throws at construction time for zero memory limit",
    "accepts mixed kinds and applies the matching one only",
    "threads dockerMemoryLimit into the docker --memory flag",
    "falls back to DEFAULT_WORKER_MEMORY_LIMIT when dockerMemoryLimit unset"
   ],
   "orchestra/memory-trim.test.ts": [
    "returns unchanged when lines under max",
    "returns unchanged when lines exactly at max",
    "preserves first 10 lines (header) and trims from middle when over max",
    "handles empty array",
    "handles maxLines of 0",
    "handles maxLines smaller than header size",
    "preserves header content with real-world MEMORY.md format",
    "single line array under max returns that line",
    "maxLines equal to header size keeps only header"
   ],
   "orchestra/metering-live-wire.test.ts": [
    "RED: today the worker-cost path (result.cost.usd) carries NO haiku helper component",
    "GREEN: extractHelperUsageEntries surfaces ONLY the haiku helper (opus excluded)",
    "GUARD: an unresolvable primary emits ZERO helper entries (never primary-as-helper)",
    "GUARD: when every envelope model IS the primary, the helper ledger totals $0",
    "buildHelperLedger sums helper cost across multiple task envelopes",
    "canonicalClaudeModelKey collapses aliases; returns null for non-claude",
    "RED→GREEN: shows real distinct files + summed cost, not the hardcoded 0",
    "renders the helper-call cost on its OWN line, separate from task cost",
    "omits the helper/total lines when there is no helper cost (≤ 0 or absent)",
    "aggregates helper cost from the injected reader, excluding the primary",
    "fail-safe: a throwing reader yields $0 and never throws",
    "no envelope (reader returns undefined) → $0, no crash",
    "starts the monitor when resource_monitor.enabled=true (SPAWN-phase wire)",
    "returns null and never builds a monitor when the block is absent (zero behavior change)",
    "returns null and does not build a monitor when enabled=false",
    "passes interval_ms and resolves the default log_path under the project root",
    "honors a custom log_path (resolved under project root)",
    "is fail-safe: a throwing factory returns null and does not propagate (sprint unaffected)",
    "is fail-safe: a throwing start() returns null and does not propagate",
    "uses the real createResourceMonitor by default (no factory) without throwing",
    "awaits stop() on a live monitor (CLEANUP-phase wire)",
    "is a no-op on null (resume path / monitor never started)",
    "is fail-safe: a rejecting stop() is swallowed and does not propagate"
   ],
   "orchestra/mid-sprint-adapter-async.test.ts": [
    "defaultGetGitDiffStats returns a Promise (was spawnSync — froze the event loop)",
    "defaultGetGitDiffStats parses the injected async runner output",
    "defaultRunTscCheck returns a Promise and maps exit status",
    "defaultRunVitestScopeCheck returns a Promise and parses the JSON pass ratio",
    "reconcileSpuriousNoGo is async end-to-end (returns a Promise resolving to the verdict)"
   ],
   "orchestra/mid-sprint-adapter-injection.test.ts": [
    "defaultGetGitDiffStats does not execute shell metacharacters in scope.directories",
    "defaultRunVitestScopeCheck does not execute shell metacharacters in scope dirs"
   ],
   "orchestra/mid-sprint-adapter.test.ts": [
    "DONE task → no reroute",
    "GO_WITH_TECH_DEBT with reroute_on_tech_debt disabled → no reroute",
    "max reroutes reached → no reroute",
    "alternative identical to original → no reroute",
    "low-confidence alternative → no reroute (honest gate)",
    "confident different alternative → reroute with decision",
    "applyReroute mutates agent/skills and stamps confidence tier",
    "suggestReroute on an empty project → null (no throw)",
    "zero diff + strict probe GREEN (ran & all passed) → DONE",
    "zero diff + probe ran but NOT all passed → NO_GO stands (fail-closed)",
    "zero diff + NO tests actually ran → NO_GO stands (empty-match is not proof)",
    "HOLD attribution is NEVER routed around — stays NO_GO even with a green probe",
    "NON-zero diff is not a no-change case — the probe is never consulted",
    "no scoped test surface → ran:false (never a silent pass, unlike the lenient probe)",
    "all tests green + exit 0 → ran:true, allPassed:true",
    "partial pass → ran:true but allPassed:false (50% is not proof)",
    "zero total tests → ran:false (an empty run is not proof)"
   ],
   "orchestra/mid-sprint-cost-abort.test.ts": [
    "returns shouldStopDispatch=false when cost_guard is absent (default-off)",
    "returns shouldStopDispatch=false when cost_guard.enabled is false",
    "returns shouldStopDispatch=false when max_limit_cost_usd is absent",
    "returns shouldStopDispatch=false when cost is below threshold",
    "returns shouldStopDispatch=true when cost equals threshold (>=)",
    "returns shouldStopDispatch=true when cost exceeds threshold",
    "emits COST_GUARD_ABORT audit event when threshold is exceeded",
    "returns shouldStopDispatch=false when getLimitCost throws (best-effort)",
    "does NOT emit event when cost is below threshold",
    "shouldStopDispatch() is false before any ticks",
    "does not start interval when cost_guard is disabled",
    "sets shouldStopDispatch=true after a tick that exceeds threshold",
    "stop() prevents further ticks"
   ],
   "orchestra/mission-verdict-honesty.test.ts": [
    "a clean DONE (ok:true, no explicit settleDetail) derives \"done\"",
    "an honest GO_WITH_TECH_DEBT (ok:true, explicit settleDetail:\"debt\") passes through as \"debt\"",
    "a NO_GO (ok:false, no explicit settleDetail) derives \"failed\"",
    "an explicit settleDetail is always trusted over the ok-derived fallback",
    "a mission with one clean DONE item + one honest DEBT item settles completed, both items done",
    "a genuine NO_GO still fails both the item and the mission (no over-correction)",
    "a mixed mission (one DONE + one genuine NO_GO) still fails overall — DEBT-honesty does not mask real failures",
    "a runTask that omits settleDetail entirely (today\\'s real autonomous.ts shape) still gets a normalized fallback"
   ],
   "orchestra/mixed-fleet-spawn-routing.test.ts": [
    "routes ollama→host adapter and claude→docker backend (no cross-routing)",
    "refreshes the ollama adapter model acceptance before spawning",
    "both fleet paths produce a .result on disk (two-result disk-verify)"
   ],
   "orchestra/moat-1-source-merge-race.test.ts": [
    "N tasks contending for the SAME file → exactly ONE wins, the rest are blocked",
    "the SpawnLockError names the conflicting holder (observability)",
    "8 tasks with DISJOINT scope.filesWrite all acquire — none falsely blocked",
    "a batch with one conflicting file rejects wholesale and leaks no lock",
    "releasing a lock makes the file re-acquirable (turnover, not deadlock)",
    "a write OUTSIDE any declared scope.filesWrite is NOT lock-guarded (known gap)"
   ],
   "orchestra/moat3-fixphase.test.ts": [
    "re-dispatches a NOT_DISPATCHED task via the same spawn/wait seam as the fix pipeline",
    "second attempt with no result and no disk trace stays honestly NOT_DISPATCHED (not silently dropped)",
    "settles the repair queue when exact custody proves ordinal-2 zero provider work",
    "re-adopts and settles a durable redispatch admission after coordinator restart",
    "second attempt with no result but a disk trace (.hb) is a real NO_GO, not a dispatch gap",
    "a task whose marker already exists is excluded — no second re-dispatch round (max-1 cap)",
    "two sequential runFixPhase calls prove the 1-round cap holds (no infinite re-dispatch loop)",
    "unverified provider-limit prose never suppresses an honest NOT_DISPATCHED retry",
    "redispatch proceeds normally when NO_GO entries exist but are not usage-limit-flavored"
   ],
   "orchestra/moat3-not-dispatched.test.ts": [
    "exposes NOT_DISPATCHED alongside legacy members",
    "is distinct from every other evaluation value",
    "classifies NOT_DISPATCHED when .result, .hb and .log are all absent",
    "classifies SYNTHETIC_NO_GO when a stale .hb exists (worker started, then died)",
    "classifies SYNTHETIC_NO_GO when a .log exists but no .hb (log-only trace)",
    "classifies SYNTHETIC_NO_GO when both .hb and .log exist",
    "defers to SYNTHETIC_NO_GO (guard) when a .result file is present",
    "reports all-false when no trace files exist on disk",
    "detects an existing .hb file as worker-start trace",
    "detects an existing .log file as worker-start trace",
    "detects an existing .result file",
    "routes NO_GO into fixCandidateTaskIds and NOT_DISPATCHED into reDispatchCandidateTaskIds",
    "never places a NOT_DISPATCHED task id in fixCandidateTaskIds (not worker-blamed)",
    "returns empty buckets for an evaluations map with no NO_GO/NOT_DISPATCHED entries",
    "counts only NOT_DISPATCHED entries",
    "returns zero for an empty or NOT_DISPATCHED-free map"
   ],
   "orchestra/mode-aware-routing.test.ts": [
    "should not block sprint mode",
    "should reject task mode with BrainError",
    "should return taskId and backend info",
    "should emit TASK_MODE_START event",
    "should throw when config is sprint mode",
    "should bypass sprint lifecycle phases",
    "should include description and timestamp in event",
    "should forward scope and options to spawn backend"
   ],
   "orchestra/model-activation-policy-enforcement.test.ts": [
    "gpt-5.6 family + gpt-5.5 exist with the owner-reviewed tiers",
    "an inactive premium model is unreachable via getByProviderAndTier",
    "deterministic tier→model resolves to the owner ladder",
    "getAllModels / getAllModelIds exclude every inactive codex model but keep the active three",
    "identity resolution stays TOTAL — an inactive model still resolves (tombstone, not deletion)",
    "a newly registered GA model can NOT auto-enter an explicit-active pool",
    "resolveTaskModel(forceModel=gpt-5.5, codex) throws MODEL_INACTIVE before any dispatch",
    "resolveTaskModel(forceModel=gpt-5.6-terra, codex) returns the active model verbatim",
    "resolves a premium task to active premium_plus sol without resurrecting inactive gpt-5.5",
    "holds instead of exceeding policy when $name",
    "with no policy, gpt-5.5 is still selectable and forceModel does not throw",
    "an implicit-active provider with an unrecorded model keeps it executable",
    "the registry-injected policy digest equals the freshly-resolved store digest"
   ],
   "orchestra/model-override-drop.test.ts": [
    "repro/fix proof: `- Model: gpt-5.5` with NO `- Provider:` line resolves to gpt-5.5, not claude-opus-4-8",
    "explicit `- Provider: codex` + `- Model: gpt-5.5` still resolves to gpt-5.5 (no regression)",
    "recommendation.modelConstraint set does NOT override a forceModel directive",
    "regression: `- Model: claude-haiku-4-5-20251001` (no provider) resolves to claude-haiku-4-5-20251001 verbatim",
    "regression: `- Model: claude-fable-5` (no provider) resolves to claude-fable-5 verbatim",
    "rejects explicit contradictory provider/model identity before task creation"
   ],
   "orchestra/model-selector-max-tier.test.ts": [
    "reproduces the uncapped premium resolution that motivated the ceiling",
    "clamps an auto-selected premium task to the declared ceiling",
    "reads the mode-preset strategy when the mode config declares no ceiling",
    "lets the mode config win over the preset strategy",
    "is inert when no ceiling is declared anywhere",
    "never upgrades — a ceiling is a ceiling, not a target",
    "refuses a ceiling below the floor instead of silently picking one",
    "lets an explicit operator model override outrank the ceiling"
   ],
   "orchestra/model-selector-provider.test.ts": [
    "floors an auto-selected economy code task to sonnet (MODEL-GUARD)",
    "honors forceModel=haiku on a code scope (explicit override)",
    "no provider parameter returns Claude model (sonnet)",
    "no provider parameter returns Claude model (opus)",
    "explicit provider=claude returns same as no provider",
    "floors an auto-selected economy code task to gpt-4.1 on codex (MODEL-GUARD)",
    "keeps gpt-5-mini for an auto-selected economy DOC task on codex (economy allowed)",
    "medium task resolves to gpt-4.1 (standard tier)",
    "complex task resolves to gpt-5 (premium tier)",
    "floors an auto-selected economy code task to gemini-2.5-flash (MODEL-GUARD)",
    "medium task resolves to gemini-2.5-flash (standard tier)",
    "complex task resolves to gemini-2.5-pro (premium tier)",
    "forceModel=opus on codex returns opus (adapter provider — forceModel is authoritative)",
    "forceModel=sonnet on gemini returns sonnet (adapter provider — forceModel is authoritative)",
    "forceModel=gpt-4.1 on codex returns gpt-4.1 directly (same provider)",
    "forceModel=opus on claude returns opus directly",
    "forceModel=haiku on gemini returns haiku (adapter provider — forceModel is authoritative)",
    "pro_plan + codex: opus->sonnet(plan filter)->gpt-4.1(codex mapping)",
    "haiku_allowed=false + gemini: haiku->sonnet(filter)->gemini-2.5-flash",
    "doc scope + codex: opus->sonnet(doc cap)->gpt-4.1(codex)",
    "skillModels + provider maps correctly"
   ],
   "orchestra/model-selector-skill.test.ts": [
    "upgrades model when skill requires opus",
    "upgrades from haiku to sonnet when skill requires sonnet",
    "does not downgrade model when skill model is lower",
    "picks highest among multiple skill models",
    "undefined skillModels has no effect",
    "empty skillModels array has no effect",
    "skill model upgrade still capped by Layer 3 (docs scope)",
    "skill model upgrade still capped by Layer 1 (economic)",
    "forceModel overrides skillModels",
    "skill model works with pattern upgrade together"
   ],
   "orchestra/model-selector.test.ts": [
    "uses min_tier when it is present, regardless of haiku_allowed",
    "retains the haiku_allowed fallback when min_tier is absent"
   ],
   "orchestra/module-count-sync.test.ts": [
    "countModules(src/core) returns code-derived count > 90 (not hardcoded old value 90)",
    "countModules result matches direct readdirSync .ts file count for src/core (no drift)",
    "countModules(src/orchestra) returns code-derived count > 76 (not hardcoded old value 76)",
    "countModules result matches direct readdirSync .ts file count for src/orchestra (no drift)",
    "findGenerator(\"Architecture\") is not null — generator is registered and ready as autoSection",
    "architecture-map generator produces table with live core/ and orchestra/ counts",
    "architecture-map table core/ count matches countModules (no hardcode drift)",
    "architecture-map table orchestra/ count matches countModules (no hardcode drift)",
    "docs.json loads successfully",
    "architecture-map generator produces non-empty content for any project root"
   ],
   "orchestra/monitor-adapter.test.ts": [
    "returns DockerMonitorAdapter for docker backend",
    "returns TmuxMonitorAdapter for tmux backend",
    "returns SubprocessMonitorAdapter for subprocess backend",
    "throws for unknown backend",
    "defaults to tmux for auto backend",
    "listActiveWorkers parses docker ps output",
    "listActiveWorkers returns empty on failure",
    "captureWorkerOutput calls docker logs",
    "getResourceUsage parses docker stats",
    "getResourceUsage returns null on failure",
    "killWorker calls docker kill",
    "listActiveWorkers parses tmux ls output",
    "listActiveWorkers filters non-deckent sessions",
    "captureWorkerOutput calls tmux capture-pane",
    "getResourceUsage returns null (tmux limitation)",
    "killWorker calls tmux kill-session",
    "listActiveWorkers reads .pid files and checks ps",
    "listActiveWorkers returns empty when pidDir missing",
    "captureWorkerOutput returns placeholder string",
    "getResourceUsage reads pid and calls ps",
    "getResourceUsage returns null when pid file missing",
    "killWorker reads pid and sends kill signal",
    "killWorker does nothing when pid file missing"
   ],
   "orchestra/multi-agent.test.ts": [
    "returns valid pipeline with single step",
    "returns valid pipeline with multiple steps",
    "throws on empty steps",
    "throws on duplicate phases",
    "throws on empty agentId",
    "throws on empty phase",
    "allows same agent in different phases",
    "runs all steps when all succeed",
    "aborts on first failure",
    "captures step outputs",
    "writes step output to shared context",
    "handles executor throwing an error",
    "records correct agentId and phase for each step",
    "handles single-step pipeline",
    "passes task to executor"
   ],
   "orchestra/no-result-stub-eradication.test.ts": [
    "passes through DONE when linesAdded>0 and testsPassed=true",
    "passes through GO_WITH_TECH_DEBT when linesAdded>0 and selfAssessment is tech debt",
    "forces a typed HOLD to remain NO_GO even when final-disk evidence exists",
    "does NOT force NO_GO when linesAdded>0 even if testsPassed=false",
    "writes honest NO_GO sentinel — not a CODE_VERIFIED_DONE stub",
    "does NOT clobber an existing .result with a sentinel (P0-B)",
    "does NOT write a sentinel when a real result is already archived (finalize-race, P0-B)",
    "downgrades selfAssessment DONE with linesAdded=0 + testsPassed=false to NO_GO",
    "isStubResult detects the literal stub pattern",
    "stub shape + DISK EVIDENCE (rename/docker untracked) → NOT a confirmed stub",
    "stub shape + NO disk evidence → confirmed stub (legacy synthetic NO_GO preserved)",
    "non-stub result → never a confirmed stub regardless of disk",
    "diskVerify throws (no git in sandbox) → fail-open to confirmed stub",
    "classifies missing result + crashed worker as worker-crashed-no-result",
    "classifies present-but-stub result as DISHONEST_DONE_STUB and triggers FIX",
    "forces NO_GO when filesChanged contains path outside scope.filesWrite",
    "does NOT flag boundary violation when all filesChanged are inside scope",
    "codex-in-docker shape (DONE, linesAdded=0, testsPassed=false) + disk evidence → HONEST (not flipped)",
    "SAME shape but NO disk evidence → still DISHONEST_DONE_STUB (integrity preserved)",
    "omitted diskVerify (e.g. retro-phase caller) → exact legacy behavior (still flips a stub)",
    "Check 3 (filesChanged>0, linesAdded=0, DONE) + disk evidence → HONEST (no SCOPE_VIOLATION_OR_EMPTY_WRITE)"
   ],
   "orchestra/nogo-note-accuracy.test.ts": [
    "result-var: worker self-NO_GO note includes files and lines",
    "result-yok: crash fallback note says \"no result file\"",
    "self-NO_GO: 0 files is shown accurately (files=0 note)",
    "docker crash pattern \"exited without writing result\" maps to no-result note",
    "buildEnrichedFixReason includes accurateReason as second element"
   ],
   "orchestra/ollama-model-flow-236.test.ts": [
    "ensureOllamaModelRegistered makes an arbitrary tag a first-class registry model",
    "is idempotent (re-register is a no-op, no throw)",
    "resolveTaskModel returns the ollama tag verbatim (no cloud re-map) when provider=ollama",
    "does NOT auto-register / short-circuit for cloud providers (real-bug signal preserved)"
   ],
   "orchestra/outcome-tracker-reclassify.test.ts": [
    "NO_GO → DONE reclassify updates agent + skill success rate (delta math)",
    "writes ADR-046 audit-trail retro entry to memory store when provided",
    "duplicate reclassify is idempotent (no stat drift, no audit write)",
    "rewrites the sprint outcome file with the new evaluation",
    "updates skillSprintHistory + synergyMatrix delta in tandem",
    "throws when sprint outcomes file is missing",
    "throws when task is not found in the sprint",
    "honors the --no-audit path (no store) without throwing",
    "DONE → NO_GO reclassify decreases success rate (reverse flip)",
    "GO_WITH_TECH_DEBT counts as success (no delta when switching DONE ↔ GO_WITH_TECH_DEBT)"
   ],
   "orchestra/outcome-tracker.test.ts": [
    "records a successful outcome",
    "records a failed outcome",
    "tracks skill performance",
    "tracks synergy between agent and skill",
    "tracks synergy between skill pairs",
    "does not track generic agent",
    "accumulates multiple outcomes",
    "tracks intent-specific performance",
    "tracks skill sprint history when recording outcomes",
    "tracks sprint order in recentSprints",
    "does not duplicate sprint IDs in recentSprints",
    "tracks coverage in skill sprint history",
    "returns empty for insufficient data",
    "returns positive bonus for high overall success",
    "returns negative bonus for low success",
    "marks synergy for high success pairs",
    "marks conflict for low success pairs",
    "returns empty map when fewer than 2 sprints seen",
    "returns +3 for skill succeeding in all recent sprints",
    "returns -2 for skill failing in all recent sprints",
    "returns +1 for skill with >=75% success rate in recent sprints",
    "caps bonus at LEARNING_BONUS_CAP in calculateBonuses",
    "tracks avgQualityScore via incremental average",
    "gives +1 bonus for avgQualityScore >= 80 with enough samples",
    "gives -1 penalty for avgQualityScore < 40 with enough samples",
    "does not apply quality bonus when avgQualityScore is 0 (no quality data)",
    "tracks quality score for skills too",
    "backfills avgQualityScore to 0 for entities loaded from older learnings",
    "uses custom minSamplesForBonus from config",
    "uses custom recentSprintWindow from config",
    "uses custom sprintRecencySuccessBonus from config",
    "uses custom sprintRecencyFailurePenalty from config"
   ],
   "orchestra/override-warning-surface.test.ts": [
    "returns [] for an empty task list",
    "returns [] when no task carries routingMeta.overrideWarnings",
    "flattens multiple warnings per task, task-id tagged, preserving order",
    "RED: an overrideWarnings-carrying fixture renders a task-id-tagged warning block",
    "RED: overrideWarnings are also surfaced in --dry-run output",
    "renders the tr header + tr count phrasing when config.language is tr",
    "multiple tasks with multiple warnings all render, one line per warning",
    "REGRESSION: no overrideWarnings anywhere (no routingMeta) -> no override-related line printed",
    "REGRESSION: routingMeta present but overrideWarnings is an empty array -> no block printed",
    "REGRESSION: plan output for a warning-free sprint is unchanged (same print-call count as baseline)"
   ],
   "orchestra/panic-guard.test.ts": [
    "should BLOCK kill by default (no options)",
    "should BLOCK when only --force is set (without --user-explicit)",
    "should BLOCK when only --user-explicit is set (without --force)",
    "should ALLOW kill when both --force and --user-explicit are set",
    "should write panic log JSON to .deckent/ directory",
    "should record events and return them via getEvents()",
    "should filter events by sprint via getSprintEvents()",
    "should build a critical notification payload",
    "should mark allowed kills as blocked=false in panic log"
   ],
   "orchestra/parallel-pipeline.test.ts": [
    "returns empty array for empty input",
    "puts all independent tasks in wave 0",
    "creates two waves for linear dependency",
    "creates three waves for A -> B -> C chain",
    "groups parallel tasks in the same wave",
    "handles diamond dependency: a -> b,c -> d",
    "throws on circular dependency A -> B -> A",
    "throws on self-dependency",
    "throws on three-node cycle A -> B -> C -> A",
    "ignores unknown dependencies (not in task list)",
    "handles single task with no dependencies",
    "sorts task IDs within each wave for determinism",
    "handles complex graph with multiple roots",
    "wave indices are sequential starting from 0",
    "handles many tasks with no deps (all wave 0)",
    "returns \"No tasks\" for empty waves",
    "formats single wave correctly",
    "formats multiple waves correctly",
    "includes Execution Plan header"
   ],
   "orchestra/parser-bare-token-validation.test.ts": [
    "rejects bare extension tokens",
    "accepts full paths",
    "rejects basename-only paths (no separator)",
    "rejects all blocklist tokens (.ts, .md, .test, test.ts, .json, .txt)",
    "returns empty sanitized array on empty input",
    "mixed valid + invalid — partial sanitization"
   ],
   "orchestra/pattern-model-suggestion.test.ts": [
    "parses valid JSON array of patterns",
    "returns empty array for empty string",
    "returns empty array for whitespace-only string",
    "returns empty array for malformed JSON",
    "returns empty array when JSON is not an array",
    "parses multiple pattern entries",
    "removes duplicate pattern entries, keeping highest occurrences",
    "preserves unique patterns",
    "handles empty array",
    "keeps single entry unchanged",
    "deduplicates three entries with same pattern",
    "returns the canonical premium model for file_outside_scope with occurrences >= 2 in src/ scope",
    "returns null for file_outside_scope with only 1 occurrence",
    "returns the canonical premium model for circular_dependency with occurrences >= 1 in src/ scope",
    "returns null for docs/ scope even with boundary violations",
    "returns null for empty patterns array",
    "returns null when all patterns are resolved",
    "returns null for stale_heartbeat patterns (not a model upgrade trigger)",
    "returns the canonical premium model for tests/ scope with circular_dependency",
    "upgrades the canonical standard model to the canonical premium model when patterns indicate boundary violations",
    "does not upgrade when no patterns provided",
    "pattern upgrade is still capped by pro_plan constraint",
    "empty patterns array has no effect on model selection",
    "resolved patterns do not trigger model upgrade",
    "multiple mixed patterns — only unresolved boundary violations count",
    "pipeline: raw JSON string → parsed → deduplicated",
    "pipeline: empty string → no patterns → no model upgrade",
    "pipeline: JSON with duplicates triggers model upgrade after deduplication",
    "pipeline: malformed JSON → empty array → no upgrade",
    "pipeline: non-array JSON → empty array",
    "deduplicatePatterns preserves resolved flag of highest-occurrence entry",
    "returns null for empty scope directories",
    "returns the canonical premium model for bare \"src\" directory (without trailing slash)",
    "returns the canonical premium model for bare \"tests\" directory",
    "mixed scope: one src dir triggers upgrade even with other non-src dirs",
    "file_outside_scope with exactly 1 occurrence does not trigger upgrade",
    "returns null when all patterns have zero occurrences"
   ],
   "orchestra/pattern-reader.test.ts": [
    "returns empty array when learning directory does not exist",
    "returns all entries when no filter is applied",
    "filters by taskType",
    "filters by agent",
    "filters by model",
    "filters by evaluation",
    "filters by minCoverage",
    "filters by sprintRange",
    "combines multiple filters",
    "skips malformed files gracefully",
    "returns empty array when no entries exist",
    "returns successful combos with DONE and coverage > 80",
    "groups by agent/skills/model combination",
    "sorts by count descending",
    "filters by taskType",
    "returns empty array when no entries exist",
    "returns only NO_GO entries",
    "includes lastSprint for each combination",
    "sorts by recency first then count"
   ],
   "orchestra/pattern-recorder.test.ts": [
    "creates learning directory if it does not exist",
    "writes new entry to sprint file when file does not exist",
    "appends to existing sprint file",
    "writes to correct sprint file path",
    "handles malformed existing file by treating as empty",
    "handles non-array existing file by treating as empty",
    "preserves all fields from the entry",
    "returns empty array when sprint file does not exist",
    "returns entries from existing sprint file",
    "returns empty array for malformed JSON",
    "returns empty array when readFileSync throws",
    "returns empty array when learning directory does not exist",
    "lists sprint IDs from files",
    "excludes summary.json from list",
    "excludes non-JSON files",
    "returns sorted sprint IDs",
    "returns empty array when readdirSync throws"
   ],
   "orchestra/pause-resume.test.ts": [
    "transitions PENDING tasks to PAUSED",
    "transitions EXECUTING tasks to PAUSED",
    "transitions CLAIMED, TESTING, DOCUMENTING tasks to PAUSED",
    "does NOT pause DONE tasks",
    "does NOT pause NO_GO tasks",
    "sets sprint status to PAUSED",
    "returns a PauseState with the correct sprintId and pausedTaskIds",
    "writes task JSON files for paused tasks",
    "writes a .paused marker file with previousStatus",
    "persists pause state to .deckent/pause-state.json",
    "updates dashboard with PAUSED sprint status",
    "includes an alert with the pause reason in dashboard update",
    "handles empty task list gracefully",
    "uses \"Manual pause\" as default reason",
    "counts DONE tasks correctly in dashboard progress",
    "continues gracefully when the task JSON write fails",
    "transitions PAUSED tasks back to PENDING",
    "does NOT modify non-PAUSED tasks",
    "sets sprint status to ACTIVE",
    "persists ACTIVE to the canonical sprint-state authority",
    "writes updated task JSON for resumed tasks",
    "removes .paused marker files when they exist",
    "removes the pause-state.json file when it exists",
    "updates dashboard with ACTIVE sprint status",
    "returns the previously saved PauseState",
    "returns null when no saved pause state exists",
    "handles empty task list gracefully",
    "correctly reflects active task count in dashboard progress",
    "continues gracefully when marker/state unlink fails",
    "tasks go PENDING → PAUSED → PENDING after pause/resume",
    "DONE tasks remain DONE through the full pause/resume cycle",
    "contains exact Docker custody without tmux kill or duplicate pending spawn",
    "keeps a recovered historical terminal authority out of a fresh sprint checkpoint",
    "still rejects a missing terminal authority for an exact current-sprint task",
    "still rejects a held terminal authority for an exact current-sprint task",
    "keeps a reconciliable exact accepted attempt EXECUTING on resume",
    "settles and fresh-revalidates an accepted exact attempt before pause projection",
    "rolls exact resume projection back when checkpoint persistence holds"
   ],
   "orchestra/pcomp-prompt-composition.test.ts": [
    "labels directories as READ scope and filesWrite as the canonical WRITE authority",
    "states explicitly that a read directory does NOT grant write permission",
    "renders sanitized exact filesRead beside directory context for a write-capable task",
    "keeps an authored but fully rejected exact read list visibly fail-closed",
    "renders a protected root target as exact read-only while withholding write authority",
    "keeps the directory-fallback wording when no filesWrite list exists (PQ-4 F5)",
    "renders the flat legacy list (no sub-headers) when trackedFiles is absent",
    "splits the write list into Existing / New / ⚠ Unverified sub-lists with worker language",
    "omits empty sub-lists — all-tracked writes render only the Existing group",
    "root-only repo (no tracked dirs): nested writes label as New, never ⚠ Unverified (born-584)",
    "names the exact .tasks/ lifecycle write exceptions — both branches",
    "classifies explicit-ref as governing, scoring-only as constraint",
    "governing ADR gets its full operative (Decision) body",
    "constraint ADR is condensed: constraint line + Contract, NO full body",
    "extractContractSection returns the Contract body through the next header",
    "writes one line per prompt build with id/score/tier/reasons",
    "is fail-soft: an unwritable root never throws"
   ],
   "orchestra/per-worker-provider-resolve.test.ts": [
    "DIRECTIVES override: task.authMode=\"api\" takes highest priority",
    "DIRECTIVES override: task.authMode=\"subscription\" overrides config api",
    "config fallback: no task.authMode, config.auth_mode=\"api\"",
    "default fallback: no task.authMode, no config → \"subscription\"",
    "config.auth_mode=\"subscription\" explicit → \"subscription\"",
    "includes authMode in routing result — DIRECTIVES api override",
    "includes authMode in routing result — config fallback subscription",
    "fails before producing a routing result when no provider is available",
    "authMode does not affect provider selection — provider resolved independently",
    "3-mode uniform: authMode resolved identically for Sprint/Task/Process context",
    "graceful: unknown/invalid provider still gets authMode resolved"
   ],
   "orchestra/persona-spawn-gate.test.ts": [
    "intact persona → byte-identical content, no refusal",
    "broken (undersized) + ADVISORY DEFAULT → spawn proceeds with the persona, no refusal",
    "broken + enforce → typed PERSONA_INTEGRITY_HOLD refusal, no silent personaless spawn",
    "D-D untouched: an ABSENT persona degrades — never classified broken, never refused",
    "generic/agentless task is out of scope entirely",
    "the floor is config-resolved through options, defaulting to the single config export"
   ],
   "orchestra/persona-task-matcher-live.test.ts": [
    "(a) CLI task + temp-react-ts-specialist → HIGH mismatch → rotates to architect",
    "(b) System task (src/orchestra/) + temp-react-ts-specialist → HIGH → architect",
    "(c) React task (src/dashboard/) + temp-react-ts-specialist → match, no rotation",
    "(d) Multi-domain task → ambiguous, no rotation even for react agent",
    "(e) generic agent always passes through without rotation",
    "temp-react-ts-specialist has domain react in TEMP_AGENT_DOMAINS",
    "getAgentDomain returns react for temp-react-ts-specialist without explicit domain field",
    "agent.domain field takes precedence over TEMP_AGENT_DOMAINS override",
    "temp-react-ts-specialist (no domain field) gets domain react via TEMP_AGENT_DOMAINS",
    "applyPersonaDomainCheck rotation uses TEMP_AGENT_DOMAINS without domain in agent.json"
   ],
   "orchestra/persona-task-matcher.test.ts": [
    "maps src/cli/ path to cli domain",
    "maps src/core/ and src/orchestra/ paths to system domain",
    "maps tests/ paths to test domain",
    "maps docs/ and .md paths to doc domain",
    "returns multiple domains for multi-domain tasks",
    "returns empty for unrecognized paths",
    "(a) CLI task with react specialist → HIGH mismatch, suggests architect/frontend",
    "(b) System task with architect → valid match, no mismatch",
    "(c) Test task with ci-guardian → valid match",
    "(d) Doc task with doc-writer → valid match",
    "(e) Multi-domain task → ambiguous, always valid regardless of agent",
    "(f) Generic agent (undefined domain) → always valid, legacy behavior",
    "system agent on test task → LOW severity (plausible overlap)",
    "returns domain from agent.domain field if set",
    "falls back to BUILTIN_AGENT_DOMAINS for known agents",
    "returns generic for unknown agents without domain field",
    "BUILTIN_AGENT_DOMAINS covers all 15 built-in agents"
   ],
   "orchestra/phase-transition-observability.test.ts": [
    "persistPhaseTransition writes PLAN/PLANNING to sprint-state.json",
    "persistPhaseTransition flips PLAN→SPAWN with status update visible on disk",
    "runEvaluatePhase writes EVALUATE/EVALUATING to sprint-state.json",
    "runFixPhase writes FIX/FIXING to sprint-state.json",
    "runEvaluatePhase writes per-task audit.json with canonical schema",
    "persistPhaseTransition leaves no .tmp/.partial leftover under .deckent"
   ],
   "orchestra/plan-improvements.test.ts": [
    "uses custom timeout when provided",
    "defaults to BRAIN_PLAN_TIMEOUT_MS when no timeout provided",
    "callZeroConfigPlanner uses custom timeout",
    "callZeroConfigPlanner defaults to BRAIN_PLAN_TIMEOUT_MS",
    "ResolvedConfig accepts ai_planner_timeout field without type error",
    "parseStructuredDirectives falls back to bullet format when no ## headings",
    "parseStructuredDirectives falls back to numbered list format",
    "parseBulletOrNumberedTasks handles \"1) title\" format",
    "parseBulletOrNumberedTasks handles \"* Task:\" format",
    "parseBulletOrNumberedTasks extracts Model/Effort from sub-lines",
    "HOLDs rather than discarding PATTERNS behind DIRECTIVES",
    "HOLDs rather than discarding DEBT behind MEMORY",
    "includes all sections when maxLines is sufficient",
    "has per-task agent selection error handling with debugLog",
    "has per-task skill selection error handling with debugLog",
    "has >2x safeguard with fallback (not just warning)",
    "also has <1x safeguard for too few tasks"
   ],
   "orchestra/plan-preview-parity.test.ts": [
    "matches digest context and task projection across preview and durable paths",
    "journals the same single catalog key-set for both routing surfaces"
   ],
   "orchestra/plan-preview-service.test.ts": [
    "keeps the shared preview service read-only for CLI dry-run and default MCP planning",
    "forwards acknowledgePromptGate through to planSprint",
    "binds planner, digest, and topology to the runtime max-worker authority",
    "same task set + same gate outcome ⇒ identical planDigest, even across different sprint IDs",
    "a different task set ⇒ a different planDigest",
    "includes a surface-resolved writeScopePolicy in the digest context",
    "maps sprint.tasks to {title, summary} taskSummaries",
    "no promptGate on the sprint ⇒ gateResult \"skipped\", policyDecision \"allow\"",
    "promptGate.ok === true ⇒ gateResult \"pass\", policyDecision \"allow\"",
    "promptGate.ok === false ⇒ gateResult \"fail\", policyDecision \"needs-approval\"",
    "makes an undeclared shared-writer collision a non-overridable structural deny",
    "keeps an explicitly ordered shared-writer chain safe and visible",
    "returns the underlying sprint unmodified so CLI/MCP display logic keeps working",
    "produces DIRECTIVES markdown that parseStructuredDirectives reads back losslessly",
    "carries flowId/tenant/project/actor/origin on the `- Meta:` line — never inside the description (U1-G2)",
    "is deterministic — same RunProposal ⇒ byte-identical markdown"
   ],
   "orchestra/plan-surface-truth.test.ts": [
    "splits a combined \"- Model: X | Agent: Y\" line into two segments",
    "is order-independent (\"- Agent: X | Model: Y\" also splits cleanly)",
    "behaves identically to a single directive on its own line (no pipe)",
    "does not mistake prose containing a colon for a directive segment",
    "returns empty array for a blank line",
    "finds a value on a combined line",
    "first occurrence wins across multiple lines",
    "returns undefined when the key never appears",
    "captures BOTH forceModel and forceAgent from the exact DIRECTIVES.md task-header shape",
    "captures both hints regardless of Model/Agent order on the combined line",
    "still supports the separate-line variant (no regression)",
    "combines Model+Agent+Effort on one line",
    "\"Agent: none\" on a combined line still maps to generic (no WARN)",
    "\"Agent: auto\" on a combined line still maps to undefined (no WARN — deliberate no-op)",
    "fails loudly when Model: resolves to an unrecognized model id",
    "does not WARN when Model: resolves to a valid model",
    "does not WARN when no Model:/Agent: directive is present at all",
    "captures both forceModel and forceAgent at the bullet-list parse site",
    "fails loudly when the bullet-list Model: value is unrecognized"
   ],
   "orchestra/planner-dependency-shape.test.ts": [
    "maps 1-based task numbers to the exact task titles",
    "maps \"Task N\" / \"task 2\" / \"#3\" strings the same way; other strings are untouched",
    "leaves out-of-range and self-referencing numbers as strings (they drop VISIBLY later, never silently re-targeted)",
    "is a no-op on non-plan shapes and never throws",
    "numeric dependencies now parse into a valid plan with title dependencies",
    "invalid JSON → stage \"json\", no result",
    "valid JSON that violates the schema → stage \"schema\" with dotted issue paths, capped and output-free",
    "first attempt with numeric dependencies → plan accepted, dependencies resolved to titles, no retry",
    "schema violation on attempt 1 → retry prompt names the violated paths; valid attempt 2 is accepted",
    "attempt 1 uses a model outside the allowed policy → retry names tasks.N.model:<id> and the allowed IDs; valid attempt 2 is accepted",
    "both attempts violate the model policy → null after exactly two attempts (finite, typed)"
   ],
   "orchestra/planner-edge.test.ts": [
    "includes project name at the top",
    "includes directives section when provided",
    "includes memory section when provided",
    "includes retro section when provided",
    "includes patterns and decisions when provided",
    "omits CRITICAL DEBT section when no critical unresolved debt",
    "includes CRITICAL DEBT section for unresolved critical debt",
    "omits resolved critical debt from CRITICAL DEBT section",
    "includes maxWorkers from recommendation in prompt",
    "includes file tree when present",
    "limits file tree to first 100 entries",
    "preserves complete directive authority beyond the removed outer line cap",
    "omits file tree section when fileTree is empty",
    "omits empty optional sections when context fields are empty string",
    "includes output format JSON template in prompt",
    "parses valid JSON with one task",
    "parses JSON wrapped in ```json code fence",
    "parses JSON wrapped in ``` code fence without language tag",
    "returns null for invalid JSON string",
    "returns null for empty string",
    "returns null when tasks array is empty (min(1) constraint)",
    "returns null when tasks field is missing",
    "returns null when reasoning field is missing",
    "returns null when a task has invalid model value",
    "returns null when a task has invalid effort value",
    "returns null when a task has invalid priority value",
    "returns null when title is empty string",
    "returns null when description is empty string",
    "parses multiple tasks correctly",
    "parses all valid model types",
    "parses all valid priority values",
    "returns null when goNogo fields are missing",
    "returns null when scope is missing directories field",
    "ignores extra fields on valid input (Zod passthrough not enabled)",
    "returns null when the spawn exits non-zero",
    "returns null when stdout is empty",
    "returns null when stdout is null-ish",
    "returns null on timeout (status null, signal SIGTERM)",
    "returns null when stdout is malformed JSON",
    "returns null when stdout has valid JSON but fails Zod validation",
    "returns PlannerResult on valid stdout",
    "spawns the correct claude command",
    "spawns with the output-format json flag",
    "passes a timeout to the spawn",
    "accepts haiku as valid model",
    "rejects when dependencies is not an array",
    "rejects when scope.directories is not an array",
    "rejects when task is missing reason field",
    "accepts reason as empty string (no min constraint on reason)",
    "accepts all valid effort values",
    "returns null for non-object top-level JSON"
   ],
   "orchestra/planner-evaluation-recovery.integration.test.ts": [
    "keeps global verification wave-owned and pauses an unchanged typed failure after one FIX"
   ],
   "orchestra/planner-failure-evidence.test.ts": [
    "is algorithm-prefixed",
    "is deterministic for the same parts",
    "does not collapse parts that a concatenation would merge",
    "separates an empty part from an absent one only by position, not by silence",
    "treats undefined as an empty part rather than throwing",
    "changes when content changes at equal length",
    "counts bytes, not code units, so multibyte output frames correctly"
   ],
   "orchestra/planner-host-completed-wiring.test.ts": [
    "turns a registered identity-only proposal into a digest-bound four-platform V2 contract",
    "keeps the immutable verifier outside the natural 7099 L1 source and test write scope",
    "rejects an identity tuple that has no code-owned proof profile",
    "accepts only the exact identity-proposal shape",
    "serializes a canonical host-owned contract back to authoring input without derived digests",
    "planner parsing replaces the identity proposal with host-completed plan evidence",
    "discards a legacy full-V2 model program and every model-authored digest",
    "reports an exact wiring issue path for an unregistered proposal",
    "discards a model wiring proposal when host scope proves wiring is not applicable",
    "both planner prompts request identities only and publish every real registry tuple",
    "preserves host-completed authority through compiler and structured DIRECTIVES admission"
   ],
   "orchestra/planner-invocation-receipt.test.ts": [
    "refuses a model-owner mismatch under the exact requested provider",
    "preserves explicit adapter precedence over receipt provider authority",
    "does not fall back to the registry default when the requested provider is absent",
    "keeps model-owner resolution for legacy zero-config callers without receipt context",
    "persists zero-config planner calls and their schema retry as separate invocations",
    "rejects an owner-inactive planner target before Brain or zero-config dispatch",
    "blocks a duplicate zero-config invocation before a second provider call",
    "keeps zero-config declaration, dispatch, and settlement failures typed and pre-dispatch safe",
    "returns a typed retry dispatch-write failure without a second provider dispatch",
    "records command-build rejection without dispatching or persisting thrown detail",
    "persists exact wire model, separate outcomes, and no prompt/response/argv",
    "persists exact OpenRouter api/api identity before a provider-native call",
    "settles %s without storing raw output",
    "settles a rejected spawn promise as a transport failure",
    "fails closed with zero spawn calls when declaration or dispatch-intent persistence fails",
    "blocks a duplicate invocation instead of replaying after restart",
    "records no-provider as not-dispatched with a queryable receipt",
    "records requested-to-resolved provider fallback instead of rewriting requested truth"
   ],
   "orchestra/planner-normalize.test.ts": [
    "a tracked path named in the description lands in filesRead (never filesWrite)",
    "untracked mentioned paths are NOT invented into scope",
    "relative imports resolve to repo paths (.js→.ts) and join filesRead",
    "unreadable files are fail-soft (task unchanged, no throw)",
    "a behavior-changing src task gains its mirror test in filesWrite",
    "does NOT add the mirror when another task in the plan owns that test file (442-decomposition stays legal)",
    "a pure test-authorship task is never given extra mirrors"
   ],
   "orchestra/planner-notify.test.ts": [
    "auto mode + parse_failed → notify called with phase-change event",
    "auto mode + timeout → notify called with reason=timeout in summary",
    "dry-run preview does not emit raw PLAN progress",
    "dry-run AI fallback also does not emit raw PLAN progress",
    "structured mode success → notify not called with ai-failed",
    ">2x directive count from AI in strict-ai mode → notify called with task-overflow",
    "uses \"Planlanıyor…\" for Turkish language",
    "uses \"Planning…\" for English language",
    "defaults to English for unknown language"
   ],
   "orchestra/planner-override-precedence.test.ts": [
    "mode=ai + ollama override → honored exactly via structured (AI result NOT used)",
    "mode=auto + ollama override → honored exactly",
    "mode=structured + ollama override → honored exactly (regression baseline)",
    "mode=ai WITHOUT override → AI planning still runs (precedence does not hijack normal AI)",
    "mode=ai + Agent/Skills-ONLY override (no Provider/Model) → honored exactly via structured (429-007 PLNR2 regression)",
    "mode=auto + Agent/Skills-ONLY override → honored exactly"
   ],
   "orchestra/planner-plan-contract.test.ts": [
    "rejects identical opposite any-of domains, independent of ordering and duplicate alternatives",
    "propagates typed source requirements before execution even with empty inventory",
    "repairs the provider-authored contradiction in the existing finite planner retry",
    "binds one exact allowed write file to one task without consulting worker concurrency",
    "leaves a non-atomic intent at the host default 1–5 range",
    "renders an atomic prompt without forcing a redundant integration writer",
    "normalizes against the explicit project root before validating the consumed plan",
    "names cardinality and undeclared shared-writer topology violations without model prose",
    "requires a referenced read-only path in filesRead without converting it to write authority",
    "reuses the canonical real-path filter for pseudo path tokens in model prose",
    "uses the existing single corrective round-trip and accepts a repaired atomic plan",
    "rejects closed-scope drift before wiring completion and repairs it without weakening wiring",
    "rejects missing or non-canonical exact writes before provider wiring completion",
    "still requires registered wiring for an exact allowed production write",
    "rejects a second contract violation after exactly two attempts"
   ],
   "orchestra/planner-preflight.test.ts": [
    "real extractScopeFromDirective no longer produces the 414-001 docs/ phantoms",
    "stripPhantomScope is idempotent after source extraction keeps only the real path",
    "the 411-001 file-as-directory phantom is no longer minted at source (row 3312c)",
    "GREEN: stripPhantomScope drops a file-as-directory phantom that reaches it (extension-detected)",
    "does NOT strip a legitimately-declared file or a real grounded directory",
    "the raw extractor no longer produces code/money phantom paths (defect dead)",
    "GREEN: the wired gate emits no BLOCK for the code/money sentence",
    "NO_GO guard — a genuinely missing real path STILL blocks (gate not weakened)",
    "isRealPathCandidate: code/money out, real paths in",
    "adds a test that imports the task source module, ignores an unrelated test",
    "matches by mirror-name convention when test content is unavailable",
    "expandScopeWithAffectedTests appends without duplicating already-scoped tests",
    "NO_GO guard — the cap is enforced AND an overflow is announced (never silent)",
    "expands affected tests and strips phantoms in one pass, reporting both",
    "is a no-op (no mutation, no report) for an already-clean scope with no affected tests"
   ],
   "orchestra/planner-scope-declared-files.test.ts": [
    "keeps existing and not-yet-existing declared files without adding undeclared paths",
    "preserves directory-only scope without inventing file authority"
   ],
   "orchestra/planner-smoke-e2e.test.ts": [
    "structured plan of a DIRECTIVES with a Smoke line → produced task carries smoke",
    "smoke survives JSON serialisation — exactly the `.tasks/task-*.json` write shape",
    "no Smoke line → task.smoke stays undefined (Tier-0, regression baseline)"
   ],
   "orchestra/planner-spawn-output-limit.test.ts": [
    "counts combined stdout/stderr bytes, bounds retention, and terminates once",
    "measures UTF-8 bytes instead of JavaScript character count",
    "preserves the typed limit when SIGTERM synchronously emits close",
    "escalates a SIGTERM-ignoring child to SIGKILL within a finite grace",
    "keeps output-limit ownership through stdin EPIPE until SIGKILL closes the child",
    "force-reaps a timeout child without letting late errors replace timeout classification",
    "rejects invalid output budgets before spawning a child",
    "lets an earlier timeout own settlement when later output and EPIPE race",
    "allows output exactly at the configured boundary"
   ],
   "orchestra/planner-zeroconfig.test.ts": [
    "includes zero-config section when description provided",
    "uses the admitted one-to-five intent-decomposition contract",
    "provides splitting example in zero-config context",
    "does NOT include zero-config section when description is absent",
    "backward compatible — existing tests unaffected (no 4th arg)",
    "includes the user description in the prompt",
    "includes project name",
    "instructs the provider with the admitted one-to-five task contract",
    "includes JSON output format instruction",
    "includes model selection criteria",
    "includes example task splitting for login/OAuth scenario",
    "keeps integration ownership intent-derived rather than forcing a final task",
    "includes file tree when provided",
    "limits file tree to 50 entries",
    "works without file tree — engages the greenfield guidance instead (F-1)",
    "F-1: carries the FILE PATH RULES contract (directory-qualified paths, no invented reads)",
    "includes task splitting parallelism rules",
    "returns parsed result when AI call succeeds with 4 tasks",
    "returns 3 tasks for a simple feature",
    "returns null when AI call fails (non-zero exit)",
    "returns null when stdout is empty",
    "returns null when AI returns invalid JSON (even after the U2 retry)",
    "reports the final typed validation failure without exposing provider output or stderr",
    "rejects production-source tasks without a registered wiring proposal",
    "passes model parameter to the planner spawn",
    "spawns with the correct timeout",
    "passes file tree context to the prompt",
    "handles valid 5-task response",
    "returns a single-task plan",
    "uses description as task title (truncated to 80 chars)",
    "truncates long descriptions in title to 80 chars",
    "keeps full description in task description field",
    "uses the canonical balanced Brain model as default",
    "sets scope.directories to src/",
    "includes description in reasoning",
    "sets goNogo criteria",
    "marks as fallback in reason field",
    "sets normal effort and priority",
    "returns empty dependencies"
   ],
   "orchestra/planner.test.ts": [
    "includes directives content",
    "includes project name",
    "includes maxWorkers as concurrent execution limit (not task count cap)",
    "instructs AI to plan ALL directive tasks without count limit",
    "includes JSON format instruction",
    "requires identity-only production-wiring authoring and keeps host authority out",
    "includes memory when present",
    "includes critical debt",
    "includes file tree (limited to 100)",
    "preserves full directives and canonical whole-unit context beyond 200 lines",
    "handles empty context gracefully",
    "F-1: carries the shared FILE PATH RULES contract (same block as the zero-config prompt)",
    "canonicalizes AI-authored V2 wiring and derives both digests on the host",
    "rejects an AI production-write task that omits V2 instead of trusting planner prose",
    "admits an omitted contract only for a host-proven test-only scope",
    "rejects AI wiring whose identity tuple has no registered host profile",
    "parses valid JSON",
    "strips code fences",
    "returns null for invalid JSON",
    "returns null for empty string",
    "returns null for missing required fields",
    "returns null for invalid model value",
    "returns null for empty tasks array",
    "validates all model values",
    "returns explicitly provided adapter",
    "returns registry default when no adapter provided",
    "throws when no adapter provided and registry is empty",
    "does NOT silently fall back to any hardcoded provider",
    "born-690: prefers the model-owning provider over the registry default",
    "fails loudly for models unknown to the model registry instead of using the registry default",
    "fails loudly when the owning provider is absent instead of using a foreign registry default",
    "fails loudly for an absent explicit requested provider instead of re-resolving from model owner",
    "born-690: an explicitly provided adapter still wins over model-aware resolution",
    "extracts CLI binary from adapter.buildCommand()",
    "builds generic args when adapter lacks buildPlannerCommand",
    "extracts \"codex\" from codex adapter buildCommand",
    "calls adapter.buildCommand to extract binary name",
    "delegates to adapter.buildPlannerCommand() when available",
    "does NOT call buildCommand when buildPlannerCommand is available",
    "codex adapter builds its own args via buildPlannerCommand",
    "preserves provider-owned stdin without placing the prompt on argv",
    "prefers a provider-native planner invocation over subprocess command construction",
    "fails loudly when provider metadata disagrees with the wire model or adapter identity",
    "maps the task default into the configured worker provider namespace",
    "registers an explicitly authorized versioned API model for downstream DIRECTIVES parsing",
    "throws when adapter.buildCommand returns empty string",
    "uses the canonical win32 wrapper and carries large/metacharacter prompts only on stdin",
    "spawns POSIX binaries directly and ignores stdin when no input is declared",
    "settles an early-exit stdin EPIPE once as a typed spawn error without crashing",
    "preserves timeout classification when SIGTERM causes a later stdin EPIPE",
    "uses adapter to determine CLI command",
    "uses codex adapter CLI binary when codex adapter provided",
    "resolves the registered model-owning adapter when no adapter is passed",
    "rejects a foreign registry default before provider spawn when the model owner is absent",
    "rejects when registry is empty and no adapter provided (no silent fallback)",
    "returns parsed result when adapter-based call succeeds",
    "returns null when adapter-based call fails (non-zero exit)",
    "returns null on empty stdout",
    "returns null when the spawn times out (SIGTERM at the deadline)",
    "uses adapter with buildPlannerCommand for callBrainPlanner",
    "passes model parameter correctly",
    "rejects when no adapter and empty registry (no silent fallback)",
    "uses adapter CLI when adapter provided",
    "forwards provider-owned stdin through the zero-config planner dispatch",
    "resolves the registered model-owning adapter when no adapter is passed",
    "returns parsed result on success",
    "returns null on failure",
    "returns null on empty stdout",
    "uses adapter with buildPlannerCommand for zero-config",
    "retries ONCE with a schema-feedback prompt when the first response is unparseable (U2)",
    "returns a single-task plan without provider interaction",
    "truncates long descriptions in title to 80 chars",
    "works without any provider registered (structured mode)",
    "includes the description in the prompt",
    "includes file tree when provided",
    "omits file tree section when empty",
    "uses default BRAIN_PLAN_TIMEOUT_MS when no timeout provided",
    "uses custom timeout when provided (config.ai_planner_timeout)",
    "custom timeout overrides the default BRAIN_PLAN_TIMEOUT_MS",
    "callZeroConfigPlanner uses custom timeout when provided",
    "callZeroConfigPlanner uses default timeout when none provided",
    "planner.ts source file contains zero hardcoded \"claude\" strings",
    "missing adapter throws error with clear message (no silent fallback)",
    "adapter with buildPlannerCommand produces non-Claude-shaped args",
    "returns all sections joined when within limit",
    "HOLDs instead of silently dropping lower-priority sections",
    "HOLDs when even the highest-priority meaning unit exceeds the caller limit",
    "never drops the lowest-priority section to satisfy a legacy limit",
    "skips empty text sections",
    "is English with no language parameter",
    "includes the context block",
    "zero-config mode block is English",
    "is English with no language parameter",
    "contains the description and project name",
    "resolves a title-string dependency to the sibling task id (faithful — pre-fix RED)",
    "resolves multiple deps mixing title and slot-id refs",
    "preserves already-correct slot-id dependencies (behaviour-preserving)",
    "de-duplicates repeated refs that resolve to the same id",
    "drops a self-reference without reporting it as unresolvable",
    "drops an unresolvable title dep and reports it (never silent)",
    "flags an id-shaped unresolvable ref distinctly from a title typo",
    "leaves tasks with empty / undefined dependencies untouched"
   ],
   "orchestra/post-settlement-binary-staging.test.ts": [
    "classifies %s as a built-binary demand (%s)",
    "treats %s as source verification (no demand)",
    "a Smoke needing the built binary never becomes an in-sprint criterion",
    "the rejected demand is restated as a bounded post-settlement obligation",
    "an explicitly declared PromotionProof wins over the restaged smoke",
    "parseStructuredDirectives raises the demand loudly while the gate is advisory",
    "lintProofStaging blocks an executable in-sprint demand and warns on prose",
    "a criterion that FORBIDS touching the build output is never read as a demand",
    "directive → parsed task → createTask → task-JSON write shape",
    "a restaged demand survives the same round-trip",
    "a correctly staged task produces no findings at all",
    "stageBuiltBinaryProofObligation refuses an empty command instead of inventing one",
    "staging is a provable no-op for a task with a %s",
    "a source-satisfiable smoke still flows through to task.smoke",
    "lintProofStaging is silent for a normal source-verification task"
   ],
   "orchestra/post-settlement-planner-wire.test.ts": [
    "structured plan of a DIRECTIVES with a PromotionProof line → produced task carries the projection, without inflating task count",
    "projection survives JSON serialisation — exactly the `.tasks/task-*.json` write shape",
    "no PromotionProof line → task.postSettlementProjection stays undefined (regression baseline)",
    "auto mode: a declared PromotionProof forces exact structured routing (never silently dropped by the AI planner)",
    "carries the host-derived contract and proof-program digests without a hidden task"
   ],
   "orchestra/postfix-pending-scan.test.ts": [
    "parent DONE + child PENDING: post-FIX pass dispatches the never-spawned child",
    "no eligible PENDING tasks: behavior is byte-identical (no wait, no event)",
    "a NEW failure from the post-FIX pass is a single-pass NO_GO — no second dispatch wave",
    "spawned task with no result (worker crashed) is skipped, not evaluated"
   ],
   "orchestra/pre-start-guards.test.ts": [
    "runs all four guards in order: staleness → CI gate → hooks → safety point",
    "checkBuildStaleness failure is fail-soft — remaining guards still run",
    "CI/tsc gate failure throws BrainError and short-circuits hooks + safety point",
    "CI/tsc gate failure without blockedReason falls back to a default message",
    "beforeSprint hook failure is fail-soft — safety point still created",
    "passes sprintId/tasks/config/projectRoot through to the beforeSprint hook context",
    "non-stash-pop safety-point failure is fail-soft — returns null safetyPoint, no throw",
    "stash-pop failure propagates as a hard error",
    "rollbackEnabled=false skips the entire safety-point guard",
    "warns and skips safety-point creation when not in a git repo",
    "cleans orphan safety points before checking the git repo",
    "cleanOrphanSafetyPoint failure is fail-soft — safety point still created",
    "passes pre_sprint_tests=true through as no override (full validation)",
    "passes pre_sprint_tests=false through as track_test_count override"
   ],
   "orchestra/preflight-cleanup.test.ts": [
    "should simulate full sprint lifecycle: finalize sprint-143, then preflight sprint-144",
    "should preserve fresh locks and clean stale ones during post-finalize",
    "should handle empty .tasks/ during preflight",
    "should create separate archive dirs per sprint"
   ],
   "orchestra/process-controller-process.test.ts": [
    "(c) parks a process with no recognizable scope (fail-safe → critical-irreversible)",
    "(c) auto-runs a docs/-scoped (reversible) process and completes via the real runtime",
    "reports failure when an auto-run process has no definition (honest-fail, not silent ok)"
   ],
   "orchestra/process-controller.test.ts": [
    "auto-runs a read-only capability (erp.read → pure) and completes",
    "parks a side-effecting capability (erp.write → critical-irreversible) for approval",
    "auto-runs a task with a reversible scope (docs/) and completes",
    "durably parks an auto task when provider authority holds before dispatch",
    "does not apply provider admission to provider-free capabilities",
    "holds an auto %s entry before task or sprint execution",
    "parks a task with no recognizable scope (fail-safe → critical-irreversible)",
    "reports failure when the capability handler denies (write → success=false)",
    "status() returns null for an unknown execution id",
    "carries the actor tenant onto the backlog entry",
    "stamps the full actor (id+role+tenant), not just tenant, onto the backlog entry",
    "passes the entry actor (real OIDC sub) into the capability invocation — not a constant system",
    "falls back to a tenant-scoped system actor when the entry carries no actor (backward compat)"
   ],
   "orchestra/process-runtime.test.ts": [
    "(a) runs a 2-step task process strictly sequentially and reports a DONE envelope",
    "(a) runs a mixed task→capability process sequentially (capability via F8 broker)",
    "aggregates GO_WITH_TECH_DEBT when a step carries tech debt but none fail",
    "threads each task step settlement authority into its result wait",
    "consumes an exact accepted result directly without reading the public result projection",
    "rejects a metadata-less exact accepted projection without polling public bytes",
    "fails closed when an exact launch has no accepted-result authority",
    "(b) NO_GO when the entry carries no process definition (not \"not available\")",
    "(b) NO_GO on an empty steps array",
    "(c) short-circuits on the first failing step — remaining steps are NOT run",
    "NO_GO when a capability step fails (broker denies/not-found)",
    "NO_GO when a task step launches but returns no taskId (untrackable)",
    "reads a structured spec.steps array",
    "reads inline JSON from spec.description",
    "reads a spec.processRef JSON file (absolute path)",
    "returns null for a processRef that points at a missing file (honest-fail)",
    "returns null when no definition source is present"
   ],
   "orchestra/processqueue-stall.test.ts": [
    "selects all 3 PENDING tasks when slots=3 and dependency_pipeline_enabled=false",
    "after 1st DONE, slot=1 opens and 4th task becomes eligible via queue",
    "selects all 6 PENDING tasks when slots=6 (Sprint 164 happy path)",
    "first wave selects 3, second wave selects 3 after first wave done",
    "after a worker silently dies (status not progressed), PENDING task is detected",
    "pickFromQueue skips tasks already in assignedTaskIds",
    "selectEligibleForSpawn skips tasks already in collectedIds (defensive)",
    "returns 0 slots and 0 eligible when all workers EXECUTING",
    "counts CLAIMED and TESTING as occupying a slot",
    "pickFromQueue+assignedTaskIds prevents double-pick on repeated calls",
    "selectEligibleForSpawn never returns a task already in assignedTaskIds",
    "returns empty list when queue is drained even on repeated invocation",
    "legacy FIFO mode ignores task.dependencies (Sprint 165 freeze)",
    "dependency pipeline mode enforces deps must be DONE",
    "root=%s: the PENDING dependent stays ineligible; an unrelated sibling still spawns",
    "legacy mode (dependency_pipeline_enabled=false) still ignores the failed root — Sprint 165 freeze unaffected"
   ],
   "orchestra/production-wiring-evaluator-gate.test.ts": [
    "preserves DONE only for exact host-settled production wiring evidence",
    "blocks a green-score DONE when host settlement is absent",
    "blocks worker-only or contradictory settlement decisions",
    "fails closed on unsupported declared ingress evidence",
    "keeps staged foundation incomplete until the exact closure barrier settles",
    "never upgrades a lower verdict and leaves legacy tasks unchanged",
    "preserves V2 DONE only when host proof and COMMITTED effect bindings are present",
    "fails closed when a V2 settlement omits or replays its proof authority"
   ],
   "orchestra/production-wiring-host-proof-absence.test.ts": [
    "accepts a byte-empty stdout (older Docker CLIs)",
    "accepts an empty JSON array (Docker 29.x, verified on 29.1.3)",
    "REFUSES a populated array — an existing container is not absent",
    "refuses any other stdout payload",
    "refuses a success exit even with matching stderr",
    "refuses when stderr does not carry the daemon refusal",
    "refuses a timed-out, cancelled, overflowed or signalled probe"
   ],
   "orchestra/production-wiring-host-proof-failure-reason.test.ts": [
    "recovers the reason the harness decided",
    "recovers a verifier-asset reason, the family that retires an attempt",
    "tolerates the trailing newline the harness writes",
    "rejects empty stderr (the pre-fix silent failure)",
    "rejects a foreign kind",
    "rejects extra keys — no smuggling past the exact key set",
    "rejects a missing reasonCode",
    "rejects free prose, a path or a stack in the reason slot",
    "admits the same code shape the settlement predicate accepts",
    "rejects an over-long reason code",
    "rejects non-canonical JSON — key order is part of the contract",
    "rejects non-JSON and a bare string",
    "rejects an array payload"
   ],
   "orchestra/production-wiring-host-proof-runner.test.ts": [
    "runs one hardened image-id-pinned container per observation group and stores hashes only",
    "holds before execution when verifier assets overlap worker write authority",
    "rejects a planner-designated arbitrary repository harness before Docker execution",
    "rejects a registered harness path when the planner forges its adapter request",
    "holds on a changed verifier asset and never accepts the structured output",
    "cleans and proves container absence after timeout before returning HOLD",
    "cleans and proves container absence after cancellation before returning HOLD",
    "never reports a timed-out proof as contained when release absence cannot be proven",
    "rejects bounded-output overflow even when the process exits zero",
    "passes no ambient environment to the actual shell-free command boundary",
    "escalates an actual SIGTERM-resistant process tree to a bounded terminal result"
   ],
   "orchestra/production-wiring-outer-barrier.test.ts": [
    "authorizes when the exact closure settled DONE with a result of record",
    "leaves sprints without a staged foundation completely unaffected",
    "blocks a NO_GO exact closure and exposes a resumable command",
    "blocks a PAUSED closure task",
    "holds a closure that is only settled behind acknowledged tech debt",
    "refuses a synthetic DONE with no worker result of record",
    "holds a closure whose own wiring contract failed to close",
    "blocks — never waits — when the exact closure task is absent from the sprint",
    "blocks an undispatched closure with no evaluation of record",
    "preserves independent settled work while the staged closure is held",
    "reports every blocked closure of a multi-closure staged foundation",
    "HOLDs a terminal publication while a staged closure is blocked",
    "authorizes the terminal handoff once the staged barrier is authorized",
    "keeps legacy behaviour when no staged authority is supplied",
    "blocks a non-staged sprint with EFFECT_HOLD while preserving independent settled work"
   ],
   "orchestra/production-wiring-production-composition.test.ts": [
    "keeps a direct low-level Docker constructor fail-closed unless a trusted observer is injected",
    "composes a trusted observer by default through the normal production factory",
    "binds one hardened group outcome to the accepted attempt and COMMITTED effect authority"
   ],
   "orchestra/production-wiring-prompt.test.ts": [
    "provides valid negative evidence for every advertised state (%s)",
    "returns empty string when the task carries no wiring authority",
    "renders the exact producer→consumer→ingress→enablement→proof chain",
    "is deterministic for the same bound contract",
    "renders V2 topology and proof digest without presenting plan/worker refs as host evidence",
    "renders identities verbatim and never invents one from task prose",
    "bounds long ingress and proof-target lists and names the omission count",
    "fails closed with no identities when the bound digest does not match the contract",
    "fails closed on an unsupported evidence version",
    "renders the resolver-typed exact delta when the chain does not resolve",
    "names the staged-foundation closure barrier instead of implying completion",
    "is absent from the compiled prompt when the task carries no contract",
    "renders exactly one block in the compiled prompt when the task carries one",
    "emits exactly one production-wiring segment, in the volatile T2 tier",
    "compiles byte-identically on the initial and FIX ingresses of the same task",
    "does not repeat mutable directive text or degrade into a generic checklist"
   ],
   "orchestra/production-wiring-surface-parity.test.ts": [
    "Process/Do preserves the canonical outer-barrier BLOCKED outcome as held",
    "Autonomous preserves the canonical outer-barrier BLOCKED outcome as parked"
   ],
   "orchestra/production-wiring-task-builder.test.ts": [
    "binds a complete explicit contract at the common task creation boundary",
    "returns typed HOLD evidence when the declared consumer scope is impossible",
    "rejects a mutated contract whose digest still claims the reviewed plan",
    "accepts foundation work only with exact same-DAG closure IDs and barrier",
    "threads AI planner authority without deriving a consumer from task filenames",
    "rejects an unregistered planner-selected harness at task creation",
    "canonicalizes an exact structured DIRECTIVES ProductionWiring JSON declaration",
    "fails closed instead of dropping a malformed structured wiring declaration",
    "keeps V1 readable but rejects it at the new task admission boundary",
    "preserves legacy non-production tasks without silently manufacturing wiring authority",
    "rejects an omitted production-scope contract instead of silently exempting it",
    "rejects a verifier asset that is inside the worker write authority",
    "compiles the exact prompt entirely in memory before public admission"
   ],
   "orchestra/production-wiring-worker-result-contract.test.ts": [
    "renders the closed-chain block",
    "names the field settlement actually reads",
    "binds version 1 and the worker observer role",
    "carries the bare-hex digest settlement validates, not the display form",
    "offers exactly the observation states settlement admits",
    "never offers \"complete\" as a worker-declarable state",
    "requires non-blank evidence refs",
    "demands the object even on NO_GO — the hold does not care why",
    "is rendered on the incomplete branch too",
    "is absent when the task carries no wiring authority at all"
   ],
   "orchestra/progress-emit.test.ts": [
    "writes EXECUTE event with channel=PROGRESS and pct payload",
    "EXECUTE event is readable back via readEvents",
    "writes SPAWN event with channel=PROGRESS and task id in detail",
    "SPAWN pct is optional — omitting it leaves pct undefined",
    "writes PRE_VITEST event with channel=PROGRESS",
    "PRE_VITEST before/after pair both write to event stream",
    "writes without throwing when sprint-state.json is missing",
    "writes without throwing when sprint-state.json is malformed JSON",
    "0/5 collected → pct=0",
    "3/5 collected → pct=60",
    "5/5 collected → pct=100",
    "0 total tasks → pct=0 (guard against division-by-zero)",
    "Math.round applied: 2/3 → pct=67 (not 66)",
    "emitProgress is called with phase=SPAWN when a queued task is spawned"
   ],
   "orchestra/projection-parity-artifacts.test.ts": [
    "a settled task\\'s landing-proposal.json does not fabricate a stray id — parity passes",
    "a mix of settled-task residue (.result, .hb, proposal and delivery sidecars) is fully excluded — parity passes",
    "a genuinely foreign task-*.json (valid task record, unrelated id) still refuses CLOSED",
    "an unplanned task-*.json with corrupt/incomplete content still refuses CLOSED (classifier non-task-artifact does not silently swallow it)",
    "missingOnDisk arm is unaffected by the classifier routing"
   ],
   "orchestra/promote-w1.test.ts": [
    "returns promoted=false when category is TECHNICAL",
    "returns promoted=false when category is RUNTIME_ERROR",
    "returns promoted=false when category is FATAL_ERROR",
    "returns promoted=false when filesInScope is empty (even with eligible category)",
    "returns promoted=false when tsc fails (BOUNDARY_VIOLATION category)",
    "returns promoted=false when tsc fails (UNKNOWN category)",
    "returns promoted=false when vitest pass ratio < 0.5",
    "returns promoted=false when vitest pass ratio is 0.49 (just below threshold)",
    "promotes when vitest pass ratio is exactly 0.5 (at threshold)",
    "A+B in-scope tsc-clean + C out-of-scope → promoted=true, inScopeFiles=[A,B], droppedFiles=[C]",
    "promotedResult preserves all other result fields unchanged",
    "promotes when category is UNKNOWN and in-scope files exist",
    "returns empty droppedFiles when all files are in-scope (UNKNOWN category)",
    "handles missing noGoCategory (undefined) as gate1 fail",
    "handles missing filesInScope/filesOutOfScope gracefully"
   ],
   "orchestra/promote-w1b.test.ts": [
    "flag-on + NO_GO + isPartialPromotable → upgrades verdict to GO_WITH_TECH_DEBT",
    "Tier-1 DONE task with a failing Smoke command → downgraded to GO_WITH_TECH_DEBT + PROOF_OF_FUNCTION_MISMATCH event",
    "flag-on + NO_GO + isPartialPromotable → emits PARTIAL_PROMOTION_APPLIED event",
    "flag-on + NO_GO + isPartialPromotable → never mutates shared Git HEAD",
    "flag-on + NO_GO + isPartialPromotable → never reverts sibling worktree files",
    "flag-on + ambient result never enters partial promotion",
    "flag-OFF → full NO_GO, attemptPartialPromotion NOT called",
    "flag-on + dishonest result → honest-gate lock overrides to full NO_GO",
    "calls git checkout HEAD -- <path> for each file",
    "falls back to git clean when checkout throws (untracked file)",
    "is a no-op for empty file list"
   ],
   "orchestra/promotion-esm-import.test.ts": [
    "contains zero require( occurrences",
    "imports readdirSync via the top-level ESM `import ... from \\'fs\\'`",
    "promote() finds and promotes a sprint-scoped temp agent under .tasks/agents/",
    "promote() returns false when no matching sprint-scoped temp dir exists"
   ],
   "orchestra/promotion-guard.test.ts": [
    "promote() should return false for a built-in agent",
    "demote() should return false for a built-in agent",
    "promote() should succeed for a temp agent in persistent temp pool",
    "promote() should update source to \"user\" after promoting a temp agent"
   ],
   "orchestra/prompt-authority-drift-race.test.ts": [
    "excludes a retained root DIRECTIVES file for an intent-sourced plan",
    "admits a byte-identical directives-sourced projection",
    "excludes a directives projection mutated between approval and prompt assembly",
    "excludes when source bytes go missing at prompt-assembly time",
    "excludes replay of an older revision/digest against current directives content",
    "keeps invariant AGENTS/CLAUDE/ADR policy language while excluding the drifted directive with typed provenance"
   ],
   "orchestra/prompt-blocks-e2e.test.ts": [
    "renders real-data env-probe + VERIFY-STEPS + allowlist blocks from one real probe→persist→buildWorkerPrompt chain",
    "keeps all three blocks absent (byte-identical legacy prompt) when no probe was ever persisted and the allowlist flag is off"
   ],
   "orchestra/prompt-comment-refresh.test.ts": [
    "spawnWorker() writes a prompt file whose name embeds the taskId"
   ],
   "orchestra/prompt-compile-authority.test.ts": [
    "carries a structured DIRECTIVES Test command into Task.verification without creating a criterion",
    "compiles two real tasks with one stable leading prefix and digest-equal protected authority"
   ],
   "orchestra/prompt-determinism.test.ts": [
    "same task+ctx produces byte-for-byte identical prompt on repeated calls",
    "ADR list in different input order renders identical prompt",
    "equal skill content in separate array instances renders identical prompt",
    "worker contract leads and Skills remains before Agent inside T1",
    "same-class tasks keep the exact leading prefix byte-identical",
    "rendered prompt contains no ISO-8601 timestamp pattern"
   ],
   "orchestra/prompt-evolution-retro-wire.test.ts": [
    "caller tetikler: reads outcome file from disk and returns a non-empty suggestion",
    "boş outcome no-op: returns the base prompt unchanged when no outcome file exists",
    "defaults basePrompt to empty string when omitted (caller convenience)",
    "does not mutate the outcome file on disk (suggestion-only contract)",
    "retro çıktısına yazılır: formats a non-empty suggestion as markdown",
    "renders the heading + \"none\" line even when there are no suggested changes",
    "end-to-end: disk outcomes → caller → formatter produces a retro-ready section",
    "produces an evolved-prompt suggestion from a sprint outcome file (outcome → öneri)",
    "reinforces a success pattern when the sprint file shows mostly DONE outcomes (başarı pattern)",
    "adds a risk warning when the sprint file contains repeated NO_GO outcomes (başarısızlık pattern)",
    "is a no-op when the outcome file is missing or empty (boş)",
    "degrades gracefully when the outcome file is malformed JSON",
    "does not mutate any disk state — purely a suggestion (no-apply contract)",
    "delegates to evolvePrompt for in-memory outcomes (companion helper)",
    "returns a no-op shape for an empty outcome list"
   ],
   "orchestra/prompt-evolution.test.ts": [
    "reinforces success pattern when ≥3 outcomes succeed at ≥75% rate",
    "adds a failure warning when ≥2 NO_GO outcomes occur",
    "is a no-op when outcomes is empty",
    "is idempotent — re-evolving the evolved prompt with the same outcomes adds no further changes",
    "does not reinforce success when sample size is too small"
   ],
   "orchestra/prompt-exact-execution-authority.test.ts": [
    "excludes root DIRECTIVES for an intent-sourced exact plan",
    "admits only a byte-identical directives projection and rejects later drift",
    "renders one digest-bound task authority while preserving invariant project policy"
   ],
   "orchestra/prompt-gate-scope-wiring.test.ts": [
    "fail-closes wildcard and cross-platform ambiguous legacy scope before scheduling",
    "buildScopeBlock renders README.md/README-TR.md when they are tracked root files",
    "buildScopeBlock renders .secrets-baseline when tracked (fixture-012 write set)",
    "evaluatePromptGate BLOCKs a root write path the render would silently drop (untracked)",
    "does NOT block the same root path once it is tracked (SAN-1 fix live at the gate)",
    "skips both scope lints entirely without a trackedFiles signal (fail-soft)",
    "refuses the directory grant when every declared write target was sanitized away",
    "keeps the legitimate PQ-4 F5 directory fallback when NO write list was declared",
    "renders the surviving subset when only SOME declared targets are dropped",
    "names the surviving path in NO finding — only the dropped one blocks",
    "stays silent when every declared write path survives sanitization",
    "does NOT block Rule 7 normalization — a trailing \"(yeni)\" suffix is stripped, not dropped",
    "does NOT block Rule 8 dedupe — a case-variant duplicate is collapsed, not dropped",
    "does NOT double-report a WHITESPACE-PADDED path the sanitizer already warned about",
    "does NOT double-report a path the sanitizer already warned about (Rule 5)",
    "leaves empty/whitespace entries to the pre-existing canonical guard, adding no duplicate",
    "passes task.scope.filesRead into the read-satisfiability gate",
    "flags a goCriteria-mentioned path missing from the write authority as BLOCK",
    "fixture-012 produces its UNCHANGED_IN_WRITE-free, self-consistent verdict end-to-end",
    "397-007: typo dir auto-replaces to the sole tracked candidate",
    "397-011: typo-duplicate is dropped, the correct path stays",
    "cross-task guard: another task sharing the typo path is NOT mutated by a foreign resolution",
    "a genuinely ambiguous suspect still blocks even with resolveSuggestions"
   ],
   "orchestra/prompt-gate-start-path.test.ts": [
    "undefined promptGate → never blocks (fail-open, e.g. router/pool load failed)",
    "zero findings → never blocks",
    "WARN-only findings (zero blockers) → never blocks",
    "a BLOCK finding with no acknowledge → blocked, message names task/lint/agent",
    "acknowledgePromptGate:false explicitly → still blocked (not just falsy-default)",
    "--force-prompt-gate / acknowledgePromptGate:true → override applies, NOT blocked",
    "truncates long finding lists to 10 + a remainder note",
    "mixed WARN + BLOCK → blocked (WARN findings never suppress a BLOCK)",
    "RunSprintOptions declares acknowledgePromptGate (the --force-prompt-gate / MCP override channel)",
    "runSprint calls decidePromptGateBlock(sprint.promptGate, opts?.acknowledgePromptGate) — call-site pin",
    "the prompt-gate call site sits AFTER the pre-spawn scope gate and BEFORE the PLAN human checkpoint ",
    "a blocked decision releases the sprint lock/state and throws BrainError tagged SprintPhase.PLAN ",
    "an overridden decision is surfaced via console.warn (never swallowed silently)",
    "baseResponse includes a promptGate field derived from sprint.promptGate",
    "promptGate response shape carries ok + findings + a blocker count",
    "an unacknowledged BLOCK appends an explicit warning to the response summary"
   ],
   "orchestra/prompt-gate.test.ts": [
    "refactorer carries the preserve-behavior mandate; architect/code-reviewer/accessibility-auditor are Write-denied",
    "blocks opposite exact presence predicates at final preview without inventory %j",
    "blocks typed evidence without depending on inventory %j",
    "preserves explicit acknowledgment and localizes typed diagnostics",
    "accepts declared read authority without treating it as physical delivery",
    "checks explicit root and extensionless file evidence with inventory %j",
    "documentation/architecture intent is never construction",
    "construction-weighted operations → true; test/document-weighted → false",
    "fallback (no taskDNA): a non-doc write target implies construction; doc-only does not",
    "387-012: security-auditor (reviewer) on a construction task → WARN persona-role, suggests secure-coding",
    "387-026: refactorer on a behavior-changing task → WARN persona-mandate",
    "refactorer on a genuine refactor task → NO mandate warning",
    "architect (Write-denied) on a code-writing task → BLOCK persona-capability, ok=false",
    "acknowledgePromptGate bypasses a BLOCK (ok=true, overrideApplied) but keeps the finding visible",
    "PASS: bug-fixer (implementer) on a bugfix task → zero findings",
    "G2 metadata: a NON-hardcoded agent carrying deniedTools:[Write] on a construction task → BLOCK (manifest signal, not name-matching)",
    "G2 metadata: a Write-ALLOWED agent (deniedTools:[]) not in the id-set → no capability block",
    "doc-only task + reviewer persona → NOT construction → no role warning",
    "goCriteria with uppercase VEYA → WARN decision-space",
    "lowercase \"or\" in ordinary prose is NOT flagged (avoids English flood)",
    "generic agent → only decision-space runs (no persona lints)",
    "the SAME mismatch is flagged whether the agent came from forceAgent or the router (reads final assignedAgent only)",
    "WARNs when a symbol claimed missing is present in the repo (probe > threshold)",
    "no WARN when the probe returns 0 (symbol genuinely absent)",
    "gate stays pure when no probeRepo is supplied (no premise lint runs)",
    "conservative: a plain English word claimed missing is NOT probed (needs a code-identifier shape)",
    "Turkish absence word (eksik) with a code identifier is matched"
   ],
   "orchestra/prompt-gocriteria-dedup.test.ts": [
    "strips the ### goNogo sub-block when goCriteria is set",
    "leaves description unchanged when goCriteria is unset (nothing else renders the block)",
    "leaves description unchanged when no ### goNogo heading is present",
    "\"${fixture.name}\": each unique clause appears 1-2x, never 0 or 3+",
    "before/after: with the raw description echo left in place, a clause would occur 3x (proves the fix is load-bearing)",
    "renders task.description byte-identical when no goNogo sub-block is present"
   ],
   "orchestra/prompt-god-template-adr-completeness.test.ts": [
    "renders a 12K-char ADR in full (no length-based summary fallback)",
    "does not emit the \"(ADR content truncated for prompt size)\" marker",
    "renders all 3 selected ADRs × 8K chars in full (~24K combined)",
    "never truncates an explicitly referenced binding ADR at operative markers",
    "labels scoring-only condensed ADRs as background without a binding sub-signal"
   ],
   "orchestra/prompt-god-template-adr-relevance.test.ts": [
    "drops ADRs whose relevance score is below the active threshold",
    "omits the entire ADR block (header included) when no ADR meets the threshold",
    "honors a custom adrMinRelevance over the default 0.3",
    "exposes DEFAULT_ADR_MIN_RELEVANCE = 0.3 (locked spec value)"
   ],
   "orchestra/prompt-god-template-idempotency.test.ts": [
    "does not emit the literal `${IDEMPOTENCY_KEY}` placeholder",
    "embeds the deterministic key `<sprintId>-<taskId>-<retryCount>` under the Idempotency Key header",
    "produces the same key when the same task is rendered twice (determinism)",
    "produces different keys for different taskIds (collision-free)",
    "changes the key when routingMeta.rerouteCount changes (retry safety)",
    "formats key as `${sprintId}-${taskId}-${retryCount}` when rerouteCount is set",
    "defaults retryCount to 0 when routingMeta is absent",
    "uses the `no-sprint` sentinel when sprintId is missing"
   ],
   "orchestra/prompt-god-template-persona.test.ts": [
    "full mode (default) renders the CORE body — guidance blocks never duplicate into the transport (F1)",
    "full mode with a MARKER-FREE prompt stays byte-identical to the legacy shape (no pointer)",
    "mode 'full' explicitly set renders identically to the omitted-mode default",
    "guidance mode with a matching intent renders the slice + pointer and omits the full body",
    "guidance mode consumes the persona slice selected by Routing Engine V3",
    "guidance mode falls back to the default slice when the intent has no dedicated section",
    "guidance mode falls back to the FULL body when the agent PROMPT.md carries no guidance markers at all",
    "segment tier/kind classification is unchanged across full and guidance modes",
    "is deterministic per (agent, intent) — repeated composes with the same inputs render identical content"
   ],
   "orchestra/prompt-god-template-skill-completeness.test.ts": [
    "injects 3 skills × 5000+ chars in full, even with effort=low",
    "preserves the tail of a 5000-char skill (no head-only clipping) at effort=low",
    "injects every assigned skill when 10 are passed (no skip on overflow)",
    "does not introduce truncation artifacts in the skill section"
   ],
   "orchestra/prompt-god-template.test.ts": [
    "writes injection audit only beneath an explicit production root",
    "carries one immutable compile-plan digest and canonicalizes overlapping scope authority",
    "does not let a supplied compile plan bypass wildcard admission",
    "adds stricter turn guidance only for the registry-resolved economy tier",
    "keeps turn-economy v2 output and termination guidance in inspection-only prompts",
    "externalizes the complete worker core when the provider seam requests it",
    "delivers the verified guide inline without requiring a private snapshot path",
    "does not confuse a host-only guide digest with delivered authority and fails closed on HOLD",
    "keeps provider/model out of worker-authored tokenUsage claims",
    "should select correct agent for core-dev task",
    "should handle documentation task correctly",
    "should handle test task correctly",
    "should produce prompt with charCount < 30000",
    "should produce prompt with estimatedTokens < 25000",
    "should render ADR content in full even when > 3000 chars (F3, Sprint 182)",
    "should skip empty filler headers",
    "should omit the skills segment entirely for an empty skill list (441-003)",
    "should omit the skills segment when skillPrompts is undefined (441-003)",
    "should include full agent prompt without truncation",
    "should inject skill prompts in order",
    "deduplicates Karpathy Notes from a real multi-skill composition",
    "should fail closed on invalid scope before prompt rendering",
    "should limit ADRs to top 3",
    "should include dependencies info in prompt",
    "should NOT contain rubric spec in prompt",
    "should include token usage specification",
    "should include the result & self-assessment block",
    "doc-only task prompt suppresses the full test suite and gives a doc verify path",
    "code task prompt KEEPS the full test suite verify",
    "mixed scope (docs + src) is NOT treated as doc-only → keeps full suite",
    "task.type=documentation → doc verify-steps (single canonical source)",
    "task.type=code-development wins over a .md file heuristic → code verify-steps",
    "doc-only task omits the npm-mutation advisory; code task keeps it (LP-6)",
    "renders the live failure count and drops the stale ~67 hardcode",
    "states a green baseline when zero pre-existing failures were measured",
    "emits no fabricated count when no baseline is available, but still warns",
    "renders the host-smoke note when the task has a Smoke: directive",
    "omits the smoke note entirely when no Smoke: directive is present",
    "drops the skill whose name matches the assigned agent",
    "keeps all skills when none collide with the agent name",
    "renders the shared one-write activity contract with bound identity",
    "holds instead of inventing unavailable attempt/backend identity",
    "renders one checklist item per structured criterion with an N/N→DONE rubric",
    "fires for refactorer on a behavior-changing (implementation) task",
    "fires for refactorer on a bugfix task",
    "is empty for refactorer on a genuine refactor task",
    "is empty for a non-refactorer agent (bug-fixer) — no persona mandate to override",
    "is empty for refactorer with no taskDNA intent (conservative: no signal → no note)",
    "is empty for refactorer on a documentation task",
    "renders the deterministic segment for every worker when ctx carries it",
    "omits the section entirely when the segment is absent"
   ],
   "orchestra/prompt-lint.test.ts": [
    "flags a file the task text asks to edit but write authority omits",
    "does NOT flag a mention inside an explicit read-only clause",
    "does NOT flag files already in filesWrite",
    "flags a goCriteria-demanded test family missing from the exact verify set",
    "silent when the demanded family resolves into the set",
    "flags refactorer+implementation intent when the text claims additive-only (src-writing task)",
    "silent for refactor intent (block will not render)",
    "flags refactorer whose every write target is a test file",
    "silent when a src file is also written",
    "flags sh-portability on a pure TS contract task (corpus class: 10/31)",
    "silent when the domain signal exists (a .sh wrapper in scope)",
    "never flags unknown/broad skills (no signal map entry)",
    "flags a write target neither tracked nor named in the task text",
    "silent for a new-by-design file the task text names",
    "skipped entirely without a trackedFiles snapshot",
    "every finding carries level=warn (fail-closed is a later, evidence-gated flip)"
   ],
   "orchestra/prompt-previous-result.test.ts": [
    "embeds DONE dependency selfAssessment, files, and notes into prompt",
    "renders \"Pending (not yet complete)\" when .result file is missing",
    "includes both added and removed line counts when both present",
    "falls back to \"Pending\" sentinel when .result file is malformed JSON",
    "omits Dependencies block when task has no deps",
    "embeds T1 result content in T2 prompt for a 2-task dependency chain",
    "handles a mix of present and missing dependency results",
    "emits \"UNKNOWN\" status when selfAssessment field is absent",
    "does NOT touch real .tasks/ when tasksDir is explicitly provided",
    "renders only file list when linesAdded/linesRemoved are absent",
    "does not throw when default tasksDir (cwd/.tasks) has no result files",
    "creates an empty tasksDir and still resolves missing deps as Pending"
   ],
   "orchestra/prompt-readonly-contract.test.ts": [
    "renders exact read/provenance/budget truth and removes construction instructions",
    "preserves the ordinary code-task compiler path",
    "renders typed inspection verification exactly, including environment prefixes",
    "rejecting every authored read path never widens authority to the directories",
    "renders exact repo-root manifests as reads while preserving zero project-write authority",
    "rejects mixed-platform path escapes without dropping a valid exact root read"
   ],
   "orchestra/prompt-result-shape.test.ts": [
    "states notes must be a single string and forbids array/object",
    "states selfAssessment is exactly one of the three literal values",
    "states filesChanged is an array of file-path strings",
    "states workerId, testsPassed, and coverage with parser-safe scalar shapes",
    "reaches the prompt identically across claude/codex/gemini provider paths"
   ],
   "orchestra/prompt-rollback-wire.test.ts": [
    "düşük-perf rollback: low success rate triggers rollback suggestion",
    "iyi-perf koru: high success rate preserves current prompt (no rollback)",
    "versiyon zinciri: rollback picks best historical version from version chain",
    "boş: empty outcomes produce no rollback suggestion",
    "smoke from parseStructuredDirectives flows through createTask to task.smoke",
    "task.smoke is undefined when CreateTaskParams carries no smoke",
    "task.smoke is undefined for parsed directive with no Smoke: line",
    "plannerTaskToParams propagates smoke from planner task to CreateTaskParams",
    "plannerTaskToParams returns smoke=undefined when PlannerTask has no smoke",
    "createTask round-trip: smoke command and expect survive JSON serialisation shape"
   ],
   "orchestra/prompt-run-policy-authority.test.ts": [
    "omits the block entirely when no run policy authority is supplied",
    "renders the digest, bounded constraints and override-precedence instruction",
    "never reproduces raw DIRECTIVES bytes — only the caller-supplied bounded summaries",
    "caps the rendered constraint count and honestly notes the omission — never an unbounded dump",
    "truncates an individual over-long constraint instead of dumping it verbatim",
    "is provider-neutral: identical run-policy segment content regardless of provider/model",
    "applies identically to an original task and its FIX attempt (same run policy, no auto-override)",
    "drops empty/blank constraints and returns an empty block when nothing renderable remains"
   ],
   "orchestra/prompt-segmentation.test.ts": [
    "classifies the global worker-contract/verify/karpathy kinds as T0",
    "classifies tenant-project stable persona/skills/context/policy kinds as T1",
    "classifies the volatile task/scope/goNogo (and id-bearing) kinds as T2",
    "an unknown kind falls back to T2 so it can never poison the shared prefix",
    "partitions segments by tier, preserving within-tier order",
    "reorders T0 → T1 → T2 while preserving within-tier order",
    "is pure — never mutates the input array",
    "two same-class tasks (differ only in id) share a byte-identical T0+T1 prefix",
    "the compiled prompt carries every protected element byte-for-byte from its source builder",
    "PROTECTED_KINDS pins exactly scope / goNogo / verify-precedence",
    "findUnprotected reports a reworded/dropped protected element",
    "exposes the scope protected segment as a standalone segment",
    "every compiled prompt carries the dependency-mutation advisory",
    "is a T0 segment with static content (no task.id interpolation)",
    "classifyTier maps npm-advisory to T0",
    "the default constant is ON",
    "default compilation leads with the worker contract and a contiguous T0+T1 prefix",
    "buildTaskPrompt is byte-identical to the default segmented assembly",
    "reorder ON still keeps Skills before Agent (within-tier order preserved)",
    "never emits an invariant tier after the first task-specific segment",
    "segmented build is idempotent (no Date/random leakage)",
    "emits for the default (no-arg) call and the targeted mode",
    "suppresses ONLY the doc path (a doc task runs no tests)",
    "a targeted (code) prompt always carries the protected precedence note",
    "is deterministic per (tenant, task-class) and distinguishes tenants",
    "emits the (tenant, profile::agent) key for a code task",
    "keys a doc-only task apart from a code task (different T0 composition)",
    "keys an inspection-only task apart (read-only discipline composition)",
    "honors the caller-supplied tenant and falls back to the local sentinel",
    "two tasks of the same class share the key AND the byte-stable prefix",
    "is metadata-only — the compiled prompt is unchanged by the key/profile",
    "every emitted segment is tagged with exactly classifyTier(kind) — default order",
    "still holds under the leading-T0 reorder (reorder preserves each tag)",
    "the byte-stable prefix excludes every per-task (T2) token — no cache poisoning"
   ],
   "orchestra/prompt-testhints.test.ts": [
    "returns empty string when no exit-path signal is present",
    "matches when the signal is only in the description",
    "matches when the signal is only in goCriteria",
    "is case-insensitive",
    "returns exactly one line (no embedded newline in the hint body)",
    "does not mutate byte-identity for a non-matching task across repeated calls",
    "an exit-path task prompt contains the hint line exactly once",
    "a non-matching task prompt is byte-identical to the pre-hint baseline shape (no hint text present)",
    "two renders of the same non-matching task are byte-for-byte identical (determinism preserved)",
    "a doc-only task with exit-path wording does NOT get the hint (no test-mocking guidance on the doc verify path)"
   ],
   "orchestra/prompt-token-optimizer.test.ts": [
    "returns high relevance for a testing skill against implementation intent with test domains",
    "returns low relevance for a documentation skill against security intent",
    "uses V2 activation rules when manifestVersion === 2",
    "returns a score between 0 and 1",
    "does NOT match a keyword inside a longer word — word-boundary aware (441)",
    "returns empty array when input is empty",
    "returns at least one skill even when none pass threshold",
    "filters out irrelevant skills and keeps relevant ones",
    "preserves order of passing skills",
    "returns input unchanged when only one prompt",
    "keeps relevant prompts for the given intent",
    "returns an EMPTY list (not the full set) when no prompt clears relevance, and logs it (441)",
    "drops a skill whose name only matched via the removed reverse direction (441)",
    "drops a skill whose name only matched a DOMAIN via the removed reverse direction (441)"
   ],
   "orchestra/prompt-turn-economy.test.ts": [
    "renders the exact block verbatim in a code task prompt",
    "renders the exact block verbatim in a doc-only task prompt (unconditional, unlike npm-advisory)",
    "covers all 4 directives: parallel batching, no stale re-read, block-level verify, single-turn plan gather",
    "appears after the Karpathy Discipline anchor",
    "appears before the Shared Context / Upstream Handoffs / Worker Communications tail blocks",
    "the block stays compact: ≤15 lines",
    "the block\\'s prompt-size footprint (content + one segment separator) stays ≤ 1200 chars"
   ],
   "orchestra/prompt-verify-exact-paths.test.ts": [
    "source 1: test files already in filesWrite are always included",
    "source 2: mirror test of an src/ write-target is included ONLY when tracked (never invented)",
    "source 3: goCriteria-named test families are included (explicit .test ext, even untracked-list-absent)",
    "deduplicates, sorts, and caps deterministically",
    "prints the exact resolved set verbatim",
    "empty/absent set → legacy placeholder line, byte-identical (backward compat)",
    "a task writing a tracked src file with tracked mirror test gets exact verify paths in the rendered prompt",
    "renders only a plain task-local Test command and hides wave-level commands",
    "renders a typed HOLD instead of generic checks when scoped proof is absent"
   ],
   "orchestra/prompt-w1.test.ts": [
    "condenses a NON-scope-intersecting ADR to head+summary+pointer (no body) when scopeGated",
    "keeps the FULL body for a scope-INTERSECTING ADR even when scopeGated",
    "keeps the FULL body when scopeGated is false (default — backward-safe)",
    "keeps explicitly referenced ADRs full and binding regardless of task type",
    "emits a verify-precedence note for a targeted (code) task",
    "a bug-fixer persona + targeted task gets the precedence note in the prompt",
    "a doc-only task does NOT get the precedence note",
    "does NOT split on a `;` nested inside parentheses",
    "preserves an internal newline inside parentheses (does not split it)",
    "still splits on a top-level newline and top-level `;`",
    "falls back to the clause-free rubric when goCriteria is empty",
    "pure-refactor task drops idempotency + host-config",
    "non-API code task drops idempotency + host-config (F1.2 opt-in flip)",
    "API-touching scope (src/providers) keeps idempotency",
    "API-relevant description (webhook) keeps idempotency even with generic scope",
    "host-facing scope (CI workflow) keeps the host-config note",
    "no-type non-API task drops idempotency (F1.2 opt-in flip)",
    "refactor task prompt omits the Idempotency Key section AND host-config note",
    "non-API task prompt omits the Idempotency Key section (F1.2)",
    "API-touching task prompt keeps the Idempotency Key section (F1.2)",
    "host-facing task prompt keeps the host-config portability note"
   ],
   "orchestra/proof-of-function.test.ts": [
    "returns passed=true when expect substring is found and exit=0",
    "returns failed and applyProofOfFunctionGate downgrades DONE → GO_WITH_TECH_DEBT",
    "treats non-zero exit code as failure even when expect matches",
    "treats spawn timeout as failure",
    "treats runner throw as failure (fail-closed)",
    "returns no-op when task.smoke is absent (Tier-1 task without DIRECTIVES Smoke: line)",
    "returns no-op for Tier-0 (non-user-surface) task even when smoke field is set",
    "returns no-op when rubric decision is not DONE (nothing to downgrade)",
    "emits PROOF_OF_FUNCTION_MISMATCH event to the audit sink on downgrade",
    "does NOT emit audit event on no-op gate result",
    "does NOT emit audit event on passed gate result",
    "audit sink throw does not corrupt the downgrade (fail-safe)",
    "does NOT downgrade GO_WITH_TECH_DEBT or NO_GO even when gate fails (nothing to downgrade)",
    "returns null when task.smoke is missing",
    "returns null on malformed smoke spec (missing fields)",
    "returns null when command is whitespace-only",
    "returns the spec when both command and expect are non-empty strings",
    "splits unquoted whitespace",
    "preserves double-quoted groups",
    "preserves single-quoted groups",
    "returns empty array for empty / whitespace input",
    "exposes verifyProofOfFunction symbol identical to the one in proof-of-function module",
    "exposes the audit channel constant via result-evaluator re-export",
    "returns gracefully when the command is empty",
    "reports spawn error for a non-existent binary (does not throw)"
   ],
   "orchestra/provider-default-resolution.test.ts": [
    "returns the registered default adapter (pure-Ollama scenario)",
    "returns null when the registry is empty (does not throw)",
    "returns the registered default name when one exists (ollama scenario)",
    "falls back to the 'claude' literal only when the registry is empty",
    "rejects empty availability even when a registry default exists",
    "rejects empty availability when both the list and registry are empty",
    "routes a configured pure-Ollama worker only when Ollama is available"
   ],
   "orchestra/provider-execution-hold.test.ts": [
    "classifies revoked OAuth as auth rather than an execution budget failure",
    "never reclassifies a worker that produced tokens and files as a provider hold",
    "completes collection when every uncollected task belongs to a held provider",
    "reads and deduplicates durable provider holds from the run event stream",
    "keeps hold evidence but removes it from active authority after resume clearance"
   ],
   "orchestra/provider-observation-aborted-retirement.test.ts": [
    "retires only terminalTruth exact UUID rows, preserves foreign and legacy rows, and is idempotent",
    "fails closed after receipt publication when retirement authority errors"
   ],
   "orchestra/provider-parity-fleet.test.ts": [
    "returns true for ollama",
    "returns true for codex",
    "returns true for gemini",
    "returns true for local-llm",
    "returns false for claude",
    "preserves the exact GPT-5.5 API identity"
   ],
   "orchestra/provider-routing-production-wire.test.ts": [
    "threads durable context and parks routing failures as a resumable HOLD",
    "fails plan admission closed when routing or its prompt gate throws",
    "classifies a plain shell command as shell-exec/medium (baseline risky class)",
    "classifies git push --force as git-mutation/critical",
    "classifies npm install as network/medium",
    "prioritizes git-mutation over network when a command matches both (git push)",
    "RISKY_APPROVAL_SCOPES is exactly the 3 named registry-risk-consistent classes",
    "returns a genuine WorkerApprovalGate instance backed by a real, disk-backed ApprovalBroker",
    "submits a REAL request to disk and fails closed (deny) when nobody decides before the timeout",
    "allows and returns no deniedOutput once the real broker decides allow",
    "returns a genuine WorkerApprovalGate instance backed by a real, disk-backed ApprovalBroker",
    "persists an allow verdict as an always RuleStore grant (tüket via permission-store)",
    "never persists a deny verdict — asking again next time stays the safe default"
   ],
   "orchestra/quality-assessor.test.ts": [
    "scores DONE task highly",
    "scores GO_WITH_TECH_DEBT moderately",
    "scores NO_GO low",
    "scores high coverage",
    "detects scope violations",
    "allows .tasks/ files in scope",
    "reads canonical FileChange objects without throwing (live sprint-664 regression)",
    "returns high relevance for successful task",
    "returns low relevance for NO_GO task",
    "returns empty for no assigned skills",
    "boosts typescript skill for .ts files",
    "boosts testing skill for test files"
   ],
   "orchestra/question-approval-bridge.test.ts": [
    "honors the suggestedAction on a human allow",
    "defaults to continue on a human allow without a suggestedAction",
    "maps deny to abort, even when the worker suggested continue",
    "falls back to continue and settles the broker on the fallback channel",
    "defers to a real decision that lands in the fallback race window",
    "does not fire the fallback when the broker decides before the timeout",
    "rejects advisory questions without ever contacting the broker",
    "detects the marker after leading whitespace, matching handleWorkerQuestion",
    "masks secrets out of both maskedArgs and the summary; rawArgsRef stays null",
    "maps the checkpoint shape: lifecycle scope, require-approval, worker requester",
    "clamps an over-long question onto the 200-char summary ceiling",
    "falls back to a task-derived instanceId when workerId is blank",
    "flags credential/secret content as critical",
    "flags destructive/outward operations as high",
    "scans the context too, not just the question text",
    "elevates a worker-suggested abort to medium without a keyword hit",
    "defaults plain ambiguity questions to low",
    "maps a TTL-expire allow to plain continue (never suggestedAction)",
    "maps defer/escalate to the auto-continue fallback",
    "is off by default and only true for an explicit boolean true",
    "binds the approval request to exact attempt/fence/question receipt data",
    "returns typed authority-hold when the exact question changes after the decision",
    "fails closed before broker submission when exact binding lacks revalidation",
    "rejects extra-key and proxied exact bindings before durable approval submission",
    "snapshots exact binding before submit so caller mutation cannot alter durable details"
   ],
   "orchestra/rbac-spawn-mainline.test.ts": [
    "DORMANT default (enforce_rbac undefined): a viewer write-task is NOT deferred",
    "DORMANT (enforce_rbac=false): a viewer write-task is NOT deferred (soft-warn)",
    "ENFORCE: a viewer role lacking fs-write is HARD-deferred for a write-task",
    "ENFORCE: a viewer read-only task is permitted (viewer has fs-read)",
    "ENFORCE: an engineer role IS permitted for a write-task (has fs-write)",
    "ENFORCE: a role-less task always permits (permissive ADR-037 V1.0 default)",
    "ENFORCE: mixed batch defers only the role-denied task, preserving ids",
    "ENFORCE: an unknown role permits (permissive — unknown role is not in the matrix)"
   ],
   "orchestra/read-satisfiability-gate.test.ts": [
    "WARNs when the description orders classifying a path outside read scope",
    "does not WARN once src/core/provider-catalog.ts is added to filesRead",
    "WARNs when the description orders consuming a path outside read scope",
    "does not WARN once src/core/ is covered by a scoped directory",
    "does not WARN when the mentioned path is itself in filesWrite (write implies read)",
    "does not WARN when no read verb is adjacent to the mention",
    "negation guard suppresses an otherwise-valid WARN in the same sentence",
    "positive control: the same sentence minus the negation DOES fire",
    "bare filename mentions are gated the same way as the write-side rule (root-tracked only)",
    "no finding at all — including this rule — on prose that names no path",
    "produces zero findings for a fully self-consistent task (no read-only mentions)",
    "a task consuming only its own filesWrite/directories never triggers MENTIONED_NOT_READABLE",
    "never emits a BLOCK severity, across every fixture in this file that fires"
   ],
   "orchestra/reconciler.test.ts": [
    "actualUsd is the EXACT sum of results[].cost.usd, and variancePct is correct",
    "reports a negative variance for an under-run sprint",
    "does not drift on float sums (0.1 + 0.2 style)",
    "flags a task that ran 3× over estimate on opus and suggests sonnet (goNogo)",
    "emits an info (not warn) signal for a moderate 2× over-run",
    "does NOT signal a task within tolerance",
    "suppresses sub-cent noise even on a large ratio",
    "respects custom signal thresholds",
    "computes per-task estimate, actual, ratio, variance, and overRun",
    "preserves results order in perTask",
    "counts an unestimated result (no matching estimate) with a null ratio",
    "counts an estimated task that produced no result (unmatchedEstimateCount)",
    "handles empty results without throwing",
    "falls back to totalApiCostUsd when no taskDetails are present",
    "clamps NaN / negative costs to 0 (honest, never silently propagated)",
    "returns variancePct null when the total estimate is zero",
    "reads provider as null when a result omits it",
    "maps known tiers down one step",
    "resolves a concrete apiId through its tier keyword",
    "is case-insensitive",
    "returns null for an unknown model (no false suggestion)",
    "honors a custom ladder",
    "uses a provider-extended ladder for non-Claude over-runs",
    "still signals an over-run for an unknown model, with no suggestion",
    "renders totals, variance, task counts, and signals",
    "shows n/a variance and no signals block when there is no baseline"
   ],
   "orchestra/recovery-resume-state-retention.test.ts": [
    "keeps the exact sprint-state bytes when housekeeping preserves its checkpoint",
    "does not alter the checkpoint or sprint-state during a dry-run",
    "never removes another sprint’s persisted state",
    "keeps the existing housekeeping behavior when no checkpoint remains",
    "retires state only for a generation-bound terminal Flow (bound=%s)"
   ],
   "orchestra/recovery-truth-nine-case.integration.test.ts": [
    "1 restart replays the exact terminal receipt without rewriting producer bytes",
    "2 duplicate live coordinator is a typed ACTIVE_AUTHORITY HOLD with no deletion",
    "3 corrupt receipt bytes fail closed as SETTLEMENT_RECEIPT_CONFLICT",
    "4 a foreign generation cannot consume the prior generation terminal receipt",
    "5 the Windows-native adapter carries production platform authority end to end",
    "6 force recovery preserves canonical checkpoint bytes and reports the same digest on replay",
    "7 stale projection is a typed STALLED gate rather than terminal success",
    "8 terminal/projection disagreement is a typed conflict and preserves the receipt",
    "9 projects and replays 10k production task artifacts within the 10s bound without deletion"
   ],
   "orchestra/rejected-result-history.test.ts": [
    "uses the actual schema error and keeps host-authority failure distinct",
    "retires only a reverified earlier terminal rejection, including a cold registry replay",
    "does not retire current/future/unbound current run (%s)",
    "keeps %s owning runs",
    "rereads owning lifecycle after asynchronous backend verification",
    "does not accept a replaced registry entry across the asynchronous check",
    "requires the issuing opaque reader and exact object",
    "does not retire a daemon %s execution",
    "fails closed on changed %s authority",
    "checks custody and acceptance again after the daemon await"
   ],
   "orchestra/released-unaccepted-host-boundary.test.ts": [
    "does not publish when %s fails absence",
    "dry-run inspects without publishing; apply rereads absence and host postimages",
    "refuses semantic drift before publication"
   ],
   "orchestra/repair-birth-authority.test.ts": [
    "admits a deterministic, bounded FIX receipt from immutable semantic evidence",
    "keeps the semantic fingerprint stable across accepted attempt identities",
    "returns terminal HOLD for unchanged evidence and admits changed evidence",
    "requires the predecessor to be the immediate same-sprint repair parent",
    "does not spend product repair authority for the %s failure domain",
    "requires a NO_GO failed authority and an accepted exact XFIX target",
    "rejects a mismatched evaluation artifact and a tampered persisted receipt",
    "derives a durable supersession receipt only for descendants of an exact accepted repair"
   ],
   "orchestra/repair-dispatch-chain-seal.test.ts": [
    "drains every admitted overflow repair before PAUSE and preserves honest PAUSE afterward"
   ],
   "orchestra/repair-overflow-dispatch.test.ts": [
    "hands a slot-overflow FIX task to the collector and dispatches it after the slot opens",
    "hands NOT_DISPATCHED re-dispatch overflow to the collector",
    "preserves queue order when an earlier dependency is not yet complete",
    "pins the fourth waitForResults argument as a defined overflow queue"
   ],
   "orchestra/repair-queue-authority.test.ts": [
    "round-trips the versioned schema and every lifecycle state",
    "admits the same queueId idempotently without adding a second record",
    "ignores a same-directory partial temp file and publishes only by rename",
    "rejects corrupt or partial final authority with a typed error",
    "rejects queueId rebinding and non-monotonic state transitions"
   ],
   "orchestra/repair-quiescence-gate.test.ts": [
    "forbids both PAUSE decisions while an admitted repair is pending",
    "allows the unchanged configured breaker after the durable queue drains",
    "surfaces a typed reason, queue count, and localized operator text",
    "keeps a dispatched attempt fenced until it settles",
    "never lets another run\\'s unsettled repair fence this run",
    "does not let a policy-terminal NOT_DISPATCHED row block quiescence"
   ],
   "orchestra/repair-task-constraint-inheritance.test.ts": [
    "persists one repair task with the original forceSkills and forceModel verbatim"
   ],
   "orchestra/replan-proposal.test.ts": [
    "produces no proposal when the classification still allows a fix task",
    "records the sprint-496 shape: needed source paths outside the reviewed scope",
    "carries the current scope verbatim so the decision is made against real authority",
    "states that a named path is evidence and never a grant",
    "does not treat an untrusted worker attribution claim as replan authority",
    "marks a path already inside the reviewed directories as needing no new authority",
    "de-duplicates repeated mentions",
    "does not swallow a sentence-final period into the filename",
    "returns nothing when the notes name no repo path"
   ],
   "orchestra/resolve-task-model.test.ts": [
    "returns opus for high-complexity cross-module task on max plan",
    "returns sonnet for single-dir moderate task",
    "returns sonnet on pro_plan even when score would give opus",
    "returns sonnet on pro_plan for cross-module task (not opus)",
    "returns sonnet instead of haiku when haiku_allowed=false",
    "floors a low-score CODE task to sonnet even when haiku_allowed=true (MODEL-GUARD)",
    "returns haiku for a low-score DOC task when haiku_allowed=true (economy allowed)",
    "returns sonnet for docs/ scope task regardless of title complexity",
    "returns sonnet for tmp-test/ scope task",
    "returns sonnet for scripts/ scope task",
    "returns sonnet for test-only filesWrite",
    "pro plan takes priority (gives sonnet)",
    "max plan with multi-dir scope returns opus",
    "max5x_plan behaves like max (allows opus)",
    "haiku_allowed=false + simple task that scores haiku → sonnet",
    "returns forceModel=opus directly, bypassing all layers",
    "returns forceModel=haiku even when haiku_allowed=false",
    "returns forceModel=sonnet without score calculation",
    "undefined forceModel uses normal auto-selection"
   ],
   "orchestra/resource-monitor.test.ts": [
    "sampleOnce returns parsed samples from mock spawn output",
    "sampleOnce filters out containers not matching filterPrefix",
    "sampleOnce derives taskId by stripping filterPrefix",
    "sampleOnce uses custom filterPrefix when provided",
    "sampleOnce returns empty array when docker is unavailable (spawn error)",
    "sampleOnce skips malformed JSON lines gracefully",
    "appends samples as JSONL to logPath",
    "uses parseMemoryString from spawn-backend-docker to parse memory values",
    "stop() after start() prevents further ticks (timer cleared)",
    "sampleOnce returns empty array when spawn emits no output"
   ],
   "orchestra/resource-report.test.ts": [
    "parses valid JSONL content into ResourceSample array",
    "skips malformed JSON lines silently",
    "skips lines missing required ts or container fields",
    "returns empty array for empty string",
    "handles trailing newlines and blank lines without error",
    "computes peak and average mem for a single task",
    "groups multiple tasks into separate summaries",
    "computes durationMs from first and last ts",
    "returns empty array for empty samples",
    "returns zeroed summary for empty samples",
    "sums concurrent container mem in the same ts window",
    "returns max concurrent peak across multiple time windows",
    "counts unique containers correctly",
    "formats bytes below 1 KB as B",
    "formats KB range",
    "formats MB range",
    "formats GB range"
   ],
   "orchestra/respawn-mrr-unblock.test.ts": [
    "born-610: dependent of a MANUAL_REVIEW_REQUIRED upstream is NOT spawned (single truth)",
    "dependent of a DONE upstream still dispatches (regression — DONE path preserved)",
    "dependent of a STILL-RUNNING (EXECUTING) upstream stays blocked (regression guard)",
    "born-610 multi-dependent: NEITHER doc spawns on a shared MRR upstream (skip-path owns them)"
   ],
   "orchestra/result-assembler.test.ts": [
    "derives filesChanged/lines from a real git repo (NOT worker-claimed), 3-line new file → linesAdded===3",
    "classifies modified and deleted tracked files by git status",
    "flags out-of-scope changed files as boundary violations",
    "fails open on a non-git directory → diskVerified:false, no fabricated changes",
    "claimed DONE while tsc dirty → honestGate.flagged, violation=claimed-done-tsc-fail",
    "does NOT flag when tsc is clean, or when the worker did not claim DONE",
    "normalizes legacy ingress into strict canonical V1 once",
    "preserves the host-authored xverify terminal projection through strict settlement",
    "preserves digest-bound evaluator fields through strict canonical settlement",
    "preserves run-policy and production-wiring evidence through canonical ingress",
    "promotes exact custody ingress to V2 without accepting worker-authored custody",
    "preserves worker-observed policy and wiring evidence in orchestrator assembly",
    "throws AssemblerError when the assembled result is invalid",
    "embeds the worker-subjective block and leaves brain/auditor slots null",
    "preserves a false worker claim separately without erasing host-measured work",
    "credits only identities rendered into a current receipt",
    "gives no identity credit when a fresh current receipt is absent or malformed",
    "allows files under scope.directories and in scope.filesWrite, flags the rest",
    "matches a directory entry with or without a trailing slash",
    "reports diskVerified=false when ok=false",
    "createDefaultGitChangeProvider is exported and returns a provider"
   ],
   "orchestra/result-collector-corrupt-result.test.ts": [
    "collects malformed bytes as typed NO_GO without enriching or replacing the source",
    "turns identity drift into typed evidence instead of normalizing a foreign DONE",
    "routes live, timeout-race, and final sweeps through the same authority helper"
   ],
   "orchestra/result-collector-disposition.test.ts": [
    "terminally cascade-skips a dependent without entering the FIX lane"
   ],
   "orchestra/result-collector-respawn.test.ts": [
    "kills a stale EXECUTING task, resets to PENDING, and re-spawns it",
    "skips a DONE task in the respawn request (only stale active workers are respawned)",
    "is a no-op when worker_respawn is disabled (config opt-out)"
   ],
   "orchestra/result-collector-settlement-authority.test.ts": [
    "syncs exact accepted custody only through terminal settlement authority",
    "holds exact collection when worker-writable public result bytes predate projection",
    "holds foreign or structurally forged terminal authority without mutating task status",
    "rolls back status when bounded terminal revalidation returns oversized or cyclic authority",
    "preserves an accepted sibling across fatal initial HOLD (deferred=%s), without new dispatch",
    "settles independent B while A is held and dependent C stays unrun",
    "holds exact custody when no terminal settlement authority is supplied",
    "settles a no-dispatch authority with zero attempts and no synthetic result",
    "fails closed instead of reading public question bytes in normal Docker mode",
    "waits past an early raw Docker DONE and collects the later host settlement",
    "bounded polling ignores raw Docker output until host settlement exists",
    "bounded polling propagates corrupt settlement evidence instead of fabricating NO_GO",
    "repairs a malformed raw result through terminal-only reconciliation during the live wait",
    "parks an unclassified capture HOLD that arrives during the IPC poll",
    "records exact IPC projection debt once without blocking private answer success"
   ],
   "orchestra/result-collector.test.ts": [
    "uses the shared debt-DONE and NO_GO projection",
    "detects .timeout file and writes synthetic NO_GO result",
    "prefers real .result over .timeout when both exist",
    "collects normal results without timeout marker",
    "handles multiple tasks with mixed results and timeouts",
    "cascade-skips a dependent whose dependency NO_GO\\'d, no EXECUTE hang (P0-A)",
    "retries an evaluator throw without losing or duplicating the collected result",
    "yields to FIX with the dependent PAUSED and no synthetic result",
    "returns undefined when no question file exists",
    "auto-answers with continue when question file exists",
    "answers questions for active (uncollected) tasks",
    "skips collected tasks",
    "handles multiple tasks with questions",
    "returns empty array when no questions exist",
    "estimates token usage from task estimatedTokens and result linesAdded",
    "falls back when task has no estimatedTokens",
    "uses forceModel over model when both present",
    "ensures minimum outputTokens of 500",
    "enriches result that has no tokenUsage",
    "does not overwrite existing tokenUsage",
    "WP-4: replaces a zero-count worker stub with the orchestrator estimate",
    "WP-4: keeps a legacy non-zero worker claim verbatim",
    "does nothing when task is undefined",
    "preserves provider field for multi-provider sprints",
    "enriches collected results with estimated tokenUsage",
    "does not overwrite worker-reported tokenUsage during collection",
    "enriches 12 tasks and total tokens > 0",
    "parks %s evidence without a TaskResult or dispatching the dependent"
   ],
   "orchestra/result-cost-enrich.test.ts": [
    "paid provider/model → cost.usd > 0 (real per-model pricing)",
    "cache-creation tokens are priced into cost (G6 — limit-dominant cache-write)",
    "local/ollama usage → cost set with usd 0 and isLocal true",
    "subscription usage keeps reference value but records zero incremental billed USD",
    "no tokenUsage → no cost (no-op)",
    "zero tokens → no cost (no-op)",
    "no projectRoot → no cost, never throws",
    "uses provider-final billing as authoritative and preserves local variance evidence",
    "ignores worker-forged providerBilling when no provider log proves it",
    "keeps a zero API provider total billed as zero while retaining the catalog reference"
   ],
   "orchestra/result-evaluator-derived-directory.test.ts": [
    "scores real report content after derived directory and missing projection entries"
   ],
   "orchestra/result-evaluator-tokenusage.test.ts": [
    "missing tokenUsage: warning does NOT contain \"will reject as NO_GO\"",
    "missing tokenUsage: warning mentions \"self-estimate\" and \"limit-ledger\"",
    "complete tokenUsage: isComplete=true, no warnings",
    "legacy .result with tokenUsage is still accepted (backward compat)",
    "partial tokenUsage (missing provider/model): isComplete=false with field-specific warnings"
   ],
   "orchestra/result-evaluator-typed.test.ts": [
    "doc-write task with coverage:null + DONE notes → DONE (Sprint 153 smoke regression guard)",
    "audit task with coverage:null + detailed report → DONE or GO_WITH_TECH_DEBT",
    "code task with coverage:null still NO_GO (schema regression preserved)",
    "doc-write task with coverage:0 (numeric) → DONE (Sprint 153 T-005 scenario)",
    "doc-write task whose worker wrote to src/x.ts → NO_GO (scope violation)",
    "doc-write task with NO_GO self-assessment → NO_GO (worker self-verdict ceiling)",
    "audit task missing the report file falls back gracefully (no crash)",
    "code task with coverage:95 + passing tests → DONE (baseline sanity)",
    "explicit rubric override still wins over registry dispatch",
    "code task: bare testsPassed:true with NO evidence caps DONE at GO_WITH_TECH_DEBT",
    "code task: a test-run trace in notes keeps DONE (evidence honored)",
    "code task: typed PASSED execution matching declared command keeps DONE without prose heuristics",
    "code task: typed PASSED execution with an undeclared command does not bypass the evidence ceiling",
    "code task: changed test file + measured coverage keep DONE (ceiling stays inert on evidence)",
    "doc task: the ceiling is inert (testsPassed already neutral for the class)"
   ],
   "orchestra/result-evaluator.test.ts": [
    "binds a decisive failure to criterion, evidence kind, normalized subject and observed state",
    "does not fingerprint a green criterion probe",
    "records a typed cascade skip and mints no repair candidate when fixEligible=false",
    "mints a repair candidate only when canonical policy sets fixEligible=true",
    "returns true for docs/ directory",
    "returns true for docs subdirectory",
    "returns true when all directories are doc-only",
    "returns false for src/ directory",
    "returns false for tests/ directory",
    "returns false for lib/ directory",
    "returns false for mixed scope (docs/ + src/)",
    "returns false for empty directories",
    "returns false for bare \"src\" directory",
    "returns false for bare \"tests\" directory",
    "returns false for bare \"lib\" directory",
    "returns true for config-only directories",
    "handles task with no scope gracefully",
    "returns NO_GO when selfAssessment is NO_GO (highest priority)",
    "returns NO_GO when selfAssessment is NO_GO even with new tests",
    "returns NO_GO when tests failed (worker says DONE)",
    "returns NO_GO when tests failed (worker says GO_WITH_TECH_DEBT)",
    "returns DONE for doc task even when testsPassed is honestly false (sprint-573/574 honesty fix)",
    "doc task with honest testsPassed:false still respects the honest-DEBT ceiling",
    "doc task with worker self-NO_GO stays NO_GO (honesty fix never overrides the worker verdict)",
    "returns DONE for doc task with passing tests (skips coverage)",
    "returns DONE for doc task even with zero coverage",
    "skips vitest validation for doc tasks",
    "EVAL-DEBT-CEILING (born-450): honest GO_WITH_TECH_DEBT is a ceiling — new tests do NOT upgrade to DONE",
    "EVAL-DEBT-CEILING: coverage>=90 + new tests still cannot upgrade past a declared debt",
    "EVAL-DEBT-CEILING: coverage>=90 alone cannot upgrade past a declared debt",
    "detects .test.ts files as new tests",
    "detects .spec.ts files as new tests",
    "detects .test.js files as new tests",
    "detects .spec.js files as new tests",
    "no test files detected when filesChanged has no test/spec files",
    "handles undefined filesChanged gracefully (no new tests)",
    "handles empty filesChanged (no new tests)",
    "returns DONE for coverage >= 90 with no new tests",
    "returns DONE for coverage exactly 90",
    "returns GO_WITH_TECH_DEBT for coverage 89.9 with no new tests",
    "returns GO_WITH_TECH_DEBT for coverage 0 with no new tests",
    "returns DONE for coverage 50 when worker wrote new tests",
    "treats mixed scope (docs + src) as normal task — low coverage, no tests",
    "mixed scope with high coverage returns DONE",
    "mixed scope with new tests returns DONE",
    "returns GO_WITH_TECH_DEBT when vitest JSON shows coverage mismatch",
    "returns DONE when vitest JSON confirms coverage and has new tests",
    "respects GO_WITH_TECH_DEBT hint as fallback when coverage < 90 and no new tests",
    "returns DONE as default when worker says DONE, coverage < 90 but not caught by other rules",
    "returns 100 when tests pass and selfAssessment is DONE",
    "returns 80 when tests pass but selfAssessment is GO_WITH_TECH_DEBT",
    "returns 0 when tests fail and selfAssessment is NO_GO",
    "doc-class task: honest testsPassed:false scores the same as testsPassed:true (no honesty penalty, no fabrication reward)",
    "doc-class task: selfAssessment component still differentiates (DONE > DEBT > NO_GO)",
    "code-class task keeps the original tests-failed penalty (task supplied)",
    "no task supplied keeps the original behaviour (backward compatible)",
    "returns coverage value boosted by new test files",
    "returns raw coverage when no test files",
    "caps score at 100",
    "returns 100 when all files are within scope",
    "returns partial score when auxiliary files are out of scope (D-5 relaxation)",
    "returns 0 when all files are fully out of scope (no auxiliary)",
    "returns 100 for empty filesChanged",
    "returns 100 for detailed notes (>=100 chars)",
    "returns 10 for minimal notes (<20 chars)",
    "does not schema-NO_GO a passing direct-test task when coverage is structurally absent",
    "treats the result-contract coverage=0 sentinel as unmeasured for a direct-test task",
    "returns DONE with default rubric for perfect result",
    "EVAL-DEBT-CEILING (born-459, 357-016 live case): honest worker GO_WITH_TECH_DEBT is never raised to DONE by a passing rubric score",
    "EVAL-DEBT-CEILING (born-482): verification-task fast-path also respects honest DEBT",
    "returns DONE with custom rubric",
    "returns NO_GO for failing result",
    "returns GO_WITH_TECH_DEBT for mediocre result",
    "caps maxRetries at 3",
    "handles unknown criterion gracefully",
    "respects per-criterion threshold",
    "detects \"Bash tool unavailable\" in notes",
    "detects \"session-env ENOENT\" pattern",
    "returns false for normal notes",
    "returns false for empty notes",
    "returns GO_WITH_TECH_DEBT when Bash unavailable and testsPassed=false (DONE self)",
    "returns GO_WITH_TECH_DEBT when Bash unavailable and testsPassed=false (TECH_DEBT self)",
    "returns NO_GO when Bash unavailable but selfAssessment is NO_GO",
    "returns NO_GO when tests fail without Bash unavailable signal",
    "detects \"cannot run tsc\" pattern as Bash unavailable",
    "detects \"ENOENT session-env\" reversed pattern",
    "returns neutral score 50 when Bash unavailable and coverage=0",
    "returns normal score when Bash unavailable but coverage > 0",
    "returns normal score when coverage=0 without Bash unavailable",
    "returns neutral score with \"cannot run vitest\" notes",
    "returns GO_WITH_TECH_DEBT (not NO_GO) for Bash unavailable result with rubric",
    "returns CODE_VERIFIED_DONE when result missing + code modified + evidence grep hit",
    "returns honest NO_GO when result is NO_GO + no files modified",
    "returns honest NO_GO when files modified but evidence grep misses",
    "skips reconciliation when result is already DONE",
    "returns fail-safe honest NO_GO on git status error",
    "skips reconciliation when NO_GO is not Docker auto-generated",
    "handles missing task JSON gracefully",
    "handles empty filesWrite array gracefully",
    "verifies CODE_VERIFIED_DONE when no evidence command in description (files-only)",
    "extracts grep command from Kanıt pattern",
    "returns null for non-grep commands (safety)",
    "returns null when no Kanıt section present",
    "handles wc command",
    "handles ls command",
    "returns DONE unchanged when no verify-delta ratio provided",
    "returns GO_WITH_TECH_DEBT unchanged when no verify-delta ratio provided",
    "never downgrades NO_GO (always respected)",
    "preserves DONE when completion >= DONE threshold",
    "downgrades DONE → GO_WITH_TECH_DEBT when completion 50-79%",
    "downgrades DONE → NO_GO when completion < 50% (Sprint 137 regression catch)",
    "escalates GO_WITH_TECH_DEBT → NO_GO when completion < 50%",
    "preserves GO_WITH_TECH_DEBT when completion >= 50%",
    "includes completionRatio in result when provided",
    "exports threshold constants with expected values",
    "returns isComplete=true and no warnings for a fully populated tokenUsage",
    "warns when tokenUsage is entirely absent",
    "warns when inputTokens is missing",
    "warns when outputTokens is missing",
    "warns when provider is missing",
    "warns when model is missing",
    "accumulates multiple warnings when several fields are missing",
    "accepts cacheReadTokens as optional — complete even without it",
    "does not affect evaluation decision — soft warning only",
    "classifies exitCode 137 as RUNTIME",
    "classifies Docker HB shutdown note as RUNTIME",
    "classifies tsc error notes as CODE",
    "classifies test failure output as CODE",
    "classifies mixed runtime+code signals as AMBIGUOUS",
    "returns AMBIGUOUS when no signals detected",
    "classifies \"no such container\" as RUNTIME",
    "classifies scope violation notes as CODE",
    "RUNTIME → retry=true, cascade=false, no fix worker",
    "CODE → retry=false, cascade=true, spawn fix worker",
    "AMBIGUOUS → retry=true, cascade=false, no fix worker",
    "includes taskId in reason string",
    "never returns a fabricated numeric-zero score — totalScore is a real weighted grade",
    "rich durable evidence (DONE claim + tests passed + in-scope + detailed notes) → DONE, not capped at tech-debt",
    "thin/absent notes → GO_WITH_TECH_DEBT (insufficient durable evidence for a clean DONE, never NO_GO)",
    "concrete test failure vetoes NO_GO — never salvaged by a good score elsewhere",
    "worker self-NO_GO is preserved — worker priority over any reconstructed score",
    "a scope violation (files outside task.scope) is a concrete veto → NO_GO",
    "a schema-invalid recovered result (missing selfAssessment) is NO_GO, not a fabricated pass",
    "writes explicit decision provenance: worker claim, recovered evidence, rubric availability, veto, reconciliation, final verdict",
    "is deterministic — the same (result, task) reconstructs to the same decision and totalScore",
    "a GO_WITH_TECH_DEBT worker self-claim is never upgraded to DONE even with strong durable evidence (EVAL-DEBT-CEILING parity)",
    "uses the canonical document rubric and records N/A test evidence during reconstruction"
   ],
   "orchestra/result-ingest-idnorm.test.ts": [
    "RED: a raw `task-`-prefixed content id lookup-MISSES buildResultsMap by the sprint task id",
    "normalizeIngestedTaskId adopts the filename-derived id and LOUD-WARNs (file + both values) on `task-` prefix drift",
    "normalizeIngestedTaskId also normalizes an ARBITRARY mismatch (filename is authoritative)",
    "normalizeIngestedTaskId fast-path: a correct content id is left verbatim with NO warn",
    "GREEN: collectResults keys a drifted result by the filename id and fails the invalid ingress closed",
    "GREEN: strict ingest leaves the worker-authored file immutable while canonicalizing the collected projection",
    "GREEN: a correctly-formed content id flows through untouched with NO spurious warn"
   ],
   "orchestra/result-json-control-char-tolerance.test.ts": [
    "round-trips a literal control character without changing its payload semantics",
    "returns a typed terminal failure for unrecoverable malformed JSON",
    "collects an unrecoverable result as a typed failure instead of waiting indefinitely"
   ],
   "orchestra/result-merger.test.ts": [
    "returns empty for no overlaps",
    "detects single file overlap",
    "detects multiple overlaps",
    "deduplicates worker IDs",
    "sorts overlaps by file name",
    "returns empty for empty input"
   ],
   "orchestra/result-watcher.test.ts": [
    "returns an object with waitForChange and close",
    "attaches an fs.watch listener when tasksDir exists",
    "does not throw when tasks dir does not exist",
    "rejects escaping wake paths before installing any watcher",
    "resolves when a .result file event fires",
    "does NOT resolve for non-.result filenames",
    "resolves after fallback timeout when no event fires",
    "timer is cleared when watcher event fires (no double-resolve)",
    "watcher event is ignored after timer fires",
    "supports multiple sequential waitForChange calls",
    "resolves only for an exact configured control-plane wake file",
    "resolves pending waitForChange immediately on close",
    "also cancels the fallback timer when closed",
    "close() on already-closed watcher does not throw",
    "waitForChange returns immediately when closed",
    "calls fsWatcher.close() when closing",
    "uses timer when watch() throws",
    "uses timer when watch error event fires",
    "close() in fallback mode still resolves pending",
    "handles rapid successive result events without double-resolve",
    "handles null filename gracefully",
    "close while timer already fired does not throw"
   ],
   "orchestra/results-map.test.ts": [
    "should create a Map with O(1) lookup by taskId",
    "should override earlier entries when duplicate taskIds exist (last wins)",
    "should produce Array.from(map.values()) equivalent to original results (backward-compat)",
    "should handle empty results array",
    "should be significantly faster than Array.find for large result sets"
   ],
   "orchestra/resume-paused-task.test.ts": [
    "rebuilds a checkpoint-pending task that is now PAUSED on disk as DISPATCHABLE (not stuck)"
   ],
   "orchestra/retro-behavior-changes.test.ts": [
    "produces an agent-skill-add row for non-empty suggestAdd",
    "produces an agent-skill-remove row for non-empty suggestRemove",
    "returns an empty array gracefully when given no inputs",
    "renders genealogy and retirement future-extension inputs",
    "renders the section header and rows when behaviorChanges is non-empty",
    "omits the section gracefully when behaviorChanges is empty or undefined",
    "reads skillAdaptation rows from .deckent/routing/outcomes/<sprintId>.json",
    "returns an empty array when no outcomes file exists (graceful)",
    "writes the Next Sprint Behavior Changes section into the retro DB entry"
   ],
   "orchestra/retro-debt-count.test.ts": [
    "calculateMetrics counts techDebtTasks from the evaluations Map (Brain-final verdict), matching the sprint-357 archive (5 debt, 0 no-go)",
    "formatHumanRetro renders a \"Tech Debt\" row the CLI reader can parse — was previously absent, always yielding 0",
    "still renders \"Tech Debt | 0\" (not omitted) for an all-clean sprint — a missing row is what caused the silent-fallback bug in the first place"
   ],
   "orchestra/retro-dual-write.test.ts": [
    "writes 3 canonical DB rows (sprint-log-NNN, retro-sprint-NNN, mem-sprint-NNN)",
    "writeRetrospective emits all 3 canonical DB rows in a single call"
   ],
   "orchestra/rollback-decide.test.ts": [
    "setupTaskSnapshot is no longer exported from worker.ts",
    "applyRollbackVerdict is no longer exported from result-evaluator.ts",
    "the git-stash snapshot/verdict primitives are no longer exported from worker-rollback.ts",
    "revertFilesToHead (the one live export, PROMOTE-W1b) still exists and works",
    "reverts a modified tracked file back to HEAD",
    "cleans an untracked file that is not in HEAD",
    "leaves files outside the given path list untouched"
   ],
   "orchestra/rollback.test.ts": [
    "returns true when git status --porcelain is empty",
    "returns false when there are uncommitted changes",
    "returns false when git command fails",
    "returns true when stdout is only whitespace/newlines",
    "returns empty array when working tree is clean",
    "parses dirty file paths correctly",
    "handles staged and unstaged changes",
    "returns empty array when git command fails",
    "returns the SHA from git rev-parse HEAD",
    "returns empty string on failure",
    "returns branch name",
    "returns HEAD on failure",
    "creates a backup branch when working tree is clean",
    "stashes and pops when working tree is dirty",
    "throws when stash pop fails after dirty tree (user-loss guard)",
    "throws when stash fails on dirty tree",
    "throws when getCurrentCommitSha returns empty",
    "force-updates branch if it already exists",
    "throws when branch creation and force both fail",
    "returns SafetyPoint with correct structure",
    "returns success when rollback succeeds",
    "returns failure when safety branch does not exist",
    "returns failure when rev-parse branch fails",
    "returns failure when git reset --hard fails",
    "returns true on successful branch deletion",
    "returns false when branch deletion fails",
    "returns true when branch exists",
    "returns false when branch does not exist",
    "returns \"auto\" when all evaluations are NO_GO",
    "returns \"ask\" when some evaluations are NO_GO",
    "returns \"never\" when all evaluations are DONE",
    "returns \"never\" when all are GO_WITH_TECH_DEBT",
    "returns \"ask\" for mixed DONE + NO_GO",
    "returns \"never\" for empty evaluations",
    "returns \"auto\" for single NO_GO",
    "returns \"never\" for single DONE",
    "writes safety point JSON to .deckent/safety-point.json",
    "creates .deckent directory if it does not exist",
    "does not throw on write failure",
    "returns SafetyPoint when file exists and is valid JSON",
    "returns null when file does not exist",
    "returns null when file contains invalid JSON",
    "returns null when readFileSync throws",
    "deletes both git branch AND JSON file on success",
    "deletes JSON file even when branch deletion fails",
    "calls rmSync with force: true",
    "does not throw when rmSync fails",
    "returns true when git rev-parse --git-dir succeeds",
    "returns false when not a git repository",
    "returns false when no safety point file exists",
    "returns false when safety point belongs to current sprint (live preservation)",
    "cleans orphan when safety point belongs to a different sprint (stale detection)",
    "cleans orphan branch when it still exists",
    "error message includes git stash recovery instructions"
   ],
   "orchestra/routing-journal-wire.test.ts": [
    "planner routing appends a decision file in the project tmpdir",
    "single-task routing journals its ad-hoc decision",
    "carries the strongest route-time domain into routingMeta"
   ],
   "orchestra/routing-skill-applicability-wire-p0.test.ts": [
    "routes TypeScript to TypeScript guidance and records Python as a task-local rejection",
    "routes a new .py write task to Python guidance, independent of root marker coincidence",
    "turns an inapplicable forced skill into a plan HOLD instead of a post-route union"
   ],
   "orchestra/rubric-armor-complete.test.ts": [
    "a rubric TypeError on the extension-hit late-result does not truncate the loop — sibling task still evaluated",
    "extension-hit fault falls back honestly: worker DONE caps at GO_WITH_TECH_DEBT",
    "extension-hit fault with worker NO_GO stays NO_GO (no silent promotion)",
    "surfaces the extension-hit fault as BRAIN→AUDITOR:EVALUATION_FAULT",
    "a rubric TypeError on one fix task does not truncate the FIX loop — sibling fix task still evaluated",
    "FIX-phase fault with worker NO_GO stays NO_GO (no silent promotion)",
    "surfaces the FIX-phase fault as BRAIN→AUDITOR:EVALUATION_FAULT"
   ],
   "orchestra/rubric-consolidation.test.ts": [
    "worker prompt should NOT contain rubricScores spec",
    "TaskResult.rubricScores should be accepted but deprecated",
    "assessQuality() should compute canonical quality dimensions",
    "formatRubricScoresSection should use Quality Assessor dimensions",
    "Quality Assessor dimensions should use canonical field names",
    "old results with rubricScores should still be parseable",
    "assessQuality result should contain skillRelevance for outcome-tracker",
    "assessQuality should work without rubricScores on result",
    "RETRO table should have correct canonical column headers",
    "should compute quality scores for multiple tasks in sprint"
   ],
   "orchestra/rubric-coverage-renorm.test.ts": [
    "renormalizes coverage=0 for a typed direct verification command",
    "coverage:null + all-good produces totalScore ≥ 90 (NOT 78.75) and decision DONE",
    "coverage:null + testsFail + selfAssessment NO_GO drives low score (NO_GO or TECH_DEBT)",
    "coverage=85 numeric uses the existing weighted formula (no renormalization)",
    "passingScore threshold is preserved after renormalization (custom rubric high bar still gates)",
    "coverage:undefined behaves identically to coverage:null (structural absence)"
   ],
   "orchestra/rubric-detail.test.ts": [
    "should render markdown table with correct header and row structure",
    "should calculate sprint overall average correctly",
    "should return empty array when results is undefined",
    "should return empty array when results is empty",
    "should return empty array when no results have matching tasks",
    "should handle result with no matching task in sprint",
    "should correctly filter and include only results with matching tasks",
    "should score NO_GO tasks with 0 correctness",
    "should score GO_WITH_TECH_DEBT with partial correctness"
   ],
   "orchestra/rubric-registry.test.ts": [
    "uses typed verification as the canonical command authority",
    "does not let stale description prose override an explicitly empty typed block",
    "keeps the legacy description adapter for tasks without typed verification",
    "returns true for a single docs/audits/sprint-N/T-N.md write with audits-only scope dir",
    "returns false when filesWrite has more than one entry",
    "returns false when scope.directories includes src/ alongside an audits write",
    "returns false for a non-audits docs path (docs/non-audit.md)",
    "returns false for empty filesWrite",
    "returns false when scope.directories contains tests/ alongside an audits write",
    "returns false when the single filesWrite is not a .md file",
    "returns true for a single docs/smoke/T.md write with docs-only scope dir",
    "returns false for an audits write (excluded from doc-write)",
    "returns true for multiple docs/*.md writes with docs-only scope dir",
    "returns false when scope.directories includes src/orchestra/ alongside doc writes",
    "returns false for empty filesWrite",
    "returns false when at least one filesWrite entry is outside docs/",
    "returns false when any filesWrite entry is under docs/audits/",
    "returns false when scope.directories contains tests/ alongside docs writes",
    "returns true for a top-level CHANGELOG.md with a non-source scope (LP-1)",
    "returns true for a .md in an arbitrary non-source directory (LP-1)",
    "returns true for .txt / .rst doc files (LP-1 doc extensions)",
    "keeps a .md ADJACENT to source classified as code, not doc (LP-1 hasSourceDirectories guard)",
    "returns \"audit\" with priority over doc-write when filesWrite is under docs/audits/",
    "returns \"document-write\" when filesWrite is under docs/smoke/",
    "returns \"code-development\" when filesWrite is under src/",
    "returns \"code-development\" when scope is empty (default fallback)",
    "returns \"code-development\" for a mixed src/+docs/ scope (not pure doc-write)",
    "returns AUDIT_RUBRIC reference for an audit task",
    "returns DOC_WRITE_RUBRIC reference for a doc-write task",
    "returns CODE_RUBRIC reference for a code-development task",
    "returns true for an audit task",
    "returns true for a doc-write task",
    "returns false for a code-development task",
    "honors an owner-declared direct test command for code without coverage output",
    "uses canonical %s authority and makes false test claims N/A",
    "keeps a code test failure required",
    "marks non-measurable language coverage optional rather than N/A",
    "keeps measurable TypeScript coverage required"
   ],
   "orchestra/rule-evolver.test.ts": [
    "returns empty rules with no data",
    "generates activation rule for high-performing agent+intent",
    "generates exclusion rule for low-performing agent+intent",
    "does not generate rules with insufficient data",
    "detects synergy in reasoning",
    "does NOT create an unconditional activation rule for synergistic skill pairs (reasoning only)",
    "does NOT create an unconditional exclusion rule for conflicting skill pairs (reasoning only)",
    "does not create skill-skill rules for agent+skill synergy pairs",
    "emits no synergy rule regardless of sample size (high samples still reasoning-only)",
    "detects conflict in reasoning for failing pairs",
    "generates activation rule for high-performing skill+intent",
    "generates exclusion rule for low-performing skill+intent",
    "isUnconditionalRule: true for empty when, false for a real condition or missing when",
    "evolveSynergyRules emits NO activation/exclusion rules (reasoning only) for skill-pair synergy/conflict"
   ],
   "orchestra/run-diff-service.test.ts": [
    "records HEAD in a git repo and fail-softs (undefined) outside one",
    "diffs the run handle's recorded base against the worktree with honest statuses",
    "pre-N1 record (no gitBase) falls back to HEAD with note:no-base",
    "a non-git project answers note:not-a-git-repo with zero files (never throws)",
    "caps an oversized file block and flags it truncated",
    "derives statuses from headers"
   ],
   "orchestra/run-flow-coordinator.test.ts": [
    "advances two flows on the same coordinator without cross-flow leakage",
    "a freshly-created coordinator instance folds events.jsonl to the same context the pre-restart instance held",
    "resolves a synthetic context from approved-snapshot + run-handle when no events.jsonl exists",
    "a second submission with the same commandId is a typed no-op, not a re-applied command",
    "a corrupted (out-of-order) event log fold throws a typed InvalidTransitionError",
    "a command issued after the flow reaches a terminal state is rejected with InvalidTransitionError",
    "recordCompletion on a flow with an empty event log throws InvalidTransitionError and appends nothing",
    "a child-process closure becomes visible to the daemon's cached getFlow AND re-publishes the delta to onEvent",
    "a command folded after a child closure is rejected typed — never appended after the terminal event",
    "bounds an oversized child-crash error before persistence and keeps the log foldable",
    "persists a within-bound child-crash error unchanged"
   ],
   "orchestra/run-flow-death-sweep.test.ts": [
    "dead pid → durable RUN_FAILED closure with an honest system narrative",
    "alive pid → untouched (never guesses a kill)",
    "pid-less legacy record → reported as unknown-liveness, left untouched",
    "sweep is idempotent: a second pass over a closed flow does nothing",
    "completed flows are never touched (terminal states out of scope)",
    "summarizes 100 skipped entries by class with at most three examples",
    "repairs ADMITTED+handle → RUN_STARTED after a crash between canonical commits",
    "dry-run classifies dead + unverifiable + alive WITHOUT writing any closure",
    "inventories 100 handleless/logless artifacts, then archives only with explicit approval",
    "projects legacy artifact inventory through the existing stale-runs report seam",
    "apply closes a dead pid as FAILED and an unverifiable record as CANCELLED — durably",
    "apply closes a PREPARED-only dead start attempt as CANCELLED — durably",
    "a PREPARED-only live start attempt is typed and never closed",
    "a jobs-terminal flow is NEVER closed — its execution truth already won (skip, not a lie)",
    "a dead pid on a LEGACY record (no event fold) closes as CANCELLED, not a fold-crashing FAILED",
    "a LEGACY dual-read flow (snapshot+handle only, no events.jsonl) closes as CANCELLED",
    "is idempotent and composes with the read-path sweep (shared dead-run closure command)"
   ],
   "orchestra/run-flow-decision-service.test.ts": [
    "approve → APPROVED + the durable snapshot is persisted from the planned sprint",
    "the SAME approve from a second surface converges idempotently (deterministic commandId)",
    "approve without a live preview refuses with NO_LIVE_PREVIEW and appends nothing",
    "approve with a missing planned-sprint record refuses with PLANNED_SPRINT_MISSING (route-preserved behavior)",
    "refuses a legacy intent plan without accepted planner evidence before approval",
    "refuses malformed v2 planning evidence before approval",
    "refuses v5 context downgraded to legacy directives authority before approval",
    "approve refuses a digest-valid v3 structural topology blocker before APPROVAL_GRANTED",
    "reject → CANCELLED with the rejection recorded durably",
    "retires the exact approved task set, projects NOT_DISPATCHED, and replays without duplicates",
    "refuses a drifted task projection before changing an approved flow",
    "start spawns exactly once, remains STARTING until child admission, and refuses an active re-start",
    "start on a non-approved flow refuses with NOT_APPROVED"
   ],
   "orchestra/run-flow-plan-service.test.ts": [
    "normalizes dependencies and scope before the final digest, then persists before events",
    "compiles an intent exactly once and can approve the same persisted snapshot",
    "refuses a raw custom planner result before preview or durable publication",
    "reuses an exact durable plan on idempotent retry without compiling or planning again",
    "requires a fresh plan when a legacy intent record has no accepted receipt binding",
    "holds malformed persisted v2 evidence before reuse or approval",
    "refuses a v5 authority downgrade to legacy directives before reuse",
    "refuses unresolved dependencies before any durable mutation",
    "keeps topology-denied plans unapproved even when an approval actor is supplied",
    "persists an explicit scope HOLD and refuses approval when tracked-file evidence is unavailable",
    "rejects planner-added writes outside a closed allowlist even with force-scope consent",
    "normalizes and accepts an exact closed allowlist backed by tracked evidence",
    "binds an explicitly acknowledged untracked allowlist entry as planned-new intent",
    "rejects an untracked closed-allowlist entry without explicit scope acknowledgement",
    "binds an explicit owner-authorized legacy projection without writing through it",
    "holds adoption when its approval actor differs from the bound owner"
   ],
   "orchestra/run-flow-recovery-adapter.test.ts": [
    "treats DETACHED_RUNNING as live only with exact process authority",
    "holds a raw detached-running claim with foreign process authority",
    "turns exact stale/dead evidence into an approval request, never an automatic replay",
    "requires an exact terminal receipt before proposing finalization"
   ],
   "orchestra/run-flow-reducer-purity.test.ts": [
    "${name}: field-carrying event -> byte-identical state to the field-less equivalent",
    "idempotent APPROVAL_GRANTED replay (matching CAS): same context reference regardless of commandId/sequence",
    "idempotent START_REQUESTED replay (matching CAS, STARTING): same context reference regardless of commandId/sequence",
    "idempotent START_REQUESTED replay (matching CAS, DETACHED_RUNNING): same context reference regardless of commandId/sequence",
    "idempotent RUN_STARTED replay (same jobId): same context reference regardless of commandId/sequence",
    "no context produced across every ordinary + mismatch transition ever carries a `sequence` key",
    "the reducer source file never references `sequence` anywhere (static regression-guard)",
    "APPROVAL_GRANTED CAS-mismatch blockedReason: exact pre-existing format when commandId is absent",
    "APPROVAL_GRANTED CAS-mismatch blockedReason: commandId is echoed when present, prefix unchanged",
    "duplicate APPROVAL_GRANTED mismatch blockedReason: exact pre-existing format when commandId is absent",
    "duplicate APPROVAL_GRANTED mismatch blockedReason: commandId echoed when present",
    "START_REQUESTED CAS-mismatch blockedReason: exact pre-existing format when commandId is absent",
    "START_REQUESTED CAS-mismatch blockedReason: commandId echoed when present, prefix unchanged",
    "duplicate START_REQUESTED mismatch blockedReason: exact pre-existing format when commandId is absent",
    "duplicate START_REQUESTED mismatch blockedReason: commandId echoed when present",
    "thrown RunFlowTransitionError (fail() path): exact pre-existing message format when commandId is absent",
    "thrown RunFlowTransitionError (fail() path): commandId echoed when present, prefix unchanged"
   ],
   "orchestra/run-flow-reducer.test.ts": [
    "drives COLLECTING through COMPLETED, carrying every payload forward",
    "DETACHED_RUNNING can also terminate via RUN_FAILED",
    "DETACHED_RUNNING pauses as resumable BLOCKED rather than FAILED",
    "STARTING can terminate via RUN_FAILED (spawn itself failed)",
    "throws RunFlowTransitionError: ${name}",
    "rejects an event whose schemaVersion is not the current one",
    "rejects an event whose flowId does not match the context flowId",
    "rejects PROPOSAL_SUBMITTED whose proposal.flowId does not match the event envelope flowId",
    "every terminal state (COMPLETED/FAILED/CANCELLED/BLOCKED) rejects every further event",
    "FLOW_ABORTED from ${stage} cancels, and START_REQUESTED never applies afterward",
    "APPROVAL_REJECTED at AWAITING_APPROVAL cancels with reason \"rejected\", and start is unreachable afterward",
    "a duplicate APPROVAL_GRANTED with a matching CAS key is a no-op (same context reference)",
    "a duplicate APPROVAL_GRANTED with a mismatched CAS key is BLOCKED, not silently replayed",
    "a duplicate START_REQUESTED with a matching CAS key is a no-op while STARTING",
    "a duplicate START_REQUESTED with a matching CAS key is a no-op while DETACHED_RUNNING (double-start guard)",
    "a duplicate START_REQUESTED with a mismatched CAS key is BLOCKED, not silently replayed",
    "a duplicate RUN_STARTED with the same jobId while DETACHED_RUNNING is a no-op",
    "APPROVAL_GRANTED targeting a stale revision/digest is BLOCKED, not an error",
    "START_REQUESTED targeting a stale revision/digest against the approved snapshot is BLOCKED",
    "DEFAULT_TERMINAL_CONFIG does not set run_flow_v2 (locked key-shape, config-terminal.test.ts)",
    "createDefaultConfig() resolves run_flow_v2 as falsy (absent) by default",
    "mergeConfigs(null, null) resolves terminal.run_flow_v2 as falsy by default",
    "an explicit project override flips terminal.run_flow_v2 to true (existing deepMerge pass-through, no new wiring)",
    "nothing under src/ imports run-flow-reducer, run-flow-contract, or run-flow-store except their designed consumers"
   ],
   "orchestra/run-job-recovery-adapter.test.ts": [
    "accepts RUNNING as live only when exact process identity verifies it",
    "does not convert a raw running flag or mismatched process into liveness",
    "retains dead evidence as a resume proposal without invoking a provider",
    "does not trust a foreign terminal receipt"
   ],
   "orchestra/run-job-service.test.ts": [
    "never imports a planning entrypoint (sprint-controller/sprint-phases/brain)",
    "never imports the cli/repl run-flow-store (ADR-D-004 C2 — orchestra must not import cli/)",
    "throws RunJobFlowNotApprovedError when no snapshot was ever approved",
    "throws RunJobDigestMismatchError when expected revision/digest does not CAS-match the approved snapshot",
    "throws RunJobDigestMismatchError on a revision mismatch even when the digest string matches",
    "refuses every budget HOLD before spawnStart and reports held tasks deterministically",
    "derives v2 HOLD from the canonical projection even when raw task budgetPolicy is absent",
    "keeps explicit local-exempt and legacy-v1 tasks startable",
    "refuses a digest-valid v3 snapshot with an undeclared writer collision before spawnStart",
    "returns the exact approved Sprint without executing a start side effect"
   ],
   "orchestra/run-policy-delivery.test.ts": [
    "resolves the Execution Contract section into a digest-bound authority (wrapped bullets joined)",
    "does not silently drop a declared policy because of heading case: %s",
    "returns undefined when no DIRECTIVES or no contract section exists (explicit absence)",
    "fail-closed: a declared section without bullets can never silently resolve empty",
    "renders the digest-bound block from the task itself and instructs the digest echo",
    "renders NO policy block when the task carries no authority (explicit absence, no silent empty)",
    "provider-parity: Codex/Fable/Qwen-shaped tasks compile a byte-identical policy block",
    "FIX inheritance: an attempt carrying the parent snapshot compiles the identical block",
    "appends the best-effort run-policy compile observation to the existing execution-authority jsonl (fail-soft surface — MASTER 9024; enforcement is the settlement parity chain)",
    "D-1 verification fast-path + missing digest echo => NO_GO through the real evaluateWithRubric",
    "D-1 verification fast-path + digest mismatch => NO_GO",
    "D-1 verification fast-path + exact digest echo => DONE (fast-path preserved)",
    "policy-free verification task keeps its historical fast-path DONE (no regression)",
    "reconstruction + missing digest echo => never DONE",
    "reconstruction + exact digest echo => can settle DONE (gate is parity, not a blanket veto)",
    "backlog terminal producer routes through the same gate (policy-free entries pass through)",
    "DONE claim + missing evidence => vetoed to NO_GO at the convergence point",
    "DONE claim + digest mismatch => vetoed to NO_GO",
    "GO_WITH_TECH_DEBT claim + missing evidence => vetoed (a policy violation cannot settle as debt)",
    "DONE claim + exact evidence => DONE preserved",
    "tampered plan snapshot => vetoed even with a matching echo",
    "policy-free task => historical DONE byte-preserved",
    "FIX attempt with inherited policy: exact evidence => DONE, missing => NO_GO",
    "passes a policy-free task through untouched",
    "downgrades DONE to typed NO_GO when the digest echo is missing",
    "downgrades DONE to typed NO_GO on digest mismatch",
    "keeps DONE and records a passing parity score on exact match"
   ],
   "orchestra/run-policy-plan-persistence.test.ts": [
    "real planSprint (structured) persists every task JSON with the digest-bound snapshot",
    "a run without an Execution Contract persists tasks with NO runPolicy field (explicit absence)"
   ],
   "orchestra/run-proposal-compiler-delimiters.test.ts": [
    "does not throw for a \"${name}\" goal and intent.goal round-trips it verbatim",
    "does not throw for a \"${name}\" goal and directivesMarkdown contains it verbatim",
    "a delimiter-free goal is completely unaffected (byte-for-byte, no regression)",
    "trims surrounding whitespace exactly like the pre-existing (non-corpus) behavior",
    "a goal quoting a \"## Task N:\"-style line no longer hard-errors (assertNoHeadingCollision guard)",
    "a goal quoting a \"### goNogo\"-style line no longer hard-errors (assertNoHeadingCollision guard)"
   ],
   "orchestra/run-proposal-compiler-sanitization.test.ts": [
    "throws RunProposalPlanError naming the offending task\\'s original title, not the valid sibling\\'s",
    "treats a whitespace-only filesWrite entry as empty (no blank-string escape hatch)",
    "compileRunProposal (markdown entrypoint) surfaces the same typed error for an empty-filesWrite task",
    "compiles successfully and canonicalizes the title + dependency ref to the IDENTICAL sanitized string",
    "round-trips the sanitized title and dependency reference through the REAL DIRECTIVES parser"
   ],
   "orchestra/run-proposal-compiler.test.ts": [
    "preserves exact verification through the real planner parser and directive compiler",
    "rejects malformed planner verification %j",
    "compiles a clean single-task proposal into a well-formed DirectiveBuildIntent",
    "compiles straight to DIRECTIVES markdown that the real parser reads back correctly",
    "carries exact planner-result and directives digests only for an explicit receipt envelope",
    "does not manufacture planner authority for the legacy raw injectable seam",
    "rejects a malformed injected receipt envelope with a typed planner failure",
    "does not turn the planner/config model into a directive forceModel override",
    "preserves an explicit owner forceModel through directives and parsing",
    "canonicalizes planner structured items and gives legacy strings typed generic items"
   ],
   "orchestra/run-proposal-planner.test.ts": [
    "maps a real multi-task PlannerResult into a real multi-task DirectiveBuildIntent",
    "U1-G2: traceability travels in the meta field — NEVER in the description (reason stays)",
    "compileRunProposal round-trips the fake-planner plan through the UNCHANGED directives-builder",
    "throws RunProposalPlanError (not a scaffold) when the injected planner returns zero tasks",
    "throws RunProposalPlanError (not a scaffold) when the injected planner itself throws",
    "compileRunProposal (markdown entrypoint) surfaces the same typed error, not partial markdown",
    "with NO planner injected, and no provider registered in this hermetic test process, ",
    "uses grouped Brain provider when no effective flat projection is supplied",
    "preserves an effective flat Brain provider over the retained grouped authoring value",
    "passes the balanced-mode canonical 'claude-sonnet-5' when no config is given",
    "passes 'claude-sonnet-5' for an economic-mode config",
    "passes 'claude-opus-4-8' for a performance-mode config",
    "keeps the Brain invocation model separate from the configured Worker task-model policy",
    "F-1: grounds the planner with the REAL tracked file tree (no more blind planning)",
    "states EVERY task.scope.filesWrite must contain at least one file path",
    "states a task title MUST NOT contain a comma character"
   ],
   "orchestra/run-proposal-planning-admission.test.ts": [
    "proves effective Docker custody before invoking the external planner",
    "surfaces a typed non-retryable HOLD and spends zero provider calls when custody is unsafe",
    "transports bounded recovery identities without retrying or invoking the planner",
    "preserves a secret-safe typed planner terminal reason and receipt through compilation"
   ],
   "orchestra/runner-death-record.test.ts": [
    "reuses the crashes-directory line format and adds the typed runner fields",
    "redacts secrets carried by the dying error",
    "writes into .deckent/crashes and is idempotent under skipIfExists",
    "never throws when the crashes directory cannot be created",
    "releases the PID file this exact process owns",
    "never deletes a PID file owned by another process",
    "never deletes a same-PID record whose start token proves a recycled PID",
    "is a no-op when there is no PID directory at all",
    "publishes a typed death record for a stale PID file with no live process",
    "publishes exactly once — a second startup against the same stale PID file is silent",
    "types a recycled PID as a death — the recorded runner is provably gone",
    "never claims a death for a live, provably-owned runner",
    "never claims a death when liveness is real but ownership is unprovable",
    "ignores this process\\'s own live PID file",
    "uncaughtException: records the death WITHOUT swallowing the original error",
    "unhandledRejection: records the death alongside error.json",
    "%s: writes TERMINATED status, a typed record, and exits %i",
    "exit: a non-zero exit nobody typed still gets a record, and the owned PID file is released",
    "exit: does not duplicate a record a specific handler already wrote",
    "exit(0): normal COMPLETE settlement writes no crash record but still releases the PID file",
    "without a projectRoot the legacy IPC-only behaviour is preserved"
   ],
   "orchestra/runsprint-debt-integration.test.ts": [
    "resolves debt in DB when task evaluates to DONE",
    "writes resolved status with correct sprintId for DONE evaluation",
    "resolves debt in DB when task evaluates to GO_WITH_TECH_DEBT",
    "writes resolved status with correct sprintId for GO_WITH_TECH_DEBT evaluation",
    "does not resolve origin debt from worker-claimed fix lineage alone",
    "PLAN resolves a verified-no-result CRITICAL debt instead of leaving it active",
    "does NOT resolve debt when task evaluates to NO_GO",
    "does NOT write resolved status for NO_GO evaluation",
    "does NOT resolve debt when task result is missing (timeout/synthetic NO_GO)",
    "uses the sprint ID from the current sprint, not a hardcoded value",
    "sprint completes successfully after debt resolution",
    "sprint completes successfully when debt resolution finds no matching entry",
    "resolveDebt only resolves the target debt, not others"
   ],
   "orchestra/runtime-budget-monitor.test.ts": [
    "persists an immutable exact-delta chain and marks repeated cache-read observations",
    "continues the observation chain after exact-attempt restart without duplicating replay",
    "recovers exact counters and dedupe state when the observation journal wins the crash window",
    "rejects a corrupt or non-contiguous observation chain",
    "binds usage and landing evidence to the host settlement attempt id",
    "persists landing-requested evidence without invoking hard containment",
    "rejects a corrupt durable landing-request marker",
    "uses the landed reserve for continuation without requesting a second soft landing",
    "does not apply parent counters twice to a checkpoint-subtracted continuation budget",
    "restores counters when the same continuation attempt restarts",
    "persists evidence before exactly-once stop and vetoes a DONE result",
    "accepts only the explicit host budget and ignores worker-writable Task JSON",
    "ignores worker-writable forged evidence under .tasks",
    "persists a terminal within-budget usage summary on settle",
    "keeps partial usage terminally unmeasurable after observer loss",
    "restores non-terminal counters and dedupe keys after coordinator restart",
    "blocks a same-budget retry after a terminal exceeded attempt",
    "uses terminal exceeded usage when the stop marker is missing",
    "projects exact later terminal counters without mutating stop-marker truth"
   ],
   "orchestra/runtime-extension.test.ts": [
    "grants an extension when enabled + heartbeat fresh + counter below cap",
    "denies extension when runtime_extension_enabled=false",
    "accepts a single-write heartbeat without requiring refresh writes",
    "denies extension when no heartbeat file exists",
    "denies extension once the per-task cap of 3 is exhausted",
    "rejects extension when config is undefined (legacy callers)",
    "counts up to the cap across successive calls (idempotent state)",
    "treats malformed heartbeat JSON as invalid (no false positive)",
    "hard cap is 3 (matches DIRECTIVES Task 191-002)",
    "per-extension budget is 5 minutes",
    "freshness threshold is 90 seconds"
   ],
   "orchestra/runtime-hygiene-finalizer.test.ts": [
    "is default-off and never invokes destructive hygiene without both opt-ins",
    "never runs before terminal receipt eligibility and archive verification",
    "retains resolved windows and writes configurable output below the configured archive root",
    "surfaces partial receipts and thrown failures as typed HOLD evidence"
   ],
   "orchestra/rvdc-deadbranch.test.ts": [
    "a stale non-v2 routing_engine value behaves IDENTICALLY to v2 (no branch left to special-case it)",
    "v2-default flow (no config) records each task outcome to learnings.json exactly once"
   ],
   "orchestra/scheduler-cascade-composition.test.ts": [
    "cascade-skipped descendants mint zero -fix/-xfix; a genuine sibling NO_GO still mints both",
    "re-deciding + re-executing the same tick, then re-running debt-manager gates, is a total no-op",
    "restore-decided cascade skips mint zero -fix/-xfix, healthy co-dependency never cross-blamed",
    "a second restore call is a no-op; re-running debt-manager gates mints nothing new"
   ],
   "orchestra/scheduler-collision-reorder-wire.test.ts": [
    "suppresses original/FIX and FIX/FIX lineage self-collisions",
    "serializes real competing writers without spending the available slot on the blocked task",
    "automatically releases the next writer after its exact predecessor becomes satisfying",
    "releases only a synthetic collision after a NO_GO predecessor is collected",
    "keeps recovery after project leadership and before checkpoint interpretation",
    "propagates checkpoint execution-authority HOLD instead of falling through to PLAN",
    "awaits the backend recovery authority exactly once",
    "propagates recovery HOLD/failure instead of continuing to checkpoint restore",
    "turns a returned exact recovery HOLD into a typed startup failure",
    "is a no-op for backends without a durable attempt journal"
   ],
   "orchestra/scheduler-continuous-quiescence.test.ts": [
    "terminalizes a deterministic PENDING/PAUSED dead-dependency chain instead of idling",
    "preserves a recoverable paused descendant while repair is enabled",
    "does not declare quiescence while live work remains",
    "does not treat an ordinary approval/operator pause as terminal",
    "does not apply the quiescence terminalizer to legacy FIFO"
   ],
   "orchestra/scheduler-continuous-terminal-wire.test.ts": [
    "persists the receipt, transitions the PAUSED task, then checkpoints once; a duplicate tick is a no-op",
    "does not terminalize or checkpoint when receipt persistence fails"
   ],
   "orchestra/scheduler-driver-composition.test.ts": [
    "is \"legacy\" for every shape except an explicit {engine:\"reducer\"}",
    "calls runLegacyTick exactly once per tick, in call order, for both triggers — never touches the executor",
    "does not invoke the legacy initial tick while the live cost gate is on HOLD",
    "is selected for every engine value other than the literal \"reducer\" (config absent, {}, and \"reducer\" typo)",
    "never calls runLegacyTick; spawns the reducer-chosen tasks via the injected backend",
    "executes KillWorker before its paired SpawnTask, in orderedEffects order (legacy-fifo queue drain)",
    "falls back to runLegacyTick when engine is \"reducer\" but no config is supplied (defense-in-depth)",
    "agrees with planDispatch on the spawned-id set for the shadow-equivalence \"empty divergence\" fixture",
    "legacy engine (config.scheduler absent — default): dispatchReadyTasks spawns through the injected canonical executor",
    "reducer engine (config.scheduler.engine=\"reducer\"): the injected driver spawns through the same canonical executor",
    "does not touch the live dogfood default: a config with no scheduler block at all behaves exactly like the explicit legacy case",
    "timeout without provider usage evidence HOLDs the initial legacy dispatch",
    "shadow_reducer and engine are read from two separate config keys — flipping one never implies the other"
   ],
   "orchestra/scheduler-effective-dependencies.test.ts": [
    "${label} -> satisfying=${expectedSatisfying} terminal=${expectedTerminal} classification=${classification}",
    "a task with no retryAfter is always retry-eligible",
    "retryAfter in the future is NOT retry-eligible",
    "retryAfter exactly equal to nowMs IS retry-eligible (<=, not <)",
    "retryAfter in the past IS retry-eligible",
    "projects a DONE FIX-of-FIX onto the logical root without a contradictory terminal failure",
    "selectEligibleForSpawn: NO_GO original + DONE <id>-fix -> dependent now eligible",
    "NO_GO original + DONE <id>-fix -> dependent is now spawned end-to-end",
    "does not respawn a %s task after its durable NOT_DISPATCHED round is spent"
   ],
   "orchestra/scheduler-effects-cascade.test.ts": [
    "persists a cascadeSkipped NO_GO result, flips task status, persists task json, and reports the id",
    "is a no-op (logged, no throw) when the CascadeSkip taskId is not in taskMap",
    "writes the .result.tmp file strictly before persisting task-<id>.json (persist-before-commit order)",
    "does NOT flip task status or persist task json when the .result write fails (legacy-bug fix)",
    "is idempotent on replay — a second identical effect does not rewrite the result or re-count the id",
    "finishes an interrupted commit (result already on disk, task still PENDING) without rewriting the result",
    "invokes the injected writeCheckpoint dep with the effect reason",
    "is a no-op when writeCheckpoint is omitted (documented default for un-wired callers)",
    "swallows a throwing writeCheckpoint dep and still processes the rest of the tick",
    "spawns a SpawnTask effect through the mock backend and persists task-<id>.json as EXECUTING",
    "settles a deterministic attribution admission failure once instead of retrying forever",
    "keeps an unclassified spawn exception retryable and does not forge a settlement",
    "retains an admitted backend failure without retry or IPC fallback",
    "bounds unknown host retries with durable backoff and terminal HOLD",
    "kills a worker via the injected killWorker dep for a KillWorker effect",
    "runs SpawnTask, KillWorker, CascadeSkip, and WriteCheckpoint in the same tick without cross-interference"
   ],
   "orchestra/scheduler-effects.test.ts": [
    "returns typed no-mint without resolving a prompt or dispatching",
    "removes the entry so no consumer can read it back as an authority hold",
    "skips a foreign terminal task whose daemon container is absent",
    "throws byte-identically for a hold on the current run task",
    "throws when the owning run of the held task cannot be proven",
    "throws when the owning run is listed but not durably terminal",
    "throws for a hold on a later sprint than the run being reconciled",
    "throws when the daemon container state is %s",
    "throws when no predicate is injected (legacy call sites unchanged)",
    "never skips in resume mode",
    "keeps the throw when the backend still inventories a live worker",
    "admits only an earlier sprint than the current run",
    "treats a zero-padded current sprint id as the same sprint",
    "fails closed on an unprovable current run or task origin",
    "still requires a durably terminal owning run",
    "completes containment when the archived current-run attempt inventories absent",
    "still throws EXACT_CONTAINMENT_INCOMPLETE when the archived attempt inventories unknown",
    "never retires an archived attempt whose container is present, even from an earlier run",
    "never retires an archived attempt whose container state is unknown"
   ],
   "orchestra/scheduler-executed-engine.test.ts": [
    "logs \"scheduler engine: legacy\" exactly once at driver construction, never again per tick",
    "logs \"scheduler engine: reducer\" exactly once when engine=\"reducer\" with a real config",
    "is honest about the defense-in-depth fallback: engine=\"reducer\" but no config logs \"legacy\", not a false \"reducer\" claim",
    "round-trips executedEngine=\"reducer\" onto the journal record when supplied",
    "dual-read: an old-style 4-arg call (no executedEngine) still produces a valid record with the field absent, not defaulted to \"legacy\"",
    "does not report a decided but skipped reducer spawn as an executed spawn",
    "buckets legacy/reducer/unknown correctly, treating a missing field as unknown (dual-read), never legacy",
    "is all-zero (including executedEngineCounts) for empty input"
   ],
   "orchestra/scheduler-fifo-composition.test.ts": [
    "blocked head stays queued+unspawned, the next eligible entry is Kill+Spawn-paired, and a cascade-dependent of a NO_GO root is persisted NO_GO on real disk",
    "blocked head stays queued+unspawned, the next eligible queue entry is spawned (no kill), and a cascade-dependent is persisted NO_GO on real disk",
    "legacy-fifo: FIFO-dep-hole stays a MARKED, pinned divergence — legacy still spawns the blocked head; the reducer\\'s real executor never does, and spawns the next eligible entry instead",
    "continuous: no divergence — both engines agree on the spawn set; the reducer\\'s real executor additionally persists the cascade-dependent as NO_GO on disk"
   ],
   "orchestra/scheduler-fifo-dependency-safety.test.ts": [
    "a dependency-pending head stays queued in place; the next eligible entry is spawned instead",
    "two completions in one tick both find the next eligible entry past the blocked head — no stall",
    "queue exhausted of eligible entries: no forced kill/spawn (\"no kill when no work\") — not a stall, a clean no-op",
    "once the blocking dependency resolves (DONE) on a later tick, the same head becomes eligible — the earlier block was not a permanent stall",
    "Step-1 queue-drain: blocked head is left in place (index-scan), next eligible entry fills the slot",
    "Step-2 pending-slot-fill does not re-evaluate (or double-block) a head already classified in Step-1",
    "legacy-fifo: a NO_GO-dependency head does not block a same-tick eligible successor (cascade removes it from the queue, not a destructive shift)",
    "the same terminally-blocked snapshot fed repeatedly never spawns the dependent, across N ticks",
    "flipping dependencyPipelineEnabled true -> false between ticks does not reopen a closed dependency bypass",
    "re-appending an already cascade-skipped id back onto a later orderedQueue (hypothetical executor replay bug) still never spawns it, in both strategies",
    "exactly one KillWorker+SpawnTask pair per eligible completedTaskId, in input order",
    "a completedTaskId beyond the number of eligible entries produces no extra pair (no forced completion)"
   ],
   "orchestra/scheduler-reducer-cascade.test.ts": [
    "NO_GO root produces a typed CascadeSkip effect + cascade-skip disposition for its PENDING dependent",
    "MANUAL_REVIEW_REQUIRED root ALSO cascades (not only NO_GO — born-610 semantics)",
    "parks repairable dependency failures for FIX instead of terminal cascade",
    "a healthy (DONE) upstream never produces a CascadeSkip for its dependent",
    "a 3-hop chain rooted in NO_GO is fully skipped in one tick, each id exactly once",
    "a re-converging diamond (two branches from one NO_GO root) skips every PENDING descendant exactly once",
    "the same snapshot fed twice yields byte-identical decisions, including the same idempotencyKey",
    "a descendant already terminal+collected (prior tick already persisted its cascade-skip) produces NO new CascadeSkip",
    "the re-decided idempotencyKey after restore is identical to what the original (pre-crash) tick would have produced",
    "both trigger kinds already live (initial/watcher) process an identical cascade-skip set"
   ],
   "orchestra/scheduler-reducer.test.ts": [
    "emits one typed NO_MINT and cascade-skips the dependent without spawning repair"
   ],
   "orchestra/scheduler-shadow-coverage.test.ts": [
    "equivalence case: an unblocked queue head is drained identically by legacy(observed) and reducer",
    "EXPECTED divergence — FIFO-dep-deliği class: legacy(observed) spawns a dependency-blocked head; reducer marks it Blocked",
    "equivalence case: a NO_GO-upstream dependent is cascade-skipped identically by legacy(observed) and reducer",
    "tick \"144\": observation-window artifact reproduced — reducer correctly spawns B early; legacy(observed) trails by one tick (EXPECTED, NOT a reducer bug)",
    "tick \"145\": the async mutation has landed — both engines agree (no divergence), pinning §4.1\\'s \"reducer-haklı\" verdict",
    "tallies trigger/mode/spawn/cascade-skip/divergence coverage across all 5 scope-gap-closing ticks above",
    "is a pure tally with no fs/live-path involvement — empty input yields an all-zero summary"
   ],
   "orchestra/scheduler-shadow-equivalence.test.ts": [
    "scheduler-reducer.ts source imports no fs/env and calls no Date.now()",
    "is deterministic — the same snapshot object yields byte-identical decisions across repeated calls",
    "continuous mode: dep-satisfied + dep-blocked + no-dep PENDING tasks agree on spawn set",
    "continuous mode: slot budget caps both identically",
    "legacy-fifo mode: an UNBLOCKED queue head is drained identically by both",
    "cascade-skip: a PENDING dependent of a NO_GO upstream is skipped, never spawned, by both engines\\' overall effect",
    "FIFO dep-hole: legacy-fifo spawns a dependency-blocked queue head; the reducer marks it Blocked instead",
    "orderedQueue is a different array reference than the live remainingQueue, and is never mutated by later live changes",
    "is false for every shape except an explicit {shadow_reducer: true}",
    "when disabled, the caller never calls the driver at all -> no journal file appears",
    "when enabled, a journal record IS appended (proves the flag actually wires through)",
    "never throws even when the journal directory path is unwritable (a file sits where a dir is expected)"
   ],
   "orchestra/scheduler-shadow-retention-finalize.test.ts": [
    "archives a stale journal and keeps a fresh one during a real finalizeSprint() call (default 14d)",
    "a 10-day-old file (kept under default 14d) is archived once .deckent/config.json sets retention_days=7",
    "SprintMetrics return value, Step 13 job summary, orphan-task archive, and stale-handoff prune all still run alongside Step 12f"
   ],
   "orchestra/scheduler-single-truth.test.ts": [
    "dependent of an MRR upstream is NOT spawned (unverified partial work is not a foundation)",
    "DONE upstream still satisfies (regression — the only satisfying status)",
    "isTaskDispatched treats MRR as settled (the 4th divergence — EVALUATE boundary)",
    "forceKillLiveWorkers sweeps every in-progress task through the kill seam (best-effort)",
    "composition pin: the --force branch sweeps workers BEFORE finalizeSprint can stamp COMPLETE",
    "handleEvaluation: cascade-skipped NO_GO icin fix-task ACILMAZ",
    "handleEvaluation: NORMAL NO_GO fix-task acar (muafiyet asiri-genis degil)",
    "handleCrossDependencies: cascade-skipped NO_GO, DONE-bagimliligina xfix ATMAZ"
   ],
   "orchestra/scheduler-spawn-executor.test.ts": [
    "refuses a closed dispatch window before prompt, provider or task mutation",
    "holds before prompt construction or backend dispatch and preserves explicit attendance",
    "binds final-only adapter admission to Docker rather than the ordinary host adapter",
    "preserves the existing executor behavior when provider authority is not configured",
    "routes an adapter provider to its host adapter even when config resolved Docker",
    "keeps an explicit task-level Docker override on the exact route",
    "snapshots every exact non-terminal entry as typed HOLD instead of dropping to legacy",
    "CAS-fences exact terminal replay instead of overwriting attempt authority",
    "routes exact-attempt IPC authority through the owning backend and preserves zero-work truth",
    "rehydrates durable exact NOT_DISPATCHED and scans one project owner across backend instances",
    "folds an out-of-order durable zero-work generation chain to its latest predecessor",
    "holds a recovered generation chain with a missing predecessor generation",
    "preserves and freshly revalidates a current terminal across accepted recovery ownership transfer",
    "keeps public task/receipt absent through dispatch including %s",
    "keeps a task-level legacy route readable without treating it as exact pending work",
    "fails closed instead of rendering mismatched or suspicious capture diagnostics",
    "preserves a verified capture diagnostic when a released entry is cold-rehydrated",
    "re-reads held release evidence and rejects foreign or no-longer-current evidence without inventing a result",
    "rehydrates cold release HOLD query, preserves contradictions and enforces containment",
    "excludes only proven terminal history from this run while preserving containment checks",
    "turns missing exact dependency authority into a durable registry HOLD",
    "holds before prompt construction and backend dispatch for every trigger path",
    "inherits forceModel/provider/modelEffort from the original when the fix-task left them unset (behavior a)",
    "preserves an explicit fix-task override instead of clobbering it with the original value",
    "inherits the backend field; when the inherited value matches config.spawn_backend the injected backend is reused (no real backend-factory spawn)",
    "persists task-<id>.json with status EXECUTING and the inherited fields after a successful spawn (behavior b)",
    "publishes one canonical TASK_ASSIGN event for a dynamically dispatched task",
    "returns routing-lineage-missing and blocks the spawn when the original task file cannot be read",
    "returns routing-lineage-missing when the original task file is corrupt JSON",
    "is a no-op for a non-fix task (no lineage lookup, spawns normally)",
    "preserves final-only Codex containment on dependency-triggered Docker dispatch",
    "blocks a continuation Docker dispatch when the task-stamped grant is missing",
    "blocks queued/respawn dispatch when attended authority is only a raw reference",
    "resolves identical forceModel/provider/modelEffort for the same fix-task fixture whether invoked with local-path-shaped or heavyweight-respawn-shaped deps",
    "spawns the queued fix-task with inherited routing fields and persists task-<id>.json (behavior a + b, live wiring)",
    "propagates one provider-authority HOLD instead of retrying a queued task",
    "spawns a dependency-ready fix-task with inherited routing fields and persists task-<id>.json (behavior a + b, live wiring)"
   ],
   "orchestra/scope-collision-blocker.test.ts": [
    "returns block decision on overlap",
    "reason mentions colliding file(s)",
    "joins multiple colliding files in reason",
    "returns SpawnDecision with action union type",
    "ScopeCollisionPayload type accepts plan-time and spawn-time"
   ],
   "orchestra/scope-deriver.test.ts": [
    "infers mirror test path for src/core/X.ts",
    "infers test paths for multiple source files",
    "infers tests/scripts/X.test.ts for scripts/X.mjs",
    "returns no test paths for docs/*.md files",
    "returns all derived candidates regardless of existing scope (caller handles idempotency)",
    "infers tests/X.test.ts for top-level src/X.ts",
    "includes edge and split variants for src/ files",
    "handles scripts/*.ts the same as scripts/*.mjs",
    "returns empty for empty filesWrite",
    "populates routingMeta.scopeDerivation when src/ files in scope",
    "does not set routingMeta.scopeDerivation when no derivable files (docs only)"
   ],
   "orchestra/scope-parser-phantom.test.ts": [
    "(a) a multi-dot root basename does not donate a phantom tail token",
    "(b) slash-qualified multi-dot paths survive render instead of collapsing to bare tails",
    "(c) the Scope label distinguishes files from directories instead of appending a slash",
    "(d) a root dotfile survives render-time sanitization without a tracked-file vouch",
    "(e) a bare test-file tail never becomes a root-level phantom test path",
    "(f) a nested tests/docs path never donates a shorter docs/ write target",
    "one sanitize authority: re-sanitizing the plan-time result never re-narrows it",
    "still rejects a path the operator never granted (no enforcement loosening)",
    "today's single-dot slash-qualified paths stay byte-identical"
   ],
   "orchestra/scope-sanitizer-multidot.test.ts": [
    "preserves a bare multi-dot basename (soul.default.md) with no warning",
    "preserves a bare triple-dot basename (a.b.c.ts) with no warning",
    "directory-prefixed variant is unaffected (no regression — Rule 5 never applied)",
    "mixed batch: multi-dot bare + directory-qualified + genuinely-unqualified single-dot",
    "existing genuine-drop behavior preserved: single-dot bare SOURCE names still drop",
    "trackedRootFiles + multi-dot preservation compose without interference",
    "GLOBAL_PROTECTED still wins even for a multi-dot-shaped protected name",
    "true for genuine compound basenames",
    "false for single-extension bare names (still genuinely unqualified)",
    "false for directory-qualified paths (Rule 5 precondition — has a separator)",
    "false for a single word with no dots",
    "false when the trailing segment is not a real alpha-led extension"
   ],
   "orchestra/scope-sanitizer-v2.test.ts": [
    "rejects placeholder paths like src/foo.ts but keeps real paths",
    "rejects JS property access patterns like .directories and .some",
    "filters paths inside code blocks in DIRECTIVES",
    "rejects shallow example.ts but preserves deeply qualified targets",
    "preserves real scope paths like src/core/config.ts",
    "Sprint 148 T-002 replay: filters .directories, .some, src/foo.ts, tests/foo.test.ts",
    "preserves composite names like src/foo-bar.ts (not a placeholder)",
    "maskCodeBlocks replaces code block content with newlines",
    "rejects foo, bar, baz, qux, test as base names",
    "preserves deeply qualified conventional filenames",
    "preserves real filenames that are not placeholders",
    "397-011 replay: preserves README.md and README-TR.md when tracked",
    "397-012 replay: preserves .secrets-baseline when tracked",
    "preserves DIRECTIVES.md when tracked",
    "still drops an unqualified name that is NOT in trackedRootFiles",
    "GLOBAL_PROTECTED still wins even when the file is also in trackedRootFiles",
    "F-1: a lowercase readme.md is preserved too (well-known root file, case-insensitive)",
    "backward-compat: omitting trackedRootFiles ≡ empty set (identical results)",
    "preserves untracked root doc files by bare name (README-TR.md, LICENSE, CHANGELOG.md)",
    "a genuinely unqualified source file still warns + drops",
    "GLOBAL_PROTECTED still wins — bare package.json is dropped, never preserved",
    "a directory-qualified readme path is untouched by the carve-out (normal Rule-5 bypass)",
    "Windows backslash path behavior unchanged regardless of trackedRootFiles",
    "preserves exact root manifests without weakening their write protection",
    "rejects cross-platform absolute, traversal, glob, control and directory-shaped reads",
    "rejects Windows absolute write paths on non-Windows hosts too",
    "detects JS-like access patterns",
    "does not flag real paths"
   ],
   "orchestra/scope-sanitizer.test.ts": [
    "removes dist/ prefix paths",
    "removes extension-only paths like .ts",
    "removes unqualified filenames with warning",
    "F-1: a bare well-known root file (README.md) is preserved even WITHOUT trackedRootFiles",
    "removes global protected files like config.json",
    "deduplicates case-insensitive paths",
    "strips (yeni) suffix and deduplicates",
    "rejects path traversal with ..",
    "rejects absolute paths",
    "leaves normal scope unchanged",
    "integration: task-builder scope output is sanitized"
   ],
   "orchestra/scope-satisfiability.test.ts": [
    "blocks and deduplicates private evidence paths absent from declared reads",
    "checks absence assertions without requiring the granted path to exist",
    "accepts exact reads, writes and directory grants without inventing write demands",
    "ignores NO-GO examples, URI/reference and code tokens",
    "uses canonical file values directly, without guessing paths from typed assertions or commands",
    "BLOCKs a goCriteria-mentioned path outside filesWrite/directories",
    "does not BLOCK when the mentioned path is covered by directories",
    "does not BLOCK when the mentioned path is in filesWrite",
    "does not BLOCK a read-only goCriteria assertion with exact filesRead authority",
    "still BLOCKs mutation language when the path has read-only authority",
    "root-file OR-clause: a bare root filename mention resolves via trackedFiles",
    "root-file OR-clause does not false-positive on a non-tracked bare word+dot token",
    "root-file OR-clause requires the trackedFiles entry to be root-level (no slash)",
    "WARNs when a positive verb sits next to an unauthorized path mention",
    "does not WARN when no verb is adjacent to the mention",
    "negation guard suppresses an otherwise-valid WARN in the same sentence",
    "positive control: the same sentence minus the negation DOES fire",
    "negation guard suppresses even with English negation lemmas (don't modify)",
    "BLOCKs a proof-command path that is neither tracked, writable, nor scoped",
    "does not BLOCK a proof-command path that is already tracked",
    "does not BLOCK a proof-command path the task is itself creating (filesWrite)",
    "does not BLOCK a proof-command path covered by directories",
    "skips glob-wildcard proof-command arguments (no fuzzy resolution)",
    "WARNs when a file declared unchanged is also present in filesWrite",
    "does not WARN when the declared-unchanged file is not in filesWrite",
    "recognizes the English \"must remain\" declaration form",
    "does not conflate an unrelated file mentioned earlier in the same clause",
    "produces zero findings for a fully self-consistent task",
    "a \"path + verb + DOKUNMA\" sentence (399-004 own style) produces no finding",
    "the literal bare \"planner.ts:973 — ona DOKUNMA\" phrasing produces no finding either",
    "task-007 (T7-ELOOP): the typo test-path is self-consistently declared in its own filesWrite",
    "task-011 (T11-DOCS-SAYILAR): catches the unlisted scripts/validate-publish.mjs rewrite",
    "task-011 (T11-DOCS-SAYILAR): catches ci-baseline-detect.test declared unchanged ∩ filesWrite",
    "task-011: README.md itself is correctly NOT flagged (it is legitimately in filesWrite)",
    "task-012 (T12-BASELINES): tracked run-targets inside backticked commands are EXEMPT from 1a",
    "397-007 window: tracked run-target whose sentence implies CHANGING it → WARN",
    "an UNTRACKED path inside a backticked run command still blocks (via rule 2, not 1a)",
    "task-012: .secrets-baseline (legitimately in filesWrite) produces no finding",
    "a goCriteria mention of a .analysis/ path in filesWrite produces no finding",
    "a .deckent/ mention outside write authority still BLOCKs (with its dot intact)",
    "the greedy-shorthand class stays dead: \"agents.md/cli.md\" never tokenizes as one path"
   ],
   "orchestra/scope-w1b.test.ts": [
    "flag-on + SCOPE_INSUFFICIENT event → task.scope.filesWrite expanded",
    "flag-on + SCOPE_INSUFFICIENT event → disk task JSON updated with expanded scope",
    "flag-on + SCOPE_INSUFFICIENT event → emits BRAIN→AUDITOR:SCOPE_AUTO_EXPANDED event",
    "flag-on + SCOPE_INSUFFICIENT event → result.notes annotated with diff-salvage",
    "flag-OFF → scope NOT expanded (backward compat)",
    "flag-on + no SCOPE_INSUFFICIENT events → scope NOT expanded",
    "flag-on + SCOPE_INSUFFICIENT for different task → scope NOT expanded",
    "flag-on + duplicate attemptedPath → deduplicates (no double-add)"
   ],
   "orchestra/self-audit-adapter-wire.test.ts": [
    "executes the scoped surface through the registry adapter invocation verbatim",
    "propagates a failing adapter outcome as a gate failure with executed evidence",
    "holds — never greens — a project type no registered adapter supports",
    "holds when the supporting adapter is unavailable on this host",
    "keeps timeout and missing-evidence holds typed and fail-closed",
    "leaves the explicit full-authority audit surface off the scoped registry path",
    "resolves an undetectable stack to an ecosystem the shipped registry refuses",
    "ships a default registry that resolves the vitest ecosystem"
   ],
   "orchestra/self-audit-gate.test.ts": [
    "happy path — tsc PASS + vitest PASS + honesty clean + metrics.jsonl exists → overallGate PASS",
    "tsc fail → overallGate GATE_FAILURE, tsc.status FAIL, tsc.errors not empty",
    "vitest fail → overallGate GATE_FAILURE, vitest.status FAIL",
    "fails closed when $label has no parseable Vitest evidence",
    "keeps exit-zero valid parsed evidence green",
    "honesty violation present → overallGate GATE_FAILURE, honesty.violations > 0",
    "metrics.jsonl missing → overallGate PASS (warning only, not GATE_FAILURE)",
    "result always has correct SelfAuditResult shape",
    "multiple honesty violations all flagged → violations count matches",
    "all three checks fail → overallGate GATE_FAILURE with all issues present",
    "B-REGGATE: pre-existing failures (zero net-new vs baseline) → vitest PASS, overallGate PASS",
    "B-REGGATE: net-new failures vs baseline → vitest FAIL, overallGate GATE_FAILURE (regression caught)"
   ],
   "orchestra/self-audit-scoped-policy.test.ts": [
    "derives a deterministic shell-free manifest from task scope and attributed paths",
    "executes only the exact manifest and persists parseable execution evidence",
    "fails closed when executable source changed but no test manifest exists",
    "does not launch a suite for documentation-only work",
    "fails closed on timeout and never accepts exit zero without executed-count evidence",
    "honesty inspection never launches a hidden second suite in scoped mode"
   ],
   "orchestra/self-mod-enforce.test.ts": [
    "flag-on: user project task touching source patterns returns enforce mode",
    "flag-on: user project task with src/orchestra/ scope returns enforce",
    "flag-off: user project with self-mod scope returns advisory mode",
    "flag-off (default): non-self-mod scope also returns advisory",
    "deckent-dev: advisory regardless of enforceEnabled=true (ADR-039)",
    "deckent-dev: advisory for non-self-mod scope too (ADR-039 preserved)",
    "user project with unrelated scope returns advisory with selfModifying=false"
   ],
   "orchestra/self-modifying-detector.test.ts": [
    "should return true for the Deckent repo (.deckent/ + package.json name === \"deckent\")",
    "should return false for a user project with .deckent/ but different name",
    "should return false when .deckent/ does not exist",
    "should return false when package.json does not exist",
    "should return false for a non-existent directory",
    "should cache results for the same projectRoot",
    "should return fresh result after clearDetectionCache()",
    "should return true when Deckent repo task writes to src/orchestra/",
    "should return true when Deckent repo task writes specific file in src/core/",
    "should return false for user project even with Deckent-like scope",
    "should return false when scope only touches non-Deckent directories",
    "should return true for .deckent/agents/ scope",
    "should return true for .deckent/skills/ scope",
    "should return false for .deckent/ root (not agents/skills)",
    "should match files within pattern directories (prefix match)",
    "should match all DECKENT_SOURCE_PATTERNS",
    "should return true when at least one task is self-modifying",
    "should return false when no tasks are self-modifying",
    "should return false for user project regardless of scope",
    "should return false for empty task array",
    "should handle readonly array",
    "should contain all expected Deckent source directories",
    "should be a readonly array (TypeScript as const)"
   ],
   "orchestra/self-modifying.test.ts": [
    "isDeckentRepository returns true for deckent-dev repo",
    "isDeckentRepository returns false for user project (package.json name=\"myapp\")",
    "isSelfModifying returns true for src/orchestra/ in deckent repo",
    "isSelfModifying returns false for docs/ in deckent repo",
    "isSelfModifying returns false for src/orchestra/ in user project",
    "isSelfModifying integrates with task scope correctly",
    "isSelfModifying detects self-modification via filesWrite only",
    "isSelfModifying returns false for empty scope",
    "isDeckentRepository detects deckent via IDENTITY.md when package.json name differs"
   ],
   "orchestra/semantic-verdict-production-consumer.test.ts": [
    "preserves DONE only when the production symbols and executed proof exactly match",
    "rejects score-only and worker-note success when structured evidence is absent",
    "rejects an unrelated fixture-local projection even when its suite is green",
    "rejects exit-zero proof with no executed assertions or skipped assertions",
    "rejects empty production symbol and proof-target identities"
   ],
   "orchestra/sensitive-redactor.test.ts": [
    "redacts API key from error message",
    "redacts Bearer token from stack",
    "redacts long file content (>100 chars) keeping path",
    "redacts env var values",
    "redacts password= patterns",
    "preserves non-sensitive content unchanged"
   ],
   "orchestra/shared-memory-wire.test.ts": [
    "returns a SharedMemory instance from worker context",
    "worker writes and reads shared-memory via getSharedMemory",
    "isolation per sprint — different projectRoots do not share memory",
    "graceful — absent key returns null (no crash)"
   ],
   "orchestra/shared-memory.test.ts": [
    "writes a value under a key",
    "throws on empty key",
    "throws on empty writerId",
    "stores complex objects",
    "overwrites existing key",
    "returns null for non-existent key",
    "returns value, writerId, and writtenAt",
    "returns null for expired entry",
    "returns empty array when no shared dir",
    "lists written keys",
    "returns true for non-existent key",
    "returns false when no TTL is set",
    "returns true for expired entry with TTL",
    "returns false for non-expired entry with TTL",
    "returns 0 when no shared dir",
    "removes expired entries",
    "does not remove non-expired entries"
   ],
   "orchestra/shared-prompt-inject.test.ts": [
    "returns empty string for undefined or empty entries",
    "renders the header and one \"- key (by writer): value\" line per entry",
    "sorts entries by key deterministically (input order independent)",
    "appends the Shared Context block when ctx.sharedContext has entries",
    "places the Shared Context block at the END (after Skills/Agent/Scope/Task regions)",
    "emits no block (byte-for-byte legacy prompt) when sharedContext is absent",
    "emits no block when sharedContext is an empty array (== no-field baseline)",
    "is deterministic and order-independent for the same entry set",
    "injects shared notes when worker_comms.enabled && inject_shared",
    "injects nothing when worker_comms is disabled (even with shared entries)",
    "injects nothing when the worker_comms block is absent",
    "excludes the worker\\'s own notes (only \"other workers\" appear)"
   ],
   "orchestra/shared-write-bridge.test.ts": [
    "getSharedMemory factory creates a SharedMemory instance",
    "writes sharedNotes to SharedMemory when worker_comms.enabled and selfAssessment=DONE",
    "writes sharedNotes when selfAssessment=GO_WITH_TECH_DEBT",
    "does NOT write sharedNotes when worker_comms is disabled",
    "does NOT write sharedNotes when config is absent",
    "skips malformed/empty-key notes without throwing",
    "writes multiple sharedNotes from a single result",
    "does NOT write sharedNotes when selfAssessment=NO_GO"
   ],
   "orchestra/skill-attribution-migration-p0.test.ts": [
    "moves every legacy attribution namespace into digest-bound quarantine and replays one receipt",
    "holds malformed source bytes without overwriting or minting a receipt",
    "resumes from PREPARED after only the learnings projection was persisted",
    "resumes from PREPARED after only the catalog-stats projection was persisted",
    "holds an unexpected mutation after PREPARED and preserves the prepared receipt"
   ],
   "orchestra/skill-attribution-projection-p0.test.ts": [
    "projects V3 work/domain/scope facts instead of writing unknown empty TaskDNA",
    "quarantines legacy skill efficacy losslessly and counts exposure without efficacy credit",
    "increments efficacy stats only for a host-validated credited receipt",
    "rejects a CREDITED projection that is not bound to the immutable attribution batch",
    "has no production RuleEvolver writer or evolved-rule injection consumer",
    "moves poisoned skill learning into a digest-bound quarantine and keeps consumers inactive",
    "records an exposure outcome without populating skill performance",
    "never re-injects legacy co-exposure outcomes through worst-combination planner input"
   ],
   "orchestra/skill-force-delivery.test.ts": [
    "an empty routing result does NOT erase the operator force (the 9034 overwrite bug)",
    "unions the force in without dropping routing-derived skills",
    "never duplicates a forced id already assigned",
    "excludeSkills prunes a routing-derived skill",
    "force beats exclude when the operator named the same id in both",
    "is idempotent — a second application changes nothing",
    "an AUTO-assigned skill is never promoted into forceSkills (GR-2026-08-08-DOGFOOD-RCPT2-01)",
    "leaves a task with no directives untouched",
    "sprint path — routeSprintTasks keeps the force",
    "sprint path — an approved exact plan keeps the force without re-routing",
    "FIX path — a rotated forceSkills FIX task keeps the force",
    "single-task path — buildWorkerPrompt repairs a direct-V3 wholesale overwrite",
    "all three paths agree on the same effective assignment",
    "a disabled forced skill is NOT silently revived into the prompt (487-023)",
    "the probe reports exactly the ids present in the prompt bytes",
    "a forced skill recovered on the direct-V3 path is counted as delivered",
    "a skill dropped from the render is NOT credited as delivered (no phantom credit)",
    "an unresolvable forced skill is reported undelivered, never as credit",
    "round-trips through .tasks/task-<id>.skill-delivery.json",
    "returns null when no proof was recorded — absence is not \"nothing delivered\"",
    "buildWorkerPrompt publishes a current receipt without requiring a caller probe",
    "legacy delivery projection cannot overwrite the canonical prompt receipt",
    "a V3 decision that omits the forced id can no longer erase it",
    "an EMPTY V3 decision still leaves the force standing",
    "orders force first, then the V3-derived ids, with no duplicates",
    "excludeSkills prunes a V3-derived id",
    "excludeSkills can NEVER remove a forced id — the directive force wins",
    "passes a V3 decision through untouched when no directive is declared",
    "is idempotent — re-merging an already-merged assignment is stable",
    "agrees with applySkillDirectiveAuthority on the effective SET (both authorities, one outcome)",
    "task-mode-runner site — the direct-V3 assignment keeps the force"
   ],
   "orchestra/skill-routing-archive-replay-p0.test.ts": [
    "turns historical Python 39/39 into Python 0/39 with exact task-local rejection evidence"
   ],
   "orchestra/skill-routing-eligibility.test.ts": [
    "admits a skill the canonical derivation authority made routable",
    "reports profile-missing when no profile can be derived",
    "reports invalid-profile when an authored profile fails validation",
    "refuses a masked row fail-closed even when the disposition reads active",
    "reports the disposition before the profile problem",
    "applies the enabled filter the pool path never had, with a typed reason",
    "partitions an in-memory pool with no silent skip",
    "partitions the canonical catalog projection with no silent skip",
    "covers every typed rejection reason across the two sources"
   ],
   "orchestra/skill-unlock-battery.test.ts": [
    "(a) derivation → eligibility → directive authority yields a NON-EMPTY assignedSkills set",
    "(b) a non-derivable / disabled / retired skill is rejected with its typed reason — never silently dropped",
    "(c) force survives an empty routing result and exclude prunes — consistently through the single authority",
    "(d) delivery evidence records exactly the ids that reached the prompt, and names undelivered forced ids",
    "(e) coverage census — counts derived from the scan, never hardcoded"
   ],
   "orchestra/smoke-audit.test.ts": [
    "Tier-1 DONE task, fake smoke process exits non-zero with non-matching output → downgraded + event, and the fake process was actually invoked with the task command",
    "Tier-1 DONE task, fake smoke process exits 0 with matching output → DONE preserved, no mismatch event",
    "Tier-0 task (no Smoke declared, non-user-surface scope) → gate stays inert, fake exec never invoked"
   ],
   "orchestra/smoke-directive-parse.test.ts": [
    "parses **Smoke:** line from description body into smoke.command and smoke.expect",
    "parses - Smoke: header directive line into smoke.command and smoke.expect",
    "returns undefined smoke when no Smoke: line present",
    "returns undefined smoke when Smoke: line has no → separator",
    "parses complex multi-word command and expect from real DIRECTIVES format",
    "parses - Smoke: line in bullet task format",
    "returns undefined smoke for bullet task with no Smoke: line"
   ],
   "orchestra/smoke-field-flow.test.ts": [
    "structured-parse: Smoke line in DIRECTIVES → task.smoke populated and gate-acceptable",
    "bullet-parse: `- Smoke:` line in bullet/numbered task → task.smoke populated and gate-acceptable",
    "AI planner path: plannerTaskToParams → createTask → task.smoke populated and gate-acceptable",
    "no Smoke: line → task.smoke is undefined and gate is inert (Tier-0 path)",
    "JSON round-trip: task.smoke survives serialisation in `.tasks/task-NNN.json` shape"
   ],
   "orchestra/snapshot-start-guards.test.ts": [
    "materializes before publishing exact execution admission",
    "does not publish admission when exact materialization fails",
    "runs the 4 guards via runPreStartGuards instead of runPlanPhase",
    "threads rollbackEnabled=false through to runPreStartGuards",
    "sets sprint.startedAt when absent",
    "preserves an existing sprint.startedAt (does not overwrite)",
    "wraps a BrainError thrown by the CI/tsc gate as \"Plan phase failed: ...\" (SprintPhase.PLAN)",
    "wraps a plain Error thrown by a stash-pop conflict the same way",
    "delegates straight to runPlanPhase with the same 5 args, unchanged",
    "delegates to runPlanPhase when opts is undefined",
    "propagates a runPlanPhase rejection unchanged (no double-wrapping)"
   ],
   "orchestra/spawn-backend-docker-container-registry.test.ts": [
    "resolves a container recorded by ANOTHER process (empty in-memory map)",
    "still refuses an unknown task — a missing record is never guessed from a name",
    "rejects a malformed container id rather than acting on it",
    "rejects a record whose taskId does not match its filename",
    "rejects a foreign record kind",
    "KEEPS the record when removal does not succeed — a live worker must stay findable",
    "keeps the record human-readable — an operator can map task to container"
   ],
   "orchestra/spawn-backend-docker-dependency-inspection.test.ts": [
    "samples fresh milliseconds after a 90-second provider turn without an intervening ISO read",
    "refreshes on successive source-read boundaries rather than only the first post-provider read",
    "wires the same fresh clock into production %s",
    "proves native read-only access rejects the old deadline while the fresh 60-second deadline reads the same file",
    "provides the restart method and emits only a verified exact-generation observation",
    "fails closed for %s without provider or mutation commands",
    "rejects retained %s mismatch before invoking Docker",
    "rehydrates actual provider-start lifecycle authority through all production read-only inspections",
    "reproduces the missing production-method regression before any Docker inspection",
    "retains a lifecycle HOLD for a same-name dependency-volume recreation"
   ],
   "orchestra/spawn-backend-docker-ipc-authority.test.ts": [
    "retries a typed custody deadline on the next poll without poisoning terminal capture",
    "does not treat changed private bytes as transient unavailability",
    "captures one private question, publishes the private answer, and rereads answered authority",
    "cold-reconciles a durable answer whose answered cursor publication failed"
   ],
   "orchestra/spawn-backend-docker-mounts.test.ts": [
    "shares verified rejected-result closure between planning and startup without replaying effects",
    "treats verified retained failure as resolved history, never accepted or zero-work",
    "does not label an admitted but unsettled attempt ready or write its missing accepted chain",
    "rejects non-exact recovery targets before Store or Docker access",
    "admits READY failure to stopped inspection only with verified pretransaction diagnostic: %s",
    "keeps causal custody time ordered when the WSL2 wall clock steps backwards",
    "admits zero-byte staged effect content without weakening nonempty metadata artifacts",
    "accepts only an exact daemon no-such-container observation as no effect",
    "accepts only exact local-volume absence and a strict named-volume projection",
    "accepts one sorted portable snapshot inventory and excludes protected authority trees",
    "reads inventory through the bounded async git seam and rejects stderr ambiguity",
    "names portable case-fold collisions before any mount or provider effect",
    "excludes tracked worktree deletions before portable collision validation",
    "mounts only the immutable snapshot RO and attempt-private output RW",
    "derives one path-free restart-stable private volume identity per exact admission",
    "reads the named workspace volume by Docker Name, not daemon host mountpoint",
    "keeps image-owned dependency population bounded and verifies the copied tree",
    "binds workspace population to equal bounded pre, destination and post content manifests",
    "gives populate and capture helpers an owner-private executable native snapshot tmpfs",
    "creates the inert .deck readonly-mask target before baseline without copying protected authority",
    "streams a population inventory larger than a non-blocking pipe buffer",
    "fails population stdin closed when bytes exceed the declared authority",
    "runs exact provider auth phases asynchronously with bounded output and lifetime",
    "fails exact execution closed before provider or Docker launch when landing policy is absent",
    "binds exact native probe parity and compensation evidence into terminal no-effect truth",
    "durably preserves typed pre-mount detail while keeping the public reason constrained",
    "contains only the durable-identity-matched pre-provider container before volume compensation",
    "freshly rereads each exact volume generation around helpers and quiescence checks",
    "writes and rereads cleanup authority before each destructive release effect",
    "durably anchors terminal landing recovery before release can begin",
    "keeps PID1 provider creation behind the one-shot post-settlement start gate",
    "loads native custody only from trusted image code, never mutable workspace bytes",
    "rejects missing, wrong and early execution commits and delivers only after durable reread",
    "behaviorally rejects missing, extra and wrong raw start authorizations before PID1 ack",
    "snapshots nested prepare authority without invoking accessors or retaining caller mutation",
    "binds actual segment order and rendered delivery without assignment phantom credit",
    "rejects every missing, extra, foreign or mutated provider-start ack field",
    "accepts provider execution only from the strict post-spawn PID1 acknowledgement",
    "orders Store settlement and reread before start delivery and durable start before release",
    "keeps unknown daemon/permission inspect failures UNKNOWN during reconciliation",
    "keeps pre-release reconciliation evidence internally consistent",
    "rejects corrupt Store observation bytes or receipt evidence before release projection",
    "reopens the same live released dispatch without a second start and fails closed on map loss",
    "rehydrates one durable released exact attempt once without redispatching the provider",
    "durably preserves %s first-failure identity and never releases on HOLD",
    "retains exact resources when host-work %s prevents durable attribution",
    "captures genuine Codex terminal usage for subscription without inventing USD billing",
    "captures immutable worker output but holds metered execution with no provider USD envelope",
    "captures immutable worker output before holding a missing provider usage envelope",
    "classifies malformed adapter usage before releasing the exact effect landing",
    "reconstructs cold exact completion only from durable post-cleanup artifacts",
    "HOLDs cold API reconciliation when the immutable provider stream lacks USD billing",
    "fails closed on stale %s timestamp during cold reconstruction",
    "keeps leadership-free recovery out of exact adoption and HOLDs a gapped start chain",
    "rejects NOT_DISPATCHED recovery when durable provider execution evidence exists",
    "isolates one pending admission HOLD and still closes a later terminal zero-work record",
    "treats an append-only retired-before-admission reservation as terminal history",
    "reports one identity-bound discovery HOLD and still reconciles its valid sibling",
    "HOLDs a released exact attempt whose admitted landing policy is missing",
    "contains every exact durable admission through stop-and-record without monitor or landing",
    "contains a live exact container through async inspect-stop-wait and durable exit reread",
    "re-observes Docker before accepting a recovered provider-exit as contained",
    "retains observed completion until accepted-result consumption and keeps HOLD state",
    "surfaces automatic acceptance rejection and HOLD through the public terminal await",
    "reopens hot/cold accepted authority with derived directory projection %s",
    "rejects terminal queries with a missing or foreign provider-start receipt",
    "defaults to false so worker mounts stay byte-identical until opted in",
    "publishes English and Turkish config-reference metadata",
    "masks exactly the two design catalogs, read-only, when the flag is on",
    "emits ZERO args when the flag is off — the default, byte-identical argv pin",
    "emits NO arg for a catalog the caller reports absent (no phantom host dir)",
    "emits zero args when NO catalog exists on the host, even with the flag on",
    "always mounts read-only (a worker must never write through the mask)",
    "creates the shared empty mask source under .tasks/ idempotently",
    "emits ZERO catalog mounts by DEFAULT (flag off) even though both catalogs exist",
    "threads effective prompt.catalog_mount_mask through the factory into docker argv",
    "masks both catalogs read-only when the flag is on and both exist on the host",
    "skips a catalog missing on the host (no phantom .claude/agents in the repo)",
    "emits no catalog mount at all when NEITHER catalog exists on the host",
    "keeps every pre-existing mount byte-identical — the mask is purely additive",
    "produces argv identical to the unmasked spawn except for the mask args"
   ],
   "orchestra/spawn-backend-docker-planning-preflight.test.ts": [
    "rejects a host state root physically nested under the project before dispatch"
   ],
   "orchestra/spawn-backend-docker-probe.test.ts": [
    "returns a frozen sanitized completion and keeps prompt/auth details out of it",
    "uses logical Cursor provider authority to mount auth.json for the real probe entrypoint",
    "reports a Cursor XDG override without auth as unavailable before the probe runner",
    "classifies a dead Docker daemon as backend-unreachable",
    "keeps an ordinary provider non-zero exit distinct from Docker daemon failure",
    "keeps the transport envelope bounded independently of the provider token budget",
    "classifies transport-envelope overflow without claiming the Docker daemon is unreachable",
    "reports absent isolated credentials as unavailable rather than unreachable",
    "accepts the %s Docker adapter platform",
    "honestly reports an unsupported adapter platform"
   ],
   "orchestra/spawn-backend-docker.test.ts": [
    "normalizes binary unit suffixes (k/m/g/t, case-insensitive) to bytes",
    "returns null for malformed / missing / non-positive input",
    "accepts decimal values like 0.5g",
    "mounts only the Claude credential file and bootstraps a private HOME",
    "does not mount subscription credentials in API mode",
    "uses each provider credential allowlist without mounting provider homes",
    "leases a writable host-owned broker and writes refreshed auth back",
    "fails closed when a required credential is missing but optional metadata exists",
    "copies only selectedType and excludes unrelated host settings",
    "fails closed on a missing or shell-unsafe selectedType",
    "exports DEFAULT_WORKER_MEMORY_LIMIT=4g and DEFAULT_WORKER_MEMORY_SWAP=6g",
    "passes --memory 4g --memory-swap 6g to docker run when opts omitted",
    "uses constructor opts to override --memory / --memory-swap",
    "keeps the new memory cap below the 8g pre-191 hardcoded value",
    "top-level max_workers, when present, is a number in [1, 20] (no string \"3\" pre-191 drift)",
    "worker_memory_limit/swap, when present, are valid docker memory strings (absent → safe 4g/6g default)",
    "every mode max_workers is a number within the WSL2-safe range [1, 8]",
    "api mode max_workers, when present, is bounded (<=4) for WSL2 host-OOM safety",
    "default subscription mounts only the Claude credential file",
    "authMode=\"api\" in task JSON skips ~/.claude mount and stamps env=api",
    "authMode=\"api\" without ANTHROPIC_API_KEY throws SpawnBackendError",
    "%s subscription mode is held before unmetered Docker work",
    "%s API mode is held before unmetered Docker work",
    "passes -e NODE_OPTIONS=... to docker run argv",
    "encodes the percentage value as 75 in the NODE_OPTIONS string",
    "overrides any host process.env.NODE_OPTIONS — container always gets the Deckent value",
    "claude: uses exact API model ID and an auth-only credential mount",
    "gemini: fails closed before Docker because incremental usage is unavailable",
    "codex: fails closed before Docker because incremental usage is unavailable",
    "claude auth failure → writes AUTH_FAILED NO_GO .result and SKIPS docker run",
    "codex is not claude-auth-gated but remains held by live-usage admission",
    "permission-denied daemon → rejects E086, image lookup + docker run never happen"
   ],
   "orchestra/spawn-backend-move.test.ts": [
    "TmuxBackend is exported from orchestra/spawn-backend.js",
    "SubprocessBackend is exported from orchestra/spawn-backend.js",
    "SpawnBackendFactory is exported from orchestra/spawn-backend.js",
    "SpawnBackendError is exported from orchestra/spawn-backend.js",
    "SpawnBackend interface: TmuxBackend satisfies all required methods",
    "SpawnBackend interface: SubprocessBackend satisfies all required methods",
    "SpawnBackendFactory.create() with \"tmux\" returns TmuxBackend",
    "SpawnBackendFactory.create() with \"subprocess\" returns SubprocessBackend",
    "SpawnBackendError carries backendName and is instanceof Error",
    "BackendType type is correctly constrained (tmux | subprocess | docker | auto)",
    "SubprocessBackend.isAvailable() always resolves to true"
   ],
   "orchestra/spawn-backend-recovery-diagnostic.test.ts": [
    "preserves typed evidence without changing Error.message or serializing extra properties",
    "rejects unsafe %s without echoing it",
    "reports truncation explicitly and never evaluates field getters"
   ],
   "orchestra/spawn-coordinator-oom.test.ts": [
    "(a) Host 12 GB + 4g worker → 2 workers (default WSL2 scenario)",
    "(b) Host 32 GB + 3g worker → 4 workers (tier cap dominates per-worker formula)",
    "(c) Operator override (configured number) wins over tier auto-resolve",
    "(d) Host detection failure → safe default of 1 worker",
    "returns 1 for hosts <8 GB, 2 for 8-16 GB, 3 for 16-32 GB, 4 for ≥32 GB",
    "returns 1 for pathological inputs (NaN, ≤0)"
   ],
   "orchestra/spawn-coordinator.test.ts": [
    "caches host detection and respects explicit worker configuration",
    "applies the conservative tier cap"
   ],
   "orchestra/spawn-failure-authority.test.ts": [
    "wires startup reconciliation failure to pre-plan authority retirement",
    "retires only the current pre-plan lock while preserving forensic state",
    "retires live PID and owned lock while preserving exact process-generation evidence",
    "contains exact EXECUTE effects before retiring live authority",
    "keeps live authority visible when exact EXECUTE containment holds"
   ],
   "orchestra/spawn-git-async.test.ts": [
    "returns a real Promise (the sync-era call sites no longer resolve synchronously)",
    "attributes only bytes added after the claim-time baseline (modified file)",
    "attributes a newly-created scoped file (added, no beforeHash)",
    "attributes deletion from the persisted claim-time blob (cat-file async path)",
    "excludes an unchanged predecessor-baseline file from the result",
    "preserves per-file ordering across multiple scoped files (sequential await, not reordered)",
    "turns missing attempt authority into a typed HOLD instead of trusting final git diff"
   ],
   "orchestra/spawn-prevention.test.ts": [
    "should call backend.spawn when SpawnBackend is provided",
    "should NOT call legacy tmux spawnWorker when backend is provided",
    "should NOT call ensureSession when backend is provided",
    "holds an owner-mismatched final-only route before backend or adapter spawn",
    "should fail closed when legacy tmux cannot meter the task budget",
    "should use adapter when no backend and provider is non-tmux",
    "should spawn multiple tasks through backend without leaking to legacy"
   ],
   "orchestra/spawn-routing-adapter.test.ts": [
    "returns true for ollama (host-HTTP provider)",
    "returns false for claude (tmux + docker backed)",
    "returns true for codex (host-process adapter)",
    "returns true for gemini (host-process adapter)",
    "routes ollama task to adapter.spawn even when docker backend is provided",
    "routes claude task to backend.spawn (no regression)",
    "MF-2: host-adapter provider with NO registered adapter → honest NO_GO, not docker degrade",
    "MF-2 lazy re-check: missing adapter → re-bootstrap registers it → runs it (not honest-fail)",
    "holds a forced Docker host-adapter route without live provider usage capability",
    "mixed sprint — ollama hits adapter, claude hits backend",
    "routes wave-2 ollama task to adapter.spawn (same routing as spawnWorkers)",
    "throws when an Ollama provider reaches Docker binary selection"
   ],
   "orchestra/spawn-spawner-wire.test.ts": [
    "reports dispatch evidence only after a worker is actually admitted",
    "blocks initial sprint dispatch when attended authority is only a raw reference",
    "routes an owner-authorized final-only Codex budget through Docker containment",
    "blocks a final-only Docker dispatch when the task-stamped grant is missing",
    "does not retry a provider-authority composition HOLD and never reaches prompt/task assignment/backend",
    "emits BRAIN→SPAWN:BLOCKED when two tasks collide on filesWrite",
    "does NOT emit TASK_ASSIGN for the deferred colliding task (winner proceeds)",
    "TASK_ASSIGN payload uses readTaskJsonFresh disk read (not stale in-memory)",
    "TASK_ASSIGN carries toolScope enforcement truth (codex write-scope → UNENFORCED)",
    "TASK_ASSIGN toolScope for a claude write-scope is flag-enforced",
    "TASK_ASSIGN carries writeScopeShellEscape (scoped Write + unscoped Bash → DEFEATED_BY_SHELL)",
    "non-colliding tasks proceed normally after a collision blocks others",
    "debounces SCOPE_COLLISION_DETECTED across repeated ticks of the same collision (FIX-5)"
   ],
   "orchestra/spawn-throw-lifecycle.test.ts": [
    "rejects with the SPAWN BrainError instead of swallowing it",
    "names every failed spawn attempt in the terminal message, not just the last",
    "clears the periodic-snapshot interval via the finally fail-safe, even though the happy-path clearInterval line is never reached",
    "removes the beforeExit listener via the finally fail-safe — no leaked listener across a SPAWN throw",
    "does not leave a dangling scanInterval — runSpawnPhase clears its own copy before throwing, and the outer fail-safe is a safe no-op on null"
   ],
   "orchestra/spawn-timeout-wiring.test.ts": [
    "emitTimeoutEvents RETURNS the adaptive timeout (no longer a void 0-caller)",
    "spawnWorkers passes taskTimeoutSeconds = brainEstimateTimeout (NOT undefined, NOT docker_timeout)",
    "taskTimeoutSeconds scales by effort (high > low) — adaptive, not a flat cap",
    "every dispatched worker receives a positive taskTimeoutSeconds (no dormant gap)"
   ],
   "orchestra/spawn-timeout.test.ts": [
    "worker.sh template uses TASK_TIMEOUT env var pattern (not hardcoded)",
    "DockerSpawnBackend constructor stores timeout correctly",
    "uses default WORKER_TIMEOUT_SECONDS when no timeoutSeconds provided",
    "uses custom timeoutSeconds when provided (low effort ~600s)",
    "uses custom timeoutSeconds for high effort (~2400s)",
    "applies tmux backend factor result (~1080s for normal with 0.9x)",
    "includes EXIT trap with result file fallback",
    "SpawnBackendOptions accepts taskTimeoutSeconds field",
    "taskTimeoutSeconds is optional (undefined by default)",
    "TmuxBackend spawn accepts opts with taskTimeoutSeconds",
    "SubprocessBackend spawn accepts opts with taskTimeoutSeconds",
    "subprocess backend factor 0.8x means lower timeout",
    "creates DockerSpawnBackend with dockerTimeoutSeconds",
    "creates TmuxBackend via factory",
    "config min_timeout floor is respected via taskTimeoutSeconds",
    "config max_timeout ceiling is respected via taskTimeoutSeconds",
    "WORKER_TIMEOUT_SECONDS constant is still exported (deprecated)",
    "buildWorkerCommand falls back to WORKER_TIMEOUT_SECONDS when timeoutSeconds is undefined"
   ],
   "orchestra/spawner-dep-normalization.test.ts": [
    "resolves a sibling TITLE dependency to its concrete slot id",
    "is behaviour-preserving for already-correct id deps (no change)",
    "drops an unresolvable dependency (no matching sibling) without throwing"
   ],
   "orchestra/spawner-single-authority.test.ts": [
    "routeTask NEVER re-derives an agent — the plan-time V3 decision is verbatim-authoritative",
    "preserves a meaningful plan-time assignedAgent — spawn-time does NOT overwrite it",
    "preserves a forceAgent-pinned plan-time assignment on a surface scope",
    "an unassigned task stays honestly generic — no spawn-time re-derivation exists (S3)",
    "a literal 'generic' assignment passes through unchanged (S3: no re-derivation)",
    "an '' assignment resolves to the spawn floor 'generic' only (S3: no re-derivation)",
    "stays generic when neither plan-time nor routeTask has a real signal (NO_GO guard)",
    "provider and skills assignment are unaffected by the agent pin",
    "S3: the spawn-fallback lane is retired — an unpinned task journals NOTHING",
    "does NOT journal anything when plan-time already pinned the agent",
    "is fail-soft/no-op when journalContext is omitted (current production call site)",
    "persists structured fallback evidence before mutating the task provider",
    "rejects a fallback without durable sprint context before task mutation",
    "does not emit fallback evidence when the preferred provider is available"
   ],
   "orchestra/spend-gate-enforce.test.ts": [
    "allows admission, reports no breach, and never touches the spend ledger",
    "stays a no-op even when explicitly set to false",
    "allows admission with no breach when the projection stays within limits",
    "refuses admission with a typed COST_GATE_EXCEEDED on a daily breach",
    "refuses admission on a monthly breach when the day window is clean",
    "never signals a kill/pause — the refusal only withholds admission",
    "allows admission and returns the byte-identical COST_LIMIT_WARN event",
    "blocks on spend summed from .deckent/settings/resource-log.jsonl",
    "allows admission when the ledger is absent (no spend recorded yet)",
    "finalize still only WARNS when the flag is ON and the ceiling is crossed",
    "stays fail-safe with the flag ON — an emitter throw cannot fail finalize",
    "does not block a breaching in-flight sprint even though admission would refuse"
   ],
   "orchestra/sprint-attribution-summary-replay.test.ts": [
    "projects only verified exact-attempt files while retaining exclusions and independent cost evidence"
   ],
   "orchestra/sprint-checkpoint.test.ts": [
    "write+read roundtrip preserves all fields",
    "persists exact terminal authority and requires Store revalidation before resume",
    "restores exact terminal status from fresh Store authority, not a stale public result",
    "keeps a restored exact terminal as a non-COMPLETE candidate until its fresh checkpoint authority is durable",
    "preserves the old checkpoint and throws HOLD when fresh exact authority cannot be persisted",
    "does not treat a public COMPLETE projection or sprint log as checkpoint deletion authority",
    "allows checkpoint cleanup only after the canonical terminal archive verifies",
    "parks recovered exact work without current authority before public cascade",
    "rejects a checkpoint whose exact terminal authority was tampered",
    "rejects a checkpoint whose payload names a different sprint",
    "throws instead of silently dropping an exact terminal authority write failure",
    "rejects an exact terminal authority map entry outside the checkpoint task universe",
    "parks a raw exact-custody result instead of trusting its public self-assessment",
    "checkpoint number increments on subsequent writes",
    "includes same-sprint dynamic FIX tasks persisted after PLAN",
    "latest checkpoint overwrites previous (only one file)",
    "readCheckpoint returns null when no checkpoint file exists",
    "hasCheckpoint returns false when file does not exist",
    "hasCheckpoint returns true after writeCheckpoint",
    "returns all tasks when none are completed",
    "excludes completed tasks from result",
    "resume from middle: correctly skips first N completed",
    "fresh start fallback: returns all tasks when checkpoint has no completed",
    "correctly categorizes EXECUTING tasks as active workers",
    "returns null gracefully when deckent dir cannot be created (fail-safe)",
    "embeds depGraph in checkpoint when graph provided",
    "depGraph embedded in checkpoint persists to disk and is readable",
    "checkpoint without graph has no depGraph field (backward compat)",
    "writes separate depgraph.json + .mmd files when graph provided",
    "restores graph from embedded checkpoint.depGraph",
    "restored graph enforces correct wave ordering on resume",
    "falls back to separate depgraph.json when embedded graph missing",
    "returns null when no graph available (caller must rebuild)",
    "discovers a pending dynamic FIX omitted by an older v2 checkpoint",
    "resets an interrupted EXECUTING worker (no .result) to PENDING and moves it active→pending",
    "NEVER touches a worker that has a valid .result (completed during crash)",
    "re-queues a PAUSED task only when its durable pause marker exists",
    "does not re-queue an unrelated markerless PAUSED task",
    "re-queues a markerless PAUSED dependent after a multi-hop FIX lineage settles",
    "restores terminal status from Brain verdict instead of worker self-assessment",
    "never restores an exact task from a forged public verdict when its checkpoint ref is missing",
    "updates the v2 taskStates entry to PENDING when present",
    "deriveResumableTaskIds = pending ∪ interrupted-active, excluding completed",
    "keeps a checkpoint-pending task resumable after a failed run projected it PAUSED",
    "restores checkpoint-PENDING disk-PAUSED work without false-completing it",
    "projects an absent-result active task to PENDING instead of synthetic NO_GO",
    "keeps the complete action for a fully terminal checkpoint with real result evidence",
    "hasValidResult requires matching taskId and a canonical terminal assessment",
    "parks a pending Docker settlement even when raw worker output says DONE",
    "parks an exact custody admission without a public result for backend reconciliation",
    "does not let a checkpoint-pending exact admission bypass backend reconciliation",
    "fails closed when exact Store ownership is unavailable",
    "refuses to build a legacy preplanned resume for a parked exact admission",
    "uses closed host settlement over contradictory raw output and parks invalid settlement",
    "redrives a closed not-dispatched Docker attempt instead of counting recovery synthesis as task NO_GO"
   ],
   "orchestra/sprint-controller-graceKill-gate.test.ts": [
    "(a) panic-guard BLOCK + disk empty → returns base NO_GO unchanged (legacy preserved)",
    "(b) panic-guard BLOCK + disk has tracked diff → reclassified with disk findings",
    "(c) explicit-kill path + untracked files → reclassified with disk findings",
    "(d) idempotent: task already MANUAL_REVIEW_REQUIRED → reclassified=false and verifier is NOT called",
    "explicit-kill + disk empty → returns base NO_GO unchanged"
   ],
   "orchestra/sprint-controller-liveness.test.ts": [
    "never-spawned → SKIP synthetic; NEVER_DISPATCHED event emitted",
    "unverifiable heartbeat → fail-closed synthetic result",
    "dead → synthetic NO_GO with liveness=dead in notes",
    "never-spawned → SKIP synthetic; NEVER_DISPATCHED event emitted",
    "unverifiable heartbeat → fail-closed synthetic result",
    "dead → synthetic NO_GO with liveness=dead label (user-explicit override notes)",
    "unavailable label propagates when authority cannot prove liveness"
   ],
   "orchestra/sprint-controller-nervous-hook.test.ts": [
    "should emit SPRINT_PHASE_CHANGE with correct oldPhase/newPhase/sprintId payload",
    "should emit exactly 1 event per phase transition call",
    "should emit SPRINT_RETRO_COMPLETE event after retro phase",
    "should emit correct sequence of events for a full sprint lifecycle",
    "should emit events via EventEmitter even without subscribers (backward compat)"
   ],
   "orchestra/sprint-controller-nervous-wire.test.ts": [
    "disabled → bootstrap loader is NOT consulted, init returns null (default-off respect)",
    "enabled → bootstrap.createNervousSystemIfEnabled is called with (config, projectRoot, sprintStateProvider) and its handle is returned",
    "disposeNervousSystem → invokes handle.dispose() once; null-safe; swallows dispose throws",
    "enabled but bootstrap loader returns null (module missing) → init returns null gracefully"
   ],
   "orchestra/sprint-controller.test.ts": [
    "stores phase information",
    "works without phase argument",
    "carries planner proof when planning fails before Sprint persistence",
    "returns true when file is older than maxAgeMs",
    "returns false when file is fresh",
    "returns false when statSync throws",
    "accepts custom maxAgeMs",
    "kills all active workers via tmux when no backend provided",
    "kills workers via spawn backend when provided",
    "releases locks for tasks with assigned workers",
    "removes task files with known extensions",
    "removes .prompt-* hidden tmpfiles",
    "removes .lock files from locks directory",
    "handles kill errors gracefully",
    "transitions PENDING tasks to PAUSED",
    "transitions EXECUTING tasks to PAUSED",
    "does not pause DONE tasks",
    "sets sprint status to PAUSED",
    "writes pause state JSON file",
    "writes .paused marker file for each paused task",
    "kills tmux workers without IPC channel",
    "updates dashboard with PAUSED status",
    "transitions PAUSED tasks to PENDING",
    "sets sprint status to ACTIVE",
    "removes .paused marker files",
    "removes pause state file",
    "updates dashboard with ACTIVE status",
    "returns true for tasks with only doc directories",
    "returns false for tasks with src directory",
    "returns false for tasks with tests directory",
    "returns false when no directories specified",
    "returns NO_GO when selfAssessment is NO_GO",
    "returns GO_WITH_TECH_DEBT when selfAssessment says so",
    "returns NO_GO when tests did not pass",
    "returns DONE for doc tasks with passing tests",
    "returns DONE for code tasks with high coverage",
    "returns GO_WITH_TECH_DEBT when coverage below 90%",
    "is exported under the disambiguated name evaluateResultSync",
    "produces a DONE verdict for a passing high-coverage result",
    "returns null when no provider registered",
    "RunSprintOptions allows optional fields",
    "PauseState has required fields",
    "returns a ChannelRegistry instance",
    "returns task.provider when explicitly set",
    "infers claude from opus model",
    "infers claude from sonnet model",
    "infers claude from haiku model",
    "infers codex from o3 model",
    "infers codex from gpt-4.1 model",
    "infers gemini from gemini-2.5-pro model",
    "infers gemini from gemini-2.5-flash model",
    "explicit provider overrides model inference",
    "falls back to registry default for unknown model",
    "throws ProviderError when registry is empty and model unknown",
    "returns true for claude provider",
    "returns false for codex provider",
    "returns false for gemini provider",
    "returns path under .tasks/ directory",
    "includes project root in path",
    "uses task ID in log file name",
    "returns null when log file does not exist",
    "returns log content when file exists",
    "returns null when readFileSync throws",
    "returns true when log file exists",
    "returns false when log file does not exist",
    "returns undefined when no provider is registered",
    "returns CLI binary from default provider",
    "holds Claude tasks before tmux worker dispatch",
    "spawns Claude tasks via SpawnBackend when provided",
    "routes codex task to CodexAdapter.spawn",
    "routes gemini task to GeminiAdapter.spawn",
    "infers codex provider from o3 model (no explicit provider)",
    "infers gemini provider from gemini-2.5-flash model",
    "handles mixed sprint: Claude + Codex + Gemini tasks",
    "does not call ensureSession when no Claude tasks exist",
    "no provider field defaults to claude for Claude models",
    "returns queued tasks beyond max_workers",
    "updates dashboard with provider info in currentAction",
    "passes allowedTools with scope directories to adapter",
    "passes autoApprove option to non-Claude adapter",
    "gracefully handles missing provider adapter (not registered)",
    "kills non-Claude workers via provider adapter during cleanup",
    "does not call adapter kill for Claude tasks during cleanup",
    "handles adapter kill errors gracefully",
    "sprint-controller.ts has zero inline hardcoded claude fallback patterns",
    "routing comparisons use isTmuxProvider() helper instead of inline string",
    "isTmuxProvider is the single source of truth for tmux routing",
    "resolveTaskProvider tries registry default before hardcoded fallback",
    "sprint-controller imports routeTask from task-router (via sprint-spawner)",
    "routeTask is called in runSprint between plan and spawn phases",
    "routing phase uses providerRegistry.listProviders() for available providers",
    "routing phase sets task.provider from router output",
    "routing phase sets task.assignedAgent when router returns non-generic",
    "routing phase sets task.assignedSkills when router returns skills",
    "routing phase persists context, parks a typed HOLD, and rethrows",
    "routeTask mock returns correct default shape",
    "routeTask can return a non-claude provider",
    "routeTask can return a specific agent",
    "routeTask can return skills",
    "Task type already has assignedSkills field",
    "Phase 1.5 comment is present in source as routing phase marker",
    "calls formatRichSprintSummary during finalization",
    "passes sprint data and evaluations to formatRichSprintSummary",
    "includes attribution-derived gitDiff in rich output options (was: raw spawnSync git diff)",
    "completes normally when formatRichSprintSummary throws",
    "respects output_mode from config",
    "defaults output_mode to normal when config has no output_mode",
    "logs rich output to console when formatRichSprintSummary returns a string",
    "passes agentPerf data from buildAgentPerformance to formatRichSprintSummary",
    "routes all tasks and sets task.provider",
    "sets task.assignedAgent when router returns non-generic agent",
    "does not override assignedAgent when router returns generic",
    "sets task.assignedSkills when router returns skills",
    "does not set assignedSkills when router returns empty skills",
    "passes config to routeTask for skill_routing overrides",
    "passes availableProviders from connector to routeTask",
    "handles empty task array gracefully",
    "routes each task independently (different routing per task)",
    "non-Claude provider routes to subprocess in spawnWorkers",
    "Claude provider routes to tmux in spawnWorkers",
    "RunSprintOptions accepts connector field",
    "source uses connector.getAvailableProviders() when connector provided",
    "source falls back to providerRegistry when no connector",
    "routeSprintTasks is exported from sprint-controller (via re-export)",
    "removes DRAFT task files from .tasks/",
    "does nothing when .tasks/ does not exist",
    "ignores malformed task files",
    "writes sprint state to .deckent/sprint-state.json",
    "includes all task IDs in state",
    "does not throw when write fails",
    "returns parsed state when file exists",
    "returns null when file does not exist",
    "removes sprint state file when it exists",
    "does nothing when file does not exist",
    "returns all workers as orphans when no sprint state exists",
    "filters out workers belonging to current sprint",
    "returns empty array when no workers exist",
    "suggests model downgrade on rate limit errors",
    "suggests tmux check on tmux errors",
    "warns about high task count",
    "provides generic hint for unknown errors",
    "RunSprintOptions includes timeoutMs field",
    "updates task.status to EXECUTING after spawning via SpawnBackend",
    "writes task JSON with EXECUTING status to disk after spawn",
    "generateTempAgents produces agents with temp- prefix for TypeScript+React",
    "generated temp agents have source=learned and enabled=true",
    "generated temp agents have v2 activation rules",
    "temp-agent IDs do not collide with built-in agent IDs",
    "generateTempAgents returns empty array for unsupported language stacks",
    "generateTempAgents produces agents with required fields for AgentPoolManager.saveTempAgentToPool",
    "generateProjectConventionsSkill paired with generateTempAgents covers full stack context",
    "interruptActiveSprint is a no-op when no active sprint is registered",
    "interruptActiveSprint writes INTERRUPTED to task JSON and ABORTED to heartbeat for active tasks",
    "isInterrupted returns false before interrupt and true after",
    "interruptActiveSprint calls killWorker for each active tmux worker",
    "writes rich evaluations with per-task details to job JSON",
    "sets techDebtDetail for GO_WITH_TECH_DEBT evaluations",
    "summary does not double-count TECH_DEBT in completed tasks",
    "refuses to settle a task with no result/attempt evidence (fail-closed, was: graceful missing-result)",
    "honors sprint_timeout_minutes when no explicit opts timeout (0 = unlimited)",
    "converts a positive sprint_timeout_minutes to milliseconds",
    "an explicit opts.timeoutMs always wins over the config knob",
    "treats a negative config as unset (undefined → waitForResults 30-minute default)",
    "emitPhaseChange emits both 'deckent-event' and 'event' on eventBus",
    "event listener (NervousObserver style) receives SPRINT_PHASE_CHANGE payload",
    "'deckent-event' emitted with correct SPRINT_PHASE_CHANGE payload"
   ],
   "orchestra/sprint-docs-cleanup.test.ts": [
    "archives .log files belonging to the sprint",
    "archives .timeout files belonging to the sprint",
    "archives .prompt-* files alongside task files",
    "does not archive files from a different sprint",
    "preserves landing proposals and temporary result residue as non-task evidence",
    "returns 0 when .tasks/ directory does not exist",
    "returns 0 when no matching files exist for the sprint",
    "returns 0 when .tasks/archive/ does not exist",
    "returns 0 when archive count is within retention limit",
    "removes oldest archive dirs when count exceeds retention",
    "removes files inside old archive dirs before removing dir",
    "ignores non-sprint-NNN entries in archive dir",
    "dry-run filter includes .timeout files"
   ],
   "orchestra/sprint-docs-helpers.test.ts": [
    "returns lines array with sprint id heading",
    "includes metrics table with all required rows",
    "handles empty tasks array gracefully",
    "collects agents and skills from task assignments",
    "shows \"-\" for agents/skills when none assigned",
    "includes task status from evaluations map if provided",
    "includes notes section when tasks have result notes",
    "truncates notes to 150 characters",
    "counts files changed from results",
    "includes previous sprint reference",
    "includes correct next sprint number",
    "handles string sprint number",
    "includes reference sections",
    "includes template task structure",
    "uses \"???\" for unknown next sprint number",
    "extracts sprint number from filename",
    "returns 0 for invalid filename",
    "extracts number with leading zeros",
    "handles filename with path",
    "parses added files from git diff output",
    "ignores non-src files",
    "returns empty array for no matches",
    "handles empty diff output",
    "ignores lines without proper format",
    "finds maximum ADR number in content",
    "returns 0 when no ADRs found",
    "handles single ADR",
    "handles large ADR numbers",
    "only matches h2 headings with ADR pattern",
    "generates ADR entry lines with correct format",
    "includes status as PROPOSED",
    "includes context with sprint number",
    "includes decision placeholder",
    "handles string sprint number",
    "handles zero-padded ADR numbers"
   ],
   "orchestra/sprint-estimator.test.ts": [
    "uses correct base minutes for a premium model",
    "uses correct base minutes for a standard model",
    "uses correct base minutes for an economy model",
    "derives base minutes from registry TIER for a canonical model id",
    "scores an economy-tier canonical model at the economy baseline",
    "uses the same base minutes for the same tier across providers",
    "fails loudly for an unregistered model instead of assuming a standard tier",
    "applies low effort multiplier (0.6)",
    "applies normal effort multiplier (1.0)",
    "applies high effort multiplier (1.6)",
    "adds scope time based on directories and filesWrite",
    "caps scope items at 10",
    "returns correct taskId",
    "total equals effortMin + scopeMin",
    "handles empty scope gracefully",
    "returns 1.0 for 1 worker",
    "returns 1.0 for 0 workers",
    "returns less than 1.0 for 2 workers",
    "returns less than 1.0 for 4 workers",
    "clamps to minimum 0.2 for very large worker counts",
    "returns value between 0.2 and 1.0 for 3 workers",
    "factor decreases as worker count increases",
    "parses duration from sprint log format",
    "returns null for missing duration line",
    "returns null for zero duration",
    "parses 30-minute sprint correctly",
    "handles extra whitespace around values",
    "returns null for non-numeric value",
    "returns 0 for empty array",
    "returns the single value for one-element array",
    "calculates average of multiple values",
    "handles floating-point values",
    "returns empty array when sprints directory does not exist",
    "returns empty array when no sprint files exist",
    "reads duration from a single sprint file",
    "reads multiple sprint files and respects limit",
    "skips files without parseable duration",
    "returns most recent sprints first",
    "returns 0 for empty task list",
    "returns positive number for non-empty task list",
    "returns lower estimate for more workers (parallelism)",
    "uses historical data when available",
    "returns at least 1 minute",
    "returns integer (rounded) value",
    "returns zero estimate for empty tasks",
    "includes task scores in result",
    "serialTotalMin is sum of individual task scores",
    "reports correct worker count",
    "reports historicalSprintCount when history is available",
    "blends estimate 70/30 with history",
    "uses only heuristic when no history",
    "does nothing when dashboard does not exist",
    "writes estimatedDurationMin to dashboard",
    "preserves existing dashboard fields",
    "writes estimationDetails to dashboard",
    "handles malformed dashboard JSON gracefully"
   ],
   "orchestra/sprint-file-retention.test.ts": [
    "should extract sprint ID from events file",
    "should extract sprint ID from gate file",
    "should return null for non-sprint files",
    "should list all sprint-prefixed files",
    "should return empty array for missing .deckent dir",
    "should exclude directories like sprint-god-analysis/",
    "should group files by sprint ID",
    "should delete -seq and -checkpoint-seq for a sprint",
    "should not delete non-counter files",
    "should not affect other sprints",
    "should move forensic files to the canonical sprint audit namespace",
    "should not move non-forensic sprint files",
    "should archive sprints beyond keep_last_n",
    "should not archive when under keep_last_n",
    "should respect custom keep_last_n=5",
    "should enforce size_cap_mb by archiving oldest kept sprints",
    "should return empty result for empty .deckent dir",
    "should handle gate.json in retention window correctly",
    "should clean counters + migrate forensic + enforce retention in one call",
    "should handle null completedSprintId gracefully",
    "should work with default config",
    "should handle non-existent .deckent directory",
    "should not archive pre-archive integrity (hash file stays with tar)",
    "should sort sprints numerically not lexicographically"
   ],
   "orchestra/sprint-finalizer-attempt-custody.test.ts": [
    "uses the Store-revalidated T11 receipt instead of conflicting public verdicts",
    "retains exact custody digests but blocks terminal receipt publication for an unminted current-shaped authority",
    "rejects a callback envelope that pairs the valid terminal wrapper with a forged verdict",
    "keeps the sprint unresolved when exact terminal revalidation is on HOLD"
   ],
   "orchestra/sprint-finalizer-retro-hook.test.ts": [
    "(a) writes sprint-log + retro + mem rows when invoked from finalize path",
    "(b) is idempotent — re-running for the same sprint upserts without duplicates",
    "(c) auto-creates memory.db when caller passes createIfMissing — chronic gap closure",
    "lands canonical sprint-log + retro + mem rows when finalize never ran",
    "is idempotent — second backfill upserts existing rows"
   ],
   "orchestra/sprint-finalizer-row-upsert.test.ts": [
    "happy path — inserts canonical sprint-log row with full payload",
    "idempotent — second call upserts the same row (no duplicate)",
    "defensive minimal payload — writes row when metrics omitted (halted sprint)",
    "race condition — two sequential upserts collapse to one row",
    "canonical id derivation — strips non-digits from sprint id"
   ],
   "orchestra/sprint-finalizer-runflow.test.ts": [
    "counts DONE/GO_WITH_TECH_DEBT/NO_GO verdicts correctly",
    "omits flowId when not provided (current callers pass none)",
    "includes flowId when provided by a run-flow-v2 caller",
    "builds an ordered task-summary array with title + evaluation + selfAssessment",
    "carries per-task file/test evidence from the result map (SURF-3 result-evidence)",
    "falls back to the evaluation string when no result is present for a task (never throws)",
    "returns an empty-but-valid record for a sprint with no evaluations"
   ],
   "orchestra/sprint-finalizer-stale-gate.test.ts": [
    "re-evaluates a stale failure, preserves it forensically, and keeps a product-green run complete",
    "never accepts stale success and keeps a fresh failure fail-closed",
    "reuses an exact-input failure without ignoring it"
   ],
   "orchestra/sprint-finalizer-task-projection.test.ts": [
    "durably projects every attempt to the receipt logical winner before publishing",
    "holds a rejected COMPLETE receipt before mutating task terminal projections",
    "keeps exact task truth when a later recovery generation aborts the outer run",
    "fails closed on a foreign generation instead of blind rewriting or publishing"
   ],
   "orchestra/sprint-finalizer-terminal-publication.test.ts": [
    "publishes COMPLETE for settled DONE, policy-skip, and cascade-skip lineages",
    "refuses COMPLETE for a genuine unrepaired worker NO_GO",
    "refuses COMPLETE for zero-task and evidence-hold shapes"
   ],
   "orchestra/sprint-finalizer-terminal-wire.test.ts": [
    "projects exhausted NOT_DISPATCHED as a settled failed lineage, not a receipt HOLD",
    "folds original and FIX attempts into one finite logical denominator while retaining attempt evidence and lineage usage",
    "treats a host pre-dispatch rejection as exact zero-work attempt evidence resolved by a later FIX",
    "reconciles the reserved legacy honestfail zero-work shape without consulting worker notes",
    "loads runtime-born FIX tasks from disk before final lineage folding",
    "returns finite zero metrics for an empty sprint truth",
    "wires the producer once into the existing finalizer and feeds KPI, rich output, and job consumers from that truth",
    "binds terminal skill attribution to the persisted semantic receipt while ignoring writtenAt replay drift",
    "authorizes cleanup from a published receipt and carries the settled metrics forward",
    "holds a settled NO_GO sprint instead of fabricating cleanup authority",
    "holds when no receipt was ever published",
    "never swallows a finalizer failure or an absent metrics outcome",
    "holds a malformed or foreign receipt rather than trusting it",
    "holds a receipt whose evidence still carries terminal holds",
    "claims the publication once and holds every replay of the same receipt",
    "treats a re-published receipt under a fresh generation as a new publication",
    "surfaces a HOLD as a typed recoverable DECKENT_E091 error",
    "resolves the receipt before CLEANUP and publishes COMPLETE only after it",
    "hands off exactly once and drops the old post-cleanup finalize check",
    "leaves PID and sprint-state authority intact on the HOLD path"
   ],
   "orchestra/sprint-finalizer.test.ts": [
    "should return 0 violations (stub)",
    "should be call-safe with any arguments",
    "should return false when no results have rubric scores",
    "should return false for empty results",
    "should return all-PASS result (stub)",
    "should have correct SelfAuditResult shape",
    "should accept valid options",
    "should return GO_WITH_GATE_FAILURE when gate is GATE_FAILURE",
    "should leave status unchanged when gate is PASS",
    "should leave status unchanged when gate is WARNING (metrics missing is not fail)",
    "generateLoadReport returns a report with a wave timeline section",
    "generateLoadReport errors propagate to its on-demand caller",
    "generateLoadReport returns a minimal report when no metrics data is available",
    "runSelfAuditGate returns valid JSON with all required fields",
    "overallGate field roundtrip: PASS gate serializes and deserializes correctly",
    "gate.json write failure does not affect sprint status (fail-safe)",
    "publishes COMPLETE with distinct completed, policy-skip, and cascade-skip counts",
    "still refuses COMPLETE for an unrepaired worker NO_GO",
    "finalizeSprint writes gate.json to .deckent/recently-works/ after runSelfAuditGate",
    "finalizeSprint does not write a product-doc load report",
    "holds when fresh gate authority cannot be persisted",
    "fails the outcome gate before running downstream code diagnostics",
    "never mutates a NO_GO verdict, even when the probe reports verified=true",
    "preserves NO_GO and fails closed when the probe itself throws (fail-safe diagnostics)",
    "calls archiveDirectives with projectRoot and sprintId during finalizeSprint",
    "never archives a zero-task sprint (fail-closed; was: called even when no tasks)",
    "holds when canonical DIRECTIVES archival fails",
    "gate.json is always written even when runSelfAuditGate succeeds",
    "gate.json is written with fallback content when runSelfAuditGate throws",
    "holds when the fresh gate cannot persist",
    "spawnSync in runSelfAuditGate does not use shell: true (ADR-006 compliance)",
    "emits SPRINT_PHASE_CHANGE EXECUTE→EVALUATE at the start of finalizeSprint",
    "emits SPRINT_PHASE_CHANGE EVALUATE→RETRO before writing RETRO.md",
    "emits SPRINT_PHASE_CHANGE RETRO→CLEANUP at the end of finalizeSprint",
    "emits all 3 SPRINT_PHASE_CHANGE events in correct order",
    "emits METRIC_EMITTED with sprint.summary after metrics calculation",
    "METRIC_EMITTED payload includes coveragePercent field",
    "emits GATE_COMPUTED after gate.json is written",
    "GATE_COMPUTED payload includes tscStatus and vitestFail fields",
    "GATE_COMPUTED is NOT emitted when gate.json write fails",
    "does not emit LOAD_REPORT_WRITTEN during finalization",
    "a non-terminal writeEvent I/O failure does not crash finalizeSprint (fail-safe)",
    "creates 3 triple-link relations during finalizeSprint",
    "closes the MemoryStore after triple-link insertion",
    "triple-link is fail-safe — finalizeSprint continues even on MemoryStore error",
    "holds before archive/triple-link when terminal KPI storage is unavailable",
    "calls runPostFinalizeHooks during finalizeSprint",
    "passes onRuleRegen callback from FinalizeSprintOptions",
    "passes skipMemoryExport and skipIdentityRegen options",
    "derives Turkish memory-export labels from the resolved project config",
    "finalizeSprint continues when post-finalize hooks fail (fail-safe)",
    "post-finalize hooks run AFTER job summary (step 13) and BEFORE RETRO→CLEANUP event",
    "metrics passed to hooks match calculated sprint metrics",
    "is callable with primitives only — no sprint object needed (mode-independent)",
    "forces decay when the budget is OVER, normal decay when OK",
    "never throws — an auditBrainBudget failure is swallowed (fail-safe)"
   ],
   "orchestra/sprint-history-richness.test.ts": [
    "strips a leading \"NNN-NNN — \" slot-id prefix from the title",
    "strips the title prefix even when it differs from task.id (auto-debt offset drift)",
    "returns the title unchanged when there is no slot-id prefix",
    "does not strip a date-like \"YYYY-MM-word\" token with no spaced separator",
    "falls back to the task id when the title is empty",
    "appends the clean task label after the decision",
    "stays parser-safe: parseTaskOutcomes reads the enriched line correctly",
    "keeps the sprint learnings header",
    "lists NO_GO and tech-debt tasks as problems",
    "adds a Gains section naming DONE deliverables with their notes",
    "orders problems before gains",
    "falls back to the task id when the title is missing — never \"- undefined:\"",
    "produces a non-empty Gains section even when all tasks are DONE",
    "caps the Gains list and summarizes the remainder",
    "preserves ## Gains and keeps adjacent sprints separate",
    "lists Delivered lines for DONE tasks using clean labels",
    "caps Delivered lines at 5"
   ],
   "orchestra/sprint-log-projection.test.ts": [
    "complete-after-retro: registry pass no-ops while status is RETROSPECTIVE, then terminal upsert writes COMPLETE",
    "force-abort: upsertSprintLog(ABORTED) writes the ABORTED section for a sprint that never went through retro",
    "idempotent on double invocation: exactly one section survives, no duplication",
    "preserves unrelated sections byte-for-byte across repeated upserts and never becomes settlement authority derivation input",
    "atomic write: no partial-write temp file is left behind in docs/"
   ],
   "orchestra/sprint-metrics-guards.test.ts": [
    "returns durationMs=0 when end < start (negative arithmetic guarded)",
    "returns coverageRatio=null when totalLines=0 (NaN division guarded)",
    "returns correct coverage ratio for normal case (totalLines > 0)"
   ],
   "orchestra/sprint-metrics-unevaluated.test.ts": [
    "counts delivered-but-unjudged results instead of reporting them as nothing",
    "never promotes a worker self-assessment into the completed count",
    "reports zero unevaluated when every result was judged",
    "counts only the unjudged subset on a partially evaluated run"
   ],
   "orchestra/sprint-metrics.test.ts": [
    "returns noGoRate=0.2 for 1 NO_GO out of 5 tasks (not 20)",
    "returns noGoRate=0 when all tasks are DONE",
    "returns noGoRate=1.0 when all tasks are NO_GO",
    "returns noGoRate=0.6 for 3 NO_GO out of 5 tasks",
    "does NOT suggest AI planning for 20% NO_GO rate (0.2 fraction)",
    "DOES suggest AI planning for 60% NO_GO rate (0.6 fraction)",
    "does NOT suggest AI planning for 0% NO_GO rate (0 fraction)",
    "20% NO_GO sprint does NOT trigger AI planning suggestion (pre-fix-red)",
    "60% NO_GO sprint triggers AI planning suggestion with percentage display",
    "counts a task that wrote a file outside its filesWrite scope (was hardcoded 0)",
    "does not count a task that stayed within its filesWrite scope",
    "reports totalTasks from sprint.tasks even when evaluations is empty",
    "partial evaluations still count the full task list",
    "does not count an injected fix attempt as a second task",
    "settles root + fix + fix-fix as one successful logical task"
   ],
   "orchestra/sprint-notification-truth.test.ts": [
    "drops failed-attempt dispatch evidence before a later zero-work SPAWN decision",
    "projects owner start only from actual evidence and reports exact releases separately",
    "has no FIX projection before the repair-dispatch callback supplies evidence",
    "uses a HOLD/no-worker message for zero dispatch in %s",
    "reports admitted dispatch evidence without raw runtime detail in %s"
   ],
   "orchestra/sprint-phases-ci-intersection.test.ts": [
    "detects overlap when task changed the file with tsc error",
    "detects overlap when task changed one of multiple error files",
    "detects overlap when task changed all error files",
    "returns false when task changed unrelated files",
    "returns false when task changed no files",
    "returns false when tsc output is empty (no errors)",
    "returns false when tsc output has no error lines",
    "handles duplicate tsc errors for the same file",
    "is case-sensitive for file paths",
    "handles Windows-style backslash paths in tsc output",
    "task A (changed config.ts) → responsible",
    "task B (changed sprint-phases.ts) → responsible",
    "task C (changed unrelated files) → NOT responsible"
   ],
   "orchestra/sprint-phases-overlap.test.ts": [
    "detects overlap when 2 workers changed the same file",
    "detects multiple overlapping files across workers",
    "includes all worker IDs for a 3-way overlap",
    "returns empty when workers changed disjoint files",
    "returns empty for a single worker (no other worker to overlap with)",
    "returns empty when results list is empty",
    "returns empty when workers changed no files",
    "workers with empty filesChanged contribute no overlaps",
    "overlap check short-circuits when fewer than 2 workers have filesChanged"
   ],
   "orchestra/sprint-phases-rollback.test.ts": [
    "cleans orphan safety point from previous sprint before creating new one",
    "warns and skips safety point when not in a git repo",
    "propagates stash pop failure as a hard error (BULGU 3)"
   ],
   "orchestra/sprint-phases-synthetic-gate.test.ts": [
    "(a) no .result + no disk evidence → returns base synthetic NO_GO unchanged (legacy preserved)",
    "(b) no .result + tracked diff (linesAdded=85) → reclassified result with disk notes",
    "(c) no .result + untracked file → reclassified with filesChanged from disk-verify",
    "(d) DISK_VS_CLAIM_MISMATCH channel constant is the documented value",
    "(e) Sprint 196 196-005 token-counter.ts scenario → reclassified instead of synthetic NO_GO",
    "preserves cause string in the enriched notes for forensic traceability"
   ],
   "orchestra/sprint-phases.test.ts": [
    "reproduces 483-003: zero diff + testsPassed=false + explicit NO_GO stays NO_GO and repair-eligible",
    "permits legitimate spurious-NO_GO recovery with exact scoped artifact and criterion proof",
    "does not reinterpret unused WRITE authority as a missing mandatory artifact",
    "keeps typed PAUSE status and HOLD evidence distinct while both remain non-promotable",
    "projects a recovered doc root through the canonical audit adapter",
    "rejects a repair candidate for a fix-ineligible host settlement"
   ],
   "orchestra/sprint-pid-manager.test.ts": [
    "should write a PID file with current process PID",
    "returns and persists one exact coordinator lifetime authority",
    "wires the exact PID authority into both production snapshot paths",
    "projects resume snapshot identity only from the exact PID authority",
    "should overwrite PID file if previous process is dead",
    "should throw if the existing PID belongs to a live process",
    "should return null when no PID file exists",
    "should return null for malformed PID file",
    "should write snapshot atomically (no .tmp file left behind)",
    "should return null when no PID file exists",
    "should return null when the coordinator process is alive",
    "should return OrphanInfo for a dead coordinator process",
    "should include last snapshot in OrphanInfo when available",
    "releases the PID file but RETAINS the snapshot by default",
    "removes both only when a settlement authority passes dropSnapshot",
    "should not throw if files do not exist",
    "should move orphan artifacts to the canonical sprint namespace",
    "should return true for current process",
    "should return false for a fake dead PID",
    "should list sprint IDs with PID files",
    "should return empty array when no pids directory exists",
    "should return null when no snapshot exists",
    "SIGTERMs an owned, alive sprint process",
    "does NOT signal when no PID is recorded",
    "does NOT signal a recorded-but-dead process",
    "proves graceful SIGTERM exit before reporting terminated",
    "escalates after grace and proves SIGKILL exit",
    "holds without signalling when ownership is unverified",
    "does not claim success while the PID remains alive after SIGKILL"
   ],
   "orchestra/sprint-planner-debt-injection.test.ts": [
    "(a) inheritance: originScope on debt is copied into the fix task scope",
    "holds when the residual scope can write a trusted verifier asset",
    "(b) skip: class=verified-no-result debt produces no fix task",
    "holds a directory residual whose inherited contract verifier is writable",
    "(c) legacy debt remains open as an explicit hold (no broad fallback)",
    "non-CRITICAL debts are ignored entirely",
    "resolved debts are skipped (no fix task generated)"
   ],
   "orchestra/sprint-planner-orphan-cleanup.test.ts": [
    "(a) unlink: files for the current sprint not in newTaskIds are removed",
    "(b) dryRun: returns paths but does not unlink any file",
    "(c) cross-sprint isolation: files from other sprints are never touched",
    "no-op when .tasks/ directory is missing (re-plan before any task write)",
    "ignores non task-*.json files (e.g. .hb, .result) when scanning"
   ],
   "orchestra/sprint-recovery-adapter.test.ts": [
    "declares only inspect + settle — resume/abort/terminate stay CLI-owned (async death-proof)",
    "never hardcodes platform — honors whichever platform the caller passes",
    "is a pure mapping from native evidence to ExecutionRecoveryInput (no I/O, no deps call)",
    "omits resumePermitRef/finalizePermitRef when not present on the native evidence",
    "applies all three housekeeping steps for the exact sprint id and reports success",
    "attempts every housekeeping step but reports a typed failure when one fails",
    "refuses the \"%s\" capability without calling any housekeeping dependency",
    "a real SAFE_TO_FINALIZE outcome derives exactly one settle effect the adapter can apply",
    "applyFencedEffect rejects a foreign identity before ever invoking the housekeeping deps",
    "derives orphan inspection plus abort while a base adapter honestly leaves abort unsupported"
   ],
   "orchestra/sprint-recovery-checkpoint-preservation.test.ts": [
    "reports the same digest-bound preservation disposition in dry-run and apply",
    "keeps task residue archive behavior and emits its policy manifest",
    "keeps the evidence-only process snapshot while checkpoint supersession is pending",
    "fails closed when source bytes drift after manifest authorization"
   ],
   "orchestra/sprint-recovery-operation.test.ts": [
    "routes partial retention through the exact approved boundary without generic mutation",
    "dry-run inspects only the exact dispatch without approval, archive or generic custody scan",
    "requires matching approval and preserves artifacts after exact fenced publication",
    "vetoes %s changes immediately before publication",
    "vetoes a process appearing after precheck without delivering a signal or calling the backend",
    "refuses the same sprint live coordinator in %s without containment or backend calls",
    "refuses released-unaccepted retention without a canonical failed flow, even in dry-run",
    "binds released-unaccepted apply to the exact failed Flow without generic settlement",
    "uses the distinct exact branch and preserves the unresolved disposition in dry-run",
    "publishes only the derived canonical status after retained apply, without generic recovery settlement",
    "vetoes a newly-live authority after retained apply before publishing status",
    "settles only after the exact coordinator is observed dead after SIGTERM",
    "reconciles exact reservation-only custody only after coordinator death and approval validation",
    "counts exact started attempts in preview and blocks generic mutation before containment",
    "reports admission-graph holds separately and counts canonical quarantine settlement",
    "resolves containment timings from project effective config when no policy is injected",
    "contains for finalize without archiving evidence before finalizeSprint owns settlement",
    "revalidates the durable PID fence before SIGKILL and HOLDs without cleanup",
    "revalidates recovery generation evidence after SIGTERM and before SIGKILL",
    "returns typed HOLD for %s without signalling",
    "HOLDs when SIGKILL cannot prove death",
    "returns HOLD before reserving authority when coordinator death is unverified"
   ],
   "orchestra/sprint-reporter-agent.test.ts": [
    "groups tasks by assignedAgent",
    "uses agentMap when provided instead of task.assignedAgent",
    "defaults to generic when no assignedAgent and no agentMap",
    "calculates avgCoverage from results",
    "returns 0 avgCoverage when no results",
    "sorts rows by task count descending",
    "returns empty array for sprint with no tasks",
    "returns empty array for empty rows",
    "generates markdown table with header",
    "generates correct row format",
    "generates multiple rows",
    "includes agent performance table in the retro entry",
    "includes agent performance using agentMap",
    "writes the retro entry without an agent section when no tasks have agents"
   ],
   "orchestra/sprint-reporter-ci.test.ts": [
    "returns empty trend when .brain dir does not exist",
    "returns stable trend with single report",
    "detects increasing test count trend",
    "detects decreasing test count trend",
    "detects increasing coverage trend",
    "detects decreasing coverage trend",
    "totals regressions across all sprints",
    "limits entries to maxSprints",
    "skips malformed report files gracefully",
    "returns empty array for null report",
    "includes CI Health header",
    "shows PASS for tsc and build when both pass",
    "shows FAIL for tsc and build when they fail",
    "shows regression count correctly",
    "shows 0 regressions label correctly",
    "shows coverage with delta sign",
    "does nothing when CI report does not exist",
    "does nothing when CI report does not exist (no store)",
    "upserts retro entry into DB when CI report exists and store is provided",
    "validates DB entry content contains CI Health section",
    "is idempotent — calling twice results in only 1 DB entry (no duplicate)",
    "is a no-op when store is undefined — does not throw",
    "is a no-op when CI report does not exist — does not write to DB",
    "DB entry tags include ci-health and retro",
    "upserts ci-learnings-latest memory entry when reports exist",
    "is a no-op when store is undefined — does not throw",
    "is idempotent — calling twice upserts, not duplicates"
   ],
   "orchestra/sprint-reporter-lineage-summary.test.ts": [
    "keeps one logical outcome distinct from its original and FIX attempts",
    "retains typed invalid progress and unavailable coverage instead of NaN or repaired totals"
   ],
   "orchestra/sprint-reporter-liveness.test.ts": [
    "counts NEVER_DISPATCHED and TIMEOUT_EXTEND events from the stream",
    "returns zero counts when the event stream file is missing",
    "ignores unrelated channels when aggregating liveness counts",
    "formats markdown with the \"Liveness Stats\" heading and pluralised counts",
    "renders the section with singular noun when a count is exactly 1",
    "still renders the heading when both counts are zero"
   ],
   "orchestra/sprint-reporter-skill.test.ts": [
    "returns empty array when no skillMap provided",
    "returns empty array when skillMap is empty",
    "counts DONE tasks correctly",
    "counts GO_WITH_TECH_DEBT correctly",
    "counts NO_GO correctly",
    "handles task with multiple skills",
    "sorts by task count descending",
    "uses task.assignedSkills as fallback when skillMap entry missing",
    "returns empty array for empty input",
    "returns markdown table with correct headers",
    "includes data rows with avgCoverage",
    "handles multiple rows",
    "includes skill performance section when skillMap is provided",
    "does not include skill performance section when skillMap is undefined",
    "skill section appears after agent section in retro",
    "skill performance table has correct data"
   ],
   "orchestra/sprint-reporter-usage.test.ts": [
    "formats the row with total cost, per-task cost, boot-cw share, and hit-rate",
    "returns null when no records are available (no transcript)",
    "returns null when ledger throws — retro must not be blocked",
    "returns null when total limitCost is zero",
    "handles zero cacheWrite gracefully (boot-cw %0)",
    "handles taskCount=0 without division error (per-task $0.00)",
    "appends cache-gate PASS when evaluateGate returns applicable+pass",
    "appends cache-gate FAIL when evaluateGate returns applicable+not-pass",
    "omits cache-gate field when gate is not applicable (single session)",
    "omits cache-gate and does not throw when evaluateGate throws",
    "shows hit-rate %0 when cacheRead is zero",
    "scopes the row to the sprint — other sprints\\' burn is excluded",
    "accepts the bare sprint number form (\"281\")",
    "returns null when no transcript session maps to the sprint",
    "returns null when the task-map builder throws — retro must not be blocked",
    "evaluates the cache-gate against the sprint-scoped sessions by default"
   ],
   "orchestra/sprint-reporter.test.ts": [
    "returns all lines joined when under maxLines",
    "returns all lines joined when exactly at maxLines",
    "preserves first 10 header lines and trims middle when over max",
    "handles empty array",
    "handles maxLines of 0",
    "handles maxLines smaller than header size (5 < 10)",
    "preserves header with real MEMORY.md format",
    "handles single-line array under maxLines",
    "maxLines equal to header size keeps only first 10 lines",
    "large excess: trims correctly, keeps header + tail only",
    "creates .brain/sprints/ directory if missing",
    "writes sprint log file named <sprint-id>.md",
    "sprint log contains sprint ID header",
    "sprint log includes metrics table",
    "sprint log includes tasks section with task statuses",
    "uses task.status when evaluations map not provided",
    "truncates output to SPRINT_LOG_MAX_LINES",
    "handles sprint with no tasks gracefully",
    "overwrites previous sprint log file on second write",
    "counts DONE evaluations correctly",
    "counts GO_WITH_TECH_DEBT as both completed and techDebt",
    "calculates totalTasks from evaluations map size",
    "calculates average coverage from results",
    "returns 0 coverage when no results",
    "calculates noGoRate as a fraction in [0,1]",
    "returns 0 noGoRate when no evaluations",
    "calculates durationMs from sprint startedAt and completedAt",
    "uses Date.now() when startedAt/completedAt are missing",
    "counts resolvedDebtCount from matching sprint debt items",
    "returns 0 resolvedDebtCount when no debt provided",
    "newDebtCount equals techDebtTasks",
    "boundaryViolations, crossAssignments, contextLinesUsed default to 0",
    "handles all tasks as NO_GO",
    "handles single result coverage",
    "returns an array of DocUpdateResult objects",
    "works without providing a config (uses default)",
    "works with an explicit config",
    "detects isInternalProject when DECKENT-MASTER-BLUEPRINT.md exists",
    "does not throw when projectRoot has no special files",
    "each result has file and updated fields",
    "writeRetrospective handles malformed startedAt/completedAt gracefully in metrics display",
    "calculateMetrics with empty evals and results returns zeroed metrics",
    "writeRetrospective and writeSprintLog can be called together without conflicts",
    "trimMemoryWithHeader with exactly MEMORY_HEADER_LINES (10) lines and maxLines > 10",
    "calculateMetrics coverage average with many results",
    "calculates positive durationChangePct when current is slower",
    "calculates negative durationChangePct when current is faster",
    "returns 0 durationChangePct when previous duration is 0",
    "calculates noGoRateChange as signed difference",
    "calculates negative noGoRateChange when improved",
    "calculates testCountDelta as difference in totalTasks",
    "calculates coverageDelta as signed percentage difference",
    "calculates completedTasksDelta",
    "calculates techDebtTasksDelta",
    "returns all zeros when metrics are identical",
    "returns 0 durationChangePct when both durations are equal and nonzero",
    "handles negative completedTasksDelta when fewer tasks completed",
    "handles negative techDebtTasksDelta when less debt",
    "handles negative testCountDelta when fewer total tasks",
    "handles large positive durationChangePct (10x slower)",
    "returns structured object with all required comparison fields",
    "returns null when sprints directory does not exist",
    "returns null when no sprint files exist",
    "returns null when only current sprint file exists",
    "reads metrics from previous sprint log",
    "picks the most recent previous sprint by sort order",
    "returns null for malformed sprint log content",
    "returns null for sprint log with only the header row and no data",
    "excludes current sprint even when it sorts last alphabetically",
    "correctly derives noGoRate from parsed noGoTasks and totalTasks",
    "summary includes task count and time",
    "summary shows self-healing rate when results have feedbackLoop",
    "includes Highlights section when tasks complete on first try",
    "includes Issues section for NO_GO tasks",
    "includes readable Metrics table",
    "includes Learnings section for NO_GO and tech debt tasks",
    "includes code change stats when results provided",
    "shows sprint time in human format",
    "omits Highlights section when nothing went well",
    "omits Issues section when no issues",
    "omits Learnings section when all tasks DONE",
    "title contains sprint ID",
    "includes agent performance when agentRows provided",
    "includes skill performance when skillRows provided",
    "reports first-try tasks",
    "reports self-healed tasks",
    "reports no boundary violations",
    "reports NO_GO rate improvement vs previous sprint",
    "lists NO_GO tasks",
    "includes notes from result in issue description",
    "reports tasks with many retries",
    "reports boundary violations",
    "returns empty when no issues",
    "generates learning for NO_GO task",
    "generates learning for tech debt task",
    "adds low self-healing insight when rate < 50%",
    "returns empty when all tasks DONE",
    "limits learnings to 10 items max",
    "returns empty string for undefined",
    "returns empty string for 0",
    "returns seconds for < 60s",
    "returns minutes for < 60min",
    "returns minutes with seconds when not exact",
    "returns hours and minutes for >= 60min",
    "returns null for empty results",
    "returns null for undefined",
    "returns null when no results have feedbackLoop",
    "returns null when no retries attempted",
    "calculates 100% when all retries succeeded",
    "calculates 0% when all retries failed",
    "calculates partial rate correctly",
    "counts GO_WITH_TECH_DEBT as healed",
    "returns 0 for undefined",
    "counts tasks with no retries as first-try",
    "excludes NO_GO tasks",
    "excludes tasks with retries",
    "counts DONE tasks without feedbackLoop as first-try",
    "returns 0 for undefined",
    "counts tasks that had retries but succeeded",
    "excludes NO_GO tasks",
    "excludes tasks without feedbackLoop",
    "reports first-try tasks",
    "reports self-healed tasks",
    "reports no boundary violations",
    "reports NO_GO tasks",
    "reports tasks with many retries",
    "includes notes from NO_GO result",
    "includes sprint number in title",
    "shows results with succeeded count",
    "shows time duration",
    "shows code stats from results",
    "shows What went well section",
    "shows What needs attention for NO_GO tasks",
    "shows self-healing rate",
    "shows Next steps section",
    "shows debt status link when open debt exists",
    "returns 0 for undefined",
    "returns 0 for empty results",
    "counts .test.ts files",
    "counts .spec.ts files",
    "counts .test.tsx and .test.js files",
    "deduplicates test files across results",
    "excludes non-test files",
    "includes new test files row when test files present",
    "omits new test files row when no test files",
    "includes recurring pattern when occurrences >= 2",
    "excludes resolved patterns",
    "excludes patterns with only 1 occurrence",
    "includes open HIGH priority debt",
    "includes open CRITICAL priority debt",
    "excludes resolved debt",
    "excludes NORMAL/LOW priority debt",
    "limits patterns to 3 max",
    "extracts number from sprint-042",
    "extracts number from sprint-001",
    "extracts number from sprint-100",
    "returns null for invalid sprint ID",
    "returns null for empty string",
    "skips non-fix tasks (isPriorityFix=false)",
    "skips tasks without fixForTaskId",
    "returns 0 when no matches found",
    "returns 0 when DEBT.md does not exist",
    "returns 0 when DEBT.md is empty",
    "skips fix tasks with NO_GO evaluation",
    "returns 0 when git diff fails (not a git repo)",
    "returns 0 when no new files under src/",
    "creates ADR for a truly new directory under src/",
    "ADR numbering continues from last existing ADR",
    "skips existing directories (with pre-existing files)",
    "creates DECISIONS.md if it does not exist",
    "ADR entry contains correct format fields",
    "extracts number from sprint-048",
    "extracts number from sprint-001",
    "extracts number from sprint-100",
    "returns null for invalid format",
    "handles sprint-0 edge case",
    "includes worker notes (first 150 chars) for NO_GO task",
    "includes worker notes (first 150 chars) for GO_WITH_TECH_DEBT task",
    "truncates worker notes to 150 chars",
    "falls back to generic message when no notes provided",
    "adds ## Notes section when tasks have result notes",
    "includes notes for DONE tasks, not just NO_GO",
    "omits ## Notes section when no tasks have notes",
    "truncates notes to 150 chars in sprint log",
    "returns empty array when skillMap is undefined",
    "returns empty array when skillMap is empty",
    "counts tasks and done/debt/noGo per skill",
    "calculates avgCoverage from results",
    "sets avgCoverage to 0 when no results provided",
    "sorts rows by task count descending",
    "returns empty array for empty rows",
    "includes ## Skill Performance header",
    "renders avgCoverage in the table",
    "renders column headers with Avg Coverage",
    "formats numbers under 1000 as-is",
    "formats thousands with K suffix",
    "formats millions with M suffix",
    "returns empty array when results is undefined",
    "returns empty array when results is empty",
    "returns empty array when no results have tokenUsage",
    "builds table with token usage data",
    "handles missing cacheReadTokens gracefully",
    "handles missing model gracefully",
    "only includes results that have tokenUsage",
    "includes Token Usage section when results have tokenUsage",
    "omits Token Usage section when no tokenUsage in results",
    "omits Token Usage section when results is undefined",
    "returns empty array when results is undefined",
    "formats correct quality dimensions table for a task",
    "calculates sprint average from multiple tasks",
    "is included in formatHumanRetro output when results have matching tasks",
    "omits quality dimensions from formatHumanRetro when no results exist"
   ],
   "orchestra/sprint-result-authority-wire.test.ts": [
    "loads checkpoint-resume results through the host settlement authority",
    "loads post-collect late results through the same authority",
    "gates grace and EVALUATE on settled result authority before synthetic paths",
    "evaluates collected results before dependency dispatch and reuses the durable verdict",
    "makes EVALUATE authority failure terminal and checks RETRO before mutation"
   ],
   "orchestra/sprint-retro-writer-forensic.test.ts": [
    "inserts a type=sprint row when writeRetrospective runs",
    "is idempotent — multiple calls upsert a single sprint row"
   ],
   "orchestra/sprint-retro-writer.test.ts": [
    "fraction 0.5 → \"50%\" (not \"1%\"/\"0%\")",
    "fraction 0.6 → \"60%\" in the metrics table",
    "writes retro entry with canonical retro-sprint-NNN id",
    "does not create retro-latest entry",
    "soft-deletes legacy retro-latest entry when it exists",
    "is a no-op when retro-latest does not exist",
    "does not affect sprint-specific retro entries",
    "returns the retro with the highest sprint_num",
    "returns null when no retro entries exist",
    "excludes soft-deleted retro entries",
    "returns WriteRetrospectiveResult with all three rows persisted when DB is healthy",
    "reports dbAttempted=false and no error when memory.db is missing (clean skip)",
    "surfaces dbError when the DB write step throws (Sprint 189 RC regression guard)",
    "writes sprint-log, retro, and memory rows in a single invocation (triple-row contract)",
    "treats a pre-existing mem entry as success (idempotent re-finalize)",
    "passes when ## Project Status heading immediately precedes identity-status AUTOGEN block",
    "fails when a manual Project Status table sits between the heading and the AUTOGEN block (Sprint 189 drift)",
    "fails when MCP Tools count in identity-status drops below the registered minimum (31)"
   ],
   "orchestra/sprint-runner-notify-lifecycle.test.ts": [
    "shares concurrent close and tears down only after terminal delivery"
   ],
   "orchestra/sprint-runner-pid-snapshot-retention.test.ts": [
    "removes the .pid but KEEPS the snapshot as correlation evidence",
    "reports the sprint it released",
    "never touches a pid file owned by another process",
    "is a no-op when there are no pid files at all"
   ],
   "orchestra/sprint-spawner-throttle.test.ts": [
    "spawns immediately when token_throttle_ms is 0 (no inter-worker delay)",
    "inserts at least token_throttle_ms between consecutive spawn calls",
    "does not delay the first spawn (only inter-worker pacing)"
   ],
   "orchestra/sprint-spawner.test.ts": [
    "should detect no collisions when tasks write to different files",
    "should detect collision when two tasks write to the same file",
    "should detect multiple collisions",
    "should handle three-way collision",
    "should handle tasks with no filesWrite",
    "should handle empty task list",
    "should return empty waves for empty tasks",
    "should put non-colliding tasks in same wave",
    "should separate colliding tasks into different waves",
    "should respect existing dependencies alongside collisions",
    "should respect maxWorkers limit per wave",
    "should handle sprint 138 meta-dogfood scenario (Task 5+6 shared file)",
    "should return valid file path unchanged",
    "should return valid directory path unchanged",
    "should preserve .tasks/ directory path",
    "should preserve deeply nested file paths",
    "should remove trailing slash from file paths (DECKENT.md/ → DECKENT.md but then ADR-013 rejects)",
    "should remove trailing slash from .ts files",
    "should NOT remove trailing slash from directory paths (no extension in basename)",
    "should NOT remove trailing slash from hidden directories",
    "should reject extension-only path \".json\"",
    "should reject extension-only path \".ts\"",
    "should reject extension-only path \".md\"",
    "should reject extension-only path \".mjs\"",
    "should reject CLAUDE.md (ADR-013 protected)",
    "should reject DECKENT.md (ADR-013 protected)",
    "should reject DECKENT.md with trailing slash",
    "should reject CLAUDE.md in nested path",
    "should reject DECKENT.md in nested path",
    "should reject empty string",
    "should reject whitespace-only string",
    "should trim surrounding whitespace before processing",
    "should always include .tasks/ as first entry",
    "should include .tasks/ even when other paths are empty",
    "should remove trailing slash from file entries in directories",
    "should keep trailing slash for directory entries",
    "should exclude CLAUDE.md from directories",
    "should exclude DECKENT.md from filesWrite",
    "should exclude DECKENT.md/ (with trailing slash) from directories",
    "should exclude extension-only entries like \".json\"",
    "should deduplicate identical paths",
    "should handle Sprint 138 xfix scenario: DECKENT.md/ + .json invalid entries",
    "should handle combined CLAUDE.md + DECKENT.md protection scenario",
    "defaults to 5 when sprint_checkpoint_interval is not set",
    "returns 3 when sprint_checkpoint_interval is set to 3 (Sprint 139 override)",
    "returns 1 when set to 1 (aggressive checkpoint)",
    "returns 10 when set to 10 (low-risk large sprint)",
    "checkpoint fires at correct terminal counts with interval=3",
    "checkpoint fires at correct terminal counts with default interval=5",
    "CHANNELS.TASK_ASSIGN is the correct Brain→Worker channel",
    "TASK_ASSIGN payload shape includes taskId, workerId, model, agent, skills, scope, provider",
    "CHANNELS.METRIC_EMITTED is the correct broadcast channel",
    "wave.respawn metric payload shape includes required fields",
    "applyUnblockToSprint uses DEPENDENCY_UNBLOCKED not DEPENDENCY_BLOCKED channel",
    "DEPENDENCY_UNBLOCKED payload shape includes transition UNBLOCKED and correct fields",
    "CHANNELS.SPRINT_PHASE_CHANGE is the correct broadcast channel",
    "SPRINT_PHASE_CHANGE payload shape includes fromPhase, toPhase, sprintId, timestamp",
    "CHANNELS.FIX_REQUEST is BRAIN→WORKER:FIX_REQUEST (existing hook)",
    "FIX_REQUEST payload carries failureCategory for cascade decision"
   ],
   "orchestra/sprint-startup-prev-sprint-orphan.test.ts": [
    "archives orphan prompt from previous sprint at startup",
    "is idempotent (safe to call when .tasks/ is empty)",
    "archives multiple orphan prompts together"
   ],
   "orchestra/sprint-state-snapshot.test.ts": [
    "(a) returns IDLE_SNAPSHOT when no sprint-state.json",
    "(b) reads active sprint snapshot",
    "(c) idempotent across calls (frozen IDLE_SNAPSHOT)",
    "(d) excludes heartbeat-only and raw-result workers without host-active evidence"
   ],
   "orchestra/sprint-state-tracker.test.ts": [
    "frozen activity with exact-attempt live host authority is not stale",
    "exact dead attempt produces a stale event",
    "residue heartbeat whose task no longer exists is not an active worker",
    "FINISHED-worker guard intact: .hb with sibling .result → not in activeWorkers",
    "includes a dynamic FIX worker even when it was not in the initial taskIds",
    "completedTasks counts distinct logical tasks, excluding fix-attempt inflation"
   ],
   "orchestra/sprint-terminal-evidence-classifier.mixed-outcome.test.ts": [
    "classifies host policy and cascade skips as settled without hiding their distinction"
   ],
   "orchestra/sprint-terminal-evidence.test.ts": [
    "settles host-confirmed dispatch exhaustion as FAILED without fabricating a worker result",
    "folds a superseding attempt lineage into one completed aggregate",
    "classifies host NOT_DISPATCHED and cascade skips as distinct settled evidence",
    "keeps an ordinary worker NO_GO and fail-closed publication guards blocking",
    "keeps active attempts and partial result payloads visible without manufacturing a HOLD",
    "never treats complete result presence as completion without terminal authority",
    "retains unknown, contradictory, attribution-excluded and coordinator-held evidence",
    "types broken lineage and duplicate exact-attempt evidence as HOLD",
    "distinguishes a cross-logical-lineage parent from absent parent evidence",
    "returns a typed HOLD with non-zero observed count when a configured bound is exceeded",
    "is deterministic across input order and does not mutate caller-owned arrays",
    "demotes a superseded, path-covered exclusion — sprint becomes cleanup-eligible",
    "UNKNOWN claims fail closed — no claimedPaths means the exclusion still blocks",
    "an UNCOVERED claimed path still blocks — nobody accountable owns it",
    "a claimed path covered only by a COMPLETED resolver WRITE-SCOPE demotes the exclusion (CR6 shape)",
    "an UNRESOLVED lineage keeps its exclusion blocking (no verified resolution exists)"
   ],
   "orchestra/sprint-terminal-receipt-order.test.ts": [
    "does not replay ordinary lifecycle events during completed-checkpoint recovery",
    "publishes one generation-fenced receipt carrying the exact logical settlement digest",
    "refuses to publish a settled NO_GO as COMPLETE (typed fail-closed, was: published with BLOCKED eligibility)",
    "publishes test-mode terminal authority without invoking the learning finalizer",
    "wires the reduced test RETRO phase to the fenced terminal receipt",
    "publishes dashboard before the canonical COMPLETE read model so status retains the receipt",
    "wires one receipt publication before archive and guards every cleanup step with receipt evidence",
    "publishes terminal lifecycle authority before the single outer archive seal and only then projects EventBus completion"
   ],
   "orchestra/sprint-terminal-settlement-hold.test.ts": [
    "rejects RETRO before sprint/evaluation/result mutation while Docker settlement is pending",
    "propagates EVALUATE authority HOLD and releases its idempotency lock",
    "wraps corrupt settlement evidence as DECKENT_E077",
    "accepts closed host truth and legacy non-Docker terminal/absent paths",
    "keeps exact-attempt host runtime-budget exhaustion terminal through EVALUATE",
    "does not let a stale runtime-budget marker veto a later settled attempt"
   ],
   "orchestra/sprint-timeout-adaptive.test.ts": [
    "exports adaptive_multiplier === 1.5",
    "exports runtime_extension_max === 5 (raised from legacy hard-coded 3)",
    "createDefaultConfig() surfaces both knobs via timeout",
    "returns the configured multiplier when set to a valid value",
    "falls back to DEFAULT_ADAPTIVE_MULTIPLIER when config is undefined",
    "falls back when value is invalid (< 1.0)",
    "falls back when value is not finite (NaN / Infinity)",
    "accepts the lower bound 1.0 exactly",
    "returns the configured cap when set to a valid integer",
    "falls back to DEFAULT_RUNTIME_EXTENSION_MAX when config is undefined",
    "falls back when value is non-integer",
    "falls back when value is < 1",
    "accepts the lower bound 1 exactly",
    "multiplies a positive base by the configured multiplier and rounds",
    "multiplies by the default 1.5 when config is undefined",
    "returns 0 unchanged (sentinel \"disabled\")",
    "returns a negative base unchanged (sentinel passthrough)",
    "rounds the product (not truncate)",
    "accepts the default adaptive knobs without error",
    "rejects adaptive_multiplier < 1.0",
    "rejects non-finite adaptive_multiplier",
    "rejects runtime_extension_max < 1",
    "rejects non-integer runtime_extension_max"
   ],
   "orchestra/sprint-utils.test.ts": [
    "keeps authored GO and NO_GO while omitting wave verification commands",
    "uses the derived NO_GO fallback when only GO is authored",
    "derives task acceptance without global verification when GO is not authored",
    "caps every authored field at 2000 characters and retains derived verification",
    "keeps explicitly authored Test execution authority out of acceptance criteria"
   ],
   "orchestra/sprint2-debt.test.ts": [
    "returns a Promise (is async, not sync)",
    "Promise resolves to an array",
    "returns empty array immediately for a sprint with no tasks",
    "collects results on first pass without polling when all files exist",
    "exits polling loop when timeout=1 and returns collected partial results",
    "finds result after one poll interval (15 s)",
    "collects all tasks when they appear at different poll intervals",
    "skips tasks whose result files contain invalid JSON",
    "skips tasks whose result files throw on read",
    "does not count the same task result twice",
    "embeds the task ID in the result file path",
    "instructs worker to run vitest",
    "instructs worker to run tsc",
    "marks the result file as mandatory (never exit without it)",
    "references result file fields in condensed format",
    "lists valid selfAssessment values in the prompt",
    "includes scope rules section",
    "instructs worker to write test result to .result file",
    "scope reflects task directories (comma-separated)",
    "preserves single quotes in prompt (tmux handles escaping)",
    "extracts a single src/ file path from \"Dosya: ...\" line",
    "extracts multiple file paths from a single \"Kapsam: ...\" line",
    "extracts test file paths (tests/ prefix)",
    "extracts a src/ directory path",
    "extracts a tests/ directory path",
    "deduplicates repeated file paths in one line",
    "deduplicates repeated directory paths in one line",
    "returns all-empty scope for a comment-style line (#)",
    "returns all-empty scope for a pure prose line",
    "always returns an empty filesRead array",
    "returns all three scope array keys",
    "handles the DEBT-002 scope line from DIRECTIVES.md",
    "handles the DEBT-005 scope line with four files from DIRECTIVES.md",
    "handles the worker prompt scope line from DIRECTIVES.md",
    "has an autoApprove boolean field",
    "has a sandbox boolean field",
    "both fields are optional (empty object is valid)",
    "autoApprove and sandbox are independent flags",
    "autoApprove maps to CLI --dangerously-skip-permissions (not model constraint)",
    "sandbox maps to Docker/isolated run mode (not permission flag)",
    "heap stays below 500MB after all describe blocks (OOM guard)",
    "task-builder pure functions callable without loading SQLite"
   ],
   "orchestra/spurious-no-go-reconciliation.test.ts": [
    "1. heuristic NO_GO salvaged to GO_WITH_TECH_DEBT (NOT clean DONE) when worker DONE + rubric avg≥85 + coverage≥80",
    "1b. evaluateWithRubric end-to-end: 162-003 scenario yields GO_WITH_TECH_DEBT not NO_GO",
    "2. testsPassed=false + worker DONE → NO_GO preserved (concrete test_failed)",
    "3. scope_compliance=70 + worker DONE + tests pass → NO_GO preserved (scope_violation)",
    "4. Worker selfAssessment=NO_GO + high rubric avg → NO_GO preserved (worker priority)",
    "5a. Worker DONE but rubric avg=70 (<85) → NO_GO preserved (threshold)",
    "5b. Worker DONE + rubric avg>=85 but coverage=50 (<80) → NO_GO preserved (coverage threshold)",
    "DONE rubric decision passes through unchanged",
    "GO_WITH_TECH_DEBT rubric decision passes through unchanged",
    "exposes the canonical threshold values"
   ],
   "orchestra/spurious-nogo-169-cascade.test.ts": [
    ".tasks/task-${id}.plan in filesChanged is NOT a boundary violation",
    ".tasks/task-${id}.result protocol file is allowed",
    ".tasks/task-${id}.hb protocol file is allowed",
    "still flags non-protocol .tasks/ files as boundary violations",
    "still flags out-of-scope source files as boundary violations",
    "bug-fixer task with coverage:null does NOT fail schema validation",
    "security-auditor task with coverage:null is accepted",
    "architect task with coverage:null is accepted",
    "generic code-development task with source-only change (no tests) still requires coverage",
    "generic code-development task that wrote a test file is exempt (P0-1 signal-based)",
    "bug-fixer task with coverage:null AND .tasks/*.plan → NOT NO_GO (cascade prevented)",
    "audit task (testsPassed YOK, coverage:null) schema validation GEÇER",
    "171-023 doc-writer audit task da kabul edilir",
    "REGRESYON: code-development task testsPassed YOK ise hâlâ REDDEDİLİR",
    "DEFANS: audit task testsPassed:false açıkça verilirse yine geçer",
    "UÇTAN-UCA: gerçek 171-014 raporu (testsPassed YOK) → schema_validation NO_GO DEĞİL"
   ],
   "orchestra/spurious-nogo.test.ts": [
    "1. TIMEOUT_WITH_WORK + git diff dolu + tsc PASS → GO_WITH_TECH_DEBT",
    "2. TIMEOUT_WITH_WORK + git diff boş → NO_GO unchanged",
    "3. TIMEOUT_WITH_WORK + tsc FAIL → NO_GO",
    "4. NO_GO + git diff > 50 lines + tsc PASS → reconciled GO_WITH_TECH_DEBT",
    "5. NO_GO + git diff < 10 lines → NO_GO unchanged (still reconciled if tsc+vitest pass)",
    "ambient shared-worktree evidence cannot reconcile a NO_GO",
    "6. Scope violation (filesChanged ⊄ scope) → NO_GO + RBAC alert",
    "7. vitest 75% PASS → GO_WITH_TECH_DEBT (notes explain gap)",
    "8. T-144-001 scenario simulate (init split TIMEOUT_WITH_WORK) → reconciled GO_WITH_TECH_DEBT",
    "TIMEOUT_WITH_WORK always returns GO_WITH_TECH_DEBT (no projectRoot)",
    "NO_GO without projectRoot → NO_GO (no reconciliation attempted)",
    "vitest < 50% pass ratio → NO_GO (reconciliation fails)"
   ],
   "orchestra/stale-spawnlock-watchdog.test.ts": [
    "releases only stale dead-owner locks with terminal results and writes typed audit evidence",
    "never touches a live owner lock even when old and terminal",
    "never touches a dead-owner lock without a terminal result",
    "does not release when audit persistence fails",
    "hard-caps each dispatch sweep"
   ],
   "orchestra/state-recovery.test.ts": [
    "1. no checkpoint → returns action:\"fresh\"",
    "2. raw DONE tasks without host evidence → action:\"resume-evaluate\"",
    "3. stale EXECUTING with .result on disk → staleTasksWithResult populated, action:\"resume-evaluate\"",
    "4. stale EXECUTING without .result → task.json overwritten to NO_GO, action:\"resume-evaluate\"",
    "holds restore when a raw DONE is backed by a pending Docker settlement",
    "uses a closed host NO_GO instead of a contradictory raw DONE on restore",
    "5. startedAt preserved from checkpoint (sprintStartedAt > timestamp fallback)",
    "6. state.json synced after restore — phase EVALUATE, sprintId set"
   ],
   "orchestra/state-w1-taxonomy.test.ts": [
    "passes through DONE decisions unchanged",
    "passes through GO_WITH_TECH_DEBT decisions unchanged",
    "out-of-scope file → noGoCategory=BOUNDARY_VIOLATION + filesInScope/Out correct",
    "test-fail (testsPassed=false, correctness=0) → noGoCategory=TECHNICAL",
    "testsPassed=false alone (no correctness rubric) → noGoCategory=TECHNICAL",
    "OOM-killed notes → noGoCategory=RUNTIME_ERROR (takes priority over boundary)",
    "SIGKILL notes → noGoCategory=RUNTIME_ERROR",
    "auth failure notes → noGoCategory=FATAL_ERROR",
    "no signals → noGoCategory=UNKNOWN",
    "filesChanged=[] → filesInScope=[], filesOutOfScope=[], isPartialPromotable=false",
    "auxiliary files (docs/) go into filesInScope, not filesOutOfScope",
    "does not mutate the original evaluation object",
    "DONE decision → no noGoCategory (backward-compatible)",
    "NO_GO from scope violation → noGoCategory=BOUNDARY_VIOLATION",
    "NO_GO from test failure → noGoCategory=TECHNICAL",
    "decision (DONE/GO_WITH_TECH_DEBT/NO_GO) values preserved — backward compat"
   ],
   "orchestra/subproc-provider-cli.test.ts": [
    "constructs SubprocessSpawnBackend with a codex providerConfig (not claude)",
    "produces a `codex exec` command (string-assert) — the born-481 fix",
    "autoApprove appends the codex sandbox-bypass flag only when set",
    "applies codex reasoning-effort via -c model_reasoning_effort=<level>",
    "carries the usage-emit flag ONLY via usageEmitArgs, never inline in buildArgs (dry-run stability)",
    "buildCommandString() also reads `codex exec ... < <promptPath>`",
    "constructs SubprocessSpawnBackend with the exact CLAUDE_SUBPROCESS_CONFIG singleton",
    "buildArgs output is unchanged from CLAUDE_SUBPROCESS_CONFIG directly (byte-identical)",
    "gemini (inline promptFeed, incompatible with this backend) throws SpawnBackendError",
    "gemini error names the reason (inline-arg CLI, stdin-only backend)",
    "ollama (host-only, no ProviderCommandSpec) throws SpawnBackendError",
    "a fully unregistered model id fails before any provider backend is constructed",
    "distinguishes restart-unknown inventory from observed-and-absent workers",
    "keeps a per-task-timeout backend visible to list and kill authority",
    "each provider gets its OWN SubprocessSpawnBackend instance with the right CLI",
    "spawning a SECOND codex task reuses the cached codex backend (no new construction)",
    "list() aggregates workers across every provider backend",
    "kill() routes to the backend instance that actually holds the taskId"
   ],
   "orchestra/surf0-flowid-seam-pin.test.ts": [
    "RunSprintOptions carries the additive correlation fields (432-001)",
    "start.ts forwards --flow-id into the runSprint options object (432-002)",
    "runSprint forwards opts?.flowId into runRetroPhase — the middle hop (CC seam fix)",
    "runRetroPhase accepts flowId and forwards it to finalizeSprint (432-004)"
   ],
   "orchestra/synthetic-nogo-diskverify.test.ts": [
    "(1) unattributed synthetic NO_GO ignores ambient disk evidence",
    "(2) synthetic NO_GO + NO disk evidence → NO_GO stays, no reclassification, no audit emit",
    "(3) normal DONE result (filesChanged populated) → disk-verify NOT invoked, untouched (regression guard)",
    "(4) genuine NO_GO with non-synthetic notes (e.g. worker self-flag) → gate skips, NO_GO stays",
    "(5) claim-time VERIFIED attribution is not replaced by the final shared-worktree diff",
    "(6) .timeout-path regression guard — empty disk + .timeout marker (no .result) → synthetic NO_GO with no reclassification"
   ],
   "orchestra/task-analyzer.test.ts": [
    "infers test type from \"test\" keyword",
    "infers test type from \"spec\" keyword",
    "infers test type from \"coverage\" keyword",
    "infers doc type from \"doc\" keyword",
    "infers doc type from \"readme\" keyword",
    "infers doc type from \"changelog\" keyword",
    "infers doc type from \"guide\" keyword",
    "infers security type from \"security\" keyword",
    "infers security type from \"auth\" keyword",
    "infers security type from \"jwt\" keyword",
    "infers security type from \"csrf\" keyword",
    "infers refactor type from \"refactor\" keyword",
    "infers refactor type from \"rename\" keyword",
    "infers refactor type from \"extract\" keyword",
    "infers refactor type from \"split\" keyword",
    "infers devops type from \"docker\" keyword",
    "infers devops type from \"ci\" keyword",
    "infers devops type from \"deploy\" keyword",
    "infers devops type from \"pipeline\" keyword",
    "infers config type from \"config\" keyword",
    "infers config type from \"settings\" keyword",
    "infers config type from \"env\" keyword",
    "defaults to code for generic text",
    "defaults to code for empty string",
    "sets type from title keywords",
    "sets type from description keywords",
    "defaults to code when no type keywords found",
    "returns low complexity for single directory",
    "returns medium complexity for 4-6 directories",
    "returns high complexity for 7+ directories",
    "boosts complexity for architectural keywords",
    "reduces complexity for trivial keywords",
    "boosts complexity for many filesWrite",
    "clamps complexity to 0-10 range",
    "extracts keywords from title",
    "extracts keywords from description",
    "filters stopwords",
    "deduplicates keywords",
    "returns empty for empty input",
    "still filters task-analyzer action verbs after SSOT collapse (behavior preserved)",
    "filters Turkish stopwords via the EN+TR canonical base (pre-fix RED)",
    "returns 0 for empty scope",
    "increases with more directories",
    "increases with more filesWrite",
    "accounts for filesRead",
    "returns a positive duration",
    "test tasks have shorter base duration than code tasks at same complexity",
    "higher complexity increases duration",
    "doc tasks have short base duration"
   ],
   "orchestra/task-artifact-projection.test.ts": [
    "inspects a fresh exact task without creating the public task directory",
    "CAS-transitions an admitted existing task and retains its exact predecessor",
    "never overwrites a task that changes after exact projection inspection",
    "preserves a writer that lands in the final-check to release race",
    "publishes missing files durably and treats semantic JSON equality as idempotent",
    "preflights every target and creates nothing when any existing payload conflicts",
    "rejects portable filename hazards and case-fold collisions before publication",
    "refuses a symlinked task directory instead of publishing outside the project",
    "binds and idempotently migrates only missing structured criteria",
    "resumes from a durable predecessor without clobbering a recreated target",
    "rejects any legacy drift beyond timestamp retention and additive criteria"
   ],
   "orchestra/task-builder-ollama-flow.test.ts": [
    "accepts \"- Provider: ollama\" (no drop)",
    "passes raw ollama model tag through (not in ALL_MODELS)",
    "fails loudly for claude + unknown cloud model",
    "rejects a retired alias at the input boundary",
    "accepts ollama model tag case-insensitively (input lower-cased)",
    "accepts ollama provider + tag at bullet-list parse site",
    "fails loudly for bullet-list claude + unknown cloud model"
   ],
   "orchestra/task-builder-routing.test.ts": [
    "returns undefined for no line",
    "returns undefined for empty line",
    "parses \"none\" as empty forceSkills",
    "parses \"auto\" as undefined (let auto-select)",
    "parses single skill",
    "parses multiple skills",
    "parses exclude with - prefix",
    "parses mixed include and exclude",
    "parses auto mixed with includes",
    "parses Agent: override from directive",
    "parses Agent: none as generic",
    "parses Agent: auto as undefined (let auto-select)",
    "parses Skills: with includes and excludes",
    "directive without Agent/Skills has undefined overrides"
   ],
   "orchestra/task-builder-skill.test.ts": [
    "includes skill section when skillPrompts are provided",
    "does not include skill section when skillPrompts is undefined",
    "does not include skill section when skillPrompts is empty array",
    "includes multiple skills in section",
    "injects individual skill content in full (Sprint 182 PQ-2 F2: no per-item cap)",
    "injects every skill without total-section cap (Sprint 182 PQ-2 F2)",
    "skill section appears before main task content",
    "skill section appears before agent section when both present",
    "standard prompt unchanged when no agent and no skills",
    "handles skill content with special characters",
    "handles skill content with newlines",
    "drops skills that would exceed total section cap"
   ],
   "orchestra/task-builder.test.ts": [
    "generates id from sprintId and sequence",
    "pads sequence to 3 digits",
    "strips \"sprint-\" prefix from sprintId",
    "uses PENDING status by default",
    "respects initialStatus override",
    "copies all fields from params",
    "sets createdAt to valid ISO string",
    "carries isPriorityFix and fixForTaskId when set",
    "extracts src/ directory from line",
    "extracts tests/ directory from line",
    "extracts .ts file from line",
    "returns empty arrays for unrelated line",
    "deduplicates directories",
    "deduplicates file paths",
    "extracts multiple directories from one line",
    "always returns empty filesRead",
    "keeps BOTH grants when Files and Reads share one line (671-review multi-label fix)",
    "parses Reads as exact read-only authority without leaking into write scope",
    "adds root-level DECKENT.md to filesWrite without adding docs/ directory",
    "adds .gitignore to filesWrite",
    "adds CONTRIBUTING.md to filesWrite without docs/ directory",
    "still adds docs/ directory for files inside docs/",
    "keeps Reads and Files in disjoint authority channels",
    "returns empty array when no structured sections",
    "parses a single Görev block",
    "parses multiple blocks",
    "parses Task keyword in addition to Görev",
    "extracts scope from Dosya and Kapsam lines",
    "extracts testTarget from Test: line",
    "testTarget is undefined when no Test: line",
    "merges scope from multiple scope lines in a block",
    "skips blocks without a title",
    "maps PlannerTask fields to CreateTaskParams",
    "uses PlannerTask model when provided",
    "falls back to modelOverride when PlannerTask.model is undefined",
    "passes initialStatus when provided",
    "initialStatus is undefined when not provided",
    "returns \"max\" for high-score tasks (score >= 6)",
    "returns \"high\" for medium-score tasks (score 1-5)",
    "returns \"low\" for very low-score tasks",
    "returns \"medium\" for score -1 to 0",
    "result is one of the valid effort strings",
    "resolves Codex core externalization when worker_core_system_prompt=$workerCore and codex_core_channel=$codexChannel",
    "preserves Claude core externalization when worker_core_system_prompt=$enabled",
    "includes task id and title in \"Your Task\" section",
    "includes task description alongside title",
    "includes model in prompt",
    "includes effort level",
    "includes \"What To Do\" section with steps and explicit verify block",
    "includes \"Scope Rules\" section with directories",
    "includes filesWrite in scope section",
    "shows no-restriction message when directories are empty",
    "warns about auditor boundary violations",
    "includes heartbeat file path",
    "includes \"Result File\" section with correct path",
    "includes selfAssessment options in result template",
    "delivers the verified worker guide inline without requiring a host path",
    "does not embed heartbeat JSON template (moved to WORKER-GUIDE.md)",
    "does not embed \"If Something Goes Wrong\" section (moved to WORKER-GUIDE.md)",
    "mentions max 3 attempts for tsc",
    "prompt is structured with markdown headers",
    "includes heartbeat file path and workerId hint",
    "result file is marked as mandatory (never exit without it)",
    "prompt is significantly shorter than original (~80 lines target)",
    "task description ratio is higher in shorter prompt",
    "includes the orchestrator-filled tokenUsage instruction in result file section (WP-4)",
    "keeps plan-time provider/model out of worker-authored tokenUsage",
    "WP-4: tokenUsage is orchestrator-owned (no stale \"missing → NO_GO\" demand)",
    "parses \"Model: claude-opus-4-8\" into forceModel",
    "parses \"Model: claude-haiku-4-5-20251001\" into forceModel",
    "returns undefined forceModel when no Model line",
    "fails loudly for an unknown model without an explicit provider",
    "accepts an exact future API model ID after catalog-backed registration",
    "does not mutate the registry while parsing a dynamic local tag",
    "rejects legacy model aliases instead of rewriting task intent",
    "parses \"Effort: high\" into forceEffort",
    "returns undefined forceEffort when no Effort line",
    "parses both Model and Effort together",
    "preserves case sensitivity and rejects a non-canonical model ID",
    "returns forceEffort when set",
    "returns score-based effort when forceEffort not set",
    "passes forceModel to task",
    "passes forceEffort to task",
    "forceModel undefined when not provided",
    "includes agent block when agentPrompt is provided",
    "does not include agent block when agentPrompt is undefined",
    "does not include agent block when agentPrompt is empty string",
    "includes full agentPrompt without truncation",
    "uses \"generic\" for assignedAgent when not set",
    "includes both agent block and standard prompt content",
    "agent block comes before task content",
    "standard prompt unchanged when agentPrompt is not provided",
    "handles agentPrompt with special characters",
    "handles agentPrompt with newlines",
    "includes combined systemPrompt + expertise + PROMPT.md content",
    "agent block appears for forceModel tasks with assigned agent",
    "threads effective persona_render config into the V3-selected guidance slice",
    "accepts a valid task with all required fields",
    "accepts optional model field with valid value",
    "rejects invalid model value",
    "accepts all valid model values",
    "accepts optional effort field with valid value",
    "rejects invalid effort value",
    "accepts all valid effort values",
    "rejects missing title",
    "rejects empty title",
    "accepts optional tests array",
    "allows model and effort to be undefined (optional)",
    "accepts a valid directive with goal and one task",
    "accepts multiple tasks",
    "rejects missing goal",
    "rejects empty goal string",
    "rejects missing tasks field",
    "rejects empty tasks array",
    "rejects when a task in array has invalid model",
    "detects partial valid + partial invalid tasks",
    "returns success=true for valid directive",
    "returns success=false for missing goal",
    "returns clear error message for missing goal field",
    "returns success=false for empty tasks array",
    "returns error mentioning task field for invalid model",
    "returns error mentioning task field for invalid effort",
    "returns error for missing title in a task",
    "returns error for empty title in a task",
    "does not throw on invalid input — returns error object instead",
    "succeeds with optional fields absent",
    "succeeds with all optional fields present",
    "error message starts with DIRECTIVES validation failed prefix",
    "parses \"Provider: codex\" into provider field",
    "parses \"Provider: gemini\" into provider field",
    "parses \"Provider: claude\" into provider field",
    "returns undefined provider when no Provider line",
    "ignores invalid provider values",
    "case-insensitive Provider parsing",
    "parses Provider with leading dash prefix",
    "parses Provider alongside Model and Effort",
    "accepts valid provider \"codex\"",
    "accepts valid provider \"gemini\"",
    "accepts valid provider \"claude\"",
    "rejects invalid provider value",
    "allows provider to be undefined (optional)",
    "passes provider to task when specified",
    "provider is undefined when not specified",
    "fails loudly when model and provider identities conflict",
    "does not warn when model and provider are compatible",
    "does not warn when provider is not specified",
    "provider appears in created task JSON",
    "compatible claude model with claude provider does not warn",
    "compatible gemini model with gemini provider does not warn",
    "parses \"- Task: <title>\" format",
    "parses numbered list \"1. <title>\" format",
    "parses \"1) <title>\" format",
    "parses \"* Task: <title>\" format",
    "extracts Model override from sub-lines",
    "extracts Effort override from sub-lines",
    "returns empty array for plain prose with no task markers",
    "ignores lines with title shorter than 3 characters",
    "falls back to bullet format when no ## Task headings present",
    "falls back to numbered list when no ## Task headings present",
    "still parses ## Task headings when present",
    "should extract docs/ directory paths",
    "should add standalone doc files like CHANGELOG.md to filesWrite",
    "should not duplicate docs/ directory",
    "adds DECKENT.md to filesWrite",
    "adds .gitignore to filesWrite",
    "adds docker-compose.yml to filesWrite",
    "adds .github/workflows yaml files via directory path",
    "does not add src/core/config.ts as standalone \"config.ts\" when path-prefixed version already present",
    "does not duplicate DECKENT.md when both blocks match",
    "does NOT add .deckent as filesWrite when path is .deckent/config.json",
    "does NOT add .brain as filesWrite when path is .brain/MEMORY.md",
    "does NOT add .contracts as filesWrite when path is .contracts/api-surface.md",
    "still matches real standalone dotfiles after directory prefix fix",
    "should add test file patterns when tests/ is in directories",
    "should not add test patterns when no tests/ directory",
    "should not add test patterns when test files already exist in filesWrite",
    "should not mutate the original scope",
    "should add test file patterns when tests/ is in scope directories",
    "should extract testTarget from Test: line",
    "initializes assignedAgent to \"generic\" by default",
    "initializes assignedSkills to empty array by default",
    "assignedAgent persists through JSON.stringify/parse",
    "assignedSkills persists through JSON.stringify/parse",
    "assignedAgent can be overwritten after creation",
    "assignedSkills can be populated after creation",
    "assignedAgent field exists in JSON keys",
    "assignedSkills field exists in JSON keys",
    "assignedAgent survives mutation + re-serialization",
    "all created tasks have assignedAgent regardless of params",
    "passes forceAgent from PlannerTask to CreateTaskParams",
    "passes forceSkills from PlannerTask to CreateTaskParams",
    "passes forceModel from PlannerTask to CreateTaskParams",
    "passes excludeAgent from PlannerTask to CreateTaskParams",
    "passes excludeSkills from PlannerTask to CreateTaskParams",
    "override fields are undefined when not set in PlannerTask",
    "applies enrichScopeWithTestFiles to scope when tests/ directory is present",
    "scope unchanged when no tests/ directory in PlannerTask",
    "high effort task allows longer skill prompts (2500 token budget)",
    "low effort task still injects full skill content (Sprint 182 PQ-2 F2: no effort-based clipping)",
    "normal effort uses 1500 token budget (unchanged from default)",
    "high effort includes more skill context than low effort for same content",
    "recognizes .brain/ as a directory scope",
    "recognizes .brain/ in multi-scope with other dirs",
    "parses \".\" as root scope → \"./\"",
    "parses multi-scope with \".\" root + named directory",
    "does not create false positive scope from results.find() in title",
    "does not create scope from code-like title patterns",
    "does not add source paths mentioned only in prose to filesWrite or directories",
    "recognizes .deckent/ as a directory scope",
    "does not parse /usr/local/bin as a valid scope",
    "returns empty directories for empty scope line",
    "correctly parses all 15 tasks from Sprint 134 DIRECTIVES",
    "includes .brain/ Files in scope when only .brain paths present",
    "parses \"- Priority: CRITICAL\" → \"CRITICAL\"",
    "parses \"- Priority: HIGH\" → \"HIGH\"",
    "parses \"- Priority: NORMAL\" → \"NORMAL\"",
    "parses \"- Priority: LOW\" → \"LOW\"",
    "returns undefined for missing line",
    "returns undefined for empty value",
    "returns undefined for invalid priority value",
    "is case-insensitive for the label and normalizes value to uppercase",
    "parses Priority: CRITICAL from structured task block",
    "parses Priority: HIGH from structured task block",
    "returns undefined priority when Priority line is missing (default NORMAL)",
    "parses multiple tasks with different priorities",
    "Sprint 136 DIRECTIVES self-parse: correct priority distribution (3 CRITICAL + 5 HIGH + 2 NORMAL)",
    "Sprint 136 DIRECTIVES self-parse: no explicit dependencies (all tasks independent)",
    "parser edge case: leading whitespace around Priority line is handled correctly",
    "backward compat: task without Priority field defaults to undefined (caller uses NORMAL)",
    "HOLDs an explicit ADR when the memory database is absent",
    "keeps required ADRs whole while query-only ADRs remain typed background",
    "includes worker prompt structure even without DB-sourced ADRs",
    "loads the ADR block from projectRoot, not process.cwd()",
    "HOLDs an explicit ADR that cannot fit the configured whole-entry budget",
    "includes the self-assessment authority section in prompt",
    "maps fully closed polarity-specific outcomes to DONE (WP-19)",
    "maps a critical open item to NO_GO (WP-19)",
    "renders a goCriteria-derived checklist judged WITH EVIDENCE (WP-19)",
    "clarifies that \"Code written\" ≠ \"DONE\"",
    "mandates silent planning and NO plan file in \"What To Do\" (7094-F1d)",
    "parses \"- Auth: api\" → \"api\"",
    "parses \"- Auth: subscription\" → \"subscription\"",
    "is case-insensitive",
    "returns undefined for missing line, empty value, or unknown value",
    "propagates \"- Auth: api\" from a structured task block to the parsed task",
    "leaves authMode undefined when no Auth: line is present",
    "adds tests/orchestra/ when kind=code-development and src/orchestra/ is in directories",
    "adds mirrored tests/ dirs for multiple src/ directories",
    "does not add duplicate tests/ dir when already present",
    "leaves scope unchanged for non-code-development kinds",
    "leaves scope unchanged when no src/ directories present",
    "does not mutate the original scope",
    "preserves filesRead and filesWrite unchanged",
    "adds tests/orchestra/ when scope is src/orchestra/ (code-development task)",
    "adds tests/core/ when scope is src/core/ (code-development task)",
    "does not duplicate tests/ dir when already explicitly in scope",
    "does not add tests/ dir for doc-only scope",
    "parses \"- Dependencies: 323-005, 323-007\" to a slot-id array",
    "parses a single bare \"Dependencies: 323-005\"",
    "parses a JSON-array dependency literal",
    "returns undefined for \"none\" and for a missing line",
    "accepts canonical NNN-NNN slot ids",
    "rejects title-prefix labels and free-text titles",
    "rejects malformed / over-length numeric refs",
    "\"0\" resolves to the first task id (index 0)",
    "\"1\" resolves to the second task id (index 1)",
    "\"2\" resolves to the third task id (index 2)",
    "out-of-bounds integer returns undefined",
    "canonical slot-id still resolves by exact id match (not treated as index)",
    "title-prefix label still resolves by title token match",
    "\"none\" is reserved and returns undefined",
    "empty string returns undefined",
    "resolves [\"0\"] to the first task id",
    "resolves mixed index + slot-id refs correctly",
    "deduplicates when index and slot-id resolve to same task",
    "drops unresolvable refs without throwing",
    "\"- Dependencies: 0\" resolves to the first task id after planning",
    "does not duplicate PROMPT.md when the full path is on the line",
    "still captures a genuinely root-level .md file"
   ],
   "orchestra/task-limit.test.ts": [
    "does NOT instruct AI to limit task count to maxWorkers",
    "instructs AI to plan ALL directive tasks",
    "includes maxWorkers value as concurrent execution limit info",
    "max_workers=8 does not create a \"create at most 8 tasks\" instruction",
    "parses 14 ## Görev blocks from a 14-task directive",
    "parses 20 ## Görev blocks from a 20-task directive",
    "parses 12 blocks (Sprint 21 size)",
    "each parsed task has a non-empty title",
    "each parsed task has a non-empty description",
    "creates 14 task JSONs when max_workers=8 and 14 structured directives",
    "creates 20 task JSONs when max_workers=6 and 20 structured directives",
    "sprint.workers length equals all task count (not max_workers)",
    "each task JSON file is valid and contains required fields",
    "with 14 tasks and max_workers=8, spawns exactly 8 workers",
    "with 14 tasks and max_workers=8, returns 6 queued tasks",
    "with 20 tasks and max_workers=8, returns 12 queued tasks",
    "dashboard progress.total = all 14 tasks (not just active 8)",
    "dashboard progress.total = all 20 tasks when max_workers=8"
   ],
   "orchestra/task-mode-agent-inject.test.ts": [
    "injects agent prompt when task has an assignedAgent",
    "injects skill prompts when task has assignedSkills",
    "falls back to generic when both resolvers return nothing (no agent/skills)",
    "does not throw when resolveAgentPrompt rejects — error propagates"
   ],
   "orchestra/task-mode-runner.test.ts": [
    "Gap E: writes .tasks/task-{id}.json before spawn",
    "Gap E: written task JSON contains description and scope",
    "Gap G: buildWorkerPrompt is called without projectRoot as agentPrompt",
    "spawn receives the prompt string from buildWorkerPrompt (not a path)",
    "returns taskId, backend, and provider from spawn",
    "persists one dispatch-started invocation receipt through the common ingress",
    "holds a remote task before Task JSON and spawn when owner budget policy is missing",
    "enforces the project active-set even when the process registry was never bootstrapped",
    "holds before Task JSON and spawn when the project model authority is unreadable",
    "closes a pre-dispatch budget refusal as one durable zero-work invocation",
    "declares the budget-resolved Docker backend before a final-only containment hold",
    "consumes configured provider authority before routing, Task JSON, prompt, event, or spawn",
    "persists and spawns with the same owner budget while request ceilings only narrow it",
    "keeps exact executionRunId on provider admission while using one executor",
    "rejects a malformed %s request budget before Task JSON and spawn",
    "does not silently route an unavailable local adapter through the configured remote backend",
    "throws when deckent_style is not \"task\""
   ],
   "orchestra/task-mode-tokenusage.test.ts": [
    "fills tokenUsage from a fake CLI-log transcript and persists it (≠ 0/0/0)",
    "wires enrichResultCost — result.cost is populated once tokenUsage is real",
    "is a safe no-op when no .result file exists yet (no transcript, task still running)"
   ],
   "orchestra/task-output-live-read.test.ts": [
    "accepts only its minted capability and uses a credential-free filtered inspect",
    "rejects forged and foreign-backend capabilities before Docker",
    "refuses changed RELEASED %s before inspect/logs",
    "returns custody-changed when authority changes after live bytes were emitted"
   ],
   "orchestra/task-output-live-transport.test.ts": [
    "does not report CLOSED when observer exit wins before a final sink rejection",
    "does not spawn when pre-aborted",
    "rejects invalid container syntax before it can be mistaken for authorization",
    "uses a single bounded docker logs observer and preserves stdout/stderr identity with awaited sinks",
    "does not miss an abort while observer spawn is still pending",
    "returns observer failure rather than task success for a nonzero logs client exit",
    "TERM-then-reaps only the logs client on abort and exposes settled resources",
    "holds when a sink fails and never swallows an unproven group reap",
    "never reports closed after a slow sink requires forced reader destruction",
    "reports the native ownership seam as unavailable instead of a guessed Windows fallback"
   ],
   "orchestra/task-output-read-service.test.ts": [
    "returns immutable sealed provider bytes with exact receipt identity and no writes",
    "refuses missing strict tenant identity",
    "holds task-only lookup with multiple verified generations instead of guessing latest",
    "preserves NOT_DISPATCHED without treating RELEASED as worker completion",
    "keeps an unsealed provider stream pending behind the opaque capability",
    "refuses a provider-exit container not bound to the released execution",
    "returns typed unavailable for missing admission, tamper, and malformed UTF-8",
    "exports a production factory that opens no missing custody root"
   ],
   "orchestra/task-projection-parity.test.ts": [
    "defers the plan for either a run-level exact request or a task-level Docker pin",
    "parity: plan == disk → silent (byte-identical behaviour)",
    "planned-but-missing on disk fails CLOSED naming the exact ids (hollow-sprint arm)",
    "stray same-sprint file outside the plan fails CLOSED (abandoned-projection arm)",
    "cross-sprint files are ignored — same rule as the planner orphan cleanup",
    "an absent .tasks dir with a non-empty plan is the fully-hollow case — fail closed",
    "exact pre-publication mode permits only explicitly deferred ids while retaining stray detection"
   ],
   "orchestra/task-queue.test.ts": [
    "creates 5 tasks when max_workers=3 and 5 directives provided",
    "creates 10 tasks when max_workers=8 and 10 directives provided",
    "writes all 10 task JSON files to .tasks/ when max_workers=8",
    "creates tasks equal to directive count when max_workers < directive count",
    "creates tasks up to directive count regardless of very low max_workers",
    "spawns only max_workers tasks when tasks > max_workers",
    "returns the remaining queued tasks when tasks > max_workers",
    "returns empty queue when tasks <= max_workers",
    "dashboard progress.total equals all tasks (not just active workers)",
    "dashboard progress.total equals all tasks when tasks == max_workers",
    "spawns all tasks and returns empty queue when tasks == max_workers",
    "returns results for all tasks when no queue",
    "spawns queued task when active task result is found",
    "does NOT kill worker when queue is empty",
    "does NOT kill worker when no queue provided",
    "collects results for all sprint tasks including those from queue",
    "spawns next queued task for each completed active worker",
    "returns partial results when timeout elapses without all completing"
   ],
   "orchestra/task-restoration.test.ts": [
    "writes a checkpoint file with correct structure",
    "increments checkpoint number monotonically",
    "persists to disk and is readable",
    "returns null for non-existent checkpoint",
    "returns null for malformed JSON",
    "returns null for checkpoint missing required fields",
    "writes checkpoint at phase transition with correct phase",
    "is idempotent — writing twice produces valid checkpoints",
    "returns true for null heartbeat",
    "returns true for heartbeat with invalid timestamp",
    "returns false for fresh heartbeat (within threshold)",
    "returns true for stale heartbeat (beyond threshold)",
    "respects custom threshold",
    "returns null when heartbeat file does not exist",
    "returns null for malformed heartbeat file",
    "returns parsed heartbeat for valid file",
    "returns empty array when no active workers",
    "detects workers with missing heartbeat files",
    "detects workers with stale heartbeat timestamps",
    "does not flag fresh workers as stale",
    "handles mixed fresh and stale workers",
    "filters out completed tasks",
    "returns empty array when all tasks completed",
    "separates pending tasks from stale executing tasks",
    "excludes tasks that completed during crash (.result file exists)",
    "returns all tasks as pending when no checkpoint data",
    "returns false when no checkpoint exists",
    "returns true when checkpoint file exists",
    "is 5 minutes (300000ms)"
   ],
   "orchestra/task-result-authority.test.ts": [
    "reads exact normal-Docker accepted result from private custody and ignores public spoof bytes",
    "keeps V2 custody/evidence in the raw accepted reader and strips terminal self-claims only at evaluator projection",
    "keeps V2 custody/evidence intact in the raw settled reader",
    "keeps no-dispatch truth at zero attempts without reading a public result",
    "returns typed HOLD for an exact sibling attempt instead of falling back to public bytes",
    "projects host evaluation with exact identity and replays idempotently",
    "holds malformed terminal decision graphs before any proxy or getter dereference",
    "holds oversized or cyclic projection revalidation before public mutation",
    "holds resolver result drift without an unsafe public rollback or false success return",
    "holds stale and sibling exact projections behind the generation fence",
    "ignores worker-writable lock spoofing but holds a newer unprojected authority",
    "keeps worker-writable raw output ineligible while Docker settlement is pending",
    "returns the immutable host settlement payload only after closure even when raw output disagrees",
    "projects a closed crash-before-prepare recovery as host-owned zero-work evidence",
    "keeps a newer active attempt pending instead of replaying an older closed receipt",
    "preserves legacy raw results only when no Docker authority exists",
    "reports missing or invalid legacy data as absent",
    "fails loudly when an active settlement file exists but is malformed"
   ],
   "orchestra/task-retry-e2e.test.ts": [
    "returns true for NO_GO result with retryCount 0",
    "returns true for NO_GO result with retryCount 1",
    "returns false for NO_GO result when retryCount >= MAX_RETRY_COUNT",
    "returns false for DONE result",
    "returns false for GO_WITH_TECH_DEBT result",
    "returns false for DONE even with retryCount 0",
    "returns 0 for first retry (retryCount=0)",
    "returns 30000 for second retry (retryCount=1)",
    "returns 0 for out-of-range retryCount (fallback)",
    "matches RETRY_BACKOFF_MS constants",
    "returns 0 when retryCount is undefined",
    "returns the actual retryCount when set",
    "appends -r1 suffix for first retry",
    "appends -r2 suffix for second retry",
    "replaces existing -rN suffix on re-retry",
    "sets status to PENDING",
    "increments retryCount",
    "preserves original task fields (title, model, scope)",
    "clears assignedWorker",
    "updates reason with retry context",
    "sets new createdAt and updatedAt timestamps",
    "resolves immediately for retryCount=0",
    "waits 30s for retryCount=1",
    "NO_GO → retry → NO_GO → retry → NO_GO → stops",
    "NO_GO → retry → DONE → stops retrying",
    "MAX_RETRY_COUNT is exactly 2"
   ],
   "orchestra/task-retry.test.ts": [
    "returns true for NO_GO result when retryCount is 0",
    "returns true for NO_GO result when retryCount is 1 (second retry)",
    "returns false for NO_GO result when retryCount equals MAX_RETRY_COUNT",
    "returns false for NO_GO result when retryCount exceeds MAX_RETRY_COUNT",
    "returns false for DONE result regardless of retryCount",
    "returns false for GO_WITH_TECH_DEBT result regardless of retryCount",
    "returns 0ms for first retry (retryCount=0)",
    "returns 30 000ms for second retry (retryCount=1)",
    "returns 0ms for unknown retry counts (fallback)",
    "RETRY_BACKOFF_MS constant has correct values",
    "returns 0 when task has no retryCount field",
    "returns the stored retryCount when present",
    "returns 0 when retryCount is explicitly undefined",
    "creates a new task with status PENDING",
    "increments retryCount by 1",
    "increments retryCount correctly from non-zero base",
    "appends -r<N> suffix to the task id",
    "replaces existing -r<N> suffix instead of double-appending",
    "clears assignedWorker",
    "includes original task id in reason",
    "preserves scope, model, dependencies and goNogo from original",
    "sets createdAt and updatedAt to a valid ISO string",
    "resolves immediately when delay is 0 (first retry)",
    "calls sleepFn with 30 000ms for second retry",
    "uses real setTimeout when no sleepFn provided (0ms delay)",
    "MAX_RETRY_COUNT is 2",
    "returns 5 000ms for first transient retry (retryCount=0)",
    "returns 30 000ms for second transient retry (retryCount=1)",
    "returns cap (120 000ms) for retryCount=2 (formula 180 000ms → capped)",
    "returns cap (120 000ms) for out-of-range retryCount",
    "grows exponentially: ratio from count=0 to count=1 equals TRANSIENT_RETRY_MULTIPLIER",
    "TRANSIENT_RETRY_BASE_MS is 5 000",
    "TRANSIENT_RETRY_MULTIPLIER is 6",
    "TRANSIENT_RETRY_CAP_MS is 120 000",
    "createRetryTask does not set retryAfter — spawner sets it separately",
    "retryAfter field is assignable to RetryableTask"
   ],
   "orchestra/task-router.test.ts": [
    "detects code tasks from src/ directories",
    "detects code tasks from .ts files",
    "detects code tasks from .py files",
    "detects test tasks from tests/ directory",
    "detects test tasks from .test. file pattern",
    "detects test tasks from .spec. file pattern",
    "detects doc tasks from docs/ directory",
    "detects doc tasks from .md files",
    "detects design tasks from ui/ directory",
    "detects design tasks from components/ directory",
    "detects design tasks from .css files",
    "returns unknown for unrecognized scope",
    "uses config skill_routing override for design tasks",
    "uses config skill_routing override for testing tasks",
    "uses config skill_routing override for doc tasks",
    "uses task forceModel to infer provider",
    "uses task forceModel with gemini model",
    "uses task provider field when set",
    "uses skill_routing.default for code tasks with no specific routing",
    "uses worker_provider when no other config matches",
    "fails loudly when no configured worker candidate is available",
    "falls back when preferred provider is unavailable",
    "fails loudly when no providers are available",
    "honors configured worker fallback order instead of availability order",
    "accepts OpenRouter as a first-class configured worker provider",
    "preserves assignedAgent from task",
    "defaults agent to generic when not assigned",
    "preserves assignedSkills from task",
    "defaults skills to empty array when not assigned",
    "config skill_routing takes priority over task forceModel",
    "forceModel takes priority over task.provider",
    "skill_routing null value is ignored",
    "handles forceModel fallback when provider unavailable"
   ],
   "orchestra/tel-w1-reason.test.ts": [
    "boundary-veto NO_GO → reason includes (cause: BOUNDARY_VIOLATION)",
    "dishonest stub NO_GO → reason includes (cause: DISHONEST_DONE_STUB)",
    "concrete_test_failed NO_GO → reason includes (cause: concrete_test_failed)",
    "worker_self_no_go → reason includes (cause: worker_self_no_go)",
    "rubric-only NO_GO (no concrete veto) → old format without cause",
    "DONE verdict → old format, no cause suffix",
    "GO_WITH_TECH_DEBT verdict → old format, no cause suffix",
    "honest-gate veto takes priority over testsPassed=false cause"
   ],
   "orchestra/temp-agent-demote.test.ts": [
    "60% success rate @ 120 tasks → demoted as underperformer",
    "85% success rate @ 120 tasks → NOT demoted",
    "built-in agent is never demoted even with 60% success @ 120 tasks",
    "60% success rate @ 5 tasks (< 20) → NOT demoted (too few tasks)",
    "exactly 65% success rate @ 20 tasks → NOT demoted (threshold is strictly < 0.65)"
   ],
   "orchestra/temp-agent-dryrun-purity.test.ts": [
    "does not create .deckent/agents/temp-* during dry-run planning",
    "persists .deckent/agents/temp-* during non-dry-run planning"
   ],
   "orchestra/temp-agent-generator.test.ts": [
    "returns the react-ts template for temp-react-ts-specialist",
    "returns the python-api template for temp-python-api-specialist",
    "falls back to generic template for unknown agent id",
    "handles agent id without the temp- prefix",
    "renders react-ts PROMPT.md with stack section + 4-discipline block",
    "renders python-api PROMPT.md mentioning FastAPI + Pydantic",
    "falls back to generic template for unknown agent id but still includes 4-discipline block",
    "includes agent description and expertise when supplied",
    "accepts an explicit template override",
    "writeAgentPromptMd creates .deckent/agents/<id>/PROMPT.md",
    "writeAgentPromptMd overwrites existing content",
    "ensureAgentPromptMd creates PROMPT.md when missing",
    "ensureAgentPromptMd preserves an existing non-empty PROMPT.md",
    "ensureAgentPromptMd rewrites empty PROMPT.md files",
    "writes a PROMPT.md for every agent and returns the write count",
    "skips agents that already have a non-empty PROMPT.md (idempotent)",
    "returns 0 when called with an empty agent list",
    "writes PROMPT.md for every generated temp agent when projectRoot is supplied",
    "does not write PROMPT.md when projectRoot is omitted (backward compat)"
   ],
   "orchestra/temp-skill-generator.test.ts": [
    "generates skill with correct id",
    "includes stack info in content",
    "includes dependencies",
    "includes multi-language info",
    "includes sub-projects",
    "has correct activation rules",
    "generates skill for domain with enough data",
    "skips domain with insufficient data",
    "skips domain with low success rate",
    "skips already-existing skill",
    "includes common files and deps in content",
    "generates react-ts-specialist for TypeScript+React stack",
    "generated agent has source=learned and enabled=true",
    "generated agent has v2 activation rules",
    "does not generate react-ts-specialist for Python stack",
    "generates python-api-specialist for Python+FastAPI stack",
    "generates go-specialist for Go stack",
    "generates prime agents for C++, Java, C#, Kotlin, and Swift stacks",
    "returns empty array for unknown/unsupported stack",
    "all generated agent IDs start with temp-",
    "returns the conventions content as plain prompt data, not a skill"
   ],
   "orchestra/terminal-authority-snapshot-boundary.test.ts": [
    "opens one fresh verified snapshot per invocation and never reuses a verdict across identities",
    "propagates a native snapshot verification failure instead of returning authority",
    "keeps missing admission as HOLD and rejects a nested snapshot before authority escapes"
   ],
   "orchestra/terminal-publication-block-trace.probe.test.ts": [
    "preserves observable asTerminalVerdict behavior for established evaluations"
   ],
   "orchestra/terminal-publication-zero-task.test.ts": [
    "COMPLETE publication with ZERO logical tasks is a typed HOLD and writes NO receipt (the 535/536 bug class)",
    "COMPLETE publication with missing/moved task evidence (unattributed hold) fails typed BEFORE any write",
    "a fully settled single-task truth still publishes a COMPLETE receipt (healthy path preserved)",
    "operator-approved ABORTED publication stays exempt — force-abort closes even a zero-task broken run"
   ],
   "orchestra/timeout-estimator-tier.test.ts": [
    "DEFAULT_TIMEOUT_CONFIG carries no model_multiplier (undefined by default)",
    "modelMultiplier is 1.0 for opus/sonnet/haiku tasks when config carries no model_multiplier",
    "today's dogfood value is unchanged: high-effort opus task on docker = 2400s",
    "returns 1.0 when table is undefined",
    "returns 1.0 for an unrecognized/custom model string and never throws",
    "resolves opus → premium, sonnet → standard, haiku → economy",
    "falls back to 1.0 for a tier missing from a partial table",
    "ignores a non-positive or non-finite entry and falls back to 1.0",
    "TT550/TT556 regression: opus high-effort task duration doubles with model_multiplier.premium=2.0",
    "sonnet (standard tier) timeout is unchanged when only premium/economy are configured",
    "haiku (economy tier) timeout shrinks under a configured sub-1.0 multiplier",
    "accepts a well-formed model_multiplier table",
    "rejects an unknown tier key",
    "rejects a non-positive multiplier value",
    "rejects a non-numeric multiplier value"
   ],
   "orchestra/timeout-estimator.test.ts": [
    "parses \"1566 → <400\" arrow pattern as LoC delta",
    "parses \"~250 LoC\" pattern",
    "parses \"+620 LoC\" pattern",
    "falls back to filesWrite * 200 when no LoC pattern found",
    "returns 0 for empty description and no files",
    "returns base ~600s for low effort with no LoC and no history",
    "calculates ~3800s for high effort + 2000 LoC on docker",
    "applies scope multiplier 1.15 for 8 files write",
    "applies correct backend factors: docker 1.0, tmux 0.9, subprocess 0.8",
    "forces locMultiplier to 1.0 when loc_scaling_enabled is false",
    "forces historyFactor to 1.0 when history_scaling_enabled is false",
    "clamps to min_timeout when estimate is below floor",
    "clamps to max_timeout when estimate exceeds ceiling",
    "returns historyFactor 1.0 with empty history (no division by zero)",
    "T-144-001 scenario: high effort, 2000 LoC, 8 files, docker → ≥2400s",
    "T-144-003 scenario: normal effort, 450 LoC, 6 files → ~1260s",
    "maintains clampedTo ↔ clampReason invariant",
    "applies history factor when avgTaskDurationMs is provided",
    "resolves auto backend to tmux (0.9x factor)",
    "locMultiplier exceeds 1.0 for very large LoC delta (50000)",
    "uses DEFAULT_TIMEOUT_CONFIG when config.timeout is undefined",
    "TaskRouting interface accepts timeoutSeconds field",
    "returns zero-fill fallback when no .brain/sprints dir exists (first sprint)",
    "returns zero-fill when logs exist but carry no usable metrics (zero tasks)",
    "aggregates per-task duration from a single sprint log",
    "averages per-task duration across multiple recent sprints",
    "limits aggregation to the most-recent N sprint logs",
    "skips logs that lack Duration/Total Tasks rows",
    "FAITHFUL: aggregated slow-sprint history drives historyFactor > 1.0",
    "FALLBACK: no past-sprint history keeps historyFactor at 1.0"
   ],
   "orchestra/timeout-placeholder-scope-diff.test.ts": [
    "single-quotes each entry and space-joins them",
    "escapes an embedded single quote via the POSIX close/escape/reopen idiom",
    "drops blank/whitespace-only entries and trims surrounding whitespace",
    "returns an empty string for an empty list",
    "2-arg call (no scope) preserves the legacy unscoped git commands (back-compat)",
    "3-arg call with a non-empty scope adds a `-- <pathspec>` filter to every diff command",
    "3-arg call with an EMPTY scope makes no git call at all — honest empty intersection",
    "TIMEOUT_WITH_WORK counts ONLY in-scope files, ignoring a concurrent sibling worker\\'s changes",
    "empty intersection (only a sibling worker changed files) honestly reports workPresent=false",
    "a genuinely in-scope change is still detected (no false negative)",
    "records `<path>\\\\t<hash>` for existing scoped files and omits absent/blank ones",
    "returns an empty string when nothing can be baselined (all entries absent)",
    "pre-existing dirty in-scope file (unchanged since task start) → no false TIMEOUT_WITH_WORK",
    "a further edit ON TOP of pre-existing dirt is still detected (recoverable)",
    "a brand-new file created after the baseline (no baseline entry) is counted as work",
    "mixed: pre-existing-dirty file excluded, genuinely-new file kept — only real work counts",
    "excludes unchanged predecessor work from a later normal result",
    "attributes only bytes added after the claim-time baseline",
    "preserves canonical structured file-change shape when the worker reported an empty list",
    "attributes a newly-created scoped file without an empty-blob subprocess probe",
    "attributes deletion from the persisted claim-time blob",
    "turns missing attempt authority into a typed HOLD instead of trusting final git diff",
    "does not erase an already host-measured diff when later authority is held"
   ],
   "orchestra/timeout-watcher.test.ts": [
    "should call killFn when timeout expires",
    "should cancel old timer and start new one on extend",
    "should kill after max extensions (2) regardless of progress",
    "should return progressing=true when heartbeat fresh and diff substantial",
    "should return progressing=false when heartbeat is stale",
    "should kill immediately when runtime_extension_enabled is false",
    "should cancel timer on stop()",
    "should write TIMEOUT_EXTEND event on extension",
    "should return progressing=false when diff lines below threshold",
    "should cap max_extensions to 2 even if config says higher",
    "reports heartbeatFresh=false when no heartbeat file, but diff still counts as progress (7094-F1d)",
    "should extract task ID from worker ID",
    "should return input if no w- prefix",
    "should parse insertions and deletions",
    "should parse small diffs",
    "should return 0 for empty output",
    "should return 0 for output without summary",
    "should handle insertions only",
    "should handle deletions only",
    "should return null when runtime_extension_enabled is false",
    "should return null with default config (disabled)",
    "should return TimeoutWatcher when enabled"
   ],
   "orchestra/tmpfile-preservation.test.ts": [
    "archives BOTH .prompt-*.txt AND .worker-*.sh into the canonical sprint archive",
    "archives only .worker-*.sh when no prompt files exist",
    "archives only .prompt-*.txt when no worker scripts exist",
    "does NOT archive unrelated files (e.g. .worker- without .sh suffix)",
    "archives only the requested sprint prefix when one is supplied",
    "sprint-end (default): archives .prompt-*.txt and .worker-*.sh to the canonical sprint archive",
    "sprint-end (explicit): archives tmpfiles (same behavior as default)",
    "spawn-fail: PRESERVES tmpfiles in-place (NOT archived, NOT deleted)",
    "tmpfiles exist mid-sprint, then move to archive after sprint-end cleanup",
    "moves a task-bound orphan prompt directly into the canonical sprint archive",
    "sprint-end archivePromptFiles drains _orphaned/ into the sprint archive"
   ],
   "orchestra/tmux-deprecation.test.ts": [
    "default→docker: resolveBackend(\"auto\") returns \"docker\"",
    "explicit-warns: resolveBackend(\"tmux\") emits deprecation warning",
    "warn-once: deprecation warning emitted only once per sprint lifecycle"
   ],
   "orchestra/tmux-edge.test.ts": [
    "has name \"TmuxError\"",
    "stores message correctly",
    "stores optional command",
    "command is undefined when not provided",
    "is instanceof Error",
    "is instanceof TmuxError",
    "returns false when spawnSync status is null (unexpected)",
    "returns false when spawnSync status is non-zero (e.g., 127 = not found)",
    "checks specifically against TMUX_SESSION_NAME \"deckent\"",
    "throws TmuxError if new-session fails",
    "does not call new-session when session is already active",
    "calls new-session with detached flag -d",
    "creates .tasks dir when it does not exist before writing prompt",
    "skips mkdirSync when .tasks dir already exists",
    "writes prompt content to file (not embedded in command)",
    "sets up pipe-pane log with correct task-prefixed filename",
    "uses both allowedTools and autoApprove together",
    "uses correct window name format w-<taskId>",
    "throws TmuxError with the original stderr message",
    "throws TmuxError when session itself does not exist",
    "passes correct kill-window target",
    "includes the tmux command in TmuxError",
    "returns empty array when output is empty string",
    "filters out non-worker windows (brain, auditor, watch)",
    "returns multiple workers when multiple w- windows exist",
    "returns empty array on tmux failure (no session)",
    "strips the w- prefix from all returned taskIds",
    "calls unlinkSync with the given path",
    "does not throw when file does not exist (ENOENT swallowed)",
    "does not throw for any unlink error (all errors swallowed)",
    "can be called multiple times on same path without throwing"
   ],
   "orchestra/tmux-prompt-filename.test.ts": [
    "emits a tmux prompt file whose filename embeds the taskId (.prompt-${taskId}-${hash}.txt)",
    "emitted filename satisfies the `-${taskId}-` selective filter used by ClaudeAdapter cleanup",
    "_cleanupOrphanedPromptFiles preserves the tmux-emitted prompt of an active worker"
   ],
   "orchestra/tmux-provider-cli.test.ts": [
    "produces a `codex exec` command with the codex binary, never claude",
    "feeds the wire apiId (gpt-5.5), never the deckent-facing alias (gpt-5)",
    "pipes the prompt file via stdin redirection (codex promptFeed is stdin)",
    "autoApprove appends the codex sandbox-bypass flag only when set",
    "applies codex reasoning-effort via -c model_reasoning_effort=<level>",
    "drops an invalid reasoning-effort for codex (claude-only vocabulary)",
    "shared-table reuse (import-evidence): matches buildProviderCommand(getProviderCommandSpec(\"codex\"), ...) directly",
    "produces the exact pre-existing hardcoded claude command shape (no --output-format json)",
    "includes --allowedTools and --dangerously-skip-permissions (no adapter, unchanged)",
    "appends --effort for a valid claude reasoning-effort (unchanged)",
    "embeds the prompt inline via \"$(cat <promptPath>)\", no trailing stdin redirection",
    "shared-table reuse: matches buildProviderCommand(getProviderCommandSpec(\"gemini\"), ...) directly",
    "ollama (host-only, no ProviderCommandSpec) throws TmuxError, never builds a claude command",
    "a fully unregistered model id fails loudly",
    "delegates the exact registered API ID to a matching adapter"
   ],
   "orchestra/tmux-timeout-parity.test.ts": [
    "wraps the worker command with a -k 30 hard-kill grace period",
    "captures the exit code instead of masking it with `|| echo \"WORKER_TIMEOUT\"`",
    "gates the .timeout marker on exit code 124 or 137 only",
    "only writes the .timeout marker when no .result file already exists",
    "respects a custom timeoutSeconds value in the -k 30 wrap",
    "preserves the existing EXIT trap / RFILE fallback contract",
    "does not wrap timeout when taskId is not provided (backward compat)",
    "does not wrap timeout when an adapter is provided"
   ],
   "orchestra/tmux.test.ts": [
    "returns true when session exists (status=0)",
    "returns false when session does not exist (status≠0)",
    "creates session when none exists",
    "is no-op when session already exists",
    "opens window and sends claude command via tmpfile",
    "prompt content is written to file, not embedded in command (injection-safe)",
    "adds --allowedTools when opts.allowedTools is set",
    "adds --dangerously-skip-permissions when autoApprove is true",
    "calls pipe-pane to capture worker output to log file",
    "uses stdin redirect instead of inline prompt",
    "kills the correct window",
    "throws TmuxError when window does not exist",
    "parses taskIds with w- prefix stripped, non-workers filtered",
    "returns empty array when session does not exist",
    "creates auditor window with sonnet model when not already running",
    "skips new-window when auditor window already exists",
    "passes allowedTools from opts",
    "kills the session",
    "does not throw when session does not exist",
    "sends correct target and keys",
    "calls unlinkSync on the given path",
    "uses stdio inherit for blocking attach",
    "creates a watch window and splits it when window does not exist",
    "skips window creation when watch window already exists",
    "uses stdio inherit for attach-session",
    "selects the correct worker window and attaches",
    "throws TmuxError when worker window does not exist",
    "produces Claude CLI command without adapter (backward compat)",
    "includes --allowedTools when opts provided (no adapter)",
    "includes --dangerously-skip-permissions when autoApprove (no adapter)",
    "includes both opts flags together (no adapter)",
    "appends --effort for a valid reasoning-effort (no adapter)",
    "drops an invalid reasoning-effort (no adapter)",
    "forwards reasoningEffort to adapter.buildCommand when adapter present",
    "delegates to adapter.buildCommand when adapter is provided",
    "passes opts to adapter.buildCommand correctly",
    "uses adapter output verbatim (does not append Claude flags)",
    "wraps with timeout when taskId is provided (no adapter)",
    "does not wrap timeout when taskId is not provided (backward compat)",
    "uses custom timeout seconds when provided",
    "does not wrap timeout when adapter is provided",
    "does not wrap timeout when timeoutSeconds is 0",
    "WORKER_TIMEOUT_SECONDS has the expected default value",
    "is the same function reference as buildWorkerCommand",
    "produces identical output to buildWorkerCommand",
    "uses adapter command when adapter is provided",
    "uses Claude default when no adapter is provided",
    "spawnWorker rejects malicious taskId: ${JSON.stringify(malId)}",
    "killWorker rejects malicious taskId: ${JSON.stringify(malId)}",
    "attachToWorkerPane rejects malicious taskId: ${JSON.stringify(malId)}",
    "buildWorkerCommand rejects malicious taskId",
    "buildWorkerCommand accepts valid taskId",
    "spawnWorker accepts valid taskId and proceeds normally",
    "uses adapter command when adapter is provided"
   ],
   "orchestra/token-counter-real-usage.test.ts": [
    "session-store hit: returns the real SUM incl. cacheCreation + source=session-store",
    "no real source: returns the heuristic estimate tagged source=estimate (honest)",
    "envelope hit (no session): uses the CLI envelope tagged source=envelope",
    "session-store wins over an available envelope",
    "non-claude provider with no envelope falls to estimate (no native store yet)"
   ],
   "orchestra/token-counter.test.ts": [
    "(a) parses the Claude CLI --output-format json envelope",
    "(b) returns null for malformed input (no usage field, non-JSON)",
    "accepts already-parsed object payloads",
    "(c) parses the Anthropic SDK messages.create() response shape",
    "returns null when usage is missing or malformed",
    "(d) measured fields override worker self-report",
    "(e) preserves worker provider/model when measured omits them",
    "(f) returns undefined when both worker and measured are missing",
    "returns the non-null input when the other is missing",
    "(g) reads .tasks/task-{id}.cli-output.json and extracts usage",
    "returns null when no log file exists",
    "falls back to scanning task-{id}.log for a JSON envelope on the last line"
   ],
   "orchestra/token-extract-via-adapter.test.ts": [
    "captures usage via the provider adapter (non-Claude usage format)",
    "the Claude-CLI-specific path returns null for the same non-Claude log (the gap this closes)",
    "returns null when the adapter has no extractUsage",
    "returns null when the adapter is undefined or the log is absent"
   ],
   "orchestra/tool-inventory-hygiene.test.ts": [
    "resolves under .deckent/runtime/tool-inventory/, not the flat .deckent root",
    "writeToolInventory persists under the new path (verified on disk)",
    "falls back to the legacy flat path when the new path is absent",
    "prefers the new path when both the new and legacy files exist",
    "returns undefined when neither path exists",
    "removes both the new-path and legacy-path files for the given sprintId",
    "is idempotent / fail-soft when no file exists for the sprintId",
    "NO_GO guard: cleaning up one sprint never deletes another still-active sprint\\'s inventory",
    "removes the completing sprint\\'s tool-inventory artifact on a real (non-delayed) cleanup pass",
    "does not touch tool-inventory artifacts when opts.skipCleanup is set",
    "keeps terminal publication blocked until configured delayed cleanup settles",
    "(.gitignore follow-up, out of write-scope) .deckent/runtime/tool-inventory/ is covered by .gitignore"
   ],
   "orchestra/topp-continuous-dispatch.test.ts": [
    "returns empty plan when sprint has no PENDING tasks and queue is empty",
    "spawns a PENDING task when slots are available",
    "does not spawn a task whose dependency is still PENDING",
    "spawns a task once its dependency reaches DONE",
    "returns no toSpawn when all worker slots are full",
    "does not re-spawn a task already in assignedTaskIds (Bug F guard)",
    "embeds selfAssessment, filesChanged, and notes from predecessor .result",
    "renders pending placeholder when predecessor result missing",
    "returns a continuous DispatchPlan whether depPipeline=true or false",
    "drains one queued task per completedTaskId when DECKENT_LEGACY_FIFO=1",
    "does not spawn beyond queue depth (legacy contract)",
    "unblocks the chain step-by-step across multiple ticks"
   ],
   "orchestra/trace-content-parity.test.ts": [
    "readLogEvents 0 event döndürür (LogEvent contract: envelope satırında ts/seq/type/content yok)",
    "toSprintTrainingExample([], meta) -> messages: [] (bu, sprint-worker.jsonl\\'de görülen buğ)",
    "events.length===0 + envelope .log mevcut -> fallback tek assistant-message üretir + contentSource etiketi",
    "redaction: envelope.result içindeki secret [REDACTED] olur (unit + redaction testi)",
    "ne .log ne recoverable envelope varsa -> hiçbir trace satırı yazılmaz (messages:[] YERİNE no-op)",
    "garbage .log (JSON değil) -> no-op",
    "fast-path korunur: geçerli LogEvent akışı varken fallback DEVREYE GİRMEZ, contentSource yok",
    "claude baseArgs: --output-format json -> stream-json + --verbose eklenir",
    "--output-format json yoksa/farklıysa değişmez (no-op, shallow copy)",
    "paylaşılan spec dizisini MUTATE etmez (shared PROVIDER_COMMAND_SPECS korunur)",
    "stream-json NDJSON fixture -> her satır bir LogEvent (ts/seq/type/content) olarak yazılır",
    "malformed / plain-text satır asla drop edilmez (normalizeStreamEvent text-fallback)",
    "boş satırlar atlanır (NDJSON inter-record whitespace)",
    "uçtan-uca: normalize-yazılmış stream-json .log -> recordSprintWorkerTrace FAST-path (fallback DEVRE DIŞI)",
    "eski single-envelope formatı ile yeni stream-json formatı BİREBİR aynı .result.tokenUsage üretir"
   ],
   "orchestra/trace-tail-parity.test.ts": [
    "codex NDJSON dump, raw (no normalization) -> 0 events (native lines lack ts/seq/content)",
    "gemini single-envelope dump, raw (no normalization) -> 0 events",
    "gemini pretty-printed multi-line envelope, raw -> 0 events (each fragment line is invalid JSON)",
    "produces one LogEvent per line, none dropped",
    "bridges thread.started -> lifecycle, turn.started -> turn, turn.completed -> usage",
    "bridges file_change item.started/completed -> tool_use/tool_result, agent_message -> text",
    "plain-text ERROR line -> type text, content is the RAW line verbatim (passthrough, no data loss)",
    "unrecognized item type (mcp_tool_call) passes through safely — never dropped",
    "bridgeCodexEvent preserves the original discriminator under codexEventType (no data loss on override)",
    "bridgeCodexEvent is a no-op for an unrecognized top-level type (returned unchanged)",
    "compact single-line envelope -> exactly 1 LogEvent, content preserves usageMetadata",
    "pretty-printed multi-line envelope -> STILL exactly 1 LogEvent (whole-value fast path, not shredded per-line)",
    "multi-event claude stream-json fixture still yields one LogEvent per line (byte-identical to born-637)",
    "codex: patchResultUsageFromEnvelope pulls the REAL turn.completed usage from pristine content",
    "gemini: patchResultUsageFromEnvelope pulls the REAL usageMetadata from pristine content",
    "codex numbers are IDENTICAL whether or not the .log is ALSO LogEvent-normalized (independent pristine-content path)",
    "finds usage nested under a LogEvent row .content — matches the pristine top-level extraction exactly",
    "false-positive guard: a LogEvent row wrapping CODEX-shaped nested usage is NOT misattributed as claude",
    "gemini-shaped nested usageMetadata (entirely different field names) is never matched by tier-2",
    "returns null (safe miss) when no candidate file exists"
   ],
   "orchestra/trace-v2-schema.test.ts": [
    "telemetry NOISE leaks in: every one of the 6 events becomes a message",
    "ORPHAN class: the tool_result carries an EMPTY tool_call_id (403-001)",
    "NO native tool_calls: the tool_use is double-represented as raw JSON in content",
    "PROMPT absent: zero system/user turns",
    "v1 is untouched: no schemaVersion, no telemetry sidecar (byte-identical to pre-TT552)",
    "schemaVersion:2 is stamped top-level AND in meta (dual-read discriminator)",
    "PROMPT injected: system + user turns lead the conversation",
    "TELEMETRY-SIDECAR ↔ PROJECTION split: telemetry is OUT of messages, in the sidecar",
    "REAL tool_calls: native call + MATCHED tool_call_id (orphan class dead)",
    "Read double-representation UNIFIED: the call is not re-embedded as JSON in content",
    "source ts/seq CARRIED (not dropped) onto the projected messages",
    "secrets REDACTED before they reach a message",
    "clean record is NOT quarantined (prompt present, ids matched)",
    "promptless transcript → no-prompt reason + meta.quarantine",
    "orphan tool_result (no originating tool_use) → orphan-tool-result reason",
    "conversation-empty transcript → no-conversation reason",
    "splitWorkerPrompt cuts at the `## Your Task` seam",
    "reads a LIVE .tasks/.prompt-<taskId>-<hash>.txt and splits it",
    "reads an ARCHIVED prompt under .tasks/archive/sprint-<id>/",
    "preferFix disambiguates original vs -fix prompt for the SAME taskId",
    "missing prompt → {} (→ the v2 builder stamps no-prompt quarantine)",
    "the UNCHANGED convertToShareGpt projects a v2 record into valid ShareGPT",
    "a v1 record still converts (old reader unbroken)",
    "isCorpusExcluded flags quarantine + envelope-fallback, passes clean records",
    "skips (and counts) incomplete records; only clean records are written",
    "emits schemaVersion:2 with injected prompt, matched tool_call_id, and telemetry sidecar"
   ],
   "orchestra/trn1-sprint-trace-wire.test.ts": [
    "flag ON: records a redacted, labeled trace entry from the collected LogEvent stream",
    "flag OFF: byte-identical no-op — collector is never read, no file is written",
    "fail-soft: a throwing collector never throws out of recordSprintWorkerTrace",
    "maps tool_result events to role \"tool\", everything else to role \"assistant\"",
    "born-614: carries workerSelfAssessment alongside the Brain-verdict label (honesty-gap signal)",
    "born-614: workerSelfAssessment absent -> field omitted (pre-614 shape byte-identical)",
    "runEvaluatePhase contains the training_trace-gated recordSprintWorkerTrace sweep",
    "CANLI-KANIT DERSI (sprint-400): sprint-controller call-siteleri config parametresini GECIRIR"
   ],
   "orchestra/tsc-settlement-gate.test.ts": [
    "returns a bounded dirty residual for compiler errors",
    "preserves clean completion truth",
    "does not invoke the runner when disabled",
    "records runner faults as an honest residual"
   ],
   "orchestra/turkish-locale.test.ts": [
    "matches \"Sprint Metrikleri\" via patternsByLang tr",
    "matches \"İlerleme\" heading (İ → i with tr-TR) via patternsByLang tr — requires toLocaleLowerCase fix in source",
    "matches \"Agent Performansı\" via patternsByLang tr",
    "pattern matching is case-insensitive for Turkish characters — requires toLocaleLowerCase fix in source",
    "finds \"Sprint Metrikleri\" section by exact title",
    "finds \"İçindekiler\" section — İ character handled correctly",
    "finds section with mixed-case Turkish title search"
   ],
   "orchestra/turn-economy-2.test.ts": [
    "renders the exact block verbatim in a code task prompt",
    "renders the exact block verbatim in a doc-only task prompt (unconditional)",
    "appears AFTER the Turn Economy directive (folded into the karpathy T0 segment)",
    "stays ≤ 400 chars (anti-bloat pin — the K1 pipe-exit clamp)",
    "teaches the un-masked read patterns (PIPESTATUS / separate-line $? / VERIFY STEPS)",
    "renders verbatim in a code task prompt",
    "renders verbatim in a doc-only task prompt (unconditional)",
    "GREEN: reports each step\\'s TRUE, separately-captured exit code",
    "a passing check never masks a failing test (separate codes, no short-circuit)",
    "all-pass → ok:true",
    "an empty command is reported as skipped (never guessed), not run",
    "demonstrates the harm of masking: a pager-masked (always-0) runner false-greens",
    "prefers typecheck, then lint, then build for the check step",
    "honest-empty when the stack defines no commands (never guessed)",
    "runVerifyTask composes resolve → run with both seams injected (hermetic)",
    "probeToolInventory reports each probed tool via the injected existence check",
    "formatToolInventory renders the stable one-line form",
    "buildEnvProbeBlock is empty for a blank/absent inventory (default → no block)",
    "buildEnvProbeBlock renders the inventory line when present",
    "is ABSENT by default (no toolInventory) → default prompt unchanged",
    "is injected after \"## What To Do\" when the caller supplies an inventory",
    "is a VOLATILE T2 segment — never poisons the shared T0/T1 cache prefix",
    "exposes the same verify_task / env-probe symbols through worker.ts"
   ],
   "orchestra/u4-integration-compose.test.ts": [
    "1. devops-intent task + devops-engineer, guidance mode → devops slice + pointer, NOT the full body",
    "2. implementation-intent coordinator-style task (sprint-442 442-003 shape) on refactorer → NO Docker guidance",
    "3a. full mode with a marker-free persona stays byte-identical (no pointer)",
    "3b. full mode with a MARKER-CARRYING persona (devops-engineer) renders core-body + appendix pointer (post-F1 contract)",
    "4. shadow-precedence — a .deckent/agents/<id> copy SHADOWS the builtin (tmpdir fixture), both at getAgentPrompt and through the compose path"
   ],
   "orchestra/user-surface-classify.test.ts": [
    "returns true when filesWrite is under src/cli/commands/",
    "returns true when filesWrite is under src/dashboard/",
    "returns true when filesWrite is under src/api/",
    "returns false for a pure src/core/ task (internal/structural — Tier-0)",
    "returns false for a docs-only task",
    "returns true when scope.directories alone signals user-surface (no filesWrite under that root)",
    "returns false for empty scope (default fallback)",
    "returns true when scope mixes orchestra/ and a user-surface dashboard write (parallel boolean — not exclusive)",
    "appends proof-of-function criterion for a user-surface code task",
    "does NOT append proof-of-function criterion for an internal src/core/ task",
    "does NOT append proof-of-function criterion for an audit task (docs/audits/)",
    "does NOT append proof-of-function criterion for a pure doc-write task",
    "returns a fresh object (not a mutation of the frozen base rubric) for user-surface tasks",
    "exposes PROOF_OF_FUNCTION_CRITERION as a stable placeholder constant"
   ],
   "orchestra/v1-strict-report.test.ts": [
    "flag-off (config absent): zero effect — no RESULT_CONTRACT_DRIFT event, result flow unchanged",
    "flag-off (enabled=false): zero effect — no RESULT_CONTRACT_DRIFT event",
    "flag-on (enabled + strict_report): emits RESULT_CONTRACT_DRIFT for a non-conforming legacy result, but does NOT alter selfAssessment or the returned result",
    "flag-on but strict_report=false: enabled alone does not activate the check (both fields required)",
    "flag-on with a fully-conforming TaskResultV1 result: no drift event",
    "a synthetic .timeout NO_GO (no worker .result at all) is NOT checked — no drift event"
   ],
   "orchestra/verify-commands-prompt.test.ts": [
    "renders the exact, rewritten block verbatim in a code task prompt",
    "renders the exact block verbatim in a doc-only task prompt (unconditional)",
    "still teaches the two REAL un-masked-read patterns (PIPESTATUS / separate-line $?)",
    "never references the non-existent verify_task tool anywhere in the compiled prompt",
    "replacement text stays reasonably compact (anti-bloat pin, mirrors the prior ≤400 discipline)",
    "keeps both original-command status mechanisms in the role-resolved read-only policy",
    "buildCheckCommandLine: legacy multi-language examples when unresolved",
    "buildCheckCommandLine: cites the concrete resolved command when present",
    "buildCheckCommandLine: honest-empty check (skipped step) falls back to legacy text",
    "buildTestCommandLine: legacy single example when unresolved",
    "buildTestCommandLine: cites the concrete resolved test command, scoped instruction included",
    "with verifyCommands resolved: prompt cites only commands with an exact scoped target",
    "without verifyCommands: fails closed instead of inventing generic commands",
    "a doc-only task never renders CRITICAL VERIFY STEPS (verifyCommands irrelevant there)",
    "extracts and de-duplicates explicit **Test:** commands",
    "uses the task-declared command without injecting unrelated tsc/vitest checks",
    "uses the same typed command authority for inspection-only tasks"
   ],
   "orchestra/wave-pipeline-activation.test.ts": [
    "builds correct wave levels from Dependencies field; cycle → DependencyCycleError",
    "respects dep gate when dependency_pipeline_enabled: true; passes through when false",
    "serializes tasks sharing a write target; independent task stays in early wave"
   ],
   "orchestra/windows-backend.test.ts": [
    "resolves auto → subprocess on win32",
    "resolves auto → docker on linux when the docker daemon is reachable",
    "resolves auto → docker on darwin when the docker daemon is reachable",
    "resolves auto → subprocess with a one-time typed notice when docker is unreachable",
    "explicit docker stays docker even when the daemon is unreachable (honest hard failure downstream)",
    "passes through explicit subprocess unchanged",
    "passes through explicit docker unchanged",
    "Atomics.wait is available and resolves quickly for short sleeps",
    "spawn-backend.ts contains process.platform win32 guard for auto resolution",
    "kill() polling loop uses Atomics.wait instead of POSIX sleep for 500ms poll"
   ],
   "orchestra/wm5-auth-guard.test.ts": [
    "sets CLAUDE_AUTH_REQUIRED for a claude model (sonnet)",
    "sets CLAUDE_AUTH_REQUIRED for opus (claude model)",
    "holds Codex before CLAUDE_AUTH_REQUIRED can enter a container",
    "holds canonical gpt-5.6-sol before CLAUDE_AUTH_REQUIRED can enter a container",
    "holds Gemini Flash before CLAUDE_AUTH_REQUIRED can enter a container",
    "holds Gemini Pro before CLAUDE_AUTH_REQUIRED can enter a container",
    "Codex command spec never receives the Claude approval flag",
    "Gemini command spec never receives the Claude approval flag",
    "Codex shared command spec contains its own external-sandbox approval flag"
   ],
   "orchestra/work-model-consumer-2.test.ts": [
    "canonical: task.type=documentation uses docs routing key",
    "canonical: task.type=design uses design routing key",
    "canonical: task.type=code-development uses null routing key (falls to default)",
    "legacy fallback: no task.type with docs/ scope uses docs routing key",
    "legacy fallback: no task.type with ui/ scope uses design routing key",
    "regression eq (docs): canonical task.type=documentation equals legacy docs/ scope routing",
    "regression eq (design): canonical task.type=design equals legacy ui/ scope routing",
    "regression eq (code): canonical task.type=code-development equals legacy src/ scope routing",
    "canonical: task.type=documentation selects docs-domain ADRs",
    "canonical: task.type=code-development selects core-dev-domain ADRs",
    "canonical: task.type=security selects security-domain ADRs",
    "legacy fallback: no task.type with docs/ scope uses classifyTaskIntent",
    "regression eq (docs): canonical task.type=documentation selects same domain ADRs as legacy docs/ task",
    "returns empty array for empty ADR list regardless of task.type"
   ],
   "orchestra/work-model-consumer.test.ts": [
    "(a1) task.type=code-development → CODE_RUBRIC",
    "(a2) task.type=documentation → DOC_WRITE_RUBRIC",
    "(a3) task.type=audit → AUDIT_RUBRIC",
    "(b1) no type, code scope → detectTaskType fallback → CODE_RUBRIC",
    "(b2) no type, doc scope → fallback → DOC_WRITE_RUBRIC",
    "(b3) no type, audit scope → fallback → AUDIT_RUBRIC",
    "(c1) code task: canonical type set == scope-shape fallback",
    "(c2) doc task: canonical type set == scope-shape fallback",
    "(c3) audit task: canonical type set == scope-shape fallback",
    "(d1) code task (src/ scope) → type=code-development",
    "(d2) doc task (docs/ scope) → type=documentation",
    "(d3) audit task (docs/audits/ scope) → type=audit",
    "(d4) detectTaskType fallback aligns with getRubric new-way for code task"
   ],
   "orchestra/work-present-eval.test.ts": [
    "workPresent:true marker → VERIFY_AND_COMPLETE signal + passthrough fields",
    "workPresent:false marker → no signal (work-absent → today\\'s behavior)",
    "ordinary DONE result (no markerType) → not an exit-without-result, no signal",
    "marker missing optional passthrough fields → safe defaults, still signals",
    "VERIFY_AND_COMPLETE signal → audit-and-finish framing with passthrough diagnostics",
    "no signal (workPresent:false) → empty string, caller leaves prompt unchanged",
    "ordinary DONE result → empty guidance (regression: healthy results untouched)",
    "signal without diffStat → falls back to a generic \"changes detected\" phrase",
    "workPresent partial original → fix prompt gains verify-and-complete framing",
    "is idempotent — a second pass does not double-append",
    "workPresent:false original → no enrichment (regression-safe)",
    "ordinary DONE original → no enrichment (real DONE results unaffected)",
    "fix task with no matching original result → skipped, no crash"
   ],
   "orchestra/worker-auth-isolation.test.ts": [
    "subscription-Claude (%s) forwards NO credential key (no ANTHROPIC_API_KEY leak)",
    "api-mode canonical Claude forwards ONLY ANTHROPIC_API_KEY",
    "codex worker (%s) HOLDs before any provider key enters a container",
    "gemini worker (%s) HOLDs before any provider key enters a container",
    "Codex with authMode=api still HOLDs before provider env assembly",
    "Gemini with authMode=api still HOLDs before provider env assembly"
   ],
   "orchestra/worker-core-backend-capability.test.ts": [
    "keeps the core inline for a backend that cannot deliver it",
    "externalizes the core only for a delivering backend",
    "fails closed when the backend is unknown to the compiler",
    "keeps the core inline on tmux, which ignores the system-prompt channel",
    "resolves auto through the same probe the spawner uses",
    "declares the capability on the non-delivering backend instances"
   ],
   "orchestra/worker-core-system-prompt.test.ts": [
    "code-class core carries all four anchors plus the npm advisory",
    "doc-only core drops the npm advisory; inspection-only core is the read-only discipline",
    "ctx.coreExternalized suppresses the inline blocks; OFF keeps them (parity)",
    "publishes full-SHA immutable bytes and binds the exact runtime invocation"
   ],
   "orchestra/worker-identity-hostbound.test.ts": [
    "turns an identity-bearing spawn into the canonical heartbeat write",
    "preserves the typed HOLD when host identity is genuinely unavailable"
   ],
   "orchestra/worker-liveness.test.ts": [
    "never-spawned when assignedWorker missing",
    "uses an exact activity attempt identity and host authority alive result",
    "does not use activity age as process truth",
    "preserves unavailable/HOLD instead of inventing dead",
    "returns dead only from explicit host authority",
    "does not call a result-settled worker stale when authority is unavailable",
    "requires one matching exact attempt authority entry",
    "uses the resolved project root identically from a nested cwd"
   ],
   "orchestra/worker-prompt-bounded-discovery.test.ts": [
    "forbids repository-wide discovery in the ${label} variant",
    "offers a bounded excerpt instead, in the ${label} variant",
    "tells the worker to stop rather than widen, in the ${label} variant",
    "keeps the .tasks lifecycle exemption alongside the new rule"
   ],
   "orchestra/worker-prompt-quality.test.ts": [
    "renders the task goCriteria value into the prompt (A)",
    "emits no empty \"=== Task ===\" header; the real header is \"## Your Task\" (B)",
    "contains no DECKENT_CACHE_KEY marker (C)",
    "injects a Karpathy essence (effect preserved) without the full rule doc (E)",
    "does not duplicate the result instructions across legacy headers (E)",
    "sets task.estimatedTokens within ±15% of the real prompt size (D)",
    "produces provider-agnostic quality (no marker, goCriteria + essence) for gemini (parity)",
    "differs between claude and gemini only by canonical provider identity fields (parity)"
   ],
   "orchestra/worker-timeout-rc.test.ts": [
    "exports DEPENDENCY_ENTRY_MAX_CHARS at a bounded value (≤4096) so per-dep digest cannot balloon",
    "caps each dependency entry total size — long filesChanged + notes do not balloon the prompt",
    "returns persisted=false + size=0 when .result file is missing",
    "returns persisted=true + size>0 after writeResult, and the disk content matches"
   ],
   "orchestra/worker-verify-isolation-wire.test.ts": [
    "executes only the adapter-provided argv without a shell and preserves exit evidence"
   ],
   "orchestra/workspace-artifacts.test.ts": [
    "creates the complete versioned workspace from canonical catalogs",
    "preserves user sections, migrates known generated sections, and is idempotent",
    "returns HOLD when the managed worker contract is modified out of band",
    "fails closed before mutation when an artifact was written by a newer schema",
    "rejects a symlinked workspace before it can write outside the project"
   ],
   "orchestra/wpopt-dedup.test.ts": [
    "the full-surface render contains the known, protected goCriteria overlap (canary)",
    "has no literal repeated block >=40 chars beyond the known protected goCriteria overlap",
    "has no literal repeated block >=40 chars in a minimal (no-optional-blocks) render",
    "has no literal repeated block >=40 chars in the doc-only verify branch",
    "records the exact byte reduction from the duplicated unset override explanation"
   ],
   "orchestra/wrapper-hb-allowlist.test.ts": [
    "filesWrite present → SOLE write authority; a read-only directory (docs/adr/) is excluded",
    "filesWrite empty, directories present → directories become the write-fallback target",
    "exact filesRead with no filesWrite → inspection-only; directories grant no Write/Edit",
    "neither directories nor filesWrite → narrows to .tasks/ only, never falls open unrestricted",
    "always includes .tasks/ (heartbeat/result write authority) even with a narrow filesWrite",
    "dedupes and trims whitespace-padded / blank entries",
    "fixture-task: task-XXX.json round-trip through disk produces the exact expected string",
    "is the exact inert no-op — never probes $HBFILE mtime or computes an age",
    "retains the documented threshold constant but no longer gates any write on it",
    "performs no write at all — no tmp file, no mv, no redirection into $HBFILE",
    "embeds no shell-fabricated heartbeat payload (no taskId/workerId JSON)",
    "buildHeartbeatWrapperLoop is the same inert seam — no 15s background driver, no subshell fork",
    "fresh $HBFILE (mtime just now) is NOT overwritten",
    "stale $HBFILE (mtime backdated past the retired threshold) is ALSO left untouched — the worker\\'s monotonic proof never regresses",
    "missing $HBFILE is NOT created — the wrapper never fabricates a heartbeat",
    "never leaves a dangling .hbwrap.$$ tmp file behind after a write",
    "derives --allowedTools from the task JSON scope, excluding a read-only directory, regardless of opts.allowedTools",
    "fails closed before Docker dispatch when no persisted task envelope exists"
   ],
   "orchestra/write-retrospective.test.ts": [
    "writes a `retro` entry for the sprint",
    "writes a `sprint` metadata entry with the canonical sprint-log id",
    "writes a `memory` learnings entry with problems AND a Gains section for DONE work",
    "records totalTasks/debtCount in the learnings metadata for DebtTrendAnalyzer (R5)",
    "does not duplicate the learnings entry when called twice for the same sprint",
    "does not throw when memory.db is absent"
   ],
   "orchestra/write-scope-backend-parity.test.ts": [
    "sprint-spawner and docker backends derive byte-equal write targets",
    "both backends emit a byte-equal --allowedTools flag",
    "an inspection-only task derives the same empty project write scope in every backend",
    "no backend widens beyond the union of .tasks/, directories and filesWrite",
    "WORKER_AVAILABLE_TOOLS is the unqualified projection of the same tool-name source (7094-F2a)"
   ],
   "orchestra/xverify-producer-fencing.test.ts": [
    "accepts the real archived raw-vs-enriched shape",
    "accepts an enriched copy that arrived through a JSON round-trip",
    "normalizes the settled copy before comparing test projections",
    "allows Brain to fill downstream-authored null fields after settlement",
    "rejects a changed value already present in a downstream-authored field",
    "still rejects a real worker-authored field difference",
    "treats an explicit undefined value as an absent field, not a divergence",
    "holds when the core field %s is tampered with",
    "holds when an UNKNOWN extra field is smuggled onto the evaluate copy",
    "holds when an UNKNOWN field is dropped from the evaluate copy",
    "holds when a core field is removed rather than changed",
    "reports every diverging core field, sorted",
    "holds when a side is not a JSON object (%s)",
    "allowlists only host-authored enrichment — never a worker-authorable field",
    "pins the downstream-authored exception separately from enrichment",
    "advances past the fence for the post-settlement-enriched evaluate copy",
    "holds the fence when a core field was mutated after settlement",
    "holds the fence when an UNKNOWN field is smuggled in"
   ]
  },
  "platform-tags.test.ts": {
   "platform-tags.test.ts": [
    "PLATFORM.md exists and documents platform categories",
    "%s declares isWindows flag",
    "%s uses describe.skipIf at least once",
    "tmux.test.ts skipIf wraps every top-level describe",
    "tmux-edge.test.ts skipIf wraps every top-level describe",
    "scripts.test.ts skipIf wraps the top-level OSS Scripts describe",
    "current platform is not Windows (skipIf should NOT skip on this host)"
   ]
  },
  "providers": {
   "providers/bedrock-conformance.test.ts": [
    "InvokeModel URL path follows /model/<bedrockId>/invoke pattern",
    "InvokeModel body includes anthropic_version matching model map",
    "InvokeModel body includes max_tokens as a positive integer",
    "InvokeModel body messages array has role and content fields",
    "InvokeModel body omits system field when not provided",
    "InvokeModel body includes system field as top-level string when provided",
    "InvokeModel body includes temperature when provided",
    "InvokeModel body omits temperature when not provided",
    "InvokeModel respects custom maxTokens override",
    "InvokeModel URL targets bedrock-runtime.<region>.amazonaws.com",
    "parses full Bedrock response with all Anthropic fields",
    "picks first text block when multiple content blocks present",
    "returns empty content when content array has no text block",
    "returns no usage when usage field is absent",
    "returns no model when model field is absent",
    "parses a JSON string response (Bedrock may return body as string)",
    "InvokeModel SigV4 known-vector: signature matches pre-computed expected value",
    "InvokeModel SigV4 known-vector: authorization header structure is spec-compliant",
    "SigV4 known-vector: changing body changes signature",
    "creds-yok: available=false with non-empty hints (bootstrap-skip diagnostic)",
    "creds-yok: hints include bootstrap-skip message",
    "missing AWS_ACCESS_KEY_ID: hint surfaces that key",
    "missing AWS_SECRET_ACCESS_KEY: hint surfaces that key",
    "missing AWS_REGION: hint surfaces region requirement",
    "all creds present: available=true, authStatus=ok, hints empty",
    "diagnoseAvailability returns supported model list"
   ],
   "providers/bedrock-sigv4.test.ts": [
    "produces an Authorization header with correct algorithm, credential, signed-headers, and signature",
    "produces a deterministic signature for fixed inputs",
    "produces different signatures for different secret keys",
    "correctly encodes the credential scope with date from datetime",
    "extracts text content from an Anthropic-on-Bedrock response",
    "returns empty content when no text block present",
    "returns empty content and no usage when fields are absent",
    "throws ProviderError for non-object response",
    "accepts a JSON string and parses it",
    "sends a POST to the InvokeModel URL with SigV4 Authorization header",
    "request body includes anthropic_version and messages",
    "throws ProviderError on non-2xx response",
    "throws ProviderError for unsupported model",
    "throws ProviderError when AWS credentials are missing",
    "includes system prompt in body when provided",
    "returns true when all three AWS env vars are set",
    "returns false when ACCESS_KEY_ID is missing",
    "returns false when SECRET_ACCESS_KEY is missing",
    "returns false when region is missing",
    "accepts AWS_DEFAULT_REGION as a fallback for region",
    "BEDROCK_MODEL_MAP contains expected internal IDs",
    "all entries have required fields",
    "adapter supportedModels lists all map keys",
    "spawn throws ProviderError",
    "kill is a no-op",
    "listWorkers returns empty array",
    "returns a BedrockAdapter"
   ],
   "providers/cache-adapter-resource.test.ts": [
    "archetype is EXPLICIT-RESOURCE",
    "runs the full create → use → delete lifecycle and ledgers storage duration",
    "passes the handle reference + tail-ONLY payload to use (prefix lives server-side)",
    "GUARD: an exception in use STILL deletes the cache (no leaked storage) and rethrows",
    "does NOT silently swallow a delete failure — records delete-failed + escalates onLeak",
    "use-error is NOT masked when delete also fails (use-error wins; leak still recorded)",
    "create failure throws CacheResourceError(phase=create); nothing to delete, no phantom ledger",
    "forwards ttlSeconds + idempotencyKey to the create call (safe retries + TTL safety net)",
    "defaults TTL to 1h when not specified (belt-and-suspenders against leaks)",
    "inlines the full prompt with no handle, threading tenantKey",
    "omits tenantKey when not supplied",
    "parses Gemini usageMetadata.cachedContentTokenCount",
    "parses Kimi/Moonshot usage.prompt_tokens_details.cached_tokens",
    "parses a flat usage.cached_tokens fallback",
    "returns unmeasured (never fabricates) when no cache field is present",
    "returns unmeasured for malformed / empty input",
    "ignores a negative/garbage cache count as unmeasured (no fabricated number)"
   ],
   "providers/cache-adapter.test.ts": [
    "archetype is IMPLICIT-AUTO",
    "keeps the prefix byte-untouched (prompt === t0+t1+t2) — no marker injected",
    "OpenAI/DeepSeek dialect: emits the tenant cache-key in `prompt_cache_key` (body)",
    "xAI dialect: emits the tenant cache-key in the `x-grok-conv-id` header",
    "omits the cache-key entirely when no tenant is supplied (single-tenant auto-detect)",
    "parses OpenAI usage.prompt_tokens_details.cached_tokens",
    "parses DeepSeek usage.prompt_cache_hit_tokens",
    "parses a flat usage.cached_tokens fallback",
    "returns unmeasured (never fabricates) when no cache field is present",
    "ignores a negative/garbage cache count as unmeasured",
    "archetype is EXPLICIT-MARKER",
    "places a cache_control breakpoint at each stable-tier boundary (T0 + T1), tail uncached",
    "emits ≤4 breakpoints (Anthropic limit) — exactly 2 for the two stable tiers",
    "does not waste a breakpoint on an empty stable tier",
    "never caches the volatile T2 tail",
    "parses Anthropic usage.cache_read_input_tokens + cache_creation_input_tokens",
    "captures creation-only (cache-write) usage — the archetype with a real write cost",
    "returns unmeasured when no anthropic cache field is present",
    "archetype is LOCAL-KV",
    "keeps the prefix byte-exact and emits cache_salt = tenant key (per-tenant isolation)",
    "isolates tenants: distinct tenant keys produce distinct salts",
    "omits the salt when no tenant is supplied (single-tenant shared local cache)",
    "extractCacheUsage is always unmeasured ($0 latency-only — never fabricates a number)",
    "archetype is NONE",
    "is an explicit no-op: full prompt + honest noCache marker, no cache fields",
    "does not thread a tenant cache-key (no tenant-scoped cache exists to isolate)",
    "extractCacheUsage is always unmeasured (no cache exists)",
    "every adapter preserves the stable prefix bytes in `prompt`",
    "every adapter returns unmeasured (never fabricates) on malformed / empty input"
   ],
   "providers/claude-account-evidence.test.ts": [
    "returns provider-verified organization identity for one exact bounded status command",
    "uses the shell-free Windows CLI wrapper contract",
    "keeps %s credential-only instead of inventing account identity",
    "rejects non-canonical organization subjects without email fallback",
    "holds exact-scope mismatches without invoking the provider CLI",
    "rejects unbounded runtime settings at construction"
   ],
   "providers/claude-cleanup-active-protected.test.ts": [
    "preserves active worker prompt on kill of another worker",
    "defaults active list from heartbeat files when caller omits activeTaskIds",
    "fails closed when an explicit active-worker snapshot is empty"
   ],
   "providers/claude-isAvailable.test.ts": [
    "mcp backend → available=false with informative reason",
    "binary missing → available=false with install hint",
    "binary OK (tmux backend) → available=true with session auth",
    "isAvailable() returns true when binary is callable (tmux backend)"
   ],
   "providers/claude-prompt-cache.test.ts": [
    "parseCacheUsage extracts cache_read_input_tokens from a Claude CLI JSON envelope",
    "parseCacheUsage returns zeros for envelopes without cache fields (cache miss fallback)",
    "attachCacheControlToMessages marks the system block as ephemeral"
   ],
   "providers/claude-provider-evidence-sources.test.ts": [
    "constructs two lazy exact-backend registrations sharing account and limit authority",
    "resolves only the two exact Claude subscription CLI backend scopes",
    "keeps registry and selection digests deterministic across environment paths",
    "uses the injected canonical transport for a live Docker observation only",
    "keeps the Docker registration on null or absent transport without host fallback"
   ],
   "providers/claude-reachability-evidence.test.ts": [
    "builds an exact isolated command and scrubs every provider credential",
    "uses the shell-free Windows CLI wrapper contract",
    "does not spawn for provider, backend, account, or profile near-matches",
    "keeps timeout, overflow, spawn, nonzero, and model mismatch fail-closed",
    "preserves explicit provider rate-limit evidence on a nonzero exit",
    "rejects invalid construction bounds before any process can exist"
   ],
   "providers/claude-reachability-observation.test.ts": [
    "derives exact called-model truth from matching init and assistant evidence",
    "tolerates helper-model usage while requiring primary-model usage",
    "rejects missing, ambiguous, or contradictory provider-native model evidence",
    "never promotes requested-model bait or untrusted prompt fields into called-model truth",
    "hashes request identifiers deterministically without returning raw sensitive fields",
    "maps explicit provider failure categories and treats unknown shapes conservatively",
    "rejects malformed wrappers, missing request ids, and multiple terminal results"
   ],
   "providers/claude-subscription-limit-evidence.test.ts": [
    "projects exact percentages but remains advisory unknown/HOLD",
    "keeps the model-specific window required when the display row is absent",
    "requires only shared windows for exact Opus while retaining Fable evidence",
    "returns unavailable and never calls the probe for a near-match scope",
    "hashes unavailable raw output without persisting it"
   ],
   "providers/claude-usage.test.ts": [
    "is implemented (pre-fix the adapter had no extractUsage)",
    "parses the REAL --output-format json envelope → real tokens (goNogo)",
    "recovers opus output_tokens from modelUsage when the top-level usage omits it (goNogo RED→GREEN)",
    "maps a no-cache envelope without emitting sparse cache-write/reasoning fields",
    "extracts the envelope even when stderr/prose lines precede it in the log",
    "keeps the LAST recognizable envelope when several appear",
    "returns null when the output carries no usage",
    "returns null for an empty usage object (no real numbers reported)",
    "returns null for empty or malformed input",
    "declares the --output-format json usage-emit flag",
    "keeps the usage-emit flag OUT of buildArgs (spawn-only) so the arg-shape seam is stable",
    "keeps the usage-emit flag OUT of buildCommandString (dry-run display stays stable)"
   ],
   "providers/claude.test.ts": [
    "should have name \"claude-tmux\"",
    "should support fable, versioned opus, sonnet, haiku models",
    "should support exactly 5 models",
    "should implement ProviderAdapter interface (name, supportedModels, spawn, kill, listWorkers, isAvailable, buildCommand)",
    "should call ensureSession before spawning",
    "should call tmux.spawnWorker with correct arguments",
    "should pass allowedTools from opts",
    "should pass autoApprove from opts",
    "should use opts.projectDir if provided",
    "should fall back to constructor projectDir when opts.projectDir is absent",
    "should call tmux.killWorker with the task ID",
    "should forward any errors thrown by killWorker",
    "should scan for orphaned prompt files after killing worker",
    "should not scan for prompt files when tasks dir does not exist",
    "should archive each .prompt-*.txt file found",
    "should only archive files matching .prompt-*.txt pattern",
    "should pass the full prompt path as the first argument",
    "should pass the tasks dir as the archive-root argument",
    "should not archive when no prompt files found",
    "should handle readdirSync throwing gracefully",
    "should still call killWorker even when cleanup scan fails",
    "should use projectDir from constructor when building cleanup path",
    "should return worker list from tmux.listWorkers",
    "should return empty array when no workers active",
    "should return true when claude --version succeeds",
    "should return false when claude --version fails",
    "should return false when spawnSync throws",
    "should call claude --version",
    "should build basic command without opts (real model name, not alias)",
    "should include --allowedTools when provided",
    "should include --dangerously-skip-permissions when autoApprove is true",
    "should not include --dangerously-skip-permissions when autoApprove is false",
    "should use stdin redirection from promptPath",
    "should pass the real model apiId (not the short alias) to --model",
    "should combine allowedTools and autoApprove",
    "appends --effort when a valid reasoning-effort is given (tmux)",
    "omits --effort when no reasoning-effort is given",
    "drops an invalid reasoning-effort (validated against claude vocabulary)",
    "accepts the full claude effort vocabulary (xhigh, max)",
    "returns ready=true when binary present (CLI manages session)",
    "returns ready=false when claude --version fails",
    "returns ready=false when spawnSync throws (ENOENT)",
    "returns ready=false for mcp backend (not yet implemented)",
    "shape conforms to ProviderDetectResult contract",
    "should return true when tmux session is active",
    "should return false when tmux session is inactive",
    "should create a ClaudeAdapter instance",
    "should create adapter with given project directory",
    "should accept claude_backend option",
    "should default to tmux backend when no option provided",
    "should default to tmux backend when claude_backend is undefined",
    "should use tmux spawn when backend is tmux",
    "should use tmux kill when backend is tmux",
    "should delegate listWorkers to tmux when backend is tmux",
    "should build tmux-style command when backend is tmux",
    "should set backend to subprocess when configured",
    "should fail closed before spawn without measurable remote budget and never call tmux",
    "should NOT call tmux.killWorker when backend is subprocess",
    "should build subprocess-style command with --dangerously-skip-permissions",
    "should include --allowedTools in subprocess command when provided",
    "should return true for isSessionActive when backend is subprocess",
    "should allow constructing ClaudeAdapter with mcp backend without throwing",
    "should set backend to mcp when configured",
    "should throw ProviderError on spawn() when backend is mcp",
    "should throw with Sprint 048 context in error message for mcp spawn",
    "should include alternatives in mcp spawn error message",
    "should include roadmap reference in mcp spawn error message",
    "should return false from isAvailable() when backend is mcp",
    "should not call spawnSync for isAvailable() when backend is mcp",
    "should create ClaudeAdapter with mcp backend via createClaudeAdapter"
   ],
   "providers/codex-integration.test.ts": [
    "should detect codex CLI as available",
    "should detect auth mode",
    "should build valid exec command",
    "should build valid planner command",
    "should return correct tier models",
    "should detect CLI variant (rust/node/unknown)",
    "should have name codex",
    "should support expected models",
    "should create adapter via factory",
    "should have CODEX_TIER_MODELS with correct tiers",
    "should tier models be valid OpenAI models",
    "should list no workers initially"
   ],
   "providers/codex-isAvailable.test.ts": [
    "binary missing → available=false, partial=false",
    "binary OK + no auth (no API key, no subscription) → partial=true",
    "binary OK + OPENAI_API_KEY → available=true, authMethod=api_key",
    "binary OK + subscription auth → available=true, authMethod=session"
   ],
   "providers/codex-provider-evidence-sources.test.ts": [
    "projects the durable account subject with stable opaque hashes",
    "rotates the credential generation reference when the stored state changes",
    "resolves the state dir from the platform home when no override is present",
    "treats a stored API key as credential-only, never as account authority",
    "holds without reading any state when the home cannot be resolved",
    "holds on a win32 host whose codex state dir does not exist",
    "holds on scope mismatch: %s",
    "survives a snapshot-less greatest-named sibling (probe-debris shadowing)",
    "projects the newest persisted rate-limit snapshot into both display windows",
    "keeps both windows required when the snapshot carries only the primary window",
    "reports scope-typed unavailable without account evidence refs: %s",
    "reports typed unsupported instead of claiming an unproven live probe",
    "maps a completed canonical probe to succeeded with the structurally pinned identity",
    "stays typed-unsupported outside the exact docker/cli/subscription scope",
    "returns typed not-run when the billing-mode budget projection is absent",
    "returns typed unsupported when no canonical docker transport is bound",
    "maps transport-error %s to %s",
    "maps a timed-out probe to timeout and a rejection to invalid-response",
    "registration binds the live source to the docker slot ONLY, host stays the honest stub",
    "constructs one lazy exact-scope registration without invoking a producer",
    "registers one deterministic concrete source bundle for the exact proven scope",
    "does not project host state truth onto %s",
    "is the single bootstrap producer registering both provider source sets"
   ],
   "providers/codex-usage.test.ts": [
    "is implemented (pre-fix the adapter had no extractUsage)",
    "parses an OpenAI Chat Completions usage object → real tokens (goNogo)",
    "parses a Codex token_count NDJSON event stream (incl. reasoning_output_tokens)",
    "takes the LAST cumulative token_count when several appear",
    "accepts a token_count event nested under msg.info and computes total when absent",
    "returns null when the output carries no usage",
    "returns null for empty or malformed input",
    "ignores an empty usage object (no real numbers reported)",
    "exposes the usage-emit flag so spawned workers emit a per-run envelope",
    "extracts per-run usage from a realistic `codex exec --json` event stream (contract)",
    "falls back to last_token_usage when total_token_usage is absent",
    "maps OpenAI completion_tokens_details.reasoning_tokens → reasoningTokens",
    "parses the REAL codex 0.138.0 `exec --json` v2 stream → real non-zero usage (goNogo)",
    "parses a minimal v2 `turn.completed.usage` (documented 3-field SDK shape)"
   ],
   "providers/codex.test.ts": [
    "should have name \"codex\"",
    "should support gpt-4.1, o3, o4-mini models",
    "supports the required canonical Codex identities without duplicates",
    "should support gpt-5.5, gpt-5-mini, gpt-4.1-mini models",
    "should implement ProviderAdapter interface",
    "should spawn codex process with exec --full-auto and correct model",
    "should have exec as the first arg and --full-auto as second",
    "should pass o3 model correctly",
    "should pass o4-mini model correctly",
    "should reject unsupported model",
    "should reject sonnet model",
    "should reject haiku model",
    "should throw when spawning duplicate taskId",
    "should pass prompt as positional arg (not stdin)",
    "should use opts.projectDir if provided",
    "should use constructor projectDir when opts.projectDir is absent",
    "should create tasks directory if it does not exist",
    "should write heartbeat file on spawn",
    "should always use --full-auto (autoApprove has no separate effect)",
    "should not include --allowed-tools (not an official Codex CLI flag)",
    "should register exit handler on child process",
    "should inject DECKENT_OPENAI_API_KEY as OPENAI_API_KEY when OPENAI_API_KEY is missing",
    "should not overwrite OPENAI_API_KEY with DECKENT_OPENAI_API_KEY when both present",
    "should prefer an explicit owned OPENAI_API_KEY over host credentials",
    "should scrub canonical, custom and raw deck credentials without mutating the host env",
    "should keep subscription/session spawn free of every provider credential",
    "should kill a running worker",
    "should remove worker from tracking after kill",
    "should throw when killing non-existent worker",
    "should return empty array initially",
    "should return spawned worker taskIds",
    "should not include killed workers",
    "should return true when codex --version succeeds and API key set",
    "should return false when both OPENAI_API_KEY and DECKENT_OPENAI_API_KEY are missing and no subscription",
    "should return true when only DECKENT_OPENAI_API_KEY is set",
    "should return false when codex --version fails",
    "should return false when spawnSync throws",
    "should call codex --version",
    "should return true with subscription auth when no API key",
    "should return api_key when OPENAI_API_KEY is set",
    "should return api_key when only DECKENT_OPENAI_API_KEY is set",
    "should return subscription when codex login status reports logged in",
    "should return none when no API key and login status reports logged out",
    "should return none when no API key and spawnSync throws",
    "should prefer API key over subscription check",
    "should return rust when output contains \"codex\" without \"codex-cli\"",
    "should return node when output contains \"codex-cli\"",
    "should return unknown when codex --version fails",
    "should return unknown when spawnSync throws",
    "should return unknown when output is empty",
    "should build command with exec --full-auto and $(cat promptPath)",
    "should include correct model in command",
    "should not include --allowed-tools (not an official Codex CLI flag)",
    "should always use --full-auto regardless of autoApprove option",
    "should use --full-auto even when autoApprove is false",
    "should use $(cat promptPath) for file-based prompt",
    "should start with codex exec",
    "should not include --quiet (removed in exec format)",
    "should not include stdin redirection (prompt is inline arg)",
    "should return a read-only ephemeral command from the canonical Codex spec",
    "should carry dash-prefixed and shell-metacharacter prompts only through stdin",
    "should include the model",
    "should expose the registry apiId as both wire and receipt model",
    "should use ignore for stdin (prompt passed as arg, not piped)",
    "should map premium to gpt-5.5",
    "should map standard to gpt-4.1",
    "should map economy to gpt-5-mini",
    "should return correct model for each tier",
    "returns ready=true when binary present + OPENAI_API_KEY set",
    "returns ready='partial' when binary present but no API key nor subscription",
    "returns ready=false when binary not found",
    "returns ready=true via subscription auth (codex login status logged in)",
    "detects DECKENT_OPENAI_API_KEY as valid auth source",
    "getProjectDir should return constructor projectDir",
    "getLogPath should return correct log path",
    "getWorkerEntry should return undefined for unknown task",
    "getWorkerEntry should return entry for spawned worker",
    "should create a CodexAdapter instance",
    "should create adapter with given project directory",
    "should pass options to constructor"
   ],
   "providers/cred-scrub-all-adapters.test.ts": [
    "removes every key in scrubKeys, keeps everything else",
    "is a no-op copy when scrubKeys is empty",
    "never mutates the input hostEnv",
    "tolerates scrubKeys not present in hostEnv (missing key is a no-op delete)",
    "scrubs a config-driven custom provider key exactly like a built-in one",
    "scrubs foreign keys then re-injects ownEnv on top",
    "with no ownEnv, the child gets NO credential key at all (subscription/session auth fallback)",
    "never mutates the input hostEnv",
    "$cli worker sees ONLY its own credential ($ownKey) — every other present host key is scrubbed",
    "subscription-mode worker (no opts.env) gets NO credential key for any provider",
    "preserves non-credential host vars (PATH) byte-for-byte across the whole matrix",
    "OllamaAdapter.spawn() removes every canonical and config-driven provider secret",
    "OpenAICompatibleAdapter.spawn() retains only its own explicitly supplied credential",
    "OpenRouterProvider.spawn() retains only its host-resolved credential",
    "GeminiAdapter.spawn() retains only its normalized Gemini credential",
    "CursorAdapter.spawn() scrubs every provider credential for session auth",
    "the static base scrub set matches BASE_KEYS used throughout this file"
   ],
   "providers/cross-provider-keys-scrub.test.ts": [
    "returns the static base set (no registry), including non-ambient OpenRouter auth",
    "unions a config provider runtime key and raw deck alias onto the base set",
    "dedupes a config apiKeyEnv that collides with a base key, and is deterministic",
    "ignores entries with missing / blank apiKeyEnv (CLI-kind aliases carry none)",
    "BASE_PROVIDER_CREDENTIAL_ENV mirrors core/provider.ts applyDeckSecretsToEnv",
    "FOREIGN worker (codex) — config provider MY_LLM_KEY is scrubbed when registry is wired",
    "OWNING worker (my-llm) — MY_LLM_KEY re-injected from opts.env only, host value scrubbed",
    "PRE-FIX RED: WITHOUT the registry, the static base set misses MY_LLM_KEY → it leaks",
    "subscription claude (no opts.env) gets NO ANTHROPIC_API_KEY even with a registry (ADR-076)",
    "preserves base non-secret host vars (PATH/LANG/probe) byte-for-byte",
    "does not mutate the host process.env while building the child env"
   ],
   "providers/cursor-provider-evidence-sources.test.ts": [
    "registers the subscription CLI scope on BOTH backends",
    "account identity is a typed hold with an opaque cursor-account ref (never fabricated)",
    "limit observation is typed unavailable — zero windows, advisory, no invented reset clock",
    "reachability is typed unsupported — no fake reachable verdict on either backend",
    "the runtime bootstrap registration set includes the cursor dual-backend scope"
   ],
   "providers/cursor.test.ts": [
    "buildCommand is byte-exact and read-only by default",
    "extracts provider-reported usage from the proven result envelope",
    "isAvailable probes cursor-agent on PATH without requiring auth",
    "spawn uses the cross-platform invocation and scrubs credentials"
   ],
   "providers/deckbroker-wire.test.ts": [
    "flag omitted → deckBroker is null, providerEnvOverrides unaffected (flag-off byte-identical)",
    "flag explicitly false → deckBroker is null",
    "flag true + auth_mode \"subscription\" → deckBroker stays null (no .deck surface at all, mirrors providerEnvOverrides gate)",
    "flag true + auth_mode \"api\" → deckBroker is a live DeckBroker whose resolveForTask matches providerEnvOverrides",
    "flag true → deckBroker resolution is task-scoped: two tasks each get an independent grant, denials are audited",
    "flag true → deckBroker honors config.providers.registry for a config-driven openai-compatible provider (parity with providerEnvOverrides)",
    "opts.deckBroker absent → env built exactly as before (flag-off byte-identical regression pin)",
    "opts.deckBroker granted → child env carries ONLY the broker-resolved credential for THIS task",
    "two different taskIds spawned off the SAME broker each get their own independent grant — no leak between tasks",
    "opts.deckBroker denial fail-closes credential reinjection from opts.env",
    "opts.deckBroker denies (no secret configured) with no opts.env fallback → worker gets no credential, still spawns",
    "the .deck project path never appears in the spawned child env when a broker is used"
   ],
   "providers/docker-bounded-reachability-evidence.test.ts": [
    "forwards the exact provider request and owner-projected bounds",
    "returns not-run for a missing projection without resolving transport",
    "returns unsupported when the lazy resolver has no canonical transport",
    "maps timeout and rejection fail-closed without retaining provider details",
    "maps transport error %s to %s",
    "produces deterministic opaque refs without retaining request secrets"
   ],
   "providers/gemini-integration.test.ts": [
    "gemini --version returns 0",
    "isCliInstalled returns true",
    "buildArgs produces valid argument array",
    "buildCommand produces valid CLI command string",
    "buildPlannerCommand uses gemini binary",
    "parseGeminiOutput handles structured JSON",
    "parseGeminiOutput handles plain text fallback",
    "parseGeminiOutput returns empty response for empty input",
    "parseGeminiOutput extracts candidates format",
    "parseGeminiOutput prefers response field over candidates",
    "adapter name is gemini",
    "supported models include gemini-2.5-pro"
   ],
   "providers/gemini-isAvailable.test.ts": [
    "binary missing → available=false, partial=false, hints to install",
    "binary OK + no API key → available=false, partial=true (with hints)",
    "binary OK + GOOGLE_API_KEY set → available=true, partial=false",
    "isAvailable() returns true when binary OK + API key set"
   ],
   "providers/gemini-usage.test.ts": [
    "is implemented (pre-fix the adapter had no extractUsage)",
    "parses a real gemini --output-format json usageMetadata → real tokens incl. reasoning (goNogo)",
    "normalizes reasoning from thoughtsTokenCount specifically (reasoning=thoughts)",
    "omits reasoningTokens entirely when Gemini reports no thoughts (sparse output)",
    "parses a stream-json (NDJSON) event stream and takes the LAST cumulative usageMetadata",
    "takes the LAST usageMetadata when several cumulative chunks appear",
    "computes total as input+output when Gemini omits totalTokenCount",
    "returns null when the output carries no usageMetadata",
    "returns null for empty or malformed input",
    "ignores an empty usageMetadata object (no real numbers reported)"
   ],
   "providers/gemini.test.ts": [
    "has correct provider name",
    "supports gemini models from current catalog",
    "does not include non-gemini models",
    "uses correct generativelanguage.googleapis.com base URL",
    "streaming endpoint uses streamGenerateContent with alt=sse",
    "uses v1beta API version",
    "auth header constant is x-goog-api-key per official docs",
    "getApiKey reads from GOOGLE_API_KEY env var",
    "getApiKey prefers DECKENT_GOOGLE_API_KEY over GOOGLE_API_KEY",
    "getApiKey accepts the Gemini CLI native GEMINI_API_KEY",
    "getApiKey returns undefined when both env vars missing",
    "getApiKey falls back to GOOGLE_API_KEY when DECKENT_GOOGLE_API_KEY is not set",
    "isAvailable returns true when gemini CLI installed and API key is set",
    "isAvailable returns false when GOOGLE_API_KEY is not set",
    "isAvailable returns false when gemini CLI is not installed",
    "isAvailable returns false when gemini CLI throws",
    "isCliInstalled returns true when gemini --version exits 0",
    "isCliInstalled returns false when gemini --version fails",
    "isCliInstalled returns false when gemini binary not found",
    "buildArgs returns correct Gemini CLI arguments",
    "buildArgs includes -p flag for headless mode",
    "buildArgs includes --output-format json",
    "buildArgs includes -m flag for model selection",
    "buildArgs includes --approval-mode yolo + --skip-trust for headless worker mode",
    "buildStreamingApiScript uses streamGenerateContent endpoint",
    "buildStreamingApiScript uses x-goog-api-key header",
    "buildStreamingApiScript reads SSE data: lines",
    "buildStreamingApiScript uses ReadableStream reader",
    "buildStreamCommand uses --no-buffer for streaming curl",
    "streaming endpoint appends alt=sse query param",
    "spawn creates subprocess with gemini CLI",
    "spawn passes the key as GEMINI_API_KEY (Developer-API), not GOOGLE_API_KEY (F3-GEMKEY)",
    "spawn writes heartbeat file",
    "spawn tracks worker entry",
    "spawn throws for unsupported model",
    "spawn succeeds without API key via OAuth session (does not throw)",
    "spawn throws for duplicate taskId",
    "spawn uses custom projectDir from opts",
    "spawn creates tasks directory if missing",
    "spawn removes worker on child exit",
    "spawn registers stdout and stderr data listeners for fast-fail monitoring",
    "spawn appends stdout data to log file via appendFileSync",
    "kill terminates running worker with SIGTERM",
    "kill throws for unknown taskId",
    "listWorkers returns empty array initially",
    "listWorkers tracks multiple workers",
    "buildCommand (worker, autoApprove) uses yolo + skip-trust",
    "buildCommand WITHOUT autoApprove does NOT emit yolo/skip-trust (security: no unconditional bypass)",
    "buildCommand uses cat to read prompt file",
    "buildCommand includes model parameter",
    "buildPlannerCommand returns gemini CLI command + args",
    "buildPlannerCommand uses correct model in args",
    "buildPlannerCommand does not throw when API key is missing",
    "validateApiKey returns invalid when key is not set",
    "validateApiKey returns invalid for too-short key",
    "validateApiKey accepts AIza-prefixed keys",
    "validateApiKey rejects short non-AIza keys",
    "validateApiKey accepts long non-AIza keys (service account etc)",
    "getLogPath returns correct path",
    "getProjectDir returns constructor projectDir",
    "timeout kills worker after configured ms",
    "no timeout when defaultTimeoutMs is 0",
    "timeout clears on child exit",
    "LOGIN_FAST_FAIL pattern matches gemini login prompt",
    "QUOTA_FAST_FAIL pattern matches 429 and RESOURCE_EXHAUSTED",
    "fast-fail kills child immediately on login prompt in stdout",
    "fast-fail kills child immediately on login prompt in stderr",
    "fast-fail kills child after 2 quota/429 hits",
    "fast-fail does NOT kill child on a single quota/429 hit",
    "fast-fail does not trigger on healthy output",
    "buildApiScript escapes backticks in prompt (REST fallback)",
    "buildApiScript escapes carriage returns (REST fallback)",
    "parseGeminiOutput parses JSON with response field",
    "parseGeminiOutput parses JSON with candidates structure",
    "parseGeminiOutput extracts usage metadata",
    "parseGeminiOutput handles plain text (non-JSON) output",
    "parseGeminiOutput handles empty string",
    "parseGeminiOutput handles whitespace-only string",
    "parseGeminiOutput handles JSON string value",
    "parseGeminiOutput without usageMetadata returns undefined stats",
    "parseGeminiOutput parses stream-json (NDJSON) with multiple chunks",
    "parseGeminiOutput parses stream-json with candidates structure",
    "parseGeminiOutput extracts usage from last stream-json chunk",
    "parseGeminiOutput falls back to plain text when NDJSON lines are not valid JSON",
    "parseGeminiOutput stream-json without usage returns undefined stats",
    "supports gemini-3.1-pro-preview model from registry",
    "supports all 4 gemini models from registry",
    "getCliVersion returns version string when CLI is installed",
    "getCliVersion returns undefined when CLI is not installed",
    "getCliVersion returns undefined when CLI throws",
    "getCliVersion returns undefined when stdout is empty",
    "returns ready=true when binary AND auth both present",
    "returns ready='partial' when binary present but GOOGLE_API_KEY missing",
    "returns ready=false when binary not found",
    "exposes parsed semver version string",
    "honors DECKENT_GOOGLE_API_KEY for partial→ready transition",
    "buildGeminiSpawnEnv strips GEMINI_CLI_IDE_* vars (no IDE-session binding)",
    "buildGeminiSpawnEnv injects the key as GEMINI_API_KEY (Developer-API), not GOOGLE_API_KEY (F3-GEMKEY)",
    "buildGeminiSpawnEnv strips an inherited GOOGLE_API_KEY so the CLI stays on the Developer endpoint (F3-GEMKEY)",
    "buildGeminiSpawnEnv keeps only Gemini-owned auth and scrubs config-driven foreign credentials",
    "buildGeminiSpawnEnv keeps OAuth/session mode free of inherited provider credentials",
    "buildGeminiSpawnEnv sets GEMINI_NONINTERACTIVE=1 to prevent interactive login hang",
    "createGeminiAdapter returns GeminiAdapter instance",
    "createGeminiAdapter passes timeout option"
   ],
   "providers/ollama-agentic-worker.test.ts": [
    "spawns `node <workerEntryPath> <taskId> <apiId> <host>` — not curl",
    "accepts any model returned by /api/tags after refreshSupportedModels",
    "keeps static catalog as fallback when /api/tags probe fails",
    "reads task json, calls the runner, and writes an api-surface-shaped .result",
    "writes a NO_GO .result and exits 1 when the runner throws",
    "writes a NO_GO .result when argv is missing pieces",
    "kill(taskId) sends SIGTERM to the spawned worker and clears the workers map",
    "exceeding defaultTimeoutMs fires SIGKILL via setTimeout",
    "child `exit` event clears the workers map even without explicit kill"
   ],
   "providers/ollama-health-gate.test.ts": [
    "returns available=true with populated model list when /api/tags returns models",
    "returns available=false with actionable reason when host is down — no throw, no hang",
    "returns available=false when the requested model is not installed",
    "returns available=true when the requested model is present",
    "returns available=false when /api/tags responds with an empty model list",
    "returns available=false when /api/tags returns non-2xx status",
    "returns available=false when /api/tags body has no models field",
    "does not fall back to another provider — honest-fail on error"
   ],
   "providers/ollama-utf8-stream.test.ts": [
    "reassembles Turkish multi-byte chars split byte-by-byte across reads",
    "reassembles across multiple NDJSON lines, each split mid-character",
    "does not corrupt a 3-byte / 4-byte sequence (em-dash + emoji) interleaved with Turkish"
   ],
   "providers/ollama.test.ts": [
    "returns false when the Ollama server is unreachable",
    "returns true when /api/tags returns 200",
    "returns false when /api/tags returns a non-2xx status",
    "reports models + version from the mocked endpoints",
    "reports not-ready when the server is offline",
    "reports ready=partial when /api/tags responds without any models (Sprint 192 Task 192-007)",
    "reports ready=partial with empty models array from /api/tags (Sprint 192 Task 192-007)",
    "reports ready=true once at least one model is present (Sprint 192 Task 192-007)",
    "POSTs to /api/generate and parses response + token stats",
    "throws ProviderError when the server returns non-2xx",
    "rejects unsupported models",
    "shapes the result with hints when server is offline",
    "shapes the result with available=true when server responds",
    "shapes the result with partial=true when server reachable but no models (Sprint 192 Task 192-007)",
    "parses a standard /api/generate JSON response",
    "falls back to raw text when the body is not JSON",
    "handles /api/chat-shaped message.content",
    "returns empty response for empty input",
    "registers 4 ollama models across the tier ladder",
    "maps qwen2.5-coder:32b → premium tier and llama3.2:3b → economy tier",
    "costPerMillion is zero for local Ollama models",
    "getModelForTier() walks the tier ladder",
    "respects DECKENT_OLLAMA_HOST env override",
    "respects the explicit host constructor option (trims trailing slash)",
    "returns an OllamaAdapter instance"
   ],
   "providers/openai-compatible-local.test.ts": [
    "sends no Authorization header and never requires or synthesizes a key",
    "parses live identity and health responses and uses them for availability",
    "keeps the keyless local provider after loadConfig resolution"
   ],
   "providers/openai-compatible.test.ts": [
    "round-trips a request through fetch with correct URL, body, and Bearer header",
    "throws ProviderError when the apiKey env var is unset",
    "throws ProviderError on non-2xx upstream responses",
    "throws ProviderError for unsupported models",
    "returns true when the apiKey env var is set",
    "returns false when the apiKey env var is unset",
    "diagnoseAvailability() surfaces a hint when the key is missing",
    "DeepSeek preset has the documented baseURL and apiKeyEnv",
    "Qwen (DashScope compat-mode) preset is wired correctly",
    "GLM (Zhipu) preset is wired correctly",
    "spawn() no longer throws — launches the node agentic-worker entry",
    "listWorkers() returns empty array before any spawn",
    "merges extension fields into the request body",
    "CANNOT overwrite model / messages / stream",
    "leaves the body byte-identical when no extraBody is configured",
    "throws ProviderError carrying the embedded error, never an empty success",
    "throws on a 200 with missing/empty choices (no error field either)"
   ],
   "providers/openrouter-usage.test.ts": [
    "is implemented (the contract method exists)",
    "parses a REAL OpenRouter response with cache + reasoning detail (goNogo)",
    "covers the gateway matrix: plain usage (LiteLLM/vLLM, no detail blocks)",
    "covers DeepSeek-direct cache shape (`prompt_cache_hit_tokens`)",
    "falls back to inputTokens + outputTokens when the gateway omits total_tokens",
    "handles a streamed SSE response with usage in the final chunk",
    "takes the LAST recognizable usage when several chunks report it",
    "returns null when the response carries no usage",
    "returns null for an empty usage object (no real numbers reported)",
    "returns null for empty or malformed input",
    "clamps negative / non-numeric token counts to 0 (defensive)"
   ],
   "providers/openrouter.test.ts": [
    "POSTs /chat/completions and maps a real OpenRouter response (content + usage + model)",
    "honors a config-overridden baseURL (trailing slash stripped)",
    "surfaces tool_calls when the model requests tools",
    "rejects a blank model id without ever calling fetch",
    "throws honestly (no silent-empty) when the deck secret is missing — never calls fetch",
    "throws with the real status + body on a 4xx — no retry (client error)",
    "never resolves to an empty/undefined result on failure — always throws",
    "retries exactly once on a network error, then succeeds",
    "retries exactly once on a network error, then throws honestly if it fails again",
    "retries exactly once on a transient 5xx, then succeeds",
    "retries exactly once on a transient 5xx, then throws with the real status if it fails again",
    "maps a real OpenRouter usage block into the normalized TokenUsage shape",
    "falls back to inputTokens + outputTokens when total_tokens is absent",
    "returns null for no usage, empty usage, or malformed input",
    "never assigns the resolved key to process.env, across success, retry, and error paths",
    "isAvailable()/diagnoseAvailability() resolve the key without ever touching process.env",
    "isAvailable() is false (never throws) when the deck secret is absent",
    "resolves DECKENT_OPENROUTER_API_KEY as a fallback when the bare key is absent ($DECK: dual-lookup parity)",
    "exposes the required ProviderAdapter surface",
    "executes planner calls with canonical api transport and backend identity",
    "carries rich provider usage and an opaque deterministic envelope digest",
    "keeps partial or malformed provider usage unknown: %j",
    "does not retry an ambiguous planner network failure",
    "is constructible via the createOpenRouterAdapter factory",
    "omits `reasoning` entirely when unconfigured (byte-identical to pre-477 body)",
    "sends the configured `reasoning` object verbatim",
    "forwards reasoning to a spawned worker via ENV, never argv (win32-safe)",
    "sets no extra-body env when reasoning is unconfigured",
    "throws ProviderError with the embedded upstream cause instead of empty content",
    "throws on 200 with no choices at all"
   ],
   "providers/pgid-teardown.test.ts": [
    "spawn() passes detached:true to spawnImpl on a POSIX platform",
    "does not regress MOAT-2: child.unref() is still called after spawn",
    "kill() (SIGTERM) signals process.kill(-pid, \"SIGTERM\") — the process-group form",
    "SIGTERM→SIGKILL escalation also targets the process group",
    "a worker that exits on SIGTERM is not SIGKILL-escalated (group form, escalation cleared on exit)",
    "falls back to the direct child pid when the group-kill form throws (e.g. group already reaped)",
    "spawn() does NOT set detached on win32",
    "kill() terminates the complete Windows child tree without POSIX group signals",
    "SIGTERM→SIGKILL escalation forces the complete Windows child tree"
   ],
   "providers/process-group-kill.test.ts": [
    "spawn() passes detached:true to spawn on a POSIX platform",
    "kill() (SIGTERM) signals process.kill(-pid, \"SIGTERM\") — the process-group form",
    "SIGTERM→SIGKILL escalation also targets the process group after the grace window",
    "a worker that exits on SIGTERM is not SIGKILL-escalated (escalation cleared on exit)",
    "falls back to the direct child pid when the group-kill form throws (e.g. group already reaped)",
    "a long-running grandchild-forking worker leaves no orphan: group-kill reaches the whole tree, no manual per-grandchild teardown needed",
    "spawn() does NOT set detached on win32",
    "kill() terminates the whole process tree with taskkill /T",
    "SIGTERM→SIGKILL escalation on win32 adds taskkill /F for the whole tree"
   ],
   "providers/provider-authority-runtime-bootstrap.test.ts": [
    "builds one immutable six-slot inventory and shares the lazy Docker resolver",
    "keeps rollout disabled without an authored parent or project layer",
    "holds a missing authored parent before key/source/provider work",
    "consumes the separate global envelope and then holds missing open-only custody",
    "enables once an authored layer exists and never falls back after open HOLD"
   ],
   "providers/provider-tier-model-authority.test.ts": [
    "returns exact API IDs from the live registry for both adapters",
    "fails loudly when the registry has no Codex tier authority",
    "fails loudly when the registry has no Gemini tier authority"
   ],
   "providers/sandbox.test.ts": [
    "should have name \"claude-sandbox\"",
    "should extend SubprocessSpawnBackend",
    "should support fable, versioned opus, sonnet, haiku models",
    "should support exactly 5 models",
    "should default memoryLimitMb to 512",
    "should use custom memoryLimitMb when provided",
    "should default allowedDirs to [projectDir]",
    "should use custom allowedDirs when provided",
    "should default blockNetwork to false",
    "should set blockNetwork when provided",
    "should not throw for projectDir itself",
    "should not throw for a subdirectory of projectDir",
    "should throw ProviderError for a directory outside allowedDirs",
    "should include \"Sandbox scope violation\" in error message",
    "should include the offending path in the error message",
    "should not throw for any of multiple allowedDirs",
    "should throw when directory only partially matches an allowedDir",
    "should include NODE_OPTIONS with memory limit",
    "should include default 512MB memory limit",
    "should preserve existing NODE_OPTIONS and append memory flag",
    "should not set proxy vars when blockNetwork is false",
    "should set proxy vars when blockNetwork is true",
    "should set no_proxy to empty string when blockNetwork is true",
    "should copy provided base env vars",
    "should use process.env as base when no arg provided",
    "should spawn when dir is within projectDir",
    "should throw ProviderError when opts.projectDir is outside allowedDirs",
    "should not call spawn when scope is violated",
    "should pass through spawn options to parent when scope is valid",
    "should return true when both checks succeed",
    "should return false when claude is not available",
    "should return false when node memory limit check fails",
    "should return a Promise",
    "getMemoryLimitMb should return configured limit",
    "getAllowedDirs should return a copy (not the internal array)",
    "isNetworkBlocked should return false by default",
    "isNetworkBlocked should return true when configured",
    "should create a SandboxSpawnBackend instance",
    "should create backend with given project directory",
    "should pass sandbox options to the backend",
    "should use default options when none provided"
   ],
   "providers/session-usage-store.test.ts": [
    "sums all turns message.usage into the 4 fields (incl. cacheCreation) + tags source",
    "matches the exact {sessionId}.jsonl when provided",
    "skips usage-less and corrupt lines, summing only real turns",
    "returns null when no session jsonl exists (honest no-source)",
    "returns null when sessionRoot directory is absent (never throws)",
    "returns null for a jsonl with no usage turns at all",
    "is a documented null seam for codex and gemini (Law #2 phase2)",
    "selects the newest jsonl inside the spawn window",
    "slugifies an absolute cwd to the Claude projects dir name"
   ],
   "providers/spawn-safe-crossplatform.test.ts": [
    "win32: wraps via cmd.exe /c, preserving original args",
    "POSIX: command/args stay byte-identical to the pre-change bare spawn",
    "POSIX: spawnOpts.shell is false (never shell:true + args array — DEP0190)",
    "win32: wraps via cmd.exe /c, preserving original args",
    "POSIX: command stays \"gemini\", args unchanged",
    "POSIX: spawnOpts.shell is false",
    "win32: wraps `node <entry>...` via cmd.exe /c",
    "POSIX: command stays \"node\", args unchanged (regression guard)",
    "win32: wraps `node <entry>...` via cmd.exe /c",
    "POSIX: command stays \"node\", args unchanged (regression guard)",
    "win32: wraps `node <entry>...` via cmd.exe /c",
    "POSIX: command stays \"node\", args unchanged (regression guard)"
   ],
   "providers/subprocess-auth-noleak.test.ts": [
    "codex worker child env carries ONLY OPENAI_API_KEY — no ANTHROPIC/GOOGLE leak",
    "claude SUBSCRIPTION worker (no opts.env) gets NO ANTHROPIC_API_KEY (ADR-076)",
    "preserves base non-secret host vars (PATH/LANG/probe) byte-for-byte",
    "api claude worker (opts.env carries its own key) gets ANTHROPIC_API_KEY, no foreign keys",
    "gemini worker gets ONLY GOOGLE_API_KEY even when host carries all three",
    "scrubs openai-compatible provider keys (deepseek/qwen/zhipu) from the host env too",
    "does not mutate the host process.env while building the child env"
   ],
   "providers/subprocess-broker-fail-closed.test.ts": [
    "CONTROL granted → child env carries the BROKER credential (not opts.env, not the host ambient one)",
    "denied \"${testCase.reason}\" → child env has NO ${OWN_KEY}, even though opts.env supplies one",
    "every denial reason in the closed union is covered by this file",
    "a denied spawn never leaks the .deck project path into the child env"
   ],
   "providers/subprocess-legacy-path.test.ts": [
    "no broker + opts.env present → legacy debugLog tag fires, opts.env is reinjected unchanged, legacy marker set (not denial)",
    "no broker + no opts.env → legacy debugLog tag still fires (broker was never supplied), child env carries no credential (unchanged)",
    "broker supplied + denied → ORIGINAL denial debugLog tag fires, legacy tag never fires, legacy marker absent",
    "broker supplied + granted → neither legacy nor denied signal fires"
   ],
   "providers/subprocess-live-activity.test.ts": [
    "flag OFF → single-envelope args + stdout FD-redirect, NO activity (byte-stable)",
    "flag ON → stream-json args, piped stdout, per-tool ACTIVITY + raw .log preserved",
    "flag ON with no stdout still spawns cleanly (defensive: null stdout)",
    "flag ON: the piped stdout is UNREF\\'d so it never re-pins the coordinator loop (MOAT-2, ADR-G-013)"
   ],
   "providers/subprocess-live-budget.test.ts": [
    "enables measured stream without live_trace and terminates on the first exceeded sample",
    "fails before spawn when the provider has no measured stream mode",
    "fails before spawn for maxUsd until live immutable pricing is available",
    "contains the exact worker and persists terminal unmeasurable evidence when stdout is unavailable",
    "contains the exact worker and persists terminal unmeasurable evidence on stdout observer error"
   ],
   "providers/subprocess-moat2-linger.test.ts": [
    "child.unref() is called after spawn so the coordinator can drain independently",
    "the 15s heartbeat setInterval is unref’d after spawn (hasRef === false)",
    "the optional kill-timeout setTimeout is unref’d when defaultTimeoutMs > 0",
    "kill() clears the heartbeat interval immediately (not waiting for child exit)",
    "kill() also terminates the child process (SIGTERM) and drops the worker",
    "a worker that IGNORES SIGTERM is SIGKILL-escalated after the grace window",
    "a worker that EXITS on SIGTERM is not SIGKILL’d (escalation cleared on exit)",
    "emitting the child exit event clears the heartbeat interval without error"
   ],
   "providers/subprocess.test.ts": [
    "should have name \"claude-subprocess\"",
    "should support fable, versioned opus, sonnet, haiku models",
    "should support exactly 5 models",
    "should expose getProjectDir()",
    "holds the production remote backend before child spawn when no budget exists",
    "should call node:child_process spawn",
    "should spawn with \"claude\" command",
    "should include model in spawn args",
    "should include --model flag in args",
    "should write prompt via stdin",
    "should close stdin after writing prompt",
    "should write heartbeat on spawn",
    "should create task log file in .tasks/ directory",
    "should throw if task is already running",
    "should use opts.projectDir if provided",
    "should fall back to constructor projectDir when no opts.projectDir",
    "should include allowedTools in spawn args when provided",
    "should include --dangerously-skip-permissions when autoApprove is true",
    "should redirect stdout/stderr to log file fd",
    "should defer log fd close to child exit handler (not immediate)",
    "should create tasks dir if it does not exist",
    "should register worker in internal map after spawn",
    "should kill the process with SIGTERM",
    "should remove worker from internal map after kill",
    "should throw ProviderError when task is not running",
    "should return empty array when no workers are running",
    "should return task IDs of running workers",
    "should not include killed workers",
    "should return a Promise",
    "should return true when claude exits with code 0",
    "should return false when claude exits with non-zero code",
    "should return false when spawn emits error",
    "should build basic command",
    "should include --allowedTools when provided",
    "should include --dangerously-skip-permissions when autoApprove is true",
    "should not include --dangerously-skip-permissions when autoApprove is false",
    "should use stdin redirection from promptPath",
    "appends --effort for a valid reasoning-effort",
    "drops an invalid reasoning-effort",
    "adds --effort argv pair for a valid level",
    "omits --effort when no reasoning-effort given",
    "drops an invalid (codex-only) level",
    "should write heartbeat file on spawn",
    "should include timestamp in heartbeat",
    "should return log path for task",
    "should use project dir in path",
    "should accept defaultTimeoutMs in constructor",
    "should auto-kill with SIGKILL after timeout",
    "should create a SubprocessSpawnBackend instance",
    "should create backend with given project directory",
    "should accept timeout option",
    "should accept providerConfig option",
    "should default to CLAUDE_SUBPROCESS_CONFIG when no providerConfig given",
    "should spawn with \"claude\" command by default (backward compat)",
    "should have name \"claude-subprocess\" by default",
    "should support claude models by default",
    "should use custom cliCommand when providerConfig provided",
    "should use custom name from providerConfig",
    "should use custom supportedModels from providerConfig",
    "should use adapter.buildArgs when adapter provided",
    "should pass allowedTools through custom buildArgs",
    "should pass autoApprove through custom buildArgs",
    "should use custom buildCommandString for buildCommand()",
    "should use custom cliCommand in isAvailable()",
    "should produce different commands for different provider configs",
    "should produce different spawn commands for different configs",
    "should export CLAUDE_SUBPROCESS_CONFIG with correct cliCommand",
    "should export CLAUDE_SUBPROCESS_CONFIG with correct name",
    "should export CLAUDE_SUBPROCESS_CONFIG with buildArgs producing correct args",
    "should export CLAUDE_SUBPROCESS_CONFIG with buildCommandString producing correct command",
    "buildArgs appends --exclude-dynamic-system-prompt-sections when opted in, omits otherwise",
    "buildCommandString appends the exclude-dynamic flag before the stdin redirect when opted in",
    "should store providerConfig accessible via getProviderConfig()"
   ]
  },
  "release": {
   "release/dep-bump-audit.test.ts": [
    "fast-uri is bumped to >=3.1.4 (CVE-2026-16221 fix floor) and stays on the 3.x line (non-major)",
    "hono is bumped to >=4.12.25 (fix floor) and stays on the 4.x line (non-major)",
    "path-to-regexp is bumped to >=8.4.0 (fix floor) and stays on the 8.x line (non-major)",
    "undici is bumped to >=6.27.0 (fix floor) and stays on the 6.x line (non-major)",
    "ws is bumped to >=8.21.0 (fix floor) and stays on the 8.x line (non-major)",
    "package.json carries a justified root override forcing undici past discord.js\\'s exact 6.24.1 pin",
    "nodemailer is on the 9.x line (DEP669B semver-major bump, CC-el 2026-07-12)",
    "the real check-dependency-audit.mjs gate PASSes (exit 0) against the patched tree",
    "none of the advisory IDs closed by this bump appear in a real npm-audit findings extraction",
    "scripts/audit-exceptions.json is EMPTY — DEP669A+B closed every advisory (⏰-bıçak söküldü)"
   ],
   "release/npm-pack-whitelist.test.ts": [
    "derived a non-empty file list via one of the two sanctioned paths",
    "every packed top-level entry is in the manifest-declared whitelist plus npm package metadata",
    "normalizes nested manifest entries to their top-level package path",
    "ships npm-shrinkwrap.json as the sole root dependency lock authority",
    "ships the exact native loader and source payload required for source identity",
    "no internal/dev state leaks in (.deckent, .brain, .tasks, .locks, .dashboard, .claude, .git, node_modules, docs, examples, deckent-hub, .contracts)",
    "no test/spec source files ship (tests/, *.test.ts, *.test.js, *.spec.ts, *.spec.js)",
    "no source maps ship (no *.map) — private source paths / sourcesContent must never leak",
    "dist/ is present and non-empty",
    "every package.json bin entry resolves to a path that would ship",
    "README and LICENSE ship (root docs required for a public package)"
   ],
   "release/packed-install-contract.test.ts": [
    "produced a real tarball and extracted it to package/",
    "ships one canonical shrinkwrap and no competing root lock authority",
    "every package.json exports entry resolves to a real, importable file in the extracted tarball",
    "every package.json bin entry exists and is executable in the extracted tarball",
    "dashboard bundle (index.html + assets) is present in the extracted tarball",
    "builtins tree ships at least one agent PROMPT.md and one skill SKILL.md",
    "assets/Dockerfile.worker is present in the extracted tarball",
    "runs the installed-package native verifier unconditionally and accepts one exact receipt",
    "rejects missing, partial, artifact, source and shrinkwrap drift",
    "no dev-only source (.deckent/ .brain/ .tasks/ tests/ src/) leaked into the tarball root"
   ],
   "release/release-prepare.test.ts": [
    "accepts a bare semver",
    "strips a leading v/V",
    "preserves prerelease metadata",
    "preserves build metadata",
    "preserves combined prerelease+build metadata",
    "rejects a missing version",
    "rejects a malformed version",
    "updates only the version field, preserving everything else",
    "updates BOTH the top-level version and packages[\"\"].version",
    "round-trips the REAL npm-shrinkwrap.json touching only the two root version fields",
    "accepts a singular, non-empty, well-headed section",
    "rejects a missing section",
    "rejects duplicate headings for the same version",
    "rejects an empty section body",
    "does not prefix-match a longer sprint-suffixed heading (the exact bug REL-03 fixed)",
    "regression pin: the REAL root CHANGELOG.md current [0.100.0] section satisfies the contract",
    "produces a section that itself passes validateChangelogSectionFormat",
    "inserts before the first existing heading, preserving older sections untouched",
    "errors honestly when a required file is missing",
    "refuses to create a duplicate section for an already-present version",
    "dry-run leaves all three files byte-for-byte unchanged",
    "updates package.json + npm-shrinkwrap.json (both version fields) + CHANGELOG.md in sync, prerelease preserved",
    "--dry-run performs zero writes against the real-shaped fixture",
    "a second invocation for the same version exits 1 and does not corrupt the files",
    "missing --version prints usage and exits 1",
    "an invalid semver exits 1 with a clear error, no writes",
    "never tags, pushes, or publishes — source never imports child_process, so it cannot shell out at all",
    "next-steps guidance is owner-manual publish (no tag-triggered publish, no --provenance)",
    "exits 1 and points to release-prepare.mjs, regardless of args",
    "is NOT deleted and still carries its shebang",
    "root CHANGELOG.md declares itself canonical and no longer defers to docs/ for \"the full changelog\"",
    "docs/CHANGELOG.md declares itself an auto-generated per-sprint log, not release notes",
    "root CHANGELOG.md MRR bullet reflects the scheduler-truth born-610 correction"
   ],
   "release/validate-publish-pack.test.ts": [
    "extracts files, fileSizes, packageSizeBytes, and fileCount from a real-shaped npm pack --json fixture",
    "returns a zeroed shape for an empty string",
    "returns a zeroed shape for non-JSON garbage stdout",
    "returns a zeroed shape when the JSON has no files[] array",
    "returns a zeroed shape for an empty JSON array",
    "checkPackSizeAndCount FAILs (not a silent pass) on the resulting zeroed shape",
    "formats MB, kB, and B ranges",
    "parses a legacy npm-notice TEXT string via parsePackOutput",
    "passes a pre-parsed JSON-path object through untouched",
    "checkPackSizeAndCount accepts the JSON-path object directly (no re-parsing as text)",
    "classifies .d.ts before the generic .js bucket",
    "buckets by first two path segments + extension class",
    "sums count and bytes per category",
    "PASSES when the pack matches the baseline (clean path)",
    "FAILS when a new category appears that is absent from the baseline",
    "FAILS when a category count grows more than 10% over baseline",
    "FAILS when total packed size grows more than 5 MB over baseline",
    "FAILS honestly on an empty fileSizes list (never a silent pass)",
    "FAILS honestly when no baseline is loaded (missing/malformed scripts/pack-baseline.json)",
    "admits only the two native categories explicitly released by a passing native gate",
    "admits a baseline write only after the normal publish gates pass",
    "accepts only the normal node-gyp singleton or exact Release/obj.target hardlink pair",
    "pins the shared 16 KiB metadata and <=128/no-NUL version bounds at all three authorities",
    "accepts exactly one byte-exact canonical root npm-shrinkwrap",
    "rejects missing, duplicate, byte-count-drifted and additional root locks",
    "rejects a source package-lock even when npm pack omits it",
    "accepts exactly one complete current-platform prebuild pair",
    "rejects an additional complete prebuild pair instead of widening package authority",
    "rejects reordered or reformatted artifact JSON even when its values are unchanged",
    "rejects duplicate inventory paths and root or nested install lifecycle hooks",
    "rejects root, nested, and artifact metadata larger than 16 KiB",
    "rejects >128 and NUL versions in root, nested, and artifact metadata",
    "rejects generic Linux and WSL2 proof-cell substitution before package evaluation",
    "rejects root, nested, and artifact metadata larger than 16 KiB",
    "rejects >128 and NUL versions in root, nested, and artifact metadata",
    "passes when lint:builtins-drift exits 0",
    "fails when lint:builtins-drift exits non-zero"
   ],
   "release/version-rebaseline.test.ts": [
    "canonical package version is 0.100.0 (#1)",
    "runtime DECKENT_VERSION (CLI --version source) resolves to 0.100.0 (#2)",
    "current-version docs no longer advertise the retired 1.0.0-beta.1 (#4)",
    "the pre-0.100.0 sprint ledger is archived, not deleted (#5)",
    "root CHANGELOG.md is the product changelog: has [0.100.0] + [Unreleased] and demotes beta.1 (#6)",
    "the sprint-finalizer changelog automation no longer mints product-version headers (#6b)",
    "FAILS a version bump with no matching root-CHANGELOG section (#7)",
    "FAILS when the matching section exists but is empty (#7b)",
    "does NOT prefix-match a sprint-tagged heading (#7c)",
    "PASSES a version bump with a matching, non-empty section (#8)",
    "PASSES for the repository itself (0.100.0 has a real section) (#8b)",
    "FAILS on a duplicate version heading — exactly one section required (#7d)",
    "treats 0.100.0 as a product successor of the retired 1.0.0-beta.1 (upgrade)",
    "treats a later 0.x (0.101.0) as a successor of 1.0.0-beta.1 too",
    "does NOT invent a successor for a real downgrade or same-version",
    "applies ONLY to the exact retired 1.0.0-beta.1 string, never to arbitrary pre-releases",
    "depends on the local repo via file:../.., not a tagless npm range (#3)",
    "the local repo packs to the expected deckent-0.100.0 tarball name (#3b)",
    "validate:publish never shells out to npm publish (#10)",
    "the release workflow runs no automatic npm publish and creates no GitHub Release (#9)",
    "release workflow uses least-privilege permissions and no --provenance (#5)",
    "release-prepare.mjs documents owner-manual publish (no --provenance, no tag-triggered publish) (#4)"
   ]
  },
  "scripts": {
   "scripts/adr-sync-parity.test.ts": [
    "collapses whitespace runs and lowercases",
    "treats undefined/null as empty string",
    "is stable for content differing only in incidental whitespace/casing",
    "differs when the actual text differs",
    "parses id/title/body, stripping the header line",
    "reports ok with no issues when db and export agree",
    "ignores incidental whitespace/casing drift between db and export",
    "flags MISSING when an accepted DB ADR has no export entry",
    "flags STALE when the export has an id not accepted in the DB",
    "flags DIVERGENT when content digests differ on real text (RCA-style drift)",
    "exits 0 and reports PASS when db and export are in sync",
    "exits 1 and reports MISSING for an accepted ADR absent from the export",
    "exits 1 and reports STALE for an export entry no longer accepted in the DB",
    "exits 1 and reports DIVERGENT when the export text drifted from the DB",
    "fails closed with exit 2 when the db file does not exist",
    "fails closed with exit 2 when the export file does not exist"
   ],
   "scripts/adr-validator.test.ts": [
    "parses well-formed ADR entries",
    "parses multiple ADRs",
    "handles status with parenthetical annotation",
    "returns no errors for valid ADRs",
    "detects missing Status field",
    "detects invalid status value",
    "detects duplicate ID without superseded",
    "allows duplicate ID when one is superseded",
    "validates the real DECISIONS.md file",
    "ADR-036 self-referential passes validation",
    "returns failure for nonexistent file"
   ],
   "scripts/affected-tests.test.ts": [
    "converts backslash separators to forward slashes",
    "leaves posix paths unchanged",
    "accepts tests/**/*.test.ts",
    "accepts tests/**/*.test.tsx",
    "rejects non-test helper files under tests/",
    "rejects src files",
    "extracts a named import-from specifier",
    "extracts an export-from specifier",
    "extracts an export-star-from specifier",
    "extracts a literal dynamic import() specifier",
    "extracts a bare side-effect import",
    "does not double-count a normal `from` import as a bare side-effect import",
    "extracts vi.mock specifiers",
    "extracts vi.doMock specifiers",
    "counts template-literal dynamic imports without producing a specifier",
    "counts a bare package import specifier (still returned for the caller to classify)",
    "resolves a .js specifier to a sibling .ts file",
    "resolves a .js specifier to a sibling .tsx file when no .ts exists",
    "resolves an explicit index.js specifier to index.ts",
    "resolves an implicit directory specifier to its index.ts",
    "returns null for a bare package specifier",
    "returns null for an alias specifier",
    "returns null for a relative specifier with no matching candidate",
    "does not extension-swap a literal .mjs specifier (real non-TS file, never resolved here)",
    "excludes any node_modules subtree at any depth",
    "includes fixture src and tests files",
    "finds a test two hops away from the changed module",
    "finds a test one hop away from the changed module",
    "resolves a .js specifier to a sibling .tsx source",
    "resolves an explicit index.js specifier",
    "resolves an implicit directory-index specifier",
    "marks a test affected via a non-test helper file under tests/",
    "does not report the helper file itself as affected (not a *.test.ts file)",
    "reports a changed test file as affected even with no importers",
    "marks a vi.mock-only consumer as affected",
    "marks a bare side-effect import consumer as affected",
    "marks an importer of a deleted (non-existent) changed file as affected",
    "does not report the deleted file itself in affected (it cannot be run)",
    "counts bare package + missing relative + template-dynamic imports honestly",
    "does not mark an unresolvable import as affected by a changed file",
    "treats a backslash-separated changed path the same as its posix form",
    "returns changed, affected, and graphStats",
    "returns posix-normalized affected paths regardless of OS path.sep",
    "returns an empty affected list for a module nobody imports",
    "builds a reverse index resolvable from target back to importer",
    "splits a comma-separated string",
    "splits a newline-separated string",
    "trims whitespace and drops empty entries",
    "returns an empty array for empty input",
    "finds at least one real affected test for src/core/scope-gate.ts",
    "returns zero affected tests for a module with no importers",
    "resolves affected tests via --changed and --json on a tmp fixture root",
    "resolves affected tests via STDIN when --changed is omitted",
    "prints one affected test path per stdout line without --json",
    "exits 0 with an empty affected list when --changed is empty and no stdin is piped",
    "scripts/*.mjs değişikliği, onu import eden testleri bulur (.mjs→.mjs zinciri dahil)"
   ],
   "scripts/agentic-do-verify.test.ts": [
    "returns false for a path that does not exist",
    "returns true for a tmpdir sandbox path after file is created there",
    "returns pass=true when the agentic-write file exists (write-verify PASS)",
    "passes for any existing file written to tmpdir",
    "returns pass=false when the target file does not exist (dosya-yok FAIL)",
    "returns pass=false with the missing path in the reason string",
    "returns pass=false (with reason) when filePath is empty string",
    "sandbox directory is created inside the OS temp directory (not project root)",
    "file written to sandbox passes evaluateWriteVerify and does not affect project root",
    "afterEach cleans up sandbox directories (no leftover tmpdir pollution)",
    "returns skipped=true and pass=true when entryPath does not exist (dist-yok skip)",
    "populates scenarios array with a SKIP entry when dist is absent",
    "provides a non-empty reason string when skipped (dist missing path)",
    "does NOT treat dist-missing as failure (skip ≠ failure)"
   ],
   "scripts/audit-operation-ingress.test.ts": [
    "inventories every closed taxonomy and accepts only real tool provenance",
    "binds operation identity only to the canonical catalog declaration",
    "rejects generated catalog lookalikes outside the exact canonical projection",
    "tracks promise namespaces, static flags, dynamic flags, and FileHandle effects",
    "uses compiler symbols to exclude shadowed effect lookalikes",
    "fails closed when a known effect boundary exposes an unknown method",
    "emits stable identities across repeated runs and distinct absolute roots",
    "normalizes POSIX, Windows-native, and WSL-style paths deterministically",
    "rejects file-level promotion, dead lookup, and irrelevant matching arguments",
    "rejects moved and substituted sites despite unchanged aggregate totals",
    "rejects unknown, unadmitted, and mismatched canonical operation identities",
    "rejects multiple operation lookups as ambiguous and unbound",
    "does not borrow an operation nested inside an unrelated action",
    "fails both coverage gain and coverage loss until a comparative refresh",
    "rejects malformed totals, duplicate ids, semantic identities, catalog drift, and diagnostics",
    "refuses an atomic write when canonical operation diagnostics exist",
    "advances only additive debt and refuses removal of prior unmatched debt",
    "migrates schema 2 only through a comparative debt-preservation gate"
   ],
   "scripts/audit-user-surfaces.test.ts": [
    "serve REAL when http_root=200, token present, api/status=200",
    "serve HOLLOW when http_root=200 but token missing (Sprint 214 regression)",
    "serve HOLLOW when http_root=200 but /api/status=401",
    "chat REAL when stdout has content and exit=0",
    "chat HOLLOW when stdout is empty",
    "chat HOLLOW when process errored (non-zero exit)",
    "renders markdown with one row per surface and REAL/HOLLOW counts",
    "renders SKIPPED report when distSkipped=true",
    "REAL serve + REAL chat produces a report listing all three surfaces with REAL count = 3",
    "HOLLOW detection: serve returns 200 but no token + chat empty → both flagged HOLLOW",
    "dist-not-present → distSkipped=true, no spawn, report marked SKIPPED",
    "serve process is killed in finally even when fetch succeeds (no leaked children)"
   ],
   "scripts/auth-mode-resolution-smoke.test.ts": [
    "returns false for subscription",
    "returns true for api",
    "returns false for undefined (default is subscription)",
    "subscription + ANTHROPIC_API_KEY set → key NOT in docker args",
    "api mode + ANTHROPIC_API_KEY set → key IS in docker args with correct value",
    "default (undefined) auth_mode → treated as subscription → key NOT in args",
    "codex provider + OPENAI_API_KEY → OPENAI forwarded, ANTHROPIC not forwarded",
    "gemini provider + GOOGLE_API_KEY → GOOGLE forwarded, ANTHROPIC not forwarded",
    "empty env produces empty args array",
    "runSmoke passes all 4 scenarios"
   ],
   "scripts/authority-handoff.test.ts": [
    "prepare → verify → commit chains digests and enforces create-only",
    "enforces transition order and tamper-evidence",
    "recovery-commit demands an explicit owner authority-ref"
   ],
   "scripts/autonomous-smoke.test.ts": [
    "returns pass=true when exit code is 0 and not timed out (temiz exit)",
    "returns pass=false when timed out, with reason mentioning timeout",
    "returns pass=false for non-zero exit code, with reason mentioning code",
    "returns pass=true when stdout contains EN header \"Autonomous runtime status\"",
    "returns pass=true when stdout contains TR header \"Otonom runtime\"",
    "returns pass=false when stdout is empty (missing header)",
    "returns pass=false for unrelated output with no header",
    "sandbox directory is created inside the OS temp directory (not project root)",
    "checkDistExists returns false for a non-existent path in tmpdir",
    "checkDistExists returns true after creating a file in tmpdir",
    "afterEach cleans up sandboxes (no tmpdir pollution)",
    "returns skipped=true and pass=true when entryPath does not exist (dist-yok skip)",
    "populates scenarios array with a SKIP entry when dist is absent",
    "provides a non-empty reason string when skipped (dist missing)",
    "does NOT treat dist-missing as failure (skip ≠ failure)"
   ],
   "scripts/backfill-sprint-log-rows.test.ts": [
    "parses --flag values and bare booleans",
    "builds taskId → selfAssessment map from .result files",
    "returns trace durationMs when present",
    "falls back to first/last timestamp delta when no trace event",
    "emits Task Outcomes section + counters",
    "returns canonical sprint ids from both archive roots, sorted",
    "--sprint sprint-194 reconstructs row from events.jsonl/metrics.jsonl",
    "--all-missing detects multiple missing sprints and writes them",
    "idempotent re-run — no duplicate row, no error",
    "skips sprints with no on-disk evidence",
    "pure planner returns canonical payload usable by applyBackfill"
   ],
   "scripts/born-intake-checker.test.ts": [
    "splits a draft into a field-name -> body-text map",
    "reports valid=true with no gaps for a fully filled draft",
    "reports a MISSING_FIELD gap when \"${field}\" is dropped",
    "reports valid=true for a real born row transcribed into the template"
   ],
   "scripts/build-clean-hold-exit.test.ts": [
    "exits 1 with typed missing-authority HOLD for sentinel-only state",
    "exits 1 with typed missing-authority HOLD for database-only state"
   ],
   "scripts/build-identity.test.ts": [
    "writes a deterministic, runtime-parseable manifest without the source path",
    "rejects missing, partial and artifact-drifted schema-v3 identity data",
    "fails closed when exact native source bytes drift after the manifest is written",
    "rejects a non-Deckent package instead of minting a misleading identity"
   ],
   "scripts/build-lifecycle.test.ts": [
    "requires the canonical root npm shrinkwrap before authority acquisition",
    "requires the core runner before authority or staging mutation",
    "requires the dashboard builder before authority or staging mutation",
    "stages, verifies and commits core output under one maintenance boundary",
    "releases authority without touching live dist on pre-boundary failure",
    "retains non-expiring authority when completion becomes uncertain",
    "surfaces committed authority projection-cleanup uncertainty without false quarantine",
    "compiles from an immutable run-scoped source snapshot across source ABA",
    "copies a closed toolchain hardlink set into independent nlink-one snapshot files",
    "rejects a toolchain hardlink set with an %s alias outside the copied tree",
    "keeps project source hardlinks outside the toolchain exception",
    "keeps generated build output hardlinks outside the toolchain exception",
    "rejects a closed toolchain inode that mutates during snapshot copy",
    "copies and digests the canonical root npm shrinkwrap as a build input",
    "attested recovery restores the authenticated old artifact after backup-phase failure",
    "rejects dashboard-only publication without a complete canonical core artifact",
    "rejects traversal-capable and ambiguous run identifiers before authority",
    "rejects unsafe heartbeat timing before authority acquisition",
    "renews authority from synchronous copy checkpoints",
    "bounds committed run retention while preserving the current recovery record",
    "contains no shell-mediated or dependency-installing child path"
   ],
   "scripts/build-staging-tools.test.ts": [
    "copies assets and writes identity into an isolated output tree",
    "restores executable bits relative to a non-live output tree",
    "runs exact local dashboard tools into the requested staging tree",
    "requires a runner before output mutation or toolchain work",
    "fails closed when the local toolchain is unavailable",
    "fails closed before Vite when the bound output directory is replaced",
    "fails closed when a bound dashboard tool changes during validation",
    "refuses a non-empty output instead of delegating destructive cleanup to Vite",
    "does not silently treat an unreadable or missing source tree as no assets",
    "rejects an output path outside the canonical project root",
    "contains no dependency installation or shell-mediated execution path"
   ],
   "scripts/builtins-drift-check.test.ts": [
    "returns [] for a missing directory",
    "excludes archive/ and temp-* directories",
    "ignores plain files, only lists directories",
    "drops a top-level stats key",
    "does NOT drop a nested key merely named stats-adjacent (only top-level \"stats\")",
    "is insensitive to key order (recursively)",
    "treats stats-value-differences as equal (605 sidecar noise)",
    "treats stats-present-vs-stats-entirely-absent as equal (the real secure-coding shape)",
    "flags a real non-stats content diff and names the differing key",
    "reports an error (not a throw) for invalid JSON",
    "reports zero added/removed for identical text",
    "detects a pure prepend (the real PROMPT.md frontmatter-block shape)",
    "detects a pure removal",
    "caps the sample at `limit` entries but still counts the full total",
    "equal for byte-identical files",
    "not equal + line-diff detail for divergent files",
    "classifies only-in-deckent, only-in-builtins, identical, content-diff, and excluded items",
    "flags a presence-mismatch file (manifest exists on one side only, both sides otherwise present)",
    "produces stable, sorted keys",
    "finds no new/resolved keys when scan matches baseline exactly",
    "flags a key present in the live scan but absent from baseline as new",
    "flags a baseline key no longer present in the live scan as resolved",
    "returns null for a missing baseline path",
    "writes a baseline that loadBaseline reads back with the same driftKeys",
    "--check exits 2 with a --write suggestion when the baseline file is missing",
    "--write then --check round-trips clean (exit 0) against the live repo trees",
    "--check exits 1 when the live baseline is missing a key the current scan reports",
    "does not throw and returns the expected category shape",
    "secure-coding manifest drift is CLOSED — the two trees agree (Alperen karar-turu merge, 2026-07-11)"
   ],
   "scripts/ccverify-affected.test.ts": [
    "defaults base to origin/main and max-files to 400",
    "parses --changed, --list, --dry-run, --max-files, --base",
    "parses --root to an absolute path",
    "returns an empty array for an empty input",
    "puts everything in one chunk when well under the byte threshold",
    "splits into multiple chunks once the running byte size exceeds the threshold",
    "gives a single pathologically-long filename its own chunk rather than dropping it",
    "never produces an empty chunk",
    "builds one `npx vitest run <files>` command per chunk",
    "produces one command per chunk when chunking is forced",
    "returns no commands for an empty affected set",
    "uses WINDOWS_ARGV_CHUNK_BYTES as the default threshold",
    "resolves the child exit code verbatim",
    "resolves 0 for a clean exit",
    "rejects when the binary does not exist",
    "passes a single command exit code through verbatim (no forced remap)",
    "returns 0 when every chunk succeeds",
    "combines chunk exit codes — any non-zero chunk makes the overall result non-zero",
    "unions merge-base range diff, working-tree diff (incl. unstaged), and untracked files",
    "rejects with BASE_REF_NOT_FOUND and a --base HEAD~1 suggestion when the base ref does not resolve",
    "rejects honestly (never a silent empty list) when the directory is not a git repo at all",
    "does not touch git when --changed is provided, even against a nonexistent cwd",
    "falls back to git when --changed is null",
    "lists every affected test path, one per line, exit 0",
    "--list bypasses the --max-files guard (pure information, never runs anything)",
    "lists nothing (empty output) for a module with no importers",
    "prints \"0 affected\" and exits 0 for a module nobody imports",
    "prints \"0 affected\" and exits 0 when --changed is empty",
    "exits 2 with an honest redirect message when the affected-set exceeds --max-files",
    "does not trigger when the affected-set is within --max-files",
    "prints the exact command it would run, never spawns vitest, exit 0",
    "dry-run output is byte-identical to the vitest argv planRun() would hand to spawn",
    "prints \"0 affected\" instead of an empty vitest command when nothing is affected",
    "exits non-zero with a --base HEAD~1 suggestion instead of a silent empty list",
    "lists tests/core/scope-gate.test.ts for --changed src/core/scope-gate.ts --list"
   ],
   "scripts/chat-native-smoke.test.ts": [
    "single user turn produces user+assistant transcript entries",
    "multiple sequential turns accumulate in transcript order",
    "empty input array produces empty transcript",
    "provider tool_use response causes dispatcher.dispatch and re-send",
    "unknown tool returns tagged mock result without throwing",
    "memory adapter receives user and assistant turns in order",
    "getChatHistory respects limit parameter",
    ":exit terminates the loop and subsequent inputs are not processed",
    ":quit alias also terminates the loop",
    "maxTurns cap stops the loop",
    "runSmoke passes all 4 scenarios"
   ],
   "scripts/ci-aggregate-gate.test.ts": [
    "parses ci.yml into jobs with names and needs (parser sanity)",
    "defines the aggregate job under the single required-check context name",
    "fans in every `Tests —` shard plus the production packed-networkless proof",
    "pins the build fan-in to the canonical fresh-cache networkless receipt",
    "declares a minimum fan-in count equal to the live shard inventory",
    "runs on pull_request/push but never in the merge queue (row 535 preserved)",
    "reads shard verdicts from the needs context rather than re-running anything",
    "leaves every shard untouched — 535 guard, typecheck dependency, advisory state",
    "passes when every shard concluded green",
    "fails when a shard failed",
    "fails when the production installed-native proof failed",
    "fails when a shard was cancelled",
    "fails when a shard skipped outside the merge queue (upstream never ran it)",
    "fails when a shard is silently dropped from the fan-in, even if the rest are green",
    "tolerates skipped shards only when the event itself is merge_group",
    "still fails a merge_group run when a shard actually failed",
    "fails closed when the declared minimum is not a positive integer"
   ],
   "scripts/ci-baseline-detect.test.ts": [
    "README sprint badge is current (>= 214, never the stale 190)",
    "isGarbage detects testCount:17/passed:0/failed:17 as garbage",
    "isGarbage returns false for real baseline counts",
    "countDescriptors returns a count >= 18000 for this project",
    "buildFixedBaseline produces non-garbage testCount",
    "main --check exits 1 for garbage baseline",
    "main --fix rewrites garbage baseline with real count",
    "main without args returns exit code 2"
   ],
   "scripts/ci-sim-signal-restore.test.ts": [
    "%s stops the nested runner, cleans the disposable worktree, and never moves live state",
    "SIGKILL can leak only disposable state; next run reaps it without touching live state"
   ],
   "scripts/clean-active-execution-guard.test.ts": [
    "allows a clean clone with no runtime directory or receipt database",
    "owns and releases the canonical v4 maintenance generation with epoch/counter/nonce fencing",
    "reads a core-produced v4 execution authority as a blocking active execution",
    "reads a core v4 resumed boundary whose original entry predates its new generation",
    "accepts a persistent authority anchor recorded in another mount namespace",
    "validates live quarantine audits with a fixed JOIN query shape",
    "inspects more than two thousand canonical execution rows without an arbitrary authority limit",
    "exposes exact full-fence boundary, renewal, assertion, and completion APIs",
    "reclaims exact-owner crashed staging only after owner-death proof",
    "surfaces committed clean renewal and release projection uncertainty",
    "keeps a committed clean completion authoritative when projection cleanup is uncertain",
    "writes clean quarantine bytes that core verifies and explicitly recovers",
    "recovers an exact clean maintenance quarantine only with fresh verified attestation",
    "commits clean v2 migration before returning quarantined admission HOLD",
    "migrates legacy v3 active and quarantine audit bytes to v4 before HOLDing",
    "rolls back clean v2 migration atomically when a later row is malformed",
    "fails closed when required v4 schema object %s is missing",
    "fails closed for missing or mismatched v4 DB/sentinel authority state",
    "fails closed when DB, sentinel, or projection authority has another hard link",
    "serializes both cross-process orderings: execution-first HOLDs clean, clean-first blocks execution",
    "rejects cross-process authority bootstrap after .locks generation replacement",
    "archives an unregistered receipt-less plan artifact with no worker evidence",
    "fails closed for a receipt-less PENDING task with a heartbeat",
    "fails closed for a receipt-less PENDING task in a live sprint",
    "projects an exact receipt-backed rejection settlement to NOT_DISPATCHED",
    "rejects rehashed NOT_DISPATCHED cause, evidence, or timestamp mismatches",
    "binds legacy attestation to exact current task bytes and createdAt",
    "holds tampered, semantically non-terminal, and multiple matching receipts",
    "rejects a canonical-hashed but structurally incomplete receipt envelope",
    "holds when a settled view has any additional matching receipt authority",
    "accepts all V1 endpointRefHash compatibility forms",
    "rejects rehashed receipt unknown keys, non-canonical strings, and fallback overflow",
    "accepts semantically valid variants across the full InvocationEvent union",
    "rejects rehashed semantic forgeries for every InvocationEvent member",
    "rejects rehashed event chronology before the receipt or predecessor",
    "requires every raw task snapshot to carry a valid createdAt authority",
    "ignores attempt-private task JSON sidecars during task-state inspection",
    "holds active, stale, and unknown task execution fences across lock roots",
    "holds malformed and orphan task execution fence evidence with typed reasons",
    "holds canonical dedicated execution locks for alive, dead, and unknown owners",
    "fails closed for malformed, misnamed, future, and orphan dedicated execution locks",
    "holds active or resumable task, worker, sprint, process, and bot evidence",
    "holds active RunFlow/jobs and autonomous-v2 mission execution authorities",
    "allows queued/approval RunFlow and mission work with no live execution lease",
    "accepts a valid terminal flow with multiline narrative intent and final newline",
    "reconciles mixed historical jobs, dead RunFlow pids, and terminal mission claims without false HOLDs",
    "holds a live RunFlow pid and an invalid event transition",
    "does not false-HOLD draft/terminal tasks, dead pid files, or queued backlog entries",
    "does not false-HOLD a reused structured bot pid or a foreign legacy pid",
    "consumes the current schema-v2 bot runtime identity and remains v1 read-compatible",
    "classifies dead MCP launch anchors and unbound sprint markers as stale HOLDs",
    "holds maintenance across the real direct clean mutation and releases only its exact generation",
    "refuses before deleting dist and emits a stable JSON reason envelope",
    "an active-status sprint with NO recorded pid degrades to a STALE_DEAD projection, not a hold",
    "an absent pid field behaves the same as an explicit null (the wedge shape)",
    "a GENUINELY LIVE sprint still holds the build (no weakening)",
    "a recorded-but-dead pid degrades to a projection",
    "an UNVERIFIABLE pid fails closed as typed-unknown, never as silently-clean",
    "a terminal sprint is untouched by the liveness path"
   ],
   "scripts/clean-clone-smoke.test.ts": [
    "PASS path: returns status PASS, captures duration and optional detail",
    "PASS path: tolerates undefined return without throwing",
    "FAIL path: thrown error becomes status FAIL with captured message",
    "FAIL path: truncates very long error messages to keep reports readable",
    "aborts after archive step fails when given an unreachable source dir",
    "script file exists and stays within reasonable size budget",
    "exposes the documented public surface (runStep + runSmoke)",
    "keeps clone-smoke build behind the active-execution clean admission",
    "smoke-verify.md exists and documents the pipeline + flags"
   ],
   "scripts/clean-dashboard-policy.test.ts": [
    "exposes a single frozen typed decision",
    "preserves exactly the dist entry the build writes",
    "keeps clean.mjs standalone: it defines the decision, imports nothing",
    "has build-dashboard.mjs read that decision instead of a private copy",
    "builds over a preserved bundle without E_DASHBOARD_BUILD_OUTPUT_NOT_EMPTY",
    "builds over an empty output directory too",
    "leaves the overwrite to vite instead of deleting the bundle itself",
    "still refuses a non-empty output directory the policy does not cover",
    "never hands --emptyOutDir to vite for a directory it does not own"
   ],
   "scripts/cli-docs-contract.test.ts": [
    "has one CLI docs producer and no source-description regex fallback",
    "keeps docs, manifest, and canonical path counts closed"
   ],
   "scripts/cli-mcp-parity-registry.test.ts": [
    "keeps the registry command universe identical to the legacy command scan",
    "passes the parity gate against the real repository"
   ],
   "scripts/config-truth-gate.test.ts": [
    "accepts matching typed metadata, canonical defaults, and loader output",
    "reports typed missing metadata, missing defaults, runtime omissions, and divergences",
    "fails closed when runtime or manifest leaves have no canonical default producer",
    "drives the real repository and reports its observed truth without weakening the gate",
    "fails closed through the CLI on fixture divergence"
   ],
   "scripts/dashboard-e2e-smoke.test.ts": [
    "returns true for no-cache, no-store, max-age=0 (the canonical cache-bust idioms)",
    "returns false for caching directives or absent header",
    "extracts hash from typical Vite HTML output",
    "handles dist index.html shape with surrounding tags",
    "returns null when no hashed bundle is referenced",
    "counts double-quoted nav route literals",
    "counts single-quoted nav route literals",
    "counts all expected nav routes when all are present",
    "returns 0 for empty / invalid input",
    "returns null when no dashboard dist exists under repoRoot (the dist-yok skip path)",
    "returns the first matching candidate when dist/dashboard/index.html exists",
    "falls through to src/dashboard/dist candidate when dist/dashboard is absent",
    "findLatestBundle returns the hashed bundle path or null",
    "returns { skipped: true } when no dashboard dist exists under repoRoot",
    "returns { skipped: true } when dist exists but dist/api/server.js does not"
   ],
   "scripts/dead-code-audit.test.ts": [
    "extracts named function exports",
    "extracts class exports",
    "extracts interface/type exports",
    "returns empty array for non-existent file",
    "returns results for all existing suspects",
    "categorizes unimported modules as Dead",
    "assigns correct categories: Dead, Dormant, Lightly-Used, or Active",
    "generates valid markdown with all sections",
    "includes correct line counts in summary table",
    "handles empty results gracefully",
    "runs successfully with exit code 0",
    "produces JSON output with --json flag",
    "writes the report to a --report-dir without touching the real docs tree"
   ],
   "scripts/deadcode-dynamic-scan.test.ts": [
    "resolves a .js specifier back to its .ts source",
    "resolves a directory specifier to its index.ts",
    "returns null for an unresolvable specifier",
    "marks an imported fixture module as reachable, and an unimported one as not",
    "returns an empty set when package.json is absent under --root",
    "auto-detects an intentionally-orphaned module with ZERO KNOWN_SUSPECTS edits",
    "does not flag consumer.ts as dead just because nothing imports it (it imports used-module, but is itself an unreferenced leaf — documents scan scope, not a false-negative)"
   ],
   "scripts/dist-clean-guard.test.ts": [
    "refuses its physical source root without risking the live checkout",
    "rejects caller-supplied authority without touching either physical root",
    "fails closed when the canonical SQLite authority dependency is unavailable",
    "cleans only its physical temp fixture and preserves dashboard",
    "preserves a case variant only when the filesystem resolves it as dashboard",
    "refuses a symlinked dist target instead of following or deleting it",
    "refuses a preserved dashboard symlink before a later build can follow it",
    "keeps the typed fail-closed HOLD when dist exists without the adapter",
    "allows a no-dist clean as observe-only when the identity adapter is absent"
   ],
   "scripts/docs-scripts-canary.test.ts": [
    "parses the test-docs-scripts job and its test step (parser sanity)",
    "runs the canary env on the existing step, not a new/duplicate step",
    "leaves the test command itself untouched — no shard scope change",
    "leaves the timeout untouched",
    "keeps job-level continue-on-error until the RCA acceptance series is met",
    "adds no bounded retry — the RCA rejected retry as a fix",
    "does not add a second job or step to run the canary in parallel",
    "matches the exact env var name vitest.config.ts reads — no drift between the two files"
   ],
   "scripts/e2e-surfaces-dashboard.test.ts": [
    "all dashboard endpoints respond 200 → assertDashboardSurfaces.pass=true",
    "/api/nervous/status=404 is acceptable (route pending wire in 218-005, !=401 check)",
    "dashboard endpoint 401 (auth-fail) → assertDashboardSurfaces.pass=false",
    "missing bundle (no /assets/index- or script type=module) → pass=false",
    "POST /api/start=202 then GET /api/status=200 → pass=true",
    "POST /api/start=409 (already running) is acceptable, status still=200",
    "FREEZE detected: /api/status returns non-200 after start → pass=false",
    "FREEZE detected: /api/status throws (timeout/hang simulation) → pass=false",
    "dist-yok skip-guard: returns { skipped: true } when entry missing",
    "kill-on-fail: throwing fetch still triggers child.kill via try/finally",
    "aggregate pass: all three gates green → runE2E.pass=true",
    "aggregate fail: dashboard endpoint 401 → runE2E.pass=false (sprint-start may still pass)",
    "script file exists and Kanit grep keywords present (>=3)"
   ],
   "scripts/exec-bit-guard.test.ts": [
    "restores 0o755 on non-executable bin files (the tsc --watch bug scenario)",
    "returns count equal to number of files that exist",
    "does not throw when dist/ is absent (missing files skipped gracefully)",
    "is idempotent: files already 0o755 remain 0o755 without error",
    "BIN_FILES export contains expected dist/ entry points"
   ],
   "scripts/forbid-clean-in-tests.test.ts": [
    "pins the exact package scripts that reach the destructive clean",
    "classifies every test-reachable caller command as a dist-clean effect",
    "leaves legitimate non-clean commands unflagged",
    "keeps scripts/build.mjs off the clean path (C5 negative)",
    "refuses an in-process import-time cleanDist() call under vitest (C1)",
    "refuses a spawned clean that inherits only the vitest env (C2)",
    "refuses a spawned clean that carries only the hermeticity env (C2)",
    "refuses through an intermediate runner process (C3/C4 env inheritance)",
    "still lets a plain operator clean run (no over-blocking)"
   ],
   "scripts/gen-reference-docs.test.ts": [
    "extracts tool name + title + description from registerTool call",
    "extracts multiple tools from a single file (nervous-style)",
    "returns empty array when no tool files exist",
    "extracts resource name + uri + title + description",
    "parses ADR md files into structured entries",
    "skips non-numbered markdown like README.md",
    "parses agent.json files into structured agents",
    "skips the archive directory",
    "renderMcpTools produces a markdown table with tool count",
    "renderAdrs groups by status",
    "renderAgents lists agents with expertise",
    "renderMcpResources lists URI + mimeType",
    "replaces content between AUTOGEN markers",
    "is idempotent — replacing the same content twice yields the same result",
    "throws or returns null when block id missing",
    "reports drift when target file is stale and clears after write",
    "exposes bilingual target paths under docs/generated/<lang>/reference and docs/adr"
   ],
   "scripts/install-ingress-census.test.ts": [
    "covers exactly the four owner-specified verb classes",
    "finds real npm ci sites in .github/workflows (a known non-empty surface)",
    "finds the 3x npm ci chain in the root package.json install:all script",
    "never claims npm rebuild as covered — out-of-verb-scope subcommands are excluded from sites",
    "filters echo-embedded prose mentions of npm install (ci.yml lockfile-sync guard)",
    "reports on the Docker worker-spawn backend without claiming an install site that is not there",
    "discovers at least one nested npm root beyond the repo-root package.json",
    "types the unknown-ingress class explicitly — never a closed-world coverage claim",
    "every site carries an explicit ignoreScriptsPosture, never a silent guess",
    "is deterministic — same source tree, same digest, no wall-clock drift"
   ],
   "scripts/lint-acceptance-confirmation-authority.test.ts": [
    "accepts one canonical declaration set and ignores prose in comments and strings",
    "requires a precise, reasoned, rule-specific allowlist comment",
    "is deterministic across CRLF input and portable path reporting"
   ],
   "scripts/lint-approval-lifecycle-authority.test.ts": [
    "passes a single canonical policy and guarded broker/store chain",
    "fails when the central decision choke-point marker disappears",
    "fails when a legacy mirror reintroduces its own TTL table",
    "fails when a consumer recreates the legacy risk mapping"
   ],
   "scripts/lint-cli-mcp-field-descriptions.test.ts": [
    "passes on the real repository with zero literal and zero unknown `.describe(` sites",
    "fails on a fixture carrying a literal and a free-expression field description, naming file:line:kind",
    "passes on a fixture whose every field reads the catalog through any of the three accepted calls"
   ],
   "scripts/lint-cli-surface.test.ts": [
    "fails (exit 1) on a hardcoded .description(\\'...\\') literal",
    "fails (exit 1) on a hardcoded description: \\'...\\' property in an MCP tool module",
    "passes (exit 0) when every description resolves through getMessage()/mcpToolDescription()",
    "does not false-positive on an unrelated description: data field in a CLI command file",
    "fails (exit 1) when a cli-shared binding key drifts from the CLI command\\'s real key",
    "passes (exit 0) when the cli-shared binding key matches the CLI command\\'s real key",
    "reports a hardcoded .option() description but stays exit 0 under the declared ratchet ceiling",
    "--surface-gate turns the same .option() literal into a hard failure",
    "--surface-baseline 0 fails the ratchet (a NEW help-surface literal cannot slip in)",
    "does not false-positive when the help-surface text resolves through getMessage()",
    "a flags/name token is never itself treated as help text (first argument is skipped)",
    "fails when an invoked register* command lacks a registry row",
    "fails when a registry row has no invoked register* command",
    "fails when a deprecated replacement has no registry command",
    "ignores nested alias metadata when reading registry rows",
    "is green against the real repository"
   ],
   "scripts/lint-config-writers.test.ts": [
    "flags a direct writeFileSync to a config-family identifier (fail-closed)",
    "flags an inline config.json literal target",
    "does not flag the cost-config/docs-config families (segment boundary)",
    "exempts the authority module interior",
    "does not flag unrelated writers or comment lines",
    "is green against the real repository (containment holds)"
   ],
   "scripts/lint-directives.test.ts": [
    "flags same-line Reads+Files, empty scope, write collision and missing test as typed problems",
    "blocks uncovered direct src-imports (sprint-670 lesson) and the generator repairs exactly that gap",
    "warns when a later task depends transitively on a task changing the engine",
    "does not warn when an engine-changing task has no dependent task"
   ],
   "scripts/lint-doc-command-truth.test.ts": [
    "flags a command that the registry does not know",
    "accepts a command the registry knows",
    "never flags prose that uses the product name as a verb",
    "checks fenced code blocks as code",
    "does not treat an option as a subcommand",
    "reports every audited document that exists",
    "extracts mentions with their line numbers"
   ],
   "scripts/lint-i18n-repl.test.ts": [
    "finds nested TSX JSX, label properties, output and pushTurn prose with stable categories",
    "ignores catalog calls, injected labels and technical protocol/path/error/ANSI literals"
   ],
   "scripts/lint-i18n-slash-desc.test.ts": [
    "fails (exit 1) on a natural-language `desc:` literal in a src/cli/commands catalog",
    "stays clean (exit 0) when the entry carries a `descKey` instead of a literal",
    "ignores short technical tokens below the natural-language threshold (same heuristic as every other scan)"
   ],
   "scripts/lint-links.test.ts": [
    "classifies http/https/mailto as external",
    "classifies #anchor as self-anchor",
    "classifies relative .md as relative-file",
    "splits anchor from relative .md",
    "classifies absolute /path as site-relative",
    "lowercases and dasherizes",
    "strips punctuation",
    "preserves consecutive spaces as multiple dashes (GitHub slug semantics)",
    "preserves underscore and produces double-dash for \"Word — Word\"",
    "extracts h1-h6",
    "extracts markdown link text + href",
    "ignores image links",
    "strips title attribute",
    "reports line numbers",
    "resolves relative .md when file exists",
    "fails when relative file missing",
    "resolves uppercase ALL-CAPS relative when sibling exists with extension",
    "site-relative with docs root resolves <docs>/foo.md",
    "validates anchor against target heading",
    "flags missing anchor",
    "self-anchor validates against the file own headings",
    "external links skipped (exists=null)",
    "flags broken relative link with file:line context",
    "clean file returns no broken",
    "skips external links",
    "reports broken anchor on same file",
    "honors ignore patterns (dir prefix)",
    "exits with broken count > 0 when broken links exist",
    "skips sprint/audit/internal trees via built-in DEFAULT_IGNORES (no .lintlinkignore needed)",
    "extraIgnorePatterns layer on top of defaults additively"
   ],
   "scripts/lint-master-plan.test.ts": [
    "declared-capable platforms run the symlink cases (no silent skip)",
    "treats escaped pipes as cell content and unescaped pipes as structure",
    "uses CommonMark backslash parity for structural pipes",
    "validates the repository MASTER snapshot without pinning today\\u2019s row counts",
    "normalizes LF/CRLF only for source digests and generated content",
    "fails closed on an extra unescaped pipe / wrong column count",
    "accepts a literal escaped pipe in a ledger cell",
    "rejects duplicate IDs, duplicate/non-monotonic orders and invalid enums",
    "rejects future Updated chronology",
    "returns a structured finding for clocks outside the JavaScript Date domain",
    "fails closed on a non-numeric row instead of silently dropping it",
    "fails closed when a ledger row loses its leading or trailing pipe",
    "fails closed on table rows hidden before a program header",
    "rejects ledger-shaped shadow work outside canonical §7 bodies",
    "rejects receipt/blocker-shaped shadow authority outside §3.4/§3.5",
    "rejects missing/self/program-root dependencies",
    "rejects empty segments in every canonical comma-delimited authority list",
    "detects dependency cycles, parent cycles and mixed closure deadlocks",
    "validates a deep 15k dependency DAG without recursive stack overflow",
    "resolves a deep 25k aggregate-parent chain in linear-scale time",
    "rejects a row listed under a program section different from its parent root",
    "enforces dependency readiness and aggregate-parent closure",
    "does not treat DISPOSED prerequisites as satisfied and closes disposed aggregates",
    "requires every BLOCKED item exactly once and rejects stale blocker assignments",
    "rejects a malformed typed blocker code instead of silently ignoring it",
    "rejects orphan and duplicate blocker work scopes",
    "accepts exactly one typed blocker assignment with a canonical remedy",
    "binds explicit blocker remedies exactly to DependsOn and declared gates",
    "rejects negated or mixed exact-DependsOn delegation",
    "fails closed on malformed receipt/blocker table structure",
    "requires scoped provenance for VERIFY mutation claims",
    "never turns raw or negated receipt prose into mutation authority",
    "rejects generic, empty and gate-incomplete historical provenance",
    "rejects malformed receipt IDs and unbound/unsafe target manifests",
    "rejects non-normalized and portable-case-colliding receipt targets",
    "rejects duplicate/conflicting authority fields, rejected owners and impossible instants",
    "invalidates active receipts outside their exact time window",
    "requires every receipt to be one-shot or explicitly expiring",
    "requires VERIFY/DONE evidence receipts to cover every mutation gate",
    "requires an active receipt for every READY/IN_PROGRESS mutation gate",
    "rejects portable target collisions across active receipts only",
    "rejects split receipts for one multi-gate Work authority",
    "enforces G7 field completeness and single-use state",
    "requires complete truth/evidence/provenance for DONE",
    "requires review-date for DEFERRED and dated owner authority for DISPOSED",
    "excludes terminal rows and carries dependency/child/blocker relationships",
    "renders byte-stable JSON and Markdown with a normalized source digest",
    "is fail-closed across lint, release, publish, and projection entry points",
    "parses check/write/json/root and rejects conflicting or unknown args",
    "fails check on missing projections, writes them, then passes check",
    "refuses stale-source CAS writes",
    "does not expose the projection write capability as a module API",
    "refuses a competing projection writer lock without changing outputs",
    "rechecks exact prior authority under lock before writing",
    "ratchets published Work IDs and Orders across regeneration",
    "ratchets terminal closures and immutable receipt authority",
    "ratchets definitions, terminal proof, receipt authority and lifecycle",
    "rejects cross-regeneration G7 attempt identity replay",
    "keeps the receipt registry append-only and order-stable",
    "fails closed without overwriting corrupt, legacy or inconsistent registries",
    "ratchets Updated chronology while allowing reviewed same-day progress",
    "never overwrites an unsafe or unreadable prior authority registry",
    "checks every active receipt baseline against the physical repository root",
    "stays silent when imported as a library (no entry argument)",
    "runs when a symlinked entry resolves to this module",
    "does not run for an unrelated entry",
    "falls back lexically instead of swallowing a realpath failure",
    "reports degraded resolution when only one side resolves",
    "declares this host\\'s symlink capability instead of assuming it",
    "reports malformed canonical input as JSON without creating projections",
    "returns scan-error exit 2 for a missing MASTER source",
    "degrades to a typed no-git mode outside a work tree",
    "reports no-history for a repository without commits",
    "anchors an uncommitted receipt to HEAD — the last reviewed state",
    "anchors a committed receipt to the PARENT of its registration commit",
    "fails closed with no-parent when the receipt was registered in the root commit",
    "fails closed in a shallow repository instead of degrading",
    "fails closed when the receipt is committed but invisible to the -S search",
    "fails closed when the registration commit is a merge — no single reviewed pre-state",
    "readTrustAnchorBlob reports a path absent at the anchor as absent",
    "readTrustAnchorBlob distinguishes a read error from absence",
    "readTrustAnchorBlob returns the blob bytes for a readable path",
    "admits an uncommitted receipt whose baseline pins committed, reviewed bytes",
    "rejects a receipt vouching for uncommitted working-tree edits",
    "rejects the xverify 5-step attack: source change and receipt in the same commit",
    "accepts the legitimate two-commit flow: receipt registered against reviewed state",
    "rejects the xverify merge-introduction attack: receipt lands only in a merge commit",
    "rejects the xverify rename attack: rename must not advance the trust anchor",
    "rejects the same-commit attack from a depth-1 shallow clone",
    "declares this host\\'s git capability instead of assuming it"
   ],
   "scripts/lint-mock-factories.test.ts": [
    "flags a node:fs full factory that does not call importOriginal",
    "accepts node:fs/promises when its factory calls the importer parameter",
    "accepts a test file without a node:fs mock",
    "fails on a stale baseline entry (only-shrink ledger)",
    "is green against the real repository and pins the measured ledger"
   ],
   "scripts/lint-no-model-literal.test.ts": [
    "slices BUILTIN_MODELS and extracts only its id fields",
    "slices CODEX_PARITY_MODELS independently",
    "never mistakes apiId for id — apiId has a capital I, so \"id:\" never occurs inside it",
    "returns an empty string when the array marker is absent",
    "unions ids from both registry arrays into one dictionary",
    "derives canonical API ids from the REAL model-registry.ts",
    "derives migration-only alias keys separately from canonical ids",
    "reads the real compatibility table",
    "detects a real model-literal site",
    "ignores the import line",
    "ignores // and * comment lines",
    "filters a single-line /** ... */ block comment",
    "matches double-quote and interpolation-free backtick literals",
    "does NOT match a longer literal that merely contains a known id as a substring",
    "ignores real template-literal interpolation (not a bare string)",
    "counts ONE site per line even when several known ids appear on it",
    "detects directive, slash-command, and exact table-cell inputs",
    "ignores prose and explicit migration/rejection documentation",
    "uses an explicit current-doc path policy",
    "recursively scans only current Markdown surfaces in a hermetic fixture",
    "makes the CLI fail closed for a new actionable alias in a hermetic repo",
    "passes when the scan equals the baseline",
    "flags a NEW model-literal site",
    "is count-based — a real duplicate line (e.g. two config presets both saying brain_model: \\'opus\\') only fails once it exceeds the baseline count",
    "includes the Markdown kind in the multiset identity",
    "allows only switch-case input discriminators in the config migration boundary",
    "never reports a site inside model-registry.ts itself, even though its own id fields would otherwise match",
    "the checked-in baseline has no new model-name literal vs the live source tree",
    "has no migration-only alias literal in runtime source",
    "excludes archived markdown from the actionable scan and diffs it purely against the baseline",
    "model-registry.ts is never scanned as a violation source"
   ],
   "scripts/lint-no-spawnsync.test.ts": [
    "detects a real spawnSync call",
    "ignores the import line",
    "detects a namespace/method call (import * as cp) — the evasion the ratchet must catch",
    "does NOT match unrelated identifiers ending in SpawnSync (capital S)",
    "ignores comment lines",
    "ignores the ADR-G-002 detection-pattern string (data, not a call)",
    "counts multiple distinct calls in one file",
    "passes when the scan equals the baseline",
    "flags a NEW non-hot-path spawnSync",
    "flags a NEW hot-path spawnSync and attaches its owner",
    "is count-based — a second identical call beyond the baseline count is NEW",
    "auditor is owned by ADR-087-W; the spawn backends + probes by the born-item",
    "the checked-in baseline has no new spawnSync vs the live source tree"
   ],
   "scripts/lint-operating-policy.test.ts": [
    "passes and reports the canonical digest when all host blocks match",
    "normalization tolerates trailing whitespace and edge blank lines (no false drift)",
    "flags HOST_BLOCK_DRIFT for a diverged host block only on the drifted host",
    "flags missing markers as typed problems",
    "writeHostBlocks syncs drifted hosts back to canonical and check turns green",
    "accepts a well-formed capsule whose MASTER row is non-terminal, ignoring train files",
    "flags a capsule with missing required fields and missing DONE section",
    "flags filename/OUTCOME_ID mismatch",
    "flags a capsule without an owner-admitted MASTER row (KANUN 4)",
    "flags delete-on-consume: capsule surviving a terminal MASTER row",
    "findMasterRowState parses the 13-column ledger row state",
    "passes with byte-identical twin blocks and reports the control digest",
    "flags DEV_CONTROL_BLOCK_MISSING when one host lacks the block",
    "flags unknown, duplicate and missing fields with typed codes",
    "flags a strict enum violation as DEV_CONTROL_INVALID_VALUE",
    "flags canonical key-order violation as DEV_CONTROL_KEY_ORDER",
    "flags twin drift when AGENTS and CLAUDE blocks are not byte-identical",
    "flags a surviving unconditional legacy DOGFOOD-MANDATORY token",
    "flags DECISION_REF not anchored in the canonical policy",
    "parseControlBlock returns found:false without markers"
   ],
   "scripts/lint-recovery-truth-authority.test.ts": [
    "accepts canonical verification and bounded recovery paging",
    "is syntax-aware and ignores comments, strings, and unrelated modules",
    "fails closed on malformed recovery source",
    "reports stable portable paths and line numbers",
    "exposes a fail-closed CLI with binary exit authority"
   ],
   "scripts/lint-skill-routing.test.ts": [
    "passes a clean routing surface",
    "fails a synthetic resolver bypass outside the adapter",
    "fails a nested routing-surface module that resolves the catalog itself",
    "fails when the adapter narrows the typed rejection vocabulary",
    "never fails on a prose mention inside a comment",
    "reports an infrastructure error (exit 2) when the routing surface is absent",
    "exits 0 on the real repository"
   ],
   "scripts/lint-sprint-archive-writers.test.ts": [
    "rejects a new production raw legacy archive writer",
    "resolves const targets before classifying archive writes",
    "resolves variable assignments before classifying archive writes",
    "keeps same-name targets decidable across lexical scopes",
    "resolves imported fs symbols and imported path aliases",
    "resolves namespace and variable aliases for createWriteStream",
    "rejects const and alias-computed filesystem mutation members",
    "resolves computed node:fs and node:path namespace members",
    "resolves dynamic import namespaces and destructured methods",
    "resolves direct dynamic import and require computed members",
    "resolves fs promises and native path namespace imports",
    "treats mutating open flags as writes but leaves read-only open alone",
    "rejects destructive canonical archive roots",
    "allows canonical authority symbols and denies unknown symbols in the same file",
    "separates verified legacy retirement from an unknown destructive caller",
    "normalizes native Windows separators in resolved target values",
    "does not classify archive reads or copy sources as writers",
    "propagates the canonical resolver and imported archive constants",
    "propagates object properties plus local wrapper arguments and returns",
    "fails closed when an archive-derived mutation target remains unresolved",
    "resolves destructured node:fs namespace methods and mkdtemp writers",
    "scans JS, MJS, CJS, and JSX production sources",
    "requires exact node:fs provenance and rejects same-name authority spoofing",
    "rejects direct metrics rotation mutations while accepting the canonical publisher",
    "rejects observability-local terminal admission while accepting canonical delegation",
    "recognizes only provenance-bound read-only open flags",
    "treats node:fs constants.%s as a mutating open flag"
   ],
   "scripts/lint-stale-adr.test.ts": [
    "fails (exit 1) on a NEW un-allowlisted numeric-ADR catalog value",
    "passes (exit 0) when the only numeric-ADR hit is the grandfathered ADR-037 (workspace.worker.contract)",
    "does not flag ADR-G-0NN / ADR-D-0NN governance ids (positive control for the digit-first regex)",
    "does not flag comment-only legacy ADR ids (out of this ratchet\\'s scope — catalog VALUES only)",
    "real repo: node scripts/lint-i18n-hardcode.mjs exits 0"
   ],
   "scripts/lint-terminal-readability.test.ts": [
    "fails on a hex literal, a named Ink color, a raw SGR color and dim inside the Terminal surface",
    "passes when colors come from the palette roles and only the caret uses inverse",
    "ignores test files and files outside the Terminal surface",
    "a typed allowance comment exempts one line and names its reason",
    "the real Terminal surface is clean"
   ],
   "scripts/lint-test-hermeticity.test.ts": [
    "detects process.cwd() + .deckent access as a violation",
    "detects process.cwd() + .brain access as a violation",
    "detects .deckent/config.json as literal in readFileSync",
    "passes clean test file (uses tmpdir sandbox) with 0 violations",
    "does not flag comment-only lines",
    "includes file:line:match:label in each violation object",
    "does not flag .deckent paths inside a tmpdir variable on the same line",
    "does not flag lines using mkdtempSync sandbox with .deckent",
    "detects split-line aliases that write into the live .tasks authority",
    "detects projectDir capability forwarding into a spawned provider",
    "does not classify a read-only open as a writer",
    "classifies tmpdir-derived writers as sandboxed without a filename waiver",
    "keeps repo-local .test-* scratch visible as migration debt, not live .tasks authority",
    "does not waive a live writer merely because it appears inside any toThrow assertion",
    "records an exact stable-code guard assertion as a guarded denial",
    "resolves same-name bindings by lexical identity instead of global name",
    "does not trust a local function that merely has the name tmpdir",
    "does not treat a local same-named function as an fs mutation sink",
    "honors lexical shadowing over an imported fs sink capability",
    "supports trusted Node import aliases, namespaces, and computed members",
    "fails loud for exact relative live authority paths with boundary-specific codes",
    "keeps an unknown path base with a protected suffix honest-unresolved",
    "requires the denial code to match the exact protected boundary",
    "rejects a forged stable-code denial produced by an explicit throw",
    "reconstructs protected paths split across binary, template, and compound assignment",
    "conservatively retains hazardous conditional and array-destructured branches",
    "binds var declarations to the nearest function/source scope",
    "propagates trusted fs authority through const and object-destructured aliases",
    "does not propagate fs authority through aliases of local fakes",
    "treats deferred path.resolve segments as absolute-reset risk without trusting join",
    "normalizes only an effective first protected relative segment",
    "requires a block denial thunk to contain exactly one direct mutation statement",
    "registers Node 24 disposable temp APIs as protected fs sinks",
    "retains live authority through cwd binary and template composition",
    "retains a hazardous conditional object branch through destructuring",
    "models var redeclarations as one hoisted binding with hazardous branches dominant",
    "keeps trusted fs capability at its use position despite a future reassignment",
    "propagates call arguments into an interprocedural fs sink helper",
    "propagates shorthand, spread, object alias, and receiver alias projectDir authority",
    "honors path.resolve absolute resets and rejects temp-root traversal escapes",
    "rejects a guard denial forged by an argument that throws before mutation",
    "recognizes fs.promises aliases and ambient CommonJS require capabilities",
    "retains fs capability through projections, bind, dynamic import, and import-equals",
    "retains possible fs authority through logical, conditional, object, and computed aliases",
    "normalizes nested destructure and call/apply/bind fs invocation wrappers",
    "propagates destructured parameters and object-method helper arguments to sinks",
    "tracks property/destructuring assignments, aliased apply argv, and getBuiltinModule",
    "does not waive a guard after a trusted fs spy replaces the sink",
    "never authenticates guard denial through mocked fs or an unbound expect",
    "canonicalizes dot segments before matching absolute repo boundaries",
    "treats portable case and trailing-dot aliases as live boundaries",
    "uses one protected-root policy for .brain and .deckent direct mutations",
    "keeps untranslatable Windows namespace paths honest-unresolved",
    "never lets guarded-denial evidence waive a confirmed live target",
    "registers opaque apply argv as unresolved instead of dropping the sink",
    "tracks trusted createRequire/module.require builtin capabilities only",
    "canonicalizes temp and repo-scratch traversal before granting sandbox authority",
    "rejects guarded denial when identifier coercion can throw before the fs call",
    "preserves trusted capabilities through common TypeScript expression wrappers",
    "tracks async disposable ownership and destructured path projections as temp",
    "covers additional fs mutators and the WriteStream constructor",
    "propagates project authority through direct, computed, nested, conditional, and return flows",
    "retains disposable temp ownership through its path projection",
    "detects legacy live reads through aliases without unrelated same-line temp waivers",
    "covers legacy fs read capabilities and conditional aliases without name-based waivers",
    "ignores the obsolete whole-file skipLegacyReads escape hatch",
    "scans a cycle-safe local support graph and Vitest setupFiles",
    "scans the cycle-safe eager production graph without executing dormant code",
    "tracks eager child effects and keeps dormant child definitions out of authority",
    "propagates eager imported const functions and class construction effects",
    "executes local class/base construction exactly and keeps dormant methods out",
    "maps concrete, omitted, undefined, spread, and destructured defaults across calls",
    "accounts for callable branches, scheduled callbacks, tags, getters, and decorators",
    "records stable unresolved outcomes for unsupported eager invocation boundaries",
    "merges production-scale unresolved edges without exceeding call argument limits",
    "resolves star exports once and fails loud for missing or ambiguous bindings",
    "keeps star-group pending accounting stable across zero-target nested branches",
    "does not execute exact ESM or CommonJS CLI main guards during import",
    "records only genuine eager-analysis gaps and fingerprints content plus edges",
    "keeps its own ratchet fingerprints stable across baseline value widths",
    "keeps scan identities and policy fingerprints equal across LF and CRLF",
    "invalidates local import resolution across missing and present rescans",
    "enforces one cooperative monotonic budget across discovery and analysis",
    "enforces an injectable RSS/heap budget through the same scan guard",
    "detects violations in a synthetic non-hermetic test file",
    "never lets a legacy allowlist suppress a newly introduced live read",
    "migrates only an exact legacy read callsite and rejects a new neighbor",
    "never lets a legacy read allowlist suppress a live writer",
    "reports 0 violations for a fully hermetic test file",
    "treats absent manifests as optional and malformed manifests as unresolved",
    "records an exact Vitest rejects guard as expected-missing evidence",
    "violation objects carry file:line info for reporting",
    "traces npm aliases to the destructive clean sink",
    "terminates alias cycles without inventing an effect",
    "supports npm global flags, Windows launchers, and node.exe clean execution",
    "does not reinterpret quoted text or non-shell argv as commands",
    "finds a test-to-shell-to-package clean chain without executing it",
    "traces a trusted child_process exec alias directly into npm clean",
    "traces trusted namespace/computed spawn with a literal argv vector",
    "does not trust a local function named exec as child_process authority",
    "propagates trusted child_process authority through a const alias",
    "keeps spawn argv structured and recognizes npm.cmd argv directly",
    "resolves npm workspace/prefix lifecycle effects from the selected manifest",
    "honors child cwd authority and resolves only exact root shell scripts",
    "rejects commands that escape an otherwise temporary child cwd",
    "includes automatic npm pre/post lifecycle scripts in transitive effects",
    "retains child authority through promisify/use-order and traces conditional commands",
    "fails closed for dynamic child commands and destructive commands with opaque cwd",
    "applies protected-root policy to child deletion and registers unknown interpreters",
    "traces Node execFile/fork forms and conditional child capabilities",
    "contains the known skip-if-absent files",
    "is an array of strings",
    "fingerprints unresolved semantic identity independent of registry order",
    "blocks strict mode and any count/digest drift from an accepted baseline",
    "ratchets production content and edge identity with full SHA-256",
    "is a non-empty array",
    "each entry has re (RegExp) and label (string)",
    "process.cwd()+.deckent pattern matches expected input",
    "process.cwd()+.brain pattern matches expected input"
   ],
   "scripts/manifest-autonomous.test.ts": [
    "sync-manifest.mjs source file contains the autonomous-runtime id string",
    "sync-manifest.mjs source file contains the F3-009 label string",
    "--dry-run --json output includes autonomous-runtime in active array",
    "--dry-run --json active entry has the correct F3-009 label",
    "features-manifest.json active array contains autonomous-runtime",
    "features-manifest.json autonomous-runtime entry has expected files"
   ],
   "scripts/memory-compact-host-proof-observer.test.ts": [
    "retains the failing import stage without leaking paths or emitting success"
   ],
   "scripts/multi-provider-fleet-smoke.test.ts": [
    "registers 8 fleet providers and lists them",
    "default provider is claude (first registered subscription)",
    "listByType returns subscription providers",
    "listByType returns api providers",
    "listByType returns local providers",
    "throws on duplicate registration",
    "empty registry getDefault returns null",
    "returns all 8 adapters with correct authMode",
    "does not mint a second model catalog inside a provider-routing smoke",
    "routes each of the 8 providers to the correct adapter",
    "rejects an explicit unknown provider instead of selecting the default",
    "no task.provider uses default (claude)",
    "throws when registry is empty",
    "concurrent routing to 8 providers resolves each correctly",
    "registry remains intact after many routing calls",
    "tasks with no provider all route to claude default",
    "no overflow when quota not exhausted",
    "overflows to API provider when subscription quota exhausted",
    "already_api task is not overflowed",
    "local provider (ollama) is not overflowed",
    "no_equivalent when registry has no API providers",
    "passes all 4 scenarios",
    "scenarios include expected labels"
   ],
   "scripts/multi-provider-smoke.test.ts": [
    "registers providers and lists them",
    "first registered provider becomes default",
    "setDefault=true overrides default",
    "has() returns true for registered, false for unknown",
    "throws on duplicate registration",
    "empty registry getDefault returns null",
    "3 providers registered — claude, ollama, openai-compat",
    "default provider is claude (registered first)",
    "routes to declared provider when registered",
    "routes openai-compat task to openai-compat adapter",
    "rejects an explicit unknown provider instead of selecting the default",
    "no task.provider uses default (claude)",
    "throws when registry is empty",
    "concurrent routing to 3 providers resolves each correctly",
    "registry remains intact after multiple routing calls",
    "does not mint a second model catalog inside a provider-routing smoke",
    "runSmoke passes all 4 scenarios",
    "runSmoke scenarios include expected labels"
   ],
   "scripts/npm-shrinkwrap-contract.test.ts": [
    "returns exact byte, digest, lockfile and package-count identity",
    "rejects a second root package-lock authority",
    "rejects every alternative root dependency-lock authority",
    "does not treat a nested product lock as a competing root authority",
    "rejects missing, malformed and oversized shrinkwrap bytes",
    "rejects symlinked and hard-linked shrinkwrap authority",
    "rejects non-v3, package identity and root dependency-map drift",
    "rejects reformatted or top-level reordered shrinkwrap bytes"
   ],
   "scripts/platform-registry.test.ts": [
    "resolves a `const X = process.platform === literal` flag definition",
    "resolves a negated-operator flag definition",
    "ignores unrelated const declarations",
    "classifies an inline process.platform literal (===)",
    "classifies an inline process.platform literal (!==)",
    "resolves a bare flag identifier through the flags map",
    "flips the operator for a negated flag identifier",
    "classifies a measured-capability probe",
    "falls back to unclassified for an unrecognized condition, never dropping it",
    "finds a describe.skipIf gated on a resolved windows flag",
    "finds an it.skipIf gated on an inline linux-required literal",
    "detects a non-skip if(process.platform) behavior-differs guard",
    "routes an unrecognized skipIf condition to unclassified instead of dropping it",
    "counts files with no platform-conditional gate as \"all other test files\"",
    "buckets platform-literal sites by tag and excluded/required-only direction",
    "places markers at ## Categories on first run and preserves everything else byte-for-byte",
    "replaces only the block between existing markers on subsequent runs",
    "is idempotent: regenerating twice with the same body yields the same content",
    "throws instead of guessing when the ## Categories heading is missing",
    "throws when markers are absent",
    "renders every platform tag section even when empty (\"None at this time\")",
    "never silently drops an unrecognized skipIf — it appears in the Unclassified section",
    "--check reports drift (exit 1) when markers are absent",
    "--write regenerates the doc, then --check reports no drift",
    "returns 2 when neither --check nor --write is passed",
    "returns 1 when tests/PLATFORM.md does not exist under root",
    "reports the committed AUTOGEN block as in sync",
    "fresh regeneration matches the committed AUTOGEN block byte-for-byte"
   ],
   "scripts/pre-flight-health-check.test.ts": [
    "returns passed when tsc exits with 0",
    "returns failed when tsc exits non-zero",
    "includes timing in durationMs",
    "skips when skip=true",
    "returns passed when vitest exits 0",
    "returns failed (non-required) when vitest exits non-zero",
    "returns passed when .brain/ does not exist",
    "returns passed when total lines within budget",
    "returns failed when total lines exceed budget",
    "returns passed when .locks/ does not exist",
    "returns passed when no lock files present",
    "returns passed when locks are fresh",
    "returns failed when stale locks present",
    "falls back to statSync mtime when acquiredAt missing",
    "returns passed when docker info exits 0",
    "returns failed (non-required) when docker not available",
    "returns passed when deckent CLI not available (non-fatal)",
    "returns passed when src/mcp/ found",
    "returns failed when neither dist nor src mcp found",
    "returns passed when all checks succeed",
    "abortSprint=true when a required check fails",
    "does not abort when only optional checks fail",
    "includes all expected check names"
   ],
   "scripts/production-wiring-host-proof-harness.test.ts": [
    "keeps the Terminal source observer fail-closed on legacy health text, output overflow and errors",
    "binds Terminal profile %s to its immutable observer, identity and success token",
    "fails closed when Terminal observer %s fails or emits a noncanonical result",
    "refuses Terminal source-runtime profile %s before execution when its observer asset is missing",
    "runs the registered Terminal provider observer with code-owned tests and target identities",
    "maps a legacy lossy memory observer failure to typed HOLD",
    "accepts only the exact memory observer protocol as a harness parser control",
    "rejects a changed memory observer asset before invoking the adapter",
    "accepts only after the real Closure OS receipt reader validates isolated durable authority data",
    "uses a fixed executable, argv and isolated environment",
    "rejects %s before invoking any adapter",
    "rejects unknown adapters, arbitrary executables and plan-authored target echoes",
    "rejects asset path drift, symlinks and digest mismatch before execution",
    "fails closed on adapter %s",
    "rejects generic structured target echo and non-authoritative success text",
    "rejects adapter errors and verifier-asset changes after observation"
   ],
   "scripts/prompt-linter.test.ts": [
    "returns score 100 for a clean prompt",
    "deducts 15pts when ADR ratio is above 50% but below 70%",
    "correctly identifies ADR ratio > 50% and applies -15 deduction",
    "deducts 20pts when agent truncation pattern is detected",
    "deducts 5pts for empty filler headers",
    "deducts 10pts when rubric spec is present",
    "deducts 10pts when char count exceeds 40000",
    "applies cumulative deductions for multiple issues",
    "returns noFiles=true when no prompt files exist for sprint",
    "Integration: sprint average score >= 75 for clean prompts",
    "reports low average score when prompts have multiple issues",
    "computes avgScore correctly across multiple files"
   ],
   "scripts/provider-free-smoke-docker.test.ts": [
    "resolves claude provider models to \"claude\" binary",
    "resolves codex provider models to \"codex\" binary",
    "resolves gemini provider models to \"gemini\" binary",
    "rejects Ollama at the Docker CLI binary boundary",
    "rejects an unknown model instead of silently selecting Claude"
   ],
   "scripts/publish-workflow.test.ts": [
    "should exist",
    "should have correct trigger event",
    "should have permissions set correctly",
    "should use Node.js 24.x",
    "should have npm ci step",
    "should have build step",
    "should have test step",
    "is dry-run-only and never executes a provenance publish",
    "uses no publish credential or OIDC grant in the retired validation-only workflow",
    "should have registry-url set to npmjs.org",
    "should run on ubuntu-latest",
    "should have checkout step (SHA-pinned, 414-001 SEC-06)",
    "should have setup-node step (SHA-pinned, 414-001 SEC-06)",
    "should have cache enabled for npm",
    "should key the npm cache explicitly from the canonical root shrinkwrap"
   ],
   "scripts/reclassify-backfill.test.ts": [
    "parses --backfill-missing and --default-prior-decision",
    "appends only the missing task lines, leaves existing ones untouched, and recomputes counters",
    "creates the Task Outcomes section when the entry has none",
    "is a no-op when every referenced task already has an outcome line",
    "produces a parseable body whose counters match the given pairs",
    "creates a sprint-log row for a sprint with no DB entry at all",
    "appends only the missing task line to an existing sprint row (task-not-in-outcomes case)",
    "is a no-op when every referenced task already has an outcome line",
    "auto-flips the prior decision away from the target so it never lands on already-classified",
    "honors an explicit defaultPriorDecision option",
    "honors a per-entry priorDecision override",
    "makes no DB writes in dry-run mode",
    "WITHOUT --backfill-missing reproduces the historical 2-applied/10-skipped split",
    "WITH --backfill-missing produces 12/12 applied and zero skipped"
   ],
   "scripts/repl-smoke-verify.test.ts": [
    "returns false for a path that does not exist",
    "returns true for package.json (always present in repo)",
    "passes when stdout has help keywords and elapsed is well under 1000ms",
    "passes when stdout contains \"help\" keyword and elapsed < 1000ms",
    "fails when elapsed exceeds 1000ms",
    "fails when stdout contains no help keywords",
    "fails when stdout is blank even if elapsed is fast",
    "passes when stdout contains \"deckent\" (from renderStatusLine)",
    "passes when \"deckent\" appears anywhere in output",
    "fails when stdout is empty",
    "fails when stdout does not contain \"deckent\"",
    "passes when elapsed is well under 8000ms",
    "passes at exactly 7999ms",
    "fails when elapsed exceeds 8000ms",
    "fails at exactly 8001ms",
    "passes when stdout contains the \"›\" user-message prefix",
    "passes with just the \"›\" prefix even without assistant header",
    "fails when stdout is empty (REPL produced no output)",
    "fails when stdout has content but no \"›\" prefix (layout not wired)",
    "returns skipped=true and pass=true when entryPath does not exist",
    "populates scenarios with SKIP entries when dist is absent",
    "does NOT mark as failed when dist is missing (skip ≠ failure)"
   ],
   "scripts/routing-distribution.test.ts": [
    "calculates percentage correctly for multiple agents",
    "returns empty entries for empty performance map",
    "returns zero percentages when total is zero",
    "sorts entries by task count descending",
    "warns when a single agent exceeds the threshold",
    "returns no warnings when all agents are below threshold",
    "returns no warnings for empty distribution",
    "uses default threshold of 70%",
    "does not warn at exactly the threshold (only strictly above)",
    "returns null for a directory without learnings.json",
    "loads real learnings.json from project root",
    "all percentage values sum to approximately 100"
   ],
   "scripts/routing-imbalance-guard.test.ts": [
    "passes when distribution is balanced (no agent exceeds 80%)",
    "fails when a single agent exceeds the 80% default threshold",
    "respects a custom threshold (e.g. 60%)",
    "passes with empty distribution (no agents = no imbalance)",
    "does not fail at exactly the threshold (only strictly above)",
    "reports all violating agents, not just the first",
    "balanced performance map passes CI guard",
    "heavily skewed performance map fails CI guard"
   ],
   "scripts/script-registry.test.ts": [
    "finds known top-level .mjs scripts in the real repo",
    "excludes non-.mjs files (.ts/.sh/.json)",
    "excludes files inside scripts/ subdirectories (non-recursive)",
    "returns a sorted, de-duplicated list",
    "reads and parses the real scripts/script-registry.json",
    "returns E_REGISTRY_MISSING for a nonexistent path",
    "returns E_REGISTRY_UNPARSABLE for malformed JSON",
    "passes for a registry that exactly covers the real files",
    "rejects a non-object registry root",
    "rejects a registry with entries not an array",
    "flags a real file with no registry entry",
    "flags a registry entry for a file that does not really exist",
    "flags a duplicate file entry",
    "flags an invalid class enum value",
    "flags a missing required field: %s",
    "flags an invalid npmScript type",
    "accepts npmScript as null or a string",
    "accepts every enum class value on an otherwise-valid entry",
    "reports ZERO registry debt — the real directory is covered exactly",
    "exits 0 against the repaired real repo root",
    "exits 1 when the registry is missing an entry for a real file",
    "exits 1 when an entry has an invalid class",
    "exits 0 when the fixture registry exactly covers the fixture directory",
    "exits 2 when scripts/script-registry.json is missing entirely",
    "exits 2 when scripts/script-registry.json is unparsable"
   ],
   "scripts/scripts.test.ts": [
    "uses taskkill first and reports its %s outcome without leaking the original child",
    "delegates once to the canonical check-only validator rooted at the script location",
    "propagates canonical validator failure without starting a build",
    "fails when packed artifact omits %s",
    "fails closed when npm pack does not return the structural JSON contract",
    "completes SIGKILL escalation for a TERM-ignoring detached descendant",
    "always fails with the retirement notice, regardless of arguments",
    "never mutates package.json (stub exits before any write)",
    "all scripts should be executable",
    "all scripts should have proper shebang",
    "bump-version.sh stays a failing stub for every historical bump type"
   ],
   "scripts/serve-localhost-smoke.test.ts": [
    "token okunur — GET / returns window.__DECKENT_API_TOKEN__ matching server token",
    "POST 200 — POST /api/config with valid Bearer token returns non-401",
    "token'sız 401 — POST /api/config without Bearer returns 401",
    "server kapanır — api.close() resolves cleanly",
    "readTokenFromHtml returns null when HTML has no token injection"
   ],
   "scripts/skill-catalog-determinism.test.ts": [
    "identical digests → IN_SYNC pass",
    "divergent digests with no baseline → typed UNDECLARED_DRIFT failure",
    "declared drift WITH a disposition passes and surfaces the disposition",
    "a bare allowlist entry (no disposition) is REFUSED — the §S8 shape is a disposition",
    "the gate derives the digest from the S5 snapshot — no second hash mechanism in the script",
    "pass 1 runs catalog-only (sidecar overlay excluded), pass 2 runs full-state — a real diff, not two identical resolutions"
   ],
   "scripts/sprint-retroactive-reclassify.test.ts": [
    "parses flags and string values",
    "extracts task outcomes from the canonical sprint body",
    "ignores lines outside the Task Outcomes section",
    "reads the decision from title-enriched lines (\"- id: EV — title\")",
    "replaces a task line and recomputes counters",
    "throws for a missing task id",
    "throws for an invalid decision string",
    "preserves the title suffix when rewriting a title-enriched line",
    "rejects missing fields",
    "accepts every member of VALID_DECISIONS",
    "moves successRate up when NO_GO -> DONE",
    "moves successRate down when DONE -> NO_GO",
    "treats GO_WITH_TECH_DEBT as success and is a no-op when category unchanged",
    "no-ops when totalUses is zero",
    "updates an agent.json on disk",
    "returns no-change when nothing flips",
    "returns manifest-missing for a non-existent file",
    "reclassifies a single task and updates the sprint entry content",
    "is idempotent — second run is a no-op for the same target",
    "skips entries whose sprint is missing or whose task is not in the outcomes section",
    "processes a 6-entry bulk batch from --from-file shape",
    "recomputes agent stats when an agent id is supplied",
    "returns base path when nothing exists",
    "appends a suffix when the base exists",
    "writes an audit file containing the applied and skipped arrays",
    "writes no audit file in dry-run mode and leaves the DB untouched",
    "rejects an empty entries array"
   ],
   "scripts/stress-simulator.test.ts": [
    "(a) exits 1 when neither --force nor env flag is set",
    "(b) prints descriptive error message when no opt-in",
    "(c) DECKENT_STRESS_SIMULATE=1 bypasses the gate (process starts)",
    "(d) backup file is created in .tmp/ when DECKENT_STRESS_SIMULATE=1",
    "(e) --force flag bypasses the safety gate",
    "(f) DIRECTIVES.md is preserved after gate blocks"
   ],
   "scripts/sync-core-memory.test.ts": [
    "projects repo authority to the configured host directory",
    "overwrites projection drift and removes projection-only markdown",
    "is idempotent when authority and projection are equal",
    "check mode reports drift without mutating the projection",
    "rejects removed host-to-authority modes",
    "requires an explicit absolute projection target"
   ],
   "scripts/sync-manifest-metadata.test.ts": [
    "preserves valid prior sprint metadata when isolated runtime evidence is absent"
   ],
   "scripts/sync-to-product.test.ts": [
    "drops files matching every EXCLUDE category; keeps everything else",
    "isExcluded matches directory prefix only at boundary (not as substring)",
    "exposes the full EXCLUDE list documented in the task spec",
    "catches Anthropic + Google key shapes; ignores plain text",
    "shouldSkipKeyScan allows test fixtures, blocks production paths",
    "syncToProduct aborts with violations when a real-shape key sits in a kept file",
    "dry-run does NOT write anything to disk",
    "apply writes a staging dir; running twice is idempotent (same drop set, exclusions absent)",
    "script stays within reasonable size budget and contains the security regex literals",
    "docs/development/repo-sync.md exists and explains two-repo model + manual-push rule"
   ],
   "scripts/terminal-acceptance-battery.test.ts": [
    "parseBatteryArgv handles help, dry-run, local-only and rejects unknown flags",
    "buildUiLineCatalog uses getMessage without fallback copy",
    "counts approvals and tool lines from UI catalog, not generic regex",
    "does not count MASTER-PLAN prose as checkpoint via regex collision",
    "counts scratch checkpoint files directly",
    "parseBootStatus and detectTurnCompleted capture provider + turn-end footer",
    "evaluateAcceptanceCriteria enforces 7107-c boot, prompt, turn-end and tool-call gates",
    "includes 7114 threshold checks with detail",
    "classifies DIST_MISSING and hosted credential SKIP",
    "writes evidence v3 JSON",
    "resolveScenarioBinding prefers config native_model when listed by local-llm",
    "repoRootForEntry resolves checkout owning dist",
    "deriveMeasurementAuthority reads catalog-rendered exact line",
    "extractToolLines returns first twelve tool rows",
    "does not treat user prompt echo as assistant narrative",
    "classifies reference child coverage and interim HOLD separately from tool-row gate",
    "excludes catalog-rendered reference unavailable lines from assistant narrative (EN/TR)",
    "separates failed reference phase from partial and marks child invocation unknown on receipt mismatch",
    "replays failed-ref digest fixture without false visible-analysis PASS",
    "does not treat real technical answers mentioning config keys as remedy guidance",
    "keeps typed unavailable envelope independent from output-invalid journal evidence",
    "treats truncated wrapped schema-unavailable and remedy guidance as non-narrative (EN/TR)",
    "replays schema-unavailable PTY fixture without false 74ms analysis PASS",
    "reconstructs PTY logical lines across chunk splits (segmentation-invariance)",
    "does not treat CRLF-wrapped interim-request envelope continuation as narrative or delivery (TR/EN)",
    "reconstructs plain text when ANSI escape sequences span PTY chunk boundaries",
    "keeps multiline bracket envelope as one host notice unit until closing bracket",
    "does not treat split interim-request envelope fragments as narrative or delivery (TR/EN)",
    "detects post-request prose delivery after split request envelope (same chunk and next chunk)",
    "excludes catalog-rendered interim request notices from narrative and structured interim delivery (EN/TR)",
    "excludes catalog-rendered interim-skipped and usage-hold host notices from narrative (EN/TR)",
    "passes interim deliverable gate for structured interim answer control (not host skip notice)",
    "replays boundary PTY fixture with request≠delivery timing (106633ms actual, not 105803ms request)",
    "replays interim-skipped PTY fixture without false 58380ms narrative PASS",
    "replays digest-pinned PTY fixture with null narrative (no false 11ms prompt PASS)",
    "replays actual D0D1D2 e-first-answer PTY with post-request interim at ~60243ms (not false HOLD)",
    "final collector parse requires observation.ptyChunks for post-request interim (plain scrollback false negative)"
   ],
   "scripts/test-ci-sim.test.ts": [
    "projects a stable dirty source snapshot without exposing ignored live state",
    "omits every implicit untracked file and rejects explicit protected state",
    "materializes and removes concurrent exact worktrees without global pruning",
    "copies dependencies so workspace writes never reach the live dependency tree",
    "skips dependency materialization during dry-run and records that fact",
    "uses an isolated credential-free Git environment for every workspace operation",
    "fails closed on repo-local executable Git filters",
    "rejects protected path traversal before touching source state",
    "binds the effective exclusion policy and executed tree into the receipt",
    "reaps only a dead-owner disposable worktree and leaves live state untouched",
    "never signals a manifest PID that may have been reused",
    "holds a crash-window child claim even before the manifest update",
    "preserves malformed stale state for an honest manual HOLD",
    "retains the exact workspace when Git removal fails",
    "disposes the isolated workspace after a successful injected run",
    "turns cleanup failure into an honest gate failure",
    "makes the child claim first-writer-wins with equal replay only",
    "admits only one host-wide two-fork lease at a time",
    "serializes stale capacity takeover so exactly one contender wins",
    "reclaims a dead-owner mutex but holds malformed and live mutex state",
    "rejects unsafe capacity paths and fail-loud lease mismatches",
    "rejects resource-cap overrides and unknown wrapper flags",
    "drains large stdout before resolving the subprocess result",
    "fails closed on a malformed gated-runner completion message",
    "does not inherit provider credentials and isolates all temp homes",
    "terminates the Windows process tree with /T and escalates with /F",
    "holds when Windows process-tree termination cannot be verified",
    "kills a gated wrapper when durable child recording fails",
    "contains no live-state rename/delete protocol and stays a thin orchestrator"
   ],
   "scripts/test-contained-ci-sim.test.ts": [
    "makes the pristine bootstrap the first external JavaScript module",
    "uses typed probe HOLD without workspace materialization or candidate execution",
    "serializes pristine-v3 cleanup against prepare and leaves failed cleanup durably held",
    "orders durable claim before birth and releases cleanup only after HMAC finality",
    "keeps cleanup authority opaque, manifest-bound, and one-shot under forgery attempts",
    "holds and retains on a forged supervisor receipt",
    "holds and retains when the trusted receipt transport loses the receipt",
    "rejects replayed facet evidence before candidate birth",
    "does not clean a workspace whose process-tree finality is unknown",
    "never promotes plain injected facets or adapters through the public wrapper",
    "rejects Node preload flags before durable prepare intent or candidate birth",
    "rejects an alternate external entry before durable prepare or candidate birth"
   ],
   "scripts/test-containment-adapters.test.ts": [
    "fails closed on malformed adapter input without executing a probe",
    "never treats binary presence as live capability proof",
    "builds a no-host-root Linux plan only from authority-derived evidence",
    "requires an attested macOS supervisor in addition to Seatbelt",
    "requires signed helper plus AppContainer, RestrictedToken, and Job Object facets",
    "rejects WSL1, drvfs, and Windows interop without raw fallback",
    "uses digest-only OCI create-before-start identity and rejects unsafe inputs"
   ],
   "scripts/test-containment-authority.test.ts": [
    "canonicalizes records deterministically and rejects non-JSON authorities",
    "signs and verifies a claim without projecting raw secret material",
    "fails closed on claim identity, MAC, digest, and secret tampering",
    "binds final receipt to claim, resource identity, admission, execution, and settlement",
    "keeps raw authority secret inside a branded replay-guarded host session",
    "selects deterministically but cannot enroll caller-declared candidates"
   ],
   "scripts/test-containment-bootstrap.test.ts": [
    "never creates a candidate without a durable resource claim",
    "awaits the exact durable release before invoking the one-shot birth callback",
    "keeps candidate birth at zero for forged or mismatched release state",
    "maps durable authority failure to HOLD without attempting spawn",
    "treats a throwing spawn boundary as unknown birth and retains authority state",
    "runs pristine bootstrap checks before the deferred candidate loader",
    "never calls the candidate loader when a bootstrap assertion is unproven",
    "rejects startup injection before the external candidate module is loaded"
   ],
   "scripts/test-containment-contract.test.ts": [
    "admits only complete strong enforcement facets and never marks admission as proof",
    "keeps audit and process-only boundaries fail-closed",
    "makes proof eligible only after settled execution and complete settlement facets",
    "rejects caller proof claims and forged facet summaries",
    "keeps facet authority identity opaque under cloning, proxies, and intrinsic tampering",
    "generates a bounded Node 24 plan without claiming containment proof or network denial",
    "rejects untrusted startup argv %s",
    "rejects untrusted startup environment key %s",
    "rejects old runtimes, broad roots, and read/write authority overlap"
   ],
   "scripts/test-containment-cose-sign1-contract.test.ts": [
    "builds an RFC 9864 ESP256 FIPS structure and remains proof-ineligible",
    "validates canonical envelopes without claiming authentication",
    "uses only fully-specified algorithms and honest portable policy",
    "makes only the Deckent profile header critical",
    "binds external AAD but never exposes mutable byte/digest pairs",
    "rejects accessors, proxies and shared-memory views without throwing",
    "rejects unprotected authority, malformed scalars and high-S ESP256",
    "enforces every ESP256 scalar and raw-shape boundary",
    "never invokes byte species or poisoned cross-realm methods",
    "returns typed HOLD for revoked proxies and preflights oversized bytes",
    "denies SharedArrayBuffer on every public byte-bearing path",
    "keeps all returned byte and digest pairs on private snapshots"
   ],
   "scripts/test-containment-dependency-projection.test.ts": [
    "binds complete inventory, package manager, exact Node realm, and pinned layer",
    "is deterministic across artifact discovery order",
    "uses locale-independent deterministic ordering for large inventories",
    "rejects missing, empty, or count-inconsistent completeness authority",
    "rejects declared cross-realm reuse before compatibility evaluation",
    "requires exact target runtime, layer, build realm, and Node version bindings",
    "keeps unknown native ABI fail-closed and rejects projection tampering",
    "never treats a caller-declared native-free inventory as scanner authority"
   ],
   "scripts/test-containment-deterministic-cbor.test.ts": [
    "uses RFC 8949 preferred integer forms and deterministic map ordering",
    "round-trips bounded scalar, byte, array and mixed-key map values",
    "rejects unsupported values, unsafe descriptors, aliases and cycles",
    "rejects non-canonical, indefinite, tagged, floating and malformed wire forms",
    "fails closed on depth, collection and byte budgets",
    "rejects proxies and shared memory without invoking traps",
    "never invokes typed-array species or foreign byte-view methods",
    "validates exact own-data limits without invoking accessors or traps",
    "preflights UTF-8, byte inputs and aggregate map-key scratch",
    "keeps encoded, decoded and validated canonical authority private"
   ],
   "scripts/test-containment-inventory-ratchet.test.ts": [
    "binds every foundation module without accepting new unresolved effects"
   ],
   "scripts/test-containment-measurement-contract.test.ts": [
    "keeps nine phases and consumes grants only after prepared/settled envelopes",
    "rejects arbitrary digest authority and recomputes full envelope links",
    "retains contradictory phase evidence in COMPLETE_HELD aggregation",
    "rejects wrong causal %s binding",
    "requires exactly one parent-issued credential for every non-root authority",
    "enforces the exact %s component-role authority set",
    "holds cross-module platform policy for %s",
    "rejects unrelated component measurements and unsupported terminal mode",
    "never delegates deterministic ordering to localeCompare",
    "rejects trusted challenge and issuer lineage drift",
    "snapshots public bytes and accepts safe cross-realm Uint8Array values",
    "rejects Proxy/SAB authority without invoking traps"
   ],
   "scripts/test-containment-owned-execution.test.ts": [
    "does not spawn without a claim-bound prepared resource",
    "releases an ambiguously born resource when the child handle is malformed",
    "settles only after explicit tree finality and adapter identity proof",
    "bounds output, terminates on overflow and never exposes the discarded bytes",
    "retains the run when finality or adapter identity is not proven",
    "never treats raw PID/process-group control as a strong execution adapter",
    "re-reads v3 manifest authority before legacy spawn despite mutable object downgrade",
    "rebinds cleanup to manifest A and never follows mutable workspace target B"
   ],
   "scripts/test-containment-platform-evidence-policy.test.ts": [
    "freezes and validates the complete environment matrix",
    "marks a generic macOS terminal as honestly unsupported with no fallback",
    "fails closed on missing and explicitly forbidden evidence",
    "requires linked outer Windows and inner Linux receipts for WSL2",
    "requires rootless/private OCI authority instead of trusting a daemon CLI",
    "requires exact ordered component-role authority without fallback",
    "rejects inherited platform keys, inherited input fields and malformed roles",
    "rejects proxy inputs without invoking reflection traps",
    "turns revoked proxies into typed HOLD results without escaping exceptions",
    "freezes the required native hardening facets into platform evidence"
   ],
   "scripts/test-containment-protocol-vectors.test.ts": [
    "keeps the honest CDDL authority freeze synchronized without claiming parser proof",
    "uses RFC 9864 fully-specified algorithms and rejects ambiguous/unsupported ids",
    "reproduces the deterministic nine-envelope causal golden chain",
    "rejects causal forks, missing credentials and grant substitutions",
    "binds trusted challenge and composite AAD roles while signatures remain structural"
   ],
   "scripts/test-containment-runtime-projection.test.ts": [
    "projects only an exact strong-boundary runtime plan without claiming proof",
    "rejects broad or host-authority source %s",
    "rejects sensitive source descendant %s despite a self-declared allowlist",
    "rejects an exact-allowlist mismatch in either direction",
    "rejects writable source or target overlap with read-only authority",
    "rejects writable source/control authority, network inheritance, and weak process ownership",
    "accepts only descriptors 0/1/2 without inherited source descriptors",
    "classifies Windows roots, device paths, and named pipes as denied",
    "rejects digest tampering, caller proof claims, and evaluation binding drift"
   ],
   "scripts/test-e2e-surfaces.test.ts": [
    "returns a positive integer port number",
    "boot+ready: resolves baseUrl when child stdout emits the listening signal",
    "port-collision / no-ready: rejects baseUrlPromise within timeout window",
    "premature exit: rejects when child exits before emitting ready",
    "pass: 200 + token placeholder + /api/status=200 → pass=true",
    "fail: /api/status=401 → pass=false (serve-401 regression detection)",
    "fail: token placeholder missing in HTML → pass=false",
    "dist-yok skip-guard: returns { skipped: true, reason } when entry path missing",
    "kill-on-fail: when fetchImpl throws mid-assert, child.kill() is still invoked (try/finally)",
    "script file exists and stays within reasonable size budget (≤300 LoC)",
    "script contains all required keywords (Kanit grep target)",
    "DEFAULT_ENTRY resolves to dist/cli/entry.js under REPO_ROOT",
    "package.json registers `test:e2e-surfaces` script pointing at the harness"
   ],
   "scripts/update-readme-stats.test.ts": [
    "reads version from package.json",
    "counts test descriptors from tests/**/*.test.ts files",
    "parses MCP tools and resources from src/mcp",
    "counts numbered ADR files (skips README.md)",
    "counts agents excluding archive directory",
    "counts skills",
    "counts dashboard pages",
    "reads sprint from the tracked stats snapshot",
    "exposes coverage as a numeric value or null",
    "HERMETICITY: ignores live .brain sprint archive and local coverage artifact",
    "HERMETICITY: excludes machine-local temp-* agents from every agent count",
    "degrades to null volatile stats when the snapshot file is missing (honest drift)",
    "reads sprint/coverage from the snapshot and rejects non-numeric values",
    "refresh derives sprint from live sources (DIRECTIVES header)",
    "refresh is monotonic — a machine without .brain history never rewinds the sprint",
    "refresh captures coverage only with withCoverage and preserves it otherwise",
    "main --refresh-snapshot writes the snapshot and exits 0",
    "produces a markdown badge line with tests/coverage/sprints/version",
    "omits coverage badge when coverage is null",
    "URL-encodes the v1.0.0-beta.1 version (hyphen → --)",
    "always emits the CI status badge (never dropped on regeneration)",
    "lists MCP tools/resources, agents, skills, dashboard pages",
    "produces a markdown table with all expected rows",
    "formats the Tests line with the provided count",
    "replaces content between AUTOGEN markers",
    "is idempotent — same body twice yields identical output",
    "throws when block id is missing",
    "appends a valid AUTOGEN block to content that lacks markers",
    "preserves existing content before the injected block",
    "produces a block that replaceAutogenBlock can subsequently update",
    "reports drift when README is missing AUTOGEN markers",
    "clears drift after writing generated content",
    "exit 2 when neither --check nor --write is given",
    "exit 1 on drift in --check mode",
    "--write auto-injects AUTOGEN markers when absent (exit 0)",
    "exit 0 after --write then --check (round-trip)"
   ],
   "scripts/validate-publish-drift-gate.test.ts": [
    "spawning builtins-drift-check --check against the real repo passes, and checkBuiltinsDrift renders the real drift-gate line",
    "FAILs with the drifted item + exact re-pin command when a fixture file is mutated post-baseline",
    "baseline-green fixture (no mutation) round-trips clean — sanity check on the fixture itself"
   ],
   "scripts/validate-publish-readiness.test.ts": [
    "declares exactly 8 readiness gate ids (Sprint 270: + bin_exec_bits, dashboard_bundle)",
    "extracts package size and file count from npm pack output",
    "passes when size <= 3 MB and file count within acceptable band",
    "passes when size is exactly at the 3 MB threshold",
    "accepts a pack exactly at the 7 MB ceiling (native-custody calibration)",
    "fails when package size exceeds the 7 MB threshold",
    "passes when engines.node is >=24",
    "passes when engines.node is >=25",
    "fails when engines.node is <24",
    "fails when engines field missing",
    "passes when main and types are declared",
    "fails when main is missing",
    "fails when types is missing",
    "passes when no internal state directories are present",
    "fails when .deckent/ leaks",
    "fails when .brain/ leaks",
    "passes when adr command exits 0",
    "fails when adr command exits non-zero",
    "passes when lint:link exits 0",
    "fails when lint:link exits non-zero",
    "runs all 8 gates and reports summary with mocked inputs (Sprint 270: +2 fs gates, hermetic fixture)",
    "reports failed gates when any fail"
   ],
   "scripts/validate-publish.test.ts": [
    "converts kB correctly",
    "converts MB correctly",
    "converts bytes",
    "converts GB correctly",
    "returns 0 for empty string",
    "returns 0 for unrecognised format",
    "handles uppercase KB alias",
    "parses fractional MB",
    "extracts major from >=24.0.0",
    "extracts major from >=18",
    "extracts major from 24.x",
    "extracts major from ^24.0.0",
    "returns null for undefined",
    "returns null for empty string",
    "handles plain integer string",
    "GATES is a non-empty array of strings with 8 entries",
    "BIN_FILES is a non-empty array of strings",
    "parsePackOutput is a function",
    "parseSizeToBytes is a function",
    "checkPackSizeAndCount is a function",
    "checkEnginesNode is a function",
    "checkEntryPoints is a function",
    "checkNoInternalStateLeak is a function",
    "checkAdrLint is a function",
    "checkLinkLint is a function",
    "checkBinExecBits is a function",
    "checkDashboardBundle is a function",
    "runReadinessGates is a function",
    "passes at 4.8 MB (full build with dashboard — Sprint 270 measured)",
    "passes at 6.9 MB (just under the 7 MB ceiling)",
    "fails at 7.1 MB (just over the 7 MB ceiling)",
    "passes at 1.8 MB (base dist without dashboard build)",
    "checkEnginesNode returns correct gate name",
    "checkEntryPoints returns correct gate name and passes",
    "checkAdrLint returns correct gate name",
    "checkLinkLint returns correct gate name",
    "returns ok=false when engines.node < 24",
    "summary fields are all numbers",
    "returns exactly 8 checks (including bin_exec_bits and dashboard_bundle)",
    "passes when all bin files exist and are executable",
    "fails when bin files are missing",
    "fails when bin files exist but have no execute bit (mode 644)",
    "fails with actionable message listing missing file path",
    "passes when index.html and assets bundle are present",
    "fails when index.html is missing",
    "fails when index.html exists but no js bundle in assets/",
    "fails when assets/ directory is missing entirely",
    "ignores non-index js files (css, other bundles)"
   ],
   "scripts/verify-cli-help-matrix.test.ts": [
    "loads public manifest commands (hidden excluded)",
    "classifies deprecated top-level and virtual child manifest paths",
    "builds root + bilingual cases for every public path",
    "unions live public paths with canonical inventory and keeps contract gaps as HOLD candidates",
    "includes representative edge env cases with a disposable Turkish project config",
    "runSelfTest passes on catalog fixtures",
    "assertHelpOutput rejects wrong locale and missing flags",
    "assertHelpOutput accepts synthesized EN help from manifest strings",
    "rejects prose-only flags, optionality drift, and an extra enum on their owning rows",
    "accepts Commander bare argument display names while keeping Usage token syntax exact",
    "assertRootHelp validates localized root usage lines",
    "normalizeWs collapses whitespace for stable substring checks",
    "descriptionPresent accepts reordered enum tokens inside parentheses",
    "assertPinsStable detects post-run drift and pins the compiled static entry closure plus build identity",
    "auditInventory reports missing normal paths and unexpected virtual live paths",
    "runner script is import-safe and exposes matrix entrypoint",
    "marks timeout when child never exits",
    "returns bounded unreaped custody when a killed parent leaves its output pipe open",
    "preserves timeout when a TERM handler exits zero rather than claiming success",
    "marks combined overflow when stdout exceeds cap",
    "keeps invalid bytes as raw buffers until after their SHA-256 is recorded",
    "records nonzero exit as failure path via evaluateCase consumer"
   ],
   "scripts/verify-packed-networkless-install.test.ts": [
    "binds optional CLI receipt-file bytes exactly to the one stdout wrapper",
    "writes a durable receipt with exact stdout byte parity and rejects overwrite or symlink",
    "serves only immutable bytes at the exact digest path for GET/HEAD",
    "warms a fresh private cache from the exact tarball before an isolated offline install",
    "uses argument-vector execution without a Windows command shell",
    "fails closed and closes the server when warm HTTP installation fails",
    "rejects a warm installed package whose shrinkwrap identity differs from source",
    "ignores disk-path ABA swap/restore during warm install and serves immutable bytes",
    "rejects source/installed shrinkwrap drift before invoking the native verifier",
    "rejects a native receipt not bound to the source shrinkwrap digest",
    "rejects source shrinkwrap mutation after native proof and installed CLI execution",
    "rejects installed shrinkwrap mutation after native proof and installed CLI execution",
    "rejects installed compiled CLI failure before issuing a wrapper receipt",
    "rejects an invalid environment before creating a proof workspace"
   ],
   "scripts/workflow-install-hygiene.test.ts": [
    "detector fires on a known fail-open pattern (sanity check on the regex itself)",
    "has at least one workflow file to scan",
    "${file.replace(resolve('.'), '.')} has no error-swallowing \"install ... || true\" steps"
   ],
   "scripts/zero-hardcode-audit.test.ts": [
    "returns true for model-registry.ts",
    "returns true for model-catalog.ts",
    "returns false for other src files",
    "returns false for test files",
    "identifies // comment lines",
    "identifies JSDoc * lines",
    "identifies JSDoc /** opener lines",
    "returns false for regular code lines",
    "returns false for empty lines",
    "detects hardcoded opus model string in code",
    "detects hardcoded sonnet model string in code",
    "skips hardcoded model string in comment lines",
    "skips hardcoded model string in JSDoc lines",
    "returns empty array for non-existent file",
    "returns violation with correct line number",
    "exits 0 on current codebase (clean)",
    "exits 1 when a violation is injected via scanFile",
    "exits 0 on current clean codebase",
    "produces JSON output with --json flag",
    "allowlist exempts model-registry.ts from violations"
   ]
  },
  "sdk": {
   "sdk/deckent-client-sprint.test.ts": [
    "spawns `deckent start` via the injected spawnFn and returns pid + logPath — never a real subprocess",
    "builds argv from options: autoApprove/sandbox/force/dryRun/timeoutMs",
    "returns pid: null when the child handle reports no pid",
    "reads tasks + results from the live .tasks/ dir (source: live)",
    "falls back to .brain/archive/<sprintId>-tasks/ when .tasks/ has nothing for that sprint (source: archive)",
    "returns empty arrays and source: none when neither location has files (no throw)",
    "returns the retro-<sprintId> entry from .brain/memory.db",
    "returns null without throwing when no memory DB exists",
    "returns null when the DB exists but has no matching retro entry"
   ],
   "sdk/deckent-client.test.ts": [
    "reads sprintId, dashboard, and tasks from a populated project",
    "returns sprintId null / dashboard null / tasks [] for an empty project (no throw)",
    "returns matching entries from .brain/memory.db",
    "returns [] without throwing when no memory DB exists",
    "parses directives text into structured tasks without writing to disk",
    "probes + gates usage via an injected spawnImpl — never spawns the real claude binary",
    "respects custom thresholds"
   ],
   "sdk/package-exports.test.ts": [
    "is valid JSON",
    "publishes the SDK entry under an exports subpath, resolving to a real dist file",
    "preserves the existing \".\" export entry",
    "preserves existing main/types/bin entries"
   ]
  },
  "security": {
   "security/api-auth.test.ts": [
    "returns a 64-char hex string",
    "generates unique tokens on each call",
    "POST /api/start with valid bearer token passes auth and reaches the retired-route contract (410)",
    "POST /api/start without token returns 401 when auth is configured",
    "POST /api/set-directives without token returns 401",
    "POST /api/set-directives with valid token succeeds",
    "POST with wrong token returns 403",
    "GET endpoints require auth when token is configured (health endpoint excepted)",
    "POST endpoints return 401 when no token configured and auth not disabled",
    "CORS origin restricts to localhost",
    "CORS allows Authorization header",
    "response includes CORS origin header on OPTIONS (preflight)"
   ],
   "security/command-guard.test.ts": [
    "(a) localhost bypass: no block even on rm -rf /",
    "(b) localhost ::1 bypass",
    "(c) non-shell bypass: ai kind never blocked",
    "(d) remote rm_rf_root: matched",
    "(e) remote mkfs: matched",
    "(f) remote dd_of_dev: matched",
    "(g) remote fork_bomb: matched",
    "(h) remote authorized_keys_write: matched",
    "(i) remote benign command: no match",
    "(j) default-deny on host=undefined: matches enforced",
    "(k) formatCommandGuardDetail: matches I2 regex",
    "(l) LOCALHOST_HOSTS export includes 127.0.0.1 + ::1 + localhost"
   ],
   "security/docker-context-deck.test.ts": [
    "implements last-match-wins and directory/file matching semantics",
    "keeps Deckent state out while retaining Dockerfile build inputs",
    "skips both npm lockfile names without weakening other tracked-file scans"
   ],
   "security/lock-atomicity.test.ts": [
    "uses openSync with O_EXCL flag for atomic lock creation",
    "writes lock content to the file descriptor and closes it",
    "throws LockError when EEXIST occurs (race condition)",
    "throws LockError with generic message when re-read fails after EEXIST",
    "idempotent re-lock by same worker returns existing lock without O_EXCL",
    "throws LockError when existing lock is owned by different worker"
   ],
   "security/outbound-limiter.test.ts": [
    "(a) per-tenant isolation: tenant A exhausting does not affect tenant B",
    "(b) warn one-shot: first crossing of warn threshold emits warn, subsequent calls pass",
    "(c) kill threshold: bytesUsed >= quota triggers kill",
    "(d) window reset: after windowMs elapses, state rotates"
   ],
   "security/prompt-guard.test.ts": [
    "(a) base_blob: matches >=256 char base64 string",
    "(b) osc_escape: matches OSC sequence start",
    "(c) curl_pipe_shell: matches curl piped to shell",
    "(d) benign input returns empty array",
    "(e) formatGuardDetail: produces signal-only string matching ^[a-z_]+:[0-9]+(:[a-z_]+)?$",
    "(f) I2: no raw bytes in detail string"
   ],
   "security/shell-injection.test.ts": [
    "prompt content is never embedded in the tmux command string",
    "$() subshell syntax in prompt does not appear in command args",
    "backtick command substitution in prompt does not appear in command args",
    "${} variable expansion in prompt does not appear in command args",
    "combined dangerous metacharacters do not leak into command",
    "writePromptFile creates a file with the correct prompt content",
    "prompt file path includes unique random id",
    "deletes the prompt file at given path",
    "does not throw when file does not exist"
   ]
  },
  "skills": {
   "skills/builtin-skills.test.ts": [
    "skills directory exists",
    "all 10 skill directories exist",
    "all skill ids are unique",
    "every skill has both manifest.json and SKILL.md",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"language\"",
    "triggers is a non-empty array containing \"typescript\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection has correct structure",
    "composableWith is an array",
    "entrypoint points to SKILL.md",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"framework\"",
    "triggers is a non-empty array containing \"react\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection detects react dependency",
    "composableWith includes typescript-expert",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"language\"",
    "triggers is a non-empty array containing \"python\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection includes python config files",
    "composableWith is an array with related skills",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"domain\"",
    "triggers is a non-empty array containing \"api\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection detects web framework dependencies",
    "composableWith is an array",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"domain\"",
    "triggers is a non-empty array containing \"database\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection detects ORM dependencies",
    "composableWith is an array",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"workflow\"",
    "triggers is a non-empty array containing \"test\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection detects test framework dependencies",
    "composableWith is an array with language skills",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"workflow\"",
    "triggers is a non-empty array containing \"docs\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection references docs files",
    "composableWith is an array",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"domain\"",
    "triggers is a non-empty array containing \"security\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection has correct structure with empty arrays",
    "composableWith is an array",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"domain\"",
    "triggers is a non-empty array containing \"performance\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection has correct structure",
    "composableWith is an array with database-migration",
    "manifest.json is valid JSON with correct id",
    "has all required manifest fields",
    "category is \"tool\"",
    "triggers is a non-empty array containing \"docker\"",
    "SKILL.md exists and has >100 characters",
    "stackDetection detects Dockerfile and CI config",
    "composableWith is an array with security-specialist",
    "all manifests have valid category from allowed set",
    "all manifests have version in semver format",
    "all manifests have enabled set to true",
    "all manifests have stats with numeric fields",
    "all SKILL.md files contain a markdown heading",
    "no manifest has an empty name",
    "no manifest has an empty description",
    "composableWith references only valid skill ids",
    "no skill is composable with itself",
    "all manifests have promptInjection with position and maxTokens"
   ]
  },
  "smoke": {
   "smoke/verify-loop-smoke.test.ts": [
    "prompt contains tsc --noEmit command",
    "prompt contains npx vitest run command",
    "prompt contains CRITICAL VERIFY STEPS section",
    "prompt does NOT contain old \"run the project lint command\" text"
   ]
  },
  "training": {
   "training/cc-trace-extractor.test.ts": [
    "remaps core-4 to deckent native names; returns null for non-mappable",
    "aligned: remaps core-4, drops thinking, builds one example with the deckent system",
    "aligned EXCLUDES an example that uses a non-mappable tool; general KEEPS it (name as-is)",
    "segments multiple real-user turns into separate examples",
    "skips meta lines and malformed JSON without throwing"
   ],
   "training/corpus-lint.test.ts": [
    "reports ok:true with zero violations for well-formed, secret-free, non-trivial examples",
    "skips blank lines without counting them (mirrors pipeline.ts convention)",
    "flags malformed JSON lines as MALFORMED_JSON at the correct line number",
    "flags structurally invalid examples as SCHEMA_INVALID",
    "flags a missing conversations array as SCHEMA_INVALID",
    "catches an un-redacted sk- style API key in a conversation turn",
    "catches an un-redacted AWS AKIA access key in the system field",
    "catches an un-redacted GitHub ghp_ token",
    "catches an un-redacted JWT remnant",
    "does NOT flag an already-redacted example",
    "flags an example with an empty conversations array as EMPTY_EXAMPLE",
    "flags an example whose turns are all empty-string values as EMPTY_EXAMPLE",
    "flags a non-empty but sub-threshold example as TOO_SHORT_EXAMPLE using the default minChars",
    "respects a custom minChars option",
    "counts and fails closed on exact duplicate trainable examples",
    "treats examples with different content as unique",
    "accepts complete provenance and reports zero-weight duplicates as a retained statistic",
    "flags malformed structured provenance without treating an ordinary duplicate as a destructive fix",
    "processes lines one at a time via the injectable async iterable (no whole-file materialization)"
   ],
   "training/historical-trace-migration.test.ts": [
    "publishes a deterministic redacted projection, quarantines malformed input, and verifies idempotent replay",
    "makes dry-run side-effect free while producing the exact apply digests",
    "handles files larger than the old 16 MiB ceiling with bounded record processing",
    "detects cumulative prefix relations in linear trie order with stable first-writer authority",
    "records line-limit failures with exact evidence and continues later records",
    "projects causally incomplete legacy records as quarantined evidence instead of dropping them as malformed",
    "refuses path escape, source/output overlap, nested symlinks, and different existing migrations"
   ],
   "training/trn-pipe-label.test.ts": [
    "selfAssessment %s -> labels.outcome %s",
    "every known-case output is a valid RunOutcomeLabel per the taxonomy schema",
    "field name stays `outcome` (ShareGPT consumers) — never renamed",
    "unknown/legacy selfAssessment value passes through unchanged (not mapped)",
    "unknown/legacy selfAssessment value is never dishonestly folded into \"failed\"",
    "a real NO_GO still maps honestly to \"failed\" (unlike passthrough unknowns)"
   ],
   "training/trn4-pipeline.test.ts": [
    "maps system -> top-level system, user -> human, assistant -> gpt, tool -> observation",
    "emits only function_call turns (no empty gpt turn) when assistant content is empty",
    "preserves order across multiple tool_calls in one assistant turn",
    "keeps a malformed tool_call arguments string as-is rather than dropping the call",
    "enriches labels with outcome/agent/model when meta is present",
    "omits labels entirely when meta is absent (TRN-3 aligned/general shape)",
    "includes only the fields present on a partial meta (model is always required)",
    "leaves content at or under the policy limit unchanged",
    "truncates deterministically with a head+tail split and an omitted-count marker",
    "only compresses `observation` turns — human/gpt/function_call are left untouched even when huge",
    "reports truncated=false when nothing needed compression",
    "redacts secrets in turn values and in system",
    "reports redacted=false when nothing needed redaction",
    "catches a secret that straddles the truncation boundary — proves the PRE-compression pass is required",
    "exactly matches manually composing convert -> redact -> compress -> redact",
    "accepts a well-formed example (with and without optional fields)",
    "rejects malformed shapes",
    "parses a valid trace line, meta included when present",
    "omits the meta key when absent (TRN-3 shape)",
    "returns null for malformed JSON or a missing messages array, without throwing",
    "normalizes structured content and preserves canonical provenance only in the versioned projection",
    "returns a typed parse rejection reason",
    "streams trace lines into ShareGPT lines, skipping malformed input",
    "counts truncated and redacted examples in the summary",
    "processes a large number of lines without materializing the whole file (approximate streaming proof)",
    "isolates one conversion failure and reconciles structured counters without truncating following records",
    "surfaces sink write failures after closing the sink",
    "reads a real input file via node:readline and writes a real output file via node:fs, streaming (no injected I/O)",
    "verifies migration authority and projects only train-ready canonical envelopes with tool causality",
    "refuses absent/tampered canonical authority and never clobbers an existing corpus"
   ]
  },
  "unit": {
   "unit/heartbeat-daemon.test.ts": [
    "should parse and execute pending tasks from HEARTBEAT.md",
    "should handle failing commands and log failures",
    "should create HEARTBEAT.md with default template if missing",
    "should skip already completed tasks",
    "should start, run immediate heartbeat, and set interval",
    "should stop cleanly and remove PID file",
    "should return null when PID file does not exist",
    "should return null for invalid PID content"
   ],
   "unit/promotion-pipeline.test.ts": [
    "should reject promotion of built-in agents",
    "should promote agent from persistent temp pool",
    "should return false when temp entity directory is not found",
    "should reject demotion of built-in agents",
    "should demote a non-built-in agent by setting enabled=false",
    "should return false when manifest file does not exist",
    "should recommend promotion when criteria are met",
    "should recommend wait when task count is insufficient",
    "should skip built-in agents in promotion evaluation",
    "should recommend demotion when fail rate exceeds threshold"
   ],
   "unit/spawn-backend-docker.test.ts": [
    "should throw SpawnBackendError when Docker image is not found",
    "image-missing failure carries the distinct IMAGE_NOT_FOUND code (not a daemon/CLI collapse)",
    "daemon permission-denied preflight is NOT reported as image-missing (distinct code)",
    "daemon-unavailable preflight surfaces E085 with evidence, never image-missing",
    "should throw a SpawnBackendError instance with correct backendName",
    "returns null for a healthy daemon (status 0)",
    "classifies a permission-denied socket as DAEMON_PERMISSION (E086)",
    "classifies an unreachable daemon as DAEMON_UNAVAILABLE (E085)",
    "classifies an absent docker binary (ENOENT) as DOCKER_ABSENT (E087)",
    "classifies status 127 as DOCKER_ABSENT (command not found)",
    "permission-denied is distinguished from image-missing — the three classes never share a code",
    "should use docker stop --time=15 for graceful shutdown",
    "should fallback to docker kill when docker stop fails",
    "should handle kill errors gracefully",
    "should return true when docker info succeeds",
    "should return false when docker info fails",
    "should return empty array initially",
    "should set backend name to docker",
    "should accept custom image and timeout options",
    "should return true when docker info exits with 0",
    "should return false when docker info exits with non-zero"
   ],
   "unit/sprint-utils.test.ts": [
    "should identify source code directories",
    "should identify non-source directories",
    "should return true when all directories are non-source",
    "should return false when any directory is source code",
    "should return false when directories are empty",
    "should handle missing scope gracefully",
    "should return true for files older than maxAgeMs",
    "should return false for recent files",
    "should return false when file does not exist",
    "should return true for claude provider",
    "should return false for other providers",
    "should return numeric max_workers directly",
    "should resolve auto to system recommendation",
    "should return a valid ISO 8601 string",
    "should suggest model downgrade on rate limit errors",
    "should suggest tmux doctor on session errors",
    "should warn about high task count",
    "should extract proof lines as specific criteria",
    "should use base criteria when no proof lines found",
    "does not duplicate test execution authority into acceptance criteria",
    "WP-13: strips **Kanıt:** (colon inside bold) without mangling to *Kanıt:**",
    "WP-13: strips **Test:** prefix cleanly",
    "WP-13: strips bulleted **Proof:** prefix cleanly",
    "F0.2: recognizes `- goCriteria:` as a task-specific GO criterion",
    "F0.2: recognizes `- nogo:` and surfaces it in noGoCriteria",
    "F0.2: nogo-only description still captures the prohibition (no proof lines)",
    "F0.2: WM-7 kind path composes goCriteria/nogo onto the kind-aware base",
    "keeps an escaped semicolon inside one explicit criterion item",
    "persists debt injection holds without resolving or dropping them",
    "preserves the producer-to-reader distinction between known empty and legacy absent holds",
    "fails loudly for malformed persisted debt hold evidence",
    "should read sprint state from disk",
    "should return null when no state file exists",
    "should clear sprint state file",
    "should construct correct log path",
    "should return false for non-existent log"
   ]
  },
  "workflows": {
   "workflows/cross-platform-e2e.test.ts": [
    "should exist",
    "keys every root npm cache explicitly from npm-shrinkwrap.json",
    "still defines the e2e job with its original matrix",
    "still excludes macos + subprocess",
    "still installs tmux per-OS and runs the original E2E test command",
    "still builds via plain `npm run build` (not build:all) — original job untouched",
    "defines the active Linux installed-native proof job",
    "does not promote macOS or Windows-native through the active package job",
    "builds, validates and invokes the fresh-cache networkless verifier unconditionally",
    "keys the packed-install cache from root shrinkwrap plus the independent dashboard lock",
    "does not hide active Linux proof failure behind an advisory job setting",
    "keeps capability probes measurement-only and native source checks non-promotional",
    "uses Node.js 24",
    "documents the deliberate WSL-gap honestly instead of adding a fragile WSL step",
    "does not add a needs: dependency on the pre-existing e2e job (independent job)"
   ],
   "workflows/dependency-audit-gate.test.ts": [
    "path 1 — clean: no findings → PASS, exit 0",
    "path 2 — high-severity finding, no exception → FAIL, exit 1",
    "path 3 — high-severity finding covered by a valid, unexpired exception → PASS, exit 0, exception named in report",
    "path 4 — matching exception but EXPIRED → still FAIL, exit 1, expiry named in report",
    "path 5a — audit could not run (npm error JSON, e.g. network failure) → fail-closed, exit 1, never \"PASS\"",
    "path 5b — audit could not run (unparseable/empty stdout) → fail-closed, exit 1, never \"PASS\"",
    "path 5c — spawn itself throws → fail-closed, exit 1",
    "validateExceptionRecord rejects a record missing \"owner\"",
    "validateExceptionRecord rejects a record missing \"reason\"",
    "validateExceptionRecord rejects a record missing \"expires\"",
    "validateExceptionRecord rejects an invalid (non-ISO) expires date",
    "validateExceptionRecord accepts a fully-populated record",
    "a malformed exception entry does not silently grant coverage — reported as invalid, finding still fails",
    "returns true for a past expiry",
    "returns false for a future expiry",
    "a missing exceptions file is treated as \"no exceptions\", not an error",
    "a non-array JSON root is rejected wholesale",
    "is a valid array with zero invalid entries — currently EMPTY (DEP669A+B closed all advisories)"
   ],
   "workflows/publish.test.ts": [
    "should exist",
    "should have release:published trigger event",
    "should have tag-based push trigger (v*)",
    "is validation-only with read-only permissions and no OIDC publish grant",
    "should use Node.js 24.x",
    "should have npm ci step",
    "should have build step",
    "should have test step",
    "is dry-run-only — the REAL publish lives solely in release.yml (born-608)",
    "should have dry-run step with --access public",
    "keeps the dry-run verify step with --access public",
    "carries zero npm-auth-secret references (SEC-06) — dry-run needs no registry auth",
    "should have registry-url set to npmjs.org",
    "should run on ubuntu-latest",
    "should have checkout step pinned to an immutable commit SHA (SEC-06)",
    "should have setup-node step pinned to an immutable commit SHA (SEC-06)",
    "should have cache enabled for npm",
    "should key the npm cache explicitly from the canonical root shrinkwrap",
    "should have type check step",
    "should exist",
    "should have tag-based push trigger (v*)",
    "is validation-only — read-only permissions, no repo-write grant",
    "performs no real npm publish (manual-publish owner contract)"
   ]
  }
 }
}
