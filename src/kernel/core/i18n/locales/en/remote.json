{
  "api.approval.authority_unavailable": "Attended execution approval authority is unavailable.",
  "api.approval.decision_failed": "Approval decision failed: {error}",
  "api.approval.decision_rejected": "Approval decision rejected: {reason}",
  "api.approval.fresh_oidc_required": "Fresh OIDC step-up authentication is required.",
  "api.approval.idempotency_required": "Idempotency-Key header is required.",
  "api.approval.request_expired": "Approval request expired.",
  "api.approvals.already_decided": "Approval already {category}",
  "api.approvals.api_decide_disabled": "Approval API decisions are disabled — set approval.api_decide: true in .deckent/config.json to enable POST /api/approvals/:id/decision",
  "api.approvals.invalid_id": "Invalid approval id",
  "api.approvals.not_found": "Approval not found",
  "bot.action_done": "✅ Executed {tool}:",
  "bot.action_expired": "⏲️ Expired — {tool} was not executed (the approval was too old). Ask again if you still want it.",
  "bot.action_failed": "⚠️ {tool} failed: {error}",
  "bot.action_rejected": "❌ Rejected — {tool} was not executed.",
  "bot.action_run_changed": "🛡️ Not executed — {tool} was tied to run {run}, which is no longer the active run. Refusing so a stale command can't hit a different run.",
  "bot.approve_ack": "✅ Approved: {id}",
  "bot.approve_ack_ctx": "✅ Approved: {id} — {what}",
  "bot.chat_empty": "(no response)",
  "bot.chat_error": "⚠️ Could not process that message — try again.",
  "bot.chat_thinking": "💭 thinking…",
  "bot.daemon_already": "ℹ️ Bot daemon is already running (pid {pid}).",
  "bot.daemon_desc": "Run the bot listener as a background daemon",
  "bot.daemon_not_running": "Bot daemon is not running.",
  "bot.daemon_ownership_unknown": "⛔ Bot process ownership cannot be proven (pid {pid}, reason {reason}); no signal or new daemon was issued.",
  "bot.daemon_pid_record_failed": "⚠️ Bot listener started, but its process ownership record could not be claimed. The listener was stopped safely.",
  "bot.daemon_reboot_note": "Note: a daemon does NOT survive a reboot/crash — use a systemd/pm2 service for that.",
  "bot.daemon_spawn_failed": "⚠️ Failed to start the bot daemon.",
  "bot.daemon_started": "🟢 Bot daemon started (pid {pid}). Always-on while this machine is up. Stop with: deckent bot stop",
  "bot.daemon_status_running": "🟢 Bot daemon is running (pid {pid}).",
  "bot.daemon_stopped": "🛑 Bot daemon stopped (pid {pid}).",
  "bot.group_desc": "Messaging-connector bot — listen/start/stop/status for inbound approve/reject",
  "bot.help_body": "🤖 deckent bot — commands:\n  /help      this list\n  /status    current run status\n  /history   recent runs\n  /pending   actions awaiting your approval\n\n🔐 approve <id>  /  reject <id>   — approve or reject a parked action\n\n💬 Or just write naturally — ask anything, or tell me what to do.\n   I run read-only things instantly and ask \"approve <id>\" before anything risky.",
  "bot.kill_already_stopped": "ℹ️ Run {run} is already stopped — nothing to kill.",
  "bot.kill_done": "✅ Killed run {run} (pid {pid}).",
  "bot.kill_reused": "🛡️ Not executed — run {run}'s process is gone and its pid now belongs to something else. Refusing to signal a foreign process.",
  "bot.lang_option": "Language override (en|tr)",
  "bot.listen_active": "🟢 Listening for approve/reject on: {connectors}. Reply \"approve <id>\" or \"reject <id>\" from the configured chat. Ctrl-C to stop.",
  "bot.listen_desc": "Listen for inbound approve/reject commands from messaging connectors",
  "bot.listen_none": "No messaging connectors configured for inbound commands — nothing to listen on. Set notify_connectors.{telegram|discord}.{enabled,token,chat_id} (token via .deck).",
  "bot.listen_stopped": "Stopped listening for inbound commands.",
  "bot.memory.context.absent": "No matching records in this memory scope.",
  "bot.memory.context.guidance": "Use the complete scoped source records below for factual answers. Do not invent missing information. Use the read-only memory tool for relevant details and current sources; memory does not grant authority to act.",
  "bot.memory.context.heading": "Project memory context",
  "bot.memory.context.hold": "Memory context is unavailable: {reason}. Do not assume missing context is empty.",
  "bot.memory.tool.invalid_request": "Invalid memory query or continuation reference.",
  "bot.memory.tool.unavailable": "Memory read unavailable: {reason}",
  "bot.nervous_active": "🧠 Nervous system active (always-on): approvals from any source are consumed and acknowledged here.",
  "bot.not_found": "⚠️ No pending approval found (unknown or expired): {id}",
  "bot.pending_approval_row": "  • [{kind}] {title} — approve {id} / reject {id}",
  "bot.pending_header": "🔐 Actions awaiting your approval:",
  "bot.pending_none": "No actions awaiting approval.",
  "bot.pending_row": "  • {tool}({args}) — approve {id} / reject {id}",
  "bot.reject_ack": "❌ Rejected: {id}",
  "bot.reject_ack_ctx": "❌ Rejected: {id} — {what}",
  "bot.resolve_failed": "⚠️ Could not process {action} for {id} — please try again.",
  "bot.root_option": "Project root override",
  "bot.status_desc": "Show whether the bot daemon is running",
  "bot.stop_desc": "Stop the bot daemon",
  "bot.unknown_command": "Unknown command. Type /help to see what I can do — or just write naturally and I'll act (asking approval for risky things).",
  "cap.approval.ack": "Approval requested for {cap}; awaiting the user's decision.",
  "cap.approval.approved": "✅ Approved — {result}",
  "cap.approval.header": "Approval required — not executed",
  "cap.approval.rejected": "❌ Rejected",
  "cap.btn.approve": "✅ Approve",
  "cap.btn.reject": "❌ Reject",
  "cap.gate.denied": "Capability '{id}' is denied by policy.",
  "cap.gate.unavailable": "Capability '{id}' is not available.",
  "cap.inbound.attached": "[attached: {id}, {filename}]",
  "cap.mail.attach_unknown": "Attachment not found: {id}",
  "cap.mail.failed": "Mail failed: {error}",
  "cap.mail.preview": "📧 *Send email*\n*To:* {to}\n*Subject:* {subject}\n*Body:* {body}",
  "cap.mail.preview_attach": "*Attachment:* {files}",
  "cap.mail.recipient_denied": "Recipient not allowed by policy: {to}",
  "cap.mail.sent": "Mail sent to {to} · {subject} ({id})",
  "cap.mail.smtp_missing": "SMTP is not configured in .deck.",
  "cap.mail.title": "Send email",
  "cap.media.fallback": "[media: {filename} — this connector cannot display it]",
  "cap.screenshot.caption": "Screen capture",
  "cap.screenshot.failed": "Screenshot failed: {error}",
  "cap.screenshot.preview": "capture {display} display",
  "cap.screenshot.title": "Screenshot",
  "cap.screenshot.unsupported": "Screenshot is not supported on this platform.",
  "connect.auth_state.hint": "Set {envKey} (e.g. `{cmd}`), or add {deckKey} to your .deck file and reference it as $DECK:{deckKey} in config.",
  "connect.step.attach_mcp": "Attach deckent to {host} via MCP.",
  "connect.step.ide_cursor_setup": "Set up the Cursor IDE integration.",
  "connect.step.ide_terminal_guidance": "Running in a plain terminal — no IDE integration needed.",
  "connect.step.install_cli": "Install the {provider} CLI: {instruction}",
  "connect.step.login": "Log in to {provider}.",
  "connect.step.mcp_unsupported": "{host} does not support MCP attachment yet.",
  "connect.step.wsl_recommended": "WSL is recommended over {shell} for the best experience.",
  "gateway.bound_ok": "Bound to {project}. Your messages now go to this project.",
  "gateway.daemon_already": "Gateway daemon already running (PID: {pid}).",
  "gateway.daemon_not_running": "Gateway daemon is not running.",
  "gateway.daemon_reboot_note": "Note: Daemon does not survive reboot automatically — set up an OS supervisor (systemd/pm2).",
  "gateway.daemon_started": "Gateway daemon started (PID: {pid}). Continues after terminal close.",
  "gateway.daemon_status_running": "Gateway daemon running (PID: {pid}).",
  "gateway.daemon_stopped": "Gateway daemon stopped (PID: {pid}).",
  "gateway.group_desc": "Manage project-scoped messaging gateway sessions and pairing",
  "gateway.listen_active": "Gateway listener active: {connectors}",
  "gateway.listen_none": "No connectors active — gateway listener not started.",
  "gateway.not_bound": "Not bound to anything.",
  "gateway.pair_approved": "Pairing approved: {chatKey} → {project}",
  "gateway.pair_list_empty": "No pending pairings.",
  "gateway.pair_list_row": "• {code} — {chatKey} ({requestedAt})",
  "gateway.pair_needed": "This chat is not authorized for {project}. Pairing code: {code}. Ask the owner to run: deckent gateway pair approve {code} {project}",
  "gateway.pair_rejected": "Pairing rejected: {code}",
  "gateway.pair_unknown_code": "Unknown pairing code: {code}",
  "gateway.pair_usage": "Usage: deckent gateway pair approve <code> <project> | reject <code> | list",
  "gateway.projects_header": "Registered projects:",
  "gateway.projects_row": "• {name} — {path}",
  "gateway.runtime_desc": "Internal: per-project runtime child (spawned by the supervisor; not for direct use)",
  "gateway.unbind_ok": "Unbound. Use `/use <name>` to bind again.",
  "gateway.unbound": "This chat is not bound to a project. Use `/projects` to list, `/use <name>` to bind.",
  "gateway.use_unknown": "Unknown project: {name}. List with `/projects`.",
  "gateway.use_usage": "Usage: /use <project-name or path>",
  "gateway.whoami": "Bound project: {project}",
  "local_llm.cmd_desc": "Manage the project-scoped local LLM runtime",
  "local_llm.start_desc": "Start the configured local LLM server",
  "local_llm.status_desc": "Inspect local LLM health and advertised models",
  "local_llm.stop_desc": "Stop the project-scoped local LLM server",
  "mcp.agent_list.detail": "Serves the same read model `deckent agent list` renders, so both surfaces always agree. Each record carries the four catalog facets kept separate: enabled (owner intent), routable (dispatchable now, with typed reasons), validity (schema conformance) and provenance (declared source, observed layer, resolved path). Records the resolver rejected are reported as validity \"invalid\" with the resolver diagnostics rather than silently dropped; archived records are never listed.",
  "mcp.agent_manage.desc": "Manage the agent pool: add a custom agent, remove one, or promote a temp agent (generated for a run under .tasks/agents/) into the persistent pool at .deckent/agents/temp-<id>/. Uses the existing AgentPoolManager API — no new agent lifecycle concept. See also deckent_agent_list for read-only listing.",
  "mcp.analyze_project.detail": "Detects language, framework, test framework, build tool, CI system, project size (by file count) and a methodology recommendation, then returns config suggestions such as plan mode and worker count. Useful before init to pick the right configuration, or to verify stack detection. Modifies no files.",
  "mcp.approvals.detail": "Serves the canonical ApprovalBroker read model — the SAME source as the CLI — and returns each pending request id, summary and expiry. READ-ONLY: this surface never decides, allows or denies. Deciding stays CLI-only behind an interactive live-authenticated TTY, so there is no self-approval path over MCP.",
  "mcp.audit.detail": "action=\"gate\" (default) runs the Brain Self-Audit Gate for a run — tsc, vitest, honesty and observability checks — returning PASS or GATE_FAILURE and writing .deckent/<runId>-gate.json. action=\"query\" filters audit-log events by channel/tenant with an optional limit. action=\"compliance\" builds a compliance report (audit-chain integrity, RBAC, tenant isolation) over the retained trail. action=\"retention\" plans retention via keepDays/keepCount and is dry-run by default with ZERO writes; apply=true is DESTRUCTIVE — it archives the planned partition and permanently deletes pruned events from the run event stream. The CLI \"forward\" subcommand (SIEM export) is intentionally not exposed over MCP because it requires network egress.",
  "mcp.autonomous.detail": "Query status, start or stop the loop, manage the backlog (add/list/remove) and resolve approval gates (pending/approve/reject). \"start\" spawns the real loop as a DETACHED background process and never blocks this MCP stdio transport — it reports spawned=false with an honest reason instead of spawning when autonomous.enabled is not true in project config, or when a previously spawned loop is still alive.",
  "mcp.autonomous_approve.detail": "Targets a backlog entry parked by the G2/G3 policy gate as `policy: approval-required`, or any other parked trigger id. Exec-free: records the accept decision to .deckent/autonomous/{pending.json,decisions.json} through the approval-adapter public API; the running loop replays the trigger on its next cycle. Nothing is executed here. See also deckent_autonomous_reject and deckent_autonomous action=pending.",
  "mcp.autonomous_backlog.detail": "Operates on .deckent/autonomous/backlog.json: list entries, add a one-off or recurring task entry, or remove an entry by id. Talks directly to the orchestra/autonomous/backlog.ts durable store (loadBacklog / validateBacklogEntry / atomic write) with no cli/ layer dependency (ADR-D-004 C3). See also deckent_autonomous_status and deckent_autonomous.",
  "mcp.autonomous_reject.detail": "Targets a backlog entry parked by the G2/G3 policy gate as `policy: approval-required`, or any other parked trigger id. Exec-free: records the reject decision to .deckent/autonomous/{pending.json,decisions.json} through the approval-adapter public API; the running loop then never replays the trigger. Nothing is executed here. See also deckent_autonomous_approve and deckent_autonomous action=pending.",
  "mcp.autonomous_status.detail": "Read-only: backlog totals by status, stop-marker presence and the pending-approval count, read straight from .deckent/autonomous/{backlog.json,stop,pending.json} with no engine process contact — the same stateless shape as deckent_autonomous action=status. See also deckent_autonomous_backlog for backlog mutation.",
  "mcp.checkpoint.detail": "Lists checkpoint gates (read-only). Approve and reject stay on the CLI behind interactive live-auth; MCP returns NOT_A_DECISION_SURFACE for those actions.",
  "mcp.checkpoint.not_a_decision_surface": "Checkpoint approve/reject is not available on MCP. Use an interactive terminal: {command}",
  "mcp.cleanup.detail": "Deletes every task file (.json, .plan, .hb, .result, .paused, .log) from .tasks/ and every lock file from .locks/. With decay=true it also runs memory decay on .brain/ files that exceed the line budget (trimming MEMORY.md, RETRO.md and run logs). Use dryRun=true first to preview exactly what would be deleted. Typically run after deckent_review, or before a fresh run following a kill.",
  "mcp.config.detail": "Three actions against .deckent/config.json: \"read\" returns the fully resolved config (3-layer merge of defaults + global + project); \"get\" returns one key by dot-notation (e.g. \"brain_provider\", \"max_workers\"); \"set\" writes a key/value pair with validation. Common keys: brain_provider, worker_provider, max_workers, mode, routing_engine.",
  "mcp.cost.detail": "Returns budget limits, per-model pricing (input/output per MTok) and today's spend from the resource log. Delegates to the project cost-config single source; read-only, with no cost math reimplemented here.",
  "mcp.docs.detail": "Actions: \"add\" registers a file, \"remove\" unregisters it, \"list\" shows all, \"update\" modifies section rules, \"run\" triggers doc updates without a run, \"track-scan\" performs a DB-only doc-tracking scan (hash + DCR + stale) and \"track-status\" lists tracked doc health. Auto sections receive generated content (metrics, debt, history); protected sections are never touched.",
  "mcp.doctor.detail": "Checks Node.js version, git availability, tmux installation, provider CLI auth, workspace directories (.deckent/, .brain/, .tasks/), brain memory budget, tech-debt level and stale lock files. Returns a healthScore (0-100) with per-check pass/fail status and recommendations. Use it when a run fails unexpectedly, or before starting a new one — fix what it reports, then re-run until healthScore reaches 100.",
  "mcp.execution_authority.detail": "On this surface the reconcile path is limited to namespace-local Linux/WSL mount metadata and never changes execution authority itself. Dry-run by default; pass apply=true to write the reconciled mount metadata.",
  "mcp.explain.detail": "Reads the run log from .brain/runs/ and the retrospective from the Memory V2 DB to summarise goal, task outcomes (completed/failed/tech debt), duration and key learnings. Use it right after a run for a quick overview. Supports a specific run lookup, verbose mode for full detail and JSON output.",
  "mcp.feature_query.detail": "Lists features by category (active, lightly_used, dormant, dead, all) or looks one up by ID, returning metadata including files, description and category. Reads .deckent/settings/features-manifest.json; regenerate it with `node scripts/sync-manifest.mjs`.",
  "mcp.help.detail": "Returns version, initialization state, run status, agent/skill counts, routing engine, available workflows, a recommended next action and the full tool + resource catalog. Use it when you are unsure what to do next or want to understand Deckent's capabilities.",
  "mcp.history.detail": "Reads archived run logs from .brain/runs/: the last N run markdown logs sorted by run ID, plus a trend analysis (improving/declining/stable) over task completion rates. Use it to understand long-term project health, compare run performance or review past decisions.",
  "mcp.init.detail": "Creates every required directory (.deckent/, .brain/, .tasks/, .locks/, .claude/rules/) and configuration file (config.json, DECKENT.md, DIRECTIVES.md, brain files). Safe to re-run — existing config fields are preserved via merge and files are written only when missing. Next: deckent_set_directives → deckent_plan → deckent_start.",
  "mcp.inspect.detail": "Serves the exact projections the `deckent inspect --json` face serves. Without arguments: the run listing — the current run from the run-status authority plus archived runs from settlement records. With taskId: the task drill-down (task json, plan truncation flag, result evidence, heartbeat, lineage). Read-only; lifecycle always comes from the run-status authority and is never re-inferred here.",
  "mcp.kill.detail": "Stops one or all running workers: sets task status to PAUSED, removes heartbeat files and releases every file lock the task owns. Use it for a stuck worker (stale heartbeat), a resource hog or a restart. Afterwards run deckent_cleanup to remove task artifacts, then deckent_start. force and userExplicit are pass-through panic-guard bypass markers — even when both are set the bypass is only recorded in the audit trail, and kill still requires explicit user intent.",
  "mcp.kpi.detail": "Scorecard (default) returns { runId, kpis } with cost, token, cache, retry, completion and quality metrics. With the trend argument it returns { kpiId, series: [{ periodKey, value, status }] }. Delegates to KpiService as the single source; read-only.",
  "mcp.memory_manage.desc": "Manage project memory: insert a new entry, update fields on an existing entry, or trigger decay (soft-deleting entries older than the retention window). Uses the existing MemoryStore public API directly against .brain/memory.db. See also deckent_memory_query for read-only search.",
  "mcp.memory_query.detail": "Use it when you need context about a past decision or how something was done. Supports full-text search with Turkish normalisation, type and status filters, and a run range.",
  "mcp.models.detail": "Actions: \"list\" lists available models (optionally filtered by provider); \"refresh\" force-refreshes the catalog from models.dev and invalidates the 24h cache; \"tier\" looks up a model's tier (premium_plus/premium/standard/economy). Catalog sources: remote (models.dev live), cache (~/.deckent/cache/models-catalog.json, 24h TTL) and bundled (offline fallback).",
  "mcp.nervous_accept.detail": "The action is then executed by the Executor. An id of the form \"panic:<taskId>\" approves a PanicGuard-blocked kill.",
  "mcp.nervous_config.detail": "Reads or modifies the authority mode preset and the per-action overrides, and lists the available actions.",
  "mcp.nervous_edit.detail": "Builds an accept-with-edited-payload PLAN for a pending Nervous System notification and returns the plan only — nothing is executed here; applying it is a separate injectable step (nervous-bridge.ts applyNervousBridgePlan).",
  "mcp.nervous_reject.detail": "The action will NOT be executed. A reason may optionally be supplied.",
  "mcp.nervous_status.desc": "Show the Nervous System dashboard: pending notifications, recent history and the current configuration.",
  "mcp.nervous_subscribe.desc": "Subscribe to Nervous System notifications for the current run. Registers this MCP client for push notifications and surfaces currently pending PanicGuard kill approvals as PANIC_GUARD_KILL_PENDING events.",
  "mcp.nervous_undo.detail": "Builds an undo PLAN for the most recent reversible accepted action, or for a specific record id, and returns { supported: false, reason } when nothing undoable is found. Plan-only — never mutates the audit trail.",
  "mcp.plan.detail": "Reads DIRECTIVES.md, analyses the task blocks and returns the proposed task list with model assignments, wave breakdown and risk assessment — without executing anything. Use it to validate directives before deckent_start. Prerequisite: deckent_init + deckent_set_directives.",
  "mcp.process.detail": "action=submit injects an ExecutionRequest that is policy-gated — read-only capabilities auto-run while side-effecting ones park for approval; action=status and action=result poll a prior submission by executionId.",
  "mcp.recover.detail": "Runs the audit, cleans orphan IPC directories (dead PIDs only), clears stale locks (>5 min) and archives terminal task files while preserving active tasks. Use dryRun=true to preview first. DESTRUCTIVE: it modifies .tasks/, .locks/ and .deckent/.",
  "mcp.retro.detail": "Reads the retrospective from the Memory V2 DB (.brain/memory.db `retro` entries): full content (run ID, task outcomes, GO/NO_GO decisions, learnings, agent performance notes) plus up to 5 extracted highlights. Every run keeps its own retro entry — pass runId for an older one.",
  "mcp.review.detail": "For each task returns selfAssessment (the worker's own DONE/GO_WITH_TECH_DEBT/NO_GO), testsPassed, filesChanged, notes and the decision (approved/rejected/pending). GO means complete with passing tests, NO_GO means rework is needed, GO_WITH_TECH_DEBT means done with known follow-up. Use auto=true to approve every DONE task whose tests passed.",
  "mcp.run.detail": "Creates a task JSON file and spawns a worker immediately, returning a jobId for tracking — no PLAN/EVALUATE/RETRO phases. Use it for a quick isolated task such as fixing one bug, writing a single test file or updating a doc, and monitor the spawned worker with deckent_status.",
  "mcp.set_directives.detail": "The brain engine parses \"## Task N:\" or \"## Görev N:\" blocks into run tasks. Each block should carry: Model (an exact provider API ID, e.g. {modelId} — see deckent_models for the live catalog; legacy aliases [{legacyAliases}] are rejected), optional Provider (explicit ownership: {providers}, required when it cannot be inferred from the id prefix), Effort (low/normal/high), Skills, Files, Scope and Description. Prerequisite: deckent_init. Overwrites DIRECTIVES.md on every call; run deckent_plan afterwards to preview the tasks.",
  "mcp.skill_list.detail": "Returns id, name, category and trigger keywords for each skill, read from the .deckent/skills/ directory. Use it to see which skills are available for task routing, check skill coverage or audit skill assignments before planning a run.",
  "mcp.skill_manage.desc": "Manage the skill pool: add a custom skill, remove one, or list the skills available in the marketplace registry (falling back to the local .deckent/skills/ listing when the registry is unreachable). Uses the existing SkillPoolManager and RegistryClient APIs. See also deckent_skill_list for read-only local listing.",
  "mcp.start.acknowledge_cost_desc": "Acknowledge a numeric over-budget estimate. Unknown pricing or an unavailable gate still blocks. Equivalent to CLI --force from the cost-gate perspective.",
  "mcp.start.acknowledge_prompt_gate_desc": "Bypass the plan-time G-series prompt gate BLOCK (persona-capability / decision-space / scope-contract findings — born-628). By default a run halts at PLAN when a task's finalized (persona × intent) fit fails a hard lint. Set true to allow such tasks to spawn anyway. Equivalent to CLI --force-prompt-gate; independent of acknowledgeCost/force/acknowledgeScopePaths.",
  "mcp.start.acknowledge_scope_paths_desc": "Bypass the pre-spawn SCOPE gate (Dimension B). By default a run is blocked before spawn when a task's filesWrite path does not exist and looks like a typo/wrong-directory (an orphan-file mode). Set true to allow such paths as intentional new files. Equivalent to CLI --force-scope; independent of acknowledgeCost/force.",
  "mcp.start.auto_approve_desc": "Auto-approve worker tool calls with --dangerously-skip-permissions. CLI default is false; set true only when the caller has confirmed the run is safe under the CLI/MCP parity contract.",
  "mcp.start.cost_limit_warning_title": "Cost limit warning",
  "mcp.start.detail": "Runs the complete lifecycle in the background: PLAN → SPAWN → EXECUTE → EVALUATE → FIX → RETRO → DECAY → COMPLETE. Pre-spawn cost admission always runs: acknowledgeCost=true or force=true may acknowledge a numeric budget overrun but can never override unknown pricing or an unavailable gate. Returns immediately with a jobId while the run continues asynchronously — poll deckent_status and evaluate with deckent_review. Prerequisite: deckent_init + deckent_set_directives.",
  "mcp.start.dry_run_complete": "Dry-run complete. No workers spawned. Review tasks, then call deckent_start without dryRun to execute.",
  "mcp.start.dry_run_desc": "Plan the run without spawning workers. Returns the planned tasks list so you can review before committing. No workers are started, no files are changed.",
  "mcp.start.estimated_duration_fallback": "~10-30 minutes",
  "mcp.start.flow_id_desc": "TERM-FLOW-UNIFY (426-001): consume an approved RunFlow snapshot instead of planning fresh — requires revision, planDigest and config.terminal.run_flow_v2=true. Must be supplied together with revision + planDigest.",
  "mcp.start.flow_params_incomplete": "flowId, revision and planDigest must be supplied together.",
  "mcp.start.flow_v2_disabled": "flowId requires config.terminal.run_flow_v2 = true.",
  "mcp.start.force_desc": "Skip the run-lock pre-flight and acknowledge a numeric cost overrun. Unknown pricing or an unavailable cost gate still blocks. Equivalent to CLI --force.",
  "mcp.start.lock_already_running": "Run already running (PID {pid}, env: {env}, run: {runId}, started: {acquiredAt}). Use force=true to override.",
  "mcp.start.phase_unknown": "unknown",
  "mcp.start.plan_digest_desc": "RunFlow planDigest to CAS-verify against the approved snapshot (used with flowId).",
  "mcp.start.revision_desc": "RunFlow proposal revision to CAS-verify against the approved snapshot (used with flowId).",
  "mcp.start.run_failed_at_phase": "Run failed at phase {phase}: {message}",
  "mcp.start.run_started": "Run started in background. Use deckent_status to track progress.",
  "mcp.start.sandbox_desc": "Run in sandbox mode: stashes local git changes before spawning and restores them after the run completes. Safe experimentation — no permanent changes on failure.",
  "mcp.start.timeout_desc": "Run maximum duration in milliseconds (default: 30 minutes = 1800000). Run is marked TIMEOUT if workers do not complete within this window.",
  "mcp.start.title": "Start Run",
  "mcp.status.detail": "Returns agents (active workers with task assignments), progress (done/total, progress bar, ETA), alerts (stale workers, boundary violations, lock issues), job (background job state RUNNING/COMPLETE/FAILED plus runId and metrics), agentAssignments and skillAssignments. Safe to call at any time and to poll repeatedly; no prerequisite.",
  "mcp.sync.detail": "Ensures the AI adapter files (CLAUDE.md, AGENTS.md) import DECKENT.md as the single source of truth. Additive only — prepends the @DECKENT.md reference when missing and never overwrites existing content. Use it when an adapter file loses its Deckent reference after a manual edit or a merge conflict. Requires DECKENT.md to exist (run deckent_init first).",
  "mcp.truth.detail": "Covers every truth-block declared in .deckent/settings/features-manifest.json and flags half-wire candidates (code shipped but no production call-site). Read-only. Pass check=true to also diff those candidates against the pinned .deckent/truth-baseline.json ratchet.",
  "mcp.usage.detail": "Default: a last-7-day model-level summary (calls, tokens, limit-cost, cache hit%). With run: the per-task breakdown plus the cache-gate report for that run. With lineage: the canonical logical-root usage/billing aggregate (the same authority as core/lineage-usage-authority.ts) projected for caller-supplied tasks and attempts — no MCP-side billing recalculation.",
  "mcp.watch.detail": "On this surface the stream is delivered as MCP logging notifications: an initial backfill of recent events followed by new events pushed as they arrive. Auto-unsubscribes on client disconnect or error.",
  "mcp.writer_lease.denied": "Write tool '{tool}' is held by another deckent window (pid {pid}). Read tools work here; mutations run in that window — the lease transfers automatically when it exits.",
  "mcp.xverify.detail": "Both interactive sessions can call the advisory referee in-band: the verifier is always chosen to DIFFER from the declared author. Provider output is evidence only — the host derives CONFIRMED/REFUTED/UNCLEAR plus the authoritative ALLOW/NO-GO/HOLD disposition.",
  "openrouter_probe.fetch_failed": "OpenRouter live fetch failed: {reason}",
  "openrouter_probe.header": "OpenRouter Live Probe",
  "openrouter_probe.model_line": "  - {id} ({context} ctx, {modality})",
  "openrouter_probe.more": "  … and {count} more",
  "openrouter_probe.summary": "{count} free model(s) found — cache written to {cacheFile}",
  "openrouter_probe.unavailable": "OpenRouter probe unavailable: {reason}",
  "tool.approval.ack": "Approval requested for {tool} — tap Approve/Reject on the message above; nothing has run yet.",
  "tool.confirm_bash": "Run command: {cmd}",
  "tool.confirm_edit": "Edit file: {path}",
  "tool.confirm_git_add": "Stage changes: {paths}",
  "tool.confirm_git_commit": "Commit: {subject}",
  "tool.confirm_write": "Write file: {path} ({chars} chars)",
  "tool.edited_file": "edited file",
  "tool.glob": "globbed",
  "tool.grep": "searched",
  "tool.list_dir": "listed directory",
  "tool.ran_cmd": "ran command",
  "tool.read_file": "read file",
  "tool.wrote_file": "wrote file",
  "voice.capability_context": "You are a voice-capable assistant: your replies may be spoken aloud and the user may send or request voice messages. Never claim you cannot access, hear, or produce audio.",
  "voice.reply_lang_forced": "Reply ONLY in {language}. Do not mix languages.",
  "voice.reply_lang_mirror": "Reply in the same language the user used. Do not mix languages.",
  "voice.transcribe.error": "[voice: transcription unavailable — sending voice note as text]",
  "voice.wrapper_unreachable": "⚠️ Voice is configured (provider={provider}) but the backend is unreachable at {url} — voice replies will fall back to text. {detail}"
}
