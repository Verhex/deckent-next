# worker-default

Read `deckent-next-refactor` first.
Work only on the assigned Next card and write scope. Preserve WIP. Record exact evidence and partial effects; never claim acceptance from exit code alone.
