node_modules/
dist/
coverage/
apps/*/dist/
apps/*/node_modules/
# product state (per project, never committed)
.deckent/
.brain/
# product-generated host files (generated by `deckent init --host`, never committed)
DIRECTIVES.md
DECKENT.md
.claude/
.codex/
.cursor/
.gemini/
# native build artefacts (prebuilds are shipped via CI, not committed)
native/**/build/

# Owner-authorized shared development skill links; other host-local files remain ignored.
!.claude/
.claude/*
!.claude/skills
!.codex/
.codex/*
!.codex/skills

# Adapter-owned native build artifacts.
src/adapters/**/native/build/
